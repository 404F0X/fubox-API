# 其他切片记录

> 由 `docs/TODO.md` 拆分而来；只保存切片日志，不作为最终完成状态的唯一依据。

> 使用方式：开发组可以直接把本文件拆到 Jira/GitHub Issues。每个任务的完成必须满足 `TEST_AND_ACCEPTANCE.md` 中 Definition of Done。
---

- **E15-005-S2 ClickHouse Log Store，已完成/阻塞收束，2026-06-05，owner Singer**：bounded durable WAL dry-run/readback contract。原因：E15-005 已有 plan-only/WAL contract，但缺可执行 dry-run/readback 边界来证明 WAL path、segment、record、dedup 与 retention/load safety。范围：`apps/worker/src/main.rs`、`apps/worker/src/clickhouse_log_store.rs`、`crates/observability/src/clickhouse_log_store.rs`、`tests/fixtures/worker/clickhouse_log_store_plan_contract.json`、`tests/fixtures/observability/clickhouse_log_store_contract.json`。结果：新增 `--readback` dry-run CLI boundary 和 WAL dry-run/readback contract，覆盖 repo-local path safety、segment/record schema、record/readback hash、ack/retry/idempotency、dedup journal evidence、bounded disk budget 与 retention/load safety；默认仍不写 filesystem、不连 ClickHouse、不发网络，`--execute/--send --force` 继续拒绝执行。验证：Observability/Worker targeted tests、worker CLI dry-run/readback/execute-refusal、`cargo fmt --check`、diff check 通过。剩余缺口：真实 ClickHouse writer/insert path、database changefeed/export cursor、runtime WAL filesystem writer、dedup journal runtime persistence、load/retention runtime smoke。
- **E15-005-S3 ClickHouse WAL Runtime，已完成/阻塞收束，2026-06-05，owner Singer**：repo-bounded runtime WAL filesystem writer opt-in/readback contract。原因：S2 证明 dry-run/readback，但仍缺受控本地 writer/readback contract 来为 runtime WAL persistence 做准备。范围：`apps/worker/src/main.rs`、`apps/worker/src/clickhouse_log_store.rs`、`crates/observability/src/clickhouse_log_store.rs`、`tests/fixtures/worker/clickhouse_log_store_plan_contract.json`、`tests/fixtures/observability/clickhouse_log_store_contract.json`。结果：新增显式 `--write-artifact` opt-in CLI，只在 dry-run/readback 路径启用；默认 dry-run 和 `--readback` 不创建文件；opt-in 时仅允许 repo-local `.tmp/**` WAL path，经 safe path gate 写 `.jsonl.tmp` 后 rename 为 `.jsonl`，并写 `dedup-journal.jsonl`，随后 readback 输出 record count/hash match/ack-retry/dedup evidence；`--execute/--send` 仍拒绝 ClickHouse/network。验证：Worker/Observability targeted tests、worker CLI default/readback/write-artifact/execute-refusal、full fmt、diff check 通过。剩余缺口：真实 ClickHouse insert writer、DB changefeed/export cursor、production WAL root/service wiring、production dedup journal persistence、load/retention smoke。
- **E15-005-S4 ClickHouse WAL Service，已完成/阻塞收束，2026-06-05，owner Chandrasekhar**：production WAL root/service wiring config guard。原因：S3 允许 repo-bounded `.tmp` runtime artifact，但 production WAL service 还需要 config guard 区分 production root readiness 与 dev/test artifact path。范围：`apps/worker/src/main.rs`、`apps/worker/src/clickhouse_log_store.rs`、`crates/observability/src/clickhouse_log_store.rs`、`tests/fixtures/worker/clickhouse_log_store_plan_contract.json`、`tests/fixtures/observability/clickhouse_log_store_contract.json`。结果：`wal_service_guard` 新增 `production_service_readiness` 与 `runtime_artifact_path_contract`；Worker CLI 新增只读 `--check-config`/`--config-readback`；`.tmp` 作为 production WAL root 会 blocker，`.tmp` 仅在显式 `--write-artifact` 下作为 repo-bounded runtime artifact path 允许；默认仍不写生产目录、不连 ClickHouse、不发网络，`--execute --force` 仍拒绝。验证：Worker/Observability targeted tests、worker CLI config/readback/write-artifact/execute-refusal、full fmt、diff check 通过。剩余缺口：真实 ClickHouse writer、DB changefeed/export cursor、production WAL service execution、production dedup persistence、load/retention smoke。
- **E15-005-S5 ClickHouse WAL Service，已完成/阻塞收束，2026-06-05，owner Chandrasekhar**：production WAL service execution dry-run command skeleton 与 runtime service readiness artifact。原因：S4 固定 production WAL root/service config guard 后，需要可审计的 service execution skeleton 和 readiness artifact。结果：Worker 支持 `--service-readiness` 默认只读 readiness/skeleton；显式 `--write-service-readiness-artifact` 仅写 repo-bounded `.tmp` artifact 并 readback，仍不写 production WAL root、不连 ClickHouse、不发网络。验证：Worker/Observability targeted tests、worker CLI service-readiness/readiness-artifact checks、full fmt、diff check 通过。剩余缺口：真实 ClickHouse writer、DB changefeed/export cursor、production WAL service runtime loop/replay/send、production dedup persistence、load/retention smoke。
- **E15-005-S6 ClickHouse Final DoD，已完成/阻塞收束，2026-06-05，owner Chandrasekhar**：ClickHouse Log Store final DoD checklist/acceptance matrix。原因：E15 已有 WAL/service readiness，但最终 `[x]` 需要明确 production writer/changefeed/WAL/dedup/load-retention 的真实 evidence。结果：新增机器可读 final DoD checklist/acceptance matrix，覆盖 real ClickHouse writer、DB changefeed/export cursor、production WAL root writes、service loop/replay/send、dedup persistence、retention/load smoke、secret-safe evidence、no-network default/refusal；明确 readiness/artifact proof 只能推进到 production-ready，最终 `[x]` 仍需 live smoke/readback。Worker 支持 `--final-dod`/`--acceptance-matrix` 只读输出，默认仍不写生产目录、不连 ClickHouse、不发网络。验证：Worker/Observability targeted tests、worker CLI final-dod/service-readiness/execute-refusal checks、scoped fmt、diff check 通过。剩余缺口：真实 ClickHouse writer、DB changefeed/export cursor、production WAL writes、service loop/replay/send、dedup persistence、load/retention smoke。
- **E15-005-S7 ClickHouse Production Smoke Handoff，已完成/阻塞收束，2026-06-05，owner Chandrasekhar**：production smoke operator handoff contract。原因：S6 已定义 final DoD，但还缺真实 production smoke 的 operator handoff、artifact schema、preflight/refusal 分类。结果：新增 `production_smoke_handoff` machine-readable contract；Worker 支持 `--production-smoke-handoff` 只读输出，`--production-smoke --force` 当前仍安全拒绝；默认不连 ClickHouse、不写 production WAL root、不发网络。Contract 固定 production smoke command shape、preflight presence readback、artifact schema、readback acceptance、failure taxonomy、refusal boundaries，并明确 `production_smoke_ready` 仅表示可由 operator 尝试 live smoke，`final_x_eligible=false`。验证：Worker/Observability targeted tests、worker CLI handoff/refusal checks、full fmt、diff check 通过。剩余缺口：真实 ClickHouse writer、DB changefeed/export cursor、production WAL service loop/replay/send、dedup persistence、load/retention smoke。
- **E15-005-S8 ClickHouse Production Smoke Acceptance Matrix，已完成/阻塞收束，2026-06-05，owner Chandrasekhar**：production smoke artifact acceptance/readback gate。原因：S7 已有 handoff contract，但需要判断外部 operator artifact 是否完整、fresh、secret-safe。结果：新增 production smoke artifact acceptance gate；默认 `artifact_read=false`，不连 ClickHouse、不写 production WAL root、不发网络；显式 `--read-production-smoke-artifact <json>` 或 env 才读取 bounded artifact，只允许 repo-relative `.tmp/**` 或 `tests/fixtures/**` JSON。Accepted-shape simulation fixture 可被 `production_smoke_evidence_accepted_for_review=true`，但 `production_smoke_passed=false`、`final_x_eligible=false`、`simulation_can_mark_final_x=false`；refusal taxonomy 覆盖 missing/unsafe/stale/simulated、writer count 缺失或 mismatch、DB cursor 缺失、WAL replay/send/ack 缺失、dedup journal 缺失、retention/load 缺失、secret/raw DSN echo、network without opt-in。验证：Worker/Observability targeted tests、worker CLI handoff/readback/refusal checks、scoped fmt、diff check 通过。剩余缺口：真实 ClickHouse writer、DB changefeed/export cursor、production WAL service loop/replay/send/ack、dedup journal persistence、load/retention smoke。
- **E15-005-S9 ClickHouse Final Closure Audit，已完成/阻塞收束，2026-06-05，owner Chandrasekhar**：ClickHouse final closure audit/report。原因：E15 final DoD、handoff、acceptance gate 已在位，需要机器可读 closure audit 汇总多项 smoke evidence。结果：新增 `final_closure_audit` 顶层报告，输出 `final_x_eligible`、blocking reasons、required evidence checklist、writer/cursor/WAL/dedup/retention-load/secret-safe 状态、artifact provenance、counts 与 next commands；默认不读 artifact、不连 ClickHouse、不发网络、不写 production WAL root；accepted-shape simulation 可通过字段形状审阅，但 audit 仍 `final_x_eligible=false`，blocker 包含 `simulated_artifact` / `non_simulated_not_accepted`；production WAL root/绝对路径 artifact 读取被拒绝。验证：Worker/Observability targeted tests、CLI final audit default/simulation/refusal checks、cargo fmt、diff check 通过。剩余缺口：真实 ClickHouse writer、DB changefeed/export cursor、production WAL service loop/replay/send、dedup persistence、load/retention smoke。
- **E15-005-S10 Final Guard Consistency Review，已完成/阻塞收束，2026-06-05，owner Chandrasekhar**：E15 final guard consistency review。原因：E15 已进入等待真实 production smoke evidence 阶段，最后风险是 simulation/fixture/handoff/audit/no-artifact/refusal paths 被误关。结果：固定 final guard semantics：simulation、fixture、handoff、audit-without-real-artifact、refusal、watcher 都不能 mark final `[x]`；`final_closure_audit` 识别 `tests/fixtures` artifact scope 并加入 `fixture_artifact` blocker；统一 machine-readable flags `simulation_can_mark_final_x=false`、`fixture_can_mark_final_x=false`、`handoff_can_mark_final_x=false`、`audit_without_real_artifact_can_mark_final_x=false`、`refusal_can_mark_final_x=false`、`watcher_can_mark_final_x=false`。验证：rg guard checks、Worker/Observability targeted tests、CLI final audit/handoff/refusal checks、cargo fmt、diff check 通过。剩余缺口：真实 ClickHouse writer、DB changefeed/export cursor、production WAL service loop/replay/send、dedup persistence、load/retention smoke。
- **E15-005-S11 Production Smoke Evidence Watcher And Checklist，已完成/阻塞收束，2026-06-05，owner Chandrasekhar**：production smoke evidence watcher/checklist。原因：E15 已进入等待真实 production smoke evidence 阶段，需要稳定入口接收 writer/cursor/WAL/dedup/load-retention artifact。结果：新增 `production_smoke_evidence_watcher` 只读 watcher/checklist 与 CLI `--evidence-watcher` / `--watch-evidence`；默认不读 artifact、不连 ClickHouse、不发网络、不写 production WAL root；输出 expected artifact path、production WAL/dedup journal path、required fields、writer/cursor/WAL/dedup/load-retention checklist、operator actions 与 next commands；无 artifact 时 `final_x_eligible=false`，状态 `blocked_waiting_for_real_production_smoke_artifact`。验证：Worker/Observability targeted tests、CLI watcher/final audit/refusal checks、cargo fmt、diff check 通过。剩余缺口：真实 ClickHouse writer、DB changefeed/export cursor、production WAL service loop/replay/send、dedup persistence、load/retention smoke。
- **E15-005-S12 Production Smoke Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，no-op review**：production smoke evidence intake repeat。结果：未改文件；expected artifact directory `.tmp/clickhouse-log-store/production-smoke` 下无 production smoke artifact；watcher status `blocked_waiting_for_real_production_smoke_artifact`，final closure audit `final_x_eligible=false`、`artifact_read=false`。剩余缺口：真实 ClickHouse writer、DB changefeed/export cursor、production WAL service loop/replay/send、dedup persistence、load/retention smoke。
- **E15-005-S13 Production Smoke Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，no-op review**：production smoke evidence intake repeat。结果：未改文件；expected artifact directory `.tmp/clickhouse-log-store/production-smoke` 下无 production smoke artifact；watcher status `blocked_waiting_for_real_production_smoke_artifact`，final closure audit `final_x_eligible=false`、`artifact_read=false`。剩余缺口：真实 ClickHouse writer、DB changefeed/export cursor、production WAL service loop/replay/send、dedup persistence、load/retention smoke。
- **E15-005-S14 Production Smoke Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，no-op review**：production smoke evidence intake repeat。结果：未改文件；`.tmp/clickhouse-log-store/production-smoke` artifact missing；未读取 artifact 内容；watcher status `blocked_waiting_for_real_production_smoke_artifact`，`artifact_read=false`、`final_x_eligible=false`。剩余缺口：真实 ClickHouse writer、DB changefeed/export cursor、production WAL service loop/replay/send、dedup persistence、load/retention smoke。
- **E15-005-S15 Production Smoke Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，no-op review**：production smoke evidence intake repeat。结果：未改文件；`.tmp/clickhouse-log-store/production-smoke` artifact missing；未读取 artifact 内容；watcher status `blocked_waiting_for_real_production_smoke_artifact`，`artifact_read=false`、`final_x_eligible=false`。剩余缺口：真实 ClickHouse writer、DB changefeed/export cursor、production WAL service loop/replay/send、dedup persistence、load/retention smoke。
- **E15-005-S16 Production Smoke Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，no-op review**：production smoke evidence intake repeat。结果：未改文件；`.tmp/clickhouse-log-store/production-smoke` artifact missing；未读取 artifact 内容；watcher status `blocked_waiting_for_real_production_smoke_artifact`，`artifact_read=false`、`final_x_eligible=false`。剩余缺口：真实 ClickHouse writer、DB changefeed/export cursor、production WAL service loop/replay/send、dedup persistence、load/retention smoke。
- **E15-005-S17 Production Smoke Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，no-op review**：production smoke evidence intake repeat。结果：未改文件；`.tmp/clickhouse-log-store/production-smoke` artifact missing；未读取 artifact 内容；watcher status `blocked_waiting_for_real_production_smoke_artifact`，`artifact_read=false`、`final_x_eligible=false`。剩余缺口：真实 ClickHouse writer、DB changefeed/export cursor、production WAL service loop/replay/send、dedup persistence、load/retention smoke。
- **E15-005-S18 Production Smoke Closure Attempt，已改派，2026-06-05，owner Chandrasekhar**：主动运行/验证 ClickHouse production smoke handoff/final audit/readback path，定位真实 writer/cursor/WAL/dedup/load-retention smoke 是否可执行；优先使用既有 worker CLI/production smoke handoff/final audit，不新增 schema/contract，不连生产或发网络，除非 explicit safe opt-in 和 existing gates 允许；不得把 fixture/simulation 标 final。验收：回报实际命令、classification、artifact 状态、`artifact_read`、`production_smoke_passed`、`final_x_eligible`、缺失 ClickHouse/changefeed/WAL/dedup/load-retention 条件与 next exact command。
- **E15-005-S19 Post-Env ClickHouse Production Smoke Closure Reattempt，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，post-env review**：用户补环境后重新尝试 ClickHouse production smoke closure。实际命令：`target\debug\ai-worker.exe clickhouse-log-store --production-smoke-handoff --input tests\fixtures\worker\clickhouse_log_store_plan_contract.json`、`target\debug\ai-worker.exe clickhouse-log-store --evidence-watcher --input tests\fixtures\worker\clickhouse_log_store_plan_contract.json`、`target\debug\ai-worker.exe clickhouse-log-store --final-closure-audit --input tests\fixtures\worker\clickhouse_log_store_plan_contract.json`。结果：未改文件；当前 Codex 进程未看到 `CLICKHOUSE_URL`、`CLICKHOUSE_DATABASE`、`CLICKHOUSE_USER`、`CLICKHOUSE_PASSWORD` 或 production smoke artifact path env；`.tmp\clickhouse-log-store\production-smoke` 不存在，未读取 artifact；gate 输出 `artifact_read=false`、`production_smoke_passed=false`、`final_x_eligible=false`，blocking reasons 覆盖 writer/cursor/WAL/dedup/retention-load/fresh/non-simulated/secret-safe evidence missing。剩余缺口：真实 ClickHouse writer insert/readback、DB changefeed/export cursor、production WAL replay/send/ack、dedup journal persistence、retention/load smoke、secret-safe fresh non-simulated artifact。
- **E15-005-S20 ClickHouse Env/Artifact Visibility Reattempt，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，safe review**：按修正后的 env 名称重新检查 ClickHouse env/artifact visibility。实际命令：`Get-ChildItem Env:AI_GATEWAY_CLICKHOUSE_LOG_STORE_ENABLED,Env:AI_GATEWAY_CLICKHOUSE_ENDPOINT,Env:AI_GATEWAY_CLICKHOUSE_DATABASE,Env:AI_GATEWAY_CLICKHOUSE_USERNAME,Env:CLICKHOUSE_USER,Env:AI_GATEWAY_CLICKHOUSE_PASSWORD,Env:CLICKHOUSE_PASSWORD,Env:AI_GATEWAY_CLICKHOUSE_PRODUCTION_SMOKE_ARTIFACT_PATH,Env:AI_GATEWAY_CLICKHOUSE_LOG_STORE_SMOKE_ARTIFACT_PATH -ErrorAction SilentlyContinue`、`target\debug\ai-worker.exe clickhouse-log-store --evidence-watcher --input tests\fixtures\worker\clickhouse_log_store_plan_contract.json`、`target\debug\ai-worker.exe clickhouse-log-store --final-closure-audit --input tests\fixtures\worker\clickhouse_log_store_plan_contract.json`。结果：未改源码、未 build、未连生产、未发网络；当前进程未看到修正后的 ClickHouse env；`.tmp\clickhouse-log-store\production-smoke` 不存在；`artifact_read=false`、`production_smoke_passed=false`、`final_x_eligible=false`。剩余缺口：env 注入到 worker 进程、真实 production smoke artifact、writer/cursor/WAL/dedup/load-retention/fresh/non-simulated/secret-safe evidence。
- **E15-005-S21 ClickHouse Env Injection And Smoke Artifact Handoff，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，safe review**：复用 E15 上下文，显式检查 env presence 且不 echo secret。实际命令：presence-only PowerShell env check、`target\debug\ai-worker.exe clickhouse-log-store --evidence-watcher --input tests\fixtures\worker\clickhouse_log_store_plan_contract.json`、`target\debug\ai-worker.exe clickhouse-log-store --final-closure-audit --input tests\fixtures\worker\clickhouse_log_store_plan_contract.json`。结果：未改源码、未 build、未连生产、未发网络；当前进程仍未看到 `AI_GATEWAY_CLICKHOUSE_LOG_STORE_ENABLED=true`、`AI_GATEWAY_CLICKHOUSE_ENDPOINT`、`AI_GATEWAY_CLICKHOUSE_DATABASE`、`AI_GATEWAY_CLICKHOUSE_USERNAME`/`CLICKHOUSE_USER`、`AI_GATEWAY_CLICKHOUSE_PASSWORD`/`CLICKHOUSE_PASSWORD`、`AI_GATEWAY_CLICKHOUSE_PRODUCTION_SMOKE_ARTIFACT_PATH` 或 `AI_GATEWAY_CLICKHOUSE_LOG_STORE_SMOKE_ARTIFACT_PATH`；`.tmp\clickhouse-log-store\production-smoke` 不存在；`artifact_read=false`、`production_smoke_passed=false`、`final_x_eligible=false`。剩余缺口：把 env 注入当前 worker 进程、生成真实 repo-bounded production smoke artifact、通过 writer/cursor/WAL/dedup/load-retention/fresh/non-simulated/secret-safe readback。
- **E15-005-S22 ClickHouse Current-Process Env Export And Artifact Readback Handoff，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，safe review**：presence-only 检查 current shell/worker env，并按 artifact 状态选择 safe path。实际命令：presence-only PowerShell env check、`.tmp\clickhouse-log-store\production-smoke` artifact check、`target\debug\ai-worker.exe clickhouse-log-store --evidence-watcher --input tests\fixtures\worker\clickhouse_log_store_plan_contract.json`、`target\debug\ai-worker.exe clickhouse-log-store --final-closure-audit --input tests\fixtures\worker\clickhouse_log_store_plan_contract.json`。结果：未改源码、未 build、未联网；当前进程仍未看到 `AI_GATEWAY_CLICKHOUSE_LOG_STORE_ENABLED=true`、`AI_GATEWAY_CLICKHOUSE_ENDPOINT`、`AI_GATEWAY_CLICKHOUSE_DATABASE`、username/password、smoke artifact path env 或 live opt-in env；`.tmp\clickhouse-log-store\production-smoke` 不存在；`artifact_read=false`、`production_smoke_passed=false`、`final_x_eligible=false`。剩余缺口：same-session env injection 或 repo-bounded real smoke artifact，再由 final closure audit readback gate 验收。
- **E15-005-S23 Same-Session Env Injection Or Bounded Artifact Readback，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，safe review**：按两类证据门槛检查。结果：未改源码、未 build、未联网；A 类 same-session env/live opt-in 不成立，ClickHouse enabled/endpoint/database/username/password/artifact path/live opt-in env 均不可见；B 类 repo-bounded artifact 不成立，`.tmp\clickhouse-log-store\production-smoke` 不存在；未运行 artifact readback；仅运行 safe watcher/final audit，`artifact_read=false`、`production_smoke_passed=false`、`final_x_eligible=false`。剩余缺口：same-session env injection 或 repo-bounded real smoke artifact。
- **E15-005-S24 Same-Session Evidence Required Gate，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，evidence gate review**：停止重复空转式 closure attempt，只检查 A/B 两类新证据。结果：未输出 secrets、未改源码、未 build、未联网；A 类 same-session env presence 不成立，ClickHouse enabled/endpoint/database/username/password/smoke artifact path/live opt-in 均不可见；B 类 bounded artifact 不成立，`.tmp\clickhouse-log-store\production-smoke` 不存在；按 DoD 未重复跑 watcher/final audit。剩余缺口：same-session local ClickHouse env + live opt-in 或 repo-bounded real smoke artifact。
- **E15-005-S25 Minimal Real ClickHouse Smoke Artifact Command Handoff，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，operator handoff**：当前同一进程仍无 ClickHouse env/live opt-in，`.tmp\clickhouse-log-store\production-smoke` 不存在，未运行 artifact readback、未联网、未 build、未改源码、未输出 secret。已给 operator 最短 same-session handoff：`$runId=Get-Date -Format 'yyyyMMdd-HHmmss'; $artifact=".tmp\clickhouse-log-store\production-smoke\$runId.json"; New-Item -ItemType Directory -Force .tmp\clickhouse-log-store\production-smoke | Out-Null; $env:AI_GATEWAY_CLICKHOUSE_LOG_STORE_ENABLED='true'; $env:AI_GATEWAY_CLICKHOUSE_ENDPOINT='http://127.0.0.1:8123'; $env:AI_GATEWAY_CLICKHOUSE_DATABASE='<database>'; $env:AI_GATEWAY_CLICKHOUSE_USERNAME='<username>'; $env:AI_GATEWAY_CLICKHOUSE_PASSWORD='<password>'; $env:AI_GATEWAY_CLICKHOUSE_LOG_STORE_SMOKE_ARTIFACT_PATH=$artifact; $env:AI_GATEWAY_CLICKHOUSE_LOG_STORE_LIVE_OPT_IN='true'; target\debug\ai-worker.exe clickhouse-log-store --execute --production-smoke --force --input tests\fixtures\worker\clickhouse_log_store_plan_contract.json --artifact $artifact; target\debug\ai-worker.exe clickhouse-log-store --final-closure-audit --read-production-smoke-artifact $artifact --input tests\fixtures\worker\clickhouse_log_store_plan_contract.json`。该命令只应在本地 ClickHouse endpoint 与明确 live opt-in 成立时运行。E15 属 Production closure/P1，不阻塞 Local P0。
- **E15-005-S26 Operator-Produced ClickHouse Smoke Artifact Readback，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，light watcher**：轻量只读 watcher；`.tmp\clickhouse-log-store\production-smoke` 不存在，未发现 operator-produced `.json` artifact；按 DoD 未运行 worker final-closure-audit、未联网、未 build、未改源码、未输出 secret。missing evidence：`.tmp\clickhouse-log-store\production-smoke\<run_id>.json`。最短 operator command 仍为 S25 same-session handoff 命令；E15 属 Production/P1，不阻塞 Local P0。
- **E15-005-S27 Operator-Produced Artifact Intake Watcher，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，light watcher**：轻量只读 watcher；`.tmp\clickhouse-log-store\production-smoke` 不存在，未发现 operator-produced `.json` artifact；按 DoD 未运行 worker final-closure-audit、未联网、未 build、未改源码、未输出 secret。missing evidence：`.tmp\clickhouse-log-store\production-smoke\<run_id>.json`。最短 operator command 仍为 S25 same-session handoff 命令；E15 属 Production/P1，不阻塞 Local P0。
- **E15-005-S28 Operator-Produced Artifact Intake Watcher，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，light watcher**：轻量只读 watcher；`.tmp\clickhouse-log-store\production-smoke` 不存在，未发现 operator-produced `.json` artifact；按 DoD 未运行 worker final-closure-audit、未联网、未 build、未改源码、未输出 secret。missing evidence：`.tmp\clickhouse-log-store\production-smoke\<run_id>.json`。最短 operator command 仍为 S25 same-session handoff 命令；fixture/simulation/handoff/watchers 不可标 final；E15 属 Production/P1，不阻塞 Local P0。
- **E15-005-S29 Production Smoke Artifact Minimal Delta Watcher，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，minimal delta**：相对 S28 无新增真实 ClickHouse production smoke artifact；`.tmp\clickhouse-log-store\production-smoke` 仍不存在，未运行 final-closure-audit、未联网、未 build、未改源码、未输出 secret。fresh delta：no production smoke artifact；blockers unchanged；operator action reference：S25 same-session handoff。fixture/simulation/handoff/watchers 不可标 final；E15 属 Production/P1，不阻塞 Local P0。
- **E15-005-S30 Production Smoke Artifact Minimal Delta Watcher，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，minimal delta**：相对 S29 无新增真实 ClickHouse production smoke artifact；`.tmp\clickhouse-log-store\production-smoke` 仍不存在，未运行 final-closure-audit、未联网、未 build、未改源码、未输出 secret。fresh delta：no production smoke artifact; blockers unchanged; operator action reference S25。fixture/simulation/handoff/watchers 不可标 final；E15 属 Production/P1，不阻塞 Local P0。
- **E15-005-S31 Production Smoke Artifact Minimal Delta Watcher，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，minimal delta**：相对 S30 无新增真实 ClickHouse production smoke artifact；`.tmp\clickhouse-log-store\production-smoke` 仍不存在，未运行 final-closure-audit、未联网、未 build、未改源码、未输出 secret。fresh delta：no production smoke artifact; blockers unchanged; operator action reference S25。fixture/simulation/handoff/watchers 不可标 final；E15 属 Production/P1，不阻塞 Local P0。
- **E15-005-S32 Production Smoke Artifact Minimal Delta Watcher，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，minimal delta**：相对 S31 无新增真实 ClickHouse production smoke artifact；`.tmp\clickhouse-log-store\production-smoke` 仍不存在，未运行 final-closure-audit、未联网、未 build、未改源码、未输出 secret。fresh delta：no production smoke artifact; blockers unchanged; operator action reference S25。fixture/simulation/handoff/watchers 不可标 final；E15 属 Production/P1，不阻塞 Local P0。
- **E15-005-S33 Production Smoke Artifact Minimal Delta Watcher，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，minimal delta**：相对 S32 无新增真实 ClickHouse production smoke artifact；`.tmp\clickhouse-log-store\production-smoke` 仍不存在，未运行 final-closure-audit、未联网、未 build、未改源码、未输出 secret。fresh delta：no production smoke artifact; blockers unchanged; operator action reference S25。fixture/simulation/handoff/watchers 不可标 final；E15 属 Production/P1，不阻塞 Local P0。
- **E15-005-S34 Production Smoke Artifact Minimal Delta Watcher，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，minimal delta**：相对 S33 无新增真实 ClickHouse production smoke artifact；`.tmp\clickhouse-log-store\production-smoke` 仍不存在，未运行 final-closure-audit、未联网、未 build、未改源码、未输出 secret。fresh delta：no production smoke artifact; blockers unchanged; operator action reference S25。fixture/simulation/handoff/watchers 不可标 final；E15 属 Production/P1，不阻塞 Local P0。
- **E15-005-S35 Production Smoke Artifact Minimal Delta Watcher，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，minimal delta**：相对 S34 无新增真实 ClickHouse production smoke artifact；`.tmp\clickhouse-log-store\production-smoke` 仍不存在，未运行 final-closure-audit、未联网、未 build、未改源码、未输出 secret。fresh delta：no production smoke artifact; blockers unchanged; operator action reference S25。fixture/simulation/handoff/watchers 不可标 final；E15 属 Production/P1，不阻塞 Local P0。
- **E15-005-S36 Production Smoke Artifact Minimal Delta Watcher，已完成/阻塞收束，2026-06-05，owner Chandrasekhar，minimal delta**：相对 S35 无新增真实 ClickHouse production smoke artifact；`.tmp\clickhouse-log-store\production-smoke` 仍不存在，未运行 final-closure-audit、未联网、未 build、未改源码、未输出 secret。fresh delta：no production smoke artifact; blockers unchanged; operator action reference S25。fixture/simulation/handoff/watchers 不可标 final；E15 属 Production/P1，不阻塞 Local P0。
- **E15-005-S37 Production Smoke Artifact Minimal Delta Watcher，已完成/历史派发，已被 S38 取代，2026-06-05，owner Chandrasekhar**：只检查相对 S36 是否出现新的 `.tmp\clickhouse-log-store\production-smoke/*.json` artifact；不输出 secrets、不改源码、不 build、不联网、不主动执行 production smoke。若缺失，仅写 fresh delta `no production smoke artifact; blockers unchanged; operator action reference S25`，不重复 S25 长命令、不运行 final-closure-audit；若 artifact 存在，运行 `target\debug\ai-worker.exe clickhouse-log-store --final-closure-audit --read-production-smoke-artifact <artifact> --input tests\fixtures\worker\clickhouse_log_store_plan_contract.json` 并报告 closure fields 与 `final_x_eligible`。S38 已改为非重复 path recovery 并确认 compose/env/worker hard-refuse 真实缺口；本条不再是 active 派发。不得把 fixture/simulation/handoff/watchers 标 final；E15 属 Production/P1，不阻塞 Local P0。
- **MISC-S29 Horizontal Release Readiness Progress Audit，已完成/横向审计收束，2026-06-05，owner Parfit/E11 lane**：E11 功能已 final closure，改派该 lane 做横向进度审计。结果：只读核对 `docs/TODO.md` 当前进度条、Local P0 vs Production/P1 分类、E8/E9/E13/E15 剩余 blockers、artifact/env evidence 与 split logs；未改源码、未 build、未 docker compose、未运行 live browser mutation、paid commit、production cutover 或 ClickHouse smoke。E11 final evidence 仍支持 100%：`artifacts/control_plane_ledger_execute_runtime_current_handoff.json` 为 `runtime_current_readback_verified`，`artifacts/billing_execute_browser_live_e2e_evidence.json` 为 browser live e2e `outcome=passed`、`mutation_pass_artifact_passed`、`artifact_readback_passed`、`closure_eligible=true`。E8 不能 final：当前进程缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`，`.tmp/gateway_tpm_production_backend/e8-s103-live-evidence.json` 不存在；该缺口属于 Production closure/P1，不阻塞 Local P0。E9 不能 final：S88/S89 仍显示 live paid commit 授权/env/runtime markers 缺失，现有 `.tmp/billing-ledger/live-commit-proof-artifact.json` 只是 `script_template_no_commit` blocker，cutover/live-probe artifacts 缺失；不得执行 commit/cutover。E13 不能 final：`.tmp/prompt_protection_runtime_redeploy_evidence_accepted.json` 不存在，template 存在但 `template_can_pass=false`。E15 不能 final：`.tmp/clickhouse-log-store/production-smoke` 不存在，未发现真实 production smoke JSON artifact。文档更新：`docs/TODO.md` 将 E11 Local P0 blocker 改为 final closed，并将横向高性能验收从 `98.2%` 调整为 `98.0%`，明确不应因 E11 关闭而上调；剩余卡点均为 Production closure/P1 evidence。
- **MISC-S30 Active `[~]` Hygiene Audit，已完成/横向审计收束，2026-06-05，owner 横向 reviewer lane**：只审计 `docs/TODO.md` 中 `[~]` 状态和进度条说明；未改源码、未 build、未运行 live checks，未改 E8/E9/E13 split log。状态规则已收紧为：`[ ]` 未开始，`[~]` 只代表当前活跃派发/等待 Review/等待外部 evidence 的切片，`[x]` 代表已完成或已阻塞收束并通过主线程 Review；父级 Epic/最终验收目标不靠 `[~]` 承载进度，进度条文字单独说明 final evidence 缺口。审计前 `docs/TODO.md` 除顶部规则文字外有 6 个切片级 `[~]`：E9 S87、E9 S91、E8 S105、E13 S81、E13 S83、E15 S30。修正：E9 S87 已由 S88/S89/S90/S91 接续，改为 `[x]` stale watcher；E13 S81 已由 S82/S83/S84 接续，改为 `[x]` stale watcher；复查时 E13 S83 已收束为 `[x]`。最后一次统计的切片级 active `[~]` 为 4 个：E8 S106、E9 S92、E13 S85、E15 S31，均有 owner/DoD/blocker/下一步 evidence。与用户给定名单的差异：当前 TODO 状态显示 E8 S104/S105 已完成、E8 S106 活跃；E9 S90/S91 已完成、E9 S92 活跃；E13 S82/S83/S84 已完成、E13 S85 活跃；E15 S29/S30 已完成、E15 S31 活跃。因写入范围限制，未直接修改 E8/E9/E13 split log，只在 TODO/MISC 记录该差异供主线程处理。
- **MISC-S31 Release Blocker Matrix And Operator Action Audit，已完成/横向审计收束，2026-06-05，owner 横向 reviewer lane**：只审计 `docs/TODO.md` 顶部进度条、剩余 final evidence 与最新 active watcher；未改源码、未 build、未运行 live checks，未执行 paid commit/cutover/ClickHouse smoke。最后一次统计的 active watcher 口径为 E8 S108、E9 S94、E13 S87、E15 S33。结果：已在 `docs/TODO.md` 顶部进度条后新增 release blocker matrix，并保持 E11 100%、E8 99.85%、E9 99.82%、E13 99.96%、横向 98.0% 不变。矩阵分类：E8 缺 production backend runner/Gateway live smoke/evidence artifact/DB acquire readback，属于 Production/P1 closure，不阻塞 Local P0，operator action reference S104/S105/S106，S108 只 watch；E9 缺 live paid commit authorization、live DB/runtime markers、accepted commit proof、cutover evidence、post-cutover readback、rollback proof，属于 paid Beta blocker + Production/P1 cutover closure，需要用户显式授权，operator action reference S68 wrapper + S88/S89 audit，S94 只 watch；E13 缺 accepted redeploy artifact，属于 Production/RC closure，不阻塞 Local P0，主要需要 artifact drop/readback，operator action reference S82-S85，S87 只 watch；E15 缺 production smoke JSON 与 ClickHouse writer/cursor/WAL/dedup/load-retention proof，属于 Production/P1 closure，不阻塞 Local P0/Beta，需要 live opt-in/外部 ClickHouse env 才执行 smoke，operator action reference S25，S33 只 watch。审计结论：operator action references 足够具体，无需新增长命令；除 E9 授权/cutover 与 E15 live opt-in 外，其余主要是 bounded artifact drop/readback。无证据支持百分比上调。
- **MISC-S32 User-Actionable Closure Checklist Audit，已完成/横向审计收束，2026-06-05，owner 横向 reviewer lane**：只审计 `docs/TODO.md` release blocker matrix 与剩余 final evidence；未改源码、未 build、未运行 live checks，未执行 paid commit/cutover/ClickHouse smoke。结果：MISC-S31 matrix 基本 sufficient，但“谁提供/风险/readback/Local P0-Beta 影响”不够一眼可执行，已在 `docs/TODO.md` 增加 `User-actionable closure checklist`。短清单：E8 由 operator 提供真实 backend runner + Gateway live smoke evidence artifact，按 S104-S107 handoff，收到后只做 bounded readback/verifier，无收费风险，不阻塞 Local P0；E9 是唯一明确 paid authorization blocker，用户必须显式授权 live paid commit/cutover 并提供 live DB/runtime markers 与 accepted artifacts，按 S68 + S88/S89，收到后只读 readiness/final audit，未授权绝不执行，挡付费 Beta；E13 由 operator 用真实 runtime markers 填 accepted redeploy artifact，按 S82-S85，收到后 readback/proof verifier，无收费风险，不阻塞 Local P0；E15 需要 ClickHouse live opt-in/外部 env 后生成 production smoke JSON，按 S25，收到后只跑 final-closure-audit readback，不阻塞 Local P0/Beta。结论：checklist sufficient，operator action references 足够具体，无需写长命令；没有新 evidence，百分比保持不变。
- **MISC-S33 Active Watcher Number Consistency Audit，已完成/横向审计收束，2026-06-05，owner 横向 reviewer lane**：只核对 `docs/TODO.md` 顶部 release blocker matrix、User-actionable checklist 与底部 active `[~]` watcher 编号；未改源码、未 build、未运行 live checks。最终核对 active watcher：E8 S110、E9 S96、E13 S89、E15 S35。修正：matrix 中 E8 从 S108 更新为 S110，operator reference 从 S104/S105/S106 改为 S104-S108；matrix 中 E13 从 S87 更新为 S89，operator reference 从 S82-S85 改为 S82-S87；User-actionable checklist 中 E8 handoff 从 S104-S107 改为 S104-S108，E13 handoff 从 S82-S85 改为 S82-S87。E9 S96 与 E15 S35 已和 matrix 对齐，无需改。没有新 evidence，不改变 E11 100% 或任何百分比。
- **MISC-S34 Release Readiness Snapshot Minimal Audit，已完成/横向审计收束，2026-06-05，owner 横向 reviewer lane**：轻量 snapshot，只核对 `docs/TODO.md` 顶部进度条、release blocker matrix 与 User-actionable checklist；未改源码、未 build、未运行 live checks，未执行 paid commit/cutover/ClickHouse smoke。当前 active watcher：E8 S112、E9 S98、E13 S91、E15 S36。结论：snapshot sufficient；Local P0/Beta 与 Production/P1 的用户可读摘要仍清楚，E9 仍是唯一明确 paid authorization blocker，E15 仍需要 ClickHouse live opt-in，E8/E13 仍主要是 artifact/runtime marker drop + readback。无新 evidence 支持百分比变化，E11 100%、E8 99.85%、E9 99.82%、E13 99.96%、横向 98.0% 保持不变。短修正：matrix/User-actionable 中 E8 从 S110/S104-S108 更新到 S112/S104-S110，E13 从 S89/S82-S87 更新到 S91/S82-S89；E9 S98 与 E15 S36 已对齐。
- **MISC-S35 Active Lane Health And No-Op Guard Audit，已完成/no-op guard，2026-06-05，owner 横向 reviewer lane**：轻量健康检查，只读 `docs/TODO.md` active watcher、release blocker matrix 与 User-actionable checklist；未改源码、未 build、未运行 live checks，未执行 paid commit/cutover/ClickHouse smoke。当前 active watcher：E8 S112、E9 S98、E13 S91、E15 S36。Presence-only artifact check：`.tmp/gateway_tpm_production_backend/e8-s103-live-evidence.json` missing，`.tmp/gateway_tpm_production_backend/e8-s112-live-evidence.json` missing，`.tmp/billing-ledger/cutover-evidence-artifact.json` missing，`.tmp/billing-ledger/live-probe-evidence-artifact.json` missing，`.tmp/prompt_protection_runtime_redeploy_evidence_accepted.json` missing，`.tmp/clickhouse-log-store/production-smoke` missing。结论：`no_new_evidence_no_percentage_change`；matrix/checklist 已同步且 sufficient，E9 仍唯一 paid authorization blocker，E15 仍需 ClickHouse live opt-in，E8/E13 仍为 artifact/runtime marker handoff。建议业务 watcher 下一轮仅在 evidence/授权/artifact 状态变化时详细写入，否则保持 minimal delta，避免继续刷屏；E11 100%、E8 99.85%、E9 99.82%、E13 99.96%、横向 98.0% 均不变。
- **MISC-S35 Active Lane Health And No-Op Guard Audit，已完成/见上一条收束，2026-06-05，owner 横向 reviewer lane**：派发记录已由上一条 MISC-S35 完成记录收束；无新增 evidence、无百分比调整。
- **MISC-S36 No-New-Evidence Stop-Repeat-Watcher Policy，已完成/横向策略收束，2026-06-05，owner 横向 reviewer lane**：只更新 `docs/TODO.md` 和本文件；未改源码、未 build、未运行 live checks，未执行 paid commit/cutover/ClickHouse smoke。当前 lane 状态：E8 已收束到 S113 且无 active `[~]`，E9 active S99，E13 active S92，E15 active S37。结果：已在 TODO 顶部补 `No-new-evidence stop-repeat policy`，明确同一 lane 连续 no-new-evidence/minimal-delta watcher 收束后，不再自动派同类 Sxx+1 watcher；只有 artifact/env/authorization 状态变化时才恢复 evidence intake。允许继续派发的非重复任务类型固定为 evidence acceptance preflight review、operator action checklist audit、security/no-secret audit、readback command dry-run contract review、release readiness snapshot。短修正：matrix/checklist 中 E8 对齐 S113/S104-S112，E13 对齐 S92/S82-S90；E9 S99 与 E15 S37 已对齐。无新 evidence，不建议调整 E11 100%、E8 99.85%、E9 99.82%、E13 99.96% 或横向 98.0%。后续 E8/E9/E13/E15 当前 watcher 收束后应停止重复派发同类 no-new-evidence watcher，除非真实 artifact/env/授权状态变化。
- **MISC-S37 Release Blocker Reconciliation After S101/S94，已完成/横向一致性收束，2026-06-05，owner Locke/横向 reviewer lane**：只审计并更新 `docs/TODO.md` 与本文件；未改源码、未 build、未运行 live checks，未执行 paid commit/cutover/ClickHouse smoke，也未直接修改 E8/E9/E13/E15 split log。结果：顶部 progress bar、release blocker matrix 与 User-actionable checklist 已基本对齐 S101/S94，但 Local P0/Beta 口径仍残留旧 blocker；已修正为 E9 S101 live runtime writer commit/readback、single-writer/no-dual 与 rollback-only proof pass，不再是 env/authorization/live commit artifact blocker，剩余真实卡点为 actual source-of-truth cutover evidence artifact、post-cutover readback、cutover rollback/source-of-truth/no-dual proof；E13 S94 accepted redeploy artifact/readback、runtime-owned/current rows、Admin UI/API readback 与 final gate 已 pass，可保持 100%/final-x eligible，不再保留 accepted artifact missing blocker。当前仍需推进的真实 release blockers：E8 production tokenizer/read-model backend runner + Gateway live smoke + DB acquire/readback evidence；E9 production/source-of-truth cutover closure；E15 real ClickHouse production smoke artifact/writer/cursor/WAL/dedup/load-retention proof。过期记录仅保留在历史 `[x]` slice 中：E9 S88-S99 的 env/authorization watcher 结论与 E13 S49-S92 accepted artifact missing 结论已被 S101/S94 取代，主线程不应再据此派重复 watcher。进度建议：保持 E8 99.85%、E9 99.93%、E11 100%、E13 100%、横向 98.6%；若主线程按 release blocker 数量重新估算，仍不应推进到 100%，因为 E8/E9/E15 production closure artifacts 未闭合。
- **E15-005-S38 ClickHouse Production Smoke Path Recovery，已完成/阻塞收束，2026-06-05，owner Boole，path recovery**：目标不是继续 S37 artifact watcher，而是检查本地是否已有 ClickHouse compose/env/worker binary 可执行 production smoke。实际命令：`docker info --format '{{.ServerVersion}}'` -> Docker server 可用；`docker compose -f deploy\docker-compose\docker-compose.yml config --services` -> services 只有 postgres/redis/control-plane/mock-provider/gateway/worker/admin-ui，无 ClickHouse；`Get-ChildItem Env:AI_GATEWAY_CLICKHOUSE*,Env:CLICKHOUSE*` 与 presence-only allowlist 检查 -> same-session ClickHouse endpoint/database/user/password/WAL/live-opt-in/artifact path env 均不可见，未输出任何 secret 值；`Get-Item target\debug\ai-worker.exe` -> worker binary 存在；`target\debug\ai-worker.exe clickhouse-log-store --production-smoke --force --input tests\fixtures\worker\clickhouse_log_store_plan_contract.json` -> hard refusal，`execute/send is not implemented... no DB read, queue write, WAL/file write, ClickHouse write, or network request was sent`；`target\debug\ai-worker.exe clickhouse-log-store --check-config --input tests\fixtures\worker\clickhouse_log_store_plan_contract.json` -> fixture-backed config gate 可读但 `writer_supported=false`、`execute_supported=false`、`send_supported=false`、`clickhouse_network_requests_enabled=false`；`target\debug\ai-worker.exe clickhouse-log-store --final-closure-audit --input tests\fixtures\worker\clickhouse_log_store_plan_contract.json` -> only read-only audit, `artifact_read=false`、`production_smoke_passed=false`、`final_x_eligible=false`、writer/db_cursor/WAL/dedup/retention-load states all `missing`、blocking reasons count 10。结果：未改源码、未 build、未启动 compose、未联网到未知生产地址、未写 production WAL、未生成或读取 production smoke artifact、未输出 secrets。非重复 blocker：本地有 Docker 和 worker binary，但没有 ClickHouse compose service、没有 same-session live env/opt-in、没有 implemented live smoke writer path；现有 fixture/env contract 只能证明 secret-safe readiness shape，不能执行真实 smoke，也不能 final。最短环境差：新增或提供本地安全 ClickHouse service（例如只绑定 127.0.0.1 的 compose override）、same-session presence-only env/live opt-in、repo-bounded `.tmp/clickhouse-log-store/production-smoke/<run_id>.json` artifact path，以及实现/启用 worker bounded live writer/cursor/WAL/dedup/load-retention producer；或者由 operator 放入非模拟、fresh、secret-safe real artifact 后只跑 final-closure-audit readback。fixture/simulation/handoff/watchers 仍不可标 final；E15 属 Production/P1，不阻塞 Local P0/Beta。
- **MISC-S38 Historical Active Marker Cleanup Audit，已完成/active-marker hygiene 收束，2026-06-05，owner Locke/横向 reviewer lane**：只更新 `docs/TODO.md` 与本文件；未改源码、未 build、未运行 live checks，未执行 paid commit/cutover/ClickHouse smoke。审计前 `rg "\[~\]" docs/TODO.md` 命中 10 行，其中 5 行为当前新派发 E8 S115、E9 S103、E15 S39、MISC S38、E13 S96，2 行为历史残留 E9 S100/E15 S37，3 行为顶部/历史说明文字。修正：E9 S100 改为 `[x]` 并标注“历史派发/已被 S101/S102 取代”，说明 S101 已完成 live runtime writer commit/readback 与 rollback-only proof，S102 已推进 cutover evidence producer boundary；E15 S37 改为 `[x]` 并标注“历史派发/已被 S38 取代”，说明 S38 已改为非重复 path recovery 并确认 compose/env/worker hard-refuse 真实缺口；顶部状态规则和历史横向说明去掉字面 active marker，避免 `rg` 把说明文字误报为活跃任务。复查：`rg "\[~\]" docs/TODO.md` 只剩当前 5 个新派发。顶部 progress/matrix 已对齐 E9 S102、E15 S38、E13 100%，未发现需要百分比调整；建议保持 E8 99.85%、E9 99.94%、E11 100%、E13 100%、横向 98.7%。真实剩余卡点仍为 E8 production backend/live smoke/DB acquire evidence、E9 external cutover command + canonical cutover artifact/post-readback/rollback proof、E15 ClickHouse live smoke writer/artifact。
- **MISC-S39 Full-Permission Execution Policy Correction，已完成/横向策略收束，2026-06-05，owner Locke/横向 reviewer lane**：只更新 `docs/TODO.md` 与本文件；未改源码、未 build、未运行 live checks，未执行 paid commit/cutover/ClickHouse smoke。结果：已在 TODO 顶部加入 full-permission execution policy，明确用户已给本地执行/实现/验证全权限，后续不得把“等待授权/权限”当 blocker；真实 blocker 只能归类为代码实现缺口、本地可执行但尚未执行、真实外部 production 系统或 artifact 不存在/不可伪造。同步修正 progress/matrix/checklist：E8 当前 blocker 是 local trusted runner implementation/evidence 待完成 + 真实 production backend/live smoke artifact 不存在，下一步 E8-S115；E9 当前 blocker 是 local-dev cutover command contract/evidence 待完成 + production source-of-truth cutover artifact 不可伪造，下一步 E9-S103；E15 当前 blocker 是 dev-only ClickHouse compose/wire-up 或 worker local smoke writer 实现缺口 + production smoke artifact 不存在，下一步 E15-S39。下一轮不空转派发建议：E8-S115 交付 local trusted tokenizer/read-model runner prototype、local artifact、verifier/readback 和 final=false 分类；E9-S103 交付 local-dev cutover command contract、dev-scoped source-of-truth switch/readback artifact、rollback/no-dual proof 和 production gap；E15-S39 交付 dev-only ClickHouse compose/wire-up 或 worker local smoke stub、bounded local smoke artifact/readback 和 final=false 分类。未调整百分比；建议保持 E8 99.85%、E9 99.94%、E11 100%、E13 100%、横向 98.7%。
- **MISC-S40 Paid/Production Closure Red-Team Review，已完成/红队边界审计收束，2026-06-05，owner Gauss/横向 reviewer lane**：只读审计 E8-S115、E9-S103、E15-S39 active task 文案与相关 scripts/runbooks；未运行 E13 proof，未改 Gateway runtime，未执行 paid cutover、production backend、ClickHouse smoke 或 docker compose。审计结论：用户全权限解除的是本地执行/实现权限，不会把不存在的外部 production evidence 变成可造证据。E8 safe execution guard：可运行 `scripts/run_gateway_tpm_production_backend_evidence.ps1 -RunLocalPrototype`、contract-only verifier、repo-bounded `.tmp/gateway_tpm_production_backend/**` readback；必须保持 `local_prototype=true`、`real_operator_evidence=false`、`gateway_live_smoke_status=local_prototype_not_run`、`db_acquire_readback_state=missing`、`final_x_eligible=false`。E8 forbidden shortcut：把 local fixture/prototype、simulation、missing Gateway live smoke 或 missing DB acquire/readback 标成 production final。E9 safe execution guard：可运行 local-dev cutover command contract，要求 `-ExecuteSourceOfTruthSwitch` + `-AcknowledgeLocalDevOnly` + `-AcknowledgeRollbackPlan` + S101 artifacts/readback pass，且只写 `.tmp/billing-ledger/cutover-evidence-artifact.local-dev.json`；verifier 已用 `environment_scope=local_dev` 加 blocker `local_dev_cutover_artifact_not_production_final`。E9 forbidden shortcut：把 local-dev artifact、S101 local commit proof 或 external command missing 状态伪装成 canonical `.tmp/billing-ledger/cutover-evidence-artifact.json` production final；真正 final 必须有 external command 实际 source-of-truth switch、post-cutover active-writer/source-of-truth/no-dual readback 与 rollback proof。E15 safe execution guard：可做 compose/config/worker feasibility、local compose/dev-only prototype、`--check-config`、`--production-smoke-handoff`、`--final-closure-audit`、repo-bounded artifact readback；默认不连外部 ClickHouse、不写 production WAL、不发网络，fixture/simulation/handoff 均 `final_x_eligible=false`。E15 forbidden shortcut：把 local smoke stub、fixture accepted-for-review、no-artifact watcher、hard-refuse output 或 missing writer/cursor/WAL/dedup/load-retention evidence 标为 production smoke passed。文档修正：`docs/TODO.md` 中 E8-S115 已从 active `[~]` 改为 `[x]`，避免后续 Agent 重复执行已完成 prototype；MISC-S40 改为 `[x]`。风险等级：E9=High（付费/source-of-truth cutover，可能产生收费/账务状态风险）；E8=Medium（路由/限流 production evidence 伪造会造成容量与滥用防护误判）；E15=Medium（observability/data retention production smoke 伪造会掩盖日志丢失风险）。总体建议：继续推进 E9/E15 implementation/prototype 时必须保留 environment/scope/final guard 字段；任何 production final 只能来自真实 external system/artifact + readback，不从 local prototype 自动升级。
- **E15-005-S39 ClickHouse Local Compose/Wire-Up Feasibility，已完成/local prototype 收束，2026-06-05，owner Boole**：按用户扩大 DoD，不停在“缺 ClickHouse 环境/权限”。检查结果：`deploy/docker-compose/docker-compose.yml` 原本无 ClickHouse service；observability config 已有 `AI_GATEWAY_CLICKHOUSE_*` feature/env contract；worker CLI 生产 `--production-smoke --force` 仍 hard-refuse，真实 writer/send 未实现。实现：新增 dev-only `deploy/docker-compose/docker-compose.clickhouse.local.yml`，ClickHouse service 只在显式 `--profile clickhouse` 下启用，host HTTP 端口绑定 `127.0.0.1:${CLICKHOUSE_HOST_PORT:-8123}`；新增 `tests/fixtures/worker/clickhouse_log_store_local_compose_contract.json`，endpoint 仅为 `http://clickhouse:8123`；worker 新增 `--local-smoke-dry-run` 和 `--write-local-smoke-artifact`，只产 repo-local `.tmp/clickhouse-log-store/local-smoke/tenant-00000000-0000-0000-0000-000000000001.json` prototype artifact，默认不连外部生产、不发网络、不写 production WAL、不输出 secret。验证命令：`docker compose -f deploy\docker-compose\docker-compose.yml -f deploy\docker-compose\docker-compose.clickhouse.local.yml --profile clickhouse config --services` -> 包含 `clickhouse`；`cargo check -p ai-worker` pass；`cargo test -p ai-worker clickhouse_log_store -- --nocapture` -> 32 pass；`cargo run -q -p ai-worker -- clickhouse-log-store --check-config --input tests\fixtures\worker\clickhouse_log_store_local_compose_contract.json` -> `writer_supported=false`、`execute_supported=false`、`send_supported=false`、`local_wire_ready=true`、`artifact_read=false`、`production_smoke_passed=false`、`final_x_eligible=false`；`cargo run -q -p ai-worker -- clickhouse-log-store --local-smoke-dry-run --input tests\fixtures\worker\clickhouse_log_store_local_compose_contract.json` -> mode `local_smoke_dry_run`、`local_wire_ready=true`、`writer_supported=false`、`execute_supported=false`、`artifact_read=false`、`production_smoke_passed=false`、`final_x_eligible=false`; `cargo run -q -p ai-worker -- clickhouse-log-store --write-local-smoke-artifact --input tests\fixtures\worker\clickhouse_log_store_local_compose_contract.json` -> mode `local_smoke_artifact`、artifact written/readback true、path `.tmp\clickhouse-log-store\local-smoke\tenant-00000000-0000-0000-0000-000000000001.json`、`production_smoke_passed=false`、`final_x_eligible=false`; `cargo run -q -p ai-worker -- clickhouse-log-store --final-closure-audit --read-production-smoke-artifact .tmp\clickhouse-log-store\local-smoke\tenant-00000000-0000-0000-0000-000000000001.json --input tests\fixtures\worker\clickhouse_log_store_local_compose_contract.json` -> production gate reads local artifact (`artifact_read=true`) but refuses review/final: `evidence_accepted_for_review=false`、`production_smoke_passed=false`、`final_x_eligible=false`，blockers include missing writer counts, DB cursor, WAL replay/send/ack, dedup, retention/load, simulated/stale/non-simulated. Local prototype 与 production final 差别：S39 只证明 dev-only compose/wire-up 与 bounded artifact/readback plumbing；它不包含 real ClickHouse insert writer、DB changefeed/export cursor、production WAL replay/send/ack、dedup journal persistence 或 retention/load smoke。下一步应派 E15-S40 实现 dev-bounded real ClickHouse HTTP writer/cursor/WAL send contract；不得把 S39 local prototype artifact 升级为 production final。
- **MISC-S42 Local-vs-Production Evidence Contract Hardening，已完成/负向 guard 审计收束，2026-06-05，owner Gauss/横向 reviewer lane**：目标：把 MISC-S40 红队结论转为可执行负向检查。未运行 production cutover、ClickHouse smoke、E13 proof、Gateway runtime 或 docker compose；未修改 E8/E9/E15 verifier，因为现有 guard 已机器可测。E8：`scripts/verify_gateway_tpm_production_backend_evidence.ps1` 对 S115 local prototype readback 返回 `status=production_ready_blocked`、`artifact_read=true`、`local_prototype=true`、`real_operator_evidence=false`、`final_gap_closure_allowed=false`、closure audit `final_x_eligible=false`，blocking reasons 包含 `missing_live_smoke`、`missing_db_acquire`、`missing_db_result`、`local_prototype_cannot_close_final_gap`、`simulation_can_close_final_gap_false`；机器可测命令为 `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_tpm_production_backend_evidence.ps1 -OptInArtifactReadback -ArtifactPath .tmp/gateway_tpm_production_backend/e8-s115-local-prototype.json -ExpectedCommit 8ac37f4d1a637f80eef0638be6093e05bb2f868c -BackendKind read_model_backend -TokenSourceKind input_tokens`。E9：`scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1` 对 `.tmp/billing-ledger/cutover-evidence-artifact.local-dev.json` readback 返回 cutover acceptance `classification=blocker`、`final_x_eligible=false`、`environment_scope=local_dev`，final closure blocking reasons 包含 `local_dev_cutover_artifact_not_production_final`；机器可测命令为 `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1 -ReadCutoverEvidenceArtifact -CutoverEvidenceArtifactPath .tmp/billing-ledger/cutover-evidence-artifact.local-dev.json -BlockedExitCode 0`。E15：`target\debug\ai-worker.exe clickhouse-log-store --final-closure-audit --read-production-smoke-artifact tests/fixtures/worker/clickhouse_production_smoke_artifact_accepted_simulation.json --input tests/fixtures/worker/clickhouse_log_store_plan_contract.json` reads the accepted-shape simulation fixture but final closure audit remains `final_x_eligible=false`; production smoke acceptance has `artifact_read=true`, `production_smoke_evidence_accepted_for_review=true`, `production_smoke_passed=false`, `simulation_can_mark_final_x=false`, `fixture_can_mark_final_x=false`, and final audit blocking reasons include `fixture_artifact`, `non_simulated_not_accepted`, `production_smoke_not_passed`, `simulated_artifact`. Guard status: E8/E9/E15 local/dev/simulation/no-artifact negative guards are machine-testable; remaining production closure still requires real external production artifacts/readback and cannot be closed by docs/manual review alone. No code hardening needed in this slice.
- **MISC-S41 Active Implementation Evidence Integrator，已完成/横向 evidence integration 收束，2026-06-05，owner Locke/横向 reviewer lane**：只读检查 `.tmp/**` 新 artifact 与 active slice tail，重点 E8-S115/S116、E9-S103、E15-S39。未改源码、未 build、未运行 production cutover/ClickHouse smoke；只更新 `docs/TODO.md` 与本文件。Evidence intake：E8-S116 split tail 已完成，`.tmp/gateway_tpm_production_backend/e8-s116-live-smoke.json` 为 dev Gateway/Postgres/mock-provider bounded live smoke artifact，`gateway_live_smoke_status=passed`、request/provider_attempt rows `3/3`、reservation acquire/release/not-applied/fallback `3/1/1/1`、DB acquire/result readback counts `3/3`，但 `local_prototype=true`、`real_operator_evidence=false`、`final_x_eligible=false`；E9-S103 split tail 已完成，`.tmp/billing-ledger/s103-local-dev-cutover-readback.json` 显示 `readiness=ready`、actual cutover/readback/rollback/no-dual dev-scoped pass、`environment_scope=local_dev`，但 cutover acceptance/final audit `final_x_eligible=false`，blocking reasons 为 `cutover_evidence_not_accepted` 与 `local_dev_cutover_artifact_not_production_final`；E15-S39 local-smoke artifact `.tmp/clickhouse-log-store/local-smoke/tenant-00000000-0000-0000-0000-000000000001.json` 存在，`writer_supported=false`、`execute_supported=false`、`send_supported=false`、`simulated=true`、`production_smoke_passed=false`、`final_x_eligible=false`；MISC-S42 已完成并确认 E8/E9/E15 local/dev/simulation negative guards 机器可测。TODO 更新：顶部 progress/matrix/checklist 已从“local-dev/S115/S103 待完成”改为“local/dev evidence produced, production final still blocked”；MISC-S41 active marker 已收束。未上调到 100%，因为没有任何 verifier 明确 non-local/non-simulated production `final_x_eligible=true`。建议主线程下一步只派 implementation/evidence-producing task：E8-S117 non-local runner provenance/readback bridge；E9-S104 repo-native production/staging cutover executor feasibility 或 production-scope canonical artifact intake；E15-S40 dev-bounded real ClickHouse HTTP writer/cursor/WAL send contract；MISC-S43 negative guard CI hook review。不得继续派 S115/S116、S103 或 S39 的重复 watcher。
