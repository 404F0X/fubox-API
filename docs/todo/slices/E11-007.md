# E11-007 切片记录

> 由 `docs/TODO.md` 拆分而来；只保存切片日志，不作为最终完成状态的唯一依据。
>
> Current launch-state override（2026-06-07）：`POST /admin/voucher-issuances` and
> `POST /billing/vouchers/redeem` are wired behind `BillingAdjust`, and
> `.tmp/launch/voucher_public_route_and_virtual_key_evidence.json` is now
> `overall_status=pass`, `voucher_route_evidence.status=live_route_verified`,
> `route_invoked=true`, `route_verified=true`, `public_routes_wired=true`, and
> `blockers=[]`. Older entries below that mention earlier partial route states,
> not-wired route blocker ids, or live route proof待补口径 are historical snapshots only. The remaining
> gap is public self-serve UX/productization, not route wiring or live proof.

- **E11-007-S4，已完成，2026-06-04，owner Goodall，live 执行阻塞**：ledger adjustment execute 后端真实写入边界。原因：Control Plane 目前已有 execute preflight contract，但 Billing/Price 页面最终验收需要后端存在可事务化写 ledger + success audit 的真实边界或明确阻塞点。范围：`apps/control-plane/**`、`examples/openapi_admin_skeleton.yaml`、control-plane fixtures；不改 Admin UI。验收：execute 前置校验、dedupe key、billing permission、transaction plan、success audit 同事务边界被 contract 固定；若缺少底层 ledger writer，则写明不可伪造执行并保留阻塞。结果：`mode=execute` 已新增最小真实事务 writer：preflight 后 revalidate/lock bounded rows，插入 confirmed ledger entry 与 success audit，共用同一 DB transaction；server-generated dedupe material 不回显，idempotent replay 返回既有 bounded ledger entry 且不伪造新 ledger/audit write。剩余缺口：未跑 live Postgres integration/smoke；真实 FK、并发 refund race、admin session audit actor 仍需 DB 环境验证。
- **E11-007-S6，已完成，2026-06-04，owner Goodall，live 执行阻塞**：Control Plane ledger adjustment execute live smoke。原因：S4 已接入最小事务 writer，但最终验收需要证明真实 migrated schema 上 FK、audit actor、idempotent replay、refund remaining recompute 与并发 race 行为可运行。范围：`scripts/**`、`tests/fixtures/control-plane/**`、必要的 control-plane smoke harness；不改 Admin UI/Gateway。验收：live DB 可用时能执行 apply + idempotent replay + refund over-remaining refusal + audit same-transaction evidence；DB 不可用时明确外部阻塞；默认测试不依赖 live DB；targeted control-plane checks 通过。结果：新增 Control Plane ledger adjustment execute live Postgres smoke 入口，覆盖 apply、idempotent replay、over-remaining refusal、concurrent refund race 与 success-audit evidence；contract-only checks 通过。剩余缺口：当前本机 Docker/compose Postgres/control-plane 不可用，真实 live run 外部阻塞。
- **E11-007-S8，已完成，2026-06-04，owner Goodall，live 执行阻塞**：Control Plane ledger adjustment execute smoke 测试入口接入。原因：S6 已新增可运行 harness，但开发/CI 需要明确默认 contract-only 与 opt-in live gate，避免真实 DB 不可用时造成默认失败。范围：`scripts/**`、必要验收文档；不改 Gateway/Admin UI/billing-ledger。验收：test/release 入口能默认跑 contract-only；显式 flag/env 才跑 live；Docker/Postgres/control-plane 不可用时明确阻塞；相关 script checks 通过。结果：ledger adjustment execute smoke 已接入 test/release gates：默认 contract-only，显式 test/release flag/env 才进入 live Postgres concurrency evidence。剩余缺口：当前本机 Docker compose Postgres/control-plane 不可用，live run 外部阻塞。
- **E11-007-S9，已完成，2026-06-04，owner Goodall，live 执行阻塞**：ledger execute live smoke runbook/acceptance blocker 文档。原因：S6/S8 已有 live harness 和 opt-in gate，但最终验收还需要明确操作手册和阻塞条件，方便有 Docker/Postgres 环境的人复跑并关闭 live 缺口。范围：`docs/**`、`TEST_AND_ACCEPTANCE.md`；不改 Gateway/Control Plane/Admin UI/billing-ledger/routing。验收：文档包含 contract-only 默认命令、live opt-in 命令、所需 compose services、预期 blocker exit code、通过后可关闭的 TODO 缺口。结果：`TEST_AND_ACCEPTANCE.md` 新增 ledger adjustment execute live smoke runbook，明确 contract-only 默认命令、test/release live opt-in flag/env、Docker compose Postgres/control-plane 条件、direct live blocker exit code `2` 与 `[BLOCKED]` 输出，以及 live 通过后可关闭 E11-S4/S6/S8 backend live 缺口。剩余缺口：当前本机仍无可用 Docker/compose Postgres/control-plane live 环境，真实 live run 需要外部环境补跑。
- **E11-007-S11，已完成，2026-06-04，owner Goodall**：ledger adjustment execute OpenAPI/acceptance contract 同步。原因：E11-S7/E9-S7 已让 UI 和 Control Plane 使用真实 execute response 与 `ledger_executor_summary` 边界，但 OpenAPI skeleton/acceptance text 需要同步最新 applied/idempotent/executor-summary/secret-safe 字段，避免客户端按旧 contract 生成。范围：`examples/openapi_admin_skeleton.yaml`、必要的 `TEST_AND_ACCEPTANCE.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger 代码。验收：OpenAPI 描述 `mode=execute` applied/idempotent response、`ledger_executor_summary_contract`/`ledger_executor_summary`、dedupe material omitted、raw metadata/credential 不回显；必要文档指向对应 contract；OpenAPI/yaml diff check 通过。结果：OpenAPI skeleton 同步 applied/idempotent execute response、execute_contract refusal response、executor summary/refusal/rollback schemas 与 secret-safe omission 语义；acceptance runbook 增加 executor summary live smoke 检查。剩余缺口：未运行完整 OpenAPI validator/client generation；live Postgres smoke 仍需外部环境。
- **E11-007-S13，已完成，2026-06-04，owner Goodall**：ledger execute OpenAPI contract validation gate。原因：S11 已同步 OpenAPI skeleton，但目前只有 YAML parse 和人工 diff；需要一个轻量可运行检查锁定 ledger adjustment execute/executor summary refs，避免后续 schema drift。范围：`scripts/**`、`examples/openapi_admin_skeleton.yaml`、必要验收文档；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger 代码。验收：新增或强化脚本/检查能 parse OpenAPI YAML，并断言 `/admin/ledger/adjustments/dry-run`、execute applied/idempotent schema、execute_contract refusal schema、executor summary/refusal/rollback refs 和 secret-safe omission markers 存在；默认不依赖外部服务；diff check 通过。结果：新增 `verify_control_plane_ledger_adjustment_openapi_contract.ps1`，默认无外部服务依赖，锁定 ledger execute OpenAPI refs 和 secret-safe omission markers，并接入 `scripts/test.ps1 -ControlPlaneLedgerAdjustmentExecuteSmokeOnly` 与 release smoke gate。剩余缺口：该 gate 不替代完整 OpenAPI semantic validator/client generation；live Postgres smoke 仍需外部环境。
- **E11-007-S14，已完成，2026-06-04，owner Goodall**：ledger execute OpenAPI semantic validator/client generation runbook。原因：S13 已提供轻量默认 gate，但完整 OpenAPI semantic validator 和 client generation 仍是独立验收项，需要明确工具、命令、外部依赖、失败解释和关闭条件。范围：`docs/**`、必要 `TEST_AND_ACCEPTANCE.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/scripts。验收：文档列出推荐 validator/client-generation 命令、依赖安装方式、离线/缺工具 blocker 语义、需要检查的 generated client contract、通过后可关闭的 E11 OpenAPI 缺口，以及不能关闭的 live Postgres/UI/audit 缺口；diff check 通过。结果：新增 `docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`，记录 Redocly/OpenAPI Generator/Python validator、`openapi-typescript`/`typescript-fetch` client generation、离线/缺工具 blocker、generated client 字段检查和 secret-safe omission 检查；`TEST_AND_ACCEPTANCE.md` 增加 11.7 入口。剩余缺口：本切片只提供 runbook，未实际运行完整 semantic validator/client generation；live Postgres、success audit live evidence、Admin UI E2E 和 billing-ledger runtime writer 仍是独立缺口。
- **E11-007-S16，已完成，2026-06-04，owner Goodall**：ledger execute OpenAPI semantic validation script/preflight。原因：S14 已写 runbook，但开发/CI 仍需要一个 opt-in wrapper，把默认轻量 gate、外部工具 blocker、semantic validation failure 和 client-generation mismatch 区分清楚。范围：`scripts/**`、`docs/**`、必要 `TEST_AND_ACCEPTANCE.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger 代码。验收：新增或强化脚本，默认不下载包、不生成客户端且能跑轻量 OpenAPI gate；显式 flag/env 才运行 Redocly/OpenAPI Generator/openapi-typescript；缺工具/离线 package cache exit `2`，schema/client mismatch exit `1`，全部通过 exit `0`；临时 generated artifacts 可清理且不提交；diff check 通过。结果：新增 `scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`，默认只运行 lightweight OpenAPI gate；`-Semantic`/`-Redocly`/`-OpenApiGeneratorValidate` 才运行 semantic validators，`-ClientGeneration`/`-OpenApiTypescript`/`-TypescriptFetch` 才生成临时客户端；缺外部工具或离线 npm cache 返回 exit `2`，contract mismatch 返回 exit `1`，通过返回 exit `0`；runbook 与 TEST 同步命令、artifact 和 exit 语义。剩余缺口：未执行真实 Redocly/OpenAPI Generator/openapi-typescript 全通过流程；live Postgres、success audit live evidence、Admin UI E2E 和 billing-ledger runtime writer 仍是独立缺口。
- **E11-007-S19，已完成，2026-06-04，owner Goodall**：ledger execute OpenAPI semantic wrapper failure-path self-test。原因：S16 已有 opt-in wrapper 和 exit `0/1/2` 语义，但 blocker/mismatch 分支仍主要靠人工触发；后续需要脚本级自测锁住默认轻量 gate、外部 blocker 和 schema/client mismatch 的退出码，避免 CI/开发入口漂移。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger，也不改 Prompt Protection 脚本。验收：新增或强化 self-test/simulated failure switches，在不下载 npm 包、不生成真实客户端、不连接 live DB 的情况下证明 default exit `0`、simulated external blocker exit `2`、simulated schema/client mismatch exit `1`；runbook 记录命令和边界；默认 wrapper 行为不变；diff check 通过。结果：新增 `-SelfTest` 与 env opt-in，子进程验证 default lightweight path exit `0`、`-SimulateExternalBlocker` exit `2`、`-SimulateSchemaMismatch`/`-SimulateClientMismatch` exit `1`；自测不下载 npm、不生成真实客户端、不连接 DB/服务，并检查输出不包含 Authorization/Cookie/bearer token/credential/raw operation-key 等材料。剩余缺口：未替代真实 Redocly/OpenAPI Generator/openapi-typescript 运行；live Postgres、真实 semantic/client generation、UI E2E 与 billing-ledger runtime writer 仍未关闭。
- **E11-007-S20，已完成，2026-06-04，owner Goodall**：ledger execute OpenAPI semantic wrapper path/output hardening。原因：S16/S19 已有 wrapper 和 failure-path self-test，但临时目录、npm cache、generated artifact 输出和 failure tail 的边界还需要更强 contract，避免外部工具路径或错误输出泄露 secret 或写出 repo 外。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection。验收：wrapper 对 `-TempRoot`/`-NpmCache` repo 内约束、artifact cleanup、child output tail redaction、external tool command display 有 self-test/contract 覆盖；默认 lightweight 行为不变；`-SelfTest`、默认 wrapper、`-Clean` 和 diff check 通过。结果：`-TempRoot` 限制在 repo `.tmp` 下，`-NpmCache` 限制在 repo `.tool-cache` 或 `.tmp` 下；`-Clean` 只清理受限 TempRoot；外部工具命令、成功 tail 与失败输出统一 redaction；`-SelfTest` 覆盖敏感 output tail/command failure redaction、路径逃逸拒绝和 artifact cleanup。剩余缺口：仍未运行真实 Redocly/OpenAPI Generator/openapi-typescript；live Postgres、真实 semantic-client generation、UI E2E 和 billing-ledger runtime writer 缺口未关闭。
- **E11-007-S22，已完成，2026-06-04，owner Goodall**：ledger execute OpenAPI generated-client inspection contract。原因：S16/S19/S20 已把 wrapper 默认/失败/路径输出边界锁住，但真实 client generation 通过后还需要有稳定 inspection contract，明确 generated client 必须包含哪些 execute response/executor summary 字段且哪些 secret 字段必须缺失。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection/TEST。验收：新增或强化 generated-client inspection helper/contract，能在模拟 generated files 或 wrapper self-test 中证明 required fields present、secret fields absent、missing required field exit `1`；不下载真实包；默认 lightweight 行为不变；`-SelfTest`、默认 wrapper、`-Clean` 和 diff check 通过。结果：wrapper 新增 generated-client inspection contract，真实 `openapi-typescript`/`typescript-fetch` 生成成功后检查 ledger execute result、execute contract envelope、executor summary/refusal/rollback summary 模型和字段；self-test 模拟 required fields present/secret fields absent exit `0` 与 missing required field exit `1`。剩余缺口：真实 Redocly/OpenAPI Generator/openapi-typescript 仍需有工具缓存或联网环境 opt-in 执行。
- **E11-007-S24，已完成，2026-06-04，owner Goodall**：ledger execute OpenAPI semantic validator opt-in evidence contract。原因：S22 已锁定 generated client inspection，但完整 semantic validator 通过后还需要有 bounded evidence 记录 validator/tool/version、checked schema 和 blocker/failure 分类，避免只看 console 输出。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection/TEST。验收：新增或强化 opt-in evidence summary/report/self-test，覆盖 semantic validator pass/fail/blocker 的 bounded fields、secret-safe output、exit `0/1/2`；不下载真实包；默认 lightweight 行为不变；`-SelfTest`、默认 wrapper、`-Clean`、diff check 通过。结果：wrapper 新增 `ledger_openapi_semantic_evidence.v1` bounded evidence report，记录 validator/client tool、version、package、checked schema、classification、redacted command、bounded output tail 和 failure/blocker reason；self-test 覆盖 semantic evidence pass/failure/blocker 三条模拟路径并锁定 exit `0/1/2`；默认 lightweight 不下载 npm、不生成 client、不写 evidence report。剩余缺口：真实 Redocly/OpenAPI Generator/openapi-typescript opt-in 仍未运行；evidence report lifecycle/path/cleanup 仍需继续固定。
- **E11-007-S27 Backend，已完成，2026-06-04，owner Goodall**：ledger execute OpenAPI semantic evidence report lifecycle contract。原因：S24 已新增 opt-in evidence report，但 report artifact 的生成、覆盖、清理和 stale evidence 边界还需要锁住，避免默认 gate 或 `-Clean` 后遗留旧报告被误读为新 validator 证据。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection/TEST。验收：新增或强化 self-test/contract，证明默认 lightweight 不写 report，opt-in/simulated report 只写在 validated temp root，`-Clean` 只删除 wrapper-owned temp artifacts 且移除 stale evidence，repo 外/source/`.git`/非 temp path 不会被写入或清理；secret-safe output 保持；默认 wrapper、`-SelfTest`、`-Clean`、diff check 通过。结果：wrapper 启动时清理 stale evidence，默认 lightweight 不写新 report；evidence report path 派生自 validated `TempRoot`；source、`.git`、repo 外、非 `.tmp` path 在写入/清理前拒绝；`-Clean` 只删除 wrapper-owned evidence/generated-client/self-test artifacts 并保留非 owned temp marker；runbook 记录 evidence lifecycle 与 cleanup allowlist。剩余缺口：report 尚未绑定当前 commit、命令 provenance 和 fixture freshness；真实 Redocly/OpenAPI Generator/openapi-typescript semantic run 仍需 opt-in 工具链证据。
- **E11-007-S29 Backend，已完成，2026-06-04，owner Goodall**：ledger execute OpenAPI semantic evidence provenance contract。原因：S27 已锁住 evidence artifact lifecycle，但 report 仍需要绑定当前 repo commit、命令摘要、工具/模拟模式和 fixture freshness，避免旧 report 或 simulated report 被误当成当前真实 validator evidence。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection/TEST。验收：新增或强化 report schema/self-test，覆盖 repo commit 或明确 unavailable marker、generated_at、command summary redaction、real/simulated provenance、OpenAPI fixture freshness guidance、stale/simulated report 不可关闭真实 semantic gap；默认 lightweight 不写 report；`-SelfTest`、default wrapper、`-Clean`、diff check 通过。结果：evidence report 新增 `repo_commit`/status、`generated_at_utc`、`provenance_mode`、bounded `command_summary` 和 OpenAPI fixture SHA-256/size/last-write fingerprint；每条 evidence record 标记 real/simulated；self-test 验证 simulated reports 的 provenance、fixture freshness、redaction 和 output bounds；默认 lightweight 仍不写 report。剩余缺口：真实 Redocly/OpenAPI Generator/openapi-typescript opt-in semantic/client generation 仍未运行；live Postgres、真实 generated-client acceptance、Admin UI E2E 仍是独立缺口。
- **E11-007-S30 Backend，已完成，2026-06-04，owner Goodall**：OpenAPI semantic real-tool opt-in preflight/performance evidence contract。原因：S29 已防止 simulated/stale evidence 被误关口，但高性能/真实工具链验收还需要明确 Redocly/OpenAPI Generator/openapi-typescript 的可用性、运行耗时和 blocker 输出。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger。验收：新增或强化 opt-in preflight/self-test，检查真实工具是否可用、版本/路径是否 safe、命令 duration 是否 bounded、package/cache 状态是否明确；默认不下载、不运行重工具；缺工具输出 blocker 而非 simulated pass；`-SelfTest`、默认 wrapper、`-Clean`、diff check 通过。结果：evidence record 新增 `tool_path`、`preflight_status`、`package_cache_status`、`package_download_allowed` 和 bounded `duration_ms`；`Invoke-NpmTool` 在 opt-in 前记录 node/npm/java 可用性，缺工具时写 blocker evidence 而不是 simulated pass；新增 `-SimulateToolPreflightBlocker` self-test 固定缺工具/cache/duration/redaction contract；默认 lightweight 仍不下载、不运行重工具、不写 evidence report。剩余缺口：真实 Redocly/OpenAPI Generator/openapi-typescript semantic/client-generation 仍未运行；本片只关闭 real-tool preflight/perf evidence contract。
- **E11-007-S33 Backend，已完成，2026-06-04，owner Goodall**：generated-client inspection readiness gate。原因：S30 已固定真实工具 preflight/perf evidence，但 generated-client acceptance 仍缺一个把 generated output freshness、target directory safety、required execute fields 和 blocker 分类串起来的 readiness gate。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger。验收：新增或强化 generated-client inspection readiness self-test/preflight，覆盖 generated output missing/stale/current marker、target dir safe path、required ledger execute/executor summary fields、duration/package/tool evidence passthrough、secret-safe output；默认不生成 client、不下载 package；`-SelfTest`、默认 wrapper、`-Clean`、diff check 通过。结果：generated-client inspection 前新增 readiness gate，要求 generated target 位于 validated `TempRoot`、output 存在、wrapper-owned `.ledger-openapi-generated-client-readiness.json` marker 当前且绑定 OpenAPI fixture SHA-256；marker 记录 target/tool/provenance/package cache/download/duration，current 后才检查 ledger execute/executor summary required fields；self-test 覆盖 missing output、stale marker、unsafe target 均 exit `1`。剩余缺口：真实 generated-client production acceptance 仍需后续显式 opt-in 跑 `-OpenApiTypescript`、`-TypescriptFetch` 或 `-ClientGeneration`。
- **E11-007-S35 Backend，已完成，2026-06-04，owner Goodall**：OpenAPI real opt-in command matrix blocker audit。原因：S30/S33 已固定工具 preflight 和 generated-client readiness，但最终真实 semantic/client-generation 前需要一份本地环境命令矩阵，清楚区分哪些 opt-in 命令在当前机器 blocked、哪些能运行，以及每个 blocker 产出的 evidence 字段。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger。验收：新增或强化 command matrix dry-run/self-test，列出 Redocly/OpenAPI Generator/openapi-typescript/typescript-fetch 的 opt-in flags、required tools、package/cache policy、expected exit classification、evidence fields、safe command examples；默认不下载、不运行 heavy tools；`-SelfTest`、默认 wrapper、`-Clean`、diff check 通过。结果：wrapper 新增 `-CommandMatrix` 与 env opt-in，输出 Redocly、OpenAPI Generator validate、openapi-typescript、typescript-fetch 四个真实 opt-in 入口的 required tools、package/cache policy、exit classification、evidence fields 和 safe wrapper command；command matrix dry-run 不运行 npm/java、不下载包、不生成 client、不写 evidence report，并进入 self-test；runbook 增加 blocker audit/command matrix 章节。剩余缺口：真实 Redocly/OpenAPI Generator/openapi-typescript/typescript-fetch opt-in pass 仍需后续可用工具/cache 环境运行。
- **E11-007-S37 Backend，已完成，2026-06-04，owner Goodall**：OpenAPI real-tool cache availability probe。原因：S35 已列出真实 opt-in command matrix；下一片需要在不下载 package、不生成 client 的前提下探测本机/CI 是否已有 Redocly、OpenAPI Generator、openapi-typescript 所需 cache/tooling，并把 blocker 输出绑定到 evidence fields。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection。验收：新增或强化 cache/tool availability probe，覆盖 node/npm/java path/version、repo cache present/missing、download disabled marker、per-command blocker classification、duration marker和 secret-safe output；默认不下载、不运行 heavy validator/generator；`-SelfTest`、默认 wrapper、`-CommandMatrix`、`-Clean`、diff check 通过。结果：新增 `-CacheProbe` / `CONTROL_PLANE_LEDGER_OPENAPI_CACHE_PROBE=1` 与 simulated cache probe self-test，输出 per-command tool path/version、offline repo cache present/missing、package download disabled marker、preflight status、classification、bounded duration 和 redacted output tail；默认仍只跑 lightweight gate，不下载 package、不运行 Redocly/OpenAPI Generator/openapi-typescript；本机实际 probe 显示 node/npm/java 可用，但 `.tool-cache\npm` 缺少所需 package cache，因此按预期返回 blocker exit 2 并写 evidence。剩余缺口：真实 Redocly/OpenAPI Generator/openapi-typescript semantic/client-generation pass 仍未运行；本片只关闭 cache/tool availability probe。
- **E11-007-S39 Backend，已完成，2026-06-04，owner Goodall**：OpenAPI real-tool opt-in execution evidence artifact。原因：S37 已能判断工具/cache 是否具备，但最终缺口仍是一次真实 Redocly/OpenAPI Generator/openapi-typescript opt-in execution 的 pass/blocker/fail evidence；下一片需要固定真实执行时的 evidence artifact 形状和 closure rules，避免 simulated/cache-only 结果被误当成真实 semantic/client-generation pass。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection。验收：新增或强化 real-tool opt-in execution evidence contract，覆盖 tool/cache preflight passed、real command duration、exit classification、generated-client readiness marker、artifact provenance/freshness、secret-safe command/output tail；默认不下载、不运行 heavy validator/generator；缺 cache/tool 返回 blocker 而非 simulated pass；`-SelfTest`、默认 wrapper、`-CommandMatrix`、`-CacheProbe` blocker path、`-Clean`、diff check 通过。结果：evidence record 新增 `execution_mode`、`real_command_executed`、`readiness_marker_status`、`closure_eligible`，并固定 closure rule：只有 `provenance_mode=real`、`execution_mode=real_tool_execution`、`real_command_executed=true`、`classification=pass` 且 generated-client readiness marker current 才能关闭真实 semantic/client-generation 缺口；新增 simulated real execution pass/fail/blocker self-test，默认仍不下载、不运行 heavy tools。当前实际 `-CacheProbe` 显示 node/npm/java 可用，但离线 npm cache 缺 Redocly/OpenAPI Generator/openapi-typescript，因此四个真实 opt-in command 仍按预期 blocker exit 2。剩余缺口：真实 Redocly/OpenAPI Generator/openapi-typescript/typescript-fetch pass 仍需预置 cache 或显式 download opt-in 后运行。
- **E11-007-S41 Backend，已完成，2026-06-04，owner Goodall**：OpenAPI package cache provisioning/download opt-in evidence gate。原因：S39 已固定真实执行 evidence closure rule，但当前实际 blocker 是 `.tool-cache\npm` 缺少 Redocly/OpenAPI Generator/openapi-typescript；下一片需要固定 package cache provisioning 或显式 download opt-in 的证据语义，避免下载行为不透明或 cache 预置后无法证明 provenance。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection。验收：新增或强化 cache provisioning/download opt-in gate，覆盖 package list、cache directory safety、download opt-in flag/env、network/download disabled default、package version/provenance marker、duration/cache size summary、secret-safe npm output tail；默认不下载、不运行 heavy validator/generator；`-SelfTest`、默认 wrapper、`-CommandMatrix`、`-CacheProbe` blocker path、`-Clean`、diff check 通过。结果：evidence record 和 command matrix 新增 `package_list`、`package_version`、`package_provenance`、`package_cache_path`、`package_cache_bytes`、`package_probe_duration_ms`，并锁定 Redocly/OpenAPI Generator/openapi-typescript package list；默认 `-CacheProbe` 在离线 cache 缺包时输出 `offline_repo_cache_missing` blocker，不下载、不运行 validator/generator；`-CacheProbe -AllowPackageDownload` 只记录 `package_download_allowed=true`、`package_provenance=download_opt_in`、bounded cache path/size/probe duration 和 safe npm/tool output tail，不实际下载或关闭真实 semantic gap。验证：默认 wrapper、`-SelfTest`、`-CommandMatrix`、`-CacheProbe` blocker path、`-CacheProbe -AllowPackageDownload`、`-Clean` 和 diff check 通过。剩余缺口：真实 Redocly/OpenAPI Generator/openapi-typescript semantic/client-generation pass 仍需预置 `.tool-cache\npm` 或显式受控下载后再跑真实工具链。
- **E11-007-S43 Backend，已完成，2026-06-04，owner Goodall**：OpenAPI package cache materialization executor boundary。原因：S41 已固定 package cache/download opt-in evidence，但还没有明确真实 npm package materialization 何时允许执行、如何限定 cache 目录、如何记录 package size/version/provenance/duration，以及失败时如何输出 blocker 而不污染 repo 或关闭 semantic gap；下一片先固定受控 executor boundary。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection。验收：新增或强化 materialization executor boundary，覆盖 disabled-by-default、`-AllowPackageDownload` + explicit materialize flag/env 双 opt-in、repo-local cache path safety、package allowlist、npm install/download command summary redaction、per-package duration/cache bytes/version/provenance evidence、failure/blocker classification、no validator/generator execution by default、stale/incomplete cache refusal；`-SelfTest`、默认 wrapper、`-CommandMatrix`、`-CacheProbe`、materialize dry-run/self-test、`-Clean` 和 diff check 通过。结果：新增 `-MaterializePackageCache` 和 `CONTROL_PLANE_LEDGER_OPENAPI_MATERIALIZE_PACKAGE_CACHE=1` 双 opt-in gate，materialization 必须同时具备 `-AllowPackageDownload`；默认 wrapper、`-CommandMatrix`、`-CacheProbe` 均不下载、不 materialize、不跑 validator/generator；固定 package allowlist 为 `@redocly/cli`、`@openapitools/openapi-generator-cli`、`openapi-typescript`；每个 package 输出 `package_materialization` evidence，包含 package/version/provenance/cache path/cache status/cache bytes/download allowed/probe duration/duration/command summary/output tail，缺 opt-in、缺工具、缺 cache 或 incomplete cache 均为 blocker，且不关闭真实 semantic/client-generation gap。剩余缺口：真实 semantic/client-generation pass 仍需在受控环境预置/materialize cache 后显式运行 Redocly/OpenAPI Generator/openapi-typescript/client generation。
- **E11-007-S45 Backend，已完成，2026-06-04，owner Goodall**：OpenAPI real-tool execution after materialization readiness gate。原因：S43 已固定 package materialization 边界，但真实 semantic/client-generation pass 仍需要明确 materialized cache 如何成为 Redocly/OpenAPI Generator/openapi-typescript 执行前置条件，以及真实命令 evidence 如何拒绝 stale/incomplete cache。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection。验收：新增或强化 real-tool execution readiness gate，覆盖 materialized cache required markers、stale/incomplete cache blocker、real command disabled by default、per-tool duration/provenance/cache readback evidence、generated client readiness marker readback、secret-safe npm/java/node output tail；`-SelfTest`、默认 wrapper、`-CommandMatrix`、`-CacheProbe`、materialization/readiness dry-run、`-Clean` 和 diff check 通过。结果：新增 `-RealToolReadiness` 与 `CONTROL_PLANE_LEDGER_OPENAPI_REAL_TOOL_READINESS=1`；package materialization 成功后会写 `.ledger-openapi-package-materialization.json` marker，绑定 OpenAPI fixture SHA-256、固定 package list、cache path/size 与 generated time；real-tool readiness 对每个真实命令执行前做 marker/current package list/offline cache readback，输出 `real_tool_readiness` evidence、package/cache provenance、cache readback duration、tool preflight duration 与 `readiness_marker_status`，缺 marker、stale marker、incomplete package list、缺 cache 或 cache readback incomplete 均为 blocker；Redocly/OpenAPI Generator/openapi-typescript/typescript-fetch 在 readiness 失败时不会执行 npm tool，generated-client inspection 仍需 current generated-client readiness marker。验证：`-SelfTest`、默认 wrapper、`-CommandMatrix`、`-CacheProbe` blocker、`-RealToolReadiness` blocker、`-MaterializePackageCache` missing download opt-in blocker、`-Redocly` readiness blocker、`-Clean` 和 scoped diff check 通过。剩余缺口：真实 Redocly/OpenAPI Generator/openapi-typescript/typescript-fetch pass 仍需先在受控环境 materialize cache 后显式运行；真实 backend/browser live smoke 与 E2E 验收仍未最终关闭。
- **E11-007-S47 Backend，已完成，2026-06-04，owner Goodall**：OpenAPI real-tool execution closure readiness marker contract。原因：S45 已完成 package materialization marker 与 real-tool readiness gate，但仍缺真实工具 pass 后如何写入 closure readiness marker、以及 stale/simulated/cache-only 证据为什么不能关闭 semantic/client-generation gap 的稳定 contract。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection。验收：新增或强化 real-tool execution closure marker/readback contract，绑定 OpenAPI fixture SHA-256、repo commit/generated_at、tool/package/cache provenance、duration summary 和 generated-client readiness marker；默认 wrapper 不写 closure marker、不下载、不运行 heavy tools；simulated pass/cache probe/materialization/readiness pass 均不能 closure eligible；self-test/simulated cases 覆盖 current closure marker pass、stale marker refusal、simulated marker refusal、missing generated-client marker refusal；输出 secret-safe；`-SelfTest`、默认 wrapper、`-CommandMatrix`、`-RealToolReadiness` blocker path、`-Clean` 和 scoped diff check 通过。结果：新增 `.ledger-openapi-real-tool-closure-readiness.json` marker contract，绑定 OpenAPI SHA-256、repo commit/status、generated_at、tool/package/cache provenance、materialization marker、generated-client readiness marker 与 duration summary；generated-client evidence 只有在真实 command pass、current materialization、current generated-client marker、current closure marker 同时满足时才允许 `closure_eligible=true`，simulated pass、cache probe、materialization 或 readiness-only 证据均不能关闭真实 semantic/client-generation gap。验证：`-SelfTest`、默认 wrapper、`-CommandMatrix`、`-RealToolReadiness` blocker path、`-SimulateClosureMarkerCurrent`、stale/simulated/missing-generated 负向、`-Clean` 和 scoped diff check 通过。剩余缺口：真实 Redocly/OpenAPI Generator/openapi-typescript pass 仍需后续受控环境显式运行。
- **E11-007-S48 Backend，已完成，2026-06-04，owner Goodall**：OpenAPI real-tool package cache materialized execution dry-run bridge。原因：S47 已固定 closure marker 不能被 simulated/cache-only 证据误关闭，但真实工具链 pass 前还缺一个把 materialized package cache、tool availability、generated client target 和 closure marker command 串起来的 dry-run bridge，方便受控环境一键执行真实 semantic/client-generation。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection。验收：新增或强化 real-tool execution bridge command/matrix，输出 cache materialization preflight、tool/package/version/cache bytes、generated target、closure marker target、duration names 和 exact opt-in commands；默认不下载、不运行 heavy tools、不写 generated clients；缺 cache/tool 输出 blocker；simulated bridge 不能 `closure_eligible=true`；self-test/default/command matrix/real-tool readiness blocker/clean/diff check 通过。结果：新增 `-RealToolExecutionBridge` / `CONTROL_PLANE_LEDGER_OPENAPI_REAL_TOOL_EXECUTION_BRIDGE=1` dry-run bridge；`-CommandMatrix` 输出 `[BRIDGE]` 行，串联 `-RealToolReadiness`、受控环境 `-Semantic -ClientGeneration`、materialization marker/npm cache、generated client targets、closure marker paths 和 closure rule；bridge 默认不下载、不运行 heavy tools、不写 generated clients，缺 materialized cache/tool readiness 时 blocker，simulated bridge 仅产生 readiness-shaped evidence 且不能 `closure_eligible=true`。验证：`-SelfTest`、默认 wrapper、`-CommandMatrix`、`-RealToolReadiness` blocker、`-RealToolExecutionBridge` blocker、`-RealToolExecutionBridge -SimulateRealToolExecutionBridgeReady`、`-Clean` 和 scoped diff check 通过。剩余缺口：真实 Redocly/OpenAPI Generator/openapi-typescript pass 仍需后续在已 materialize package cache 的受控环境显式运行。
- **E11-007-S50 Backend，已完成，2026-06-04，owner Goodall**：OpenAPI real-tool controlled execution pass gate。原因：S48 已给出受控执行 bridge，但仍未实际把 `-Semantic -ClientGeneration` 的真实工具 pass/readback gate 固定到 closure marker；下一片需要强化真实执行 pass gate，即使当前环境缺 cache/tool 也要输出强 blocker，具备条件时能生成 client readiness + closure marker。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection。验收：默认不下载、不运行 heavy tools；显式 `-Semantic -ClientGeneration` 才进入真实执行 gate；缺 materialized cache/tool/generated target -> blocker；真实 command pass 后写 generated-client readiness marker 与 closure marker，readback 校验 current OpenAPI SHA、repo commit、duration、tool/package/cache provenance；simulated pass 不能 closure；self-test/default/matrix/bridge/readiness/clean/diff check 通过。结果：`-Semantic -ClientGeneration` 先进入 controlled real-tool execution pass gate，真实工具执行前校验 materialized package cache/tool readiness；真实 generated-client pass 后还要求 generated target 存在且可检查，才允许写 readiness/closure marker；runbook 同步 simulated pass 不能 closure。剩余缺口：当前环境缺 materialized npm package cache readiness，真实 Redocly/OpenAPI Generator/openapi-typescript pass 仍需后续受控环境完成。
- **E11-007-S52 Backend，已完成，2026-06-04，owner Goodall**：OpenAPI materialized package cache closure preflight。原因：S50 已把真实工具执行挡在 controlled pass gate 后，但当前 blocker 是 materialized package cache readiness；下一片需要让缓存/工具/materialization 的 closure preflight 更接近可执行，输出每个 package/tool 的缺失原因、可复制 materialization command、cache size/duration/provenance 和 closure blocker。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`、必要 `.tool-cache` marker contract；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection。验收：默认不下载、不跑 heavy tools；显式 materialization/readiness/preflight 才检查 npm cache；缺 package/tool/marker -> blocker 并输出 per-package reason、bounded cache path、duration、safe materialization command；具备 cache 时 readback current marker；simulated/materialized stale 不能 closure；self-test/default/matrix/readiness/bridge/clean/diff check 通过。结果：real-tool readiness/materialization evidence 增加 per-package closure preflight reason、bounded cache path/size、safe wrapper materialization command、per-package diagnostic command、package/cache provenance 和 probe duration；缺 materialization marker 时直接输出可复制 wrapper command，simulated/stale materialization 仍不能 closure。剩余缺口：当前环境仍缺 `.tool-cache\npm\.ledger-openapi-package-materialization.json`；真实 semantic/client-generation pass 仍需后续受控工具环境。
- **E11-007-S54 Backend，已完成，2026-06-04，owner Goodall，加大切片**：OpenAPI package materialization + real semantic/client-generation pass attempt。结果：repo-local `.tool-cache\npm` materialized Redocly/OpenAPI Generator/openapi-typescript；`-RealToolReadiness`、`-Semantic -ClientGeneration -AllowPackageDownload` 真实通过，生成并 readback openapi-typescript 与 typescript-fetch closure markers；修复 `AlertWebhookPayloadSummary.kind` enum 真实语义校验问题，并强化 generated-client model block 检查。验证：OpenAPI self/default/matrix/real readiness/semantic/client-generation/clean 通过；Redocly 仅剩既有 warnings，OpenAPI Generator 仅剩 unused model recommendations。剩余缺口：E11 backend OpenAPI 工具链可视为关闭，最终 `[x]` 仍取决于 browser mutation live pass。
- **E11-007-S56 Backend，已完成，2026-06-04，owner Goodall，加大切片**：OpenAPI real-tool pass stabilization + cache hygiene。结果：`.gitignore` 加入 `openapitools.json`，OpenAPI semantic wrapper 会在 validate/generate 后清理 transient root config，`-Clean` 也清理 wrapper-owned `.tmp` 和带 marker 的 `openapitools.json`；self-test 覆盖 cleanup。验证：default/self/matrix/real readiness/semantic/client-generation/clean/diff check 通过；无 blocker，仅保留 Redocly 22 warnings 和 OpenAPI Generator 3 unused model recommendations。剩余缺口：E11 backend OpenAPI 方向基本关闭，最终缺口转到 browser session/live mutation。
- **E11-007-S58 Backend，已完成，2026-06-04，owner Goodall，加大切片**：Billing execute backend/admin session readiness handoff。原因：E11 browser pass 的实际 blocker 是 admin session，Backend lane 需要给 Smoke lane 一个安全、可复跑的 session readiness/readback boundary。范围：Control Plane/Admin session smoke scripts 或 Admin UI API client/fixtures；不改 Gateway/E8/E9 writer/E13。验收：确认 dev admin credentials/session generation path，输出 session-present marker 但不回显 token/cookie；login 401 时给出精确 blocker；能提供 `CONTROL_PLANE_ADMIN_SESSION_TOKEN` handoff 的可复制命令；targeted tests/build/diff check 通过。结果：新增 `-AdminSessionHandoff` / `CONTROL_PLANE_LEDGER_ADJUSTMENT_EXECUTE_ADMIN_SESSION_HANDOFF=1`，优先验证既有 `CONTROL_PLANE_ADMIN_SESSION_TOKEN`，否则尝试 dev admin login；输出仅含 session-present/source/env/header marker 与 echoed=false，不回显 token/cookie。当前 live 服务 reachable，但 `admin@example.com` dev login 返回 401，脚本以 expected blocker exit 2 退出。剩余缺口：修复 dev admin seed/password 或提供有效 admin session 后，才能关闭 browser mutation pass。
- **E11-007-S60 Backend/Session，已完成，2026-06-04，owner Goodall，加大切片**：dev admin seed/session repair for browser E2E。原因：S58/S59 已把 E11 blocker 收敛为 admin session，但 dev login 仍 401；本片直接修复或明确证明 dev admin seed/password/session 生成路径。范围：`db/dev-seeds/**`、admin auth smoke scripts/fixtures；不改 Gateway/E8/E9 writer/E13/browser runner。结果：dev admin team/project membership seed 改为 update + insert where not exists，不再依赖旧 volume 缺失的 membership unique constraints；auth smoke 诊断 `user_sessions` migration、dev admin seed、hash algorithm、owner membership，并新增 `-RepairDevAdminSessionPrereqs` 受控修复入口。验证：auth smoke repair/default 通过，viewer RBAC 仍 pending。剩余缺口：Docker/compose/Postgres 缺失时仍是外部 blocker。
- **E11-007-S62 Backend/Runtime，已完成，2026-06-04，owner Goodall，加大切片**：runtime/source stale diagnostic。原因：S60 修复 session 后，live Docker 仍出现 source-level replay ordering pass 但 runtime 行为旧的问题；下一步需要 bounded runtime mismatch 诊断。范围：`scripts/verify_control_plane_auth_smoke.ps1`、`scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1`、`tests/fixtures/control-plane/admin_auth_smoke.json`；不改 browser runner 行为。结果：auth 和 ledger execute smoke 新增只读 runtime/source mismatch probe，比较 control-plane container/image 创建时间与关键源文件 mtime，输出 `runtime_source_mismatch_probe=control_plane_image_timestamp`、`runtime_image_stale_or_unverified=true|false`、`runtime_image_stale_reason`，不 build、不输出 secret。当前环境诊断为 `source_newer_than_runtime_image`。验证：auth smoke、ledger contract-only、diff check 通过。剩余缺口：live browser mutation pass 仍需 runtime current 后复测。
- **E11-007-S64 Backend/Runtime，已完成/阻塞收束，2026-06-04，owner Goodall，加大切片**：runtime current handoff without rebuild。原因：S62 已能诊断 stale，但当前约束下不再自动 Docker rebuild；下一步需要提供低成本 runtime-current handoff 或明确复跑条件。范围：E11 auth/ledger smoke scripts/fixtures；不改 browser runner。结果：runtime source probe 后输出 no-build handoff marker：`runtime_current_handoff=control_plane_no_build_recreate`、`docker compose -f deploy/docker-compose/docker-compose.yml up -d --no-build --no-deps --force-recreate control-plane`、`build_allowed=false`；source 新于 runtime image 时明确 blocker `runtime_image_requires_rebuild_but_build_forbidden`。验证：auth smoke、ledger contract-only、diff check 通过。剩余缺口：no-build recreate 不能关闭 source-newer-than-image，runtime current 仍需外部 rebuild/重启后复测。
- **E11-007-S5，已完成，2026-06-04，owner Godel**：Admin UI execute-contract v2 呈现与错误流。原因：后端真实执行落地前，UI 需要把 execute contract v2 的预检、dedupe、事务/audit 摘要和阻塞原因展示清楚，避免用户把 plan-only 当成已入账。范围：`web/admin-ui/**`；不改后端。验收：UI 明确区分 dry-run/execute-preflight/blocked/success；错误不泄露 secret；相关 UI 测试和 bundle check 覆盖。结果：Billing/Price 页面已区分 dry-run、execute-preflight、blocked 和 future execute 状态，并展示 v2 transaction/dedupe/audit/safe-output 摘要，未暴露 raw credential 或 idempotency material。剩余缺口：后端真实 execute submit 尚未接通。
- **E11-007-S7，已完成，2026-06-04，owner Godel**：Billing/Price 页面真实 execute 提交流程。原因：S4 后端已支持 `mode=execute` 最小事务 writer，S5 UI 已能展示 future execute states，但页面仍缺真实提交入口和成功/idempotent response 呈现。范围：`web/admin-ui/**`；不改后端/Gateway。验收：UI 在 dry-run fresh 且用户显式操作后调用 `mode=execute`；区分 applied/idempotent/blocked/failed；不回显 dedupe material/raw metadata/credential；相关 UI tests、build、bundle check 通过。结果：Billing/Price 页面新增 fresh dry-run gated 的 `mode=execute` 显式提交，展示 applied/idempotent/blocked/failed 安全摘要，API client 固定同源 execute POST，UI 不渲染 dedupe material、raw metadata、Authorization/Cookie/token/credential。剩余缺口：未跑真实 backend smoke；后端新增 executor summary 字段尚未在 UI 中专门展示。
- **E11-007-S10，已完成，2026-06-04，owner Godel**：Billing/Price 页面展示 ledger executor summary 安全摘要。原因：E9-S7 已准备 `billing_ledger_postgres_executor_summary.v1` 兼容边界，E11-S7 UI 已能执行 `mode=execute`，但页面尚未专门展示 executor summary，用户无法区分 writer outcome、statement count、row-count mismatch 等执行层信号。范围：`web/admin-ui/**`；不改后端/Gateway/routing/scripts。验收：dry-run/execute contract 和 applied/idempotent execute response 中的 `ledger_executor_summary_contract`/`ledger_executor_summary` 有可读安全摘要；展示 schema、executor、outcome、commit/rollback、statement/row count、final statement kind、row-count mismatch；不展示 operation key、error detail、dedupe material、raw metadata、credential；相关 UI tests、build、bundle check 通过。结果：Billing/Price 页面新增 executor summary contract 与 applied/idempotent executor summary 白名单展示，固定 schema/executor/outcome/commit/rollback/statement/row-count 安全字段，过滤 operation key、error detail、dedupe material、raw metadata、Authorization/Cookie/token/credential。剩余缺口：未跑真实后端 smoke；后端后续新增安全字段需显式加入 UI 白名单。
- **E11-007-S12，已完成，2026-06-04，owner Godel**：Billing/Price 页面展示 execute refusal/rollback executor summary。原因：E9-S8 已在 Control Plane execute contract refusal response 中加入 `refused_preflight` summary，并在 transaction contract 中预留 rollback summary contract；E11-S10 只覆盖 applied/idempotent 与 summary contract，错误侧还未专门展示。范围：`web/admin-ui/**`；不改后端/Gateway/routing/scripts/docs。验收：execute_contract refusal response 的 `ledger_executor_summary` 和 rollback executor summary contract 有白名单展示；展示 refused_preflight/refused_rollback、committed/rolled_back、refused statement count、row-count mismatch、error_detail_output omitted；不展示 operation key、raw metadata、raw executor error detail、credential；相关 UI tests、build、bundle check 通过。结果：Billing/Price 页面现在对 execute_contract refusal response 的 `ledger_executor_summary`、`ledger_executor_refusal_summary_contract`、transaction rollback executor summary contract 进行白名单展示，覆盖 refused_preflight/refused_rollback、committed/rolled_back、refused statement count、row-count mismatch、failure output omitted 和 raw executor error omission；API 类型和 UI tests 固定 secret-safe 输出。剩余缺口：未跑真实后端/UI E2E smoke；executor/rollback summary 后续新增字段仍需显式加入 UI 白名单。
- **E11-007-S15，已完成，2026-06-04，owner Godel**：Billing/Price execute freshness/refresh regression。原因：S7/S10/S12 已覆盖 submit 与 executor summary 展示，但真实用户流程还需要锁定 stale dry-run、execute 后刷新和 blocked/failed 状态之间的边界，避免用户把旧 plan 或旧 summary 当作当前入账结果。范围：`web/admin-ui/**`；不改后端/Gateway/routing/scripts/docs。验收：修改调整/退款输入后 execute 操作必须重新回到 stale/disabled 或明确需要重新 dry-run；applied/idempotent execute 成功后刷新相关 ledger view 或明确显示 fresh result 不混入旧 dry-run；blocked/failed/refusal 不覆盖为成功态；所有状态仍保持 executor summary secret-safe；相关 UI tests、build、bundle check 和 diff check 通过。结果：Billing/Price 修改 adjustment/refund 输入后会清掉旧 execute/contract result 并禁用 execute；execute/execute-contract 开始时清除旧 success summary；applied/idempotent 成功后刷新 ledger entries 并显示 `execute_result_fresh=true`、`ledger_entries_refresh_after_execute=success`；后续 failed/refusal 不保留旧 applied summary/executor summary，secret-safe DOM 断言继续覆盖敏感字段。剩余缺口：未跑真实后端 smoke；ledger refresh 失败会显示 refresh failure 并保留 fresh execute result，但失败分支尚缺专门 UI 测试。
- **E11-007-S17，已完成，2026-06-04，owner Godel**：Billing/Price execute refresh failure regression。原因：S15 已实现 execute 后刷新 ledger entries，但 refresh failure 分支目前只靠实现逻辑，没有 UI regression 证明失败时既不丢失 fresh execute result，也不会把 ledger list 失败误读为执行失败。范围：`web/admin-ui/**`；不改后端/Gateway/routing/scripts/docs。验收：mock ledger entries refresh failure 后，applied/idempotent execute result 仍显示 fresh 且 executor summary 保持 secret-safe；UI 明确展示 refresh failure，不把 execute outcome 改成 failed/refusal；旧/stale dry-run guard 仍生效；相关 UI tests、build、bundle check 和 diff check 通过。结果：新增 applied/idempotent 两条 refresh-failure UI regression，证明 execute 成功结果保持 `execute_result_fresh=true`，显示 `ledger_entries_refresh_after_execute=error`，executor summary 仍按白名单展示且不泄露 Authorization/token/credential/operation key/raw metadata/raw executor error；表单修改后旧 execute summary 被清除并回到 stale/disabled guard。剩余缺口：未跑真实后端/UI E2E smoke；refresh failure 仍是 mock 级证据。
- **E11-007-S18，已完成，2026-06-04，owner Godel**：Billing/Price execute backend-smoke readiness regression。原因：S7/S10/S12/S15/S17 已覆盖 UI mock flows，但 E11 最终仍缺真实后端 smoke；在执行 live smoke 前，UI 需要先固定可重复的 readiness regression，证明页面选择器、fresh-result 标记、refresh 状态和 secret-safe 断言足够支撑后续 backend smoke。范围：`web/admin-ui/**`；不改后端/Gateway/routing/scripts/docs。验收：新增或强化 UI test/helper，覆盖 Billing/Price execute smoke 所需的稳定选择器与状态标记（dry-run fresh、execute enabled、execute result fresh、ledger refresh success/error）；断言 applied/idempotent/blocked/failed 不混淆，敏感字段仍不渲染；`npm test -- --run src/App.test.tsx src/api/client.test.ts`、build、bundle check 和 diff check 通过。结果：Billing execute readiness 区域新增稳定 `data-testid`，覆盖 dry-run fresh、execute enabled、execute result fresh、execute outcome、ledger refresh success/error、execute/contract network-call markers；新增 readiness helper 和 blocked execute regression，固定 applied/idempotent/blocked/failed/stale guard 的 smoke selector/state contract，secret-safe DOM 断言继续覆盖敏感字段。剩余缺口：仍未执行真实 backend smoke；下一步需验证真实后端响应中的新增/未知安全字段不会破坏 UI 状态。
- **E11-007-S21，已完成，2026-06-04，owner Godel**：Billing/Price execute backend response tolerance regression。原因：S18 已固定 backend-smoke 选择器和状态标记，但真实后端后续接 billing-ledger runtime writer 时可能增加安全字段或轻微调整 summary payload；UI 需要证明只按白名单读取并忽略未知字段，不把新增安全字段误渲染为 raw JSON 或混淆 outcome。范围：`web/admin-ui/**`；不改后端/Gateway/routing/scripts/docs。验收：新增或强化 UI/API tests，覆盖 applied/idempotent/blocked/failed response 携带未知安全字段、额外 executor summary 字段或 null/missing optional fields 时的渲染；白名单字段可读，unknown/raw metadata/credential/operation key/error detail 不渲染；readiness testids 仍稳定；`npm test -- --run src/App.test.tsx src/api/client.test.ts`、build、bundle check 和 diff check 通过。结果：UI/API tests 覆盖 applied/idempotent tolerant backend response、null/missing optional fields、unknown/extra executor fields，以及 blocked/failed error envelope with data；readiness testids 保持稳定，unknown/raw metadata/credential/operation_key/dedupe/raw executor detail/Authorization/Cookie/token 不渲染。剩余缺口：仍未跑真实 backend smoke；本轮固定 UI/API response tolerance。
- **E11-007-S23，已完成，2026-06-04，owner Godel**：Billing/Price execute live-smoke selector export contract。原因：S18/S21 已固定页面内 selectors 和 response tolerance；后续真实 backend smoke 需要一个可复用的 selector/status contract，避免 smoke 脚本复制易漂移的字符串。范围：`web/admin-ui/**`；不改后端/Gateway/routing/scripts/docs。验收：新增或强化 UI-side selector/status contract export 或 test helper，列出 live smoke 应使用的 `data-testid`、expected status markers 和 forbidden sensitive markers；不改变可见 UI；UI/API tests、build、bundle check、diff check 通过。结果：新增 `ledgerAdjustmentExecuteLiveSmokeContract`，集中导出 live smoke selectors、status markers、refresh markers 和 forbidden sensitive markers；Billing readiness `data-testid` 与 App test helper 改为引用同一 contract，并新增完整性测试。剩余缺口：仍未执行真实 backend smoke；本轮只锁定 UI-side selector/status contract。
- **E11-007-S25，已完成，2026-06-04，owner Godel**：Billing/Price execute live-smoke script handoff contract。原因：S23 已导出 UI-side selector/status contract，但还缺把这些 selector 如何供 smoke 脚本使用的 handoff 说明/测试，避免后续脚本误用可见文案或不稳定 DOM。范围：`web/admin-ui/**`；不改后端/Gateway/routing/scripts/docs。验收：新增或强化 UI-side handoff test/fixture，证明 smoke 应使用 exported selectors、expected status markers、forbidden sensitive markers 和 readiness states；不改变可见 UI；UI/API tests、build、bundle check、diff check 通过。结果：新增 `ledgerAdjustmentExecuteLiveSmokeHandoff`，导出 smoke 脚本应使用的 selectors、status markers、forbidden sensitive markers、script usage 规则和 readiness states；App tests 将页面 readiness 与 handoff states 对齐，覆盖 dry-run required、execute preflight、contract blocked、applied/idempotent refresh success/error、blocked、failed 和 stale plan；不改变可见 UI。剩余缺口：真实 backend live-smoke 脚本仍未接入这些导出的 selectors/readiness states。
- **E11-007-S26，已完成，2026-06-04，owner Godel**：Billing/Price execute live-smoke handoff serialization contract。原因：S25 已导出 UI-side handoff object，但 smoke 脚本后续可能通过 JSON/compiled artifact 消费 contract；需要先锁定 handoff 的可序列化形状，避免 `undefined` marker、函数或不可枚举字段在序列化后丢失并导致脚本误判。范围：`web/admin-ui/**`；不改后端/Gateway/routing/scripts/docs。验收：新增或强化 UI-side serializable handoff export/test，证明 selectors、status markers、readiness states、absent optional markers 和 forbidden sensitive markers 能稳定 JSON 序列化/反序列化；缺省字段使用明确 `null` 或等价稳定标记；不改变可见 UI；UI/API tests、build、bundle check、diff check 通过。结果：`ledgerAdjustmentExecuteLiveSmokeHandoff` 的缺省 optional marker 改为显式 `null`，新增 `ledgerAdjustmentExecuteLiveSmokeSerializableHandoff`、serialization metadata 和 readiness marker key list；测试证明 JSON serialize/parse 后 selectors、status markers、readiness states、forbidden markers 和 absent marker 均稳定保留；不改变可见 UI。剩余缺口：真实 live-smoke 脚本仍未消费该 serializable handoff。
- **E11-007-S28，已完成，2026-06-04，owner Godel**：Billing/Price execute live-smoke selector consumption contract。原因：S26 已提供 UI-side serializable handoff，但真实 smoke 脚本仍未接入；本切片固定脚本侧如何消费 selectors/readiness states，避免脚本复制可见文案或硬编码漂移 DOM。范围：`scripts/**` 中 ledger adjustment execute smoke 相关脚本、必要 `web/admin-ui/**` contract 辅助；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection。验收：新增或强化 smoke script dry-run/contract，证明脚本使用 `ledgerAdjustmentExecuteLiveSmokeSerializableHandoff` 或等价稳定 artifact 的 selectors、readiness states 和 forbidden markers；默认不跑 live backend；不依赖可见 UI 文案；相关 UI tests/build/bundle 与 script diff/default check 通过。结果：新增 `web/admin-ui/src/billingExecuteSmokeContract.serializable.json`，脚本 `-ContractOnly` 读取该 artifact 并校验 selectors、readiness states、forbidden sensitive markers、JSON null optional marker 和 round-trip 稳定性；UI test 断言 TS export 与 JSON artifact 完全一致；默认 smoke 不跑 live backend、不依赖可见 UI 文案。剩余缺口：真实 browser/backend live smoke 还没有实际驱动 Admin UI 页面。
- **E11-007-S31 Smoke，已完成，2026-06-04，owner Godel**：Billing execute browser live-smoke harness preflight。原因：S28 已让脚本消费 UI handoff artifact，但还没有真正证明 browser/backend live smoke 的启动条件、选择器驱动和性能摘要可用。范围：`scripts/**` 中 ledger adjustment execute smoke 相关脚本、必要 `web/admin-ui/**` contract 辅助；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection。验收：新增或强化 browser live-smoke preflight/dry-run，检查 Playwright/browser tooling、Admin UI URL、Control Plane backend URL、handoff artifact freshness；默认不跑 live backend；输出 readiness、submit latency、ledger refresh duration 或明确 unavailable marker；不依赖可见文案、不泄露 secret；相关 script default/contract check、UI tests/build/bundle 通过。结果：UI handoff artifact 新增 `browserPreflight` contract，script contract-only 路径校验 Admin UI/Control Plane safe URL、handoff freshness、node/Playwright tooling，并输出 `browser_smoke_readiness`、`submit_latency_ms`、`ledger_refresh_duration_ms`；默认不访问 live backend、不依赖可见文案，缺 Playwright 时明确输出 `unavailable:playwright`。剩余缺口：真实 browser/backend E2E smoke 仍未执行；本片只关闭 harness preflight/perf contract。
- **E11-007-S32 Smoke，已完成，2026-06-04，owner Godel**：Billing execute browser smoke service readiness gate。原因：S31 已固定 browser harness preflight，但真实 E2E 前还需要把 Admin UI/Control Plane service readiness、health probing、auth/session material omission 和 failure classification 固定，避免 live smoke 因环境缺失被误读为 UI 失败。范围：`scripts/**` 中 ledger adjustment execute smoke 相关脚本、必要 `web/admin-ui/**` contract 辅助；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection。验收：新增或强化 live service readiness dry-run/preflight，覆盖 Admin UI reachable/unreachable、Control Plane health reachable/unreachable、session/token present marker without echo、browser tooling available/unavailable、bounded timeout/duration output、secret-safe blocker classification；默认不跑 live mutation；相关 script check、UI tests/build/bundle 通过。结果：browser smoke handoff 增加 service readiness gate，脚本 contract-only 路径只读 probe Admin UI `/` 与 Control Plane `/healthz`，输出 `admin_ui_reachable`、`control_plane_health_reachable`、`service_probe_timeout_ms`、`service_readiness_duration_ms`、`session_material_present`、`session_material_echoed=false` 和 `service_blocker`；当前本机缺 Playwright/服务时归类为 `browser_tooling_unavailable+admin_ui_unreachable+control_plane_health_unreachable`，不做 live mutation、不回显 token。剩余缺口：真实 apply/idempotent/refund browser E2E 仍需后续 live 环境验收。
- **E11-007-S34 Smoke，已完成，2026-06-04，owner Godel**：Billing execute browser action plan dry-run contract。原因：S32 已固定服务 readiness gate；下一步在不执行 live mutation 的前提下固定 browser E2E 会如何驱动 dry-run、execute、idempotent replay、refund refusal 和 ledger refresh，避免真正 live 前 action sequence 依赖可见文案或不稳定 DOM。范围：`scripts/**` 中 ledger adjustment execute smoke 相关脚本、必要 `web/admin-ui/**` contract 辅助；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection。验收：新增或强化 browser action plan dry-run/contract，列出 stable selectors、expected states、mutation opt-in gate、action duration markers、failure classification 和 forbidden sensitive markers；默认不点击/不提交 live mutation；script check、UI tests/build/bundle 通过。结果：UI handoff artifact 新增 `browserActionPlan`，固定 dry-run、execute apply、idempotent replay、refund refusal、ledger refresh 的 stable selector、expected readiness state、live mutation opt-in gate、failure classification 与 duration markers；Billing dry-run form/input/button 增加 smoke-only `data-testid`；脚本 contract-only 输出 `browser_action_plan_live_mutation_enabled`、`browser_action_plan_failure_classification`、`dry_run_plan_duration_ms`、`execute_apply_duration_ms`、`idempotent_replay_duration_ms`、`refund_refusal_duration_ms` 和 `ledger_refresh_duration_ms`，默认不点击/不提交 live mutation，显式 env opt-in 也仅关闭 contract gate 不执行 live。剩余缺口：真实 browser E2E 仍需 live Admin UI、Control Plane、Playwright/session 与 mutation 环境验收。
- **E11-007-S36 Smoke，已完成，2026-06-04，owner Godel**：Billing execute browser E2E live opt-in/runbook gate。原因：S34 已固定 action plan dry-run，但真正 live 前还缺一个明确的 opt-in run command gate 和 blocker matrix，把 Playwright、Admin UI、Control Plane、session material、mutation opt-in 与性能 duration evidence 统一起来，避免默认路径误触发真实账务 mutation。范围：`scripts/**` 中 ledger adjustment execute smoke 相关脚本、必要 `web/admin-ui/**` contract 辅助；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection/OpenAPI semantic。验收：新增或强化 live opt-in gate/runbook contract，输出可复制的 live run command、required env/flag matrix、mutation disabled 默认、session material present/echo omitted、service/browser/tooling blocker classification、action duration evidence 名称；默认仍 contract-only/dry-run；script check、UI tests/build/bundle 通过。结果：新增 `browserLiveRunbook` handoff contract，脚本 `-ContractOnly` 输出可复制 live run command、required env/flag matrix、`browser_live_mutation_enabled`、`browser_live_session_material_present`、`browser_live_session_material_echoed=false`、service/browser/session/mutation blocker classification，以及 `service_readiness_duration_ms`、`submit_latency_ms`、`dry_run_plan_duration_ms`、`execute_apply_duration_ms`、`idempotent_replay_duration_ms`、`refund_refusal_duration_ms`、`ledger_refresh_duration_ms` evidence 名称；默认 mutation disabled，显式 env+flag 只关闭 gate，不执行真实账务 mutation，也不回显 session material。剩余缺口：真实 apply/idempotent/refund browser E2E 仍需具备 live Admin UI、Control Plane、Playwright/session 与 mutation 环境后验收。
- **E11-007-S38 Smoke，已完成，2026-06-04，owner Godel**：Billing execute browser live E2E blocker/pass evidence artifact。原因：S36 已给出 live opt-in command gate，但下一步需要把真实 browser E2E 的 blocker/pass/fail evidence artifact 形状固定下来，确保即使缺 Playwright/Admin UI/Control Plane/session，也能产出可审查、secret-safe、带性能 duration 名称的证据；具备 live 环境时同一 artifact 能记录 apply/idempotent/refund/refresh 的 pass evidence。范围：`scripts/**` 中 ledger adjustment execute smoke 相关脚本、必要 `web/admin-ui/**` contract 辅助；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection/OpenAPI semantic。验收：新增或强化 live E2E evidence artifact contract，覆盖 blocked/pass/fail 三态、per-action duration fields、service readiness duration、mutation opt-in/session/tooling/service blocker matrix、secret-safe command summary、artifact freshness/provenance；默认不执行 live mutation、不写真实账务；script check、UI tests/build/bundle 通过。结果：新增 `billing_execute_browser_live_e2e_evidence.v1` handoff contract 和 dry-run artifact 输出，固定 `blocked`/`failed`/`passed` 三态、service readiness/submit/dry-run/apply/idempotent/refund/refresh duration 字段、tooling/service/session/mutation matrix、per-action expected state、provenance 和 secret-safe summary；默认缺 Playwright/Admin UI/Control Plane/session/mutation opt-in 时输出 blocked evidence，不执行真实账务 mutation，显式 env+flag 也只关闭 gate 并保持 session/token 不回显。验证：script contract、opt-in secret-safe contract、`scripts\test.ps1 -ControlPlaneLedgerAdjustmentExecuteSmokeOnly`、build、bundle 和 scoped diff check 通过；全量 Admin UI test 当前被并行 E13-S23 未验收变更中的重复 `blocked` 文本断言打断，已反馈给 Pauli 修正，S38 提交不包含该 E13 hunk。剩余缺口：真实 apply/idempotent/refund/ledger refresh browser E2E pass 仍需具备 live Admin UI、Control Plane、Playwright/session 与 mutation 环境后验收。
- **E11-007-S40 Smoke，已完成，2026-06-04，owner Godel**：Billing execute browser live runner readiness gate。原因：S38 已固定 blocker/pass/fail evidence artifact，下一步需要固定真实 browser runner 何时允许驱动 Admin UI actions，如何在默认 no-mutation 下只做 readiness/blocker evidence，以及具备 Playwright/服务/session/mutation opt-in 时如何把 per-action duration 写回同一 artifact。范围：`scripts/**` 中 ledger adjustment execute smoke 相关脚本、必要 `web/admin-ui/**` contract 辅助；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection/OpenAPI semantic。验收：新增或强化 browser runner readiness gate，覆盖 Playwright install/browser availability、Admin UI/Control Plane URL safety、session/mutation opt-in guard、action selector readiness、default no-click/no-mutation behavior、per-action duration capture names、artifact write/read freshness、secret-safe output；script check、UI tests/build/bundle 通过。结果：新增 `browserRunnerReadiness` handoff contract，固定 `runner_readiness_only` 默认模式、Playwright/Admin UI/Control Plane/session/mutation opt-in/action selector readiness guard、默认不点击 Admin UI actions、不写 live mutation；runner readiness 输出 `actions_allowed`、`browser_available`、`session_material_present`、`mutation_opt_in_enabled`、`selector_readiness`、`no_mutation_default` 和 `artifact_roundtrip_fresh`，并通过 `browser_runner_evidence_json` round-trip 同一 `billing_execute_browser_live_e2e_evidence.v1` artifact；duration capture 名称与 `service_readiness_duration_ms`、`submit_latency_ms`、`dry_run_plan_duration_ms`、`execute_apply_duration_ms`、`idempotent_replay_duration_ms`、`refund_refusal_duration_ms`、`ledger_refresh_duration_ms` 对齐，输出保持 secret-safe。验证：`verify_control_plane_ledger_adjustment_execute_smoke.ps1 -ContractOnly -BrowserProbeTimeoutMilliseconds 120`、带 session/mutation opt-in 的 contract run、`scripts/test.ps1 -ControlPlaneLedgerAdjustmentExecuteSmokeOnly`、`npm test -- --run src/App.test.tsx src/api/client.test.ts`、`npm run build`、`npm run check:bundle` 和 diff check 通过。剩余缺口：真实 apply/idempotent/refund browser E2E pass 仍需 Playwright、Admin UI、Control Plane、session 和 mutation opt-in live 环境验收。
- **E11-007-S42 Smoke，已完成，2026-06-04，owner Godel**：Billing execute browser runner artifact write/read opt-in path。原因：S40 已固定 runner readiness gate 和 `browser_runner_evidence_json` round-trip，但真实 browser live smoke 还需要一个明确的 opt-in artifact write/read 路径，证明具备 Playwright/服务/session/mutation opt-in 后如何把 dry-run/apply/idempotent/refund/ledger refresh 的 duration 与 outcome 写入同一 evidence artifact，并在默认模式下只产出 blocker/readiness 证据。范围：`scripts/**` 中 ledger adjustment execute smoke 相关脚本、必要 `web/admin-ui/**` Billing smoke contract；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection/OpenAPI semantic。验收：新增或强化 browser runner artifact write/read opt-in contract，覆盖 artifact path safety、write disabled by default、explicit write flag/env、JSON schema round-trip、per-action outcome/duration capture、stale artifact refusal、secret-safe session/request/material omission、missing Playwright/Admin UI/Control Plane/session/mutation blocker 输出；script check、UI targeted tests/build/bundle 和 diff check 通过。结果：browser evidence artifact contract 新增 `freshness` 顶层字段；runner readiness 新增 explicit opt-in artifact write/read gate，默认不写 artifact，`-BrowserEvidenceArtifactWriteOptIn` 或 `CONTROL_PLANE_LEDGER_ADJUSTMENT_EXECUTE_BROWSER_ARTIFACT_WRITE=1` 才写入 repo 内 bounded path；readback 会校验 provenance、freshness、matrix、duration field names、per-action `outcome`/`duration_ms`、secret-safe；old commit、old generated_at、handoff stale 和缺 freshness 均被拒绝；repo 外路径按预期非零退出并输出 path refusal。剩余缺口：真实 browser E2E mutation pass 仍需具备 Playwright/Admin UI/Control Plane/session/mutation opt-in 的 live 环境运行。
- **E11-007-S44 Smoke，已完成，2026-06-04，owner Godel**：Billing execute browser action runner dry-run DOM plan boundary。原因：S42 已有 artifact write/read freshness gate，但真实 browser live smoke 还缺一个明确的 Playwright/DOM action runner dry-run 边界，说明 selector、step ordering、mutation guard、duration capture 与 artifact emission 如何从 contract 走向可执行 browser runner。范围：`scripts/**` 中 ledger adjustment execute smoke/browser runner 相关脚本、必要 `web/admin-ui/**` Billing smoke contract；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection/OpenAPI semantic。验收：新增或强化 dry-run DOM action runner boundary，覆盖 default no-click/no-mutation、Playwright unavailable blocker、selector availability summary、step ordering dry-run、per-step planned timeout/duration field mapping、artifact emission handoff、secret-safe URL/session/request omission；script contract checks、Admin UI targeted tests/build/bundle 和 diff check 通过。结果：新增 `browserDomActionRunner` handoff contract 和 script dry-run boundary，固定 `dom_action_runner_dry_run_only` 默认模式、no-click/no-mutation、Playwright unavailable blocker、selector availability summary、5 个 action step order、每步 5000ms planned timeout、duration field mapping、artifact emission 到 `billing_execute_browser_live_e2e_evidence.v1`/`browser_runner_evidence_json`，以及 URL credential/session/request material 不回显；contract-only smoke、test gate、Admin UI targeted tests、build、bundle 和 diff check 通过。剩余缺口：真实 browser mutation pass 仍需后续 live Playwright/Admin UI/Control Plane/session/mutation opt-in 环境运行。
- **E11-007-S46 Smoke，已完成，2026-06-04，owner Godel**：Billing execute browser live Playwright launch/readiness command boundary。原因：S44 已完成 dry-run DOM plan boundary，但真实 browser live smoke 还缺 Playwright launch/readiness command boundary 来证明 browser/context/page/selector readiness 和性能 duration evidence 的可执行形状，同时默认不能点击或提交 live mutation。范围：`scripts/**` 中 ledger adjustment execute smoke/browser runner 相关脚本、必要 `web/admin-ui/**` Billing smoke contract 辅助；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection/OpenAPI semantic。验收：新增或强化 Playwright launch/readiness boundary，覆盖 browser launch availability、context/page readiness、safe URL classification、selector snapshot readiness、default no-click/no-mutation；evidence 包含 `browser_launch_duration_ms`、`context_setup_duration_ms`、`page_ready_duration_ms`、`selector_snapshot_duration_ms`、`service_readiness_duration_ms`、`mutation_allowed=false`；缺 Playwright/服务/session/mutation opt-in 输出 blocker，不伪造 pass；不回显 URL credentials/session token/request material；contract-only smoke、test gate、UI targeted tests/build/bundle（如触及 UI）和 scoped diff check 通过。结果：Billing smoke handoff 新增 `browserPlaywrightLaunchReadiness` contract，script 输出 browser/context/page/selector readiness、safe Admin UI/Control Plane URL 分类、`mutation_allowed=false`、browser/context/page/selector/service duration 字段、artifact marker 和 secret-safe omission；缺 Playwright/服务/session/mutation opt-in 时输出 blocker，不伪造 pass，不点击、不提交 live mutation。验证：`npm test -- --run src/App.test.tsx src/api/client.test.ts`、`powershell -File scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1 -ContractOnly -BrowserProbeTimeoutMilliseconds 120`、`powershell -File scripts/test.ps1 -ControlPlaneLedgerAdjustmentExecuteSmokeOnly`、`npm run build`、`npm run check:bundle`、带 userinfo 的 `ADMIN_UI_BASE_URL` 负向检查和 scoped diff check 通过。剩余缺口：真实 apply/idempotent/refund mutation pass 仍需 live 环境验收。
- **E11-007-S49 Smoke，已完成，2026-06-04，owner Godel**：Billing execute browser live mutation pass artifact closure gate。原因：S46 已固定 Playwright launch/readiness boundary，但真实 browser E2E 仍没有 pass artifact closure gate；下一片需要定义具备 Playwright/Admin UI/Control Plane/session/mutation opt-in 后，apply/idempotent/refund/refresh 何时能把 artifact 判为 pass，以及缺任一条件时如何保持 blocker。范围：`scripts/**` 中 ledger adjustment execute smoke/browser runner 相关脚本、必要 `web/admin-ui/**` Billing smoke contract 辅助；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection/OpenAPI semantic。验收：新增或强化 browser live mutation pass artifact closure gate，覆盖 apply/idempotent/refund/ledger refresh outcome、per-action duration、service/browser/session/mutation readiness、artifact write/read freshness、safe URL/session omission、default no mutation；缺 Playwright/Admin UI/Control Plane/session/mutation opt-in -> blocker，不伪造 pass；contract-only smoke、test gate、UI targeted tests/build/bundle 和 scoped diff check 通过。结果：新增 `browserMutationPassArtifactClosure` handoff contract 和 script closure gate，固定 `mutation_pass_artifact_closure_gate`、required readiness、artifact freshness/readback、expected action outcomes、duration requirements、`closure_eligible` marker 与 default no-mutation/no-live-gap-close；contract-only 当前缺 Playwright/Admin UI/Control Plane/session/mutation opt-in 时保持 blocker，synthetic complete artifact 仅用于证明 gate 条件，不伪造真实 pass。验证：`npm test -- --run src/App.test.tsx src/api/client.test.ts`、`powershell -File scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1 -ContractOnly -BrowserProbeTimeoutMilliseconds 120`、`powershell -File scripts/test.ps1 -ControlPlaneLedgerAdjustmentExecuteSmokeOnly`、`npm run build`、`npm run check:bundle`、带 userinfo 的 `ADMIN_UI_BASE_URL` 负向检查和 scoped diff check 通过。剩余缺口：真实 live mutation pass 仍需具备 Playwright/Admin UI/Control Plane/session/mutation opt-in 环境后运行。
- **E11-007-S51 Smoke，已完成，2026-06-04，owner Godel**：Billing execute browser live runner execution bridge。原因：S49 已固定 pass artifact closure gate，但缺少从 readiness/blocker 到真实 Playwright runner command 的受控 execution bridge；下一片需要列出 exact live command、preflight blockers、artifact path、session/mutation opt-in 和 action duration capture，让有环境的人能一键跑真实 browser E2E。范围：`scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1`、`web/admin-ui/src/billingExecuteSmokeContract.ts`、`web/admin-ui/src/billingExecuteSmokeContract.serializable.json`、`web/admin-ui/src/App.test.tsx`；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection/OpenAPI semantic。验收：新增或强化 browser live runner execution bridge，默认不点击、不提交 mutation；显式 env/flag 才输出/运行受控 bridge；缺 Playwright/Admin UI/Control Plane/session/mutation -> blocker；输出 exact command、artifact write/read path、per-action timeout/duration names、safe URL/session omission；contract-only smoke、test gate、UI tests/build/bundle/scoped diff check 通过。结果：新增 `browserLiveRunnerExecutionBridge` handoff contract 和 script gate，输出 exact live command、bounded artifact path、artifact write/read opt-in、service/browser/session/mutation blockers、safe URL/session omission 与 action duration names；默认 no-click/no-mutation，当前缺 Playwright/Admin UI/Control Plane/session/mutation/artifact-write opt-in 时保持 blocker。剩余缺口：真实 browser runner live mutation pass 仍需具备 Playwright/Admin UI/Control Plane/session/mutation/artifact-write opt-in 环境后运行。
- **E11-007-S53 Smoke，已完成，2026-06-04，owner Godel**：Billing execute browser runner live pass artifact readback gate。原因：S51 已给出 exact live runner bridge command，但缺少真实/模拟 pass artifact readback 如何关闭 browser E2E gap 的最终 gate；下一片需要固定 pass artifact 的 readback 判定，确保缺 action outcome/duration/freshness/session/mutation 时不能 closure。范围：`scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1`、必要 `web/admin-ui/**` Billing smoke contract/tests；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection/OpenAPI semantic。验收：默认不点击、不提交 mutation；artifact readback gate 支持 blocked/pass/fail 三态；pass 需要 apply/idempotent/refund/ledger refresh outcome、per-action duration、service/browser duration、freshness current、artifact write opt-in、session/mutation readiness；缺任一条件 -> blocker/fail；safe URL/session/request omission；contract-only smoke、test gate、UI tests/build/bundle/scoped diff check 通过。结果：新增 `browserLivePassArtifactReadbackGate` handoff contract 和 script gate，固定 blocked/pass/fail、action outcome/duration、service/browser duration、freshness current、artifact write/read、session/mutation readiness 和 secret-safe omission；默认不读写 pass artifact、不点击、不提交 mutation，缺 live 条件时保持 blocker。剩余缺口：真实 browser mutation pass 仍需后续 live 环境运行。
- **E11-007-S55 Smoke，已完成，2026-06-04，owner Godel，加大切片**：Billing execute browser live environment bootstrap + mutation/pass artifact attempt。结果：新增 browser live environment bootstrap attempt contract、Admin UI dev-server helper、artifact live runner/readback attempt shape；默认不启动、不安装、不写 artifact、不点击、不 mutation。Docker/Admin UI/Control Plane 可用后，加入本地 `@playwright/test` devDependency 并安装 Chromium，`verify_control_plane_ledger_adjustment_execute_smoke.ps1 -ContractOnly -BrowserPreflight -ComposeFile deploy/docker-compose/docker-compose.live.yml` 显示 `browser_tooling=available`、Admin UI/Control Plane reachable、browser launch ready；剩余 blocker 收敛为 `session_material_missing + live_mutation_opt_in_missing`。验证：smoke gate、UI tests/build/bundle/diff check 通过。剩余缺口：真实 apply/idempotent/refund/refresh mutation pass 需要 admin session 和显式 mutation/artifact opt-in。
- **E11-007-S57 Smoke，已完成，2026-06-04，owner Godel，加大切片**：Billing execute browser live mutation pass with admin session handoff。结果：新增 safe admin session handoff contract，只接受 `CONTROL_PLANE_ADMIN_SESSION_TOKEN`，不回显 token/cookie/header；opt-in browser live mutation runner 必须同时具备 session、mutation/artifact/runner flags、Playwright/Admin UI/Control Plane readiness，使用 Playwright route 注入 `X-Admin-Session`，并能写回 apply/idempotent/refund/ledger refresh outcome 与 duration 到同一 artifact。验证：smoke gates、Admin UI tests 95 passed、build/bundle/diff check 通过；本地 `/admin/auth/login` 返回 401，未执行真实 mutation。剩余缺口：有效 admin session 后重跑 opt-in command 才能关闭真实 browser mutation pass。
- **E11-007-S59 Smoke，已完成，2026-06-04，owner Godel，加大切片**：Billing execute browser mutation pass with valid session material。原因：S57 已有安全 runner，唯一实际 blocker 是有效 session。范围：`scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1`、必要 `web/admin-ui/**` Billing smoke tests；不改 Gateway/E8/E9 backend/OpenAPI/E13。验收：复用 S58 session handoff；若拿到有效 token，运行 opt-in browser mutation pass artifact；否则输出 login/session blocker、可复制命令和不泄密 evidence；UI tests/build/bundle/smoke/diff check 通过。结果：browser live runner 在显式 mutation/artifact/runner opt-in 下会安全消费已验证 admin session，并临时发布到 runner env；session/token 不进入日志、artifact、URL 或 DOM。当前服务和 Playwright ready，但无有效 admin session，最终状态精确收敛为 `session_material_missing` / dev login 401 blocker。剩余缺口：提供有效 `CONTROL_PLANE_ADMIN_SESSION_TOKEN` 后重跑 opt-in browser mutation command，才能关闭真实 mutation pass。
- **E11-007-S61 Smoke，已完成/阻塞收束，2026-06-04，owner Godel，加大切片**：Billing execute real browser mutation pass after session repair。原因：S59 已把 runner 准备好，S60 会修复或输出 session handoff；本片负责在有效 session 后跑真实 browser mutation pass artifact/readback。范围：`scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1`、必要 `web/admin-ui/**` Billing smoke tests；不改 E8/E9 backend/E13。结果：`-BrowserPreflight` 现在保持 preflight-only；live runner 增加内部 deadline、外层 PowerShell 90s timeout、Playwright timeout、失败时 close browser、清理临时 runner；pass closure gate 要求所有 browser/context/page/selector/action duration 为数值，`unavailable` 不再能关闭 live gap；failure artifact 输出安全 `error_class`。验证：contract-only、bounded live attempt、UI targeted tests/build/bundle 通过。剩余缺口：bounded live artifact 仍 failed，当前 blocker 为 runtime idempotent replay mismatch 与 selector snapshot timeout，不能标 `[x]`。
- **E11-007-S63 Smoke/Selector，已完成/阻塞收束，2026-06-04，owner Godel，加大切片**：Billing execute browser selector snapshot diagnostic。原因：S61 已防止 runner 挂住并拒绝弱证据，但 page_ready 后 selector snapshot 仍 timeout；下一步需要定位缺失 selector 并输出 bounded missing-selector evidence。范围：`scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1`；不改 E8/E9/E13。结果：定位 selector snapshot timeout 是 runner 初始 snapshot 错误要求 execute 后才出现的 `executeOutcome`、`executeResultFresh`、`ledgerRefreshStatus`；初始 snapshot 改为只检查表单/按钮基础 selector，execute 后 selector 仍在对应 action 后等待；failure artifact 增加 `selector_snapshot.missing_selector_keys` / count，runner timeout/catch 保留已采集 action evidence。验证：contract-only、bounded live runner attempt、`scripts/test.ps1 -ControlPlaneLedgerAdjustmentExecuteSmokeOnly`、diff check 通过。剩余缺口：selector snapshot 已关闭，但 idempotent replay 阶段仍受 runtime stale/旧行为影响，artifact outcome failed。
- **E11-007-S65 Smoke/Replay，已完成/阻塞收束，2026-06-04，owner Godel，加大切片**：browser replay pass after runtime-current。原因：S63 已关闭 selector 缺口，下一步集中在 idempotent replay 阶段 pass closure。范围：E11 ledger smoke script/UI tests；不改 E8/E9/E13。结果：live mutation runner 先经过 runtime-current gate；runtime stale/unverified 时直接 blocker，不进入 browser runner、不伪造 pass；artifact 增加 `browser_runner.failed_action`，idempotent replay 阶段失败/超时分类为 `browser_idempotent_replay_failed` 并尽量记录 replay duration；closure gate 仍拒绝 unavailable/non-numeric durations。验证：contract-only、bounded live attempt、`scripts/test.ps1 -ControlPlaneLedgerAdjustmentExecuteSmokeOnly`、diff check 通过。剩余缺口：runtime image 仍 stale，browser replay pass closure 需 runtime current 后复测。
- **E11-007-S66 Smoke/Replay，已完成/阻塞收束，2026-06-05，owner Socrates**：browser replay/runtime-current/mutation artifact readback classification。原因：S65 已有 runtime-current gate，但真实 pass artifact仍缺稳定 readback 分类，idempotent replay failure 也需要可复跑 evidence。范围：`scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1`、`web/admin-ui/src/billingExecuteSmokeContract.ts`、`web/admin-ui/src/billingExecuteSmokeContract.serializable.json`、`web/admin-ui/src/App.test.tsx`。结果：browser evidence artifact 新增 `runtime_current`、`replay`、`mutation_pass_artifact`、`readback`、`failure` 分类；runtime 未 verified 时不能 closure；readback 区分 not_requested/missing/failed/passed；bounded live attempt 当前仍被 `runtime_image_stale_or_unverified` 阻断。验证：contract-only smoke、Admin UI targeted test/build/bundle、scoped diff check 通过。剩余缺口：runtime-current 后带有效 admin session 重跑真实 browser mutation pass，并要求 `mutation_pass_artifact_passed` + `artifact_readback_passed`。
- **E11-007-S67 Smoke/Runtime，已完成/阻塞收束，2026-06-05，owner Socrates**：runtime-current no-build/rebuild handoff evidence。原因：S66 已把 browser mutation pass closure 绑定到 runtime-current verified，但当前 blocker 是 source newer than runtime image，需要可复跑 operator handoff/readback 分类。范围：`scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1`、`web/admin-ui/src/billingExecuteSmokeContract.ts`、`web/admin-ui/src/billingExecuteSmokeContract.serializable.json`、`web/admin-ui/src/App.test.tsx`。结果：新增 `runtimeCurrentHandoff` smoke contract，输出 no-build recreate、rebuild handoff、readback、operator command 和 secret-safe evidence JSON；contract-only 模拟 source newer、container recreate available、container unavailable、docker unavailable，默认不 build、不 mutation；当前 live gate 仍 blocker：`source_newer_than_runtime_image` / `runtime_image_requires_rebuild_but_build_forbidden`。验证：contract-only smoke、Admin UI targeted test/build/bundle、full fmt、diff check 通过。剩余缺口：operator rebuild/recreate 后验证 `runtime_current_verified`，再跑真实 browser mutation pass。
- **E11-007-S68 Smoke/Runtime，已完成/阻塞收束，2026-06-05，owner Parfit**：runtime-current rebuild/recreate opt-in execution/readback gate。原因：S67 只输出 operator handoff，下一步需要显式 opt-in gate 来区分 no-build recreate execution/readback 与 rebuild handoff，不允许默认 build/mutation。范围：`scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1`、`web/admin-ui/src/billingExecuteSmokeContract.ts`、`web/admin-ui/src/billingExecuteSmokeContract.serializable.json`、`web/admin-ui/src/App.test.tsx`。结果：新增 `-RuntimeCurrentRebuildHandoffOptIn` / env gate；默认不 build、不 recreate、不 mutate；no-build recreate 仅显式 opt-in 执行并强制 evidence readback；rebuild opt-in 只记录 operator handoff，`rebuild_handoff_execution_allowed=false`；browser mutation pass gate 仍要求 `runtime_current_verified`、session/mutation/artifact opt-in 和 artifact readback pass。验证：contract-only smoke、runtime-current rebuild/no-build simulations、Admin UI targeted test/build/bundle、full fmt、diff check 通过。剩余缺口：operator rebuild/recreate 后验证 `runtime_current_verified`，再跑真实 browser mutation pass 并要求 `mutation_pass_artifact_passed` + `artifact_readback_passed`。
- **E11-007-S69 Smoke/Runtime，已完成/阻塞收束，2026-06-05，owner Parfit**：runtime-current verified 后的 browser mutation runner unblock gate。原因：S68 固定 rebuild/recreate opt-in gate 后，需要明确 runtime-current artifact 如何解锁 browser runner 但不自动执行 mutation。结果：新增 runtime-current verified artifact/readback 解锁 browser runner gate；默认不 build、不读 artifact、不运行 browser runner、不 mutate。显式 runtime-current artifact readback 只有 verified/current evidence 才能把 blocker 从 runtime-current 转到 session/mutation/artifact gate，stale/missing artifact 继续阻断且不伪造 pass。验证：contract-only smoke、runtime-current artifact verified/stale simulations、Admin UI targeted tests/build/bundle、scoped diff check 通过。剩余缺口：operator rebuild/recreate 后提供真实 `runtime_current_verified` artifact/readback，再带有效 admin session 和 mutation/artifact opt-in 跑真实 browser mutation pass。
- **E11-007-S70 Browser Mutation DoD，已完成/阻塞收束，2026-06-05，owner Parfit**：真实 browser mutation pass final DoD checklist 与 artifact acceptance matrix。原因：S69 已有 runtime-current unlock gate，但最终 `[x]` 还需要明确 session/mutation/readback evidence 条件。结果：新增真实 browser mutation pass final DoD checklist 与 artifact acceptance matrix，机器可读固定 `runtime_current_verified`、admin session secret-safe、mutation/write/readback opt-in、apply/idempotent/refund/ledger refresh outcome、numeric durations、artifact freshness/current commit 与 secret-safe omission；stale/missing/simulated/browser unavailable/session missing 均不能 close E11。验证：contract-only smoke、Control Plane smoke gate、Admin UI targeted tests/build/bundle、cargo fmt、scoped diff check 通过。剩余缺口：operator rebuild/recreate 后提供 `runtime_current_verified`，并带有效 admin session 跑真实 browser mutation artifact readback pass。
- **E11-007-S71 Mutation Runner Artifact DoD，已完成/阻塞收束，2026-06-05，owner Parfit**：真实 browser mutation runner artifact schema、UI/API/ledger readback fields 与 failure taxonomy。原因：S70 固定 final DoD 后，需要让 artifact 字段不足在有效 session 前就失败，避免后续空转。结果：扩展 `billing_execute_browser_live_e2e_evidence.v1` artifact schema；新增 required top-level sections `runtime_current_artifact`、`session_verification`、`mutation_controls`、`api_readback`、`ledger_readback`、`failure_taxonomy`；固定 dry-run/apply/idempotent/refund/ledger refresh 的 API/UI/ledger readback fields 与 `session_missing`、`runtime_stale`、`mutation_opt_in_missing`、`artifact_write_missing`、`artifact_readback_failed`、`idempotent_replay_failed`、`refund_refusal_missing`、`ledger_refresh_missing`、`duration_non_numeric`、`stale_or_simulated_artifact`、`browser_unavailable` taxonomy。验证：contract-only smoke、Control Plane smoke gate、Admin UI targeted tests/build/bundle、cargo fmt、scoped diff check 通过。剩余缺口：operator rebuild/recreate 后提供 `runtime_current_verified`，并用有效 admin session 跑真实 browser mutation artifact pass/readback。
- **E11-007-S72 Runtime Current Operator Handoff Pack，已完成/阻塞收束，2026-06-05，owner Parfit**：runtime-current operator handoff pack。原因：S71 固定 mutation runner artifact schema 后，还需要把 operator rebuild/recreate 后如何生成 `runtime_current_verified`、admin session marker、mutation/artifact opt-in 与 browser runner command 串成可复跑流程。结果：新增 `runtimeCurrentOperatorHandoffPack` contract，固定 runtime-current artifact write/readback、admin session marker、mutation/artifact opt-in、bounded browser runner command、artifact readback 顺序与 failure taxonomy；默认不 build/recreate/mutate/session，缺 opt-in 只输出 blocker/command summary。State definitions 覆盖 `runtime_current_handoff_ready`、`mutation_runner_ready_blocked`、`mutation_pass_artifact_passed`、`e11_x`。验证：Control Plane smoke gate、Admin UI targeted tests/build/bundle、Prettier check、scoped diff check 通过。剩余缺口：真实 `runtime_current_verified` artifact/readback、有效 admin session、真实 browser mutation artifact pass/readback。
- **E11-007-S73 Runtime Current Evidence Acceptance Matrix，已完成/阻塞收束，2026-06-05，owner Parfit**：runtime-current + mutation evidence acceptance matrix。原因：S72 固定 handoff pack 后，需要让真实 runtime-current/browser artifact 到来时机器判定 accepted/refused。结果：新增 `runtimeCurrentEvidenceAcceptanceMatrix` contract，覆盖 runtime artifact provenance/current commit/source-runtime timestamp comparison、admin session marker secret-omitted、mutation controls、browser artifact provenance、per-action/readback/durations、API/ledger readback、failure classification、secret-safe omission；accepted-shape simulations 全部 `simulation_can_mark_final_x=false`，只可证明 shape/review gate。状态区分 `runtime_current_evidence_accepted_for_review`、`mutation_runner_ready_blocked`、`mutation_pass_artifact_passed` 与最终 `e11X`。验证：Control Plane smoke gate、Admin UI tests/build/bundle、Prettier、scoped diff check 通过。剩余缺口：真实 `runtime_current_verified` artifact/readback、有效 admin session、真实 browser mutation artifact pass/readback。
- **E11-007-S74 Browser Mutation Final Closure Audit，已完成/阻塞收束，2026-06-05，owner Parfit**：browser mutation final closure audit。原因：E11 acceptance matrix 已在位，需要单一 closure report 判断最终 `[x]` eligibility。结果：新增 `runtimeCurrentFinalClosureAudit` / `billing_execute_browser_mutation_final_closure_audit.v1`，输出 `final_x_eligible`、blocking reasons、required evidence、runtime/session/mutation/browser/API/ledger/duration/secret-safe states、generated_at/current_commit 与 exact next commands。当前无真实 runtime-current artifact、admin session、browser mutation artifact 时 `final_x_eligible=false`；accepted-shape simulation 仍 `simulation_can_mark_final_x=false`，不能关闭 E11。验证：Control Plane smoke gate、Admin UI tests/build/bundle、Prettier、scoped diff check 通过。剩余缺口：真实 `runtime_current_verified` artifact、有效 admin session、真实 browser mutation artifact pass/readback。
- **E11-007-S75 Browser Mutation Evidence Watcher And Final Guard Review，已完成/阻塞收束，2026-06-05，owner Parfit**：browser mutation evidence watcher/final guard。原因：E11 final closure audit 已在位，需要稳定入口等待真实 runtime/session/browser artifact，并防止 simulation/no-artifact 误关。结果：新增 `browserMutationEvidenceWatcherFinalGuard` / `billing_execute_browser_mutation_evidence_watcher_final_guard.v1`，输出 current blocked state、expected artifact paths `artifacts/control_plane_ledger_execute_runtime_current_handoff.json` 与 `artifacts/billing_execute_browser_live_e2e_evidence.json`、session marker requirements、mutation opt-in requirements、exact next commands 和 final review checklist；final guard flags 明确 simulation、accepted-shape、watcher-only、no-artifact、session-missing 均不能 mark final `[x]`。验证：Control Plane smoke gate、Admin UI tests/build/bundle、Prettier、scoped diff check 通过。剩余缺口：真实 `runtime_current_verified` artifact/readback、有效 admin session、真实 browser mutation artifact pass/readback。
- **E11-007-S76 Browser Mutation Evidence Intake Watchdog，已完成/阻塞收束，2026-06-05，owner Parfit，no-op review**：browser mutation evidence intake watchdog。结果：未改文件、未 build/recreate、未运行 mutation、未读取 session；expected runtime-current artifact `artifacts/control_plane_ledger_execute_runtime_current_handoff.json` 不存在；browser evidence artifact `artifacts/billing_execute_browser_live_e2e_evidence.json` 存在，但 runtime-current artifact 缺失且未显式 readback/acceptance gate，因此不能推进 final。Watcher/audit 状态 blocked，`final_x_eligible=false`；blocking reasons 包含 runtime-current artifact missing、admin session marker missing、browser mutation artifact gate missing。剩余缺口：真实 `runtime_current_verified` artifact、有效 admin session、真实 browser mutation artifact pass/readback。
- **E11-007-S77 Browser Mutation Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Parfit，no-op review**：browser mutation evidence intake repeat。结果：未改文件、未 build/recreate、未运行 mutation、未读取 session；`artifacts/control_plane_ledger_execute_runtime_current_handoff.json` 不存在，`artifacts/billing_execute_browser_live_e2e_evidence.json` 存在但 runtime-current artifact 缺失且未显式 readback/acceptance gate；watcher blocked，`final_x_eligible=false`，final guard 仍禁止 simulation/accepted-shape/watcher/no-artifact/session-missing 关闭。剩余缺口：真实 `runtime_current_verified` artifact、有效 admin session、真实 browser mutation artifact pass/readback。
- **E11-007-S78 Browser Mutation Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Parfit，no-op review**：browser mutation evidence intake repeat。结果：未改文件、未 build/recreate、未 mutate、未读取 session；`artifacts/control_plane_ledger_execute_runtime_current_handoff.json` 不存在，`artifacts/billing_execute_browser_live_e2e_evidence.json` 存在；watcher blocked，`final_x_eligible=false`，blocking reasons 包含 runtime-current artifact missing、admin session marker missing、browser mutation artifact missing。剩余缺口：真实 `runtime_current_verified` artifact、有效 admin session、真实 browser mutation artifact pass/readback。
- **E11-007-S79 Browser Mutation Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Parfit，no-op review**：browser mutation evidence intake repeat。结果：未改文件、未 build/recreate、未 mutate、未读 session；runtime-current artifact 不存在，browser evidence artifact 存在；watcher blocked，`final_x_eligible=false`，blocking reasons 包含 `runtime_current_verified_artifact_missing`、`admin_session_marker_missing`、`browser_mutation_artifact_missing`。剩余缺口：真实 `runtime_current_verified` artifact、有效 admin session、真实 browser mutation artifact pass/readback。
- **E11-007-S80 Browser Mutation Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Parfit，no-op review**：browser mutation evidence intake repeat。结果：未改文件、未 build/recreate、未 mutate、未读 session；runtime-current artifact 不存在，browser evidence artifact 存在，admin session marker 未满足；watcher blocked，`final_x_eligible=false`，existing browser artifact 不能绕过 runtime-current/session gate。剩余缺口：真实 `runtime_current_verified` artifact、有效 admin session、真实 browser mutation artifact pass/readback。
- **E11-007-S81 Browser Mutation Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Parfit，no-op review**：browser mutation evidence intake repeat。结果：未改文件、未 build/recreate、未 mutate、未读 session；runtime-current artifact 不存在，browser evidence artifact 存在，admin session marker 未满足；watcher blocked，`final_x_eligible=false`，existing browser artifact 不能绕过 runtime-current/session gate。剩余缺口：真实 `runtime_current_verified` artifact、有效 admin session、真实 browser mutation artifact pass/readback。
- **E11-007-S82 Browser Mutation Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Parfit，no-op review**：browser mutation evidence intake repeat。结果：未改文件、未 build/recreate、未 mutate、未读 session；runtime-current artifact 不存在，browser evidence artifact 存在，admin session marker 未满足；watcher blocked，`final_x_eligible=false`，existing browser artifact 不能绕过 runtime-current/session gate。剩余缺口：真实 `runtime_current_verified` artifact、有效 admin session、真实 browser mutation artifact pass/readback。
- **E11-007-S83 Browser Mutation Closure Attempt，已改派，2026-06-05，owner Parfit**：主动验证 runtime-current artifact/readback、admin session marker、browser mutation artifact pass/readback closure path；优先使用既有 ledger adjustment execute smoke 脚本、Admin UI contract 和 artifacts；不得实际 mutate，除非 runtime-current/session/mutation/artifact opt-in 全部满足；不得把 existing browser artifact 绕过 gate 标 final。验收：回报实际命令、classification、`runtime_current_verified`、`session_marker_present`、`mutation_pass_artifact_passed`、`artifact_readback_passed`、`final_x_eligible` 与 next exact commands。
- **E11-007-S84 Environment Remediation Checklist，已完成/阻塞收束，2026-06-05，owner Parfit**：汇总 E11 到 100% 的 runtime-current rebuild/session/Playwright/mutation artifact 缺口。原因：用户确认环境已补齐后，需要在不 build、不跑重型测试、不提交 live mutation 的前提下重新判定 E11 哪些 blocker 已解除。范围：`scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1` 参数/env、`docs/TODO.md`、本 split log；不改 Gateway/E8/E9/E13，不改 Admin UI 代码，不执行 browser runner。结果：脚本支持 env 覆盖 `CONTROL_PLANE_BASE_URL`、`ADMIN_UI_BASE_URL`、`CONTROL_PLANE_ADMIN_EMAIL`、`CONTROL_PLANE_ADMIN_PASSWORD`、runtime/browser artifact path 与 opt-in；命令内注入 dev env 后，`-ContractOnly` 证明 Admin UI reachable、Control Plane health reachable、Playwright tooling available，但默认 browser evidence artifact stale by `generated_at`，contract run exit 1；`-AdminSessionHandoff` 证明 dev admin login 可生成 `CONTROL_PLANE_ADMIN_SESSION_TOKEN` handoff，`admin_session_present=true`、`admin_session_source=dev_admin_login`、token/cookie/header value 未回显，但同一前置 stale artifact failure 仍使整脚本 exit 1。未 build、未 docker compose build、未运行 browser runner、未执行 mutation、未伪造 artifact。剩余缺口：runtime-current 仍 `control_plane_container_unavailable`/`runtime_current_readback_stale_or_unverified`，需要真实 `runtime_current_verified` artifact/readback；随后才可用有效 session 跑真实 browser mutation pass，并要求 `mutation_pass_artifact_passed` + `artifact_readback_passed`。
- **E11-007-S85 Runtime Current Artifact Visibility Reattempt，已完成/阻塞收束，2026-06-05，owner Parfit**：只处理 runtime-current artifact/container visibility。原因：S84 已证明 service/tooling/session 基本可用，但 final blocker 仍是 runtime-current artifact 缺失或 compose container visibility 不一致；本片先确认 artifact write/readback 和脚本视角的 runtime-current 状态，不进入 browser mutation。范围：`scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1` runtime-current artifact write/readback gate、Docker compose visibility、`docs/TODO.md`、本 split log；不改源码，不 build，不 docker compose build，不运行 browser mutation，不伪造 pass artifact。结果：外部 `docker compose -f deploy/docker-compose/docker-compose.yml ps control-plane` 可见 `docker-compose-control-plane-1` 且 `ps -q control-plane` 返回容器 ID；命令内显式注入 `CONTROL_PLANE_BASE_URL=http://127.0.0.1:8081`、`ADMIN_UI_BASE_URL=http://127.0.0.1:5173` 后运行 `-RuntimeCurrentEvidenceArtifactWriteOptIn -RuntimeCurrentEvidenceArtifactPath artifacts/control_plane_ledger_execute_runtime_current_handoff.json` 与 `-RuntimeCurrentEvidenceArtifactReadbackOptIn`。Artifact 写入 repo-bounded path `G:\fubox_API\artifacts\control_plane_ledger_execute_runtime_current_handoff.json`，readback fresh，但 gate 仍输出 `classification=control_plane_container_unavailable`、`blocker=control_plane_container_unavailable_for_no_build_handoff`、`readback_classification=runtime_current_readback_stale_or_unverified`、`after_recreate_classification=runtime_current_after_recreate_unverified`、`source_newest_utc=2026-06-05T09:30:33Z`、`container_created_utc=unavailable`、`image_created_utc=unavailable`、`runtime_current_verified=false`；secret-safe 字段 `secret_material_echoed=false`、`url_credentials_echoed=false`、`request_material_echoed=false`。最短 no-build recreate operator command：`docker compose -f deploy/docker-compose/docker-compose.yml up -d --no-build --no-deps --force-recreate control-plane`。剩余缺口：需要 no-build recreate 后重新写/readback runtime-current artifact；若仍 unavailable，再诊断脚本 Docker probe mismatch；不得进入 browser mutation。
- **E11-007-S86 No-Build Recreate Runtime Artifact Reattempt，已完成/阻塞收束，2026-06-05，owner Parfit**：只处理 S85 的 `control_plane_container_unavailable`。原因：S85 已证明 artifact path/readback/secret-safe OK，但 runtime-current 仍不能 verified；本片只允许 no-build recreate + artifact write/readback。范围：Docker compose no-build recreate、runtime-current artifact write/readback gate、TODO 同步；未 build、未 docker compose build、未运行 browser mutation、未伪造 pass artifact。结果：执行 `docker compose -f deploy/docker-compose/docker-compose.yml up -d --no-build --no-deps --force-recreate control-plane` 成功，compose 输出 `docker-compose-control-plane-1` recreated/started，随后 `docker compose ps control-plane` 显示容器 `Up` 且端口 `127.0.0.1:8081->8081/tcp`。命令内注入 `CONTROL_PLANE_BASE_URL=http://127.0.0.1:8081`、`ADMIN_UI_BASE_URL=http://127.0.0.1:5173` 后运行 `-RuntimeCurrentEvidenceArtifactWriteOptIn -RuntimeCurrentEvidenceArtifactReadbackOptIn -RuntimeCurrentEvidenceArtifactPath artifacts/control_plane_ledger_execute_runtime_current_handoff.json`；artifact 仍 repo-bounded/fresh/secret-safe，但 gate 输出 `runtime_current_verified=false`、`classification=control_plane_container_unavailable`、`blocker=control_plane_container_unavailable_for_no_build_handoff`、`readback_classification=runtime_current_readback_stale_or_unverified`、`after_recreate_classification=runtime_current_after_recreate_unverified`、`source_newest_utc=2026-06-05T09:30:33Z`、`container_created_utc=unavailable`、`image_created_utc=unavailable`、`secret_material_echoed=false`、`url_credentials_echoed=false`、`request_material_echoed=false`。外部 `docker inspect` 采样出现 Docker Desktop pipe permission denied，不能补真实 container/image timestamps。剩余缺口：只允许下一片诊断 Docker probe/permission mismatch，不进入 browser mutation。
- **E11-007-S87 Docker Probe Permission Mismatch Diagnostic，已完成/阻塞收束，2026-06-05，owner Parfit**：只诊断 runtime-current Docker probe mismatch。原因：S86 no-build recreate 后 compose service 可见，但脚本 artifact 仍 `control_plane_container_unavailable` 且外部 inspect 出现 Docker Desktop pipe permission denied，需要解释 probe 与 Docker 权限/上下文差异，避免继续空转。范围：只读 `Get-DockerCommand`、`Invoke-NativeCapture`、`Write-ControlPlaneRuntimeSourceProbe` 相关脚本逻辑，轻量运行 `docker compose ps control-plane`、`docker compose ps -q control-plane`、`docker inspect`、`docker image inspect`；未 build、未 docker compose build、未运行 browser mutation、未生成 pass artifact。结果：脚本 `Get-DockerCommand` 会把 `C:\Program Files\Docker\Docker\resources\bin` 加入 PATH 并返回 `docker.exe`；`Invoke-NativeCapture` 用 `& $Command @Arguments 2>&1` 捕获 exit code/output；`Write-ControlPlaneRuntimeSourceProbe` 先跑 `docker compose -f <compose> ps -q control-plane`，若 exit 非 0 或输出空，直接把 reason 设为 `control_plane_container_unavailable`，不会进入 `docker inspect` 或 `docker image inspect`。诊断命令证明 working directory=`G:\fubox_API`、compose file 存在、service name `control-plane` 正确，`Get-Command docker` 解析到 Docker Desktop `docker.exe`，context=`desktop-linux`；直接 `docker compose ps control-plane` 可见容器，但脚本同款 `& (Get-Command docker).Source @('compose','-f','deploy/docker-compose/docker-compose.yml','ps','-q','control-plane')` 返回 exit=1，输出 `permission denied while trying to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine`；`docker version` 也只有 client 版本、server 访问 permission denied。结论：不是路径/service 名称问题，而是 Docker Desktop Linux engine pipe/API 权限在当前进程里不稳定/受限，导致脚本拿不到容器 ID并按设计写 `control_plane_container_unavailable`。最小修复/操作建议：重启或恢复 Docker Desktop Linux engine pipe 权限，必要时重开同权限 PowerShell；先确认 `docker version --format '{{.Client.Version}}|{{.Server.Version}}'`、`docker compose -f deploy/docker-compose/docker-compose.yml ps -q control-plane`、`docker inspect -f '{{.Created}}|{{.Image}}' <cid>`、`docker image inspect -f '{{.Created}}' <image>` 均 exit=0，再重跑 runtime-current artifact write/readback gate。
- **E11-007-S88 Docker Pipe Recovery Runtime Artifact Reattempt，已完成/主线程验收，2026-06-05，owner 主线程代跑/Parfit lane**：只在 Docker Desktop pipe/API 权限恢复后重跑 runtime-current artifact gate。原因：S87 已把 blocker 收敛为 Docker API pipe permission，下一步不能继续 browser mutation，必须先让 container/image timestamps 可读并产出真实 runtime-current verified/readback 分类。范围：Docker pipe preflight 与 runtime-current artifact write/readback gate；未 build、未 docker compose build、未运行 browser mutation、未伪造 pass artifact。结果：`docker version --format '{{.Client.Version}}|{{.Server.Version}}'` 返回 `29.5.2|29.5.2`，`docker compose -f deploy/docker-compose/docker-compose.yml ps -q control-plane` 返回容器 ID。命令内注入 `CONTROL_PLANE_BASE_URL=http://127.0.0.1:8081`、`ADMIN_UI_BASE_URL=http://127.0.0.1:5173` 后运行 `scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1 -RuntimeCurrentEvidenceArtifactWriteOptIn -RuntimeCurrentEvidenceArtifactReadbackOptIn -RuntimeCurrentEvidenceArtifactPath artifacts/control_plane_ledger_execute_runtime_current_handoff.json`；输出 `runtime_source_newest_utc=2026-06-05T09:30:33.4756558Z`、`runtime_container_created_utc=2026-06-05T10:49:18.9253653Z`、`runtime_image_created_utc=2026-06-05T10:34:49.0706156Z`、`runtime_image_stale_or_unverified=false`、`runtime_current_handoff_classification=container_recreate_available`、`runtime_current_handoff_readback_classification=runtime_current_readback_verified`、artifact readback fresh、secret/url/request material echoed=false。脚本整体 exit=1 仅因默认 browser evidence artifact stale by `generated_at`，不能关闭 E11 final；Docker/runtime-current blocker 已解除。剩余缺口：真实 browser mutation pass artifact、artifact readback、api/ledger readback、numeric durations、secret-safe omission。
- **E11-007-S89 Browser Mutation Artifact Pass And Readback / Runtime Probe Scope Fix，已完成/阻塞收束，2026-06-05，owner Parfit**：在 S88 runtime-current verified 后准备推进真实 browser mutation pass artifact，但主线程 preflight 发现 `runtime_source_newest_utc=2026-06-05T12:04:32.8142370Z`、`image_created=2026-06-05T10:34:49Z`、`classification=source_newer_than_runtime_image`，同时人工核对 `apps/control-plane/src/admin.rs` 最新时间早于 image，怀疑 verifier 脚本自身 `$PSCommandPath` 被纳入 image freshness。原因：runtime-current freshness 应覆盖 control-plane runtime source/admin UI contract，不应因 verifier 被 lane 修改而误判需要 build。范围：只读/最小改 `scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1`，同步 TODO；未 build、未 docker compose build、未运行 browser mutation、未伪造 pass artifact。结果：新增 `Get-ControlPlaneRuntimeSourcePaths`，image freshness 仅使用 `apps/control-plane/src/admin.rs` 与 Admin UI dry-run contract source；新增 `runtime_verifier_script_newest_utc` 与 `runtime_verifier_script_affects_image_freshness=false` 输出，保留 verifier provenance/secret-safe，但不让 `$PSCommandPath` 触发 `source_newer_than_runtime_image`。验证：命令内注入 `CONTROL_PLANE_BASE_URL=http://127.0.0.1:8081`、`ADMIN_UI_BASE_URL=http://127.0.0.1:5173` 后运行 runtime-current write/readback gate，输出 `runtime_source_newest_utc=2026-06-05T09:20:43.6481624Z`、`runtime_verifier_script_newest_utc=2026-06-05T12:06:07.5915384Z`、`runtime_verifier_script_affects_image_freshness=false`，证明 false stale 已消除；当前子进程 Docker container/image 不可见，artifact 被写回 `classification=control_plane_container_unavailable`、`readback_classification=runtime_current_readback_stale_or_unverified`，不是 real source stale，也不需要 build。随后只跑 `-AdminSessionHandoff`，`admin_session_present=true`、`admin_session_source=dev_admin_login`、token/cookie/header value 未回显；脚本整体仍因默认 browser evidence artifact stale by `generated_at` exit=1。剩余缺口：在 Docker 可见进程复写/readback verified runtime-current artifact，再跑真实 browser mutation artifact pass/readback。
- **E11-007-S90 Runtime Artifact Reverify After Probe Scope Fix，已完成/阻塞收束，2026-06-05，owner Parfit/主线程代跑**：只在 Docker server/container/image 可见的进程里复写/readback `artifacts/control_plane_ledger_execute_runtime_current_handoff.json`。原因：S89 已排除 verifier script false stale，但上一轮子进程 Docker container/image 不可见并曾把 artifact 写回 unverified；进入 browser mutation 前必须恢复或确认 verified runtime-current artifact。范围：Docker visibility preflight 与 runtime-current artifact state；未 build、未 docker compose build、未运行 browser mutation。结果：主线程已在同一工作区复跑 runtime-current gate，输出 `runtime_verifier_script_affects_image_freshness=false`、`runtime_current_handoff_readback_classification=runtime_current_readback_verified`。本子进程复查：`docker version --format '{{.Client.Version}}|{{.Server.Version}}'` 返回 client `29.5.2` 但 server 为空并报 `permission denied while trying to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine`；`docker compose -f deploy/docker-compose/docker-compose.yml ps -q control-plane` 可返回容器 ID `48b7780898bdc28d6005f115331af9aaf3b768c58d459c28541d06332b713325`；同一进程 `docker inspect -f '{{.Created}}|{{.Image}}' <cid>` 仍 permission denied。因此未运行 runtime-current write/readback gate，避免覆盖主线程 verified artifact。当前 artifact readback 为 `status=ready`、`classification=container_recreate_available`、`reason=none`、`source_newest_utc=2026-06-05T09:20:43`、`container_created_utc=2026-06-05T10:49:18`、`image_created_utc=2026-06-05T10:34:49`、`readback_classification=runtime_current_readback_verified`、`secret_material_echoed=false`、`url_credentials_echoed=false`、`request_material_echoed=false`。剩余缺口：真实 browser mutation pass artifact/readback。
- **E11-007-S91 Browser Mutation Artifact Pass After Runtime Verified Artifact，已完成/真实 blocker 收束，2026-06-05，owner 主线程代跑/Parfit lane**：复用 S88/S90 verified runtime-current artifact，只推进真实 browser mutation pass/readback。原因：runtime-current false stale 和 verified artifact blocker 已收束，E11 final 只剩真实 browser mutation artifact 与 readback。范围：admin session marker、runtime-current artifact readback、browser mutation runner opt-in、browser evidence artifact write/readback；未 build、未 docker compose build、未伪造 pass artifact。结果：主线程显式注入 `CONTROL_PLANE_BASE_URL=http://127.0.0.1:8081`、`ADMIN_UI_BASE_URL=http://127.0.0.1:5173`、dev admin seed 与 `CONTROL_PLANE_LEDGER_ADJUSTMENT_EXECUTE_BROWSER_MUTATION=1`，运行 `scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1 -AdminSessionHandoff -RuntimeCurrentEvidenceArtifactReadbackOptIn -RuntimeCurrentEvidenceArtifactPath artifacts/control_plane_ledger_execute_runtime_current_handoff.json -BrowserPreflight -BrowserMutationOptIn -BrowserEvidenceArtifactWriteOptIn -BrowserLiveRunnerExecutionOptIn -BrowserEvidenceArtifactReadbackOptIn -BrowserEvidenceArtifactPath artifacts/billing_execute_browser_live_e2e_evidence.json`。Runtime-current readback verified；admin session handoff/consume 成功，token/cookie/header 未回显；browser runner 真实执行，写入 fresh artifact。Artifact readback：`outcome=failed`、`runtime_current=runtime_current_verified`、`session_verification.verified=true`、`failure=browser_idempotent_replay_failed`、`failed_action=idempotent_replay`、`mutation_pass_artifact=mutation_pass_artifact_failed`、`artifact_readback_failed`，durations 包括 `browser_launch_duration_ms=1660`、`page_ready_duration_ms=2675`、`dry_run_plan_duration_ms=58`、`execute_apply_duration_ms=74`、`idempotent_replay_duration_ms=20035`。精确 blocker：`backend_idempotent_replay_after_remaining_check: execute replay returned over-remaining refusal before dedupe replay`。剩余缺口：修复后端 idempotent replay ordering 后重跑 browser mutation pass/readback。
- **E11-007-S92 Backend Idempotent Replay Ordering For Browser Mutation Pass，已完成/待 runtime 复验，2026-06-05，owner Parfit**：修复真实 browser mutation pass 中的后端 idempotent replay ordering。原因：S91 已证明 session/runtime/browser runner 环境可用，当前 blocker 是 replay request 在 dedupe 前被 remaining/budget refusal 拦住。范围：Control Plane ledger adjustment execute 后端、相关 tests/fixtures、TODO 同步；未 build、未 docker compose build，未碰 E8/E9/E13/E15。结果：定位 `backend_idempotent_replay_after_remaining_check` 到 `apps/control-plane/src/admin.rs::execute_ledger_adjustment`；修复 `ledger_adjustment_execute_dedupe_key` 生成顺序，从原始 request optional 字段提前生成，改为 related ledger entry 校验并 canonical 回填 `project_id`、`wallet_id`、`request_id` 后生成，且仍在 `get_ledger_adjustment_dedupe_entry_for_update_tx` lookup 与 `validate_refund_remaining_amount`/remaining refusal 前；同一 related-entry replay 应先命中 dedupe/idempotent response，不写新 ledger entry/audit。Targeted test：`cargo test -p ai-control-plane ledger_adjustment_execute_checks_dedupe_before_refund_remaining_validation` 通过。S91 live command 已重跑：`-AdminSessionHandoff` session consume 成功且 secret omitted，runtime artifact readback verified；但本子进程在 live Docker compose availability 被 Docker Desktop pipe `npipe:////./pipe/dockerDesktopLinuxEngine` permission denied 阻断，未进入 browser mutation。compose control-plane 无源码 volume，因此当前 runtime image 还未包含 S92 修复；现有 browser artifact 仍为 S91 failed (`browser_idempotent_replay_failed`)，不能 final `[x]`。剩余缺口：rebuild/redeploy control-plane runtime 后复写 runtime-current artifact，再重跑 browser mutation pass/readback。
- **E11-007-S93 Rebuild Runtime And Browser Mutation Pass Reverify / Opt-In Handoff，已完成/等待授权，2026-06-05，owner Parfit**：让 Control Plane runtime 包含 S92 后端修复后复验真实 browser mutation pass。原因：S92 已修源码和 targeted test，但当前 Docker image 没有源码 volume，且本子进程 Docker pipe 不可用，无法证明 live runtime 已执行新 dedupe ordering。范围：只读 build/redeploy opt-in handoff、Docker/runtime/source/artifact 状态、TODO 同步；未 build、未 docker compose build、未运行 browser mutation、未伪造 pass artifact，未碰 E8/E9/E13/E15。结果：`CONTROL_PLANE_LEDGER_ADJUSTMENT_EXECUTE_RUNTIME_REBUILD_APPROVED=<unset>`，无显式 rebuild opt-in；`docker version --format '{{.Client.Version}}|{{.Server.Version}}'` 仅返回 client `29.5.2`，server 报 Docker Desktop pipe permission denied；`docker compose -f deploy/docker-compose/docker-compose.yml ps -q control-plane` 可返回旧容器 ID，但同进程 `docker inspect`/`docker image inspect` 不可用。`apps/control-plane/src/admin.rs` source newest=`2026-06-05T12:31:52.3995976Z`；现有 runtime-current artifact 为 S90 verified 状态，记录 `status=ready`、`classification=container_recreate_available`、`reason=none`、`source_newest_utc=2026-06-05T09:20:43`、`container_created_utc=2026-06-05T10:49:18`、`image_created_utc=2026-06-05T10:34:49`、`readback_classification=runtime_current_readback_verified`、secret/url/request echoed=false，但已不能证明包含 S92 修复。当前 blocker：`runtime_requires_control_plane_rebuild_opt_in`。Exact command：`docker compose -f deploy/docker-compose/docker-compose.yml build control-plane && docker compose -f deploy/docker-compose/docker-compose.yml up -d --no-deps --force-recreate control-plane`。
- **E11-007-S94 Rebuild Opt-In Boundary Watcher，已完成/等待授权，2026-06-05，owner Parfit**：复用 S93 上下文，只读检查是否已有明确 control-plane rebuild/redeploy 授权；不要重新探索，不要执行。原因：S92 已修源码且当前 runtime 镜像未证明包含修复，E11 100% 下一步需要显式重建并复跑 browser mutation，但该动作会消耗资源/重启容器，需要 coordinator 确认。范围：仅 `docs/TODO.md` 与本 split log；未改源码、未 build、未 docker compose build、未重启 Docker、未运行 browser mutation、未伪造 runtime-current artifact。结果：`CONTROL_PLANE_LEDGER_ADJUSTMENT_EXECUTE_RUNTIME_REBUILD_APPROVED=<unset>`，TODO 中也无等价已授权记录；没有把普通环境可用误判为授权。当前 blocker=`runtime_requires_control_plane_rebuild_opt_in`。Exact command：`docker compose -f deploy/docker-compose/docker-compose.yml build control-plane && docker compose -f deploy/docker-compose/docker-compose.yml up -d --no-deps --force-recreate control-plane`。主线程 Docker 可见且获授权后可执行；本切片不执行。剩余缺口：control-plane runtime rebuild/redeploy、runtime-current artifact 重新 verified、真实 browser mutation pass artifact、`mutation_pass_artifact_passed` + `artifact_readback_passed`。
- **E11-007-S95 Runtime Rebuild / Browser Mutation Final Closure，已完成/主线程验收，2026-06-05，owner 主线程代跑/Parfit lane**：按用户“实际去做”授权执行 control-plane rebuild/recreate 与 browser mutation final closure。原因：S92/S94 已证明 runtime image 未包含后端修复，E11 最终 `[x]` 只差真实 runtime-current verified artifact 与 browser mutation pass/readback。范围：`apps/control-plane/src/admin.rs`、`scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1`、runtime-current artifact、browser evidence artifact 与 TODO；未碰 E8/E9/E13/E15。结果：先执行 `docker compose -f deploy/docker-compose/docker-compose.yml build control-plane` 与 `up -d --no-deps --force-recreate control-plane`，build 成功，仅有既有 dead-code warnings。复写 `artifacts/control_plane_ledger_execute_runtime_current_handoff.json` 后 readback=`runtime_current_readback_verified`，source newest `2026-06-05T13:18:12.6499800Z` 早于 container/image created `2026-06-05T13:20:47Z/13:20:46Z`。首次 browser 复跑关闭旧 `browser_idempotent_replay_failed`，暴露 UI runner `refund_refusal` timeout；继续修复真实原因：handler 对 execute mode 不再先调用会做 remaining 校验的 dry-run plan builder，而是先进入 execute transaction/dedupe，success/idempotent response 在 execute 内生成 validated plan；browser runner 将 over-remaining UI dry-run refusal 作为 blocked UI evidence，并把 artifact write/readback opt-in 写入 artifact。验证：`cargo test -p ai-control-plane ledger_adjustment_execute_checks_dedupe_before_refund_remaining_validation`、`cargo test -p ai-control-plane ledger_adjustment_execute`、`cargo fmt -p ai-control-plane --check`、`pwsh -File scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1 -ContractOnly`、最终真实 browser mutation command 均通过。最终 `artifacts/billing_execute_browser_live_e2e_evidence.json`：`outcome=passed`、`runtime_current_verified`、`idempotent_replay_passed`、`mutation_pass_artifact_passed`、`artifact_readback_passed`、`failure=none`、`closure_eligible=true`；API readback 为 dry-run executePreflight / execute applied / replay idempotent / refund refusal blocked / ledger refresh success；ledger readback 全 true；durations 均 numeric；session/request/metadata material 未回显。E11 final closure 完成。
- **E11-007-S96 Final Closure Independent Evidence Audit，已完成/独立审计通过，2026-06-05，owner Parfit reviewer**：只读审计 runtime-current artifact、browser evidence artifact 与 TODO/split final closure 记录。原因：主线程已将 E11 推到 100%，需要独立确认 artifact 字段满足最终 DoD，而不是只依赖 exit=0。范围：`docs/TODO.md`、本 split log；未改源码、未 build、未重启 Docker、未跑 browser mutation、未伪造 artifact。结果：`artifacts/control_plane_ledger_execute_runtime_current_handoff.json` 为 `status=ready`、`readback_classification=runtime_current_readback_verified`，`source_newest_utc=2026-06-05T13:18:12` <= `image_created_utc=2026-06-05T13:20:46` / `container_created_utc=2026-06-05T13:20:47`，`secret_material_echoed=false`、`url_credentials_echoed=false`、`request_material_echoed=false`。`artifacts/billing_execute_browser_live_e2e_evidence.json` 为 `outcome=passed`、`runtime_current_verified`、`idempotent_replay_passed`、`mutation_pass_artifact_passed`、`artifact_readback_passed`、`failure=none`、`readback.closure_eligible=true`；API readback 为 `dry_run_plan=executePreflight`、`execute_apply=applied`、`idempotent_replay=idempotent`、`refund_refusal=blocked`、`ledger_refresh=success`；ledger readback `applied_ledger_entry_visible`、`idempotent_replay_reused_ledger_entry`、`refund_refusal_no_ledger_write`、`ledger_refresh_visible` 全 true；所有 durations 均 numeric；session/request/metadata material 未回显，session secret omitted。只读断言 `all_pass=True`。结论：E11 100% / final `[x]` closure 证据充分，不再派 E11 功能切片；后续该 lane 可转横向 release audit。
- **E11-007-S97 Agent-E11 TODO-10 fresh beta rerun + runtime dispatch gate fix，已完成，2026-06-05，owner Agent-E11/Admin Billing**：按 TODO-10 重新生成当前 HEAD 证据，拒绝复用旧 browser artifact。基线：`git rev-parse --short HEAD=8e3318d`；初始 `git status --short` 显示多 lane 并行改动；初始/最终 `git diff --check` 均只有既有 CRLF/LF warning、无 whitespace error。改动：`scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1` runtime-current artifact 新增 `image_id`、`git_commit`、`alignment_rules`，readback probe 传递这些 marker，并将 stale image 继续分类为 blocker；`web/admin-ui/src/billingExecuteSmokeContract.ts`、`billingExecuteSmokeContract.serializable.json`、`App.test.tsx` 同步 runtime artifact required fields；`apps/control-plane/src/admin.rs` 修复 persistent cutover dispatch decision，将 `canonical_final_gate_passed=false` 纳入 runtime writer selection，避免 production persistent state 被并行 E9 切到 `billing_ledger_runtime_writer` 后绕过 canonical gate 阻断 E11 本地事务 writer。命令：contract-only gate exit `0`；首次 runtime-current beta artifact write/readback exit `0`；首次 browser mutation exit `1`，真实 blocker 为 runtime writer `balance_assert_missing_row` / `409`；`cargo test -p ai-control-plane ledger_adjustment_execute -- --nocapture` exit `0`；`docker compose -f deploy/docker-compose/docker-compose.yml build control-plane`、`up -d --no-deps --force-recreate control-plane` exit `0`；修复后 runtime-current beta artifact write/readback exit `0`；真实 browser mutation/write/readback exit `0`；`npm --prefix web/admin-ui ci`、`npm --prefix web/admin-ui test`、`npm --prefix web/admin-ui run build`、`npm --prefix web/admin-ui run check:bundle`、`cargo fmt -p ai-control-plane --check`、最终 `git diff --check` 均 exit `0`。Artifacts：`artifacts/control_plane_ledger_execute_runtime_current_verified_beta.json` 为 `status=ready`、`classification=container_recreate_available`、`readback_classification=runtime_current_readback_verified`，`source_newest_utc=2026-06-05T18:35:25.3710802Z` <= `image_created_utc=2026-06-05T18:39:05.4724326Z` <= `container_created_utc=2026-06-05T18:39:18.5003083Z`，`image_id=sha256:b4e763...`，`git_commit=8e3318d5200b9f93c429864b3ca338f0b87cfdb9`，secret/url/request echoed 全 false；`artifacts/billing_execute_browser_live_e2e_evidence.json` 为 `outcome=passed`、`runtime_current_verified`、`mutation_pass_artifact_passed`、`artifact_readback_passed`、`failure=none`、`closure_eligible=true`，API readback `execute_apply=applied`、`idempotent_replay=idempotent`、`refund_refusal=blocked`、`ledger_refresh=success`，ledger readback 四项全 true，durations numeric，session/request/metadata material 未回显。Acceptance：真实 Admin UI/session 执行 Billing ledger adjustment mutation，后端写 ledger entry + success audit，API/DB/UI readback 通过；stale image 仍 blocker，不再误 pass；secret-safe artifact 通过。Blockers：无剩余 E11 blocker；已处理并行 E9 persistent runtime writer cutover 对 E11 的 blocker，后续 E9 仍需自己的 canonical production cutover final gate。Next task：转横向 release audit，避免再派 E11 功能切片。
- **E11-007-S97 Release Gate Artifact Readback Pack，已完成，2026-06-05，owner Agent-E11/Admin Billing**：把 E11 runtime-current artifact 与 browser mutation artifact readback 固定为 QA 可复跑的只读 release gate。范围：`scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1` 新增 `-ArtifactReadbackOnly` 与 release readback assertions，默认读取 `artifacts/control_plane_ledger_execute_runtime_current_verified_beta.json` 和 `artifacts/billing_execute_browser_live_e2e_evidence.json`；`scripts/test.ps1` 新增 `-ControlPlaneLedgerAdjustmentExecuteBrowserReadbackOnly`；`project/RELEASE_CHECKLIST.md` 与权威 TODO E11 段落记录命令和 pass 标准。命令：`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1 -ArtifactReadbackOnly` exit `0`，输出 `e11_release_artifact_readback_status=pass`、`runtime_current_verified=true`、`mutation_pass_artifact_passed=true`、`artifact_readback_passed=true`、API/DB/UI/audit readback pass 与 `e11_release_secret_safe=true`；`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1 -ContractOnly` exit `0`；首次 `scripts/test.ps1 -ControlPlaneLedgerAdjustmentExecuteBrowserReadbackOnly` 因 PowerShell env expression 解析错误 exit `1`，修复后重跑 exit `0`；scoped `git diff --check -- scripts/release_check.ps1 scripts/test.ps1 scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1 project/RELEASE_CHECKLIST.md docs/todo/slices/E11-007.md TODO/AI_GATEWAY_REBASED_TODO_2026-06-05.md` exit `0`，仅 LF/CRLF warning。Acceptance：gate 绑定 current commit `8e3318d5200b9f93c429864b3ca338f0b87cfdb9`、runtime image id、source/image/container timestamp ordering、runtime-current readback classification、browser pass/readback classifications、API readback、ledger DB readback、UI action outcomes、failure taxonomy、numeric durations 和 secret-safe markers；missing/stale/simulated/wrong-commit/runtime-current-unverified artifact 均不会 pass。Artifacts written：无新 artifact；只读复用 `artifacts/control_plane_ledger_execute_runtime_current_verified_beta.json` 与 `artifacts/billing_execute_browser_live_e2e_evidence.json`。Blockers：无。Next task：QA 在 release/RC 前归档 `scripts/test.ps1 -ControlPlaneLedgerAdjustmentExecuteBrowserReadbackOnly` 输出；若 artifact 因 generated_at 或 commit 过期被拒绝，重新跑已验收的 E11 live mutation flow 生成 fresh artifacts 后再运行只读 gate。
- **E11-007-S98 / TODO-30C Control Plane Paid Ledger Readback + Reconciliation Evidence Slice，ready / blocked by Gateway artifact，2026-06-05，owner Agent-E11/Admin Billing**：优先级切换到 paid evidence readback/reconciliation 支撑；未继续 TODO-14C，未改 Gateway/Admin UI React，未重跑 live mutation。改动：新增 `scripts/verify_control_plane_paid_ledger_readback.ps1`，读取 Gateway TODO-30B paid hot path artifact 后输出 `control_plane_paid_ledger_readback_verification.v1`，把 reserve/settle/refund/readback/rollback/reconciliation/insufficient-balance no-provider-call 映射为 E9 七项 readiness evidence；新增 `scripts/operator/control_plane_paid_ledger_reconciliation_readback.sql`，对 ledger/request/provider_attempts/audit 做 bounded SQL 聚合；新增 `tests/fixtures/billing/control_plane_paid_ledger_readback_contract.json` 与 `tests/fixtures/billing/control_plane_paid_gateway_hot_path_artifact.accepted_shape.json`。命令：`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_control_plane_paid_ledger_readback.ps1 -SelfTest` exit `0`；默认 `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_control_plane_paid_ledger_readback.ps1` 输出 `gateway_paid_hot_path_artifact_missing`，contract exit `2`；accepted-shape fixture readback 输出七项 artifact evidence accepted，但 real SQL readback 当前 `control_plane_paid_readback_sql_unavailable`，exit `2`；`cargo test -p ai-control-plane billing_reconciliation -- --nocapture` exit `0`；E11 browser release readback regression 因旧 browser artifact `generated_at` 过期而 blocked exit `2`，未放宽 stale gate。Acceptance：offline fixture selftest 覆盖完整 accepted-shape、missing Gateway artifact、raw secret/Auth marker、operation id mismatch、refund idempotency mismatch refusal；输出不回显 Admin session、Authorization、Cookie、DB URL、provider key、virtual key 或 raw request body；paid 状态只记录 `paid_controlled_beta_requested` / `blocked_until_real_evidence`，不声明 paid 已开放。Blockers：等待 TODO-30B Gateway hot path artifact；本地 real SQL readback 需要可用 Control Plane DB/tables 后复跑；E11 release readback gate 需要重新生成 fresh browser artifact 才能回归 pass。Next task：Fermat/TODO-30B 产出 real Gateway paid hot path artifact 后，运行 `scripts/verify_control_plane_paid_ledger_readback.ps1 -GatewayArtifactPath <artifact>` 并将 output 接入 E9 paid evidence bundle。
- **E11-007-S99 / TODO-30C-S2 Fresh Readback Evidence and Paid Readback Exit Semantics，已完成，2026-06-05，owner Agent-E11/Admin Billing**：收束 S98 review 两个缺口。改动：`scripts/verify_control_plane_paid_ledger_readback.ps1` 输出新增 `actual_exit_code`、`expected_shell_exit_code`、`exit_code_contract`，blocked JSON 明确为 `actual_exit_code=2`；普通 `cmd /v:on` shell 确认 `%ERRORLEVEL%=2`，Codex tool UI 仍会把非零显示成 `1`，QA 应按 JSON/普通 shell 判定。`scripts/verify_control_plane_ledger_adjustment_execute_smoke.ps1` 的 E11 release readback gate 新增 stale blocker report writer：若 browser artifact 过期，会写 bounded secret-safe `artifacts/control_plane_ledger_execute_release_readback_blocked_stale.json`，包含 `generated_at`、max age、age、env/session presence booleans 和 exact regeneration command，不放宽 freshness。实际本轮环境可用，已刷新 `artifacts/control_plane_ledger_execute_runtime_current_verified_beta.json` 和 `artifacts/billing_execute_browser_live_e2e_evidence.json`；真实 browser live mutation pass 写入 fresh `generated_at=2026-06-05T19:25:45.1113631Z`，runtime-current verified，`mutation_pass_artifact_passed`、`artifact_readback_passed`、API/DB/UI/audit readback pass，secret-safe。命令：runtime-current artifact write/readback exit `0`；browser live mutation artifact write/readback exit `0`; `scripts/test.ps1 -ControlPlaneLedgerAdjustmentExecuteBrowserReadbackOnly` exit `0`; paid verifier `-SelfTest` exit `0`; default paid verifier JSON `actual_exit_code=2` / shell `%ERRORLEVEL%=2` with `gateway_paid_hot_path_artifact_missing`; fixture paid verifier JSON `actual_exit_code=2` with `control_plane_paid_readback_sql_unavailable`; `cargo test -p ai-control-plane billing_reconciliation -- --nocapture` exit `0`; scoped `git diff --check` exit `0`。Current status：E11 release readback is `pass`; paid readback remains blocked until TODO-30B real Gateway artifact + SQL readback.
- **E11-007-S100 / TODO-30C-S3 Paid Readback Output Artifact Handoff，已完成，2026-06-05，owner Agent-E11/Admin Billing**：让 E11 paid readback verifier 写出 QA/E9 默认 handoff artifact。改动：`scripts/verify_control_plane_paid_ledger_readback.ps1` 新增 `-OutputPath`，默认 `.tmp/paid-beta/e11_control_plane_paid_readback_reconciliation.json`；输出路径必须 repo-bounded 且只允许 `.tmp/**` 或 `artifacts/**`；非 selftest summary 会写入 `output_artifact` metadata，标记 `path_bounded=true`、`written=true`、`raw_gateway_artifact_written=false`、`secret_material_written=false`。默认无 Gateway artifact：写 `.tmp/paid-beta/e11_control_plane_paid_readback_reconciliation.json`，`overall_status=blocked`、`actual_exit_code=2`、`blockers=[gateway_paid_hot_path_artifact_missing]`、`paid_controlled_beta_opened_by_this_check=false`。Fixture Gateway artifact：写 `.tmp/paid-beta/e11_control_plane_paid_readback_reconciliation.fixture.json`，`actual_exit_code=2`、`blockers=[control_plane_paid_readback_sql_unavailable]`。真实 Gateway artifact 未来存在时仍会先尝试 SQL/readback，只有 post-commit readback、rollback、reconciliation 和 required evidence 全 pass 才能 `overall_status=passed`。验证：`-SelfTest` exit `0`；default blocked with output path runner exit `1` / JSON `actual_exit_code=2`；fixture blocked with output path runner exit `1` / JSON `actual_exit_code=2`；output text secret-safe check pass；`scripts/test.ps1 -ControlPlaneLedgerAdjustmentExecuteBrowserReadbackOnly` exit `0`；scoped `git diff --check` exit `0`（仅 line-ending warning）。Current blocker：等待 TODO-30B real Gateway artifact；default handoff artifact 已可供 Sagan aggregator / Harvey composer 消费。
- **E11-007-S101 / TODO-30C-S4 Consume E8 Gateway Paid Artifact If Available，blocked by E8 request/operation id handoff，2026-06-05，owner Agent-E11/Admin Billing**：将 E11 paid readback verifier 默认 Gateway 输入切到 `.tmp/paid-beta/e8_gateway_paid_hot_path.json`，并强化存在但不可 SQL readback 的 E8 artifact 分类。改动：`scripts/verify_control_plane_paid_ledger_readback.ps1` 默认 `GatewayArtifactPath=.tmp/paid-beta/e8_gateway_paid_hot_path.json`；读取 Gateway artifact 后记录 bounded schema/status，不写 raw artifact；top-level `blocked/failed/refused` -> E11 blocked；每项 required E8 evidence 必须有 passed marker、request_id、operation_id、operation，缺 request/operation ids 输出 `gateway_paid_hot_path_request_or_operation_ids_missing`，不得 pass；valid E8 shape 仍会进入 Control Plane SQL/readback，只有 post-commit readback、rollback_proof、reconciliation_report、settle/refund idempotency 全 pass 才能 `overall_status=passed`。当前 `.tmp/paid-beta/e8_gateway_paid_hot_path.json` 存在，schema `gateway_paid_hot_path_smoke_v1`、status `passed`，但没有 request/operation ids/evidence items；E11 output `.tmp/paid-beta/e11_control_plane_paid_readback_reconciliation.json` 为 `overall_status=blocked`、`actual_exit_code=2`、blocker `gateway_paid_hot_path_request_or_operation_ids_missing`，secret-safe，未写 raw Gateway artifact。验证：`-SelfTest` exit `0`；default with E8 artifact runner exit `1` / JSON `actual_exit_code=2` blocked by request/operation ids missing；accepted fixture path仍 blocked by `control_plane_paid_readback_sql_unavailable`；E11 browser release readback gate exit `0`；scoped `git diff --check` exit `0`。Next task：E8/TODO-30B 需要在 `.tmp/paid-beta/e8_gateway_paid_hot_path.json` 写出 E11 可消费的 request/operation-id-bearing evidence array 或 agreed equivalent 后，E11 再跑 SQL/readback。
- **E11-007-S102 / TODO-30C-S5 Required Gateway Artifact Shape Contract，已完成/等待 Control Plane SQL readback，2026-06-05，owner Agent-E11/Admin Billing**：固化 E11 需要 E8 提供的 Gateway paid artifact shape，避免 top-level `status=passed` 但缺 request/operation ids。新增 fixture：`tests/fixtures/billing/control_plane_paid_gateway_hot_path_artifact.required_shape.json`，明确 required fields：`schema_version`、`status=passed`、`smoke_run_id`、`secret_safe` false flags、top-level `request_ids`/`operation_ids`、七项 `evidence[]` mapping，每项包含 `evidence_key`、`status`、`passed`、`request_id`、`operation`、`operation_id`，refund 还包含 `related_ledger_entry_id`、`refund_operation_id` 和 expected idempotency key。Verifier 更新：`-SelfTest` 使用 required-shape fixture 做 positive input parsing，不跑 SQL/readback、不允许 paid；负向覆盖 missing request ids -> `gateway_paid_hot_path_request_ids_missing`，missing operation ids -> `gateway_paid_hot_path_operation_ids_missing`，missing evidence mapping -> `gateway_paid_hot_path_evidence_mapping_missing`，另保留 raw secret、operation id mismatch、refund idempotency mismatch refusal。并行 E8 在本轮中更新了 `.tmp/paid-beta/e8_gateway_paid_hot_path.json`：当前 schema/status/smoke_run_id/evidence shape 可解析，E11 accepted 七项 evidence，request_id_count=3、operation_id_count=3；E11 已进入 Control Plane SQL/readback，当前 output `.tmp/paid-beta/e11_control_plane_paid_readback_reconciliation.json` 为 `overall_status=blocked`、`actual_exit_code=2`、blocker `control_plane_paid_readback_sql_unavailable`，secret-safe，未写 raw Gateway artifact。回归：selftest exit `0`；default with E8 artifact runner exit `1` / JSON `actual_exit_code=2` blocked by SQL unavailable；E11 browser readback gate exit `0`；scoped `git diff --check` exit `0`。Next task：在可用 Control Plane paid SQL/readback schema/runtime 下复跑 E11 verifier，使 post_commit_readback、rollback_proof、reconciliation_report、settle/refund idempotency 全 pass 后才允许 E9/QA 组合。
- **E11-007-S103 / TODO-30C-S6 Composer-Required Evidence Mapping Output，完成 / paid 仍 blocked，2026-06-05，owner Agent-E11/Admin Billing**：让 E11 paid readback artifact 在 blocked/pass 两种情况下都输出 Harvey/E9 composer 可读的 top-level `evidence[]` mapping。改动：`scripts/verify_control_plane_paid_ledger_readback.ps1` 新增 Control Plane composer evidence projection，固定三项 `post_commit_readback`、`rollback_proof`、`reconciliation_report`；每项包含 `evidence_key`、`status`、`passed`、`evidence_id`、`request_id`、`operation_id`、`operation`、E8 artifact 来源和 Control Plane SQL/readback 来源；SQL unavailable 时保持 `overall_status=blocked`、`actual_exit_code=2`、`paid_controlled_beta_opened_by_this_check=false`，mapping 状态为 `blocked` / `passed=false`，不得 pass。Selftest 新增：accepted real-ish shape includes composer-required E11 mapping；missing E11 mapping returns `control_plane_paid_readback_evidence_mapping_missing`；SQL unavailable remains blocked but shape-present。命令：`scripts/verify_control_plane_paid_ledger_readback.ps1 -SelfTest` exit `0`；E8 artifact -> E11 output command runner exit `1` / JSON `actual_exit_code=2`，blocker `control_plane_paid_readback_sql_unavailable`，evidence keys present；Harvey composer command runner exit `1` / JSON `actual_exit_code=2`，blockers now `control_plane_paid_readback_not_passed` and `control_plane_paid_readback_sql_unavailable` instead of `control_plane_paid_readback_evidence_mapping_missing`；`scripts/test.ps1 -ControlPlaneLedgerAdjustmentExecuteBrowserReadbackOnly` exit `1` because the existing browser artifact is stale (`release_readback_blocked_by_stale_artifact`, generated_at `2026-06-05T18:25:45Z`, max age 30m), and this gate was not relaxed. Artifacts：`.tmp/paid-beta/e11_control_plane_paid_readback_reconciliation.json` secret-safe, raw Gateway artifact omitted, top-level `evidence[]` contains the three blocked Control Plane readback items; `.tmp/paid-beta/real_paid_evidence_bundle.json` was not written because composer remained blocked. Next task：provide a usable Control Plane paid SQL/readback runtime/schema and rerun E11 verifier; separately refresh E11 browser mutation evidence if release readback gate is required again.
- **E11-007-S104 / TODO-30C-S7 Control Plane Paid SQL Readback Runtime Enablement，完成 / paid 仍 blocked by data gap，2026-06-05，owner Agent-E11/Admin Billing**：将 S6 的 generic `control_plane_paid_readback_sql_unavailable` 收敛为实际 compose SQL readback 和精确 machine blocker。诊断结果：Docker compose `postgres` service 正在运行，database `ai_gateway`、schema `public`、tables `ledger_entries` / `request_logs` / `provider_attempts` / `audit_logs` 均存在；原 generic blocker 根因是 verifier 用 `psql -f scripts/operator/control_plane_paid_ledger_reconciliation_readback.sql` 让容器读取宿主机路径，容器内不存在该 SQL file。修复：`scripts/verify_control_plane_paid_ledger_readback.ps1` 改为 repo-bounded host SQL file + `docker compose exec -T postgres psql ...` stdin 执行，并新增 top-level `diagnostics`：compose file/service、database/schema、execution method、host SQL existence、request id counts、table presence、bounded row counts、secret-safe marker；psql/compose/schema 异常分类为具体 machine blockers，不再兜底 generic unavailable。当前 E8 artifact readback 结果：SQL runtime readback complete，counts 为 ledger `3`、request_logs `3`、provider_attempts `2`、audit `0`、reserve `2`、settle `1`、refund `0`、reversed `2`、insufficient provider attempts `0`；`post_commit_readback`、`rollback_proof`、`reconciliation_report` 三项 Control Plane evidence 均 pass，但 `gateway_hot_path_reserve_settle_refund` 缺 refund ledger row，因此 E11 artifact 仍 `overall_status=blocked`、JSON `actual_exit_code=2`、blocker `control_plane_paid_readback_refund_rows_missing`、paid opened false。Composer 结果：blocked with `control_plane_paid_readback_not_passed` + `control_plane_paid_readback_refund_rows_missing`，未写 real paid bundle。E11 browser release readback regression 仍 blocked by stale browser artifact；stale blocker report `artifacts/control_plane_ledger_execute_release_readback_blocked_stale.json` 保留，未将 stale artifact 作为 pass。Next task：E8/Gateway paid hot path or ledger contract must produce/record an actual refund ledger row, or TODO owners must explicitly align the evidence contract if reserve reversal is the intended refund representation; then rerun E11 verifier and composer.
- **E11-007-S105 / TODO-30C-S8 Consume E8 Refund-After-Settle Evidence，等待 E8 refund row / paid 仍 blocked，2026-06-05，owner Agent-E11/Admin Billing**：单次消费当前 `.tmp/paid-beta/e8_gateway_paid_hot_path.json`，未做 watcher 循环。当前 E8 artifact 为 `smoke_run_id=e8-paid-1780689186388`、`status=passed`、七项 evidence / 3 request ids / 3 operation ids 存在，包含 `refund_idempotency` evidence item，但 Control Plane bounded SQL readback 仍显示实际 ledger rows 中 `refund_count=0`。E11 verifier 命令 runner exit `1` / JSON `actual_exit_code=2`，保持 precise blocker `control_plane_paid_readback_refund_rows_missing`，未退回 generic SQL unavailable；readback counts 仍为 ledger `3`、request_logs `3`、provider_attempts `2`、audit `0`、reserve `2`、settle `1`、refund `0`、reversed `2`、insufficient provider attempts `0`；Control Plane `post_commit_readback`、`rollback_proof`、`reconciliation_report` evidence 均 pass。Composer runner exit `1` / JSON `actual_exit_code=2`，blockers `control_plane_paid_readback_not_passed` + `control_plane_paid_readback_refund_rows_missing`，未写 `.tmp/paid-beta/real_paid_evidence_bundle.json`。E11 browser release gate runner exit `1`，独立 stale blocker report 仍为 `release_readback_blocked_by_stale_artifact`，未作为 pass。Next task：等待 E8 TODO-30B-S5 产出真实 refund-after-settle ledger row/evidence 后再次运行 E11 verifier + composer；若 E8 将 reserve reversal 定义为 refund 表示，需要主线程明确更新 E8/E11/E9/QA contract。
- **E11-007-S106 / TODO-30C-S10 E11 Pass Handoff to E9/QA，E11 pass / paid overall 仍待 E9+QA，2026-06-05，owner Agent-E11/Admin Billing**：主线程提示 E8-S6 已刷新 refund-after-settle evidence 后，复核并重跑 E11 verifier 到默认 handoff path。当前 `.tmp/paid-beta/e8_gateway_paid_hot_path.json` 为 `smoke_run_id=e8-paid-1780691021962`、`status=passed`、七项 evidence、2 unique request ids、3 operation ids。E11 artifact `.tmp/paid-beta/e11_control_plane_paid_readback_reconciliation.json` 复跑后 `overall_status=passed`、`actual_exit_code=0`、`blockers=[]`、`secret_safe.secret_safe=true`、`paid_controlled_beta_opened_by_this_check=false`；bounded SQL readback counts：ledger `3`、request_logs `2`、provider_attempts `1`、audit `0`、reserve `1`、settle `1`、refund `1`、reversed `1`、insufficient provider attempts `0`；Control Plane evidence `post_commit_readback`、`rollback_proof`、`reconciliation_report` 均 `passed=true`。Commands：`verify_control_plane_paid_ledger_readback.ps1 -SelfTest` exit `0`; `verify_control_plane_paid_ledger_readback.ps1 -GatewayArtifactPath .tmp/paid-beta/e8_gateway_paid_hot_path.json -OutputPath .tmp/paid-beta/e11_control_plane_paid_readback_reconciliation.json` exit `0`。Scope：未改 Gateway/E9/QA/Admin UI React，未运行/修改 E9 composer；E11 pass 只表示 Control Plane paid readback handoff ready，不代表 paid overall allowed。Next task：E9-S128 composer/bundle inclusion、E9 readiness gate 和 QA paid acceptance 聚合完成后，主线程再决定 paid controlled beta。
- **E11-CREDIT-01 / Admin Credit-Wallet Surface Gap Audit，完成 / docs-only，2026-06-05，owner Agent-E11/Admin Billing**：审查 Control Plane/Admin 当前 credit-like 产品能力，未改 Gateway、Admin UI React 或 Control Plane 业务逻辑，未影响 paid final accepted 状态。Evidence：`db/migrations/0001_init.sql`、`db/migrations/0002_upgrade_dev_skeleton.sql`、`examples/sql_schema_draft.sql` 均有 `wallets` / `credit_grants` schema；`apps/control-plane/src/admin.rs` router 暴露 `/admin/ledger/entries`、`/admin/ledger/adjustments/dry-run`、`/admin/audit-logs`、`/admin/billing/reconciliation`，且 execute 路径会 lock wallet、写 ledger entry、写 success audit；OpenAPI skeleton 同步这些 ledger/audit/billing endpoints；Admin UI Billing page 和 tests 覆盖 ledger overview、wallet_id filter/input、adjust/refund dry-run/execute、audit/readback secret-safe。未发现 `/admin/wallets`、`/admin/credit-grants`、wallet snapshot aggregation、credit grant CRUD/list view、recharge/voucher/payment/order/invoice Admin/API/UI surface。Capability matrix：wallet/credit DB schema present=`yes`; Admin ledger adjustment/refund present=`yes`; credit grant CRUD present=`no`; wallet balance / credit grant user-facing view present=`no dedicated view`; recharge/voucher/payment/order/invoice present=`no`; audit for ledger adjustment present=`yes`, credit-grant-specific audit=`not applicable until CRUD exists`。Commands：`rg -n "wallet|credit|grant|ledger|adjust|refund|balance|voucher|invoice|payment|order" web/admin-ui/src/components web/admin-ui/src/App.test.tsx -S` exit `0`; `rg -n "/admin/(wallet|credit|billing|ledger|audit)|credit_grants|wallets|recharge|voucher|invoice|payment|order" examples/openapi_admin_skeleton.yaml apps/control-plane/src/admin.rs db/migrations examples/sql_schema_draft.sql -S` exit `0`; `rg -n "create table if not exists (wallets|credit_grants|.*voucher.*|.*invoice.*|.*orders.*|.*payments.*|.*recharge.*)|/admin/(wallet|credit|voucher|invoice|payment|order|recharge)|credit_grants|wallets" db/migrations examples/openapi_admin_skeleton.yaml web/admin-ui/src apps/control-plane/src/admin.rs -S` exit `0`; `cargo test -p ai-control-plane ledger_adjustment_execute -- --nocapture` exit `0` with 6 tests passed. Recommendation：新增 E11-CREDIT-02 read-only Admin wallet/credit surface before CRUD：`GET /admin/wallets` / wallet detail with computed ledger balance, active/expired credit grants, last ledger entry, audit/readback links, OpenAPI/Admin UI tests; E11-CREDIT-03 可再做 credit grant create/expire/revoke with audit；recharge/voucher/payment/order/invoice 应作为 product/payment backlog，不应由当前 ledger adjustment surface 伪装为已支持。
- **E11-CREDIT-02 / Read-only Admin Wallet-Credit Surface Contract，完成 / contract-only，2026-06-05，owner Agent-E11/Admin Billing**：新增 OpenAPI skeleton contract，不实现 Control Plane runtime route、不改 Admin UI React、不做 credit grant create/expire/revoke。Contract endpoints：`GET /admin/wallets` 和 `GET /admin/wallets/{wallet_id}`；均为 tenant-scoped read-only Admin Ledger surface。Response fields：`wallet`（id/tenant/project/name/currency/status/balance_floor/created_at/updated_at）、`credit_grants`（total/active/consumed/expired/voided counts, active/remaining/expired fixed-decimal totals, bounded grant rows with amount/remaining/source/status/validity）、`ledger_balance_window`（window_start/end, confirmed debit/credit/net, pending/reversed fixed-decimal totals, count, last confirmed ledger id）、`pending_reserves`（count/amount/oldest/newest）、`budget_remaining` marker（not_configured/computed/stale/unavailable, source, remaining_amount, conservative deny marker）、`last_ledger_entry_ids`, bounded public-id links for ledger/request/trace/audit, `consistency` marker（read model, computed_at, max_source_updated_at, staleness_status, bounded_window_days, no uncommitted writes）, and `secret_safe` marker（no raw metadata/request payload/credentials/Auth/Cookie/provider key/virtual key/DB URL）。Money fields are fixed-decimal strings; trace/audit links are bounded ids only. Semantic check：YAML parsed with PyYAML, required paths/schemas present. Commands：OpenAPI wallet-credit parse selfcheck exit `0`; `rg -n "/admin/wallets|AdminWalletCreditSurface|AdminWalletSecretSafeMarker|E11-CREDIT-02" examples/openapi_admin_skeleton.yaml docs/todo/slices/E11-007.md TODO/AI_GATEWAY_REBASED_TODO_2026-06-05.md` exit `0`。Next task：E11-CREDIT-02B can add low-risk contract fixture/Admin UI test skeleton if QA wants UI selector coverage; E11-CREDIT-03 remains mutation policy/design for credit grant create/expire/void with transactional audit.
- **E11-CREDIT-02R / TODO-32A Admin read-only wallet/credit runtime implementation，完成 / live readback pending，2026-06-05，owner Agent-E11/Admin Billing**：在 `apps/control-plane/src/admin.rs` 落地只读 Admin runtime endpoints：`GET /admin/wallets`、`GET /admin/wallets/{wallet_id}`。Scope：未改 Gateway/E9 paid gate/Admin UI React；未新增 create/expire/void/revoke credit grant mutation；handlers 仅执行 bounded `select` SQL。Response 覆盖 wallet metadata、credit grant total/active/consumed/expired/voided summary 与 bounded rows、ledger balance window、pending reserves、budget remaining marker、last ledger ids、bounded public-id ledger/request/trace/audit links、consistency/staleness marker、`secret_safe=true`、`read_only=true`。Money 字段经 `FixedDecimal` 规范化为 fixed-decimal string；wallet/grant display fields 走 `redact_secrets`；response secret marker 明确 raw metadata/raw request payload/credential material/Auth/Cookie/provider key/virtual key/DB URL 均未回显。Tests：`cargo test -p ai-control-plane admin_wallet_credit_surface -- --nocapture` exit `0`，2 tests passed，覆盖 response shape/read_only/secret marker/fixed-decimal strings and OpenAPI/runtime path contract。Remaining gap：本轮未跑 live Admin session endpoint readback；QA/TODO-32C 后续可用 seeded wallet/credit/ledger fixture 调 `GET /admin/wallets` 和 detail endpoint 验证 DB rows。
- **E11-CREDIT-02R-live / Admin read-only wallet-credit runtime readback artifact，完成 / blocked unit-only artifact，2026-06-05，owner Agent-E11/Admin Billing**：写出 QA 默认 artifact `.tmp/credit-wallet/admin_readonly_wallet_credit_runtime.json`，schema `admin_readonly_wallet_credit_runtime.v1`。当前 artifact `overall_status=blocked`、`runtime_surface=true`、`read_only=true`、`secret_safe=true`、`money_decimal_strings=true`、`raw_secret_markers_present=false`、`paid_gate_changed=false`、`live_verified=false`、`unit_verified=true`、`live_db_or_unit_readback=unit_readback_only`、`admin_readonly_runtime_verified=false`。Live probe：`/healthz` reachable (`200`)，但 no-session `GET /admin/wallets` 返回 `404` and no admin session/admin creds env configured；因此 blockers=`admin_session_missing`,`live_control_plane_wallet_route_not_current_or_unavailable`,`live_endpoint_readback_not_verified`。QA verifier consumption：`scripts/verify_credit_wallet_ledger_surface.ps1` exit `0`，classifies artifact as `runtime_artifact_present_but_not_verified` and keeps `admin_readonly_runtime_verified=false`，paid gate unchanged。Tests rerun：`cargo test -p ai-control-plane admin_wallet_credit_surface -- --nocapture` exit `0` (2 passed)；`cargo test -p ai-control-plane openapi -- --nocapture` exit `0` (5 passed)。Next task：run/recreate Control Plane from current source, provide valid Admin session handoff, seed/read an existing wallet id, call both endpoints, then rewrite artifact with `overall_status=pass` only after bounded live readback succeeds.
- **E11-CREDIT-03 / TODO-32F Opening Balance Import API boundary，完成 / contract-only route skeleton，2026-06-05，owner Agent-E11/Admin Billing**：确认 opening-balance import belongs in the Control Plane Admin/API boundary as `POST /billing/opening-balance-imports` behind Admin session + `billing_adjust` RBAC. 本轮只落地 OpenAPI skeleton、Control Plane 501 contract-only route skeleton、RBAC path mapping and serialization/OpenAPI tests；未实现 live ledger mutation、opening import DB table、audit write、idempotent replay persistence, or live artifact。Contract input covers `tenant_id`, optional `project_id`, `wallet_id`, `currency`, `opening_amount` fixed-decimal string, `external_source`, `external_reference_id`, `effective_at`, `reason`, `actor_id`, `actor_type`, and required `idempotency_key`; output covers nullable `opening_import_id`, `ledger_entry_id`, `admin_adjustment_entry_id`, `audit_id`, status `imported|replayed|refused`, idempotency result, request summary with raw idempotency omitted, guardrails, and `secret_safe=true`. Guardrails recorded：Admin/RBAC required, idempotency required for writes, direct wallet snapshot mutation forbidden, ledger/admin adjustment entry required for runtime apply, all outcomes audit-safe, raw import payload/token/Auth/Cookie/DB URL/provider key/virtual key/raw idempotency material not echoed. Commands：`cargo fmt -p ai-control-plane` exit `0`; `cargo test -p ai-control-plane opening_balance_import -- --nocapture` exit `0` (2 passed); `cargo test -p ai-control-plane openapi -- --nocapture` exit `0` (6 passed). Scope：未改 Gateway/Admin UI React/billing-ledger crate；`paid_gate_changed=false`。Next task：TODO-32F-runtime should implement a transactional import writer or explicit `opening_balance_imports` table, lock/read wallet, write confirmed admin adjustment/opening ledger entry and audit summary with idempotency/replay/refusal readback, then write `.tmp/credit-wallet/opening_balance_import_contract.json` or `artifacts/credit_wallet_opening_balance_import.json` only after live readback succeeds.
- **E11-CREDIT-04 / TODO-32F runtime implementation feasibility and bounded DoD，完成 / docs-only feasibility，2026-06-05，owner Agent-E11/Admin Billing**：审查现有 `execute_ledger_adjustment` runtime 后确认可复用的 Control Plane pieces：single DB transaction, wallet lock/read via `select ... for update`, `ledger_entries` insert with `(tenant_id,idempotency_key)` uniqueness, success `audit_logs` insert, and rollback on ledger/audit failure. Feasibility verdict：runtime is feasible, but production DoD should add `opening_balance_imports` table before live acceptance because ledger metadata alone cannot safely enforce duplicate `(tenant_id, external_source, external_reference_id)` conflicts under concurrency or preserve `opening_import_id`/replay status as a first-class readback object. A ledger/audit-only marker could be a prototype but must remain `runtime_partial_not_QA_accepted`. Minimal TODO-32F-runtime DoD：transaction starts after Admin/RBAC; validate tenant/currency/positive fixed-decimal/opening source/reference/reason/actor/idempotency; lock wallet and verify tenant/currency/project; `select ... for update` opening import by idempotency key and external reference; same idempotency key + identical body replays original ids without new ledger/audit writes; same key with different body refuses `idempotency_conflict`; duplicate external reference with different key refuses `external_reference_conflict`; new import inserts `opening_balance_imports(status='imported')`, confirmed `ledger_entries(entry_type='adjust', amount>0, metadata.operation='opening_balance_import')` or accepted admin-adjustment writer equivalent, and audit log in one transaction; audit failure rolls back ledger/import; refused outcomes return audit-safe summaries and never echo raw import payload/Auth/Cookie/DB URL/provider key/virtual key/raw idempotency material. QA artifact shape：schema `opening_balance_import_runtime.v1`, `overall_status`, `runtime_implemented`, `contract_only=false`, `paid_gate_changed=false`, endpoint, ids (`opening_import_id`,`ledger_entry_id`/`admin_adjustment_entry_id`,`audit_id`), statuses imported/replayed/refused, idempotency result, external reference conflict proof, rollback/refusal taxonomy, DB readback booleans, money decimal string marker, `secret_safe=true`, and bounded command/test records. Commands：`rg -n "ledger_adjustment|insert into ledger_entries|audit_logs|opening_balance_imports|idempotency_key" apps/control-plane/src/admin.rs db/migrations -S` exit `0`; `cargo test -p ai-control-plane opening_balance_import -- --nocapture` exit `0` (2 passed); scoped `git diff --check` exit `0`。Scope：docs/TODO only in this slice; no Gateway/Admin UI React/billing-ledger runtime changes; `paid_gate_changed=false`。Next task：TODO-32F-S1 schema patch for `opening_balance_imports` + route runtime plan/DB tests, then TODO-32F-S2 live artifact writer/readback.
- **E11-CREDIT-05 / TODO-32F-S1 opening_balance_imports schema and runtime skeleton plan，完成 / schema added + runtime plan only，2026-06-05，owner Agent-E11/Admin Billing**：新增真实 migration `db/migrations/0011_opening_balance_imports.sql` 并同步 `examples/sql_schema_draft.sql`。Schema fields：`id` as opening import id, tenant/project/wallet, `currency`, positive `opening_amount numeric(20,8)`, `external_source`, `external_reference_id`, `effective_at`, `reason`, `actor_id`, `actor_type`, `idempotency_key`, `status`, nullable `ledger_entry_id` / `admin_adjustment_entry_id` / `audit_id`, safe `request_summary` and `metadata` JSON, created/updated timestamps. Constraints：unique `(tenant_id,idempotency_key)` and `(tenant_id,external_source,external_reference_id)`；indexes cover tenant created time, tenant/wallet/status/created time, project created time, ledger entry and audit ids；checks enforce currency shape, positive amount, non-empty identity fields, status `imported|replayed|refused`, object JSON. Control Plane route remains contract-only 501, but response now includes `runtime_plan` and `readback_marker` showing schema table/migration, transaction boundary, wallet lock, read/write sequence, idempotency/external-reference conflict behavior, outcomes apply/replay/conflict/refusal, QA artifact shape, and secret-safe markers; no live mutation or DB write is performed. Tests：`cargo test -p ai-control-plane opening_balance_import -- --nocapture` exit `0` (3 passed, including DB-free runtime plan); `cargo test -p ai-control-plane openapi -- --nocapture` exit `0` (6 passed); required `rg` schema evidence exit `0`; scoped `git diff --check` exit `0`。Scope：未改 Gateway/billing-ledger crate/Admin UI React；`paid_gate_changed=false`。Next task：TODO-32F-S2 implement bounded runtime writer using `opening_balance_imports` with live DB tests/readback artifact; accepted runtime still pending.
- **E11-CREDIT-06 / TODO-32F-S2 runtime mutation design-to-code first patch，完成 / partial SQL contract，2026-06-05，owner Agent-E11/Admin Billing**：未启用 live mutation；继续保持 `POST /billing/opening-balance-imports` route 为 contract-only 501。新增 DB-free runtime SQL contract `opening_balance_import_sql_contract.v1`，嵌入 `runtime_plan.sql_contract`，把下一步 live implementation 固定为可测试 statements/outcomes：`lock_wallet` (`wallets ... for update`), `read_by_idempotency` (`opening_balance_imports ... idempotency_key ... for update`), `read_by_external_reference` (`external_source` + `external_reference_id ... for update`), `insert_opening_import`, `insert_ledger_entry` as confirmed `adjust`, `insert_audit_log` action `billing.opening_balance_import`, and `link_import` updating ledger/admin-adjustment/audit ids. Outcome contract covers apply/imported with new import+ledger+audit, replay/replayed with no new ledger/audit, idempotency conflict/refused, external reference conflict/refused, and wallet/currency refusal without ledger write. Secret-safe contract keeps raw import payload, raw idempotency material, Auth/Cookie, DB URL, provider key and virtual key omitted. Tests：`cargo test -p ai-control-plane opening_balance_import -- --nocapture` exit `0` (4 passed, including SQL contract); `cargo test -p ai-control-plane openapi -- --nocapture` exit `0` (6 passed); scoped `git diff --check` exit `0`。Runtime status：`partial` / design-to-code SQL contract only；no live DB mutation/readback artifact yet. Scope：未改 Gateway/billing-ledger crate/Admin UI React；`paid_gate_changed=false`。Next task：TODO-32F-S3 replace contract-only handler with bounded transaction behind feature/explicit runtime path or implement internal function with DB integration tests: validate request, lock wallet, perform replay/conflict checks, insert import+ledger+audit atomically, return secret-safe applied/replayed/refused response, then write live artifact only after readback.
- **E11-CREDIT-07 / TODO-32F-S3 bounded runtime transaction attempt，完成 / partial internal transaction-shape function，2026-06-05，owner Agent-E11/Admin Billing**：尝试推进 runtime，但未启用 route live mutation、未写 artifact。`POST /billing/opening-balance-imports` 仍返回 contract-only 501 / `runtime_implemented=false`；新增 internal DB-free `opening_balance_import_runtime_attempt_contract_shape`，将 request + wallet snapshot + existing import snapshots 转成 transaction outcome shape，用于下一步接入真实 DB transaction。Internal function enforces validated request, wallet tenant/id/project/currency/status check, idempotency replay/conflict, external-reference conflict, apply shape, refusal shape, transaction order, and secret-safe response. Tests cover：apply/imported would insert import+confirmed adjust ledger+audit+link and not mutate wallet snapshot；same key/body replay returns original ids and no new ledger/audit；same key different body -> `idempotency_conflict`; duplicate external reference different idempotency key -> `external_reference_conflict`; wallet currency mismatch -> refusal/no ledger write；serialized shapes omit raw idempotency material, raw import payload, Auth/Cookie, DB URL, provider key, virtual key. Commands：`cargo fmt -p ai-control-plane` exit `0`; `cargo test -p ai-control-plane opening_balance_import -- --nocapture` exit `0` (5 passed); `cargo test -p ai-control-plane openapi -- --nocapture` exit `0` (6 passed); scoped `git diff --check` exit `0`。Runtime status：`partial` / internal transaction-shape only；no live DB writes/readback, no artifact. Scope：未改 Gateway/billing-ledger crate/Admin UI React；`paid_gate_changed=false`。Next task：TODO-32F-S4 wire the internal attempt shape to actual `sqlx` transaction under tests or feature gate, then prove DB apply/replay/conflict/refusal readback before changing route from 501 or writing live artifact.
- **E11-CREDIT-08 / TODO-32F-S4 wire internal opening import shape to sqlx transaction，完成 / partial compiled internal sqlx function，2026-06-05，owner Agent-E11/Admin Billing**：新增 compiled internal async function `execute_opening_balance_import_internal_tx`，但未挂到 public route；`POST /billing/opening-balance-imports` 仍保持 contract-only 501，未写 runtime artifact。Internal transaction function uses `sqlx::Transaction<'_, Postgres>` and implements the intended order：validate request -> lock wallet `for update` -> read `opening_balance_imports` by idempotency `for update` -> read by external reference `for update` -> decide replay/conflict/refusal/apply -> insert `opening_balance_imports` -> insert confirmed `ledger_entries(entry_type='adjust')` with bounded metadata -> insert `audit_logs` via existing audit helper -> update/link `opening_balance_imports` with ledger/admin-adjustment/audit ids -> return secret-safe applied shape. Helper functions compile for wallet/import row mapping, opening import insert, ledger insert, audit metadata/resource, link update, and applied response. Tests remain DB-free: source-order test proves transaction command order and that the public route does not call the internal function; shape tests still cover apply/replay/idempotency conflict/external reference conflict/wallet refusal; secret-safe checks remain. Commands：`cargo fmt -p ai-control-plane` exit `0`; `cargo test -p ai-control-plane opening_balance_import -- --nocapture` exit `0` (6 passed); `cargo test -p ai-control-plane openapi -- --nocapture` exit `0` (6 passed); scoped `git diff --check` exit `0`。Runtime status：`partial`; `sqlx_transaction_function_added=true`; `route_live_changed=false`; no live DB readback and no artifact. Scope：未改 Gateway/billing-ledger crate/Admin UI React；`paid_gate_changed=false`。Next task：TODO-32F-S5 run an opt-in DB integration/readback against seeded Control Plane DB, then only after apply/replay/conflict/refusal readback passes wire the route or write `runtime_implemented=true` artifact.
- **E11-CREDIT-09 / TODO-32F-S5 opt-in DB integration/readback for opening balance import，完成 / blocked artifact + verifier scaffold，2026-06-05，owner Agent-E11/Admin Billing**：新增 `scripts/verify_opening_balance_import_runtime.ps1`，作为 opt-in runtime/readback verifier scaffold；默认不连接 DB、不执行 live mutation、不更改 public route。Default artifact `.tmp/credit-wallet/opening_balance_import_runtime.json` 写出 `schema=opening_balance_import_runtime.v1`、`overall_status=blocked`、`runtime_implemented=false`、`contract_only=true`、`db_integration_ran=false`、`route_live_changed=false`、`paid_gate_changed=false`、`secret_safe=true`、`raw_secret_markers_present=false`；checks confirm internal compiled sqlx function present, route remains 501 contract-only, schema present, source/migration secret-safe；blocker=`opening_balance_import_db_integration_not_requested`。显式 `-RunDbIntegration` without `OPENING_BALANCE_IMPORT_DB_OPT_IN=1` writes `.tmp/credit-wallet/opening_balance_import_runtime.optin_blocked.json` with blocker=`opening_balance_import_db_opt_in_env_missing`；script does not echo DB URL/env/token/idempotency material。Selftest：`pwsh ... verify_opening_balance_import_runtime.ps1 -SelfTest` exit `0`。Regression：`cargo test -p ai-control-plane opening_balance_import -- --nocapture` exit `0` (6 passed); `cargo test -p ai-control-plane openapi -- --nocapture` exit `0` (6 passed)。Runtime status remains `partial/blocked`; no DB integration ran, no live artifact, no `runtime_implemented=true` evidence. Scope：未改 Gateway/billing-ledger crate/Admin UI React；`paid_gate_changed=false`。Next task：TODO-32F-S6 implement the actual opt-in DB runner behind `OPENING_BALANCE_IMPORT_DB_OPT_IN=1`, seed wallet/import cases, execute internal transaction, prove apply/replay/conflict/refusal ledger/audit readback, then and only then write runtime true artifact or wire route.
- **E11-CREDIT-10 / TODO-32F-S6 actual opt-in DB runner guard / executable DB plan，完成 / blocked by missing env，2026-06-05，owner Agent-E11/Admin Billing**：增强 `scripts/verify_opening_balance_import_runtime.ps1`：`-RunDbIntegration` 现在有真实 opt-in guard and executable rollback-contained `psql` DB plan。Required env：`OPENING_BALANCE_IMPORT_DB_OPT_IN=1` plus `CONTROL_PLANE_DATABASE_URL` or `DATABASE_URL`；script never prints those values。When env is present, runner is prepared to seed/find a bounded wallet, insert `opening_balance_imports`, insert confirmed `ledger_entries(entry_type='adjust')`, insert `audit_logs`, link import with ledger/audit ids, prove replay same key/body, idempotency conflict, external reference conflict, ledger/audit readback, and rollback-contained no-persistence behavior. Because current environment lacks opt-in/DB URL, no DB connection was attempted：default artifact `.tmp/credit-wallet/opening_balance_import_runtime.json` remains `overall_status=blocked`, `runtime_implemented=false`, `db_integration_ran=false`, `required_env=[OPENING_BALANCE_IMPORT_DB_OPT_IN, CONTROL_PLANE_DATABASE_URL or DATABASE_URL]`, missing both envs, blocker `opening_balance_import_db_integration_not_requested`; explicit `-RunDbIntegration` artifact `.tmp/credit-wallet/opening_balance_import_runtime.optin_blocked.json` blocks on `opening_balance_import_db_opt_in_env_missing` and `opening_balance_import_database_url_env_missing`。`db_runner_implemented=true` as rollback-contained executable DB plan, but `route_live_changed=false` and `runtime_implemented=false` because route/internal Rust live path was not invoked and DB readback did not run. Commands：selftest exit `0`; default artifact command exit `1` blocked; opt-in blocked command exit `1`; `cargo test -p ai-control-plane opening_balance_import -- --nocapture` exit `0` (6 passed); `cargo test -p ai-control-plane openapi -- --nocapture` exit `0` (6 passed); scoped `git diff --check` exit `0`。Scope：未改 Gateway/billing-ledger crate/Admin UI React；`paid_gate_changed=false`。Next task：TODO-32F-S7 provide DB env/opt-in and run executable DB plan; if rollback-contained plan passes, decide whether to wire public route/internal Rust execution and produce `runtime_implemented=true` only after route/internal readback is real.
- **Main-thread TODO-32F-S7 / local compose DB plan readback，完成 / partial DB-plan evidence，2026-06-05，owner Main Review**：local compose stack had `postgres` running on localhost and `psql` available；applied `db/migrations/0011_opening_balance_imports.sql` to compose DB, fixed `scripts/verify_opening_balance_import_runtime.ps1` so DB-plan readback uses statement-separated import/ledger/audit/link/readback instead of same-statement data-modifying CTE reads, and reran opt-in DB integration. Current `.tmp/credit-wallet/opening_balance_import_runtime.json` is `overall_status=partial`, `db_integration_ran=true`, `db_runner_implemented=true`, `executable_db_plan.passed=true`, `apply_readback_passed=true`, `replay_same_key_body_passed=true`, `idempotency_conflict_refusal_passed=true`, `external_reference_conflict_refusal_passed=true`, `ledger_entry_readback_passed=true`, `audit_readback_passed=true`, `wallet_currency_refusal_no_ledger_write_plan_passed=true`, `transaction_rolled_back=true`, and `secret_safe=true`. It intentionally remains `runtime_implemented=false`, `contract_only=true`, `route_invoked=false`, and blockers are `opening_balance_import_public_route_not_wired` and `opening_balance_import_internal_rust_function_not_invoked_by_verifier`; QA verifier still reports `opening_balance_import_runtime_verified=false`. Commands：`verify_opening_balance_import_runtime.ps1 -SelfTest` exit `0`; opt-in DB verifier exit `1` because status partial/runtime false; `cargo test -p ai-control-plane opening_balance_import -- --nocapture` exit `0`; `cargo test -p ai-control-plane openapi -- --nocapture` exit `0`; `verify_credit_wallet_ledger_surface.ps1` exit `0` with `pass_with_productization_gaps`。Next task：E11-CREDIT-11 wire public route or verifier-callable internal Rust/sqlx transaction and produce runtime true only after route/internal Rust readback is real.
- **E11-CREDIT-11 / TODO-32F-S8 route/internal Rust runtime invocation + replay fix，完成 / live route artifact verified，2026-06-05，owner Agent-E11/Admin Billing + Main Review**：wired `POST /billing/opening-balance-imports` to `execute_opening_balance_import_internal_tx` behind existing Admin/RBAC and kept the contract-only helper for contract tests. Critical replay bug fixed：same idempotency key + same logical body now compares canonical fixed-decimal amount and canonicalized `effective_at` (`2026-06-05T12:00:00Z` equals DB `2026-06-05 12:00:00+00`) and returns `status=replayed`, `outcome=replay`, original `opening_import_id` / `ledger_entry_id` / `audit_id`, with no new ledger/audit rows. Same-key different body still refuses `idempotency_conflict`; duplicate external reference different key still refuses `external_reference_conflict`; wallet currency mismatch still refuses without import/ledger write. `scripts/verify_opening_balance_import_runtime.ps1` now requires live route proof for runtime pass: DB/Rust-only evidence can be partial, but `runtime_implemented=true` requires `CONTROL_PLANE_ADMIN_SESSION_TOKEN` route probe plus DB readback. Main-thread rebuilt compose control-plane with `POSTGRES_HOST_PORT=55432` and `REDIS_HOST_PORT=56379`; live artifact `.tmp/credit-wallet/opening_balance_import_runtime.json` is `overall_status=pass`, `runtime_implemented=true`, `contract_only=false`, `route_invoked=true`, `internal_sqlx_function_invoked=true`, `live_route_probe_ran=true`, `opening_import_readback_passed=true`, `ledger_entry_readback_passed=true`, `audit_readback_passed=true`, `replay_readback_passed=true`, `refusal_readback_passed=true`, `rollback_readback_passed=true`, and `secret_safe=true`; live route matrix passed apply HTTP 201, replay HTTP 200 same ids, idempotency conflict, external-ref conflict, and wallet-currency refusal. Local commands：`cargo fmt -p ai-control-plane` exit `0`; `cargo test -p ai-control-plane opening_balance_import -- --nocapture` exit `0` (7 passed, 1 ignored); ignored Rust/sqlx DB integration with opt-in env exit `0`; `cargo test -p ai-control-plane openapi -- --nocapture` exit `0`; verifier selftest exit `0`; verifier default blocked exit `1`; opt-in verifier without Admin session writes partial with blocker `opening_balance_import_admin_session_token_missing_for_live_route_probe`; `verify_credit_wallet_ledger_surface.ps1` exit `0` and reports `opening_balance_import_runtime_verified=true` plus `new_api_one_api_balance_import=runtime_verified`. Scope：未改 Gateway/billing-ledger crate/Admin UI React；`paid_gate_changed=false`。Next task：QA/main review can keep consuming the pass artifact; any rerun that lacks Admin session must remain partial/blocked and must not overwrite a verified live artifact as pass.
- **E11-CREDIT-12 / TODO-32G credit grant CRUD Admin API first slice，完成 / create+list runtime code + contract artifact，2026-06-05，owner Agent-E11/Admin Billing**：implemented first Control Plane/Admin slice for credit grants without Gateway/Admin UI changes. Runtime routes：`GET /admin/credit-grants` lists bounded secret-safe grant rows with fixed-decimal strings；`POST /admin/credit-grants` creates an active credit grant behind existing Admin/RBAC (`BillingRead` for GET, `BillingAdjust` for POST). Create validates tenant, wallet, positive fixed decimal amount, currency, source, reason, actor fields, optional RFC3339 validity window, and required idempotency key. Idempotency uses a secret-safe SHA-256 fingerprint plus transaction advisory lock and stored metadata fingerprint：same key/body replays the existing grant with no new audit；same key/different body returns `idempotency_conflict`；wallet/currency mismatch refuses without grant/audit；no direct wallet snapshot mutation is performed. Successful create inserts `credit_grants` and `credit_grant.create` audit in one DB transaction, returning schema `credit_grant_crud_runtime.v1`, grant id/status, audit id, idempotency/readback markers, `secret_safe=true`, `paid_gate_changed=false`, and no raw idempotency/Auth/Cookie/DB URL/provider key/virtual key. OpenAPI adds `GET/POST /admin/credit-grants`, `AdminCreditGrantCreateRequest`, `AdminCreditGrantRuntimeResult`, and list/runtime envelopes. Contract artifact `.tmp/credit-wallet/credit_grant_crud_contract.json` refreshed as contract/pass with `control_plane_endpoints_present=true`, implemented scope create+list, pending expire/revoke/read/live runtime artifact. Commands：`cargo fmt -p ai-control-plane` exit `0`; `cargo test -p ai-control-plane credit_grant -- --nocapture` exit `0` (2 passed); `cargo test -p ai-control-plane openapi -- --nocapture` exit `0` (7 passed); `cargo test -p ai-control-plane billing_adjust_writes_require_billing_adjust -- --nocapture` exit `0`; `cargo test -p ai-control-plane permission_map_requires_billing_read_for_reconciliation -- --nocapture` exit `0`; `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_credit_wallet_ledger_surface.ps1` exit `0` (`pass_with_productization_gaps`, credit grant CRUD contract verified but runtime not verified); artifact secret scan exit `0`。Remaining：TODO-32G-S2 implement expire/revoke/read-by-id with idempotency/audit/readback；TODO-32G-S3 live Admin session + DB route matrix artifact `.tmp/credit-wallet/credit_grant_crud_runtime.json` before QA can set `credit_grant_crud_runtime_verified=true`.
- **E11-CREDIT-13 / TODO-32G-S2 complete credit grant read/expire/revoke runtime shape，完成 / lifecycle code + contract artifact，2026-06-05，owner Agent-E11/Admin Billing**：added Admin runtime routes `GET /admin/credit-grants/{credit_grant_id}`, `POST /admin/credit-grants/{credit_grant_id}/expire`, and `POST /admin/credit-grants/{credit_grant_id}/revoke`; retained existing create/list routes. RBAC maps list/read to `BillingRead` and create/expire/revoke to `BillingAdjust`, with capability fixture updated. Expire/revoke run in a DB transaction after Admin/RBAC, lock the credit grant, lock idempotency by secret-safe fingerprint, replay same-key/same-body to the original lifecycle audit id, refuse same-key/different-body as `idempotency_conflict`, refuse wallet/currency/status guard failures without direct wallet snapshot mutation, update grant status (`expired` or `voided`), and write success audit in the same transaction. Create replay now looks up the original create audit id when available; create amount validation now rejects `FixedDecimal.units() <= 0`, including `-1.00000000`, satisfying `non_positive_amount_refusal`. OpenAPI skeleton now covers read/expire/revoke paths and `AdminCreditGrantLifecycleRequest`; responses remain `credit_grant_crud_runtime.v1`, fixed-decimal, secret-safe, and `paid_gate_changed=false`. Contract artifact `.tmp/credit-wallet/credit_grant_crud_contract.json` refreshed with implemented lifecycle scope but still `runtime_implemented=false` / `contract_only=true`; no live DB route matrix artifact was generated in this slice. Commands：`cargo fmt -p ai-control-plane` exit `0`; `cargo test -p ai-control-plane credit_grant -- --nocapture` exit `0` (4 passed); `cargo test -p ai-control-plane openapi -- --nocapture` exit `0` (7 passed); `cargo test -p ai-control-plane rbac -- --nocapture` exit `0` (17 passed). Remaining：TODO-32G-S3 live Admin session + DB route matrix artifact `.tmp/credit-wallet/credit_grant_crud_runtime.json` proving create/list/read/expire/revoke/replay/conflict/refusal/audit/readback and secret-safety before QA can mark runtime verified.
## E11-CREDIT-15 / TODO-32H user-facing remaining balance runtime API

Status: completed as Admin-readonly runtime / full user scope pending, 2026-06-06, owner Agent-E11/Admin Billing.

Result: added read-only Control Plane route `GET /billing/wallets/{wallet_id}/remaining-balance` behind the current Admin/BillingRead boundary. The response schema is `user_remaining_balance_runtime.v1` and includes wallet/tenant/project/currency, decimal-string `available_to_spend`, `wallet_balance_floor`, `active_credit_grant_total`, `pending_confirmed_ledger_window`, ledger/grant bounded ids, budget marker, consistency/readback markers, `read_only=true`, `secret_safe=true`, and `paid_gate_changed=false`. Formula is `active_credit_grant_total + pending_confirmed_ledger_window - wallet_balance_floor`. This does not modify Gateway paid hot path or paid artifacts.

Artifact: `.tmp/credit-wallet/user_remaining_balance_runtime.json` is `overall_status=pass`, `runtime_implemented=true`, `contract_only=false`, `admin_readonly_runtime=true`, `user_api_runtime=false`, `available_to_spend=4.50000000`, wallet/credit-grant/ledger/refusal readback booleans true, and secret-safe. `scripts/verify_credit_wallet_ledger_surface.ps1` reports `user_remaining_balance_admin_readonly_runtime_verified=true`; full `user_remaining_balance_runtime_verified=false` remains correct until a dedicated user/developer-token ownership boundary is implemented and verified.

Commands: `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_user_remaining_balance_runtime.ps1 -SelfTest` exit 0; `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_user_remaining_balance_runtime.ps1 -RunLiveRouteMatrix -OutputPath .tmp/credit-wallet/user_remaining_balance_runtime.json` exit 0; `cargo test -p ai-control-plane remaining_balance -- --nocapture` exit 0; `cargo test -p ai-control-plane openapi -- --nocapture` exit 0; `cargo test -p ai-control-plane permission_map_requires_billing_read_for_reconciliation -- --nocapture` exit 0; `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_credit_wallet_ledger_surface.ps1 -SelfTest` exit 0; `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_credit_wallet_ledger_surface.ps1` exit 0; `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/scan_secrets.ps1` exit 0.

Remaining gap / next task: TODO-32H-S2 should add a real user/developer-token ownership/session boundary for this read model, then regenerate a full user runtime artifact. Recharge/voucher, payment/order/invoice, and subscription/package lifecycle remain separate TODO-32I/J/K productization work.

## E11-CREDIT-16 / TODO-32H-S2 user/developer-token ownership scoped remaining-balance plan

Status: completed as blocked runtime plan / no full user runtime, 2026-06-06, owner Agent-E11/Admin Billing.

Auth review: Control Plane `Authorization: Bearer`, `x-admin-session`, and admin cookie auth all resolve through `authenticate_headers` to `AdminSession`. `AdminSession` carries tenant and roles but not a wallet/project user ownership proof. Admin API key profile and virtual key endpoints manage project-scoped objects, but there is no Control Plane middleware that authenticates a virtual key/developer token and resolves it to a tenant/project/wallet ownership scope. Gateway has virtual-key auth for model data-plane admission, but that is intentionally not reused for Control Plane wallet disclosure.

Artifact: `.tmp/credit-wallet/user_remaining_balance_ownership_plan.json` is `schema=user_remaining_balance_ownership_scope_plan.v1`, `overall_status=blocked`, `auth_primitive_found=false`, `user_api_runtime=false`, `ownership_scope_verified=false`, `runtime_implemented=false`, `contract_only=true`, `read_only=true`, `secret_safe=true`, `paid_gate_changed=false`, and blockers `control_plane_user_or_developer_token_auth_primitive_missing` plus `wallet_ownership_scope_not_verifiable_without_new_auth_boundary`. The existing Admin-readonly artifact `.tmp/credit-wallet/user_remaining_balance_runtime.json` remains pass and must continue to report `admin_readonly_runtime=true` / `user_api_runtime=false`.

Commands: `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_user_remaining_balance_runtime.ps1 -OwnershipPlanOnly -OwnershipPlanOutputPath .tmp/credit-wallet/user_remaining_balance_ownership_plan.json` exit 1 expected blocked; `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_user_remaining_balance_runtime.ps1 -RunLiveRouteMatrix -OutputPath .tmp/credit-wallet/user_remaining_balance_runtime.json` exit 0; `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_user_remaining_balance_runtime.ps1 -SelfTest` exit 0.

Remaining gap / next task: TODO-32H-S3 should add a real Control Plane user/developer-token auth middleware or session type that resolves tenant/project/user or virtual-key ownership, then prove wallet scope/currency/refusal/no-write readback with `user_api_runtime=true` and `ownership_scope_verified=true`.

## E11-CREDIT-17 / TODO-32H-S3 bounded user/developer-token auth contract first slice

Status: completed as contract-ready / runtime blocked, 2026-06-06, owner Agent-E11/Admin Billing.

Auth contract: `apps/control-plane/src/auth.rs` now has a DB-free remaining-balance ownership contract decision used by focused tests. It rejects AdminSession as `admin_session_not_user_ownership_scope`, rejects direct Gateway virtual-key reuse as `gateway_virtual_key_not_control_plane_auth_boundary`, rejects unverified client scope as `token_scope_not_server_verified`, and only allows `user_api_runtime=true` / `ownership_scope_verified=true` for server-verified Control Plane user/developer tokens with matching tenant, project, and wallet scope. OpenAPI adds `x-user-remaining-balance-auth-contract` on `GET /billing/wallets/{wallet_id}/remaining-balance` with the same accepted/rejected sources, scope fields, and refusal taxonomy.

Artifact: `.tmp/credit-wallet/user_remaining_balance_ownership_plan.json` is now `overall_status=contract_ready`, `auth_contract_status=contract_ready_runtime_blocked`, `runtime_implemented=false`, `contract_only=true`, `user_api_runtime=false`, `ownership_scope_verified=false`, `admin_readonly_runtime=true`, `secret_safe=true`, and `paid_gate_changed=false`. It records token source contract, ownership scope resolution rules, AdminSession isolation, and Gateway virtual-key cross-boundary guardrails. The Admin-readonly runtime artifact remains pass and must continue to report `admin_readonly_runtime=true` / `user_api_runtime=false`.

Remaining gap / next task: TODO-32H-S4 should implement a real server-side Control Plane user session or developer-token resolver, including lookup/readback to tenant/project/wallet scope and refusal/no-write evidence. Until that lands, `scripts/verify_credit_wallet_ledger_surface.ps1` must continue to verify Admin-readonly runtime only and keep full `user_remaining_balance_runtime_verified=false`.

## E11-CREDIT-18 / TODO-32H-S4 server-side ownership resolver feasibility spike

Status: completed as feasible contract-only resolver skeleton / runtime still blocked, 2026-06-06, owner Agent-E11/Admin Billing.

Resolver feasibility: existing schema has enough server-side data for a first resolver slice without a new migration. `user_sessions` can be parsed by lookup prefix/hash and joined to `project_members` plus `wallets` for user-session project/wallet scope. `virtual_keys` can be parsed by key prefix/secret hash and joined to `wallets` for developer-token project/wallet scope. `projects` and wallet status/currency checks remain part of the readback/refusal plan. The missing piece is Control Plane middleware/principal wiring for non-AdminSession callers; the route must not trust client-submitted wallet claims and must not directly reuse Gateway data-plane auth without server-side lookup/hash verification.

Code artifact: `apps/control-plane/src/auth.rs` now includes DB-free SQL skeleton contract tests for both planned resolvers. They assert server-side lookup fields, tenant/project/wallet joins, active/revoked/expiry guards, wallet scope matching, and secret-safe non-echo behavior. OpenAPI `x-user-remaining-balance-auth-contract` now records `resolver_feasibility=feasible_contract_only`, evaluated data sources `user_sessions`, `project_members`, `virtual_keys`, and `wallets`, and `runtime_middleware_required=true`.

Runtime artifact: `.tmp/credit-wallet/user_remaining_balance_ownership_plan.json` remains `overall_status=contract_ready`, `auth_contract_status=contract_ready_runtime_blocked`, `runtime_implemented=false`, `contract_only=true`, `user_api_runtime=false`, `ownership_scope_verified=false`, `secret_safe=true`, and `paid_gate_changed=false`. Its `resolver_feasibility.status` is `feasible_contract_only`, `requires_new_schema=false`, `requires_new_middleware=true`, and `runtime_wired=false`.

Remaining gap / next task: TODO-32H-S5 should implement `RemainingBalancePrincipal` middleware/resolver for user-session or developer-token callers, wire the route to accept that principal without weakening Admin/RBAC, and produce a live user/developer-token route matrix artifact with `ownership_scope_verified=true`.

## E11-CREDIT-19 / TODO-32H-S5 minimal RemainingBalancePrincipal wiring attempt

Status: partial code wiring / live ownership route artifact blocked, 2026-06-06, owner Agent-E11/Admin Billing.

Implementation attempt: `apps/control-plane/src/auth.rs` now includes a real `RemainingBalancePrincipal` type and server-side resolver functions for remaining-balance ownership. The user-session resolver parses a session token into lookup prefix/hash, joins `user_sessions -> users -> project_members -> wallets`, and requires active session/user/wallet plus wallet project match. The developer-token resolver parses a Bearer virtual key into key prefix/secret hash, joins `virtual_keys -> wallets`, and requires active non-expired key plus wallet project match. `apps/control-plane/src/rbac.rs` keeps Admin/BillingRead behavior for the remaining-balance path, but falls through to the ownership resolver for non-Admin/BillingRead callers. `apps/control-plane/src/admin.rs` can mark responses as `user_api_runtime=true` / `ownership_scope_verified=true` only when a `RemainingBalancePrincipal` extension exists.

Live artifact: `.tmp/credit-wallet/user_remaining_balance_ownership_runtime.json` is `overall_status=blocked`, `runtime_implemented=false`, `user_api_runtime=false`, `ownership_scope_verified=false`, `secret_safe=true`, and blocker `control_plane_remaining_balance_ownership_route_not_accepting_seeded_user_session`. The verifier seeded a bounded project, user, user session, project membership, wallet, credit grant, and ledger rows; DB readback for session/project membership/wallet/credit/ledger passed, but the public route did not accept the seeded user session in the current runtime. This is not a full user runtime pass.

Regression: `.tmp/credit-wallet/user_remaining_balance_runtime.json` remains the Admin-readonly pass artifact; `scripts/verify_credit_wallet_ledger_surface.ps1` must continue to report `user_remaining_balance_admin_readonly_runtime_verified=true` and full `user_remaining_balance_runtime_verified=false` until the ownership route matrix passes.

Remaining gap / next task: TODO-32H-S6 should rebuild/restart current Control Plane runtime if needed, then re-run the ownership matrix. If the route still rejects the seeded session on current source, debug `require_admin_rbac` extension insertion and `RemainingBalancePrincipal` extraction.

## E11-CREDIT-20 / TODO-32H-S6 current-runtime ownership route acceptance fix

Status: completed / full user-session remaining-balance runtime verified, 2026-06-06, owner Agent-E11/Admin Billing.

Runtime-current result: the E19 failure was a stale Control Plane runtime. Rebuilt and force-recreated `control-plane` with `POSTGRES_HOST_PORT=55432` and `REDIS_HOST_PORT=56379`; container image/runtime proof records image `sha256:38c124ada78d863e6f39630a844c79d1f938e79f05503d08bc86b58501e2b82a`, container created `2026-06-06T00:40:24Z`, started `2026-06-06T00:40:28Z`, and `/healthz` HTTP 200.

Ownership runtime artifact: `.tmp/credit-wallet/user_remaining_balance_ownership_runtime.json` is `overall_status=pass`, `runtime_implemented=true`, `contract_only=false`, `route_invoked=true`, `user_api_runtime=true`, `admin_readonly_runtime=false`, `ownership_scope_verified=true`, `auth_source=control_plane_user_session`, `wallet_readback_passed=true`, `project_membership_readback_passed=true`, `session_readback_passed=true`, `credit_grants_readback_passed=true`, `ledger_window_readback_passed=true`, `refusal_readback_passed=true`, `missing_session_rejected=true`, `secret_safe=true`, and `paid_gate_changed=false`.

Verifier result: `scripts/verify_credit_wallet_ledger_surface.ps1` now separates Admin-readonly and full user ownership artifacts. It reports `user_remaining_balance_admin_readonly_runtime_verified=true` for `.tmp/credit-wallet/user_remaining_balance_runtime.json` and `user_remaining_balance_runtime_verified=true` for `.tmp/credit-wallet/user_remaining_balance_ownership_runtime.json`. Remaining product gaps are TODO-32I recharge/voucher, TODO-32J payment/order/invoice, and TODO-32K subscription/package lifecycle; paid gate unchanged.

## E11-CREDIT-21 / TODO-32I recharge-voucher runtime implementation feasibility (historical; superseded by E11-CREDIT-28)

Status: completed as implementation plan artifact / runtime still pending, 2026-06-06, owner Agent-E11/Admin Billing.

TODO-32H regression check: `.tmp/credit-wallet/user_remaining_balance_ownership_runtime.json` remains accepted by main thread as pass, and `scripts/verify_credit_wallet_ledger_surface.ps1` reports `user_remaining_balance_runtime_verified=true`. E11-CREDIT-21 did not continue TODO-32H or change Gateway/paid gate.

Feasibility review: current Control Plane has the accounting/readback primitives needed to back a future recharge/voucher runtime: `wallets`, `credit_grants`, `ledger_entries`, admin audit writes, the verified opening-balance import transaction pattern, verified credit-grant CRUD runtime, and remaining-balance readback. It does not have durable recharge/voucher product persistence: no `recharge_intents`, `voucher_campaigns`, `voucher_issuances`, `voucher_redemptions`, voucher-code hash storage, redeem attempt/abuse-event table, payment-provider handoff state, or refund/cancel route matrix.

Artifact: `scripts/write_recharge_voucher_runtime_plan_artifact.ps1` writes `.tmp/credit-wallet/recharge_voucher_runtime_plan.json` with `schema=recharge_voucher_runtime_plan.v1`, `overall_status=contract_ready_runtime_pending`, `runtime_implemented=false`, `contract_only=true`, `control_plane_runtime_invoked=false`, `external_payment_provider_implemented=false`, `secret_safe=true`, `money_decimal_strings=true`, and `paid_gate_changed=false`. This is a feasibility/plan artifact only and must not satisfy `recharge_voucher_runtime_verified`.

Proposed slices: TODO-32I-S1 schema/OpenAPI boundary for recharge intents, voucher campaigns, voucher issuances, redemptions, and redeem attempts; TODO-32I-S2 internal SQLx transaction for voucher issue/redeem/replay/refusal/audit; TODO-32I-S3 server-side voucher-code hashing plus abuse/rate-limit persistence; TODO-32I-S4 recharge payment-provider stub/handoff/callback state; TODO-32I-S5 QA live route matrix artifact `.tmp/credit-wallet/recharge_voucher_runtime.json`.

## E11-CREDIT-22 / TODO-32I-S1 recharge-voucher schema + OpenAPI boundary (historical; superseded by E11-CREDIT-28 for runtime status)

Status: completed as schema/OpenAPI boundary / runtime still pending, 2026-06-06, owner Agent-E11/Admin Billing.

Schema boundary: added `db/migrations/0012_recharge_voucher_boundary.sql` and synced `examples/sql_schema_draft.sql` with contract-only tables for `recharge_intents`, `voucher_campaigns`, `voucher_issuances`, `voucher_redemptions`, and `voucher_redeem_attempts`. The boundary stores fixed-decimal amounts, hashed idempotency, voucher `code_hash` / `code_lookup_prefix` / `code_redacted`, bounded provider reference redaction, credit/ledger/audit links, and abuse attempt markers. It intentionally does not store raw voucher codes, provider payloads, payment secrets, DB URLs, provider keys, virtual keys, or raw idempotency material.

OpenAPI boundary: `examples/openapi_admin_skeleton.yaml` now documents contract-only endpoints `POST /billing/recharge-intents`, `POST /admin/voucher-campaigns`, `POST /admin/voucher-issuances`, and `POST /billing/vouchers/redeem`, plus request/envelope schemas. These routes are not claimed live; exposed implementations must return contract-only/501 until TODO-32I runtime lands. Future runtime artifacts must keep `runtime_implemented=true` only after route/internal invocation plus voucher storage/code hash/readback, redeem replay/refusal, abuse no-write, ledger/credit effect, reversal, audit, secret-safe, and paid-gate-neutral checks pass.

Tests: added control-plane unit coverage for recharge/voucher migration and OpenAPI boundary strings, including secret-safe voucher-code handling and raw-provider-payload omission. TODO-32H remains verified; Gateway and paid gate unchanged.

## E11-CREDIT-23 / TODO-32I-S2 voucher issue/redeem internal transaction skeleton (historical; superseded by E11-CREDIT-28 for runtime status)

Status: completed as DB-free internal transaction skeleton / runtime still pending, 2026-06-06, owner Agent-E11/Admin Billing.

Implementation scope: `apps/control-plane/src/admin.rs` now includes a contract-backed voucher issue/redeem skeleton used by focused tests. It fixes the intended transaction order for voucher issue and redeem: validate, lock/read wallet or voucher rows, read idempotency rows by `idempotency_key_hash`, hash/redact voucher codes via `code_hash`, `code_lookup_prefix`, and `code_redacted`, insert `voucher_issuances` or `voucher_redemptions`, insert `voucher_redeem_attempts` for redeem/refusal, link credit grant or ledger effect on redeem success, insert audit, then commit/read back. This is not wired to public routes and must not be counted as `recharge_voucher_runtime_verified`.

Coverage: focused tests cover issue success with hash/redacted output, redeem success with credit/ledger/audit links, same-key replay with no duplicate credit/ledger writes, same-key conflict refusal, expired/revoked/currency/ownership/non-positive refusals, abuse attempt marker, refusal no-write markers, `runtime_implemented=false`, `contract_only=true`, `secret_safe=true`, and `paid_gate_changed=false`. Raw voucher code, raw idempotency material, provider payload, Authorization/Cookie, DB URL, provider key, and virtual key are not output.

Remaining gap / next task: TODO-32I-S3 should turn this skeleton into internal SQLx statement contracts or an opt-in DB function that exercises `voucher_issuances`, `voucher_redemptions`, and `voucher_redeem_attempts`; live route/runtime artifact remains pending.

## E11-CREDIT-24 / TODO-32I-S3 voucher issue/redeem SQLx statement contract (historical; superseded by E11-CREDIT-28 for runtime status)

Status: completed as SQLx statement contract / runtime still pending, 2026-06-06, owner Agent-E11/Admin Billing.

Implementation scope: `apps/control-plane/src/admin.rs` now pins strict statement contracts for voucher issue and redeem. Issue contract order is wallet lock, idempotency lookup by `voucher_issuances.idempotency_key_hash`, duplicate code lookup by `code_hash`, insert `voucher_issuances` with `code_hash`, `code_lookup_prefix`, `code_redacted`, and `audit_id`, then insert issue audit. Redeem contract order is insert `voucher_redeem_attempts`, lock voucher by `code_hash`, idempotency lookup in `voucher_redemptions`, insert redemption with `credit_grant_id` / `ledger_entry_id` / `audit_id` / `refusal_code`, insert credit grant effect, insert redeem audit, and update issuance redemption count.

Coverage: focused tests verify statement order, `for update` lock/readback markers, issue/redeem idempotency lookup, abuse attempt marker, credit/ledger/audit link fields, refusal no-write support, and secret-safe SQL shape. SQL statements do not include raw voucher code, raw idempotency material, provider payload, auth/cookie, DB URL, provider key, or virtual key markers.

Runtime status: no public route or opt-in DB execution is claimed, `.tmp/credit-wallet/recharge_voucher_runtime.json` remains absent, and `recharge_voucher_runtime_verified=false` remains correct. Next task should implement an opt-in DB transaction/readback verifier over the 0012 schema or wire an internal SQLx function behind a non-runtime artifact gate.

## E11-CREDIT-25 / TODO-32I-S4 opt-in DB verifier for voucher issue/redeem (historical; superseded by E11-CREDIT-28 for runtime status)

Status: completed as opt-in DB verifier / current local DB blocked by unapplied 0012 schema, 2026-06-06, owner Agent-E11/Admin Billing.

Implementation scope: added `scripts/verify_recharge_voucher_runtime.ps1`. Default mode writes a blocked contract-only DB-plan artifact unless `-RunDbIntegration`, `RECHARGE_VOUCHER_DB_OPT_IN=1`, and a DB URL env are present. To preserve QA's most specific blocker evidence, a default blocked run no longer overwrites an existing `.tmp/credit-wallet/recharge_voucher_runtime_db_plan.json` DB-attempt artifact; it writes `.tmp/credit-wallet/recharge_voucher_runtime_db_plan.default_blocked.json` unless `-Overwrite` is supplied. Opt-in mode executes a rollback-contained SQL plan through psql: issue insert/readback in `voucher_issuances`, redeem attempt insert in `voucher_redeem_attempts`, redeem insert/readback in `voucher_redemptions`, credit grant and ledger effect link readback, audit link readback, idempotency replay lookup, and conflict/refusal no-write checks.

Current artifact: `.tmp/credit-wallet/recharge_voucher_runtime_db_plan.json` is blocked, not runtime: `runtime_implemented=false`, `contract_only=true`, `secret_safe=true`, `paid_gate_changed=false`, blocker `recharge_voucher_schema_0012_not_applied`. The compose DB used for the opt-in attempt has not applied `db/migrations/0012_recharge_voucher_boundary.sql`, so no DB readback pass is claimed.

Evidence management: default blocked proof now coexists with DB-attempt proof. `.tmp/credit-wallet/recharge_voucher_runtime_db_plan.default_blocked.json` records missing flag/env blockers and `preserved_db_attempt_artifact_path=.tmp/credit-wallet/recharge_voucher_runtime_db_plan.json`, while the DB-attempt artifact keeps the more precise `recharge_voucher_schema_0012_not_applied` blocker.

Runtime status: `.tmp/credit-wallet/recharge_voucher_runtime.json` remains absent and `recharge_voucher_runtime_verified=false` remains correct. Next task should apply migration 0012 in a bounded compose DB and re-run the opt-in DB verifier; only after DB readback passes should route/internal runtime invocation plus QA live artifact be attempted.

## E11-CREDIT-26 / TODO-32I-S5 apply 0012 migration and rerun voucher DB plan (historical; superseded by E11-CREDIT-28 for runtime status)

Status: completed as DB-plan progress / runtime still pending, 2026-06-06, owner Agent-E11/Admin Billing.

DB environment: bounded local compose Postgres on `127.0.0.1:55432`; DSN and credentials were not written to artifacts or docs. Pre-check showed the DB reachable and the voucher tables absent. Applied `db/migrations/0012_recharge_voucher_boundary.sql` to the local compose DB and verified `voucher_issuances`, `voucher_redemptions`, and `voucher_redeem_attempts` exist.

Artifact: reran `scripts/verify_recharge_voucher_runtime.ps1 -RunDbIntegration -OutputPath .tmp/credit-wallet/recharge_voucher_runtime_db_plan.json`. The artifact is now `overall_status=partial`, `db_integration_ran=true`, `runtime_implemented=false`, `contract_only=true`, `route_invoked=false`, `internal_sqlx_function_invoked=false`, `secret_safe=true`, and `paid_gate_changed=false`. DB-plan readback booleans are true for voucher issuance, redeem, redeem attempts, issue/redeem idempotency replay lookup, conflict/refusal no-write, credit grant link, ledger link, and audit link.

Runtime status: `.tmp/credit-wallet/recharge_voucher_runtime.json` remains absent and must not be inferred from the DB-plan artifact. Remaining blockers are `route_runtime_not_invoked` and `qa_runtime_artifact_missing`; `recharge_voucher_runtime_verified=false` remains correct. Next task should wire route/internal runtime invocation and produce a QA live artifact only after refund/cancel reversal and public/internal invocation readback are proven.

## E11-CREDIT-27 / TODO-32I-S6 runtime invocation gate (historical; superseded by E11-CREDIT-28)

Status: completed as strict blocked runtime handoff / runtime still pending, 2026-06-06, owner Agent-E11/Admin Billing.

Runtime review: current Control Plane router does not expose recharge/voucher live handlers for `POST /billing/recharge-intents`, `POST /admin/voucher-campaigns`, `POST /admin/voucher-issuances`, or `POST /billing/vouchers/redeem`; OpenAPI still marks these routes contract-only/future runtime. `apps/control-plane/src/admin.rs` has DB-free contract helpers and SQL statement contracts, not a public route or internal Rust/sqlx transaction invocation that the verifier can call as business runtime.

Verifier update: extended `scripts/verify_recharge_voucher_runtime.ps1` with `-WriteRuntimeBlockedArtifact` and `-RuntimeBlockedOutputPath`. With opt-in DB enabled, the script still writes the migration-applied DB-plan artifact as partial and additionally writes `.tmp/credit-wallet/recharge_voucher_runtime_s6_blocked.json` as a distinct blocked artifact. It intentionally does not write `.tmp/credit-wallet/recharge_voucher_runtime.json`.

Artifact: `.tmp/credit-wallet/recharge_voucher_runtime_s6_blocked.json` has `schema=recharge_voucher_runtime_s6_blocked.v1`, `overall_status=blocked`, `runtime_implemented=false`, `contract_only=true`, `route_invoked=false`, `internal_runtime_function_invoked=false`, `db_integration_ran=true`, `db_plan_passed=true`, `secret_safe=true`, and `paid_gate_changed=false`. Evidence is true for voucher issue storage/readback, voucher code hash readback, raw voucher code not echoed, redeem readback, issue/redeem idempotency replay lookup, conflict/refusal no duplicate write, refusal/redeem attempt persistence, credit grant effect, ledger effect, and audit readback. Explicit blockers are `route_runtime_not_invoked`, `internal_runtime_function_not_invoked`, `refund_cancel_reversal_not_implemented`, and `qa_runtime_artifact_missing`.

Runtime status: `recharge_voucher_runtime_verified=false` remains correct. Next task should implement either public route invocation or a verifier-callable internal Rust/sqlx transaction function, add refund/cancel reversal readback, and only then write `.tmp/credit-wallet/recharge_voucher_runtime.json` with `schema=recharge_voucher_runtime.v1`.

## E11-CREDIT-28 / TODO-32I-S7 internal runtime invocation

Status: completed / verifier-callable internal Rust/sqlx runtime verified, 2026-06-06, owner Agent-E11/Admin Billing.

Implementation: `apps/control-plane/src/admin.rs` now includes an ignored opt-in DB integration test `recharge_voucher_internal_runtime_db_integration` backed by `execute_recharge_voucher_internal_runtime_tx`. This is a Rust/sqlx business runtime path, not a psql DB-plan: it seeds/locks a wallet in a rollback-contained transaction, issues a voucher with hashed code fields, redeems it with redeem-attempt and idempotency readback, records conflict/refusal attempt without duplicate credit/ledger writes, writes credit grant and ledger effects, writes audit logs, and writes a bounded refund/cancel reversal ledger entry plus reversal audit.

Verifier: `scripts/verify_recharge_voucher_runtime.ps1 -RunInternalRuntime` calls the ignored Rust integration test and validates the runtime artifact before returning pass. It writes `.tmp/credit-wallet/recharge_voucher_runtime.json` only after all runtime acceptance fields are true. The artifact has `schema=recharge_voucher_runtime.v1`, `overall_status=pass`, `runtime_implemented=true`, `contract_only=false`, `internal_runtime_function_invoked=true`, `internal_sqlx_function_invoked=true`, `db_integration_ran=true`, `secret_safe=true`, and `paid_gate_changed=false`.

Readback evidence: the runtime artifact proves voucher issue storage/readback, voucher code hash/readback and redacted output, raw voucher code not echoed, redeem readback, redeem idempotency replay, conflict/refusal no duplicate write, refusal and redeem attempt persistence, credit grant effect readback, ledger effect readback, audit readback, refund/cancel reversal readback, and no direct wallet snapshot mutation. `scripts/verify_credit_wallet_ledger_surface.ps1` now reports `recharge_voucher_runtime_verified=true`.

Remaining gap / next task: public Control Plane route wiring for the recharge/voucher endpoints is still future work if product requires public route evidence; TODO-32J payment/order/invoice and TODO-32K subscription/package lifecycle remain runtime gaps. Paid gate unchanged.

## E11-CREDIT-29 / TODO-32J-S1 payment-order-invoice runtime planning and verifier slice

Status: completed as strict blocked runtime plan / runtime pending, 2026-06-06, owner Agent-E11/Admin Billing.

Verifier: added `scripts/verify_payment_order_invoice_runtime.ps1`. `-SelfTest` proves the blocked artifact does not claim runtime, includes machine blockers for missing schema/runtime invocation, remains secret-safe, and rejects raw auth markers. The missing-schema blocker was an initial S1 condition and is now superseded by TODO-32J schema-boundary progress. Default mode reads `.tmp/credit-wallet/payment_order_invoice_contract.json`, verifies the E9 contract artifact is present/accepted/secret-safe, and writes `.tmp/credit-wallet/payment_order_invoice_runtime_s1_blocked.json` rather than `.tmp/credit-wallet/payment_order_invoice_runtime.json`.

Artifact: `.tmp/credit-wallet/payment_order_invoice_runtime_s1_blocked.json` has `schema=payment_order_invoice_runtime.v1`, `overall_status=blocked`, `actual_exit_code=2`, `runtime_implemented=false`, `contract_only=true`, `route_invoked=false`, `internal_runtime_function_invoked=false`, `secret_safe=true`, and `paid_gate_changed=false`. The contract readback is accepted, but runtime readback booleans remain false. Initial S1 machine blockers included `payment_order_invoice_schema_missing`; that blocker is superseded/closed by `db/migrations/0013_payment_order_invoice_boundary.sql`, `examples/sql_schema_draft.sql`, and diagnostics showing required tables present. Current machine blockers are `payment_order_invoice_runtime_not_invoked`, `payment_provider_handoff_runtime_missing`, `provider_callback_or_capture_readback_missing`, `invoice_receipt_runtime_missing`, `refund_cancel_chargeback_reversal_runtime_missing`, and `payment_order_invoice_reconciliation_readback_missing`.

Runtime status: `payment_order_invoice_contract_verified=true` remains correct and `payment_order_invoice_runtime_verified=false` remains correct. TODO-32I remains verified by the internal Rust/sqlx recharge/voucher runtime artifact. Next TODO-32J slice should implement an internal Rust/sqlx transaction or public route that proves order lifecycle, redacted provider handoff/callback or bounded internal simulation policy, invoice/receipt, ledger-or-credit effect, refund/cancel/chargeback reversal, idempotency replay/conflict no-duplicate, audit, reconciliation, fixed-decimal money, secret safety, and paid-gate neutrality before writing `.tmp/credit-wallet/payment_order_invoice_runtime.json`.

## E11-CREDIT-30 / TODO-32J-S2 payment-order-invoice schema and internal runtime first implementation

Status: completed as schema + verifier-callable internal Rust/sqlx runtime helper / opt-in runtime blocked by missing env in this shell, 2026-06-06, owner Agent-E11/Admin Billing.

Schema: added `db/migrations/0013_payment_order_invoice_boundary.sql` and synced `examples/sql_schema_draft.sql`. The schema introduces `payment_orders`, `payment_intents`, `payment_captures`, `payment_refunds`, `payment_events`, `invoices`, `invoice_receipts`, and `payment_reconciliations` with tenant/wallet scope, fixed-decimal numeric amounts, status constraints, hashed idempotency, redacted provider-reference fields, credit/ledger/audit links, refund/reversal links, safe request summaries, and safe metadata. It intentionally does not store raw provider payloads, raw provider credentials, raw idempotency material, DB URLs, auth/cookie material, or client secrets.

Internal runtime helper: `apps/control-plane/src/admin.rs` now includes ignored opt-in test `payment_order_invoice_internal_runtime_db_integration` backed by `execute_payment_order_invoice_internal_runtime_tx`. The transaction path seeds/locks a wallet, creates an order, writes redacted provider handoff intent, confirms capture using a bounded internal provider policy, inserts credit grant and ledger effect rows, writes invoice and receipt rows, proves capture replay by idempotency hash, persists a refusal `payment_events` row without duplicate capture/credit/ledger writes, writes refund/cancel/chargeback reversal ledger and refund rows, writes audit logs, and writes reconciliation rows. Default focused tests cover the 0013 schema boundary and runtime acceptance shape; the DB integration test remains ignored unless explicitly opted in.

Verifier: `scripts/verify_payment_order_invoice_runtime.ps1` now supports `-RunInternalRuntime` and validates `.tmp/credit-wallet/payment_order_invoice_runtime.json` only when the ignored Rust/sqlx runtime test writes an accepted pass artifact. In this shell, no `PAYMENT_ORDER_INVOICE_DB_OPT_IN` or DB URL env was present, so the verifier wrote `.tmp/credit-wallet/payment_order_invoice_runtime_s2_blocked.json` with `overall_status=blocked`, `actual_exit_code=2`, `runtime_implemented=false`, `contract_only=true`, `secret_safe=true`, and `paid_gate_changed=false`. Schema diagnostics now pass with no missing required tables; remaining blockers are runtime/provider/callback/invoice/reversal/reconciliation readback plus missing opt-in/env for this run.

Runtime status: `.tmp/credit-wallet/payment_order_invoice_runtime.json` was not written as pass, and `scripts/verify_credit_wallet_ledger_surface.ps1` still reports `payment_order_invoice_contract_verified=true` and `payment_order_invoice_runtime_verified=false`. TODO-32I remains verified, TODO-32K remains false, and paid gate unchanged. Next task should apply migration 0013 to bounded compose DB and run `scripts/verify_payment_order_invoice_runtime.ps1 -RunInternalRuntime` with opt-in env; only a verifier-accepted runtime artifact may promote TODO-32J.

### E11-CREDIT-30A compile blocker fix

Status: completed, 2026-06-06. QA-CREDIT-63 reported a `serde_json::json!` recursion-limit compile blocker around the new payment/order/invoice runtime artifact shape. E11 refactored the `payment_order_invoice_runtime.v1` artifact construction in `apps/control-plane/src/admin.rs` to build the base artifact with `serde_json::Map` / `Value::Object` and insert dynamic fields incrementally. The artifact field names and verifier contract were preserved; no evidence fields were removed or hidden.

Verification: `cargo test -p ai-control-plane recharge_voucher -- --nocapture` now passes with 6 passed / 1 ignored; `cargo test -p ai-control-plane payment_order_invoice -- --nocapture` passes with 2 passed / 1 ignored. `scripts/verify_payment_order_invoice_runtime.ps1 -SelfTest`, `scripts/verify_credit_wallet_ledger_surface.ps1`, `scripts/scan_secrets.ps1`, and scoped `git diff --check` pass. TODO-32J runtime remains false until the opt-in DB/runtime artifact is accepted.

## E11-CREDIT-31-DEFER / TODO-32J temporary deferred runtime handoff

Status: deferred_runtime_external_dependency, 2026-06-06, owner Agent-E11/Admin Billing.

Current facts: TODO-32I recharge/voucher runtime remains verified; TODO-32J payment/order/invoice contract is verified, schema boundary is present, and verifier/blocked sidecars exist; TODO-32J runtime remains false; TODO-32K remains false. This is not production distribution readiness and must not be marketed as complete commercial payment/order/invoice support.

Deferred artifact: `.tmp/credit-wallet/payment_order_invoice_runtime_deferred.json` records `schema=payment_order_invoice_runtime_deferred.v1`, `overall_status=deferred_runtime_external_dependency`, `runtime_implemented=false`, `contract_only=true`, `route_invoked=false`, `internal_runtime_function_invoked=false`, `secret_safe=true`, `paid_gate_changed=false`, and `production_distribution_ready=false`. Existing `.tmp/credit-wallet/payment_order_invoice_runtime_s2_blocked.json` remains the bounded blocked runtime sidecar and is still secret-safe/runtime-false.

Resume conditions: provider callback/capture or an explicitly approved bounded internal simulation policy; invoice/receipt runtime readback; refund/cancel/chargeback reversal readback; payment/order/invoice reconciliation readback; a pass `.tmp/credit-wallet/payment_order_invoice_runtime.json` artifact accepted by the verifier; and `scripts/verify_credit_wallet_ledger_surface.ps1` reporting `payment_order_invoice_runtime_verified=true`.

Recommended next task: move E11 to TODO-32K contract/schema/defer assessment, or to public recharge/voucher route polish if product wants public route evidence and the prerequisites are available. Do not keep spinning on TODO-32J runtime until the resume conditions above exist.

## E11-CREDIT-32 / TODO-32K-S1 subscription-package lifecycle contract/schema/defer assessment

Status: deferred_runtime_external_dependency / contract verified / runtime false, 2026-06-06, owner Agent-E11/Admin Billing.

Current facts: `.tmp/credit-wallet/subscription_package_lifecycle_contract.json` is present and accepted as `schema=subscription_package_lifecycle_contract.v1`, `status=pass`, `runtime_implemented=false`, `contract_only=true`, `secret_safe=true`, and `paid_gate_changed=false`. The artifact includes `subscription_package_lifecycle_schema_contract.v1`, runtime acceptance fields, reusable ledger/credit/audit primitives, and proposed TODO-32K implementation slices, but it is not runtime evidence.

Verifier handoff: added `scripts/verify_subscription_package_lifecycle_runtime.ps1`. `-SelfTest` proves accepted contract-only input is not promoted to runtime, raw secret markers are rejected, and future runtime acceptance requires route/internal runtime or scheduler proof. Default mode writes `.tmp/credit-wallet/subscription_package_lifecycle_runtime_deferred.json` with `schema=subscription_package_lifecycle_runtime_deferred.v1`, `overall_status=deferred_runtime_external_dependency`, `actual_exit_code=2`, `runtime_implemented=false`, `contract_only=true`, `route_invoked=false`, `internal_runtime_function_invoked=false`, `scheduler_invoked=false`, `secret_safe=true`, `paid_gate_changed=false`, and `production_distribution_ready=false`.

Assessment update for E11-CREDIT-33: durable schema migration `db/migrations/0014_subscription_package_lifecycle_boundary.sql` now defines `subscription_plans`, `subscription_packages`, `subscriptions`, and `subscription_events_or_schedules`, so the earlier `subscription_package_lifecycle_schema_migration_missing` blocker is superseded. Runtime remains false because Control Plane route/internal lifecycle invocation, scheduler/provider callback, invoice/order linkage runtime, trial/proration/dunning runtime, credit/ledger effect readback, renewal/cancel/pause/resume readback, reconciliation, and pass artifact `.tmp/credit-wallet/subscription_package_lifecycle_runtime.json` are still missing.

Resume conditions: apply/verify the new subscription schema in bounded DB runtime; implement Admin/Product plan-package CRUD readback; implement subscription create/activate/renew/cancel/pause/resume readback; implement trial/proration/dunning scheduler or callback readback; prove credit-grant or ledger effect readback; prove invoice/order linkage runtime readback; prove idempotency replay, same-key conflict, and refusal no-write readback; prove audit and reconciliation readback; write an accepted `.tmp/credit-wallet/subscription_package_lifecycle_runtime.json` with `schema=subscription_package_lifecycle_runtime.v1`; and have `scripts/verify_credit_wallet_ledger_surface.ps1` report `subscription_package_lifecycle_runtime_verified=true`.

Preserved status: TODO-32I recharge/voucher runtime remains verified; TODO-32J remains `deferred_runtime_external_dependency` and runtime false; TODO-32K has schema migration present but runtime remains false; controlled paid beta and paid gate are unchanged.

## E11-LAUNCH-01 / Voucher-backed API distribution readiness

Status: partial_ready_with_route_gaps, 2026-06-06, owner Agent-E11/Admin Billing.

Surface map: Control Plane has Admin virtual-key routes `GET /admin/virtual-keys`, `POST /admin/virtual-keys`, `GET /admin/virtual-keys/{id}`, `POST /admin/virtual-keys/{id}/disable`, and `POST /admin/virtual-keys/{id}/expire`; create uses `create_virtual_key`, readback uses `list_virtual_keys` / `get_virtual_key`, and create writes audit through `create_virtual_key_with_default_profile_and_audit`. User/developer remaining balance is wired at `GET /billing/wallets/{wallet_id}/remaining-balance` with `authenticate_remaining_balance_principal` resolving user session or server-side virtual-key hash scope. Audit readback route `GET /admin/audit-logs` is present. Voucher OpenAPI boundaries exist for `POST /billing/recharge-intents`, `POST /admin/voucher-campaigns`, `POST /admin/voucher-issuances`, and `POST /billing/vouchers/redeem`, but those public Control Plane routes are not wired in the router; accepted TODO-32I evidence is internal Rust/sqlx runtime via `execute_recharge_voucher_internal_runtime_tx`.

Artifact: added `scripts/verify_voucher_api_distribution_readiness.ps1`, writing `.tmp/launch/voucher_api_distribution_readiness.json` with `schema=voucher_api_distribution_readiness.v1`, `overall_status=partial_ready_with_route_gaps`, `production_distribution_ready=false`, `secret_safe=true`, and `paid_gate_changed=false`. Current booleans: `voucher_internal_runtime_verified=true`, `voucher_redeem_route_verified=false`, `virtual_key_issue_route_found=true`, `virtual_key_issue_route_verified=false`, `user_balance_route_verified=true`, `gateway_paid_hot_path_artifact_present=true`, and `admin_audit_readback_route_found=true`.

Remaining blockers: public voucher Control Plane routes are not wired, and there is no live virtual-key issue route artifact proving create/readback/audit with safe fixture data. Do not chase TODO-32J payment/order/provider or TODO-32K subscription/scheduler runtime for this launch target; those remain deferred external runtime items.

## E11-LAUNCH-02 / Voucher public route blocker + virtual-key bounded route evidence

Status: partial / bounded route evidence added, 2026-06-06, owner Agent-E11/Admin Billing.

Feasibility decision: E11 did not wire `POST /admin/voucher-issuances` or `POST /billing/vouchers/redeem` in this slice. The accepted TODO-32I voucher evidence is an internal Rust/sqlx verifier path (`execute_recharge_voucher_internal_runtime_tx`), while public route promotion still needs request handlers with auth/RBAC or ownership boundary, request parsing, idempotency replay/conflict semantics, refusal no-write response shape, audit/readback response shape, and safe redaction policy. Treating OpenAPI-only evidence or the internal verifier as route invocation would be misleading.

Artifact: added `scripts/verify_voucher_public_route_and_virtual_key_evidence.ps1`, writing `.tmp/launch/voucher_public_route_and_virtual_key_evidence.json` with `schema=voucher_public_route_virtual_key_evidence.v1`, `overall_status=partial`, `voucher_route_evidence.public_routes_wired=false`, `voucher_route_evidence.route_verified=false`, and blocker `voucher_public_control_plane_routes_not_wired`. The same artifact verifies a bounded DB-free virtual-key route contract: `POST /admin/virtual-keys`, `GET /admin/virtual-keys`, and `GET /admin/virtual-keys/{id}` are present; create generates the secret server-side, rejects client-supplied secret/secret_hash/key_prefix, returns the secret only once, list/get use redacted responses, create writes `virtual_key.create` audit, and RBAC maps these paths to `KeyManage`. It does not claim live route invocation and does not output raw virtual-key material.

Readiness sync: `scripts/verify_voucher_api_distribution_readiness.ps1` now consumes `.tmp/launch/voucher_public_route_and_virtual_key_evidence.json` and records `virtual_key_issue_bounded_contract_verified=true` while keeping `virtual_key_issue_route_verified=false` and `voucher_public_route_artifact_verified=false`. Current `.tmp/launch/voucher_api_distribution_readiness.json` is `overall_status=blocked` because current Gateway launch evidence reports `gateway_current_launch_hot_path_verified=false`; historical paid evidence remains present, but current runtime evidence takes precedence. `production_distribution_ready=false`, `secret_safe=true`, `paid_gate_changed=false`.

Remaining blockers/gaps: E8/Gateway current launch hot-path blocker must be cleared or explicitly waived by release owner; voucher public Control Plane route evidence remains pending; live Admin virtual-key route smoke remains optional if QA requires `route_invoked=true` instead of bounded contract evidence. TODO-32J/TODO-32K remain deferred external runtime items and are not reopened by this task.

## E11-LAUNCH-03 / Voucher route implementation decision + operator-only exception

Status: formal exception artifact added / unapproved / readiness still blocked, 2026-06-06, owner Agent-E11/Admin Billing.

Implementation decision: E11 did not wire `POST /admin/voucher-issuances` or `POST /billing/vouchers/redeem` in this slice. Inspection shows `execute_recharge_voucher_internal_runtime_tx` and the voucher issue/redeem SQLx proof live inside the Rust test module as an opt-in verifier path. Promoting it to production HTTP handlers would require moving and reshaping test-only helpers into runtime code plus adding request structs, auth/RBAC or ownership scope, idempotency and refusal response semantics, audit/readback response shape, and safe redaction policy. That is too risky for a small launch slice and must not be faked as route evidence.

Artifacts: `scripts/verify_voucher_public_route_and_virtual_key_evidence.ps1` now writes both `.tmp/launch/voucher_public_route_and_virtual_key_evidence.json` and `.tmp/launch/voucher_operator_only_exception.json`. The exception artifact has `schema=voucher_operator_only_exception.v1`, `overall_status=unapproved`, `approved=false`, `route_substitution_allowed=false`, `production_distribution_ready=false`, `secret_safe=true`, and `paid_gate_changed=false`. It records the manual operator flow, risks, and resume conditions but intentionally cannot satisfy production readiness without explicit release-owner/Product/Ops approval.

Readiness sync: `scripts/verify_voucher_api_distribution_readiness.ps1` now consumes `.tmp/launch/voucher_operator_only_exception.json`. It will accept route substitution only when an approved secret-safe exception artifact is present. Current `.tmp/launch/voucher_api_distribution_readiness.json` records `operator_only_exception_approved=false`, `public_route_voucher_evidence_present=false`, `virtual_key_issue_bounded_contract_verified=true`, `overall_status=blocked`, and `production_distribution_ready=false` because the current Gateway launch hot-path blocker still takes precedence.

Remaining blockers/gaps: current Gateway launch hot-path evidence must pass or receive explicit waiver; voucher route handlers remain unwired; operator-only exception remains unapproved; live virtual-key route invocation remains optional unless QA requires `route_invoked=true`. TODO-32J/TODO-32K stay deferred/runtime false.

## E11-LAUNCH-04 / Virtual-key launch evidence + operator packet

Status: completed as stronger DB-free virtual-key safety evidence plus blocked operator packet, 2026-06-06, owner Agent-E11/Admin Billing.

Scope decision: E11 did not continue voucher route wiring. Public voucher routes remain unwired, and `.tmp/launch/voucher_operator_only_exception.json` remains unapproved. This slice focused on launch-critical evidence that can be completed now: virtual-key distribution safety and the operator packet needed for trusted-user voucher-backed API distribution review.

Virtual-key evidence: `scripts/verify_voucher_public_route_and_virtual_key_evidence.ps1` continues to write `.tmp/launch/voucher_public_route_and_virtual_key_evidence.json`, now serving as the stronger virtual-key launch artifact. It verifies Admin create/list/get/disable/expire route presence, server-side generated virtual-key secret, caller-supplied secret/secret_hash/key_prefix refusal, one-time secret return on create only, list/get redacted output, `virtual_key.create` audit marker, RBAC `KeyManage` scope, and no raw virtual-key secret in artifact. It remains DB-free/bounded and does not claim `route_invoked=true`.

Operator packet: added `.tmp/launch/api_distribution_operator_packet.json` with `schema=api_distribution_operator_packet.v1`, `overall_status=blocked`, `ready_to_send=false`, `secret_safe=true`, and `paid_gate_changed=false`. The packet contains bounded tenant/project/wallet/currency fields from existing safe artifacts, virtual-key issuance status, voucher quota status, remaining-balance artifact link, Gateway blocker status, rollback/revoke steps, support/audit owner placeholders, and artifact links. It stays blocked because current Gateway launch hot-path evidence is false and neither voucher public route evidence nor an approved operator-only exception exists.

Current launch state: `.tmp/launch/voucher_api_distribution_readiness.json` remains `overall_status=blocked`, `production_distribution_ready=false`, `virtual_key_issue_bounded_contract_verified=true`, `operator_only_exception_approved=false`, `public_route_voucher_evidence_present=false`, and `gateway_current_launch_hot_path_verified=false`. TODO-32J and TODO-32K remain deferred/runtime false.

## E11-LAUNCH-05 / Admin distribution packet rollback/revoke verifier

Status: completed as bounded rollback/revoke contract evidence, 2026-06-06, owner Agent-E11/Admin Billing.

Packet update: `scripts/verify_voucher_public_route_and_virtual_key_evidence.ps1` now strengthens `.tmp/launch/api_distribution_operator_packet.json` with rollback/revoke verification fields. Current packet has `schema=api_distribution_operator_packet.v1`, `overall_status=blocked`, `ready_to_send=false`, `rollback_revoke_verification.status=bounded_contract_ready_live_invocation_pending`, `secret_safe=true`, and `paid_gate_changed=false`.

Rollback/revoke contract: the packet verifies `rollback_virtual_key_disable_route_present=true`, `rollback_virtual_key_expire_route_present=true`, `credit_revoke_or_expire_route_present=true`, `remaining_balance_recheck_present=true`, and `audit_readback_route_present=true`. These are bounded contract/source checks only; `live_invocation=false` remains explicit. Support fields now include placeholders for support owner, audit owner, launch approver, escalation contact, and rollback owner.

Current blockers unchanged: `gateway_current_launch_hot_path_not_verified` and `voucher_route_or_approved_operator_exception_missing`. Voucher public route evidence is still false, operator-only exception is still unapproved, and live virtual-key invocation is still not claimed.

## E11-LAUNCH-06 / Developer API distribution quickstart contract

Status: completed as DB-free quickstart contract / unpublished, 2026-06-06, owner Agent-E11/Admin Billing.

Artifact: added `.tmp/launch/developer_api_distribution_quickstart_contract.json` with `schema=developer_api_distribution_quickstart_contract.v1`, `overall_status=blocked`, `ready_to_publish=false`, `secret_safe=true`, and `paid_gate_changed=false`. It is a trusted-user/operator-mediated beta quickstart contract, not public distribution readiness.

Contract contents: base URL placeholder, bearer auth header shape with raw key omitted, supported endpoint examples for chat completion and model list, model placeholder, expected error codes for `billing_insufficient_balance` / rate limit / auth failure / scope failure, request id capture fields, support escalation placeholders, rollback/readiness artifact links, and current blockers. It forbids raw virtual key, voucher code, provider key, Authorization value, Cookie, and DB URL output.

Current blockers unchanged: `gateway_current_launch_hot_path_not_verified` and `voucher_route_or_approved_operator_exception_missing`. The quickstart remains `ready_to_publish=false` until launch gate blockers clear or receive explicit approval/waiver.

## E11-LAUNCH-08 / Voucher-backed distribution route-gap reconciliation

Status: reviewed / pass-with-productization-gaps classification, 2026-06-06, owner Agent-E11/Admin Billing.

Current launch gate: `.tmp/launch/voucher_api_distribution_readiness.json` now reports `schema=voucher_api_distribution_launch_gate.v1`, `overall_status=pass_with_productization_gaps`, `qa_verdict=pass_with_productization_gaps`, `production_distribution_ready=true`, `production_distribution_full_ready=false`, `gateway_current_launch_hot_path_verified=true`, `voucher_redeem_runtime_verified=true`, `user_remaining_balance_runtime_verified=true`, `secret_safe=true`, and `paid_gate_changed=false`.

Route evidence remains unchanged and intentionally partial: `.tmp/launch/voucher_public_route_and_virtual_key_evidence.json` still has `overall_status=partial`, `voucher_route_evidence.public_routes_wired=false`, `voucher_route_evidence.route_invoked=false`, `voucher_route_evidence.route_verified=false`, and blocker `voucher_public_control_plane_routes_not_wired`. `.tmp/launch/voucher_operator_only_exception.json` remains `overall_status=unapproved`, `approved=false`, and `route_substitution_allowed=false`; it must not be treated as a public-route substitute without explicit Product/Ops approval.

E11 judgment for the current trusted-user voucher-backed beta: the missing public Control Plane voucher routes are a productization gap / resume condition, not a current hard blocker, because accepted runtime evidence already covers the internal voucher redeem path, remaining-balance runtime, Gateway current launch hot path, and virtual-key bounded contract. Public voucher route evidence is still required for full self-serve/public productization or if QA/Product changes the launch policy.

Virtual-key route contract remains valid and secret-safe: the bounded evidence verifies Admin create/list/get/disable/expire route presence, server-side secret generation, client-supplied secret material refusal, one-time secret return on create only, list/get redaction, `virtual_key.create` audit marker, and `KeyManage` RBAC. It remains bounded contract evidence (`route_invoked=false`), not a live virtual-key route smoke.

Resume conditions: implement `POST /admin/voucher-issuances` and `POST /billing/vouchers/redeem` route handlers with auth/RBAC or ownership scope, voucher-code hash/redaction, idempotency replay/conflict, refusal no-write, audit/readback, ledger/credit effect readback, and secret-safe artifact proof; or attach an explicit approved Product/Ops operator-only exception. Until then, do not claim `production_distribution_full_ready=true`.

## E11-LAUNCH-09 / API distribution operator packet refresh

Status: completed / packet refreshed from current launch gate, 2026-06-06, owner Agent-E11/Admin Billing.

Fix: updated `scripts/verify_voucher_public_route_and_virtual_key_evidence.ps1` selftest coverage so a recovered Gateway readiness artifact cannot leave stale Gateway blockers in `.tmp/launch/api_distribution_operator_packet.json`. The generator now keeps Gateway readiness, global blockers, per-user send requirements, and public-route productization gaps separate.

Regenerated artifact: `.tmp/launch/api_distribution_operator_packet.json` now has `schema=api_distribution_operator_packet.v1`, `overall_status=per_user_metadata_required`, `ready_to_send=false`, `gateway.status=verified`, `gateway.blocker=""`, `blockers=[]`, `productization_gaps=["public_voucher_route_or_operator_exception_policy_pending","per_user_operator_metadata_required"]`, `secret_safe=true`, and `paid_gate_changed=false`.

Interpretation: trusted-user voucher-backed beta launch gate remains `pass_with_productization_gaps`; the operator packet is not ready to hand to a specific user until operator-owned fields are filled and reviewed. Public voucher HTTP route wiring or approved operator-only substitution remains a productization gap / resume condition, not a stale Gateway hard blocker.

Secret safety: packet and route evidence continue to avoid raw voucher code, raw virtual key, Authorization/Cookie, provider key, DB URL, and raw secret material. Virtual-key bounded contract still records one-time secret return on create and list/get redaction.

## E11-LAUNCH-10 / Operator packet watcher + public voucher route backlog

Status: no-op watcher / backlog clarified, 2026-06-06, owner Agent-E11/Admin Billing.

Packet scan: `.tmp/launch/api_distribution_operator_packet.json` remains current and does not need regeneration. It reports `schema=api_distribution_operator_packet.v1`, `overall_status=per_user_metadata_required`, `ready_to_send=false`, `gateway.status=verified`, `gateway.blocker=""`, `blockers=[]`, `productization_gaps=["public_voucher_route_or_operator_exception_policy_pending","per_user_operator_metadata_required"]`, `secret_safe=true`, and `paid_gate_changed=false`.

No code or artifact changes were required for this watcher. The current trusted-user voucher-backed beta can continue with selected-user packet fill-in; `ready_to_send=false` is not a Gateway blocker.

Public voucher route productization backlog minimal DoD: wire `POST /admin/voucher-issuances` and `POST /billing/vouchers/redeem` only in a future bounded slice with auth/RBAC or ownership scope, route request parsing, server-side voucher-code hash/redaction, fixed-decimal positive amount validation, idempotency replay/conflict semantics, audit write/readback, ledger-or-credit effect readback, refusal no-write proof, no direct wallet snapshot mutation, secret-safe route artifact, and `paid_gate_changed=false`. Until that evidence exists or Product/Ops approves operator-only substitution, keep `production_distribution_full_ready=false`.

## E11-LAUNCH-11 / Public voucher route productization implementation brief

Status: docs-only implementation brief / no runtime route wiring, 2026-06-06, owner Agent-E11/Admin Billing.

Current implementation basis: Control Plane already has the accepted internal Rust/sqlx voucher runtime proof `execute_recharge_voucher_internal_runtime_tx`, OpenAPI contract-only paths for `POST /admin/voucher-issuances` and `POST /billing/vouchers/redeem`, Admin virtual-key route/RBAC evidence, user/developer remaining-balance ownership runtime, and audit readback. This slice does not move test-only/internal voucher helpers into public handlers and does not change runtime code.

Non-blocking classification: public voucher routes are productization work for full self-serve/public distribution. They are not a current trusted-user voucher-backed beta blocker while `.tmp/launch/voucher_api_distribution_readiness.json` remains `pass_with_productization_gaps`, `.tmp/launch/api_distribution_operator_packet.json` remains `per_user_metadata_required`, and selected-user packet fields are filled before handoff.

`POST /admin/voucher-issuances` minimum route DoD:

- Auth/RBAC: require Admin session with billing-adjust or equivalent voucher-issue permission; reject missing/insufficient session before parsing secrets into logs.
- Request schema: tenant/project/wallet/currency, voucher campaign or bounded issue source, fixed-decimal positive amount/quota, valid-from/valid-until, reason, actor metadata, and required idempotency key.
- Redaction/hash: accept raw voucher code only if necessary at request boundary; immediately hash server-side, store `code_hash` / `code_lookup_prefix` / `code_redacted`, and never echo raw code in response, audit, logs, or artifacts.
- Idempotency: same key + same normalized body replays original issuance ids; same key + different body returns idempotency conflict; duplicate external/code reference returns bounded refusal.
- Transaction/readback: issue row, audit row, and optional credit/ledger effect link must be written/read back atomically; no direct wallet snapshot mutation.
- Refusal no-write: invalid amount/currency/wallet/tenant/window/campaign/status refusals must not create issuance, grant, ledger, or success audit rows; refusal attempt may be recorded only as bounded safe metadata.

`POST /billing/vouchers/redeem` minimum route DoD:

- Auth/ownership: require user/developer-token ownership scope or approved trusted-user server-side principal; do not trust client-supplied wallet claims.
- Request schema: wallet/project/currency, raw redeem code at boundary, idempotency key, optional bounded client reference, and request trace id; money fields remain fixed-decimal strings.
- Redaction/hash: lookup by server-side code hash/prefix; response returns only redacted voucher identifiers and public ids.
- Idempotency: same key/body replays original redemption/credit/ledger/audit ids; conflict refuses without duplicate credit grant, ledger entry, or success audit.
- Transaction/readback: redemption, redeem attempt, credit-grant or ledger effect, audit, and remaining-balance readback must reconcile to the same public ids.
- Abuse/refusal: expired/revoked/currency mismatch/ownership mismatch/already redeemed/non-positive quota refusals persist safe attempt markers where appropriate and prove no credit/ledger success write.

QA artifact acceptance for the future route slice: write a secret-safe route-level artifact with schema such as `voucher_public_route_runtime.v1`, `route_invoked=true`, `runtime_implemented=true`, `contract_only=false`, `admin_issue_route_verified`, `billing_redeem_route_verified`, `idempotency_replay_passed`, `conflict_no_duplicate_write_passed`, `audit_readback_passed`, `ledger_or_credit_effect_readback_passed`, `refusal_no_write_passed`, `voucher_code_hashed_or_redacted=true`, `raw_voucher_code_echoed=false`, `secret_safe=true`, and `paid_gate_changed=false`.

## E11-LAUNCH-12 / Public voucher route productization watcher

Status: no-op watcher / TODO synchronized, 2026-06-06, owner Agent-E11/Admin Billing.

Artifact scan confirmed no launch-state change. `.tmp/launch/voucher_public_route_and_virtual_key_evidence.json` remains `overall_status=partial` with `voucher_route_evidence.public_routes_wired=false`, `voucher_route_evidence.route_verified=false`, blocker `voucher_public_control_plane_routes_not_wired`, `secret_safe=true`, and `paid_gate_changed=false`. Its virtual-key bounded contract remains verified: server-generated secret is one-time on create, list/get never return raw secret material, audit marker is present, and RBAC maps to `KeyManage`.

`.tmp/launch/api_distribution_operator_packet.json` remains `overall_status=per_user_metadata_required`, `gateway.status=verified`, `gateway.blocker=""`, `blockers=[]`, and `ready_to_send=false` due to per-user metadata plus public route/operator-exception productization scope. `.tmp/launch/voucher_api_distribution_readiness.json` remains `pass_with_productization_gaps` with `production_distribution_full_ready=false`.

E11 classification is unchanged: missing public voucher HTTP route wiring is not a trusted-user beta blocker only because the accepted path is operator-mediated trusted-user handoff. Future productization must implement route auth/RBAC or ownership, request parsing/validation, idempotency replay/conflict, audit/readback, voucher code hash/redaction, refusal no-write, ledger/credit effect readback, no direct wallet snapshot mutation, and a secret-safe route runtime artifact.

## E11-LAUNCH-13 / Public-route backlog after trusted-user handoff orchestrator

Status: docs-only backlog integration / no runtime route wiring, 2026-06-06, owner Agent-E11/Admin Billing.

Current handoff orchestrator: `scripts/prepare_trusted_user_api_distribution_packet.ps1` exists and is the current Release/Ops path for operator-mediated trusted-user beta handoff. Main coordination records its selftest as `ready_to_send_trusted_user_beta` when required values are supplied, and its default/allow-missing mode as blocked only by missing per-user fields.

E11 route classification: public `POST /admin/voucher-issuances` and `POST /billing/vouchers/redeem` remain unwired/partial productization work. They follow the trusted-user handoff orchestrator and are not a current beta blocker while the accepted path is operator-mediated packet preparation plus final secret scan/release checks. They are required for full public/self-serve productization or if Product changes the launch policy away from operator-mediated handoff.

Future public route implementation DoD:

- Auth/RBAC or ownership scope for Admin issuance and billing redeem.
- Fixed-decimal request parsing and tenant/project/wallet/currency validation.
- Server-side voucher code hash/redaction; no raw voucher code, raw virtual key, Authorization/Cookie, provider key, DB URL, or raw secret material in response/artifact.
- Idempotency replay and same-key conflict semantics.
- Audit write/readback and ledger-or-credit effect readback tied to the same public ids.
- Refusal no-write proof for invalid amount, currency mismatch, ownership mismatch, expired/revoked/already redeemed, and status-not-allowed paths.
- Route-level QA artifact with `route_invoked=true`, `runtime_implemented=true`, `contract_only=false`, `secret_safe=true`, and `paid_gate_changed=false`.

## E11-LAUNCH-14 / Voucher route evidence manifest review

Status: docs-only manifest requirements / no runtime route wiring, 2026-06-06, owner Agent-E11/Admin Billing.

Inspected artifacts:

- `.tmp/launch/voucher_public_route_and_virtual_key_evidence.json`: `schema=voucher_public_route_virtual_key_evidence.v1`, `overall_status=partial`, `voucher_route_evidence.public_routes_wired=false`, `voucher_route_evidence.route_invoked=false`, `voucher_route_evidence.route_verified=false`, blocker `voucher_public_control_plane_routes_not_wired`, `secret_safe=true`, `paid_gate_changed=false`.
- `.tmp/launch/api_distribution_operator_packet.json`: `schema=api_distribution_operator_packet.v1`, `overall_status=per_user_metadata_required`, `ready_to_send=false`, `gateway.status=verified`, `blockers=[]`, productization gaps include public voucher route/operator exception policy and per-user metadata, `secret_safe=true`, `paid_gate_changed=false`.

Handoff manifest requirements for E11-owned evidence:

- Include and hash `.tmp/launch/voucher_public_route_and_virtual_key_evidence.json`; manifest fields should expose `overall_status`, `voucher_route_evidence.public_routes_wired`, `voucher_route_evidence.route_invoked`, `voucher_route_evidence.route_verified`, `voucher_route_evidence.blocker`, `implemented_runtime_flags.virtual_key_bounded_route_contract_verified`, `secret_safe`, and `paid_gate_changed`.
- Include and hash `.tmp/launch/api_distribution_operator_packet.json`; manifest fields should expose `overall_status`, `ready_to_send`, `gateway.status`, `blockers`, `productization_gaps`, `virtual_key_distribution.one_time_secret_only`, `virtual_key_distribution.list_get_never_return_secret`, `virtual_key_distribution.raw_virtual_key_secret_in_packet`, `rollback_revoke_verification.status`, `secret_safe`, and `paid_gate_changed`.
- No manifest entry may include raw virtual key, raw voucher code, Authorization/Cookie, provider key, DB URL, raw request body, raw idempotency key, or unredacted secret material.

Bounded/no-raw-secret proof fields: virtual-key handoff is bounded when the route evidence / operator packet show server-side generated key material, one-time secret return on create, list/get redacted output, raw virtual-key secret absent from artifacts, `virtual_key.create` audit marker present, `KeyManage` RBAC present, `secret_safe=true`, and `paid_gate_changed=false`.

Public voucher route classification remains unchanged: it is post-beta productization unless a future route-level artifact proves `route_invoked=true`, `runtime_implemented=true`, `contract_only=false`, Admin issuance and billing redeem route verification, auth/RBAC or ownership scope, fixed-decimal request parsing, voucher hash/redaction, idempotency replay/conflict, audit/readback, ledger-or-credit effect readback, refusal no-write, `raw_voucher_code_echoed=false`, `secret_safe=true`, and `paid_gate_changed=false`.

## E11-LAUNCH-15 / Current evidence_manifest rereview

Status: current manifest reviewed / docs-only, 2026-06-06, owner Agent-E11/Admin Billing.

Current handoff summaries inspected:

- `.tmp/launch/trusted_user_api_distribution_handoff_summary.json`: `overall_status=blocked_by_missing_user_fields_only`, `ready_to_send=false`, evidence manifest present, missing required manifest entries empty.
- `.tmp/launch/trusted_user_api_distribution_handoff_summary.selftest.json`: `overall_status=ready_to_send_trusted_user_beta`, `ready_to_send=true`, evidence manifest present, missing required manifest entries empty.

E11 manifest entries now present with SHA256 hashes in the current summaries:

- `voucher_public_route_and_virtual_key_evidence`: path `.tmp/launch/voucher_public_route_and_virtual_key_evidence.json`, required `true`, exists `true`, schema `voucher_public_route_virtual_key_evidence.v1`, `overall_status=partial`, `blockers_count=1`, SHA256 present. This correctly records public voucher route as partial/productization gap.
- `api_distribution_operator_packet`: path `.tmp/launch/api_distribution_operator_packet.json`, required `false`, exists `true`, schema `api_distribution_operator_packet.v1`, `overall_status=per_user_metadata_required`, `ready_to_send=false`, `blockers_count=0`, SHA256 present. This correctly records per-user metadata gating, not a global launch blocker.
- `voucher_operator_only_exception`: path `.tmp/launch/voucher_operator_only_exception.json`, required `false`, exists `true`, schema `voucher_operator_only_exception.v1`, `overall_status=unapproved`, SHA256 present. This correctly records that operator-only route substitution is not approved.

Manifest verdict: current evidence manifests match E11 posture. They include hashes and bounded status fields for the E11 route/virtual-key evidence, operator packet, and operator-only exception without needing raw voucher code, raw virtual key, Authorization/Cookie, provider key, DB URL, raw request body, or raw provider payload.

Launch classification remains unchanged: the public voucher route gap does not block the accepted operator-mediated trusted-user beta because current handoff is packet/orchestrator based. Full public route productization still requires future route evidence with `route_invoked=true`, `runtime_implemented=true`, `contract_only=false`, auth/RBAC or ownership, fixed-decimal parsing, voucher hash/redaction, idempotency replay/conflict, audit/readback, ledger-or-credit effect readback, refusal no-write proof, `secret_safe=true`, and `paid_gate_changed=false`.

## E11-LAUNCH-16 / Public-route implementation slice scoping

Status: implementation scope defined / no runtime code changed, 2026-06-06, owner Agent-E11/Admin Billing.

Files inspected for route ownership:

- `apps/control-plane/src/admin.rs`: router has Admin virtual-key routes and many billing/admin routes, but no `POST /admin/voucher-issuances` or `POST /billing/vouchers/redeem` route handlers wired. The accepted voucher business proof currently lives as `execute_recharge_voucher_internal_runtime_tx` in the recharge/voucher runtime proof area, using SQLx transaction logic and test/opt-in evidence.
- `apps/control-plane/src/rbac.rs`: current billing write mapping covers price versions, credit grants, ledger adjustment, and opening balance import. It does not yet include voucher issuance/redeem route capabilities or permission mapping.
- `apps/control-plane/src/auth.rs`: remaining-balance ownership principal support exists and can guide billing redeem ownership, but redeem must not trust client-supplied wallet claims.
- `examples/openapi_admin_skeleton.yaml`: `POST /admin/voucher-issuances` and `POST /billing/vouchers/redeem` exist as contract-only boundaries with 501 refusal wording and future runtime schemas.

Smallest implementation slice that can produce `route_invoked=true` evidence:

1. Refactor the reusable issue/redeem SQLx transaction logic from `execute_recharge_voucher_internal_runtime_tx` into production-callable helpers that accept normalized request structs and an authenticated principal/session, while preserving hash/redaction, idempotency, audit, ledger/credit effect, refusal no-write, and rollback semantics.
2. Add `POST /admin/voucher-issuances` handler in `apps/control-plane/src/admin.rs` behind Admin session + `BillingAdjust`; validate tenant/project/wallet/currency, fixed-decimal positive amount/quota, validity window, actor/reason, and required idempotency key.
3. Add `POST /billing/vouchers/redeem` handler in `apps/control-plane/src/admin.rs` behind server-side user/developer-token ownership scope or an explicitly approved trusted-user principal; reject missing/invalid scope before any credit/ledger effect and never trust client wallet claims.
4. Add RBAC/capability entries in `apps/control-plane/src/rbac.rs`: Admin issue should map to `BillingAdjust`; redeem should map to a billing/user ownership boundary rather than broad unauthenticated public access.
5. Update OpenAPI from contract-only/501 wording to runtime-aware responses only after route tests and verifier prove route invocation.
6. Add a bounded verifier, likely extending `scripts/verify_voucher_public_route_and_virtual_key_evidence.ps1` or adding `scripts/verify_voucher_public_route_runtime.ps1`, that calls both routes against local Control Plane, performs DB/readback checks, and writes `.tmp/launch/voucher_public_route_runtime.json`.

Future route artifact acceptance: `.tmp/launch/voucher_public_route_runtime.json` must include `schema=voucher_public_route_runtime.v1`, `overall_status=pass`, `route_invoked=true`, `runtime_implemented=true`, `contract_only=false`, `admin_issue_route_verified=true`, `billing_redeem_route_verified=true`, `auth_rbac_or_ownership_verified=true`, `money_decimal_strings=true`, `voucher_code_hashed_or_redacted=true`, `raw_voucher_code_echoed=false`, `idempotency_replay_passed=true`, `conflict_no_duplicate_write_passed=true`, `audit_readback_passed=true`, `ledger_or_credit_effect_readback_passed=true`, `refusal_no_write_passed=true`, `secret_safe=true`, and `paid_gate_changed=false`.

Risks / blockers to handle in the implementation slice: moving test/opt-in SQLx proof into production code without widening secret exposure; deciding exact redeem auth boundary for user/developer-token versus Admin-mediated trusted-user flow; preserving idempotency replay semantics across HTTP request normalization; ensuring refusal attempts do not create success audit/ledger/credit rows; and avoiding a false pass from OpenAPI-only or internal-function-only evidence.

## E11-LAUNCH-17 / Manifest verifier E11 criteria review

Status: docs-only verifier criteria / no runtime or verifier code changed, 2026-06-06, owner Agent-E11/Admin Billing.

Current manifest state inspected: `.tmp/launch/trusted_user_api_distribution_handoff_summary.json` contains `evidence_manifest.schema=trusted_user_api_distribution_evidence_manifest.v1`, `hash_algorithm=SHA256`, `missing_required_entries=[]`, `no_raw_secret_material_expected=true`, and E11 entries for `voucher_public_route_and_virtual_key_evidence`, `api_distribution_operator_packet`, and `voucher_operator_only_exception`.

E11 criteria the standalone manifest verifier should enforce:

- Required hashed entry: `voucher_public_route_and_virtual_key_evidence` must exist with repo-bounded path `.tmp/launch/voucher_public_route_and_virtual_key_evidence.json`, `exists=true`, non-empty SHA256, schema `voucher_public_route_virtual_key_evidence.v1`, `overall_status=partial|pass` or equivalent known status, and metadata counts present.
- Required hashed entry: `api_distribution_operator_packet` must exist with repo-bounded path `.tmp/launch/api_distribution_operator_packet.json`, `exists=true`, non-empty SHA256, schema `api_distribution_operator_packet.v1`, known `overall_status`, `ready_to_send` field present, and metadata counts present.
- Optional hashed entry when present: `voucher_operator_only_exception` should have repo-bounded path `.tmp/launch/voucher_operator_only_exception.json`, non-empty SHA256, schema `voucher_operator_only_exception.v1`, and known `overall_status` such as `unapproved` or approved policy state.
- Manifest-level checks: `hash_algorithm=SHA256`, no missing required E11 entries, no empty hash for existing E11 artifacts, no absolute/out-of-repo paths, and no duplicate E11 entry names with conflicting hashes.

Criteria the manifest verifier should not over-enforce:

- Do not fail the trusted-user beta solely because `voucher_public_route_and_virtual_key_evidence.overall_status=partial` or `blockers_count=1`. That is the expected public route productization gap and should remain visible for review.
- Do not infer route pass from manifest metadata alone. Route behavior remains a source-artifact/verifier concern until a future `voucher_public_route_runtime.v1` artifact claims `route_invoked=true`.
- Do not require `voucher_operator_only_exception` to be approved for the current operator-mediated beta; approval is only needed if Product/Ops wants it to substitute for missing public route evidence.

E11 manifest secret-like metadata checks: reject manifest entry names, paths, status fields, notes, or summary strings containing raw voucher codes, full virtual keys, `Authorization`, `Cookie`, bearer token values, provider keys, DB URLs such as `postgres://`, raw request bodies, raw provider payloads, or raw idempotency material. Hashes, byte sizes, schemas, bounded paths, booleans, and blocker/status counts are safe to include.

Remaining E11 gap: public voucher route remains productization until a route-level artifact proves `route_invoked=true`, `runtime_implemented=true`, `contract_only=false`, auth/RBAC or ownership, fixed-decimal parsing, voucher hash/redaction, idempotency, audit/readback, ledger-or-credit effect, refusal no-write, `secret_safe=true`, and `paid_gate_changed=false`.

## E11-LAUNCH-18 / Public route implementation preflight

Status: code-level preflight complete / no runtime code changed, 2026-06-06, owner Agent-E11/Admin Billing.

Exact code touch list for the future route implementation:

- `apps/control-plane/src/admin.rs::router`: add `POST /admin/voucher-issuances` first; defer `POST /billing/vouchers/redeem` until the issue route artifact is proven.
- `apps/control-plane/src/admin.rs`: add request/response structs and handler such as `create_voucher_issuance` / `issue_admin_voucher_issuance`; mirror the existing `create_admin_credit_grant` handler style for session extraction, `Json<T>` request parsing, transaction boundary, and response envelope.
- `apps/control-plane/src/admin.rs`: refactor reusable issue-side logic from the current voucher proof area. Candidate symbols to split out or reuse are `execute_recharge_voucher_internal_runtime_tx` for transaction ordering, `voucher_code_hash`, `voucher_code_lookup_prefix`, `voucher_code_redacted`, `insert_admin_audit_log_tx`, and `new_admin_audit_log`. The current function is too broad for first slice because it also covers redeem, refusal, and reversal.
- `apps/control-plane/src/rbac.rs::CONTROL_PLANE_CAPABILITIES`: add a voucher issue capability, for example key `voucher.issue`, method `POST`, path `/admin/voucher-issuances`, required permission `BillingAdjust`, `secret_safe=true`.
- `apps/control-plane/src/rbac.rs::billing_adjust_path`: include `POST /admin/voucher-issuances`; add focused permission tests beside existing credit grant and opening balance tests.
- `examples/openapi_admin_skeleton.yaml`: update contract-only wording only after the route handler and verifier prove runtime route invocation.
- Future verifier path: add or extend a route verifier to write `.tmp/launch/voucher_public_route_runtime.json`; do not mark pass from OpenAPI or internal SQLx-only evidence.

Recommended first implementation slice: implement Admin voucher issuance route only. This is smaller and safer than redeem because it can reuse Admin session + `BillingAdjust`, issue-side voucher hash/redaction, idempotency, issue audit, and issuance readback without settling the user/developer-token redeem ownership boundary. The slice should produce route evidence for `POST /admin/voucher-issuances` with `route_invoked=true`, `admin_issue_route_verified=true`, `billing_redeem_route_verified=false`, and an explicit remaining blocker for redeem route not implemented.

First-slice acceptance DoD:

- Admin session + `BillingAdjust` enforced before write.
- Fixed-decimal positive amount/quota, tenant/project/wallet/currency, validity window, actor/reason, and idempotency key normalized.
- Raw voucher code accepted only at boundary if required; stored and returned as hash/prefix/redacted only.
- Same idempotency key/body replays original issuance/audit ids; same key different body refuses conflict.
- Successful issue writes `voucher_issuances` and success audit in one transaction; optional credit/ledger effect is either linked/read back or explicitly deferred with no false pass.
- Refusals prove no voucher issuance, credit, ledger, or success audit write.
- Artifact is secret-safe: no raw voucher code, raw virtual key, Authorization/Cookie, provider key, DB URL, raw idempotency material, or raw request body.

Blockers/risks before the redeem route slice: decide the exact `POST /billing/vouchers/redeem` auth boundary (`authenticate_remaining_balance_principal` pattern versus Admin-mediated trusted-user principal), avoid trusting client wallet claims, preserve idempotency across HTTP normalization, and prove redeem refusal attempts do not create success ledger/credit/audit rows.

## E11-LAUNCH-19 / Operator-mediated voucher handoff evidence classification

Status: historical verifier/artifact classification; superseded by 2026-06-07 live route verification, owner Agent-E11-OperatorRoute.

Historical run: `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_voucher_public_route_and_virtual_key_evidence.ps1` previously reported `.tmp/launch/voucher_public_route_and_virtual_key_evidence.json` as `overall_status=partial` with route wiring/invocation missing. Current launch state supersedes that: the artifact is pass with public routes wired and live route invocation/readback verified.

Verifier classification now separates operator-mediated handoff from public self-serve productization. `operator_mediated_handoff_evidence.status=bounded_evidence_verified_public_route_productization_gap`, `sufficient_for_operator_mediated_handoff=true`, `not_a_public_route_pass=true`, `operator_path_blockers=[]`, and `public_route_gap_blocks_operator_mediated_handoff=false`.

Evidence basis at the time: existing internal recharge/voucher runtime artifact proved runtime implemented, internal SQLx path invoked, voucher hash/redaction readback, idempotency readback, audit/readback, ledger-or-credit effect readback, refusal no-write, `secret_safe=true`, and `paid_gate_changed=false`. Admin virtual-key bounded route contract proved server-generated key material, one-time secret return on create, list/get secret omission, audit marker, `KeyManage` RBAC, and no raw virtual-key secret in artifacts. Current evidence additionally proves the Control Plane voucher issue/redeem routes live behind `BillingAdjust`.

Operator-only exception remains governed by its own artifact fields and must not be treated as public/self-serve readiness. Since public route proof now exists, route substitution is no longer needed to explain the current API distribution pass. Resume conditions for full public self-serve are Product UX/client flow, ownership policy, user-facing errors, handoff metadata capture, and keeping route-level evidence current.

## E11-LAUNCH-20 / Operator-only exception approval

Status: historical operator-only exception approval; current route proof has since passed, 2026-06-06, owner Agent-E11-OperatorExceptionApproval.

Approval boundary: Product/Release/Ops approved the voucher-backed trusted-user Beta operator-only exception as a manual substitute for the then-missing public self-serve voucher route. This applied only to selected trusted-user Beta distribution mediated by an operator. It did not approve full commercial launch, public self-serve voucher UX, or product UX readiness. The later route proof means the current remaining gap is Product UX/self-serve polish rather than route wiring.

Verifier/artifact requirements:

- `.tmp/launch/voucher_operator_only_exception.json` remains governed by its own `approved` and `route_substitution_allowed` fields; do not use it to claim public/self-serve readiness.
- `.tmp/launch/voucher_public_route_and_virtual_key_evidence.json` is now the current route evidence and should remain `overall_status=pass` with route wiring and live invocation/readback verified unless a future regression proves otherwise.
- `.tmp/launch/api_distribution_operator_packet.json` may remain `ready_to_send=false` when only per-user operator metadata is missing. That state is classified as `per_user_external_inputs=["per_user_operator_metadata_required"]`, not a global blocker and not public-route readiness.

Public self-serve Product UX remains backlog: full public/self-serve productization still requires approved screens/client flow, ownership policy, quota/rate-budget selection UX, user-facing errors, handoff metadata capture, and keeping the existing route-level evidence current for auth/RBAC or ownership, fixed-decimal parsing, voucher hash/redaction, idempotency, audit/readback, ledger-or-credit effect, refusal no-write, `secret_safe=true`, and `paid_gate_changed=false`.
2026-06-12 update: ledger adjustment/runtime writer response now includes `ledger_adjustment_execution_readback` for dry_run, execute_contract/refused, and execute applied/idempotent. The readback is front-end consumable and limited to mode/outcome/status, wallet/credit_grant/budget/ledger/request ref presence, safe idempotency fingerprint, blocked reasons, and safe next action. No production validation was run; raw SQL, wallet secret, Authorization, provider key, raw metadata, and raw idempotency remain forbidden.
