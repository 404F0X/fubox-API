## E12-006 Provider Key Secret Path Artifact Verifier

Status: completed / verifier-only, 2026-06-07

Scope:
- Added `scripts/importers/verify-import-provider-key-secret-path-artifacts.ps1`.
- Covers the checked-in New API and One API OpenAI-compatible importer samples.
- Writes derived artifacts under `.tmp/importers/provider_key_secret_path/`.

Acceptance evidence:
- Command: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/importers/verify-import-provider-key-secret-path-artifacts.ps1`
- Result: pass.
- Summary artifact: `.tmp/importers/provider_key_secret_path/summary.json`
- Summary schema: `importer.provider-key-secret-path-artifact-verification.v1`
- Command: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/importers/verify-import-provider-key-operator-handoff-packet.ps1`
- Result: pass.
- Summary artifact: `.tmp/importers/provider_key_operator_handoff/summary.json`
- Packet artifacts: `.tmp/importers/provider_key_operator_handoff/newapi.operator_handoff_packet.json`, `.tmp/importers/provider_key_operator_handoff/oneapi.operator_handoff_packet.json`
- Summary schema: `importer.provider-key-operator-handoff-packet-verification.v1`

What it proves:
- Source dry-run, internal mapping, and apply-plan artifacts do not contain raw fixture credential locators such as `${OPENAI_API_KEY}`, `${ONE_API_OPENAI_KEY}`, or `env:AZURE_OPENAI_API_KEY`.
- Provider key evidence survives as sidecar handoff metadata for both samples: `source_provider_keys=3`, `mapping_provider_key_handoffs=3`, and `apply_plan_provider_key_handoffs=3` per sample.
- Apply plan keeps provider key material out of write targets and SQL operation targets: `planned_provider_key_writes=0`, `sql_provider_key_operations=0`.
- Handoff contract remains sidecar-only with `raw_material_allowed=false`, `apply_directly_supported=false`, and `required_operator_path="POST /admin/provider-keys"`.
- Operator handoff packets can be generated from importer output for both samples with `operator_handoff_metadata_entries=3` per sample.
- The packets include only non-secret create metadata: provider/channel IDs, key alias, binding status, redacted locator, and locator hash evidence.
- Packet verification records `raw_provider_keys_in_packets=false`, `raw_provider_keys_written=false`, `operator_secret_status=deferred_required`, `audit_readback_status=deferred`, and `api_distribution_blocked=false`.

Remaining E12 work:
- Live Control Plane provider-key creation audit/readback from the handoff metadata.
- Full New API / One API sample apply/rollback parity after conflicts/manual review are resolved into an apply-ready sample.
- Complex channel mapping policy apply/rollback coverage.
