# E9-004 切片记录

> 由 `docs/TODO.md` 拆分而来；只保存切片日志，不作为最终完成状态的唯一依据。

- **E9-004-S4，已完成，2026-06-04，owner Confucius，live 执行阻塞**：billing ledger SQLx/live schema 验证。原因：ledger reserve/settle/refund 已有计划转换与 contract，但最终完成需要证明 SQLx query、schema、事务边界和幂等键在真实迁移结构上可执行。范围：`crates/billing-ledger/**`、ledger 相关 fixtures、必要的 integration harness；不改 Gateway/Control Plane 路由。验收：reserve/settle/refund 的 SQLx bind/returning/schema contract 固定；真实 Postgres 可用时跑 live schema smoke；不可用时记录明确外部阻塞和已覆盖的静态/contract 检查。结果：SQLx reserve/settle/refund executable schema contract 已固定 bind order、`RETURNING` columns、row-count-backed `fetch_all` execution、secret-safe operation-key debug，以及 `ledger_entry_id` status-update targeting。剩余缺口：未设置 `BILLING_LEDGER_LIVE_DATABASE_URL`，尚未在 migrated schema 上跑 live Postgres smoke。
- **E9-004-S5，已完成，2026-06-04，owner Confucius，live 执行阻塞**：billing-ledger SQLx live smoke opt-in/preflight 入口。原因：S4 已固定 SQLx schema contract，但 live DB URL 缺失时开发者需要明确入口、环境变量说明和 preflight failure，而不是把 contract-only 测试误读为 live 已完成。范围：`crates/billing-ledger/**`、`scripts/**`、billing fixtures 或验收文档；不改 Gateway/Control Plane/UI。验收：有明确命令可 dry-run/preflight SQLx live smoke；未设置 `BILLING_LEDGER_LIVE_DATABASE_URL` 时报告外部阻塞；默认测试不依赖 live DB；相关 billing-ledger tests/check 通过。结果：新增 opt-in billing-ledger SQLx live smoke preflight 入口；默认测试保持 DB-free，缺 `BILLING_LEDGER_LIVE_DATABASE_URL` 明确报告外部阻塞，live run 使用 `EXPLAIN` + transaction rollback 验证 reserve/settle/refund SQL shape。剩余缺口：尚未在 migrated live DB 上执行 preflight。
- **E9-004-S6，已完成，2026-06-04，owner Confucius**：billing-ledger SQLx executor runtime wiring readiness。原因：S4/S5 已固定 SQLx statement/schema/live preflight，但后续接 Control Plane/Gateway 前还需要 crate 内清晰证明 executor API 的事务结果、row-count error、secret-safe output 能作为 runtime writer 边界复用。范围：`crates/billing-ledger/**`、billing fixtures；不改 scripts/Gateway/Control Plane/UI。验收：新增或强化 executor-facing contract/test，覆盖 transaction result summary、row-count mismatch/refusal、operation key secret-safe debug/output；默认 tests 和 `postgres-sqlx` feature tests 通过。结果：新增 billing-ledger executor result summary contract，覆盖 transaction flags/counts、row-count refusal mapping、SQLx validation refusal，以及 secret-safe operation-key output/debug，且不依赖 live DB。剩余缺口：尚未把该 runtime writer 边界接入外部 app。
- **E9-004-S7，已完成，2026-06-04，owner Confucius**：Control Plane execute writer 与 billing-ledger executor summary 集成准备。原因：E11-S4 已有 Control Plane 本地最小 SQL writer，E9-S6 已有 billing-ledger executor summary；下一步需要锁定两者之间可迁移的 response/audit summary 边界，避免后续 runtime writer 接入时改变 UI/API 安全输出。范围：`apps/control-plane/**`、control-plane billing fixtures；不改 scripts/Gateway/Admin UI/routing。验收：Control Plane execute response 或 fixture 明确包含/预留 billing-ledger executor summary 兼容字段，保持 secret-safe；targeted control-plane billing/ledger tests 通过。结果：Control Plane ledger adjustment execute contract、applied response、idempotent replay response 与 success audit metadata 增加 `billing_ledger_postgres_executor_summary.v1` 兼容的 secret-safe summary 边界，固定 omitted operation key/error detail/raw metadata/credential 语义。剩余缺口：尚未把 Control Plane 本地 writer 迁移到 billing-ledger runtime writer；后续迁移需保持当前 `ledger_executor_summary` 字段语义不变。
- **E9-004-S8，已完成，2026-06-04，owner Confucius**：Control Plane execute refusal/rollback executor summary 边界。原因：S7 固定了 applied/idempotent success side summary，但 runtime writer 迁移还需要 blocked/refusal/rollback 侧的安全输出语义，避免 UI/API 在错误状态下泄露 operation key、raw metadata 或 executor error detail。范围：`apps/control-plane/**`、control-plane billing fixtures；不改 Gateway/Admin UI/routing/scripts。验收：execute preflight/refusal 或 rollback-adjacent contract 明确包含 compatible refusal summary 或 refusal summary contract；覆盖 committed/rolled_back、refused_statement_count、row_count_mismatch/error_detail_output 语义；保持 secret-safe；targeted control-plane ledger/billing tests 通过。结果：execute contract refusal response 增加 `refused_preflight` ledger executor summary，execute/transaction contract 预留 rollback summary contract，覆盖 committed/rolled_back、refused statement count、row-count mismatch 与 `error_detail_output=omitted`，fixture 同步并增加 secret-safe regression。剩余缺口：仍未迁移到 billing-ledger runtime writer；真实 DB write/audit insert 失败路径后续需映射到同一 summary 形状。
- **E9-004-S9，已完成，2026-06-04，owner Confucius**：Control Plane 到 billing-ledger runtime writer 的纯映射 contract。原因：S7/S8 已固定 response/refusal summary 形状，下一步需要先用 DB-free contract 锁定 Control Plane validated ledger adjustment plan 到 billing-ledger runtime writer operation/summary 的字段映射，再做真实 writer 替换。范围：`apps/control-plane/**`、必要的 `crates/billing-ledger/**` contract/tests 与 fixtures；不改 Gateway/Admin UI/routing/scripts。验收：新增或强化纯函数/test，覆盖 adjust/refund applied/idempotent/refusal 映射到 billing-ledger executor operation/outcome/statement summary；保持 operation key/raw metadata/credential omitted；不要求 live DB；targeted control-plane 与 billing-ledger tests 通过。结果：新增 `control_plane_billing_ledger_runtime_writer_mapping.v1` DB-free mapping contract，锁定 refund -> `ConsistentLedgerWriteRequest::RefundPartial`，adjust -> `admin_adjustment_extension_required`，并证明 applied/idempotent/refused_preflight/refused_rollback summary 与现有 Control Plane response shape 一致且不回显 operation key/raw metadata/credential/raw error detail。剩余缺口：未替换生产 SQL writer；billing-ledger 仍需 admin adjust operation 或替代 writer path。
- **E9-004-S10，已完成，2026-06-04，owner Confucius**：billing-ledger admin adjustment operation contract。原因：S9 已证明 refund 可映射到 `RefundPartial`，但 adjust 仍是 `admin_adjustment_extension_required`；迁移 Control Plane writer 前，billing-ledger 需要 DB-free admin adjustment operation/summary contract 或明确拒绝边界。范围：`crates/billing-ledger/**`、billing-ledger fixtures/tests；不改 Gateway/Admin UI/routing/scripts/Control Plane。验收：新增或强化 billing-ledger pure contract，覆盖 admin adjust confirmed credit/debit plan、idempotency/operation key secret-safe output、executor summary applied/idempotent/refusal shape；默认 tests 通过，不要求 live DB。结果：billing-ledger 新增 `AdminAdjustmentLedgerRequest`、`LedgerOperationKind::AdminAdjustment`、`LedgerEntryType::Adjust` 与 credit/debit metadata，`plan_ledger_admin_adjustment` 支持 confirmed credit/debit、零金额拒绝、idempotent replay 与 conflict refusal；Postgres execution/writer summary contract 支持 admin adjustment executor summary，输出继续隐藏 operation key/idempotency material/raw metadata/credential/raw executor detail。剩余缺口：尚未把 admin adjustment 接入 `ConsistentLedgerWriteRequest` / runtime command executor 的真实 write path；还没有 live SQLx admin adjustment write smoke；Control Plane writer 尚未迁移到 billing-ledger runtime writer。
- **E9-004-S11，已完成，2026-06-04，owner Confucius**：billing-ledger admin adjustment runtime writer bridge。原因：S10 已定义 admin adjustment operation contract，但 consistent writer/runtime command 仍缺将 admin adjustment request 转成可执行 writer command 的边界，Control Plane 迁移前需要先在 ledger crate 内固定该 bridge。范围：`crates/billing-ledger/**`；不改 Gateway/Control Plane/Admin UI/routing/scripts/docs。验收：`ConsistentLedgerWriteRequest` 或等价 runtime writer request 支持 admin adjustment credit/debit；覆盖 apply/idempotent/refusal 到 executor summary 的映射；SQL/Postgres command shape 保持 bounded、row-count enforced、operation key bind-only；public output 不含 operation key/raw metadata/credential/raw executor detail；默认 tests、`postgres-sqlx` feature contract tests、fmt/diff check 通过。结果：`ConsistentLedgerWriteRequest::AdminAdjustment` 支持 confirmed credit/debit，admin debit 进入余额/预算检查，admin credit 进入 credit/refund-like balance window；command executor、Postgres plan 与 SQLx adapter contract 覆盖 admin adjustment applied/idempotent/refusal summary，operation key 仍为 bind-only/private，public serialization/debug 不输出 raw operation key/raw metadata/credential/executor detail/idempotency material。剩余缺口：尚未替换 Control Plane writer；真实 Postgres I/O/live DB smoke 仍需外部环境。
- **E9-004-S12，已完成，2026-06-04，owner Confucius**：Control Plane billing-ledger writer mapping unblocks admin adjustment。原因：S9 的 Control Plane -> billing-ledger mapping 仍把 `adjust` 标为 `admin_adjustment_extension_required`，S10/S11 已在 billing-ledger 内补齐 admin adjustment operation/runtime writer bridge；下一步需要更新 Control Plane mapping contract，证明 adjust/refund 都能映射到 billing-ledger writer request。范围：`apps/control-plane/**`、必要 `crates/billing-ledger/**` tests/fixtures；不改 Gateway/Admin UI/routing/scripts/docs。验收：Control Plane validated adjustment/refund plan 映射到 `ConsistentLedgerWriteRequest::AdminAdjustment`/`RefundPartial` 或等价 writer request；覆盖 applied/idempotent/refused_preflight/refused_rollback summary compatibility；不要求 live DB；operation key/raw metadata/credential/raw error detail omitted；targeted control-plane 与 billing-ledger tests、fmt/diff check 通过。结果：Control Plane mapping contract 现在将 `adjust` 映射到 `ConsistentLedgerWriteRequest::AdminAdjustment`，`refund` 继续映射到 `RefundPartial`，并固定 writer request 字段、operation id 来源、scope/request/amount/currency 和 bind-only operation key；applied/idempotent/refused_preflight/refused_rollback summary compatibility 继续保持 secret-safe。剩余缺口：尚未把 Control Plane 真实 execute writer 迁移到 billing-ledger runtime writer；未做 live DB/runtime adapter 切换。
- **E9-004-S13，已完成，2026-06-04，owner Confucius**：Control Plane billing-ledger writer adapter boundary。原因：S12 已证明 adjustment/refund 都能映射到 billing-ledger writer request，但生产 execute path 仍使用 Control Plane 本地 SQL writer；迁移前需要先固定 adapter 输入、事务结果、错误摘要和 secret-safe 输出边界，避免一次性替换真实 writer。范围：`apps/control-plane/**`、必要 control-plane fixtures/tests；不改 Gateway/Admin UI/routing/scripts/docs。验收：新增或强化 DB-free adapter boundary contract，覆盖 execute validated plan -> billing-ledger writer request -> response/audit executor summary 的字段映射；applied/idempotent/refused_preflight/refused_rollback 均保持现有 API/UI summary shape；operation key/raw metadata/credential/raw executor error detail omitted；不要求 live DB；targeted control-plane tests、fmt 和 diff check 通过。结果：新增 `control_plane_billing_ledger_writer_adapter_boundary.v1`，固定 Control Plane execute validated plan -> billing-ledger writer request contract -> response/audit executor summary 的 DB-free adapter boundary，覆盖 AdminAdjustment/RefundPartial 与 applied/idempotent/refused_preflight/refused_rollback；success audit summary 只在 applied 时写入，secret-safe 输出保持现有 API/UI shape。剩余缺口：生产 execute path 仍未迁移到 billing-ledger runtime writer；未做 live DB 或真实 adapter 调用。
- **E9-004-S14，已完成，2026-06-04，owner Confucius**：Control Plane billing-ledger writer adapter refusal boundary。原因：S13 固定了 adapter 输入/输出和 success audit summary，但真实 writer 迁移前还需要锁定 adapter failure/refusal 分类，避免 billing-ledger runtime writer 返回 row-count mismatch、SQLx validation refusal 或 adapter unavailable 时破坏现有 execute_contract/refusal UI 语义。范围：`apps/control-plane/**`、必要 control-plane fixtures/tests；不改 Gateway/Admin UI/routing/scripts/docs。验收：DB-free contract 覆盖 adapter unavailable、row_count_mismatch、sqlx_validation_refusal 或等价 failure 分类到 refused_preflight/refused_rollback summary；operation key/raw metadata/credential/raw executor error detail omitted；不要求 live DB；targeted control-plane tests、fmt 和 diff check 通过。结果：adapter boundary 增加 `adapter_unavailable`/`sqlx_validation_refusal` -> `refused_preflight` 和 `row_count_mismatch` -> `refused_rollback` 分类，输出 UI-safe status/message 与 commit/rollback/row-count 语义，同时保持 `ledger_executor_summary` shape、operation key/error detail omitted 和 refused/idempotent 无 success audit summary。剩余缺口：尚未接入真实 billing-ledger runtime writer；未做 live DB/SQLx runtime refusal e2e。
- **E9-004-S15，已完成，2026-06-04，owner Confucius**：Control Plane billing-ledger writer adapter shadow invocation contract。原因：S13/S14 已固定 adapter success/refusal summary contract，但生产 writer 仍未替换；下一步需要 shadow invocation contract，证明未来调用 billing-ledger writer 时如何和当前本地 SQL writer 对齐且不产生双写。范围：`apps/control-plane/**`、必要 control-plane fixtures/tests；不改 Gateway/Admin UI/routing/scripts/docs。验收：DB-free contract 覆盖 shadow invocation enabled/disabled、no-double-write、response parity summary 和 audit parity summary；不要求 live DB、不替换生产 writer；secret-safe 输出保持；targeted control-plane tests、fmt 和 diff check 通过。结果：新增 `control_plane_billing_ledger_writer_shadow_invocation.v1`，覆盖 shadow disabled/enabled-contract-only、现有本地 SQL writer 仍为 source of truth、billing-ledger shadow 不允许 ledger/audit write 或 transaction commit、response/audit parity summary 与 operation key bind-only/secret-safe 输出；execute_contract、applied、idempotent 和 success audit metadata 均固定该 handoff。剩余缺口：尚未调用真实 billing-ledger runtime writer；未做 live DB 或 shadow runtime invocation e2e。
- **E9-004-S16，已完成，2026-06-04，owner Confucius**：Control Plane billing-ledger writer cutover preflight contract。原因：S15 已固定 shadow invocation 与 no-double-write 语义；真正替换 production writer 前，需要先锁定 cutover preflight、source-of-truth switch、rollback-to-local writer 和 refusal mapping，避免在没有 live DB 证据时误把 shadow contract 当成生产切换。范围：`apps/control-plane/**`、必要 control-plane fixtures/tests；不改 Gateway/Admin UI/routing/scripts/docs。验收：DB-free contract 覆盖 cutover disabled/shadow/ready 状态、feature flag/env 或等价配置摘要、source-of-truth 字段、no-double-write、rollback-to-local writer、secret-safe refusal output；不要求 live DB、不实际替换 production writer；targeted control-plane tests、fmt 和 diff check 通过。结果：新增 `control_plane_billing_ledger_writer_cutover_preflight.v1`，覆盖 `disabled`/`shadow`/`ready` 三种 DB-free 状态、`AI_CONTROL_PLANE_BILLING_LEDGER_WRITER_CUTOVER_MODE` 摘要、source-of-truth、no-double-write、rollback-to-local writer 和 adapter/sqlx/row-count refusal mapping；当前 contract 不允许 billing-ledger writer commit，也不替换生产 writer。剩余缺口：尚未解析真实 cutover mode 配置；未调用 billing-ledger runtime writer；未做 live DB/SQLx cutover preflight e2e。
- **E9-004-S17，已完成，2026-06-04，owner Confucius**：Control Plane billing-ledger writer cutover mode parser guard。原因：S16 已固定 cutover preflight contract，但配置模式仍只是摘要；本切片锁定 cutover mode 的解析/默认/非法值边界，确保未显式配置时保持 disabled，非法值不会进入 ready，也不会回显 raw env/secret。范围：`apps/control-plane/**`、必要 control-plane fixtures/tests；不改 Gateway/Admin UI/routing/scripts/docs。验收：新增或强化 parser/guard contract，覆盖 missing/default disabled、`disabled`、`shadow`、`ready`、case/whitespace normalization 或明确拒绝、invalid secret-safe refusal；invalid 不允许 production writer replacement 或 billing-ledger commit；operation key/raw metadata/credential/raw env value omitted；targeted control-plane tests、fmt 和 diff check 通过。结果：新增 billing-ledger writer cutover mode parser guard，固定 missing/blank 默认 disabled、`disabled`/`shadow`/`ready` 合法值、ASCII trim/lowercase normalization、invalid -> `disabled_guard_refusal`，并把 parser guard 嵌入 cutover preflight contract；非法值不会替换 production writer、不会允许 billing-ledger commit，raw env/secret/operation key/raw metadata 均保持 omitted。剩余缺口：parser 仍主要通过 contract/config helper 验证；下一片需要锁定实际 env/config integration 的 disabled-by-default 行为。
- **E9-004-S18，已完成，2026-06-04，owner Confucius**：Control Plane billing-ledger writer cutover env integration disabled-by-default guard。原因：S17 已固定 cutover mode parser guard；本切片把 parser 绑定到 env/config injection 摘要路径，证明未配置或非法配置不会让 billing-ledger runtime writer 接管生产写入。范围：`apps/control-plane/**`、必要 control-plane fixtures/tests；不改 Gateway/Admin UI/routing/scripts/docs。验收：新增或强化 env/config injection 或纯 adapter tests，覆盖 missing/invalid 默认 disabled、合法 `ready` 只有显式配置才可进入 ready 摘要、raw env/secret 不输出；runtime 仍不替换 production writer、不允许同请求双写；targeted control-plane tests、fmt 和 diff check 通过。结果：新增 `billing_ledger_writer_cutover_env_config_injection` summary contract，execute contract/applied/idempotent 响应通过 injected config path 且未注入时默认 disabled；missing/blank/invalid 保持 `control_plane_local_sql_writer`，合法 `ready` 只有显式配置才进入 ready 摘要；raw env/secret/operation key/raw metadata/credential 仍 omitted/bind-only。剩余缺口：仍是 DB-free/injected env-config contract；尚未接真实 runtime env provider，也未替换 production local SQL writer。
- **E9-004-S19 Performance，已完成，2026-06-04，owner Confucius**：billing-ledger writer runtime handoff performance guard。原因：E9 要从 local SQL writer/cutover contract 走向真实 runtime writer，下一步需要证明 handoff 摘要能承载 transaction latency、row-count、commit/rollback timing 等高性能/可观测字段，同时默认不接管生产写入。范围：`apps/control-plane/**`、必要 control-plane fixtures/tests；不改 Gateway/Admin UI/scripts/docs。验收：新增或强化 runtime writer handoff/cutover tests，覆盖 disabled-by-default、ready-but-not-cutover、latency/row-count/commit/rollback timing summary、double-write prevention、secret-safe output；targeted control-plane tests、fmt 和 diff check 通过。结果：新增 `control_plane_billing_ledger_writer_handoff_performance.v1` summary contract，cutover preflight 输出 `handoff_performance_summary`，覆盖 disabled、ready-but-not-cutover、row-count mismatch rollback refusal，包含 transaction/begin/lock/statement/commit/rollback latency 字段、row-count/refused count/mismatch、bounded statement scope、no-double-write 和 secret-safe output；当前 contract 明确 `db_io_performed=false`、`production_writer_replaced=false`、`billing_ledger_writer_commit_allowed_in_this_contract=false`。剩余缺口：真实 billing-ledger DB writer 接管和 live cutover 仍未实现，必须单独验收。
- **E9-004-S20 Runtime，已完成，2026-06-04，owner Confucius**：Control Plane billing-ledger runtime env provider preflight。原因：S19 已固定 handoff performance summary，但仍没有把真实 runtime env/config provider、feature gate 和 live cutover preflight 的边界写清；下一片需要证明配置来源可以被安全读取和汇总，同时仍不替换 production writer。范围：`apps/control-plane/**`、必要 control-plane fixtures/tests；不改 Gateway/Admin UI/scripts/docs。验收：新增或强化 runtime env/config provider/preflight tests，覆盖 missing/invalid/disabled/shadow/ready、live DB URL presence marker、runtime writer feature availability marker、handoff performance summary passthrough、no double write、secret-safe output；targeted control-plane tests、fmt 和 diff check 通过。结果：新增 `control_plane_billing_ledger_writer_runtime_env_provider_preflight.v1` contract，覆盖 cutover mode provider、`BILLING_LEDGER_LIVE_DATABASE_URL` presence marker、`billing_ledger_runtime_writer` feature availability marker、handoff performance passthrough 和 live cutover prerequisites；missing/invalid/disabled/shadow/ready 均保持 `production_writer_replaced=false`、`billing_ledger_writer_commit_allowed_in_this_contract=false`、no double write，DB URL 和 env 原值只输出 omitted/presence marker。剩余缺口：真实 billing-ledger writer 接管、live DB cutover 和 production source-of-truth switch 仍需单独验收。
- **E9-004-S21 Runtime，已完成，2026-06-04，owner Confucius**：billing-ledger runtime writer readiness smoke wrapper。原因：S20 已固定 env provider preflight，但后续 live cutover 还需要一个默认 DB-free、显式 opt-in 的 readiness smoke wrapper 来分类 live DB/runtime writer unavailable、ready、blocked，并复用 performance/env summaries。范围：`scripts/**` 中 billing-ledger/runtime writer readiness 相关脚本、必要 `apps/control-plane/**` contract helper/tests；不改 Gateway/Admin UI/routing。验收：新增或强化 script/preflight contract，默认不接 live DB、不切 production writer；显式 flag/env 才检查 live DB URL/runtime writer availability；输出 readiness、duration、row-count/transaction summary unavailable marker 或 blocker；secret-safe；targeted control-plane/script checks、fmt/diff check 通过。结果：新增 `control_plane_billing_ledger_writer_readiness_smoke_wrapper.v1` contract 和 `scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1`；默认 DB-free 输出 `readiness=unavailable`，显式 `-Live`/env 才检查 live DB URL presence 和 runtime writer feature marker，ready/blocked/unavailable 均输出 duration、row-count/transaction/handoff performance unavailable marker、no-double-write 和 secret-safe summary，不回显 DB URL/env/raw metadata/credential。剩余缺口：真实 billing-ledger writer 接管、live DB cutover、production source-of-truth switch 仍需后续单独验收。
- **E9-004-S22 Runtime，已完成，2026-06-04，owner Confucius**：billing-ledger live cutover blocker/evidence matrix。原因：S21 已有 readiness wrapper，但仍需要把 live cutover 前必须满足的 evidence matrix 固定，包括 migrated DB、runtime writer feature、source-of-truth switch、rollback path、performance summaries 和 blocker 分类。范围：`scripts/**` billing-ledger runtime readiness 相关脚本、必要 `apps/control-plane/**` contract/tests；不改 Gateway/Admin UI/routing。验收：新增或强化 live cutover blocker/evidence matrix contract，覆盖 ready/pass/blocker/fail 分类、required env/tool/schema markers、performance summary required fields、rollback/local-writer fallback marker、secret-safe output；默认不切 production writer；targeted tests/script checks、fmt/diff check 通过。结果：新增 `control_plane_billing_ledger_live_cutover_evidence_matrix.v1`，固定 migrated DB presence、runtime writer feature、source-of-truth switch guard、rollback/local writer fallback、performance summaries 和 schema markers 的 required evidence；readiness wrapper 输出 `evidence_matrix`，默认 `classification=blocker`，显式 live/ready/feature opt-in 时可输出 `classification=pass`，但仍保持 `production_source_of_truth_switch_allowed=false`、`billing_ledger_writer_commit_allowed=false`、`dual_commit_allowed=false`；DB URL/env/operation key/credential/raw executor detail 均不回显。剩余缺口：真实 billing-ledger writer 接管、live DB cutover、production source-of-truth switch 和真实 row-count/transaction performance evidence 仍需后续验收。
- **E9-004-S23 Runtime，已完成，2026-06-04，owner Confucius**：billing-ledger runtime writer dry-run execution evidence gate。原因：S22 已固定 cutover evidence matrix，但 `pass` 仍是 readiness-level marker；下一片需要证明在不切 production writer 的前提下，可以形成一次 billing-ledger runtime writer dry-run execution evidence，包含 bounded command count、row-count expectation、transaction step timing 名称和 rollback/fallback guard。范围：`scripts/**` billing-ledger readiness 相关脚本、必要 `apps/control-plane/**` contract/tests 或 `crates/billing-ledger/**` dry-run contract；不改 Gateway/Admin UI/routing/OpenAPI。验收：新增或强化 dry-run execution evidence contract，默认不写 DB、不切 writer、不 dual commit；输出 bounded command/row-count/transaction timing evidence 名称、failure/blocker classification、secret-safe summary；targeted tests/script checks、fmt/diff check 通过。结果：新增 `control_plane_billing_ledger_runtime_writer_dry_run_execution_evidence.v1`，readiness wrapper 输出 `dry_run_execution_evidence`，包含 bounded command count、command kinds、row-count expectations、transaction timing names、rollback/local writer fallback guard 和 blocker/fail classification；默认和 ready opt-in 都保持 `db_write_performed=false`、`production_writer_replaced=false`、`billing_ledger_writer_commit_allowed=false`、`dual_commit_allowed=false`，DB URL/env/operation key/raw metadata/credential/raw executor detail 均不回显。剩余缺口：真实 billing-ledger runtime writer commit、production source-of-truth cutover 和 live DB transaction/row-count/timing 采样仍需后续验收。
- **E9-004-S24 Runtime，已完成，2026-06-04，owner Confucius**：billing-ledger live execution blocker-to-command handoff。原因：S23 已固定 dry-run execution evidence，但还缺把 blocker matrix 转成可执行的 live command handoff，明确在具备 DB/runtime writer 后如何跑一次不切 production writer 的 live execution probe，并如何采集 row-count/timing evidence。范围：`scripts/**` billing-ledger readiness/runtime writer 相关脚本、必要 `apps/control-plane/**` contract/tests；不改 Gateway/Admin UI/routing/OpenAPI/E11/E13。验收：新增或强化 live execution handoff contract，输出 safe live probe command、required env/flag、expected blocker/pass/fail classification、row-count/timing evidence names、rollback/no-commit guard；默认不写 DB、不切 writer、不 dual commit；targeted tests/script checks、fmt/diff check 通过。结果：新增 `control_plane_billing_ledger_live_execution_handoff.v1` contract 和 `-LiveExecutionProbe` script handoff，输出 safe live probe command、required env markers、blocker/pass/fail classification、row-count evidence names、transaction timing names、rollback/no-commit guard 与 secret-safe output；默认 probe_requested=false 且 blocker，ready opt-in 输出 pass handoff 但 `probe_commits_billing_ledger=false`、`production_writer_replaced=false`、`dual_commit_allowed=false`，缺 live DB URL 时按预期 blocker exit 2。剩余缺口：真实 billing-ledger DB writer commit、row-count measurement 和 production cutover 仍需后续 live 验收。
- **E9-004-S25 Runtime，已完成，2026-06-04，owner Confucius**：billing-ledger live probe evidence artifact contract。原因：S24 已给出 live execution handoff，但真实 DB writer 验收前还需要固定 probe 执行后的 evidence artifact 结构，明确 row-count/timing/rollback measurement 如何判定 pass/fail/blocker，避免未来 live probe 只输出日志而无法关闭验收缺口。范围：`scripts/**` billing-ledger readiness/runtime writer 相关脚本、必要 `apps/control-plane/**` contract/tests；不改 Gateway/Admin UI/routing/OpenAPI/E11/E13。验收：新增或强化 live probe evidence artifact contract，覆盖 blocker/pass/fail 三态、row-count actual/expected fields、transaction timing duration fields、rollback/no-commit proof、production writer unchanged、secret-safe command summary；默认不写 DB、不切 writer、不 dual commit；targeted tests/script checks、fmt/diff check 通过。结果：新增 `control_plane_billing_ledger_live_probe_evidence_artifact.v1`，固定 blocker/pass/fail 三态、row-count expected/actual/rows_match/mismatch classification、transaction timing duration fields、rollback/no-commit proof、production writer unchanged 和 secret-safe command summary；默认和 ready handoff 均不写 DB、不切 writer、不 dual commit，ready handoff 下 artifact 仍保持 measurement blocker，`-SimulateProbeRowCountMismatch` 只用于 contract fail 形状验证且不触 DB。剩余缺口：真实 billing-ledger runtime writer commit、live row-count/timing/rollback measurement 和 production cutover 仍需后续验收。
- **E9-004-S26 Runtime，已完成，2026-06-04，owner Confucius**：billing-ledger live probe executor boundary。原因：S25 已固定 post-probe evidence artifact，下一步需要定义真实 DB probe executor 的事务边界、rollback/no-commit 语义和 row-count measurement 输入，确保后续接入 live DB 时不会切 production writer 或双写。范围：`scripts/**` billing-ledger readiness/runtime writer 相关脚本、必要 `apps/control-plane/**` contract/tests；不改 Gateway/Admin UI/routing/OpenAPI/E11/E13。验收：新增或强化 live probe executor boundary contract，覆盖 begin/lock/insert/mark/rollback step ordering、row-count actual/expected capture、timing duration capture、rollback required、commit forbidden、production writer unchanged、secret-safe output；默认不写 DB、不切 writer、不 dual commit；targeted tests/script checks、fmt/diff check 通过。结果：新增 `control_plane_billing_ledger_live_probe_executor_boundary.v1`，固定 rollback-only probe executor step ordering、row-count capture fields、begin/lock/insert/mark/row-count/rollback timing duration fields、row-count expectation inputs、rollback required、commit forbidden、production writer unchanged、dual commit forbidden、bounded statement scope 和 secret-safe output；readiness script 输出 `live_probe_executor_boundary`，默认 `db_write_performed=false`，`-Live -LiveExecutionProbe` 也只进入 handoff/probe boundary，不替换 production writer、不提交 billing ledger、不回显 DB URL/env/operation key/raw metadata。验证：`cargo test -p ai-control-plane ledger_adjustment_billing_runtime -- --nocapture`、`cargo test -p ai-control-plane ledger_adjustment -- --nocapture`、默认 readiness script、`-Live -LiveExecutionProbe` secret-safe run、`cargo fmt -p ai-control-plane --check` 和 scoped diff check 通过。剩余缺口：真实 billing-ledger runtime writer commit、production source-of-truth cutover、live DB begin/lock/insert/rollback executor measurement 仍需后续验收。
- **E9-004-S27 Runtime，已完成，2026-06-04，owner Confucius**：billing-ledger live probe executor measurement artifact contract。原因：S26 已固定 rollback-only executor boundary，但 row-count/timing 仍是 pending inputs；下一片需要定义真实 probe executor 运行后如何把 actual row-count、duration、rollback/no-commit proof 写入 evidence artifact，并拒绝缺失或 stale measurement。范围：`scripts/**` billing-ledger readiness/runtime writer 相关脚本、必要 `apps/control-plane/**` contract/tests；不改 Gateway/Admin UI/routing/OpenAPI/E11/E13。验收：新增或强化 live probe measurement artifact contract，覆盖 row-count actual/expected/rows_match、begin/lock/insert/mark/row-count/rollback duration、rollback observed、commit observed false、production writer unchanged、stale/missing measurement refusal、secret-safe DB URL/env/operation key omission；默认不写 DB、不切 writer、不 dual commit；targeted tests/script checks、fmt/diff check 通过。结果：live probe artifact 新增 freshness/provenance 字段、row-count `rows_affected_source`、begin/lock/insert/mark/row-count/rollback duration、rollback/no-commit proof，并把 missing row-count/timing/rollback proof 与 stale artifact 明确归为 blocker；script 增加 `-SimulateProbeMeasurements`、`-SimulateProbeRowCountMismatch`、`-SimulateStaleProbeArtifact`，可在 contract-only 模式验证 pass/fail/blocker 三态，且不写 DB、不替换 production writer、不 dual commit、不回显 DB URL/env/raw executor detail。剩余缺口：真实 billing-ledger runtime writer commit、production source-of-truth cutover、live DB begin/lock/insert/rollback executor measurement 仍需后续验收。
- **E9-004-S28 Runtime，已完成，2026-06-04，owner Confucius**：billing-ledger live probe measurement readback/freshness gate。原因：S27 已能生成 measurement artifact 三态，但还没有独立 readback gate 来拒绝旧 artifact、缺字段 artifact、row-count/timing/rollback proof 不一致 artifact；下一片固定 readback 验收边界，避免未来 live probe 产物被误读。范围：`scripts/**` billing-ledger readiness/runtime writer 相关脚本、必要 `apps/control-plane/**` contract/tests；不改 Gateway/Admin UI/routing/OpenAPI/E11/E13。验收：新增或强化 artifact readback/freshness contract，覆盖 current commit/generated_at/freshness marker、missing row-count/timing/rollback fields、row-count mismatch、commit observed true、production writer replaced true、dual commit observed true、stale artifact refusal、secret-safe DB URL/env/raw executor omission；默认不写 DB、不切 writer、不 dual commit；targeted tests/script checks、fmt/diff check 通过。结果：新增 `control_plane_billing_ledger_live_probe_measurement_readback_gate.v1`，readback gate 校验 `generated_at_utc`、`current_commit`、`freshness_marker`、`stale_artifact`、row-count actual/expected/rows_match、transaction timing duration 与 rollback/no-commit proof；`-SimulateMissingRowCountReadback`、`-SimulateMissingTimingReadback`、`-SimulateMissingRollbackProofReadback`、stale artifact 均归为 blocker，row-count mismatch、commit observed、production writer replaced、dual commit observed 均归为 fail；默认不写 DB、不替换 production writer、不 dual commit、不回显 DB URL/env/operation key/raw executor detail。验证：`cargo test -p ai-control-plane ledger_adjustment_billing_runtime`、`cargo test -p ai-control-plane ledger_adjustment`、默认 readiness script、ready + simulated measurements pass、missing row-count/timing/rollback proof blocker、row-count mismatch/commit observed fail、`cargo fmt -p ai-control-plane --check` 和 scoped diff check 通过。剩余缺口：真实 billing-ledger runtime writer commit、production source-of-truth cutover、live DB begin/lock/insert/rollback executor measurement 仍需后续验收。
- **E9-004-S29 Runtime，已完成，2026-06-04，owner Confucius**：billing-ledger live probe readback artifact write/read opt-in path。原因：S28 已完成 live probe measurement readback/freshness gate，但 readback 仍主要来自当前 summary 对象；未来真实 DB probe 需要一个 repo-bounded artifact write/read path 来保存并读取 row-count/timing/rollback evidence。范围：`scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1`、必要 `apps/control-plane/src/admin.rs` contract/tests、必要 `tests/fixtures/control-plane/**`；不改 Gateway/Admin UI/OpenAPI/E11/E13。验收：默认不写 artifact、不写 DB、不切 writer、不 dual commit；显式 flag/env 才写入 repo 内 `.tmp/**`；拒绝 repo 外、`.git`、source/docs/scripts 覆盖；artifact readback 校验 schema/current commit/generated_at/freshness/row-count/timing/rollback proof；stale commit/generated_at、missing row-count/timing/rollback proof -> blocker；row-count mismatch/commit observed/production writer replaced/dual commit -> fail；输出不回显 DB URL/env/operation key/raw executor detail；targeted tests/script checks、fmt 和 scoped diff check 通过。结果：新增 `control_plane_billing_ledger_live_probe_artifact_path_gate.v1`，支持 `-WriteProbeArtifact`、`-ReadProbeArtifact`、`-ArtifactPath` 与对应 env；默认不写 artifact，安全路径限定 repo 内 `.tmp/**/*.json`，拒绝 repo 外、`.git`、source/docs/scripts 覆盖；write/read round-trip 后 readback gate 校验 current commit、generated_at、freshness、row-count、timing 与 rollback proof，stale/missing proof 归为 blocker，row-count mismatch/commit observed/production writer replaced/dual commit 归为 fail，DB URL/env/operation key/raw executor detail 仍不回显。验证：`cargo test -p ai-control-plane ledger_adjustment_billing_runtime`、`cargo test -p ai-control-plane ledger_adjustment`、默认 readiness script、artifact write/read round-trip、不安全路径/仓库外路径/stale/missing proof blocker、row-count mismatch/commit/production-writer/dual-commit fail、`cargo fmt -p ai-control-plane --check` 和 scoped diff check 通过。剩余缺口：真实 DB writer commit、production cutover、live DB measurement 仍需后续验收。
- **E9-004-S30 Runtime，已完成，2026-06-04，owner Confucius**：billing-ledger runtime writer live DB executor opt-in command boundary。原因：S29 已固定 live probe artifact write/read path，但真实 runtime writer 仍没有明确的 opt-in live DB executor command boundary；下一片需要把“可执行但 rollback-only/no-cutover”的 SQLx writer probe 命令、row-count/timing summary 和 blocker/fail 分类固定下来。范围：`scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1`、必要 `apps/control-plane/**` 或 `crates/billing-ledger/**` contract/tests、必要 fixtures；不改 Gateway/Admin UI/OpenAPI/E11/E13。验收：默认不写 DB、不切 production writer、不 dual commit；显式 env/flag 才允许 live DB executor probe，且仍 rollback-only/no production cutover；输出 begin/lock/insert/mark/rollback duration、row-count actual/expected、commit forbidden、production writer unchanged、artifact write/read handoff；缺 DB/schema/tool -> blocker，row mismatch/commit observed/dual commit -> fail；DB URL/env/operation key/raw executor detail 不回显；targeted tests/script checks、fmt 和 scoped diff check 通过。结果：新增 `control_plane_billing_ledger_live_db_executor_probe_command_boundary.v1`，支持 `-RunLiveDbExecutorProbe` / `AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_DB_EXECUTOR_PROBE`，要求 live opt-in、ready cutover mode、DB URL presence marker、runtime writer/schema/tool marker 与 artifact write/read handoff；summary 输出 rollback-only、commit forbidden、production writer unchanged、dual commit forbidden、row-count/timing/rollback proof 和 secret-safe command summary。验证：control-plane targeted tests、默认 readiness script、缺 schema/tool blocker、完整 opt-in pass、row-count mismatch/commit observed/production writer replaced/dual commit fail、`cargo fmt -p ai-control-plane --check` 和 scoped diff check 通过。剩余缺口：真实 DB executor 尚未执行 begin/lock/insert/mark/rollback；真实 billing-ledger writer commit 和 production cutover 仍需后续验收。
- **E9-004-S31 Runtime，已完成，2026-06-04，owner Confucius**：billing-ledger live DB executor SQL bridge/readiness artifact。原因：S30 已固定 live DB executor command boundary，但缺少把 bounded SQL plan、schema/tool readiness 和 rollback-only execution artifact 串起来的 bridge；下一片需要让受控 live 环境能看清将执行哪些 bounded SQL、bind marker、row-count/timing proof 和 rollback proof。范围：`scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1`、`apps/control-plane/src/admin.rs`、`tests/fixtures/control-plane/ledger_adjustment_dry_run_contract.json`；不改 Gateway/Admin UI/OpenAPI/E11/E13。验收：默认不写 DB、不输出 raw SQL secrets、不切 writer；显式 opt-in 输出 safe SQL bridge/readiness artifact，包含 bounded statement kinds、bind marker counts、schema/tool readiness、row-count/timing field names、rollback-only/no-commit proof；缺 DB/schema/tool -> blocker；row mismatch/commit/dual commit -> fail；secret-safe；targeted tests/script checks、fmt 和 scoped diff check 通过。结果：新增 `control_plane_billing_ledger_live_db_executor_sql_bridge_readiness_artifact.v1`，输出 bounded statement kinds、bind marker counts、schema/tool readiness、row-count/timing 字段名、rollback/no-commit proof 与 safe-output markers；默认保持 blocker/no DB write。剩余缺口：真实 live DB executor 尚未执行 SQL begin/lock/insert/mark/rollback；真实 billing-ledger writer commit 与 production cutover 仍需后续验收。
- **E9-004-S32 Runtime，已完成，2026-06-04，owner Confucius**：billing-ledger live DB rollback executor dry-run-to-live gate。原因：S31 已有 SQL bridge/readiness artifact，但真实 executor 仍停在 command/readiness 层；下一片需要固定在显式 live opt-in 下如何执行 rollback-only SQL probe，并把 row-count/timing/rollback proof 写入 artifact，同时默认不写 DB。范围：`scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1`、必要 `apps/control-plane/**`/`crates/billing-ledger/**` contract 或 fixtures；不改 Gateway/Admin UI/OpenAPI/E11/E13。验收：默认 no DB write；显式 `-Live -LiveExecutionProbe` + env/tool/schema/artifact opt-in 才允许 rollback-only executor；缺 DB/schema/tool/artifact -> blocker；row mismatch、commit observed、dual commit observed、raw SQL output -> fail；pass artifact 必须包含 bounded statement kind、bind marker count、actual row counts、per-step duration、rollback observed、commit=false、secret-safe provenance；targeted tests/script checks、fmt 和 scoped diff check 通过。结果：新增 `control_plane_billing_ledger_live_db_rollback_executor_gate.v1`，固定 `-ExecuteRollbackOnlyLiveProbe`/env opt-in、artifact write/read handoff、SQL bridge/readback dependency、row-count/timing/rollback proof、commit/dual/raw SQL fail classification 和 secret-safe provenance；默认仍 blocker/no DB write。剩余缺口：真实 live DB SQL executor 尚未实际执行 begin/lock/insert/mark/rollback；production writer commit 与 cutover 仍需后续验收。
- **E9-004-S33 Runtime/Live，已完成，2026-06-04，owner Confucius，加大切片**：billing-ledger rollback executor live attempt 与 production writer cutover preflight。结果：强化 runtime writer readiness/cutover preflight、dry-run/live evidence、row-count/timing/rollback proof shape 与 production writer no-dual-commit guard；Docker 启动后显式 ready-mode + live DB URL + runtime writer/schema/tool marker 的 live probe 返回 readiness `ready`/evidence matrix `pass`，默认无 env 仍保持 DB-free unavailable/blocker。验证：`ledger_adjustment_billing_runtime`、`ledger_adjustment`、readiness script default/live、fmt、diff check 通过。剩余缺口：当前 live probe 仍未完成真实 DB row-count/timing executor DB IO；production writer commit/cutover 仍需单独显式验收。
- **E9-004-S34 Runtime，已完成，2026-06-04，owner Confucius，加大切片**：rollback-only live DB executor real row-count/timing proof。结果：新增显式 opt-in Docker Postgres rollback-only probe，使用 compose exec psql 执行 begin/lock/prepare wallet/insert/mark/capture/rollback，artifact/readback 包含 actual row counts、per-step durations、rollback_observed=true、commit_observed=false、production_writer_replaced=false、dual_commit_observed=false；默认路径仍不写 DB、不切 writer。验证：真实 rollback-only DB IO pass、negative simulations、Control Plane targeted tests、fmt、diff check 通过。剩余缺口：billing-ledger runtime writer commit 与 production source-of-truth cutover 仍未启用。
- **E9-004-S35 Runtime/Cutover，已完成，2026-06-04，owner Confucius，加大切片**：production writer shadow/commit handoff without source-of-truth cutover。原因：S34 已证明 rollback-only DB IO，下一步需要把真实 runtime writer commit 的 shadow/guard 条件固定，但仍不能切 production source of truth。范围：`apps/control-plane/**`、`scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1`、必要 fixtures；不改 Gateway/Admin UI/OpenAPI/E13。验收：新增/强化 shadow writer commit handoff，要求 explicit opt-in、no dual commit、rollback fallback、row-count/timing summary、production source-of-truth switch remains blocked；默认不 commit；targeted tests/script checks/fmt/diff check 通过。结果：新增 `-ShadowCommitHandoff` 与 `control_plane_billing_ledger_shadow_commit_handoff.v1`，复用 rollback-only live probe row-count/timing evidence，同时固定 `real_commit_performed=false`、`production_source_of_truth_switch_allowed=false`、`billing_ledger_shadow_commit_allowed=false`、`dual_commit_allowed=false`；真实 live DB rollback-only probe 通过，row-count/timing 可用。剩余缺口：真实 billing-ledger runtime writer commit 和 production source-of-truth cutover 仍需独立显式验收。
- **E9-004-S36 Runtime/Cutover，已完成，2026-06-04，owner Confucius，加大切片**：production cutover acceptance gate and rollback plan。原因：S35 已固定 shadow commit handoff，但 production source-of-truth cutover 不能靠默认脚本隐式发生；需要可审计的 acceptance gate、rollback command 和 no-dual-commit proof。范围：`apps/control-plane/**`、`scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1`、必要 fixtures/runbook；不改 Gateway/Admin UI/E11 browser/E13。结果：新增 `control_plane_billing_ledger_production_cutover_acceptance_gate.v1`，默认拒绝 production switch；新增 `-AcceptProductionCutover`、`-LiveCommitProof`、`-AcknowledgeRollbackPlan` gate，缺 live commit proof/rollback acknowledgement/shadow handoff/rollback proof/no-dual-commit 时明确 blocker；输出 rollback command、row-count/timing/commit eligibility 且不输出 raw env/DB URL。验证：`cargo test -p ai-control-plane ledger_adjustment_billing_runtime -- --nocapture`、`cargo test -p ai-control-plane ledger_adjustment -- --nocapture` 通过。剩余缺口：真实 production source-of-truth cutover 与 billing-ledger runtime writer commit 尚未执行。
- **E9-004-S37 Runtime/Cutover，已完成，2026-06-04，owner Confucius，加大切片**：runtime/source evidence mismatch gate。原因：S36 已有 acceptance gate，但 live/runtime 证据仍可能与 source-level tests 脱节；下一步区分 source pass、runtime stale、rollback-only live probe 与真实 cutover。范围：`scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1`、`tests/fixtures/control-plane/ledger_adjustment_dry_run_contract.json`、必要 E9 contract 函数；不改 E11 browser runner。结果：新增 `control_plane_billing_ledger_runtime_evidence_trust_gate.v1`、`-RuntimeContainerCommitMarker` / `AI_CONTROL_PLANE_BILLING_LEDGER_RUNTIME_CONTAINER_COMMIT`，区分 `source_level_pass`、`rollback_only_live_probe_only`、`container_runtime_stale`、`container_runtime_current`；production cutover acceptance gate 要求 `container_runtime_current` 才能 commit eligible。验证：`cargo test -p ai-control-plane ledger_adjustment_billing_runtime -- --nocapture`、readiness checks、fmt/diff check 通过。剩余缺口：runtime marker 匹配只是 cutover 前置，不执行 source-of-truth switch。
- **E9-004-S38 Runtime/Cutover，已完成/阻塞收束，2026-06-04，owner Confucius，加大切片**：single-writer production cutover proof。原因：S37 已区分 runtime evidence trust，但最终缺真实 writer commit、单一 active writer、无 dual commit、rollback path。范围：E9 readiness/cutover scripts 与 Control Plane E9 contract；不改 E11 runner。结果：新增 `control_plane_billing_ledger_single_writer_cutover_proof_gate.v1`、`-SingleWriterCutoverProof`、`-ActiveWriterMarker`、active writer/local writer disabled env markers；gate 要求 runtime evidence 为 `container_runtime_current`、active writer 为 `billing_ledger_runtime_writer`、local writer disabled、rollback observed、no commit/no dual commit observed；acceptance gate 必须 single-writer proof pass 才 eligible。验证：Control Plane targeted tests、readiness checks、fmt/diff check 通过。剩余缺口：single-writer proof gate 是 marker/evidence gate，不执行真实 source-of-truth switch。
- **E9-004-S39 Runtime/Cutover，已完成/阻塞收束，2026-06-05，owner Turing**：single-writer cutover proof explicit readback gate。原因：S38 仍偏 marker/evidence gate，生产 cutover 前需要把 live commit readback、source-of-truth、runtime commit、rollback freshness 和 no-dual-commit 分类固定为可拒绝 stale/missing/simulated evidence。范围：`apps/control-plane/src/admin.rs`、`scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1`、`tests/fixtures/control-plane/ledger_adjustment_dry_run_contract.json`。结果：single-writer gate 新增 explicit readback/freshness/no-dual-commit 分类；默认不 commit、不切 writer、不允许 dual commit；missing live/current/source-of-truth/rollback proof 为 blocker，dual commit/commit observed/source-of-truth mismatch 为 fail，simulated rollback proof 不能让 cutover proof pass。验证：Control Plane targeted test、readiness default/negative/pass simulations、`cargo fmt -p ai-control-plane --check`、scoped diff check 通过。剩余缺口：真实 billing-ledger runtime writer commit 与 production source-of-truth cutover execution/readback 仍未完成。
- **E9-004-S40 Runtime/Cutover，已完成/阻塞收束，2026-06-05，owner Turing**：no-cutover live commit proof artifact/readback boundary。原因：S39 固定 explicit readback gate 后，仍缺外部受控 runtime writer commit proof artifact 的写入/readback/path gate，无法区分真实 commit proof、script template 和 simulated evidence。范围：`apps/control-plane/src/admin.rs`、`scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1`、`tests/fixtures/control-plane/ledger_adjustment_dry_run_contract.json`。结果：新增 no-cutover live commit proof artifact/readback boundary，定义 current commit、runtime container commit、measurement source、active writer、source-of-truth、single active writer、commit proof、no-dual-commit proof；repo-bounded artifact path 只允许 `.tmp` JSON，拒绝 `.git`、source/docs/scripts/tests/web；`-WriteLiveCommitProofArtifact` 只写 template 且 `template_can_pass=false`，`-ReadLiveCommitProofArtifact` 校验外部 artifact，stale/missing/simulated/script-generated/mismatch 均 blocker/fail。验证：Control Plane targeted tests、readiness default/missing/template/external artifact simulations、full fmt、diff check 通过。剩余缺口：真实 billing-ledger runtime writer commit runner 与 production source-of-truth cutover execution/readback。
- **E9-004-S41 Runtime/Cutover，已完成/阻塞收束，2026-06-05，owner Pauli**：runtime writer commit runner no-cutover command skeleton and artifact handoff。原因：S40 已有 external artifact readback boundary，但缺受控 runner command skeleton 来说明真实 runtime writer commit proof 如何由外部提供，同时防止脚本内联 commit/cutover。范围：`apps/control-plane/src/admin.rs`、`scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1`、`tests/fixtures/control-plane/ledger_adjustment_dry_run_contract.json`。结果：新增 runtime writer commit runner command skeleton contract；`-PlanRuntimeWriterCommitRunner` 只输出受控 command/required env/flags/artifact handoff；`-RunRuntimeWriterCommitRunner` 明确拒绝脚本内执行并分类 blocker，不 commit、不 cutover；live commit proof artifact 增加 row-count proof schema/readback，要求 row-count/commit/no-dual markers；script-generated/template artifact 只能 blocker；single-writer gate 要求 runner artifact handoff 通过。验证：Control Plane targeted tests、readiness default/negative/ready-plan/run-refusal simulations、full fmt、diff check 通过。剩余缺口：真实 external billing-ledger runtime writer commit runner 与 production source-of-truth cutover execution/readback。
- **E9-004-S42 Runtime/Cutover，已完成/阻塞收束，2026-06-05，owner Pauli**：external runtime writer commit runner artifact acceptance/readback self-test。原因：S41 有 command skeleton，但还需要 artifact acceptance/readback 证明外部 proof 的 freshness、provenance、row-count/no-dual 可被拒绝或接受。结果：live commit proof artifact 增加 runner provenance proof；self-test 覆盖 accepted external artifact、row-count mismatch、commit missing、no-dual failure、stale runtime commit、stale artifact、provenance mismatch 分类。默认不请求 self-test、不写 DB、不 commit、不 cutover；self-test pass 不能替代真实 external proof，simulated/template artifact 仍不能 pass。验证：Control Plane targeted tests、readiness default/negative/acceptance simulations、scoped fmt、scoped diff check 通过。剩余缺口：真实 external billing-ledger runtime writer commit runner 与 production source-of-truth cutover execution/readback。
- **E9-004-S43 Cutover Final DoD，已完成/阻塞收束，2026-06-05，owner Pauli**：billing-ledger runtime writer commit 与 production source-of-truth cutover final DoD。原因：E9 已有多个 preflight/proof gate，但最终 `[x]` 必须区别 cutover-ready 与真实 cutover execution/readback。结果：新增 final DoD checklist/acceptance matrix；机器可读 summary 区分 cutover-ready 与最终 `[x]`，覆盖 current runtime、external commit runner proof、row-count/no-dual/active-writer/source-of-truth/rollback/post-cutover readback 以及 stale/simulated/template refusal。默认不 commit、不 cutover、不写 DB；self-test/contract pass 不能替代真实 external commit proof，cutover-ready 也不能标最终完成。验证：Control Plane targeted tests、readiness default/template/self-test/ready-plan simulations、full/scoped fmt、diff check 通过。剩余缺口：真实 external runtime writer commit runner、actual production source-of-truth switch execution、post-cutover readback。
- **E9-004-S44 Cutover Operator Runbook And Preflight，已完成/阻塞收束，2026-06-05，owner Pauli**：cutover operator runbook/preflight contract。原因：S43 已定义 final DoD，但真实 cutover 执行前后还需要可审计、可回滚、可拒绝 stale/simulated/template evidence 的执行手册和 preflight gate。结果：新增 `control_plane_billing_ledger_cutover_operator_runbook_preflight.v1`，固定 preflight、external commit proof readback、cutover dry-run/ready、actual cutover opt-in、post-cutover readback、rollback proof 的顺序、命令形状、artifact schema 和 refusal taxonomy；默认不 commit、不 cutover、不写 DB，actual cutover 需要独立 operator opt-in 且本脚本不执行。验证：Control Plane targeted tests、readiness default/negative/ready/self-test/unsafe-path simulations、full/scoped fmt、diff check 通过。剩余缺口：真实 external runtime writer commit runner、production source-of-truth switch execution/readback、rollback proof。
- **E9-004-S45 Cutover Evidence Acceptance Matrix，已完成/阻塞收束，2026-06-05，owner Pauli**：external commit/cutover/readback/rollback artifact acceptance gate。原因：S44 已有 operator runbook/preflight，但还需要外部 artifact 的 accepted/refused 状态判定。结果：新增 cutover evidence acceptance matrix/readback gate；默认不读 artifact、不 commit、不 cutover、不写 DB，只有 `-ReadCutoverEvidenceArtifact` / env opt-in 才读取 `.tmp/**/*.json` bounded artifact。Accepted-shape simulation 可进入 `cutover_evidence_accepted_for_review=true`，但 `actual_cutover_executed=false`、`final_x_eligible=false`、`simulation_can_mark_final_x=false`；refusal 覆盖 missing/unsafe/stale/template/simulated/runtime mismatch/commit proof missing/row-count/no-dual/active-writer/source-of-truth/post-readback/rollback/raw-secret。验证：Control Plane targeted tests、readiness default/unsafe/accepted-shape/raw-secret simulations、scoped fmt、diff check 通过。剩余缺口：真实 external runtime writer commit runner、actual production source-of-truth switch execution/readback、rollback proof。
- **E9-004-S46 Cutover Final Closure Audit，已完成/阻塞收束，2026-06-05，owner Pauli**：final closure audit/report。原因：E9 的 final DoD、operator runbook 和 evidence acceptance gate 已在位，需要单一 closure report 供主线程判断是否仍 blocked。结果：新增 `cutover_final_closure_audit`，输出 `final_x_eligible`、blocking reasons、required evidence、commit proof/cutover evidence/actual cutover/post-cutover readback/rollback proof 状态、active writer/source-of-truth/no-dual summary、generated_at/current commit、secret-safe omission 与 exact next commands。当前无真实 production artifact 时 audit 必须 blocked，`final_x_eligible=false`；accepted-shape simulation 只证明 shape/review，仍 blocked 并给出 `accepted_shape_simulation_not_final`。验证：Control Plane targeted tests、readiness final audit default/unsafe/simulation/raw-secret checks、scoped fmt、diff check 通过。剩余缺口：真实 external runtime writer commit runner、actual production source-of-truth switch execution/readback、rollback proof。
- **E9-004-S47 Cutover Evidence Watcher And Operator Checklist，已完成/阻塞收束，2026-06-05，owner Pauli**：cutover evidence watcher/checklist。原因：E9 已进入等待真实 cutover evidence 阶段，需要稳定入口接收 operator artifact。结果：新增 `cutover_evidence_watcher_checklist`，输出 `current_status`、expected artifact paths、required operator actions、exact commands、final review checklist 与 safe defaults；默认 `blocked_waiting_for_operator_evidence`、`final_x_eligible=false`，不轮询、不读 artifact、不写 DB、不 commit/cutover。Artifact 不存在时不报假 fail，只输出 blocked 和下一条 exact readback command；accepted-shape simulation 仍 blocked/final=false。验证：Control Plane targeted tests、readiness watcher default/missing/simulation checks、scoped fmt、diff check 通过。剩余缺口：真实 external runtime writer commit runner、actual production source-of-truth switch execution/readback、rollback proof。
- **E9-004-S48 Final Guard Consistency Review，已完成/阻塞收束，2026-06-05，owner Pauli**：E9 final guard consistency review。原因：E9 已进入等待真实证据阶段，最后风险是状态语义不一致导致误关。结果：补齐统一 machine-readable guard：`simulation_can_mark_final_x=false`、`template_can_mark_final_x=false`、`watcher_can_mark_final_x=false`、`accepted_for_review_can_mark_final_x=false`、`cutover_ready_can_mark_final_x=false`。`cutover_ready`、`accepted_for_review`、watcher blocked/waiting、template/simulation artifact 都不能关闭 E9；final `[x]` 只允许真实 external commit runner proof + actual source-of-truth switch + post-cutover readback + rollback proof + no-dual/secret-safe proof 全部通过。验证：rg guard checks、PowerShell parse、fixture JSON parse、readiness guard checks、Control Plane targeted tests、cargo fmt、diff check 通过。剩余缺口：真实 external runtime writer commit runner、actual production source-of-truth switch execution/readback、rollback proof。
- **E9-004-S49 Cutover Evidence Wait-State Maintenance，已完成/阻塞收束，2026-06-05，owner Pauli，no-op review**：cutover evidence wait-state maintenance。结果：未改文件、未执行 commit/cutover、未写 DB、默认未读 artifact；`.tmp\billing-ledger\live-commit-proof-artifact.json` 存在但未读取，cutover/live-probe evidence artifact 不存在；watcher/default audit 仍 blocked，`cutover_final_closure_audit.classification=blocked`、`final_x_eligible=false`、`cutover_evidence_watcher_checklist.current_status=blocked_waiting_for_operator_evidence`。剩余缺口：真实 external runtime writer commit runner、actual source-of-truth switch/readback、rollback proof。
- **E9-004-S50/S51 Cutover Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Pauli，no-op reviews**：cutover evidence intake repeat。结果：未改文件、未执行 commit/cutover/DB write、未读 artifact；`.tmp\billing-ledger\live-commit-proof-artifact.json` 存在但未读，cutover/live-probe evidence artifact 不存在；watcher/final audit 仍 blocked，`final_x_eligible=false`、`cutover_evidence_watcher_checklist.current_status=blocked_waiting_for_operator_evidence`。剩余缺口：真实 external runtime writer commit runner、actual source-of-truth switch/readback、rollback proof。
- **E9-004-S52 Cutover Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Pauli，no-op review**：cutover evidence intake repeat。结果：未改文件、未执行 commit/cutover/DB write、未读 artifact；`.tmp\billing-ledger\live-commit-proof-artifact.json` 存在但未读，cutover/live-probe evidence artifact 不存在；watcher/final audit 仍 blocked，`final_x_eligible=false`。剩余缺口：真实 external runtime writer commit runner、actual source-of-truth switch/readback、rollback proof。
- **E9-004-S53 Cutover Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Pauli，no-op review**：cutover evidence intake repeat。结果：未改文件、未执行 commit/cutover/DB write、未读 artifact；`.tmp\billing-ledger\live-commit-proof-artifact.json` 存在但未读，cutover/live-probe evidence artifact 不存在；watcher/final audit 仍 blocked，`final_x_eligible=false`。剩余缺口：真实 external runtime writer commit runner、actual source-of-truth switch/readback、rollback proof。
- **E9-004-S54 Cutover Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Pauli，no-op review**：cutover evidence intake repeat。结果：未改文件、未执行 commit/cutover/DB write、未读 artifact；`.tmp\billing-ledger\live-commit-proof-artifact.json` 存在但未读，cutover/live-probe artifacts 不存在；watcher/final audit blocked/final=false。剩余缺口：真实 external runtime writer commit runner、actual source-of-truth switch/readback、rollback proof。
- **E9-004-S55 Cutover Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Pauli，no-op review**：cutover evidence intake repeat。结果：未改文件、未执行 commit/cutover/DB write、未读 artifact；`.tmp\billing-ledger\live-commit-proof-artifact.json` 存在但未读，cutover/live-probe artifacts 不存在；watcher/final audit blocked/final=false。剩余缺口：真实 external runtime writer commit runner、actual source-of-truth switch/readback、rollback proof。
- **E9-004-S56 Cutover Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Pauli，no-op review**：cutover evidence intake repeat。结果：未改文件、未执行 commit/cutover/DB write、未读 artifact；`.tmp\billing-ledger\live-commit-proof-artifact.json` 存在但未读，cutover/live-probe artifacts 不存在；`readiness=unavailable`，watcher/final audit blocked，`final_x_eligible=false`。剩余缺口：真实 external runtime writer commit runner、actual source-of-truth switch/readback、rollback proof。
- **E9-004-S57 Cutover Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Pauli，no-op review**：cutover evidence intake repeat。结果：未改文件、未执行 commit/cutover/DB write、未读 artifact；`.tmp\billing-ledger\live-commit-proof-artifact.json` 存在但未读，cutover/live-probe artifacts 不存在；`readiness=unavailable`，watcher/final audit blocked，`final_x_eligible=false`。剩余缺口：真实 external runtime writer commit runner、actual source-of-truth switch/readback、rollback proof。
- **E9-004-S58 Cutover Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Pauli，no-op review**：cutover evidence intake repeat。结果：未改文件、未执行 commit/cutover/DB write、未读 artifact；`.tmp\billing-ledger\live-commit-proof-artifact.json` 存在但未读，cutover/live-probe artifacts 不存在；`readiness=unavailable`，watcher/final audit blocked，`final_x_eligible=false`。剩余缺口：真实 external runtime writer commit runner、actual source-of-truth switch/readback、rollback proof。
- **E9-004-S59 Cutover Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Pauli，no-op review**：cutover evidence intake repeat。结果：未改文件、未执行 commit/cutover/DB write、未读 artifact；`.tmp\billing-ledger\live-commit-proof-artifact.json` 存在但未读，cutover/live-probe artifacts 不存在；`readiness=unavailable`，watcher/final audit blocked，`final_x_eligible=false`。剩余缺口：真实 external runtime writer commit runner、actual source-of-truth switch/readback、rollback proof。
- **E9-004-S60 Cutover Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Pauli，no-op review**：cutover evidence intake repeat。结果：未改文件、未执行 commit/cutover/DB write、未读 artifact；`.tmp\billing-ledger\live-commit-proof-artifact.json` 存在但未读，cutover/live-probe artifacts 不存在；`readiness=unavailable`，watcher/final audit blocked，`final_x_eligible=false`。剩余缺口：真实 external runtime writer commit runner、actual source-of-truth switch/readback、rollback proof。
- **E9-004-S61 Cutover Closure Attempt，已改派，2026-06-05，owner Pauli**：主动运行/验证 live commit proof/cutover evidence readback path，确认真实 external runtime writer commit runner、source-of-truth cutover/readback、rollback proof 是否可执行；优先使用既有 readiness 脚本和 artifacts，不新增 schema/contract，不执行 actual cutover/DB write 除非全部安全 opt-in gate 满足。验收：回报实际命令、exit/classification、live commit/cutover/live-probe artifact 状态、`commit_proof_read`、`cutover_artifact_found`、`rollback_proof_found`、`final_x_eligible` 与 next exact command。
- **E9-004-S62 Post-Env Cutover Closure Reattempt，已完成/阻塞收束，2026-06-05，owner 主线程代跑/Pauli lane**：用户确认环境已补齐后重新尝试 E9 closure。执行命令：`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1 -ReadLiveCommitProofArtifact -LiveCommitProofArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json -ReadCutoverEvidenceArtifact -CutoverEvidenceArtifactPath .tmp/billing-ledger/cutover-evidence-artifact.json -BlockedExitCode 0`。结果：`pwsh`、`psql` 已在 PATH；当前 Codex 进程内 `BILLING_LEDGER_LIVE_DATABASE_URL`、`AI_CONTROL_PLANE_BILLING_LEDGER_WRITER_CUTOVER_MODE`、runtime writer/schema/tool/readback/cutover env 均 missing；commit proof artifact read_performed=true，但 `runtime_writer_commit_runner_artifact_handoff=blocker`、`live_commit_proof_readback=blocker`；`.tmp\billing-ledger\cutover-evidence-artifact.json` 与 `.tmp\billing-ledger\live-probe-evidence-artifact.json` 缺失；final audit `classification=blocked`、`final_x_eligible=false`。未改源码、未写 DB、未 commit、未 cutover。下一条最短 operator command：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1 -ReadCutoverEvidenceArtifact -CutoverEvidenceArtifactPath .tmp/billing-ledger/cutover-evidence-artifact.json`。
- **E9-004-S63 Env Visibility And Cutover Artifact Reattempt，已完成/阻塞收束，2026-06-05，owner 主线程代跑/Pauli lane**：按用户提供 dev DB env 在命令进程内显式注入 `BILLING_LEDGER_LIVE_DATABASE_URL` presence、`AI_CONTROL_PLANE_BILLING_LEDGER_WRITER_CUTOVER_MODE=ready`、runtime writer/schema/tool marker 与 commit/cutover/probe artifact readback paths；未输出原始 DB URL。执行 existing readiness/final audit readback gate，输出保存在 `.tmp\billing-ledger\s63-final-audit-readback.json`。结果：`readiness=ready`、`live_opt_in=true`、`live_database_url.present=true`、`mode=ready`、`runtime_writer_feature.available=true`、`evidence_matrix=pass`、`dry_run_execution=ready`，env visibility blocker 已消除；`live_commit_proof` read_performed=true，但 `runtime_writer_commit_runner_artifact_handoff=blocker`、`live_commit_proof_readback=blocker`；cutover artifact read_requested=true/read_performed=false/source=`missing_artifact`；rollback probe read_requested=true/read_performed=false/reason=`artifact_file_missing`；final audit `classification=blocked`、`final_x_eligible=false`，blocking reasons 为 external commit proof missing/readback not passed、cutover evidence missing、actual cutover not executed、post-cutover readback missing、rollback proof missing、no-dual proof failed、active writer/source-of-truth mismatch。未 build、未改源码、未写 DB、未 commit、未 cutover。
- **E9-004-S64 External Commit/Cutover Artifact Intake，已完成/阻塞收束，2026-06-05，owner 主线程代跑/Pauli lane**：只读检查真实 evidence intake。结果：`.tmp\billing-ledger\live-commit-proof-artifact.json` 存在，schema 为 `control_plane_billing_ledger_live_commit_proof_artifact.v1`，但 `runtime_container_commit=missing`，无 `artifact_provenance`、无 external runner provenance、无 `commit_proof_row_counts`、无 `no_dual_result`、无 `secret_safe_omission`，因此不是 accepted external runtime writer commit runner proof；`.tmp\billing-ledger\cutover-evidence-artifact.json` 与 `.tmp\billing-ledger\live-probe-evidence-artifact.json` 均缺失。运行 existing readback/final audit gate，输出保存在 `.tmp\billing-ledger\s64-final-audit-readback.json`：`readiness=ready`、`evidence_matrix=pass`、`dry_run_execution=ready`、commit artifact read=true，但 `commit_handoff=blocker`、`commit_readback=blocker`、cutover artifact source=`missing_artifact`、probe artifact reason=`artifact_file_missing`、post-cutover readback=false、rollback proof=false、no-dual=false、first blocker=`external_commit_proof_missing`、`final_x_eligible=false`。未 build、未改源码、未写 DB、未 commit、未 cutover。
- **E9-004-S65 External Commit Proof Producer Handoff，已完成/阻塞收束，2026-06-05，owner 主线程代跑/Pauli lane**：只读现有 readiness script/runbook/fixture，未 build、未改源码、未写 DB、未 commit、未 cutover。结论：repo 内没有真实 external runtime writer commit runner 或 operator producer 能产出 accepted `.tmp\billing-ledger\live-commit-proof-artifact.json`；`scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1` 的 `-PlanRuntimeWriterCommitRunner` 只产生 command boundary，`-RunRuntimeWriterCommitRunner` 会添加 `runtime_writer_commit_runner_external_execution_required` blocker，`-WriteLiveCommitProofArtifact` 是 template-only，self-test artifact 只验证 acceptance gate，不能替代真实 external proof。Accepted live commit proof artifact 要求 `schema_version=control_plane_billing_ledger_live_commit_proof_artifact.v1`、`current_commit`、`runtime_container_commit`、`measurement_source=external_controlled_runtime_writer_commit`、`generated_by_this_script=false`、`simulated=false`、`live_commit_readback/active_writer/source_of_truth= billing_ledger_runtime_writer`、`single_active_writer_count=1`、row counts for `insert_runtime_writer_commit_ledger_entry` and `mark_runtime_writer_commit_idempotency`、`commit_proof.runtime_writer_commit_observed=true`、`runner_provenance.artifact_origin=external_runtime_writer_commit_runner` and `generated_by_external_runner=true`、`no_dual_commit_proof` pass、secret-safe omission。最小缺失：repo-bounded external commit proof producer script/API、真实 runtime writer commit invocation boundary、row-count/readback/no-dual capture、accepted artifact writer。Cutover artifact 与 rollback proof 仍不能由 simulation/template 代替，`final_x_eligible` 预计仍 false。
- **E9-004-S66 Minimal External Commit Proof Producer Boundary，已完成/阻塞收束，2026-06-05，owner 主线程实现/Pauli lane**：新增 `scripts/operator/e9_billing_ledger_external_commit_proof_producer.ps1`。结果：helper 默认 `safe_dry_run`，输出 `control_plane_billing_ledger_external_commit_proof_producer_boundary.v1`、expected artifact schema/required fields、required env markers、repo-bounded `.tmp/**/*.json` path gate、action plan、readback command 与 secret-safe output policy；默认 `default_db_write=false`、`script_performs_cutover=false`、`script_generates_accepted_evidence=false`，不写 DB、不生成 accepted evidence、不执行 production cutover。Explicit live path 要求 `-LiveCommitOptIn -ExecuteExternalRunner`、live DB presence、runtime writer/schema/tool markers、operation scope、idempotency key、artifact path、runtime commit、active/source/readback writer markers、no-dual/readback opt-in；只有全部满足才调用 operator-supplied `AI_CONTROL_PLANE_BILLING_LEDGER_EXTERNAL_COMMIT_RUNNER_COMMAND`，stdout/stderr 临时重定向后删除，不回显 raw command output；artifact 必须由 external runner 写入 `.tmp\billing-ledger\live-commit-proof-artifact.json`，helper 不伪造 `generated_by_this_script=false` evidence。验收：PowerShell parse 通过；safe dry-run 返回 `classification=dry_run_ready`、`runner_executed=false`；注入 dev DB/runtime markers 但不提供 `-ExecuteExternalRunner` 时返回 `classification=blocked`、first blocker=`execute_external_runner_opt_in_missing`。未 build、未写 DB、未 commit、未 cutover。剩余：真实 runtime writer commit invocation adapter、accepted commit artifact、cutover artifact、post-cutover readback、rollback proof。
- **E9-004-S67 Runtime Writer Commit Invocation Adapter，已完成/阻塞收束，2026-06-05，owner 主线程实现/Pauli lane**：把 S66 producer boundary 接到真实 billing-ledger runtime writer commit invocation 的可调用性检查。结果：新增 `scripts/operator/e9_billing_ledger_runtime_writer_commit_adapter.ps1`；定位到 repo 内已有 `crates/billing-ledger/src/postgres_execution.rs:809` 的 `execute_consistent_ledger_postgres_sqlx_writer_plan` Rust SQLx writer function，也有 Control Plane `/admin/ledger/adjustments/dry-run` -> `execute_ledger_adjustment` 本地 execute 路径，但二者都还不是可由 S66 external runner command 直接调用、执行 bounded paid ledger commit、并写入 accepted `.tmp\billing-ledger\live-commit-proof-artifact.json` 的 operator CLI/API。Adapter 默认 `dry_run_refusal_ready`，输出 detected capabilities、required env markers、repo-bounded artifact path gate、expected artifact schema、minimal missing boundary、S66 command shape 与 readback command；默认不写 DB、不 cutover、不生成 accepted/simulated evidence。验收：PowerShell parse 通过；safe dry-run 返回 `default_db_write=false`、`script_performs_cutover=false`、`accepted_artifact_written=false`、`final_x_eligible=false`；显式注入 dev DB/runtime writer/schema/tool/readback markers 后 live-opt-in refusal 首个 blocker 收敛为 `callable_runtime_writer_boundary_missing`。未 build、未写 DB、未 commit、未 cutover。剩余：repo-bounded callable runtime writer boundary、真实 accepted commit artifact、cutover artifact、post-cutover readback、rollback proof。
- **E9-004-S68 Callable Runtime Writer Boundary Implementation，已完成/阻塞收束，2026-06-05，owner 主线程实现/Pauli lane**：实现最小 repo-bounded callable runtime writer boundary。结果：`crates/billing-ledger/Cargo.toml` 新增 bin `billing-ledger-runtime-writer-commit`（required feature `postgres-sqlx`），新增 `crates/billing-ledger/src/bin/runtime_writer_commit.rs`，live 路径以 reserve paid ledger commit input 构造 writer plan/command plan/postgres execution plan，并调用 `execute_consistent_ledger_postgres_sqlx_writer_plan`；成功 commit 后写 accepted `.tmp\billing-ledger\live-commit-proof-artifact.json`，覆盖 `measurement_source=external_controlled_runtime_writer_commit`、`generated_by_this_script=false`、`simulated=false`、row counts、`commit_proof.runtime_writer_commit_observed`、external runner provenance、no-dual proof 与 secret-safe omission。新增 `scripts/operator/e9_billing_ledger_callable_runtime_writer_boundary.ps1` 作为 operator gate，默认 `dry_run_refusal_ready`，输出 command shape、required env markers、action plan、expected input/artifact schema、repo-bounded path gate、first blocker；只有 `-LiveCommitOptIn -ExecuteLiveCommit`、live DB env、runtime writer/schema/tool markers、operation scope、idempotency key、current/runtime commit、active/source/readback writer marker、no-dual/readback opt-in 与 repo-bounded input/artifact path 全齐时才调用 bin；wrapper 不回显 DB URL/raw env/raw runner output，不执行 production source-of-truth cutover。验收：PowerShell parse 通过；safe dry-run 返回 `default_db_write=false`、`script_performs_cutover=false`、`final_x_eligible=false`；注入 dev DB/runtime markers 但不加 `-ExecuteLiveCommit` 时首个 blocker=`execute_live_commit_opt_in_missing`、runner not executed；`cargo fmt -p ai-gateway-billing-ledger --check` 与 scoped diff check 通过。未 build、未写 DB、未 commit、未 cutover、未伪造 evidence。剩余：真实 live paid commit opt-in run、accepted artifact readback pass、cutover artifact、post-cutover readback、rollback proof。
- **E9-004-S69 Callable Boundary Fixture And Readback Preflight，已完成/阻塞收束，2026-06-05，owner 主线程实现/Pauli lane**：补最小 repo-bounded reserve commit input fixture/runbook 与 no-build acceptance preflight。结果：新增 `tests/fixtures/billing-ledger/runtime_writer_commit_input.reserve_paid_minimal.json`，覆盖 reserve paid ledger commit shape：tenant/project/virtual key/user/wallet/request/source/amount/currency/available balance/budget/operation scope/artifact path/secret-safe contract；新增 `docs/todo/slices/E9-004-S69-callable-boundary-preflight.md`，固定 operator required env markers、`.tmp/billing-ledger/runtime-writer-commit-input.json` input path、`.tmp/billing-ledger/live-commit-proof-artifact.json` artifact path、一次性 command shape、expected first blocker=`execute_live_commit_opt_in_missing` 与 readback command。同步修正 S68 bin 的 artifact `generated_at_utc` 为 RFC3339/ISO UTC formatter，避免 future readback freshness 因 `unix_ms:*` 格式失败。验收：fixture JSON parse 通过；PowerShell wrapper parse 通过；safe dry-run 返回 `classification=dry_run_refusal_ready`、first blocker=`live_commit_opt_in_missing`；命令内注入 dev DB/runtime writer/schema/tool/no-dual/readback markers 但不加 `-ExecuteLiveCommit` 时 first blocker=`execute_live_commit_opt_in_missing`、runner not executed、artifact not written；readback-only gate 指向 `.tmp/billing-ledger/s69-live-commit-proof-artifact.json` 返回 artifact missing/blocker；`Test-Path` 确认 S69 artifact 不存在；`cargo fmt -p ai-gateway-billing-ledger --check` 通过。未 build、未写 DB、未执行 live paid commit、未 cutover、未伪造 evidence。剩余：显式授权 live paid commit rehearsal、accepted artifact readback pass、cutover artifact、post-cutover readback、rollback proof。
- **E9-004-S70 Explicit Live Paid Commit Rehearsal Readback，已完成/授权前预检收束，2026-06-05，owner 主线程代跑/Pauli lane**：用户尚未再次显式授权 live paid commit，因此本片按非写入预检/等待模式收束。结果：未运行 `-ExecuteLiveCommit`；未写 DB、未生成 accepted artifact、未 production cutover。已将 S69 fixture shape 复制到 `.tmp/billing-ledger/runtime-writer-commit-input.json`，并验证 input shape 覆盖 tenant/project/user/wallet/request/source/amount/currency/operation scope/artifact path；presence-only env 检查只输出 env present/value omitted，当前进程未继承 `DATABASE_URL`、`BILLING_LEDGER_LIVE_DATABASE_URL`、runtime writer/schema/tool/no-dual/readback/input/artifact env markers。执行 wrapper 命令：`scripts/operator/e9_billing_ledger_callable_runtime_writer_boundary.ps1 -LiveCommitOptIn -RuntimeWriterAvailable -RuntimeSchemaAvailable -RuntimeToolAvailable -NoDualReadbackOptIn -CommitReadbackOptIn -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/s70-live-commit-proof-artifact.json ...`，结果 `classification=dry_run_refusal_ready`、first blocker=`execute_live_commit_opt_in_missing`、runner requested=false/executed=false、artifact exists=false、input/artifact path gates safe。`Test-Path .tmp\billing-ledger\s70-live-commit-proof-artifact.json` 返回 false。Readback-only gate 指向 S70 artifact 返回 `runtime_writer_commit_runner_artifact_handoff.classification=blocker`、`live_commit_proof_artifact_missing`、`cutover_final_closure_audit.final_x_eligible=false`，blocking reasons 包括 external commit proof missing、commit proof readback not passed、cutover evidence missing、actual cutover not executed、post-cutover readback missing、rollback proof missing、no-dual failed、active/source-of-truth mismatch。剩余：用户显式授权后的 live paid commit rehearsal、accepted artifact readback pass、cutover artifact、post-cutover readback、rollback proof。
- **E9-004-S71 Await Explicit Live Paid Commit Authorization / Rollback-Cutover Preflight，已完成/授权等待收束，2026-06-05，owner 主线程代跑/Pauli lane**：等待用户明确授权“一笔 live paid commit rehearsal”，未授权期间只做 rollback/cutover readback preflight。结果：presence-only 授权检查显示 `AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_PAID_COMMIT_AUTHORIZED`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_PAID_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_COMMIT` 均 missing/false；live DB/runtime writer/schema/tool/no-dual/readback env 也未继承；输出只包含 present/truthy/value omitted，不输出 secret。未运行 `-ExecuteLiveCommit`，未写 DB、未 commit、未 cutover、未伪造 evidence。Artifact state：`.tmp/billing-ledger/runtime-writer-commit-input.json` 存在，`.tmp/billing-ledger/live-commit-proof-artifact.json` 存在但 readback preflight 仍为 blocker（generated by script/stale/rollback proof not fresh），`.tmp/billing-ledger/s70-live-commit-proof-artifact.json` 不存在，`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` 不存在。运行 readback preflight：`runtime_writer_commit_runner_artifact_handoff.classification=blocker`、`live_commit_proof_readback=blocker/null`、cutover evidence `artifact_source=missing_artifact`、cutover final audit `final_x_eligible=false`；remaining blockers 为 external commit proof missing、commit proof readback not passed、cutover evidence missing、actual cutover not executed、post-cutover readback missing、rollback proof missing、no-dual proof failed、active writer/source-of-truth mismatch。最短授权后执行命令：准备 `.tmp/billing-ledger/runtime-writer-commit-input.json`，设置 required live DB/runtime markers，运行 `scripts/operator/e9_billing_ledger_callable_runtime_writer_boundary.ps1 -LiveCommitOptIn -ExecuteLiveCommit ... -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json`，随后运行 `scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1 -ReadLiveCommitProofArtifact -LiveCommitProofArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json -SingleWriterCutoverProof -LiveCommitProof`。
- **E9-004-S72 Explicit Live Paid Commit Authorization Wait / Artifact Readback，已完成/授权等待收束，2026-06-05，owner 主线程代跑/Pauli lane**：继续等待用户明确授权“一笔 live paid commit rehearsal”。结果：presence-only 授权检查显示 `AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_PAID_COMMIT_AUTHORIZED`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_PAID_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_COMMIT` 均 missing/false；live DB/runtime writer/schema/tool/no-dual/readback env 也 missing；输出只包含 presence/truthy/value omitted，不输出 secret。因未授权，未运行 `-ExecuteLiveCommit`，未写 DB、未 commit、未 cutover、未生成 accepted artifact。Artifact state：`.tmp/billing-ledger/runtime-writer-commit-input.json` 存在；`.tmp/billing-ledger/live-commit-proof-artifact.json` 存在但 readback watcher 分类为 blocker（not accepted external/current proof）；cutover/live-probe artifacts 缺失。Readback-only watcher 输出：`runtime_writer_commit_runner_artifact_handoff.classification=blocker`、`live_commit_proof_readback=blocker/null`、cutover evidence `artifact_source=missing_artifact`、cutover final audit `final_x_eligible=false`；remaining blockers 为 external commit proof missing/readback not passed、cutover evidence missing、actual cutover not executed、post-cutover readback missing、rollback proof missing、no-dual failed、active/source-of-truth mismatch。最短授权后命令：设置 required live DB/runtime markers，运行 S68 wrapper `-LiveCommitOptIn -ExecuteLiveCommit -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json ...`，随后运行 readiness readback gate。
- **E9-004-S73 Explicit Live Paid Commit Authorization Watcher，已完成/授权等待收束，2026-06-05，owner 主线程代跑/Pauli lane**：低频 watcher。结果：presence-only 授权检查显示 `AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_PAID_COMMIT_AUTHORIZED`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_PAID_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_COMMIT` 均 missing/false；未输出 secret/raw DB URL。未运行 `-ExecuteLiveCommit`，未写 DB、未 commit、未 cutover、未生成 accepted artifact。Artifact state：`.tmp/billing-ledger/runtime-writer-commit-input.json` 存在；`.tmp/billing-ledger/live-commit-proof-artifact.json` 存在但 readback watcher 仍分类 blocker（not accepted external/current proof）；cutover/live-probe artifacts 缺失。Readback-only gate 输出：`runtime_writer_commit_runner_artifact_handoff.classification=blocker`、`live_commit_proof_readback=blocker/null`、cutover evidence `artifact_source=missing_artifact`、rollback proof missing/no-dual failed、cutover final audit `final_x_eligible=false`；remaining blockers 为 external commit proof missing/readback not passed、cutover evidence missing、actual cutover not executed、post-cutover readback missing、rollback proof missing、no-dual failed、active/source-of-truth mismatch。授权后最短命令仍为 S68 wrapper `-LiveCommitOptIn -ExecuteLiveCommit -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json ...`，随后运行 readiness readback gate。
- **E9-004-S74 Explicit Live Paid Commit Authorization Low-Frequency Watcher，已完成/授权等待收束，2026-06-05，owner 主线程代跑/Pauli lane**：低频 watcher。结果：presence-only 授权检查显示 `AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_PAID_COMMIT_AUTHORIZED`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_PAID_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_COMMIT` 均 missing/false；未输出 secret/raw DB URL。未运行 `-ExecuteLiveCommit`，未写 DB、未 commit、未 cutover、未生成 accepted artifact。Artifact state：`.tmp/billing-ledger/runtime-writer-commit-input.json` 存在；`.tmp/billing-ledger/live-commit-proof-artifact.json` 存在但 readback watcher 仍分类 blocker（not accepted external/current proof）；cutover/live-probe artifacts 缺失。Readback-only gate 输出：`runtime_writer_commit_runner_artifact_handoff.classification=blocker`、`live_commit_proof_readback=blocker/null`、cutover evidence `artifact_source=missing_artifact`、rollback proof missing/no-dual failed、cutover final audit `final_x_eligible=false`；remaining blockers 为 external commit proof missing/readback not passed、cutover evidence missing、actual cutover not executed、post-cutover readback missing、rollback proof missing、no-dual failed、active/source-of-truth mismatch。授权后最短命令仍为 S68 wrapper `-LiveCommitOptIn -ExecuteLiveCommit -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json ...`，随后运行 readiness readback gate。
- **E9-004-S75 Explicit Live Paid Commit Authorization Low-Frequency Watcher，已完成/授权等待收束，2026-06-05，owner 主线程代跑/Pauli lane**：低频 watcher。结果：presence-only 授权检查显示 `AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_PAID_COMMIT_AUTHORIZED`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_PAID_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_COMMIT` 均 missing/false；未输出 secret/raw DB URL。未运行 `-ExecuteLiveCommit`，未写 DB、未 commit、未 cutover、未生成 accepted artifact。Artifact state：`.tmp/billing-ledger/runtime-writer-commit-input.json` 存在；`.tmp/billing-ledger/live-commit-proof-artifact.json` 存在但 readback watcher 仍分类 blocker（not accepted external/current proof）；cutover/live-probe artifacts 缺失。Readback-only gate 输出：`runtime_writer_commit_runner_artifact_handoff.classification=blocker`、`live_commit_proof_readback=blocker/null`、cutover evidence `artifact_source=missing_artifact`、rollback proof missing/no-dual failed、cutover final audit `final_x_eligible=false`；remaining blockers 为 external commit proof missing/readback not passed、cutover evidence missing、actual cutover not executed、post-cutover readback missing、rollback proof missing、no-dual failed、active/source-of-truth mismatch。授权后最短命令仍为 S68 wrapper `-LiveCommitOptIn -ExecuteLiveCommit -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json ...`，随后运行 readiness readback gate。
- **E9-004-S76 Explicit Live Paid Commit Authorization Low-Frequency Watcher，已完成/授权等待收束，2026-06-05，owner 主线程代跑/Pauli lane**：低频 watcher。结果：presence-only 授权检查显示 `AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_PAID_COMMIT_AUTHORIZED`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_PAID_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_COMMIT` 均 missing/false；未输出 secret/raw DB URL。未运行 `-ExecuteLiveCommit`，未写 DB、未 commit、未 cutover、未生成 accepted artifact。Artifact state：`.tmp/billing-ledger/runtime-writer-commit-input.json` 存在；`.tmp/billing-ledger/live-commit-proof-artifact.json` 存在但 readback watcher 仍分类 blocker（not accepted external/current proof）；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` 缺失。Readback-only gate 输出：readiness=`unavailable`、`runtime_writer_commit_runner_artifact_handoff.classification=blocker`、`live_commit_proof_readback=blocker/null`、cutover evidence `artifact_source=missing_artifact`、rollback proof missing/no-dual failed、cutover final audit `classification=blocked` 且 `final_x_eligible=false`；remaining blockers 为 external commit proof missing/readback not passed、cutover evidence missing、actual cutover not executed、post-cutover readback missing、rollback proof missing、no-dual failed、active/source-of-truth mismatch。授权后最短命令仍为 S68 wrapper `-LiveCommitOptIn -ExecuteLiveCommit -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json ...`，随后运行 readiness readback gate。
- **E9-004-S77 Explicit Live Paid Commit Authorization Low-Frequency Watcher，已完成/授权等待收束，2026-06-05，owner 主线程代跑/Pauli lane**：低频 watcher。结果：presence-only 授权检查显示 `AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_PAID_COMMIT_AUTHORIZED`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_PAID_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_COMMIT` 均 missing/false；未输出 secret/raw DB URL。未运行 `-ExecuteLiveCommit`，未写 DB、未 commit、未 cutover、未生成 accepted artifact。Artifact state：`.tmp/billing-ledger/runtime-writer-commit-input.json` 存在；`.tmp/billing-ledger/live-commit-proof-artifact.json` 存在但 readback watcher 仍分类 blocker（not accepted external/current proof）；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` 缺失。Readback-only gate 输出：readiness=`unavailable`、`runtime_writer_commit_runner_artifact_handoff.classification=blocker`、`live_commit_proof_readback=blocker/null`、cutover evidence `artifact_source=missing_artifact`、rollback proof missing/no-dual failed、cutover final audit `classification=blocked` 且 `final_x_eligible=false`。First blockers：commit handoff 为 `runtime_writer_commit_runner_not_planned`、`runtime_writer_commit_runner_not_requested`、`cutover_mode_not_ready`、`live_database_url_missing`、`runtime_writer_feature_unavailable`；final audit 为 `external_commit_proof_missing`、`commit_proof_readback_not_passed`、`cutover_evidence_not_accepted`、`cutover_evidence_artifact_missing`、`actual_cutover_not_executed`。授权后最短命令仍为 S68 wrapper `-LiveCommitOptIn -ExecuteLiveCommit -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json ...`，随后运行 readiness readback gate。
- **E9-004-S78 Explicit Live Paid Commit Authorization Low-Frequency Watcher，已完成/授权等待收束，2026-06-05，owner 主线程代跑/Pauli lane**：低频 watcher。结果：presence-only 授权与 runtime marker 检查显示 `AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_PAID_COMMIT_AUTHORIZED`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_PAID_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_COMMIT` 均 missing/false；`BILLING_LEDGER_LIVE_DATABASE_URL`、`DATABASE_URL`、`POSTGRES_URL`、runtime writer/schema/tool、no-dual/readback opt-in markers 也均 missing/false；未输出 secret/raw DB URL。未运行 `-ExecuteLiveCommit`，未写 DB、未 commit、未 cutover、未生成 accepted artifact。Artifact state：`.tmp/billing-ledger/runtime-writer-commit-input.json` 存在；`.tmp/billing-ledger/live-commit-proof-artifact.json` 存在但 readback watcher 仍分类 blocker（not accepted external/current proof）；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` 缺失。Readback-only gate 输出：readiness=`unavailable`、`runtime_writer_commit_runner_artifact_handoff.classification=blocker`、`live_commit_proof_readback=blocker/null`、cutover evidence `artifact_source=missing_artifact`、rollback proof missing/no-dual failed、cutover final audit `classification=blocked` 且 `final_x_eligible=false`。First blockers：commit handoff 为 `runtime_writer_commit_runner_not_planned`、`runtime_writer_commit_runner_not_requested`、`cutover_mode_not_ready`、`live_database_url_missing`、`runtime_writer_feature_unavailable`；final audit 为 `external_commit_proof_missing`、`commit_proof_readback_not_passed`、`cutover_evidence_not_accepted`、`cutover_evidence_artifact_missing`、`actual_cutover_not_executed`。授权后最短命令仍为 S68 wrapper `-LiveCommitOptIn -ExecuteLiveCommit -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json ...`，随后运行 readiness readback gate。
- **E9-004-S79 Explicit Live Paid Commit Authorization Low-Frequency Watcher，已完成/授权等待收束，2026-06-05，owner 主线程代跑/Pauli lane**：低频 watcher。结果：presence-only 授权与 runtime marker 检查显示 `AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_PAID_COMMIT_AUTHORIZED`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_PAID_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_COMMIT` 均 missing/false；`BILLING_LEDGER_LIVE_DATABASE_URL`、`DATABASE_URL`、`POSTGRES_URL`、runtime writer/schema/tool、no-dual/readback opt-in markers 也均 missing/false；未输出 secret/raw DB URL。未运行 `-ExecuteLiveCommit`，未写 DB、未 commit、未 cutover、未生成 accepted artifact。Artifact state：`.tmp/billing-ledger/runtime-writer-commit-input.json` 存在；`.tmp/billing-ledger/live-commit-proof-artifact.json` 存在但 readback watcher 仍分类 blocker（not accepted external/current proof）；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` 缺失。Readback-only gate 输出：readiness=`unavailable`、`runtime_writer_commit_runner_artifact_handoff.classification=blocker`、`live_commit_proof_readback=blocker/null`、cutover evidence `artifact_source=missing_artifact`、rollback proof missing/no-dual failed、cutover final audit `classification=blocked` 且 `final_x_eligible=false`。First blockers：commit handoff 为 `runtime_writer_commit_runner_not_planned`、`runtime_writer_commit_runner_not_requested`、`cutover_mode_not_ready`、`live_database_url_missing`、`runtime_writer_feature_unavailable`；final audit 为 `external_commit_proof_missing`、`commit_proof_readback_not_passed`、`cutover_evidence_not_accepted`、`cutover_evidence_artifact_missing`、`actual_cutover_not_executed`。授权后最短命令仍为 S68 wrapper `-LiveCommitOptIn -ExecuteLiveCommit -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json ...`，随后运行 readiness readback gate。
- **E9-004-S80 Explicit Live Paid Commit Authorization Low-Frequency Watcher，已完成/授权等待收束，2026-06-05，owner 主线程代跑/Pauli lane**：低频 watcher。结果：presence-only 授权与 runtime marker 检查显示 `AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_PAID_COMMIT_AUTHORIZED`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_PAID_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_COMMIT` 均 missing/false；`BILLING_LEDGER_LIVE_DATABASE_URL`、`DATABASE_URL`、`POSTGRES_URL`、runtime writer/schema/tool、no-dual/readback opt-in markers 也均 missing/false；未输出 secret/raw DB URL。未运行 `-ExecuteLiveCommit`，未写 DB、未 commit、未 cutover、未生成 accepted artifact。Artifact state：`.tmp/billing-ledger/runtime-writer-commit-input.json` 存在；`.tmp/billing-ledger/live-commit-proof-artifact.json` 存在但 readback watcher 仍分类 blocker（not accepted external/current proof）；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` 缺失。Readback-only gate 输出：readiness=`unavailable`、`runtime_writer_commit_runner_artifact_handoff.classification=blocker`、`live_commit_proof_readback=blocker/null`、cutover evidence `artifact_source=missing_artifact`、rollback proof missing/no-dual failed、cutover final audit `classification=blocked` 且 `final_x_eligible=false`。First blockers：commit handoff 为 `runtime_writer_commit_runner_not_planned`、`runtime_writer_commit_runner_not_requested`、`cutover_mode_not_ready`、`live_database_url_missing`、`runtime_writer_feature_unavailable`；final audit 为 `external_commit_proof_missing`、`commit_proof_readback_not_passed`、`cutover_evidence_not_accepted`、`cutover_evidence_artifact_missing`、`actual_cutover_not_executed`。授权后最短命令仍为 S68 wrapper `-LiveCommitOptIn -ExecuteLiveCommit -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json ...`，随后运行 readiness readback gate。
- **E9-004-S81 Explicit Live Paid Commit Authorization Low-Frequency Watcher，已完成/授权等待收束，2026-06-05，owner 主线程代跑/Pauli lane**：低频 watcher。结果：presence-only 授权与 runtime marker 检查显示 `AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_PAID_COMMIT_AUTHORIZED`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_PAID_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_COMMIT` 均 missing/false；`BILLING_LEDGER_LIVE_DATABASE_URL`、`DATABASE_URL`、`POSTGRES_URL`、runtime writer/schema/tool、no-dual/readback opt-in markers 也均 missing/false；未输出 secret/raw DB URL。未运行 `-ExecuteLiveCommit`，未写 DB、未 commit、未 cutover、未生成 accepted artifact。Artifact state：`.tmp/billing-ledger/runtime-writer-commit-input.json` 存在；`.tmp/billing-ledger/live-commit-proof-artifact.json` 存在但 readback watcher 仍分类 blocker（not accepted external/current proof）；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` 缺失。Readback-only gate 输出：readiness=`unavailable`、`runtime_writer_commit_runner_artifact_handoff.classification=blocker`、`live_commit_proof_readback=blocker/null`、cutover evidence `artifact_source=missing_artifact`、rollback proof missing/no-dual failed、cutover final audit `classification=blocked` 且 `final_x_eligible=false`。First blockers：commit handoff 为 `runtime_writer_commit_runner_not_planned`、`runtime_writer_commit_runner_not_requested`、`cutover_mode_not_ready`、`live_database_url_missing`、`runtime_writer_feature_unavailable`；final audit 为 `external_commit_proof_missing`、`commit_proof_readback_not_passed`、`cutover_evidence_not_accepted`、`cutover_evidence_artifact_missing`、`actual_cutover_not_executed`。授权后最短命令仍为 S68 wrapper `-LiveCommitOptIn -ExecuteLiveCommit -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json ...`，随后运行 readiness readback gate。
- **E9-004-S82 Explicit Live Paid Commit Authorization Low-Frequency Watcher，已完成/授权等待收束，2026-06-05，owner 主线程代跑/Pauli lane**：低频 watcher。结果：presence-only 授权与 runtime marker 检查显示 `AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_PAID_COMMIT_AUTHORIZED`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_PAID_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_COMMIT` 均 missing/false；`BILLING_LEDGER_LIVE_DATABASE_URL`、`DATABASE_URL`、`POSTGRES_URL`、runtime writer/schema/tool、no-dual/readback opt-in markers 也均 missing/false；未输出 secret/raw DB URL。未运行 `-ExecuteLiveCommit`，未写 DB、未 commit、未 cutover、未生成 accepted artifact。Artifact state：`.tmp/billing-ledger/runtime-writer-commit-input.json` 存在；`.tmp/billing-ledger/live-commit-proof-artifact.json` 存在但 readback watcher 仍分类 blocker（not accepted external/current proof）；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` 缺失。Readback-only gate 输出：readiness=`unavailable`、`runtime_writer_commit_runner_artifact_handoff.classification=blocker`、`live_commit_proof_readback=blocker/null`、cutover evidence `artifact_source=missing_artifact`、rollback proof missing/no-dual failed、cutover final audit `classification=blocked` 且 `final_x_eligible=false`。First blockers：commit handoff 为 `runtime_writer_commit_runner_not_planned`、`runtime_writer_commit_runner_not_requested`、`cutover_mode_not_ready`、`live_database_url_missing`、`runtime_writer_feature_unavailable`；final audit 为 `external_commit_proof_missing`、`commit_proof_readback_not_passed`、`cutover_evidence_not_accepted`、`cutover_evidence_artifact_missing`、`actual_cutover_not_executed`。授权后最短命令仍为 S68 wrapper `-LiveCommitOptIn -ExecuteLiveCommit -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json ...`，随后运行 readiness readback gate。
- **E9-004-S83 Explicit Live Paid Commit Authorization Low-Frequency Watcher，已完成/授权等待收束，2026-06-05，owner 主线程代跑/Pauli lane**：低频 watcher。结果：presence-only 授权与 runtime marker 检查显示 `AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_PAID_COMMIT_AUTHORIZED`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_PAID_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_COMMIT` 均 missing/false；`BILLING_LEDGER_LIVE_DATABASE_URL`、`DATABASE_URL`、`POSTGRES_URL`、runtime writer/schema/tool、no-dual/readback opt-in markers 也均 missing/false；未输出 secret/raw DB URL。未运行 `-ExecuteLiveCommit`，未写 DB、未 commit、未 cutover、未生成 accepted artifact。Artifact state：`.tmp/billing-ledger/runtime-writer-commit-input.json` 存在；`.tmp/billing-ledger/live-commit-proof-artifact.json` 存在但 readback watcher 仍分类 blocker（not accepted external/current proof）；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` 缺失。Readback-only gate 输出：readiness=`unavailable`、`runtime_writer_commit_runner_artifact_handoff.classification=blocker`、`live_commit_proof_readback=blocker/null`、cutover evidence `artifact_source=missing_artifact`、rollback proof missing/no-dual failed、cutover final audit `classification=blocked` 且 `final_x_eligible=false`。First blockers：commit handoff 为 `runtime_writer_commit_runner_not_planned`、`runtime_writer_commit_runner_not_requested`、`cutover_mode_not_ready`、`live_database_url_missing`、`runtime_writer_feature_unavailable`；final audit 为 `external_commit_proof_missing`、`commit_proof_readback_not_passed`、`cutover_evidence_not_accepted`、`cutover_evidence_artifact_missing`、`actual_cutover_not_executed`。授权后最短命令仍为 S68 wrapper `-LiveCommitOptIn -ExecuteLiveCommit -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json ...`，随后运行 readiness readback gate。
- **E9-004-S84 Explicit Live Paid Commit Authorization Low-Frequency Watcher，已完成/授权等待收束，2026-06-05，owner 主线程代跑/Pauli lane**：低频 watcher。结果：presence-only 授权与 runtime marker 检查显示 `AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_PAID_COMMIT_AUTHORIZED`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_PAID_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_COMMIT` 均 missing/false；`BILLING_LEDGER_LIVE_DATABASE_URL`、`DATABASE_URL`、`POSTGRES_URL`、runtime writer/schema/tool、no-dual/readback opt-in markers 也均 missing/false；未输出 secret/raw DB URL。未运行 `-ExecuteLiveCommit`，未写 DB、未 commit、未 cutover、未生成 accepted artifact。Artifact state：`.tmp/billing-ledger/runtime-writer-commit-input.json` 存在；`.tmp/billing-ledger/live-commit-proof-artifact.json` 存在但 readback watcher 仍分类 blocker（not accepted external/current proof）；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` 缺失。Readback-only gate 输出：readiness=`unavailable`、`runtime_writer_commit_runner_artifact_handoff.classification=blocker`、`live_commit_proof_readback=blocker/null`、cutover evidence `artifact_source=missing_artifact`、rollback proof missing/no-dual failed、cutover final audit `classification=blocked` 且 `final_x_eligible=false`。First blockers：commit handoff 为 `runtime_writer_commit_runner_not_planned`、`runtime_writer_commit_runner_not_requested`、`cutover_mode_not_ready`、`live_database_url_missing`、`runtime_writer_feature_unavailable`；final audit 为 `external_commit_proof_missing`、`commit_proof_readback_not_passed`、`cutover_evidence_not_accepted`、`cutover_evidence_artifact_missing`、`actual_cutover_not_executed`。授权后最短命令仍为 S68 wrapper `-LiveCommitOptIn -ExecuteLiveCommit -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json ...`，随后运行 readiness readback gate。
- **E9-004-S85 Explicit Live Paid Commit Authorization Low-Frequency Watcher，已完成/授权等待收束，2026-06-05，owner 主线程代跑/Pauli lane**：低频 watcher。结果：presence-only 授权与 runtime marker 检查显示 `AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_PAID_COMMIT_AUTHORIZED`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_PAID_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_COMMIT` 均 missing/false；`BILLING_LEDGER_LIVE_DATABASE_URL`、`DATABASE_URL`、`POSTGRES_URL`、runtime writer/schema/tool、no-dual/readback opt-in markers 也均 missing/false；未输出 secret/raw DB URL。未运行 `-ExecuteLiveCommit`，未写 DB、未 commit、未 cutover、未生成 accepted artifact。Artifact state：`.tmp/billing-ledger/runtime-writer-commit-input.json` 存在；`.tmp/billing-ledger/live-commit-proof-artifact.json` 存在但 readback watcher 仍分类 blocker（not accepted external/current proof）；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` 缺失。Readback-only gate 输出：readiness=`unavailable`、`runtime_writer_commit_runner_artifact_handoff.classification=blocker`、`live_commit_proof_readback=blocker/null`、cutover evidence `artifact_source=missing_artifact`、rollback proof missing/no-dual failed、cutover final audit `classification=blocked` 且 `final_x_eligible=false`。First blockers：commit handoff 为 `runtime_writer_commit_runner_not_planned`、`runtime_writer_commit_runner_not_requested`、`cutover_mode_not_ready`、`live_database_url_missing`、`runtime_writer_feature_unavailable`；final audit 为 `external_commit_proof_missing`、`commit_proof_readback_not_passed`、`cutover_evidence_not_accepted`、`cutover_evidence_artifact_missing`、`actual_cutover_not_executed`。授权后最短命令仍为 S68 wrapper `-LiveCommitOptIn -ExecuteLiveCommit -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json ...`，随后运行 readiness readback gate。
- **E9-004-S86 Explicit Live Paid Commit Authorization Low-Frequency Watcher，已完成/授权等待收束，2026-06-05，owner 主线程代跑/Pauli lane**：低频 watcher。结果：presence-only 授权与 runtime marker 检查显示 `AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_PAID_COMMIT_AUTHORIZED`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_PAID_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_COMMIT` 均 missing/false；`BILLING_LEDGER_LIVE_DATABASE_URL`、`DATABASE_URL`、`POSTGRES_URL`、runtime writer/schema/tool、no-dual/readback opt-in markers 也均 missing/false；未输出 secret/raw DB URL。未运行 `-ExecuteLiveCommit`，未写 DB、未 commit、未 cutover、未生成 accepted artifact。Artifact state：`.tmp/billing-ledger/runtime-writer-commit-input.json` 存在；`.tmp/billing-ledger/live-commit-proof-artifact.json` 存在但 readback watcher 仍分类 blocker（not accepted external/current proof）；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` 缺失。Readback-only gate 输出：readiness=`unavailable`、`runtime_writer_commit_runner_artifact_handoff.classification=blocker`、`live_commit_proof_readback=blocker/null`、cutover evidence `artifact_source=missing_artifact`、rollback proof missing/no-dual failed、cutover final audit `classification=blocked` 且 `final_x_eligible=false`。First blockers：commit handoff 为 `runtime_writer_commit_runner_not_planned`、`runtime_writer_commit_runner_not_requested`、`cutover_mode_not_ready`、`live_database_url_missing`、`runtime_writer_feature_unavailable`；final audit 为 `external_commit_proof_missing`、`commit_proof_readback_not_passed`、`cutover_evidence_not_accepted`、`cutover_evidence_artifact_missing`、`actual_cutover_not_executed`。授权后最短命令仍为 S68 wrapper `-LiveCommitOptIn -ExecuteLiveCommit -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json ...`，随后运行 readiness readback gate。
- **E9-004-S87 Explicit Live Paid Commit Authorization Low-Frequency Watcher，进行中，2026-06-05，owner Pauli**：继续等待用户明确授权“一笔 live paid commit rehearsal”和同进程 live DB/runtime writer markers。DoD：若授权到位，执行 S68 wrapper `-LiveCommitOptIn -ExecuteLiveCommit`，input path `.tmp/billing-ledger/runtime-writer-commit-input.json`，artifact path `.tmp/billing-ledger/live-commit-proof-artifact.json`，随后 readback accepted artifact；若仍未授权，只做低频 presence/readback-only watcher，不写 DB、不 commit、不 cutover、不生成 accepted artifact。
- **E9-004-S88 Live Paid Commit Authorization Gate Fresh Audit，已完成/阻塞收束，2026-06-05，owner Einstein**：旧 E9 Agent 已 `not_found` 后接手，只做 read-only/fresh audit；未执行真实 paid commit、未 production cutover、未 build、未改源码、未输出 secret。检查过的 env/artifact 名称：`AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_PAID_COMMIT_AUTHORIZED`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_PAID_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_COMMIT`、`BILLING_LEDGER_LIVE_DATABASE_URL`/`DATABASE_URL`/`POSTGRES_URL`、`AI_CONTROL_PLANE_BILLING_LEDGER_WRITER_CUTOVER_MODE`、`AI_CONTROL_PLANE_BILLING_LEDGER_RUNTIME_WRITER_AVAILABLE`、`AI_CONTROL_PLANE_BILLING_LEDGER_RUNTIME_SCHEMA_AVAILABLE`、`AI_CONTROL_PLANE_BILLING_LEDGER_RUNTIME_TOOL_AVAILABLE`、`AI_CONTROL_PLANE_BILLING_LEDGER_RUNTIME_CONTAINER_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_CURRENT_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_SOURCE_OF_TRUTH`、`AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_COMMIT_READBACK`、`AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_COMMIT_PROOF_ARTIFACT_PATH`、`AI_CONTROL_PLANE_BILLING_LEDGER_RUNTIME_WRITER_COMMIT_INPUT_PATH`、`AI_CONTROL_PLANE_BILLING_LEDGER_NO_DUAL_READBACK_OPT_IN`、`AI_CONTROL_PLANE_BILLING_LEDGER_COMMIT_READBACK_OPT_IN`、`AI_CONTROL_PLANE_BILLING_LEDGER_CUTOVER_EVIDENCE_ARTIFACT_PATH`、`AI_CONTROL_PLANE_BILLING_LEDGER_CUTOVER_EVIDENCE_ARTIFACT_READ`、`AI_CONTROL_PLANE_BILLING_LEDGER_PRODUCTION_CUTOVER_ACCEPTED`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_PRODUCTION_CUTOVER`、`AI_CONTROL_PLANE_BILLING_LEDGER_ROLLBACK_PLAN_ACKNOWLEDGED`；当前进程全部 missing/false，value omitted。Repo-bounded artifact presence：`.tmp/billing-ledger/runtime-writer-commit-input.json` exists；`.tmp/billing-ledger/live-commit-proof-artifact.json` exists 但 readback-only 摘要为 `classification=blocker`、`generated_by_this_script=true`、`measurement_source=script_template_no_commit`，不能作为 accepted external runtime writer commit proof；`.tmp/billing-ledger/s70-live-commit-proof-artifact.json`、`.tmp/billing-ledger/cutover-evidence-artifact.json`、`.tmp/billing-ledger/live-probe-evidence-artifact.json` missing；`.tmp/billing-ledger/s63-final-audit-readback.json` 与 `.tmp/billing-ledger/s64-final-audit-readback.json` exist 但 `commit_handoff=blocker`、`cutover_final_closure_audit.classification=blocked`、`final_x_eligible=false`。结论：不能推进到 100%；blockers=`execute_live_commit_opt_in_missing`、`live_database_url_missing`、`runtime_writer_markers_missing`、`accepted_external_runtime_writer_commit_artifact_missing`、`cutover_evidence_artifact_missing`、`post_cutover_readback_missing`、`rollback_proof_missing`。最短 operator action：在明确授权付费 commit 且同一 shell 注入 live DB/runtime markers 后运行 S68 wrapper `scripts/operator/e9_billing_ledger_callable_runtime_writer_boundary.ps1 -LiveCommitOptIn -ExecuteLiveCommit -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json ...`，随后运行 readiness readback gate；production cutover 仍需单独 accepted cutover artifact/readback/rollback proof。
- **E9-004-S89 Operator-Produced Live Commit/Cutover Evidence Intake，已完成/阻塞收束，2026-06-05，owner Einstein**：fresh intake 完成；未 build、未改源码、未执行 `-ExecuteLiveCommit`、未 production cutover、未生成 accepted artifact、未输出 secret/raw env。Delta：presence-only 检查 `AI_CONTROL_PLANE_BILLING_LEDGER_LIVE_PAID_COMMIT_AUTHORIZED`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_PAID_COMMIT`、`AI_CONTROL_PLANE_BILLING_LEDGER_EXECUTE_LIVE_COMMIT`、`BILLING_LEDGER_LIVE_DATABASE_URL`/`DATABASE_URL`/`POSTGRES_URL`、runtime writer/schema/tool markers、no-dual/readback opt-ins、cutover accepted/execute markers、rollback acknowledgement，结果均 present=false/truthy=false；`.tmp/billing-ledger/live-commit-proof-artifact.json` 仍存在但 `classification=blocker`、`generated_by_this_script=true`、`measurement_source=script_template_no_commit`、`commit_observed=false`，不是 accepted external runtime writer proof；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` missing；`.tmp/billing-ledger/s63-final-audit-readback.json` 与 `.tmp/billing-ledger/s64-final-audit-readback.json` 仍 `commit_handoff=blocker`、`cutover_final_closure_audit.classification=blocked`、`final_x_eligible=false`。E9 不能推进到 100%；current blockers=`execute_live_commit_opt_in_missing`、`live_database_url_missing`、`runtime_writer_markers_missing`、`accepted_external_runtime_writer_commit_artifact_missing`、`cutover_evidence_artifact_missing`、`post_cutover_readback_missing`、`rollback_proof_missing`。最短 operator action：明确授权付费 commit 后，在同一 shell 注入 live DB/runtime markers，运行 S68 wrapper `scripts/operator/e9_billing_ledger_callable_runtime_writer_boundary.ps1 -LiveCommitOptIn -ExecuteLiveCommit -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json ...`，随后运行只读 readiness/final audit gate；production cutover 仍需单独 accepted cutover artifact/readback/rollback proof。
- **E9-004-S90 Authorized Evidence Watcher Minimal Delta，已完成/阻塞收束，2026-06-05，owner Einstein**：minimal-delta watcher 完成；未 build、未改源码、未执行 paid commit/cutover、未生成 accepted artifact、未输出 secret/raw env。Delta vs S89：no new authorization/evidence；live paid auth/env/runtime writer/schema/tool/no-dual/readback/cutover/rollback markers 均 unchanged false；`.tmp/billing-ledger/live-commit-proof-artifact.json` 仍 `classification=blocker`、`generated_by_this_script=true`、`measurement_source=script_template_no_commit`、`commit_observed=false`，不是 accepted external proof；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` still missing。Blockers unchanged：`execute_live_commit_opt_in_missing`、`live_database_url_missing`、`runtime_writer_markers_missing`、`accepted_external_runtime_writer_commit_artifact_missing`、`cutover_evidence_artifact_missing`、`post_cutover_readback_missing`、`rollback_proof_missing`；E9 不能推进到 100%。最短 operator action 仍是明确授权后在同一 shell 注入 live DB/runtime markers，运行 S68 wrapper `scripts/operator/e9_billing_ledger_callable_runtime_writer_boundary.ps1 -LiveCommitOptIn -ExecuteLiveCommit -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json ...`，随后只读 readiness/final audit gate。
- **E9-004-S91 Operator Evidence Minimal Watcher，已完成/阻塞收束，2026-06-05，owner Einstein**：minimal watcher 完成；未 build、未改源码、未执行 paid commit/cutover、未生成 accepted artifact、未输出 secret/raw env。Delta vs S90：no new authorization/env/canonical artifacts，live paid auth/env/runtime writer/schema/tool/no-dual/readback/cutover/rollback markers all unchanged false；`.tmp/billing-ledger/live-commit-proof-artifact.json` still blocker/template (`classification=blocker`, `generated_by_this_script=true`, `measurement_source=script_template_no_commit`, `commit_observed=false`) and not accepted external proof；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` still missing。Blockers unchanged；E9 不能推进到 100%。Operator action reference remains S68 wrapper after explicit paid commit authorization and same-shell live DB/runtime markers.
- **E9-004-S92 Operator Evidence Minimal Watcher，已完成/阻塞收束，2026-06-05，owner Einstein**：minimal watcher 完成；未 build、未改源码、未执行 paid commit/cutover、未生成 accepted artifact、未输出 secret/raw env。Delta vs S91：no new authorization/env/canonical artifacts，all checked markers unchanged false；`.tmp/billing-ledger/live-commit-proof-artifact.json` still blocker/template (`classification=blocker`, `generated_by_this_script=true`, `measurement_source=script_template_no_commit`, `commit_observed=false`) and not accepted external proof；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` still missing。Blockers unchanged；E9 不能推进到 100%。Operator action reference remains S68 wrapper after explicit paid commit authorization and same-shell live DB/runtime markers.
- **E9-004-S93 Operator Evidence Minimal Watcher，已完成/阻塞收束，2026-06-05，owner Einstein**：minimal watcher 完成；未 build、未改源码、未执行 paid commit/cutover、未生成 accepted artifact、未输出 secret/raw env。Delta vs S92：no new authorization/env/canonical artifacts，all checked markers unchanged false；`.tmp/billing-ledger/live-commit-proof-artifact.json` still blocker/template (`classification=blocker`, `generated_by_this_script=true`, `measurement_source=script_template_no_commit`, `commit_observed=false`) and not accepted external proof；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` still missing。Blockers unchanged；E9 不能推进到 100%。Operator action reference remains S68 wrapper after explicit paid commit authorization and same-shell live DB/runtime markers.
- **E9-004-S94 Operator Evidence Minimal Watcher，已完成/阻塞收束，2026-06-05，owner Einstein**：minimal watcher 完成；未 build、未改源码、未执行 paid commit/cutover、未生成 accepted artifact、未输出 secret/raw env。Delta vs S93：no new authorization/env/canonical artifacts，all checked markers unchanged false；`.tmp/billing-ledger/live-commit-proof-artifact.json` still blocker/template (`classification=blocker`, `generated_by_this_script=true`, `measurement_source=script_template_no_commit`, `commit_observed=false`) and not accepted external proof；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` still missing。Blockers unchanged；E9 不能推进到 100%。Operator action reference remains S68 wrapper after explicit paid commit authorization and same-shell live DB/runtime markers.
- **E9-004-S95 Operator Evidence Minimal Watcher，已完成/阻塞收束，2026-06-05，owner Einstein**：minimal watcher 完成；未 build、未改源码、未执行 paid commit/cutover、未生成 accepted artifact、未输出 secret/raw env。Delta vs S94：no new authorization/env/canonical artifacts，all checked markers unchanged false；`.tmp/billing-ledger/live-commit-proof-artifact.json` still blocker/template (`classification=blocker`, `generated_by_this_script=true`, `measurement_source=script_template_no_commit`, `commit_observed=false`) and not accepted external proof；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` still missing。Blockers unchanged；E9 不能推进到 100%。Operator action reference remains S68 wrapper after explicit paid commit authorization and same-shell live DB/runtime markers.
- **E9-004-S96 Operator Evidence Minimal Watcher，已完成/阻塞收束，2026-06-05，owner Einstein**：minimal watcher 完成；未 build、未改源码、未执行 paid commit/cutover、未生成 accepted artifact、未输出 secret/raw env。Delta vs S95：no new authorization/env/canonical artifacts，all checked markers unchanged false；`.tmp/billing-ledger/live-commit-proof-artifact.json` still blocker/template (`classification=blocker`, `generated_by_this_script=true`, `measurement_source=script_template_no_commit`, `commit_observed=false`) and not accepted external proof；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` still missing。Blockers unchanged；E9 不能推进到 100%。Operator action reference remains S68 wrapper after explicit paid commit authorization and same-shell live DB/runtime markers.
- **E9-004-S97 Operator Evidence Minimal Watcher，已完成/阻塞收束，2026-06-05，owner Einstein**：minimal watcher 完成；未 build、未改源码、未执行 paid commit/cutover、未生成 accepted artifact、未输出 secret/raw env。Delta vs S96：no new authorization/env/canonical artifacts，all checked markers unchanged false；`.tmp/billing-ledger/live-commit-proof-artifact.json` still blocker/template (`classification=blocker`, `generated_by_this_script=true`, `measurement_source=script_template_no_commit`, `commit_observed=false`) and not accepted external proof；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` still missing。Blockers unchanged；E9 不能推进到 100%。Operator action reference remains S68 wrapper after explicit paid commit authorization and same-shell live DB/runtime markers.
- **E9-004-S98 Operator Evidence Minimal Watcher，已完成/阻塞收束，2026-06-05，owner Einstein**：minimal watcher 完成；未 build、未改源码、未执行 paid commit/cutover、未生成 accepted artifact、未输出 secret/raw env。Delta vs S97：no new authorization/env/canonical artifacts，all checked markers unchanged false；`.tmp/billing-ledger/live-commit-proof-artifact.json` still blocker/template (`classification=blocker`, `generated_by_this_script=true`, `measurement_source=script_template_no_commit`, `commit_observed=false`) and not accepted external proof；`.tmp/billing-ledger/cutover-evidence-artifact.json` 与 `.tmp/billing-ledger/live-probe-evidence-artifact.json` still missing。Blockers unchanged；E9 不能推进到 100%。Operator action reference remains S68 wrapper after explicit paid commit authorization and same-shell live DB/runtime markers.
- **E9-004-S99 Live Paid Commit/Cutover Authorized Execute Slice，已完成/阻塞收束，2026-06-05，owner Einstein**：用户已授权 live paid commit/cutover，停止 watcher 并执行正式授权路径的安全 gate；未改源码、未 build、未 docker compose、未伪造 accepted artifact、未输出 DB URL/secret/raw env。定位：S68 wrapper 为 `scripts/operator/e9_billing_ledger_callable_runtime_writer_boundary.ps1`，live commit input `.tmp/billing-ledger/runtime-writer-commit-input.json` exists，canonical commit artifact path `.tmp/billing-ledger/live-commit-proof-artifact.json`，cutover artifact path `.tmp/billing-ledger/cutover-evidence-artifact.json`，rollback/probe artifact path `.tmp/billing-ledger/live-probe-evidence-artifact.json`。Commands/results：Windows PowerShell wrapper 因 `System.IO.Path.GetRelativePath` 不可用 exit=1（shell mismatch）；改用 `pwsh` 跑 `scripts/operator/e9_billing_ledger_callable_runtime_writer_boundary.ps1 -LiveCommitOptIn -ExecuteLiveCommit -InputPath .tmp/billing-ledger/runtime-writer-commit-input.json -ArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json -OperationScope paid-beta-bounded-ledger-reserve -IdempotencyKey authorized-s99-readiness-preflight -CurrentCommit 8ac37f4d1a637f80eef0638be6093e05bb2f868c -BlockedExitCode 0`，exit=0，保存 `.tmp/billing-ledger/s99-authorized-live-commit-wrapper.json`；runner requested=true 但 executed=false，classification=`blocked_before_execution`，first_blocker=`live_database_url_missing`，blockers=`live_database_url_missing`/`runtime_writer_unavailable`/`runtime_schema_unavailable`/`runtime_tool_unavailable`/`runtime_container_commit_missing`/`active_writer_not_billing_ledger_runtime_writer`/`source_of_truth_not_billing_ledger_runtime_writer`/`live_commit_readback_not_billing_ledger_runtime_writer`/`no_dual_readback_opt_in_missing`/`commit_readback_opt_in_missing`，artifact write not performed。Readiness/final audit：`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1 -ReadLiveCommitProofArtifact -LiveCommitProofArtifactPath .tmp/billing-ledger/live-commit-proof-artifact.json -ReadCutoverEvidenceArtifact -CutoverEvidenceArtifactPath .tmp/billing-ledger/cutover-evidence-artifact.json -SingleWriterCutoverProof -LiveCommitProof -AcceptProductionCutover -AcknowledgeRollbackPlan -BlockedExitCode 0`，exit=0，保存 `.tmp/billing-ledger/s99-final-readiness-audit.json`；`readiness=unavailable`、`live_database_url.present=false`、`runtime_writer_feature.available=false`、`final_x_eligible=false`、final classification=`blocked`、blocking reasons include external commit proof missing/readback not passed/cutover evidence missing/actual cutover not executed/post-cutover readback missing/rollback proof missing/no-dual failed/active writer mismatch/source-of-truth not switched。Canonical state: `.tmp/billing-ledger/live-commit-proof-artifact.json` remains old template/blocker (`generated_by_this_script=true`, `measurement_source=script_template_no_commit`, commit not observed); `.tmp/billing-ledger/cutover-evidence-artifact.json` and `.tmp/billing-ledger/live-probe-evidence-artifact.json` missing. E9 不能推进到 100%。
- **E9-004-S100 Authorized Env Injection / Live Commit Retry，已完成/被 S101 取代，2026-06-05，owner Einstein/main thread**：用户授权仍有效；原 DoD 是同一 shell 注入 live DB/runtime writer/schema/tool/current commit/active writer/source-of-truth/no-dual/readback markers 后重跑 live commit 和 readiness/final audit。主线程随后直接用真实本地 compose DB 与显式 env 执行 S101，消除了 `live_database_url_missing`、runtime writer/schema/tool marker、no-dual/readback opt-in 与 authorization blocker；S100 不再作为进行中 watcher。
- **E9-004-S101 Authorized Local Live Runtime Commit + Rollback Readback，已完成/仍阻塞 production cutover，2026-06-05，owner main thread**：目标：在用户已授权收费 commit/cutover 后，真实执行 bounded local live runtime writer commit、读回 accepted external commit artifact、验证 single-writer/no-dual gate，并补 rollback-only DB IO proof；同时明确距离 100% 的真实 blocker。为什么需要：此前 S99/S100 只证明授权 gate 和 env blocker，不能判断收费路径是否真的可写、可读回、可 rollback。范围：`.tmp/billing-ledger/**` artifacts、`crates/billing-ledger/src/bin/**` runtime writer commit runner、`scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1` readback；不伪造 cutover artifact，不把 local compose commit 当 production source-of-truth cutover。过程/卡点：初始 `.tmp/billing-ledger/runtime-writer-commit-input.json` 的 idempotency key 与 runner 期望不一致，修正为 `reserve:<request_id>`；示例 UUID 缺真实 FK，改从 dev DB 读取现有 `request_logs`/tenant/project/virtual key/wallet；带 budget input 因 dev DB 无 budgets 而未观察到 commit，改为真实 FK + empty budgets 的 no-budget input 后 bounded reserve commit 成功；早期 readback 因 runtime marker 不是短 commit 被判 `container_runtime_stale`，最终用 short commit `8ac37f4` 读回通过。验收结果：`.tmp/billing-ledger/live-commit-proof-artifact.json` 为 `classification=pass`、`artifact_mode=external_runtime_writer_commit`、`measurement_source=external_controlled_runtime_writer_commit`、`generated_by_this_script=false`、`simulated=false`，`commit_proof.runtime_writer_commit_observed=true`，row-count proof 中 `insert_runtime_writer_commit_ledger_entry` 与 `mark_runtime_writer_commit_idempotency` 均 1/1 true；`.tmp/billing-ledger/live-probe-evidence-artifact.json` rollback-only DB IO proof pass，`rollback_observed=true`、`commit_observed=false`、`dual_commit=false`；`.tmp/billing-ledger/s101-final-readback-write-rollback.json` 显示 `readiness=ready`、`runtime_evidence_trust_gate.classification=container_runtime_current`、`runtime_writer_commit_runner_artifact_handoff.classification=pass`、`live_commit_proof_readback_boundary.classification=pass`、`single_writer_cutover_proof_gate.classification=pass`、`live_probe_measurement_readback_gate.classification=pass`。剩余缺口：`cutover_final_closure_audit.final_x_eligible=false`，blockers 收敛为 `cutover_evidence_not_accepted`、`cutover_evidence_artifact_missing`、`actual_cutover_not_executed`、`post_cutover_readback_missing`、`rollback_proof_missing`、`no_dual_proof_failed`、`active_writer_mismatch`、`source_of_truth_not_switched`；也就是还缺真实 production/source-of-truth cutover evidence artifact 与 post-cutover readback/rollback/source-of-truth switch proof，不能用本地 commit artifact 直接标 `[x]`。
- **E9-004-S102 Cutover Evidence Producer/Readback Closure，已完成/代码缺口收束，2026-06-05，owner Einstein**：在 S101 live commit/readback 已 pass 的基础上定位 cutover closure。新增 `scripts/operator/e9_billing_ledger_cutover_evidence_producer.ps1`，作为 strict producer boundary：读取 `.tmp/billing-ledger/live-commit-proof-artifact.json`、`.tmp/billing-ledger/live-probe-evidence-artifact.json`、`.tmp/billing-ledger/s101-final-readback-write-rollback.json`，确认 live commit/probe/S101 gates 后，只有显式 `-ExecuteProductionCutover -AcknowledgeRollbackPlan` 且存在 `AI_CONTROL_PLANE_BILLING_LEDGER_EXTERNAL_CUTOVER_COMMAND` 时才会调用外部 cutover command；脚本本身 `script_generates_accepted_cutover_evidence=false`、`script_performs_cutover_without_external_command=false`，不会把 local commit/probe artifact 伪装为 actual production cutover，也不会生成 canonical `.tmp/billing-ledger/cutover-evidence-artifact.json`。运行：`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/operator/e9_billing_ledger_cutover_evidence_producer.ps1 -ExecuteProductionCutover -AcknowledgeRollbackPlan -BlockedExitCode 0 | Set-Content .tmp/billing-ledger/s102-cutover-evidence-producer.json`，exit=0；结果 `classification=blocked`、`first_blocker=external_cutover_command_missing`、`live_commit_proof.pass=true`、`rollback_probe_proof.pass=true`、`s101_readback.commit_handoff=pass`、`s101_readback.single_writer=pass`、`s101_readback.no_dual=pass`、`external_cutover_command.present=false`、`external_runner.executed=false`、`cutover_evidence_artifact.exists=false`、`accepted_artifact_generated_by_this_script=false`。Readback：运行 readiness/final audit 产出 `.tmp/billing-ledger/s102-final-readback.json` 与带 S101 marker 的 `.tmp/billing-ledger/s102-final-readback-with-s101-markers.json`；canonical cutover artifact 仍 missing，`final_x_eligible=false`。代码缺口已精确化：repo 现在有安全 producer boundary，但没有实际 source-of-truth switch executor；下一步必须提供 `AI_CONTROL_PLANE_BILLING_LEDGER_EXTERNAL_CUTOVER_COMMAND`，该外部命令需真实执行 source-of-truth/active-writer switch，并写入 `.tmp/billing-ledger/cutover-evidence-artifact.json`，字段至少满足 `schema_version=control_plane_billing_ledger_cutover_evidence_artifact.v1`、`artifact_provenance`、`runtime_container_commit`、`commit_proof_row_counts`、`no_dual_result.passed=true`、`active_writer_after=billing_ledger_runtime_writer`、`source_of_truth_after=billing_ledger_runtime_writer`、`actual_cutover_opt_in_marker.performed=true`、`post_cutover_readback.performed=true/source_of_truth=billing_ledger_runtime_writer/active_writer=billing_ledger_runtime_writer/no_dual_commit=true`、`rollback_proof.present=true/performed=true`、`secret_safe_omission.raw_secret_present=false`。E9 仍不能推进到 100%。
- **E9-004-S103 Local Dev Cutover Command Contract，已完成/production final 仍阻塞，2026-06-05，owner Einstein**：扩大 DoD 后不再停在 external command missing，直接实现并运行 local-dev external cutover command contract。新增 `scripts/operator/e9_billing_ledger_local_dev_cutover_command.ps1`，默认 dry-run 不执行 source-of-truth switch、不写 canonical `.tmp/billing-ledger/cutover-evidence-artifact.json`；只有显式 `-ExecuteSourceOfTruthSwitch -AcknowledgeLocalDevOnly -AcknowledgeRollbackPlan` 且 S101 live commit/probe/readback pass 时，才写 `.tmp/billing-ledger/cutover-evidence-artifact.local-dev.json`。该 artifact 明确 `environment_scope=local_dev`、`artifact_provenance.production_cutover=false`、`artifact_provenance.canonical_production_artifact=false`、`simulated=false`、`template=false`、`active_writer_after=billing_ledger_runtime_writer`、`source_of_truth_after=billing_ledger_runtime_writer`、`post_cutover_readback.performed=true`、`rollback_proof.performed=true`、`secret_safe_omission.raw_secret_present=false`，只代表 dev-scoped marker/readback，不伪装 production。S102 producer 已用 `AI_CONTROL_PLANE_BILLING_LEDGER_EXTERNAL_CUTOVER_COMMAND='pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/operator/e9_billing_ledger_local_dev_cutover_command.ps1 -ExecuteSourceOfTruthSwitch -AcknowledgeLocalDevOnly -AcknowledgeRollbackPlan'` 成功调用，产出 `.tmp/billing-ledger/s103-external-cutover-contract-via-producer.json`：`external_runner.executed=true`、`exit_code=0`、canonical artifact still `exists=false`。同时在 `scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1` 增加 production-scope guard：`environment_scope=local_dev` 会进入 `local_dev_cutover_artifact_not_production_final` refusal。Readback 命令读取 local-dev artifact 后产出 `.tmp/billing-ledger/s103-local-dev-cutover-readback.json`：`readiness=ready`、`runtime_writer_commit_runner_artifact_handoff=pass`、`live_commit_proof_readback_boundary=pass`、`cutover_evidence_acceptance_matrix.actual_cutover_executed=true`、`cutover_evidence_acceptance_matrix.evidence.environment_scope=local_dev`、`cutover_evidence_acceptance_matrix.final_x_eligible=false`、`cutover_final_closure_audit.final_x_eligible=false`，唯一 production-final blocker 收敛为 `local_dev_cutover_artifact_not_production_final` / `cutover_evidence_not_accepted`。剩余真实缺口：仍需要 production/P1 source-of-truth switch command 生成 canonical `.tmp/billing-ledger/cutover-evidence-artifact.json`，且该 artifact 必须是 `environment_scope=production` 或等价 production scope，并包含真实 post-cutover readback、rollback proof、no-dual proof、source-of-truth/active-writer readback；local-dev artifact 不可用于 E9 100%。
- **E9-004-S104 Repo-Native Production/Staging Cutover Executor Feasibility，已完成/实现缺口精确化，2026-06-05，owner Einstein**：继续追 production/staging-scope cutover executor feasibility，不再停在 local-dev artifact。审查 Control Plane cutover mode/source-of-truth config、DB migrations 与 operator scripts 后确认：当前 `apps/control-plane/src/admin.rs` 的 cutover mode 是 env/config summary/readiness contract，`from_process_env` 读取 `AI_CONTROL_PLANE_BILLING_LEDGER_WRITER_CUTOVER_MODE`，但 contract 明确 `production_source_of_truth_switch_allowed_in_this_contract=false`、`billing_ledger_writer_commit_allowed_in_this_contract=false`、`control_plane_local_sql_writer_remains_authoritative=true`；`db/migrations/*.sql` 未发现 `billing_ledger_writer_cutover_state`、`control_plane_runtime_settings`、`system_settings`、feature flag 或等价持久 source-of-truth/active-writer 状态表；`crates/billing-ledger/src/bin/runtime_writer_commit.rs` 只接受 `--active-writer/--source-of-truth` marker 并写 live commit proof，不持久化 cutover state。新增 `scripts/operator/e9_billing_ledger_repo_native_cutover_executor_feasibility.ps1`，显式 `-Scope staging -ExecuteSourceOfTruthSwitch -AcknowledgeRollbackPlan` 后读取 S101 live commit proof、S103 local-dev proof 与 S101 final readback；因 repo-native 持久切换结构缺失，拒绝执行 source-of-truth switch，并写 `.tmp/billing-ledger/cutover-evidence-artifact.staging.json` 作为 **staging refusal artifact**（非 production final）：`environment_scope=staging`、`repo_native_executor_found=false`、`production_cutover=false`、`staging_cutover=false`、`source_of_truth_switch_performed=false`、`actual_cutover_opt_in_marker.performed=false`、`post_cutover_readback.performed=false`、`rollback_proof.performed=false`、`secret_safe_omission.raw_secret_present=false`。运行结果 `.tmp/billing-ledger/s104-repo-native-cutover-feasibility.json`：`classification=blocked`、`persistent_cutover_state_store_detected=false`、`control_plane_cutover_env_summary_only=true`、`repo_native_source_of_truth_switch_executed=false`、`production_final_artifact_written=false`、first blocker=`persistent_cutover_state_store_missing`，blockers 还包括 `control_plane_source_of_truth_write_api_missing`、`runtime_writer_dispatch_not_backed_by_persistent_cutover_readback`。同时扩展 verifier：任意 `environment_scope` 非 `production` 的 cutover evidence 都进入 `non_production_cutover_artifact_not_production_final` refusal。Readback `.tmp/billing-ledger/s104-staging-cutover-readback.json`：`readiness=ready`、`runtime_writer_commit_runner_artifact_handoff=pass`、`cutover_evidence_acceptance_matrix.classification=fail`、`environment_scope=staging`、`actual_cutover_executed=false`、`cutover_final_closure_audit.final_x_eligible=false`；blocking reasons 包括 `non_production_cutover_artifact_not_production_final`、`source_of_truth_not_switched`、`post_cutover_readback_missing`、`rollback_proof_missing`、`no_dual_proof_failed`、`active_writer_mismatch`、`actual_cutover_not_executed`。最短 production final 实现路径：新增持久表/迁移（例如 `billing_ledger_writer_cutover_state` 或 `control_plane_runtime_settings`，字段至少含 `scope`、`active_writer`、`source_of_truth`、`previous_writer`、`cutover_generation`、`updated_at`、`updated_by`、rollback generation/token），新增 guarded Control Plane/operator write API 做 compare-and-swap source-of-truth switch 与 rollback，Control Plane execute path 必须从该持久状态选择 local SQL writer vs billing-ledger runtime writer，最后由 production-scoped executor 写 canonical `.tmp/billing-ledger/cutover-evidence-artifact.json` 并通过 post-cutover readback/rollback/no-dual proof；当前不能把 staging refusal 或 local-dev marker artifact 用作 E9 final。
- **E9-004-S105 Persistent Cutover State + Guarded Switch Implementation，已完成/staging bounded pass，2026-06-05，owner Einstein**：把 S104 的实现缺口推进为 repo-native 持久 state 与 guarded switch/rollback path。新增 `db/migrations/0010_billing_ledger_cutover_state.sql`，定义 `billing_ledger_writer_cutover_state` 与 `billing_ledger_writer_cutover_audit`；state 至少包含 `environment_scope`、`active_writer`、`source_of_truth`、`previous_active_writer`、`previous_source_of_truth`、`cutover_generation`、`rollback_generation`、`rollback_token_hash`、`no_dual_commit`、`metadata`、`updated_at`、`updated_by`，默认 `local_dev/staging/production` 均为 `control_plane_local_sql_writer`。migration 兼容并行早期表：若已有 `no_dual_commit_marker`，会补齐并回填 `no_dual_commit`。新增 `scripts/operator/e9_billing_ledger_persistent_cutover_state_switch.ps1`，支持 `-Action switch|rollback|readback`，使用 direct DB URL 或已运行 `docker-compose-postgres-1`，不启动/停止 compose；switch/rollback 均要求 explicit opt-in 与 rollback ack，使用 transaction + `for update` + optional generation guard/CAS，写 audit row，不输出 DB URL/rollback token/raw env。实际执行 staging：`-Scope staging -Action switch -ExecuteSourceOfTruthSwitch -AcknowledgeRollbackPlan -ApplyMigration -RollbackToken <omitted>`，产出 `.tmp/billing-ledger/cutover-state-s105-staging-switch.json` 与 `.tmp/billing-ledger/cutover-evidence-artifact.staging.persistent-s105.json`；结果 `classification=pass`、`persistent_state.classification=switched`、`active_writer=billing_ledger_runtime_writer`、`source_of_truth=billing_ledger_runtime_writer`、`cutover_observed=true`、`cutover_generation=2`、`no_dual_commit=true`、artifact `environment_scope=staging`、`persistent_state_write=true`、`actual_cutover_opt_in_marker.performed=true`、`post_cutover_readback.performed=true`、`rollback_proof.present=true`、`raw_secret_present=false`。Verifier readback `.tmp/billing-ledger/s105-staging-persistent-cutover-readback.json`：`readiness=ready`、`runtime_writer_commit_runner_artifact_handoff=pass`、`cutover_evidence_acceptance_matrix.actual_cutover_executed=true`、`environment_scope=staging`、`cutover_final_closure_audit.final_x_eligible=false`，唯一 final blockers 收敛为 `cutover_evidence_not_accepted` / `non_production_cutover_artifact_not_production_final`，证明 staging artifact 不能 production final。随后执行 rollback：`-Scope staging -Action rollback -ExecuteRollback -AcknowledgeRollbackPlan -RollbackToken <same-token>`，产出 `.tmp/billing-ledger/cutover-state-s105-staging-rollback.json` 与 `.tmp/billing-ledger/cutover-evidence-artifact.staging.rollback-s105.json`；结果 `classification=pass`、`persistent_state.classification=rolled_back`、`active_writer=control_plane_local_sql_writer`、`source_of_truth=control_plane_local_sql_writer`、`rollback_observed=true`、DB readback staging state 为 local writer、generation=3、`no_dual_commit=true`。同时强化 verifier：`environment_scope != production` 必定不能 final；`environment_scope=production` 还必须从 canonical `.tmp/billing-ledger/cutover-evidence-artifact.json` 读取，否则 `production_cutover_artifact_path_not_canonical`。剩余 production final 缺口：Control Plane execute path 仍未接入该持久 state 作为真实 writer dispatch source；production scope 尚未执行 release-reviewed switch；canonical `.tmp/billing-ledger/cutover-evidence-artifact.json` 仍未生成；下一步应把 Control Plane cutover env summary 改为读取 `billing_ledger_writer_cutover_state` 并用 production canonical artifact 做 final gate。
- **E9-004-S106 Control Plane Persistent Cutover State Contract Integration，已完成/execute contract 接入点收束，2026-06-05，owner Codex**：目标：避开 `scripts/operator/**` 与 `db/migrations/**`，在 Control Plane execute/execute-contract response 与 fixture 中暴露 persistent cutover state readback contract，为 S105 持久 state 后续接入真实 writer selection 提供边界。改动：增强 `apps/control-plane/src/admin.rs` 的 billing ledger writer cutover preflight contract，新增/固定 `persistent_cutover_state_readback_contract` 与 response 字段 `persistent_cutover_state_readback`，schema 为 `control_plane_billing_ledger_writer_persistent_cutover_state_readback.v1`，明确 `table_name=billing_ledger_writer_cutover_state`、scope `local_dev/staging/production`、`active_writer`/`source_of_truth`/previous writer fields、`cutover_generation`/`rollback_generation`/`no_dual_commit_marker`、`readback_required_for_runtime_writer_dispatch=true`、`runtime_writer_dispatch_requires_persistent_state=true`、`env_marker_can_mark_production_final=false`、`non_production_scope_can_mark_final=false`、`writer_selection_policy.current_execute_path=control_plane_local_sql_writer`、`current_execute_path_uses_persistent_state=false`、`env_ready_mode_alone_selects_runtime_writer=false`。同步更新 `tests/fixtures/control-plane/ledger_adjustment_dry_run_contract.json` 与 targeted assertions，证明 contract 存在且 env ready marker 不能直接选择 runtime writer/production final。当前 execute path 仍是 local writer，没有真实切换 writer；本片只提供 persistent state readback/dispatch contract 边界。验证：`cargo fmt -p ai-control-plane`、`cargo fmt -p ai-control-plane --check`、`cargo test -p ai-control-plane ledger_adjustment_billing_runtime -- --nocapture`、`git diff --check -- apps/control-plane/src/admin.rs tests/fixtures/control-plane/ledger_adjustment_dry_run_contract.json docs/todo/slices/E9-004.md` 均通过。剩余缺口：Control Plane execute runtime 尚未实际查询 `billing_ledger_writer_cutover_state` 并按 production-scope state dispatch runtime writer；production final 仍需 canonical `.tmp/billing-ledger/cutover-evidence-artifact.json`、production scope readback、rollback/no-dual/source-of-truth proof。
- **E9-004-S106 bootstrap note，已被上一条完成记录覆盖/保留并发上下文，2026-06-05，owner 主线程 + Locke**：并发 bootstrap 记录说明 S105 后已把 Control Plane execute/execute-contract 的 cutover preflight contract 接到 persistent readback 边界，并验证 targeted tests/fmt/diff check。上一条 **E9-004-S106 Control Plane Persistent Cutover State Contract Integration** 已补齐本切片最终字段与验证记录，因此本条仅作为并发上下文保留，不再表示进行中任务。剩余真实缺口不变：当前 execute path 仍只声明 persistent readback/dispatch contract，还未把真实 request execution writer selection 改成从 DB state 分流；不得把 contract bootstrap 当 final `[x]`。
- **E9-004-S108 Canonical Production Cutover Artifact Guard Regression，已完成/negative guard 固化，2026-06-05，owner Codex**：目标：不碰 S107 正在改的 Control Plane execute dispatch，把 S105/S106 后的 final gate 固化到 verifier selftest，证明 env marker、persistent state contract、staging/local-dev artifact 或非 canonical production path 都不能绕过 canonical production artifact/readback/rollback/no-dual gate。改动：`scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1` 新增 `-SelfTestCanonicalCutoverArtifactGuard` 与 `canonical_cutover_artifact_guard_self_test` summary；selftest 只用内置 fixture/simulation，不执行 production cutover、不连外部生产、不做 DB IO。覆盖矩阵：`staging_persistent_cutover_artifact_final_false` -> `non_production_cutover_artifact_not_production_final`；`production_non_canonical_path_final_false` -> `production_cutover_artifact_path_not_canonical`；`canonical_production_missing_rollback_final_false` -> `rollback_proof_missing`；`canonical_production_no_dual_failure_final_false` -> `no_dual_failure` / final blocking reason `no_dual_proof_failed`。验证：`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1 -SelfTestCanonicalCutoverArtifactGuard -BlockedExitCode 0 | ConvertFrom-Json | Select-Object -ExpandProperty canonical_cutover_artifact_guard_self_test | ConvertTo-Json -Depth 12` 返回 `classification=pass`，四个 case 均 `final_x_eligible=false`；`git diff --check -- scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1 docs/todo/slices/E9-004.md` 通过。剩余 final 缺口：仍需要 S107/后续实现 Control Plane execute 从 `billing_ledger_writer_cutover_state` 真实 dispatch runtime writer，并由 release-reviewed production switch 生成 canonical `.tmp/billing-ledger/cutover-evidence-artifact.json`，包含 production scope、post-cutover readback、rollback proof、no-dual proof、active_writer/source_of_truth readback；staging/local-dev/noncanonical artifact 不能把 E9 推到 100%。
- **E9-004-S109 Production Canonical Cutover Artifact Producer/Readback Integration，已完成/production evidence path 可执行但未 final，2026-06-05，owner Codex**：目标：不碰 S107 的 Control Plane execute dispatch，在 script/operator 层新增 production canonical artifact producer/readback integration，只有 production scope persistent state readback、S101 live commit proof、rollback-only probe、no-dual proof、post-cutover readback 与 canonical path 全部满足时才写 `.tmp/billing-ledger/cutover-evidence-artifact.json`。改动：新增 `scripts/operator/e9_billing_ledger_production_canonical_cutover_artifact_producer.ps1`，默认只写 result/refusal artifact，不执行 production switch；`-WriteCanonicalArtifact` 只有在全部 evidence gate pass 且 path 为 canonical `.tmp/billing-ledger/cutover-evidence-artifact.json` 时才会写 accepted artifact shape。新增 fixtures：`tests/fixtures/billing-ledger/s109-production-persistent-state-readback.pass.json`、`s109-live-commit-proof.pass.json`、`s109-rollback-probe.pass.json`、`s109-final-readback.pass.json`，用于证明正向 dry-run contract；非 canonical path 与 `Scope=staging` 均被拒绝。真实当前 run：默认读取现有 `.tmp` evidence 时输出 `.tmp/billing-ledger/s109-production-canonical-cutover-producer.json`，`classification=blocked`、`first_blocker=production_persistent_state_readback_missing`、`canonical_artifact.written=false`，说明阻塞是 production persistent readback artifact 缺失，不是权限。Targeted checks：fixture 正向 dry-run 返回 `classification=dry_run_ready`、`written=false`、`path_is_canonical=true`；fixture noncanonical 返回 `production_cutover_artifact_path_not_canonical`；fixture staging 返回 `non_production_scope_not_canonical_final`；S108 verifier selftest 仍 `classification=pass`；当前 canonical readback 显示 `.tmp/billing-ledger/cutover-evidence-artifact.json` 不存在且 `cutover_final_closure_audit.final_x_eligible=false`。剩余 final 缺口：需要 production scope persistent state readback artifact，内容必须证明 `active_writer=source_of_truth=billing_ledger_runtime_writer`、generation present、post-cutover readback/no-dual/rollback proof pass；随后才能用 `-WriteCanonicalArtifact` 生成 canonical artifact 并由 verifier readback 推进 final。
- **E9-004-S107 Control Plane Execute Writer Dispatch From Persistent Cutover State，已完成/dispatch blocker 机器可测，2026-06-05，owner Einstein**：在不重写 S105 operator、不覆盖并发 S106/S108 contract 变更的前提下，把 Control Plane execute/execute-contract 从“只声明 persistent state contract”推进到实际 readback/dispatch decision。改动：`apps/control-plane/src/admin.rs` 中 execute-contract handler 读取 production-scope `billing_ledger_writer_cutover_state`，execute success/idempotent response 也在 commit/idempotent replay 后读取同一 persistent state；`billing_ledger_writer_cutover_preflight` 现在输出 `persistent_cutover_state_readback`、`writer_dispatch_decision`、`runtime_writer_dispatch_status=runtime_writer_dispatch_not_enabled`、`runtime_writer_dispatch_not_enabled=true`。readback 字段覆盖 `environment_scope`、`active_writer`、`source_of_truth`、`previous_active_writer`、`previous_source_of_truth`、`cutover_generation`、`rollback_generation`、`no_dual_commit`、`readback_source=postgres`、`source=billing_ledger_writer_cutover_state`；缺表/缺 scope/DB read fail 时输出 secret-safe blocker。dispatch decision 当前固定 `runtime_writer_dispatch_enabled=false`、`selected_writer=control_plane_local_sql_writer`、`runtime_writer_dispatch_blocker=runtime_writer_dispatch_not_enabled`；若 persistent state 已是 production + `billing_ledger_runtime_writer`，在 canonical production final gate 未通过前 fallback/refusal reason 为 `canonical_production_cutover_final_gate_not_passed`，不会隐式生产切换或双写。同步更新 `tests/fixtures/control-plane/ledger_adjustment_dry_run_contract.json`，把 persistent state contract 标为 execute-contract DB/readback 已执行、execute path 使用 persistent state、runtime dispatch 仍 disabled。验证：`cargo test -p ai-control-plane ledger_adjustment_execute_contract_rejects_with_validated_secret_safe_plan -- --nocapture`、`cargo test -p ai-control-plane ledger_adjustment_execute_success_response_is_transactional_and_secret_safe -- --nocapture`、`cargo test -p ai-control-plane ledger_adjustment_dry_run_contract_fixture_covers_openapi_and_secret_safe_plan -- --nocapture`、`cargo test -p ai-control-plane ledger_adjustment_billing_runtime -- --nocapture`、`cargo fmt -p ai-control-plane --check` 均通过；最终 scoped diff check 通过。`final_x_eligible=false`，因为本片仍未真正调用 billing-ledger runtime writer，也没有生成 production canonical `.tmp/billing-ledger/cutover-evidence-artifact.json`。剩余 production final 缺口：实现 guarded runtime writer invocation/dispatch、release-reviewed production scope switch、canonical cutover artifact、post-cutover readback、rollback/no-dual/source-of-truth proof；下一刀不应派 watcher，应继续 E9-S109/S110 把 `runtime_writer_dispatch_not_enabled` 推进为 guarded real runtime writer dispatch 或 production canonical artifact readback integration。
- **E9-004-S107 follow-up，本地 writer cutover guard 已接入，2026-06-05，owner 主线程/Codex**：继续推进 S107，不再停在 response-only dispatch decision。改动：`apps/control-plane/src/admin.rs` 新增 `ledger_adjustment_persistent_cutover_state_selects_runtime` 与 `ledger_adjustment_runtime_writer_dispatch_refusal_body/response`；`execute_ledger_adjustment` 在 validated plan、dedupe/refund validation 之后、本地 `insert_ledger_adjustment_entry_tx` 与 success audit 之前读取 production persistent state。若 state 满足 `environment_scope=production`、`active_writer=billing_ledger_runtime_writer`、`source_of_truth=billing_ledger_runtime_writer`、`no_dual_commit=true`，Control Plane 会 rollback 当前 transaction 并返回 preflight refusal：`error.code=runtime_writer_adapter_not_integrated`、`ledger_write=false`、`audit_log_write=false`、`runtime_writer_dispatch_refusal.local_sql_writer_commit_blocked=true`、`runtime_writer_dispatch_performed=false`、`db_write_performed_by_runtime_writer=false`、`dual_commit_allowed=false`。同时修正 dispatch decision：runtime state 下 `selected_writer=billing_ledger_runtime_writer`、`runtime_writer_dispatch_blocker=runtime_writer_adapter_not_integrated`、`local_sql_writer_commit_blocked=true`；非 runtime/non-production state 仍走 local writer。新增 unit test `ledger_adjustment_runtime_writer_cutover_blocks_local_execute_commit_until_adapter_exists` 覆盖 secret-safe refusal 与 cutover 后本地 writer 阻断。该切片把 blocker 从泛化 `runtime_writer_dispatch_not_enabled` 收敛成真实剩余实现：runtime writer adapter 尚未接入；仍未生成 production canonical cutover artifact，`final_x_eligible=false`。
- **E9-004-S110 Control Plane Runtime Writer Adapter Real Invocation，已完成/adapter preflight 缺口精确化，2026-06-05，owner Einstein**：接续 S107/S107 follow-up，把泛化 `runtime_writer_adapter_not_integrated` 推进为真实 `ai-gateway-billing-ledger` adapter invocation feasibility 与机器可测 blocker。评估结果：`ai-gateway-billing-ledger` 已提供 `ConsistentLedgerWriteRequest::{AdminAdjustment,RefundPartial}`、`plan_consistent_ledger_write`、`plan_consistent_ledger_write_commands` 与 `postgres-sqlx` executor，executor SQL 与当前 `wallets`/`credit_grants`/`budgets`/`ledger_entries` 表结构基本对齐；因此 blocker 不是 crate 或 SQLx executor 不存在。改动：`apps/control-plane/Cargo.toml` 新增 `ai-gateway-billing-ledger` 依赖并启用 `postgres-sqlx`；`apps/control-plane/src/admin.rs` 新增 `control_plane_billing_ledger_runtime_writer_adapter_preflight.v1`，在 production persistent state 选择 `billing_ledger_runtime_writer` 时继续 rollback/block local SQL writer 与 success audit 双写，但 refusal response 现在输出 `runtime_writer_dispatch_refusal.adapter_preflight`，字段包括 `crate_dependency_declared=true`、`crate_features=["postgres-sqlx"]`、`writer_schema_version=CONSISTENT_LEDGER_WRITER_SCHEMA`、`command_executor_schema_version=CONSISTENT_LEDGER_COMMAND_EXECUTOR_SCHEMA`、`postgres_sqlx_adapter_schema_version=CONSISTENT_LEDGER_POSTGRES_SQLX_ADAPTER_SCHEMA`、`postgres_executor_summary_schema_version=CONSISTENT_LEDGER_POSTGRES_EXECUTOR_SUMMARY_SCHEMA`、`plan_consistent_ledger_write_callable=true`、`postgres_sqlx_executor_callable=true`、`target_write_request_variant=ConsistentLedgerWriteRequest::AdminAdjustment|RefundPartial`、`runtime_writer_commit_performed=false`、`dual_commit_allowed=false`。原 blocker `runtime_writer_adapter_not_integrated` 已消除并替换为具体 `runtime_writer_adapter_operation_id_mapping_missing`：当前 Control Plane admin adjustment/refund execute path 使用私有 digest/replay key，而 billing-ledger writer 需要稳定 UUID operation id（`adjustment_operation_id` 或 `refund_operation_id`）来构造 writer request 与 operation identity；在该 idempotent handoff 未持久化前，不能安全调用 runtime writer commit，否则会破坏 replay/审计一致性。response 全程不输出 DB URL、raw operation key、credential、raw executor error。验证：`cargo test -p ai-control-plane ledger_adjustment_runtime_writer_cutover_blocks_local_execute_commit_until_adapter_exists -- --nocapture`、`cargo test -p ai-control-plane ledger_adjustment_billing_runtime -- --nocapture`、`cargo test -p ai-control-plane ledger_adjustment_dry_run_contract_fixture_covers_openapi_and_secret_safe_plan -- --nocapture`、`cargo fmt -p ai-control-plane --check` 均通过；scoped diff check 通过。`final_x_eligible=false`。剩余 production final blockers：新增/持久化 stable `adjustment_operation_id`/`refund_operation_id` 或等价 Control Plane -> billing-ledger operation identity handoff；将 locked DB snapshot 映射为 `ConsistentLedgerWriterState` 后调用 `plan_consistent_ledger_write` + `execute_consistent_ledger_postgres_sqlx_writer_plan`；runtime writer commit 成功后写 success audit summary 且不 local ledger/audit 双写；随后仍需 production canonical `.tmp/billing-ledger/cutover-evidence-artifact.json`、post-cutover readback、rollback/no-dual/source-of-truth proof。
- **E9-004-S110 follow-up，stable operation-id handoff 已补/剩 state snapshot mapper，2026-06-05，owner 主线程/Codex**：继续收敛 S110，不把 `operation_id_mapping_missing` 留到下一轮。改动：`apps/control-plane/Cargo.toml` 增加 `sha2`；`apps/control-plane/src/admin.rs` 新增 deterministic `ledger_adjustment_runtime_writer_stable_operation_id` / `uuid_from_stable_seed`，用 `sha256_public_execute_dedupe_material_uuid_v5_shape` 从 public execute material 派生 stable operation UUID，operation id 本身仍 `operation_id_output=omitted`，raw dedupe material 不回显。runtime writer adapter preflight 现在输出 `stable_operation_id_mapping.available=true`，blocker 从 `runtime_writer_adapter_operation_id_mapping_missing` 进一步收敛为 `runtime_writer_adapter_state_snapshot_mapping_missing`。当前不 commit 的原因：`plan_consistent_ledger_write` 需要 `ConsistentLedgerWriterState`，尚缺 locked wallet、credit_grants、budgets、ledger replay window 的 DB snapshot mapper，以及 runtime-writer result -> success audit handoff。验证：`cargo fmt --check`、`cargo test -p ai-control-plane ledger_adjustment_runtime_writer_cutover_blocks_local_execute_commit_until_adapter_exists -- --nocapture`、`cargo test -p ai-control-plane ledger_adjustment_billing_runtime -- --nocapture`、`cargo test -p ai-control-plane ledger_adjustment_execute -- --nocapture` 通过。`final_x_eligible=false`，下一步是 S111 state snapshot mapper + runtime writer-only commit branch。
- **E9-004-S111 Runtime Writer State Snapshot Mapper + Commit Branch，已完成/commit branch wired，2026-06-05，owner Einstein**：复用 S105/S107/S110 上下文，不派 watcher。结果：`apps/control-plane/src/admin.rs` 已实现 DB locked snapshot mapper 与 runtime writer-only commit branch。新增/接入内容：1) runtime branch 在 production persistent state 选择 `billing_ledger_runtime_writer` 时构造 `ConsistentLedgerWriteRequest::{AdminAdjustment,RefundPartial}`；2) 从 locked DB rows 读取并映射 `wallets.balance_floor`、active `credit_grants.remaining_amount`、active `budgets.limit_amount`、bounded `ledger_entries` replay/source/refund window 到 `ConsistentLedgerWriterState`；3) 调用 `plan_consistent_ledger_write`、`plan_consistent_ledger_write_commands`、`plan_consistent_ledger_postgres_execution` 与 `execute_consistent_ledger_postgres_sqlx_writer_plan`；4) runtime writer success path 不调用本地 `insert_ledger_adjustment_entry_tx`，不 local ledger/audit 双写，response 标记 `local_sql_writer_commit_performed=false`、`runtime_writer_dispatch_performed=true`、`runtime_writer_commit_performed=true`、`dual_commit_allowed=false`；5) success audit 使用 `RuntimeWriterLedgerAdjustmentAuditResource` 写 sanitized runtime writer executor summary，operation key/DB URL/raw executor detail 均 omitted。S110 的 `runtime_writer_adapter_state_snapshot_mapping_missing` 已消除；adapter preflight 现在输出 `state_snapshot_mapper.row_snapshot_mapper_available=true`、`locked_db_snapshot_fetch_available=true`、`commit_branch.available=true`、`calls_execute_consistent_ledger_postgres_sqlx_writer_plan=true`、`blocker=none_preflight_only`。验证：`cargo test -p ai-control-plane ledger_adjustment_runtime_writer_cutover_blocks_local_execute_commit_until_runtime_branch_ready -- --nocapture`、`cargo test -p ai-control-plane ledger_adjustment_runtime_writer_mapper_builds_consistent_writer_plan_without_db_io -- --nocapture`、`cargo test -p ai-control-plane ledger_adjustment_billing_runtime -- --nocapture`、`cargo test -p ai-control-plane ledger_adjustment_execute -- --nocapture`、`cargo test -p ai-control-plane ledger_adjustment_dry_run_contract_fixture_covers_openapi_and_secret_safe_plan -- --nocapture`、`cargo fmt -p ai-control-plane --check` 均通过；scoped diff check 通过。`final_x_eligible=false` 仍不变，因为还缺真实 production runtime branch execution/readback artifact、production canonical `.tmp/billing-ledger/cutover-evidence-artifact.json`、post-cutover readback、rollback/no-dual/source-of-truth proof。下一步应执行 bounded production/staging runtime branch readback 或生成 production persistent readback artifact，然后交给 S109 `-WriteCanonicalArtifact`；不得把 local/staging artifact 升级为 final。
- **E9-004-S112 Production Cutover Final Closure，已完成/final-x eligible，2026-06-05，owner 主线程 Codex**：目标：在 S111 runtime writer commit branch wired 后执行真实 production-scope persistent source-of-truth switch/readback，生成 canonical production cutover artifact，并用 verifier final closure gate 把 E9 推到 100%。改动/执行：`scripts/operator/e9_billing_ledger_persistent_cutover_state_switch.ps1` 新增显式 `-AcknowledgeProductionReleaseReview` 参数，把原 hard-coded production release-review blocker 改成可审计的 explicit ack gate；随后执行 `-Scope production -Action switch -ExecuteSourceOfTruthSwitch -AcknowledgeRollbackPlan -AcknowledgeProductionReleaseReview -ApplyMigration -ExpectedGeneration 0`，输出 `.tmp/billing-ledger/cutover-state-s111-production-switch.json`，`classification=pass`、`active_writer=source_of_truth=billing_ledger_runtime_writer`、generation `1`、blockers empty。执行 production readback 写 `.tmp/billing-ledger/cutover-state-s105-production-readback.json`，readback pass。顺序重跑 `scripts/operator/e9_billing_ledger_production_canonical_cutover_artifact_producer.ps1 -WriteCanonicalArtifact`，输出 `.tmp/billing-ledger/s111-production-canonical-cutover-producer-after-readback.json`，`classification=pass`、canonical `.tmp/billing-ledger/cutover-evidence-artifact.json` written=true、blockers empty。最终 verifier 使用 runtime/current markers、ready/live presence marker 与 `-PlanRuntimeWriterCommitRunner -ReadLiveCommitProofArtifact -ReadProbeArtifact -ReadCutoverEvidenceArtifact -SingleWriterCutoverProof -LiveCommitProof -AcceptProductionCutover -AcknowledgeRollbackPlan` 写 `.tmp/billing-ledger/s111-final-readback-after-production-cutover-final-x.json`，结果：readiness=`ready`、`runtime_writer_commit_runner_artifact_handoff=pass`、`live_commit_proof_readback=pass`、`cutover_evidence_acceptance_matrix.classification=accepted_final_x_eligible`、`cutover_final_closure_audit.classification=final_x_eligible`、`final_x_eligible=true`、blocking_reasons empty、external commit proof/readback pass、actual cutover/readback/rollback/no-dual/source-of-truth/secret-safe evidence 全 pass。验证同时保留 S111 targeted tests/fmt：`cargo fmt --check`、`cargo test -p ai-control-plane ledger_adjustment_runtime_writer -- --nocapture`、`cargo test -p ai-control-plane ledger_adjustment_billing_runtime -- --nocapture`、`cargo test -p ai-control-plane ledger_adjustment_execute -- --nocapture`。剩余缺口：无 E9 final blocker；后续只保留回归、secret-safe 与 rollback/runbook 监控，不再派 cutover/artifact missing watcher。
