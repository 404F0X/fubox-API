# E8-004 切片记录

> 由 `docs/TODO.md` 拆分而来；只保存切片日志，不作为最终完成状态的唯一依据。

- **E8-004-S1，已完成，2026-06-04**：建立 DB repository execution boundary。原因：Gateway 不能直接靠 request-local 计划实现分布式限流，必须先有 bounded DB acquire/release 写边界。范围：`crates/db/**`。验收：Noop/Refused 不查询 DB，SqlReady 使用 bounded SQL，tenant/provider_key/channel bind，secret-safe summary，`<=1` row semantics。
- **E8-004-S2，已完成，2026-06-04**：强化 DB execution contract。原因：在接 Gateway 前先锁住 no-query、limited/unlimited、<=1 row 和 secret-safe 语义，避免 runtime 接线后才发现 DB 合约不稳。范围：`crates/db/**`、`tests/fixtures/db/**`。验收：fixture 覆盖 no-query Noop、invalid required Refused、limited/unlimited dimensions、acquire/release bind count、`fetch_optional`、schema scope 和 secret-safe output。
- **E8-004-S3，已完成，2026-06-04**：Gateway runtime 接入 DB reservation execution。原因：把已存在的 request-local reservation 计划提升为真实 provider-key counter mutation 的运行时路径，同时保留 fallback 和 no-side-effect ordering。范围：`apps/gateway/**`、`tests/fixtures/gateway/**`、`Cargo.lock`。验收：provider key 选中后、provider_attempt/key open/upstream 前 acquire；NotApplied/Refused 走 fallback/429；已 Applied 但未正常消耗时 release；全 unlimited/no-counter route 不打 DB；不重复 release；request/provider logs 只记录安全摘要；targeted gateway/db/routing tests、workspace check/test 通过。
- **E8-004-S4，已完成，2026-06-04，owner Mencius，live 执行阻塞**：live Postgres/concurrency smoke。原因：S1-S3 已覆盖 contract 和 runtime ordering，但 `[x]` 需要证明真实 DB counter mutation、并发 NotApplied、fallback、release 在实际 schema 上工作。范围：smoke scripts/fixtures 或现有 integration harness。验收：真实 Postgres 上 acquire 会更新 bounded counters；并发超额请求产生 NotApplied/fallback/429；失败/fallback 会 release；secret-safe logs/snapshots；若本机缺 Docker/Postgres，则标记外部阻塞而不是继续增加 contract-only 切片。结果：新增 `scripts/verify_gateway_rate_limit_reservation_smoke.ps1` 与 live smoke fixture，覆盖 provider-key acquire counter mutation、fallback release、concurrent NotApplied -> fallback -> 429 和 secret-safe evidence；dry-run 与 targeted Gateway tests 通过。剩余缺口：本机 Docker daemon 未运行，尚未执行真实 Postgres/Gateway live smoke；E8 仍不能标 `[x]`。
- **E8-004-S5，已完成，2026-06-04，owner Mencius，live 执行阻塞**：rate-limit reservation live smoke 测试入口接入。原因：S4 已新增可运行 harness，但开发/CI 需要清晰 opt-in 入口和 Docker/Postgres preflight，避免 live smoke 被误当成普通 contract test 或因环境缺失造成假失败。范围：`scripts/**`、必要的测试/验收文档；不改 Gateway 生产逻辑。验收：现有测试入口能发现/调用 dry-run 或 opt-in live smoke；Docker/Postgres 不可用时输出明确阻塞；默认非 live 流程不被破坏；相关 script dry-run/check 通过。结果：rate-limit reservation live smoke 已接入 `scripts/test.ps1` 与 release smoke gate，默认 dry-run，显式 flag/env 才进入 preflight/live。剩余缺口：本机 Docker daemon 不可用，live Postgres/concurrency 验证待可用环境补跑。
- **E8-004-S6，已完成，2026-06-04，owner Mencius**：precise TPM estimate contract。原因：E8 runtime DB reservation 与 live smoke 入口已落地，但 `[x]` 仍需要 rate-limit key selection 对 token-per-minute reservation 有稳定、可解释、不过度乐观的估算 contract。范围：`crates/routing/**`、routing fixtures/tests；不改 Gateway/Control Plane/UI。验收：routing pure planner 对 prompt/completion/token estimate 的 TPM reservation 选择有 contract fixture；缺 usage/tokenizer 数据时使用明确保守 fallback；相关 routing tests 通过。结果：routing pure planner 新增 TPM reservation estimate contract，优先使用 total/prompt/completion/max completion 数值估算，缺 usage/tokenizer 时走 conservative fallback，并用 fixture/tests 锁定 secret-safe 输出。剩余缺口：Gateway runtime 尚未接入 tokenizer/usage 输入。
- **E8-004-S7，已完成，2026-06-04，owner Mencius**：TPM reservation estimate edge-case hardening。原因：S6 已定义 routing TPM estimate contract，后续 Gateway runtime 接线前需要锁定极端大数、最小 1-token clamp、negative sanitization 与 `i64` capacity clamp，避免 runtime 输入导致过度乐观或溢出。范围：`crates/routing/**`、routing fixtures/tests；不改 Gateway/Control Plane/UI/scripts。验收：新增/强化 edge-case tests 或 fixture，覆盖 overflow/minimum/negative fallback；routing tests 通过。结果：routing TPM estimate 增加 zero/negative/extreme edge-case contract，锁定最小 1-token clamp、负数 sanitize 后保守 fallback，以及 `i64::MAX` capacity clamp，fixture 同步记录安全输出。剩余缺口：Gateway runtime 尚未把实际 tokenizer/usage/fallback 配置接入该估算输入。
- **E8-004-S8，已完成，2026-06-04，owner Mencius**：TPM estimate 到 reservation capacity 的 runtime bridge contract。原因：S6/S7 已锁定估算本身，但 Gateway runtime 接线前还需要证明 `RateLimitTpmReservationEstimate` 转成 `RateLimitRequiredCapacity` 与 reservation plan 时不会丢失 clamp/保守 fallback 语义，也不会把 payload/window/provider secret material 写进输出。范围：`crates/routing/**`、routing fixtures/tests；不改 Gateway/Control Plane/UI/scripts。验收：新增或强化 fixture/test，覆盖 estimate source 到 `tokens_per_minute` required capacity 的转换、`required_tokens_i64` clamp、reservation dimension 使用的 TPM required 值、secret-safe serialized output；routing tests 通过。结果：新增 estimate -> required capacity -> reservation plan bridge contract，覆盖 total/prompt+completion/max-completion/fallback/partial source、`i64::MAX` clamp、TPM dimension required 值与 secret-safe serialized output，fixture 增加 `capacity_bridge_behaviors`。剩余缺口：Gateway runtime 仍需把 tokenizer/usage/fallback 配置接入 `RateLimitTpmEstimateInput`。
- **E8-004-S9，已完成，2026-06-04，owner Mencius**：Gateway TPM runtime wiring 设计/验收文档。原因：routing 纯 contract 已完成，但 Gateway 代码当前由 Prompt Protection native spy 切片占用；进入接线前需要先固定 endpoint/request token 信号到 `RateLimitTpmEstimateInput` 的映射、fallback 策略和验收测试清单，避免后续实现时和 prompt-protection side-effect ordering 混淆。范围：`docs/**`；不改 Gateway/Control Plane/UI/routing/scripts。验收：文档列出 OpenAI chat/responses、Anthropic、Gemini native 的可用 token/max-token 字段、缺失时 fallback、负数/大数处理、reservation timing、secret-safe logging/test plan；明确下一代码切片的文件边界和退出条件。结果：新增 `docs/E8-004_GATEWAY_TPM_RUNTIME_WIRING.md`，固定 OpenAI chat/responses、Anthropic、Gemini native 的 request-local token/max-output 映射、conservative fallback、负数/大数/0-token 处理、prompt-protection reject 与 reservation acquire ordering、secret-safe runtime metadata 白名单和下一代码切片退出条件。剩余缺口：Gateway runtime 代码尚未接入共享 TPM estimate mapper/reservation summary。
- **E8-004-S10，已完成，2026-06-04，owner Mencius**：Gateway TPM estimate mapper code contract。原因：S9 已固定设计，下一步先实现共享 mapper 和 DB-free endpoint mapping tests，再接 reservation acquire side effects，降低与 prompt-protection/provider-attempt ordering 的耦合风险。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：共享 helper 将 OpenAI chat/responses、Anthropic messages、Gemini native 的 request max-output/token signals 映射到 `RateLimitTpmEstimateInput` 或等价 safe summary；覆盖 missing/negative/large/fallback 行为；不执行 reservation acquire/provider side effects；targeted Gateway tests 与 diff check 通过。结果：新增 DB-free `tpm_estimate` mapper，覆盖 OpenAI chat `max_completion_tokens/max_tokens`、OpenAI Responses `max_output_tokens`、Anthropic `max_tokens`、Gemini native `generationConfig.maxOutputTokens` 到 routing TPM estimate 的映射；fixture 和单测锁定 missing tokenizer、partial fallback、negative sanitize、大整数 clamp、0-token minimum clamp，以及 safe summary 不泄露 raw prompt/body/header/provider secret。剩余缺口：mapper 尚未接入 runtime reservation acquire；真实 tokenizer/prompt-token estimate 仍未实现，当前只提供缺 tokenizer 时的保守 fallback contract。
- **E8-004-S11，已完成，2026-06-04，owner Mencius**：Gateway TPM estimate mapper runtime reservation bridge。原因：S10 已有 DB-free endpoint mapper，但 Gateway reservation acquire/release 仍使用固定 fallback token capacity；下一步先把 mapper 输出桥接到 request-local reservation required capacity 和 safe metadata，再逐步覆盖更多 endpoint/tokenizer signals。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：OpenAI chat/responses 至少使用 request max-output + conservative fallback 计算 TPM required capacity，并传入 acquire/release plan 与 DB execution required capacity；reservation metadata 展示 secret-safe TPM estimate summary；prompt-protection reject 仍在 reservation/provider side effect 前；不回显 raw prompt/body/header/provider secret；targeted Gateway tests、fmt check 和 diff check 通过。结果：OpenAI chat/responses 在 prompt-protection 后、provider attempt/provider key/upstream 前生成 TPM estimate，non-stream 和 streaming attempt loop 均将 `required_tokens_i64` 传入 acquire/release plan 与 DB required capacity；reservation metadata 增加 secret-safe `tpm_estimate` source/numeric summary，fixture 固定 partial fallback、missing max fallback 和 forbidden marker。剩余缺口：Anthropic/Gemini/embeddings 尚未接 runtime TPM mapper；当前 bridge 会从 bounded request body 再解析 JSON，后续可减少重复解析。
- **E8-004-S12，已完成，2026-06-04，owner Mencius**：Gateway TPM runtime bridge for native providers。原因：S11 已覆盖 OpenAI chat/responses，但 Gateway native provider paths 仍沿用旧固定 fallback capacity；最终 TPM reservation 需要 Anthropic Messages 和 Gemini native 的 max-output signals 也进入同一 dynamic capacity/metadata 边界。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：Anthropic `/v1/messages` 和 Gemini native generate/stream 至少使用 request max-output + conservative fallback 计算 TPM required capacity，并传入 acquire/release plan 与 DB execution required capacity；prompt-protection reject ordering 保持在 reservation/provider side effect 前；metadata secret-safe；targeted Gateway tests、fmt check 和 diff check 通过。结果：Anthropic Messages non-stream/stream 使用 `max_tokens + 1024` conservative fallback，Gemini native generate/stream 使用 `generationConfig.maxOutputTokens + 1024` conservative fallback，并将估算结果传入 acquire/release plan、DB required capacity 和 secret-safe reservation metadata；prompt-protection reject 仍早于 reservation/provider side effect。剩余缺口：embeddings 仍走旧 fixed fallback；真实 tokenizer/prompt token 信号和 live DB/provider e2e 仍未接入。
- **E8-004-S13，已完成，2026-06-04，owner Mencius**：Gateway TPM runtime bridge for embeddings fallback boundary。原因：S11/S12 已覆盖 OpenAI chat/responses、Anthropic Messages 和 Gemini native，但 embeddings 路径仍沿用旧固定 TPM fallback；E8 最终需要所有 Gateway rate-limitable endpoints 至少进入同一 safe TPM estimate metadata/capacity 边界。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：embeddings 请求在 prompt-protection 通过后、reservation/provider side effect 前生成 secret-safe TPM estimate；缺 tokenizer/usage 时使用明确 conservative fallback，输入数组/字符串不回显；estimate 传入 reservation acquire/release plan 与 DB required capacity；targeted Gateway tests、fmt check 和 diff check 通过。结果：新增 `OpenAiEmbeddings` TPM estimate endpoint；embeddings 在 prompt-protection 通过后、reservation/provider side effect 前生成 conservative fallback estimate，并将该 estimate 传入 reservation acquire/release plan 与 DB required capacity；metadata 只保留 endpoint/source/numeric summary，不输出 raw input/body/header/provider secret/current window。剩余缺口：embeddings 仍未接入 tokenizer 或可信 input-token 信号；本轮仍是 Gateway contract/unit 覆盖，未做 live DB/provider e2e。
- **E8-004-S14，已完成，2026-06-04，owner Mencius**：Gateway TPM trusted input-token signal boundary。原因：S11-S13 已让主要 Gateway endpoints 都进入 TPM estimate/capacity 边界，但 estimate 仍主要依赖 max-output 和 conservative fallback；后续需要先固定可信 input-token/tokenizer 信号如何进入 mapper，避免直接从 raw prompt/input 推断并泄露内容。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：新增或强化 DB-free mapper/summary contract，覆盖 trusted prompt/input token signal 存在、缺失、负数、溢出和 fallback；secret-safe summary 只输出 numeric/source，不输出 raw prompt/input；不改变 runtime provider side-effect ordering；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `GatewayTpmEstimateSignals::trusted_prompt_tokens` 与 `trusted_input_tokens`，固定 trusted prompt+max-output、input-only total tokens、缺失/负数/overflow/invalid fallback 行为；summary 仍只输出 numeric/source，不接收或输出 raw prompt/input/body/header/provider secret。剩余缺口：runtime 仍未接真实 tokenizer/input-token provider；后续接线必须只传可信数字结果，不能从 raw prompt/input 做 ad hoc 推断。
- **E8-004-S15，已完成，2026-06-04，owner Mencius**：Gateway TPM runtime trusted-signal source guard。原因：S14 已开放 trusted numeric signal API，但当前 runtime 仍应只传 missing-tokenizer fallback，直到有可信 tokenizer/read-model 数字来源；需要用 source-order/contract 测试防止 runtime 从 raw prompt/input 长度或内容做临时估算。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：新增或强化 runtime/source-order contract，证明 OpenAI chat/responses/embeddings、Anthropic、Gemini native 只传可信 numeric signal 或 explicit missing-tokenizer fallback；禁止 raw prompt/input/body string length 估算；metadata secret-safe；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 runtime/source-order guard，覆盖 OpenAI chat/responses/embeddings、Anthropic Messages 和 Gemini native，证明当前 runtime TPM estimate 只使用 `missing_tokenizer` 与 bounded conservative fallback，禁止 `trusted_*` helper、`new(...)` 和 raw prompt/input 长度/内容估算模式。剩余缺口：trusted numeric signal API 仍未接真实 tokenizer/read-model 来源；后续接线需更新该 guard。
- **E8-004-S16，已完成，2026-06-04，owner Mencius**：Gateway TPM trusted signal runtime wiring checklist。原因：S14/S15 已固定 trusted numeric signal API 和 runtime guard；真正接 tokenizer/read-model 前，需要列清每个 endpoint 可接受的可信 token 来源、禁止来源和退出条件，避免实现时重新打开 raw prompt/input 推断。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：新增或强化 Gateway-local checklist/fixture/test，按 endpoint 列出 trusted signal allowed sources、current missing-tokenizer status、future wiring exit condition 和 forbidden raw-material sources；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `trusted_signal_runtime_wiring_checklist` fixture/test，覆盖 OpenAI chat/responses/embeddings、Anthropic Messages、Gemini native 的 current missing-tokenizer status、allowed trusted numeric sources、future wiring exit condition、forbidden raw-material sources 和 no provider side-effect 边界。剩余缺口：仍未接真实 tokenizer/read-model numeric source。
- **E8-004-S17，已完成，2026-06-04，owner Mencius**：Gateway TPM trusted signal smoke handoff contract。原因：S16 已有 endpoint checklist；后续真正接 trusted tokenizer 前，需要给 smoke/acceptance 一个稳定 handoff contract，说明哪些 endpoint 仍 fallback、哪些证据能证明未来 trusted source 已接入。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：新增或强化 Gateway fixture/test，输出 per-endpoint handoff status、required evidence fields、forbidden evidence fields 和 live smoke closure conditions；不改变 runtime behavior；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `gateway_tpm_trusted_signal_smoke_handoff_v1` fixture/test，按 OpenAI chat/responses/embeddings、Anthropic Messages、Gemini native 输出当前 `fallback_missing_tokenizer` 状态、未来 tokenizer/read-model allowed sources、required evidence fields、forbidden raw-material fields 和 live smoke closure conditions；不改变 runtime behavior。剩余缺口：仍未接真实 trusted tokenizer/read-model numeric source；后续 live smoke 还需用该 handoff contract 证明 trusted signal 已在 reservation acquire 前接入且无 raw material 输出。
- **E8-004-S18，已完成，2026-06-04，owner Mencius**：Gateway TPM trusted signal smoke evidence projection contract。原因：S17 已定义 smoke/acceptance 需要采集的证据字段，但后续 smoke 仍需要 Gateway-local projection contract，证明现有 secret-safe TPM/reservation metadata 能稳定映射到这些证据字段，而不是依赖可见日志文案或 raw payload。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：新增或强化 Gateway fixture/test，按 endpoint 生成或验证 smoke evidence projection，覆盖 `tpm_estimate.*`、`required_capacity.tokens_per_minute`、`acquire.dimensions.tpm.required`、`db_required_capacity.tokens_per_minute` 和 `trusted_signal.*` 当前 fallback 状态；证明 capacity 字段一致、forbidden raw material 不出现；不改变 runtime behavior；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `gateway_tpm_trusted_signal_smoke_evidence_projection_v1` fixture contract 和 Gateway-local regression，覆盖 OpenAI chat/responses/embeddings、Anthropic Messages、Gemini native；证明 `tpm_estimate.required_tokens_i64`、reservation required capacity、acquire TPM required 和 DB required capacity 一致，projection evidence 只含 endpoint/source/numeric/status 字段且不泄露 raw prompt/input/body/header/provider/current-window material。剩余缺口：仍未接真实 trusted tokenizer/read-model numeric source；后续 live smoke 需要采集同形 evidence。
- **E8-004-S19，已完成，2026-06-04，owner Mencius**：Gateway TPM trusted numeric source availability contract。原因：S18 已证明 smoke evidence projection 稳定，但 Gateway runtime 仍处于 `missing_tokenizer` fallback；本切片锁定 trusted numeric source 的可用性边界，明确当前没有 tokenizer/read-model 时为何必须 fallback，以及未来 source 接入前不能使用 raw prompt/input 长度估算。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：新增或强化 Gateway fixture/test，覆盖 trusted numeric source available/unavailable/invalid 状态、source type whitelist（tokenizer/read-model）、numeric-only material policy、unavailable 时继续 conservative fallback、forbidden raw prompt/input/body/header/provider material；不改变 provider side-effect ordering；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `gateway_tpm_trusted_numeric_source_availability_v1` contract，区分 available/unavailable/invalid trusted numeric source，只允许 tokenizer/read-model numeric token material，并证明 request body/raw prompt/raw input/header/provider/body length 等 raw material 不能进入 TPM estimate；当前 runtime 仍保持 missing-tokenizer conservative fallback。剩余缺口：helper 尚未接入 runtime adapter；下一片需要证明 runtime 不会旁路 helper 或意外启用 raw source。
- **E8-004-S20，已完成，2026-06-04，owner Mencius**：Gateway TPM trusted numeric source runtime no-op integration guard。原因：S19 已提供 trusted numeric source helper，但 runtime 当前仍应只走 `missing_tokenizer` fallback；本切片把“尚未接入 tokenizer/read-model adapter 时不启用 trusted source”的运行时边界写清，避免后续接线前出现隐式 raw source 或 bypass helper。范围：`apps/gateway/**`、必要 Gateway fixtures/tests；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：新增或强化 runtime/source-order tests，证明当前 runtime 没有 tokenizer/read-model adapter 时仍只输出 missing-tokenizer fallback，trusted helper 不被 bypass，raw prompt/body/input/header/provider material 不进入 estimate summary；保留 provider side-effect ordering；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `gateway_tpm_trusted_numeric_source_runtime_noop_guard_v1` fixture/test，覆盖 OpenAI chat/responses/embeddings、Anthropic、Gemini native 当前仍使用 `GatewayTpmEstimateSignals::missing_tokenizer` 和 conservative fallback，且不调用 trusted source helper 或 raw length/tokenize bypass；safe summary 测试证明 raw prompt/input/body/header/provider/current-window material 不泄露。剩余缺口：tokenizer/read-model trusted numeric source 仍未接入 runtime；后续接入必须走 S19 helper/contract。
- **E8-004-S21 Performance，已完成，2026-06-04，owner Mencius**：Gateway rate-limit reservation performance smoke preflight contract。原因：E8 当前 contract 很多，但高性能目标需要把 live/preflight smoke 的并发、latency/TTFT、NotApplied/fallback、acquire/release 证据写成可验收输出，避免只证明“会跑”而不证明“高性能路径可观测”。范围：`scripts/verify_gateway_rate_limit_reservation_smoke.ps1`、必要 Gateway smoke fixtures/tests；不改 Control Plane/Admin UI/billing-ledger。验收：默认不跑 live；dry-run/preflight 输出 bounded performance evidence schema，包含 concurrency/latency/TTFT 或明确 unavailable marker、NotApplied/fallback rate、acquire/release count、secret-safe command summary；Docker/Postgres/Gateway 缺失时输出 blocker 而非假通过；相关 script self-test/default check 通过。结果：新增 `gateway_rate_limit_reservation_performance_evidence_v1` contract，dry-run 和 preflight 输出 bounded performance evidence，覆盖 concurrency、latency/TTFT、NotApplied/fallback rate、acquire/release expected counts、secret-safe command summary 和 blocker status；当前 Docker/Postgres/Gateway 不可用时输出 `status=blocked` 或 unavailable marker，不伪造 live latency/observed counts。剩余缺口：真实 live Postgres/Gateway smoke 仍未执行，实际 latency/TTFT/observed counts 仍需 live 环境采集，E8 不能标 `[x]`。
- **E8-004-S22 Runtime，已完成，2026-06-04，owner Mencius**：Gateway TPM trusted numeric source adapter boundary。原因：S19-S20 已证明 runtime 不能旁路 trusted source helper，S21 已补 smoke performance evidence；下一片需要为真实 tokenizer/read-model numeric source adapter 固定安全接入边界，避免后续直接用 raw prompt/body 长度估算。范围：`apps/gateway/**`、必要 Gateway fixtures/tests；不改 Control Plane/Admin UI/billing-ledger/scripts。验收：新增或强化 adapter boundary tests，覆盖 trusted source adapter unavailable/available/invalid、source type whitelist、numeric-only material、raw prompt/body/header/provider/window material omitted、fallback 保守且不 bypass helper、provider side-effect ordering 不变；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `GatewayTrustedNumericSourceAdapterOutput` 和 `gateway_trusted_numeric_source_availability_from_adapter(...)`，固定未来 tokenizer/read-model adapter 的 numeric-only 输出边界；fixture/test 覆盖 adapter unavailable、available tokenizer prompt、available read-model input、negative tokens invalid、raw source rejected，并证明 runtime 当前仍未接入 adapter，prompt-protection before estimate、estimate before reservation 顺序不变。剩余缺口：真实 tokenizer/read-model implementation 未接入，live smoke 仍需后续采集真实 trusted numeric source evidence。
- **E8-004-S23 Runtime，已完成，2026-06-04，owner Mencius**：Gateway tokenizer/read-model adapter readiness guard。原因：S22 已固定 adapter boundary，下一片需要在 runtime 接线前明确哪些 tokenizer/read-model provider 可用、缺失时如何 fallback、以及 adapter readiness evidence 如何进入 smoke report。范围：`apps/gateway/**`、必要 Gateway fixtures/tests；不改 Control Plane/Admin UI/billing-ledger/scripts。验收：新增或强化 readiness guard/tests，覆盖 tokenizer/read-model unavailable marker、adapter config disabled-by-default、numeric source feature availability marker、fallback conservative、safe summary、no raw material、runtime ordering 不变；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `gateway_tpm_trusted_numeric_source_readiness_v1` readiness guard，固定 tokenizer/read-model `disabled/unavailable/available` 状态、`feature_available`、`fallback_required`、`material_in_output=false` 和 smoke evidence projection；readiness disabled/unavailable 时强制走 `missing_tokenizer` conservative fallback，即使 adapter availability 有数字也不进入 runtime；runtime source-order guard 证明当前请求路径没有接入 readiness/adapter helper、没有从 raw prompt/body/input/header/provider/window material 估算 token，prompt-protection 仍早于 reservation/provider side effect。剩余缺口：真实 tokenizer/read-model runtime implementation、配置接入和 live trusted numeric source evidence 仍需后续验收。
- **E8-004-S24 Runtime，已完成，2026-06-04，owner Mencius**：Gateway trusted tokenizer/read-model config preflight gate。原因：S23 已固定 readiness guard，但真正 runtime 接线前还缺配置 preflight，明确哪些 env/config 可以启用 tokenizer/read-model trusted numeric source、默认禁用如何输出 blocker、以及启用但 provider unavailable 时如何继续 fallback。范围：`apps/gateway/**`、必要 Gateway fixtures/tests；不改 Control Plane/Admin UI/billing-ledger/scripts。验收：新增或强化 config/preflight contract，覆盖 default disabled、enabled-but-unavailable、available-ready 三类状态；输出 secret-safe readiness/performance evidence 字段，不回显 raw prompt/input/header/provider secret；不改变 provider side-effect ordering；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `gateway_tpm_trusted_numeric_source_config_preflight_v1` contract，覆盖 default disabled、tokenizer/read-model enabled-but-provider-unavailable、tokenizer/read-model ready 三态；输出 `feature_enabled`、`feature_available`、`fallback_required`、blocker、availability marker 和 duration marker 等 secret-safe evidence；runtime source-order guard 证明当前请求路径未接入真实 tokenizer/read-model config/preflight helper，prompt-protection 仍早于 estimate/reservation/provider side effect。剩余缺口：真实 tokenizer/read-model implementation、配置读取接入和 live smoke evidence 仍未实现。
- **E8-004-S25 Runtime，已完成，2026-06-04，owner Mencius**：Gateway trusted numeric source opt-in runtime evidence gate。原因：S24 已固定 config preflight；下一片需要定义显式 opt-in runtime evidence gate，说明未来启用 tokenizer/read-model 后如何在不泄露 raw prompt/input 的前提下证明 trusted numeric source 在 reservation 前生效，并输出高性能 evidence 名称。范围：`apps/gateway/**`、必要 Gateway fixtures/tests；不改 Control Plane/Admin UI/billing-ledger/scripts/E11/E13。验收：新增或强化 opt-in evidence contract，覆盖 disabled/blocker/ready 三态到 reservation evidence 的映射、duration/availability/source/token count marker 名称、forbidden raw material、prompt-protection before estimate before reservation ordering；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `gateway_tpm_trusted_numeric_source_opt_in_evidence_v1` evidence gate，覆盖 disabled、blocked、ready、ready-but-capacity-misaligned 四态；safe summary 输出 availability/preflight/estimate duration marker、source/token-count marker、TPM estimate/acquire/DB required capacity 对齐状态、reservation evidence readiness 和 live gap closure readiness；runtime ordering guard 证明当前没有把 tokenizer/read-model evidence 直接接入生产路径，也不从 raw prompt/input/body length 推断 token。剩余缺口：真实 tokenizer/read-model implementation、reservation evidence runtime projection 和 live DB/provider smoke 仍需后续验收。
- **E8-004-S26 Runtime，已完成，2026-06-04，owner Mencius**：Gateway trusted numeric source reservation evidence projection handoff。原因：S25 已定义 opt-in evidence gate，但最终 E8 `[x]` 还需要证明未来 trusted tokenizer/read-model 结果进入 reservation/smoke evidence 的字段、顺序和性能摘要；下一片先固定 projection handoff，避免 runtime implementation 后 evidence 丢失或泄露 raw prompt/input。范围：`apps/gateway/**`、必要 Gateway fixtures/tests；不改 Control Plane/Admin UI/billing-ledger/scripts/E11/E13。验收：新增或强化 reservation evidence projection contract，覆盖 trusted_source_evidence 字段进入 rate-limit reservation metadata/smoke evidence 的 blocked/ready/unavailable 三态、availability/preflight/estimate duration markers、source/token-count marker、capacity alignment、secret-safe omission、prompt-protection allow before estimate before reservation acquire ordering；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 trusted source reservation/smoke evidence projection handoff，固定 `rate_limit_reservation.trusted_source_evidence` 与 `smoke.rate_limit_reservation.trusted_source_evidence` 字段，覆盖 unavailable/blocked/ready projection、availability/preflight/estimate duration markers、source/token-count marker、capacity alignment、performance markers present 和 secret-safe material omission；runtime ordering guard 继续证明 prompt-protection allow 在 estimate 前、estimate 在 reservation acquire/provider side effect 前。剩余缺口：真实 tokenizer/read-model implementation、runtime evidence 写入和 live DB/provider smoke 仍需后续验收。
- **E8-004-S27 Runtime，已完成，2026-06-04，owner Mencius**：Gateway trusted numeric source runtime adapter boundary。原因：S26 已固定 projection handoff，下一步需要定义真实 tokenizer/read-model runtime adapter 的安全边界和 opt-in wiring guard，开始从 contract-only 走向 implementation，但仍禁止 raw prompt/input ad hoc 估算。范围：`apps/gateway/**`、必要 Gateway fixtures/tests；不改 Control Plane/Admin UI/billing-ledger/scripts/E11/E13。验收：新增或强化 runtime adapter boundary，覆盖 adapter trait/input/output、disabled/blocker/ready 三态、duration/availability/source/token-count evidence、raw material forbidden markers、no provider side effect、prompt-protection allow before adapter before reservation acquire ordering；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `GatewayTrustedNumericSourceRuntimeAdapter` trait、`GatewayTrustedNumericSourceRuntimeAdapterInput`/`Output`、`gateway_trusted_numeric_source_runtime_adapter_boundary` 和 safe summary，固定 disabled/blocked 跳过 adapter、ready 调用 adapter、adapter unavailable/material/provider-side-effect 时 blocker fallback；summary 只输出 endpoint/preflight/availability/source/token-count/adapter_invoked/fallback_required/conservative fallback 与 marker 名称，不接受 raw prompt/input/header/body/window/provider material；runtime ordering guard 继续证明当前生产路径没有接入 trusted adapter，也不从 `.len()`/`.chars()`/`.bytes()`/tokenize/raw material 推断 tokens。验证：`cargo test -p ai-gateway tpm_estimate -- --nocapture`、`cargo test -p ai-gateway rate_limit_reservation -- --nocapture`、`cargo test -p ai-gateway rate_limit -- --nocapture`、`cargo test -p ai-gateway prompt_protection -- --nocapture`、`cargo fmt -p ai-gateway --check` 和 scoped diff check 通过。剩余缺口：真实 tokenizer/read-model provider implementation、runtime wiring、live DB/provider smoke 仍需后续验收。
- **E8-004-S28 Runtime，已完成，2026-06-04，owner Mencius**：Gateway trusted numeric source runtime config wiring guard。原因：S27 已有 adapter trait/boundary，但生产路径仍没有配置化入口决定何时允许调用 adapter；下一片需要固定 runtime config/env wiring guard，确保未来开启 tokenizer/read-model 时必须经过显式 opt-in、preflight ready、secret-safe evidence 和 prompt-protection-before-adapter ordering。范围：`apps/gateway/**`、必要 Gateway fixtures/tests；不改 Control Plane/Admin UI/billing-ledger/scripts/E11/E13。验收：新增或强化 runtime config wiring guard，覆盖 env/config disabled default、tokenizer/read-model opt-in flag/env、provider unavailable blocker、adapter not wired blocker、ready-but-no-raw-material input、availability/preflight/estimate duration marker projection、no provider side effect、prompt-protection allow before config/preflight before adapter before reservation acquire ordering；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `GatewayTrustedNumericSourceRuntimeConfigInput`、`GatewayTrustedNumericSourceRuntimeConfigGuard` 和 `gateway_trusted_numeric_source_runtime_config_guard(...)`，固定 `GATEWAY_TPM_TRUSTED_TOKENIZER_ENABLED`/`GATEWAY_TPM_TRUSTED_READ_MODEL_ENABLED` 显式 opt-in、disabled/blocked/ready 状态、`opt_in_missing`/`adapter_unavailable` blocker、adapter invocation/fallback guard、preflight projection，以及 availability/preflight/estimate duration/source/token-count marker 的 secret-safe summary；runtime ordering guard 证明当前生产路径仍不调用 trusted adapter，不从 `.len()`/`.chars()`/`.bytes()`/tokenize/raw prompt/input/body/header/provider/window material 推断 tokens，prompt-protection 仍早于 estimate/reservation/provider side effect。剩余缺口：真实 tokenizer/read-model provider implementation、production env/config 读取接线、runtime evidence 写入、live DB/provider smoke 仍未完成。
- **E8-004-S29 Runtime，已完成，2026-06-04，owner Mencius**：Gateway trusted numeric source env/config provider read boundary。原因：S28 已固定 runtime config guard，但还没有证明生产 env/config 读取如何进入该 guard、默认禁用如何保持 fallback、非法/secret-like 值如何脱敏拒绝；下一片只固定 env/config provider read boundary，不接真实 tokenizer/read-model provider。范围：`apps/gateway/**`、必要 Gateway fixtures/tests；不改 Control Plane/Admin UI/billing-ledger/scripts/E11/E13。验收：新增或强化 env/config provider boundary，覆盖 missing/blank/false 默认 disabled、true opt-in、invalid value refusal、tokenizer/read-model adapter availability marker、raw env/config value omitted、secret-like value 不回显、guard safe summary 继续输出 availability/preflight/estimate duration/source/token-count marker；生产 request path 仍不调用 adapter、不改变 provider side-effect ordering；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `GatewayTrustedNumericSourceEnvConfigInput`、`GatewayTrustedNumericSourceEnvConfigRead` 和 `gateway_trusted_numeric_source_env_config_read(...)`，固定 missing/blank/false 默认 disabled、true opt-in、invalid/secret-like value refused 且 raw value omitted；safe summary 继续投影 adapter availability、fallback、availability/preflight/estimate duration/source/token-count marker；runtime ordering guard 证明生产 request path 仍未调用 adapter、不从 raw prompt/body/input/header/provider/window material 推断 tokens，provider side-effect ordering 未改变。剩余缺口：真实 tokenizer/read-model implementation、runtime evidence 写入和 live DB/provider smoke 仍需后续验收。
- **E8-004-S30 Runtime，已完成，2026-06-04，owner Mencius**：Gateway trusted numeric source runtime evidence projection boundary。原因：S29 已固定 env/config provider read boundary，但进度条要继续向 `[x]` 靠近，需要证明 env/config read + trusted numeric source guard 的 safe summary 如何投影到 reservation evidence/performance summary，而不是只停留在配置读取。范围：`apps/gateway/**`、必要 `tests/fixtures/gateway/**`；不改 Control Plane/Admin UI/billing-ledger/scripts/E11/E13。验收：新增或强化 runtime evidence projection boundary，覆盖 disabled/blocker/ready/ready-but-not-used 四态到 runtime evidence summary 的映射；evidence 包含 availability/preflight/estimate duration marker、source/token-count marker、reservation acquire readiness、live gap closure marker；继续证明 prompt-protection before trusted numeric source before reservation/provider side effect ordering；不输出 raw prompt/input/body/header/provider/window/env/config secret；targeted Gateway tests、fmt 和 scoped diff check 通过。结果：新增 `gateway_tpm_trusted_numeric_source_runtime_evidence_projection_v1`、`GatewayTrustedNumericSourceRuntimeEvidenceProjection` 和 safe summary，固定 disabled/blocker/ready/ready_but_not_used 四态到 env_config/adapter/reservation_projection evidence 的投影；summary 输出 availability/preflight/estimate duration marker、source/token-count marker、reservation_acquire_ready、performance_markers_present 与 `gateway_tpm_trusted_numeric_source_live_gap_closure_ready`，并通过 runtime ordering regression 证明生产 request path 默认仍不调用 adapter、不写 runtime evidence、不从 raw prompt/body/input/header 推断 token，prompt-protection 仍早于 TPM estimate/reservation/provider side effect。验证：`cargo test -p ai-gateway tpm_estimate -- --nocapture`、`cargo test -p ai-gateway rate_limit_reservation -- --nocapture`、`cargo fmt -p ai-gateway --check` 和 `git diff --check -- apps/gateway tests/fixtures/gateway` 通过。剩余缺口：真实 tokenizer/read-model implementation、runtime evidence 写入和 live DB/Gateway smoke 仍需后续验收。
- **E8-004-S31 Runtime，已完成，2026-06-04，owner Mencius**：Gateway trusted numeric source runtime evidence write/read opt-in contract。原因：S30 已能把 trusted numeric source guard 投影到 reservation/performance summary，但最终 `[x]` 还需要一个 repo-bounded runtime evidence artifact 写入/读回边界，证明未来真实 tokenizer/read-model 数字来源生效后能被安全记录、读回并拒绝 stale/simulated/missing evidence。范围：`apps/gateway/**`、必要 `tests/fixtures/gateway/**`；不改 Control Plane/Admin UI/billing-ledger/scripts/E11/E13。验收：默认不写 artifact、不调用真实 tokenizer、不改 provider side effect；显式 test-only/opt-in path 才写入 repo 内 `.tmp/**` 或 fixture-safe artifact；readback 校验 schema/current commit/generated_at/source/token-count marker/availability/preflight/estimate duration/reservation acquire readiness；stale/simulated/missing duration/missing source -> blocker；raw prompt/input/body/header/provider/window/env/config secret 不输出；targeted Gateway tests、fmt 和 scoped diff check 通过。结果：新增 `gateway_tpm_trusted_numeric_source_runtime_evidence_artifact_v1` helper/contract，支持 opt-in 写入与读回 `.tmp/**` runtime evidence artifact；readback 校验 schema/current commit/generated_at、source/token-count marker、duration markers、reservation acquire readiness 和 live gap closure marker，stale/simulated/missing duration/missing source 均 blocker；生产 runtime ordering regression 证明默认路径不写/读 artifact、不调用真实 tokenizer、不改变 prompt-protection -> estimate -> reservation/provider side effect 顺序。验证：`cargo test -p ai-gateway tpm_estimate -- --nocapture`、`cargo test -p ai-gateway rate_limit_reservation -- --nocapture`、`cargo fmt -p ai-gateway --check`、scoped diff check 通过，`.tmp/gateway_tpm_runtime_evidence_artifact*_test.json` 无残留。剩余缺口：真实 tokenizer/read-model implementation、生产 runtime evidence 写入接线、live DB/Gateway smoke 仍需后续验收。
- **E8-004-S32 Runtime，已完成，2026-06-04，owner Mencius**：Gateway trusted numeric source production runtime evidence opt-in wiring guard。原因：S31 已提供 artifact helper/readback，但生产 Gateway request path 仍未有明确 opt-in wiring guard；下一片需要定义在显式 env/config 下如何把 safe runtime evidence 写入 artifact，同时默认仍不写、不调用真实 tokenizer、不影响 provider side effects。范围：`apps/gateway/**`、必要 `tests/fixtures/gateway/**`；不改 Control Plane/Admin UI/billing-ledger/scripts/E11/E13。验收：新增或强化 production runtime opt-in wiring guard，覆盖 disabled/ready/blocker 三态；默认 request path 不写 artifact；显式 env/config + test harness 才允许 write/read safe evidence；写入发生在 prompt-protection 通过后、reservation acquire 前后可解释位置，且不晚于 provider side effect；evidence 包含 source/token-count marker、estimate duration、reservation acquire readiness、live gap closure marker；不输出 raw prompt/input/body/header/provider/window/env/config secret；targeted Gateway tests、fmt 和 scoped diff check 通过。结果：新增 `gateway_tpm_trusted_numeric_source_production_wiring_guard_v1`，覆盖 disabled/blocked/ready 状态、test harness + artifact opt-in + safe path gating、adapter/artifact/readback/reservation evidence allowance 和 secret-safe summary；runtime-ordering regression 证明默认 request path 仍不写 artifact、不调用真实 tokenizer、不改变 prompt-protection -> estimate -> reservation/provider side effect 顺序。验证：`cargo test -p ai-gateway tpm_estimate -- --nocapture`、`cargo test -p ai-gateway rate_limit_reservation -- --nocapture`、`cargo fmt -p ai-gateway --check`、scoped diff check 通过，`.tmp/gateway_tpm_runtime_evidence_artifact*_test.json` 无残留。剩余缺口：真实 tokenizer/read-model provider implementation、生产 runtime evidence 写入接线、live DB/Gateway smoke 仍需后续验收。
- **E8-004-S33 Runtime，已完成，2026-06-04，owner Mencius**：Gateway trusted tokenizer/read-model provider implementation boundary。原因：S32 已固定生产 evidence opt-in wiring guard，但仍没有真实可信数字来源的实现边界；下一片需要把 tokenizer/read-model provider 从“可用性/配置 contract”推进到可调用 runtime provider skeleton，且默认不改变 request path。范围：`apps/gateway/**`、必要 `tests/fixtures/gateway/**`；不改 Control Plane/Admin UI/billing-ledger/E11/E13。验收：新增或强化 trusted tokenizer/read-model provider implementation boundary，覆盖 provider disabled/missing/available、safe token-count return、estimate duration、source marker、negative/overflow/zero clamp、secret-safe error summary；默认 request path 不调用 provider；显式 test/runtime opt-in 才调用 provider skeleton 并投影到 TPM estimate evidence；不得用 raw prompt/input 长度做临时估算；targeted Gateway tests、fmt 和 scoped diff check 通过。结果：新增 `GatewayTrustedNumericSourceProvider` skeleton boundary 与 `gateway_tpm_trusted_numeric_source_provider_boundary_v1` contract，覆盖 disabled/missing/available/error、zero clamp、`i64::MAX` overflow clamp、negative refusal、safe source/token-count/duration markers 和 secret-safe summary；runtime ordering guard 证明默认 request path 不调用 provider、不从 raw prompt/input/body 长度估算 token、不改变 prompt-protection -> TPM estimate -> reservation/provider side-effect 顺序。剩余缺口：真实 provider 尚未接入生产 request path；production runtime evidence 写入与 live Gateway/Postgres smoke 仍需后续验收。
- **E8-004-S34 Runtime/Smoke，已完成，2026-06-04，owner Mencius，加大切片**：Gateway trusted provider request-path opt-in 与 live-smoke handoff。结果：新增 trusted numeric source request-path opt-in handoff，默认不调用 provider、不写 artifact；显式 opt-in 才允许 skeleton provider 写入 repo-bounded evidence。Docker 启动并补跑已有 0009 迁移后，`verify_gateway_rate_limit_reservation_smoke.ps1 -ComposeFile deploy/docker-compose/docker-compose.live.yml` 真实 live 通过，覆盖 acquire counter、fallback release、concurrent NotApplied -> 429 和 secret-safe evidence；同时修复 live smoke SQL `jsonb` window state quoting，并把 live TPM 期望对齐当前 conservative fallback 1024。验证：Gateway targeted tests、live smoke、fmt、diff check 通过。剩余缺口：live performance summary 仍只证明流程通过，尚未沉淀 latency/TTFT/concurrency/row-count 的可比较闭环指标；真实 tokenizer/read-model provider 仍未接入。
- **E8-004-S35 Performance，已完成，2026-06-04，owner Mencius，加大切片**：rate-limit live performance evidence closure。结果：live smoke performance evidence 新增 `measurements_available=true`/`closure_eligible=true`，并输出 gateway latency、request_log/provider_attempt row-count、acquire/release/not_applied/fallback counts、not_applied/fallback rate、concurrency contender count/duration、bounded scope 和 secret-safe summary。验证：E8 live smoke 通过，observed request logs=3、provider attempts=3、acquire=3、release=1、not_applied=1、fallback=1、contender=1；Gateway targeted tests、fmt、diff check 通过。剩余缺口：非流式 smoke 无 TTFT，最终真实 tokenizer/read-model provider 仍未接入。
- **E8-004-S36 Runtime，已完成，2026-06-04，owner Mencius，加大切片**：trusted tokenizer/read-model provider production wiring attempt。原因：E8 rate-limit live/performance 已闭环，最终缺口集中在真实可信 token 数字来源。范围：`apps/gateway/**`、必要 Gateway fixtures/scripts；不改 E9/E11/E13。验收：显式 opt-in 配置下接入真实或可替换 tokenizer/read-model numeric provider，默认仍走 conservative fallback；provider failure/fallback/latency/source markers secret-safe；不得从 raw prompt/input ad hoc 估算；targeted tests/live smoke/fmt/diff check 通过。结果：新增 env-backed replaceable trusted numeric source provider opt-in，读取 `GATEWAY_TPM_TRUSTED_TOKENIZER_*` / `GATEWAY_TPM_TRUSTED_READ_MODEL_*` 数字信号并投影到 `tpm_estimate.trusted_source_provider` safe evidence；missing/illegal/negative provider 值均 fallback，不阻塞请求，不从 raw prompt/input/body/header 做 ad hoc 估算；本轮 live smoke 通过并保持 `closure_eligible=true`。剩余缺口：真实 tokenizer/read-model implementation 与 streaming path 完整生产接入仍未完成。
- **E8-004-S37 Runtime/Streaming，已完成，2026-06-04，owner Mencius，加大切片**：trusted numeric provider streaming closure and concrete implementation boundary。原因：S36 已有 env-backed replaceable provider，但最终 `[x]` 仍卡在 streaming path 与真实/可替换 tokenizer/read-model implementation 边界。范围：`apps/gateway/**`、`tests/fixtures/gateway/**`；不改 E9/E11/E13/Control Plane/Admin UI。结果：新增 `gateway_tpm_trusted_numeric_source_implementation_slot_v1`，锁定 disabled/blocked/ready 实现插槽；OpenAI chat/responses、Anthropic Messages、Gemini native streaming 均要求消费同一个 `GatewayTpmEstimatePlan` 和 trusted provider safe summary 后再进入 reservation acquire；evidence 仅允许 source/token-count/duration/bounded numeric summary，不输出 raw prompt/input/body/header/provider/window/env/config secret。验证：`cargo test -p ai-gateway tpm_estimate -- --nocapture`、`cargo test -p ai-gateway rate_limit_reservation -- --nocapture` 通过。剩余缺口：真实 tokenizer/read-model provider implementation 尚未生产接入。
- **E8-004-S38 Runtime/Tokenizer，已完成，2026-06-04，owner Mencius，加大切片**：trusted tokenizer/read-model implementation。原因：S37 已固定 streaming/implementation slot，下一步需要从 slot 推进到真实或本地可替换 tokenizer/read-model numeric provider。范围：`apps/gateway/src/tpm_estimate.rs`、`apps/gateway/src/main.rs`、`tests/fixtures/gateway/rate_limit_tpm_estimate_mapper_contract.json`；不改 E9/E11/E13。结果：`GatewayTpmEstimatePlan.safe_summary()` 携带 `trusted_source_implementation_slot`；runtime helper 通过 implementation slot 决定是否调用 trusted numeric provider；新增 read-model trusted input-token 可执行路径，显式 opt-in + numeric env 返回 `read_model/input_tokens` 并进入 `TotalTokens` estimate，缺失/非法仍 fallback。验证：`cargo test -p ai-gateway tpm_estimate -- --nocapture`、`cargo test -p ai-gateway rate_limit_reservation -- --nocapture`、fmt/diff check 通过。剩余缺口：仍未接真实 tokenizer/read-model 服务或持久 read-model backend。
- **E8-004-S39 Runtime/Tokenizer，已完成/阻塞收束，2026-06-04，owner Mencius，加大切片**：production tokenizer/read-model backend handoff。原因：S38 关闭了 env-backed executable boundary，但最终 `[x]` 需要真实 tokenizer/read-model 服务或持久 backend 的生产接入证据。范围：`apps/gateway/**`、`tests/fixtures/gateway/**`；不改 E9/E11/E13。结果：新增 `gateway_tpm_trusted_numeric_source_backend_handoff_v1` contract/summary，`GatewayTpmEstimatePlan.safe_summary()` 包含 backend status/kind/source-token kind/availability/latency/source-token markers；runtime TPM helper 通过 backend handoff 控制 provider invocation，默认 disabled/unavailable 继续 fallback；streaming 继续共用同一个 `GatewayTpmEstimatePlan`。验证：Gateway targeted tests/fmt/diff check 通过。剩余缺口：真实 tokenizer/read-model 服务或持久 backend implementation 与 live smoke 尚未接入。
- **E8-004-S40 Runtime/Tokenizer，已完成/阻塞收束，2026-06-05，owner Epicurus**：trusted tokenizer/read-model backend executable preflight/readback boundary。原因：S39 已有 backend handoff marker，但仍缺可执行 backend preflight/readback contract 来拒绝 unavailable/stale/misaligned evidence。范围：`apps/gateway/src/tpm_estimate.rs`、`tests/fixtures/gateway/rate_limit_tpm_estimate_mapper_contract.json`。结果：新增 `GatewayTrustedNumericSourceBackendReadback` trait 与 preflight evidence，固定 disabled/unavailable/ready 三态、readback source/token marker 对齐、duration/source/token-count evidence、closure blocker 与 raw material omission；`GatewayTpmEstimatePlan.safe_summary()` 可携带 optional backend preflight evidence，默认 `None` 且不改变生产 fallback。验证：`cargo test -p ai-gateway tpm_estimate_mapper_trusted_numeric_source_backend_preflight_is_executable_secret_safe -- --nocapture`、`cargo fmt -p ai-gateway --check`、fixture JSON parse、scoped diff check 通过。剩余缺口：真实 tokenizer/read-model 服务或持久 read-model backend 与 live smoke 仍未接入。
- **E8-004-S41 Runtime/Tokenizer，已完成/阻塞收束，2026-06-05，owner Epicurus**：trusted tokenizer/read-model backend adapter slot/readiness contract。原因：S40 固定 preflight/readback gate 后，还需要可替换、可测试、repo-bounded 的 backend adapter implementation slot，避免后续真实 backend 接入时绕过 secret-safe evidence。范围：`apps/gateway/src/tpm_estimate.rs`、`tests/fixtures/gateway/rate_limit_tpm_estimate_mapper_contract.json`。结果：新增 `gateway_trusted_numeric_source_backend_adapter_slot` 与 readiness contract；默认 disabled/fallback，不调用 adapter、不改变生产行为；只有显式 slot ready 且 S40 backend preflight/readback ready、source/token marker 对齐、token_count 存在时，readiness 才能进入 reservation evidence；blocked/unavailable 不能关闭 E8。验证：`cargo test -p ai-gateway trusted_numeric_source_backend -- --nocapture`、full `cargo fmt --check`、fixture JSON parse、diff check 通过。剩余缺口：真实 tokenizer 服务或持久 read-model backend 与 live smoke 仍未接入。
- **E8-004-S42 Runtime/Tokenizer，已完成/阻塞收束，2026-06-05，owner Curie**：repo-bounded file/persistent read-model backend harness proof。原因：S41 固定 adapter slot/readiness contract 后，还需要一个不接外部服务、不读 raw prompt/input 的 repo-bounded implementation proof 来证明 future persistent read-model backend 的 readback/evidence shape。范围：`apps/gateway/src/tpm_estimate.rs`、`tests/fixtures/gateway/rate_limit_tpm_estimate_mapper_contract.json`、`tests/fixtures/gateway/trusted_read_model_backend_harness_ready.json`。结果：新增 file read-model backend harness，默认 disabled/fallback；显式 opt-in 且路径限定 `tests/fixtures/gateway/**` 时读取 source/type/token-count fixture，并输出 source/token-count/preflight/estimate duration/readback evidence，接入 backend preflight/readiness/reservation evidence；fixture 不含 raw input。验证：Gateway backend/tpm/rate_limit_reservation targeted tests、full fmt、fixture JSON parse、diff check 通过。剩余缺口：真实生产 tokenizer/read-model backend 与 live Gateway smoke。
- **E8-004-S43 Runtime/Tokenizer，已完成/阻塞收束，2026-06-05，owner Curie**：file/persistent read-model backend harness opt-in live-smoke/readback command gate。原因：S42 已有 repo-bounded harness proof，但还需要受控命令边界证明 artifact 如何驱动 reservation evidence。结果：将 file/persistent read-model backend harness 推进到 opt-in live-smoke/readback command gate；默认 disabled/fallback，不读 raw prompt/input/body、不接外部服务；显式 opt-in 后只读 `tests/fixtures/gateway/**` repo-bounded artifact，并将 source/token-count/duration evidence 投影到 backend preflight、adapter readiness、TPM required capacity、reservation acquire 和 DB capacity evidence。验证：Gateway targeted backend/tpm/rate_limit tests、Gateway scoped fmt、fixture JSON、scoped diff check 通过。剩余缺口：真实生产 tokenizer/read-model backend 与 live Gateway smoke。
- **E8-004-S44 Runtime/Tokenizer DoD，已完成/阻塞收束，2026-06-05，owner Curie**：production tokenizer/read-model backend final DoD checklist。原因：E8 进入 99% 后，必须明确哪些真实 evidence 能关闭最终缺口。结果：新增 production tokenizer/read-model backend DoD checklist 与 live smoke blocker matrix；最终 `[x]` 必须同时具备真实 backend kind、runtime opt-in、current runtime marker、token count source、latency/duration、reservation capacity projection、DB acquire evidence、secret-safe raw omission、live smoke command/readback；blocked/unavailable/simulated/repo-fixture-only evidence 均不能关闭 E8。验证：Gateway targeted tpm/rate_limit tests、Gateway scoped fmt、fixture JSON、scoped diff check 通过。剩余缺口：真实 production tokenizer/read-model backend 与 live Gateway smoke/readback。
- **E8-004-S45 Production Backend Runner DoD，已完成/阻塞收束，2026-06-05，owner Curie**：production backend runner artifact gate。原因：S44 固定 final DoD 后，还需要机器可读 runner artifact gate 来拒绝伪造或不完整 operator evidence。结果：新增真实 tokenizer/read-model backend runner artifact schema/contract 与 operator evidence gate；覆盖 runner provenance、backend kind、runtime/current commit marker、token source kind/count、duration、reservation projection、DB acquire/result、live smoke command、generated_at/current commit、secret-safe omission。missing backend、stale runtime、repo fixture only、simulated runner、token mismatch、missing DB acquire、missing duration、raw material present、artifact stale 均进入 `production_ready_blocked`；只有 fresh non-simulated non-fixture production artifact 且 runtime/commit/DB/readback 全部对齐才为 `production_backend_live_smoke_passed`。验证：Gateway targeted tpm/rate_limit tests、Gateway scoped fmt、fixture JSON、scoped diff check 通过。剩余缺口：真实 production backend runner artifact 与 live Gateway smoke/readback。
- **E8-004-S46 Production Backend Evidence Runbook，已完成/阻塞收束，2026-06-05，owner Curie**：production backend evidence runbook and opt-in readback command contract。原因：S45 已有 runner artifact gate，但还缺 operator 如何提供真实 evidence 的可复跑 runbook/CLI preflight/readback 边界。结果：新增 `docs/E8-004_PRODUCTION_BACKEND_EVIDENCE_RUNBOOK.md`、`scripts/verify_gateway_tpm_production_backend_evidence.ps1` 与 repo-fixture-only sample；默认脚本只输出 contract，不读 artifact、不接 backend、不读 raw prompt/input/body/header；显式 `-OptInArtifactReadback` 才读取 repo-bounded artifact。repo-fixture-only sample 只能进入 `production_ready_blocked`，不能关闭 E8；真实 closure 仍需 non-simulated production tokenizer/read-model backend runner artifact + live Gateway smoke/readback + DB acquire/result evidence。验证：Gateway tpm/rate_limit tests、script contract/readback/unsafe-path checks、fixture JSON、cargo fmt、diff check 通过。剩余缺口：真实 production backend runner artifact 与 live Gateway smoke/readback。
- **E8-004-S47 Production Backend Evidence Acceptance Matrix，已完成/阻塞收束，2026-06-05，owner Curie**：external production backend runner/live smoke artifact accepted/refused gate。原因：S46 已有 runbook/readback command，但还需要机器判断真实 artifact 是否能从 `production_ready_blocked` 推到 `production_backend_live_smoke_passed`。结果：production backend evidence acceptance matrix/readback gate 已区分 `production_ready_blocked`、`production_evidence_accepted_for_review` 与 `production_backend_live_smoke_passed`，并用 secret-safe artifact fixtures 和 refusal taxonomy 覆盖 missing opt-in、unsafe path、stale/runtime mismatch、repo fixture only、simulated runner、missing live smoke、missing DB result、duration non-numeric 等场景。Accepted external simulation 仍 `final_gap_closure_allowed=false`；只有 production-shaped bounded live-smoke evidence 才进入 `production_backend_live_smoke_passed`。验证：Gateway tpm/rate_limit tests、script readback simulations、fixture JSON、cargo fmt、scoped diff check 通过。剩余缺口：真实 production backend runner artifact 与 live Gateway smoke/readback。
- **E8-004-S48 Production Backend Live Execution Pack，已完成/阻塞收束，2026-06-05，owner Curie**：operator live execution pack。原因：S47 已能判定 artifact，但还需要真实 backend 可用时的一键执行 plan。结果：新增 `gateway_tpm_trusted_numeric_source_production_backend_live_execution_pack_v1`；plan-only 固定 backend runner command、Gateway live smoke command、Postgres/DB acquire readback command、artifact write path `.tmp/gateway_tpm_production_backend/<commit>-<backend-kind>-e8-live-evidence.json`、artifact readback command、cleanup/rollback notes、preflight checklist 与 final E8 `[x]` proof bundle。默认/`-EmitExecutionPack` 只输出 command plan，不执行 live command、不读 artifact、不连 backend、不发网络；显式 `-OptInArtifactReadback` 才读取 bounded artifact。验证：Gateway tpm/rate_limit tests、execution pack command、accepted simulation readback、fixture JSON、cargo fmt、scoped diff check 通过。剩余缺口：真实 production backend runner and live Gateway smoke/readback execution。
- **E8-004-S49 Final Closure Evidence Audit，已完成/阻塞收束，2026-06-05，owner Curie**：final closure evidence audit/report。原因：E8 的 command/acceptance/execution pack 已在位，最终需要单一机器可读 audit 判断当前为何不能 `[x]`。结果：新增 `gateway_tpm_trusted_numeric_source_final_closure_evidence_audit_v1`，输出 `closure_audit`，汇总 artifact acceptance、live Gateway smoke、DB acquire/readback、backend/runtime provenance、secret-safe omission、exact next commands 与 final E8 `[x]` eligibility。No-artifact/default 与 accepted simulation 均 `final_x_eligible=false`；negative artifact 保持具体 blocker；bounded production-shaped temp artifact 可证明 gate shape，但真实 closure 仍要求 real operator evidence。验证：Gateway tpm/rate_limit tests、script audit no-artifact/simulation/negative/production-shaped simulations、fixture JSON、cargo fmt、scoped diff check 通过。剩余缺口：真实 production backend runner and live Gateway smoke/readback execution。
- **E8-004-S50 Production Evidence Watcher And Operator Checklist，已完成/阻塞收束，2026-06-05，owner Curie**：production evidence watcher/checklist。原因：E8 已进入等待真实证据阶段，需要稳定入口接收 operator artifact。结果：新增 `-EmitWatcher` 与 `gateway_tpm_trusted_numeric_source_production_evidence_watcher_v1`；watcher 默认 `production_ready_blocked/waiting_for_production_evidence`、`artifact_read=false`、`polls=false`，只输出 expected artifact paths、required operator actions、exact commands、final review checklist 和 safe defaults。Missing bounded artifact readback 输出 blocked + exact next command，不把缺失 artifact 当最终失败；accepted simulation/template 只能 `production_evidence_accepted_for_review`，`final_x_eligible=false`。验证：Gateway tpm/rate_limit tests、watcher default/missing-artifact/simulation checks、fixture JSON、cargo fmt、scoped diff check 通过。剩余缺口：真实 production backend runner and live Gateway smoke/readback execution。
- **E8-004-S51 Final Guard Consistency Review，已完成/阻塞收束，2026-06-05，owner Curie**：final guard consistency review。原因：E8 已进入等待真实证据阶段，最后风险是状态语义不一致导致误关。结果：补齐 machine-readable final guard flags：`simulation_can_mark_final_x=false`、`template_can_pass=false`、`watcher_can_mark_final_x=false`、`accepted_for_review_can_mark_final_x=false`、`production_shaped_temp_artifact_can_replace_real_evidence=false`；runbook 重申 final `[x]` 只允许真实 production backend runner + live Gateway smoke/readback + DB acquire/readback + secret-safe proof。验证：rg consistency checks、contract-only script、Gateway tpm/rate_limit tests、fixture JSON、cargo fmt、scoped diff check 通过。剩余缺口：真实 production backend runner and live Gateway smoke/readback execution。
- **E8-004-S52 Evidence Wait State Maintenance，已完成/阻塞收束，2026-06-05，owner Curie，no-op review**：E8 evidence wait-state maintenance。原因：E8 只剩真实 operator evidence，需要防止状态漂移和误关。结果：未改文件；复核 watcher/audit/final guard 仍为 `production_ready_blocked`、`final_x_eligible=false`，默认 watcher/contract paths `artifact_read=false`；simulation/template/watcher/accepted-for-review/production-shaped temp artifact replacement guard flags 均为 false。下一步 operator command：`pwsh -File scripts/verify_gateway_tpm_production_backend_evidence.ps1 -EmitExecutionPack -ExpectedCommit <commit> -BackendKind read_model_backend -TokenSourceKind input_tokens`。验证：contract-only script、watcher command、rg final guard flags、fixture JSON parse 通过。剩余缺口：真实 production backend runner and live Gateway smoke/readback execution。
- **E8-004-S53 Wait-State Drift Check，已完成/阻塞收束，2026-06-05，owner Curie，no-op review**：E8 wait-state drift check。原因：其它 lane 并行修改时需要确认 E8 watcher/audit/final guard 未漂移。结果：未改文件；contract 默认仍 `production_ready_blocked`，watcher 仍 `waiting_for_production_evidence`、`artifact_read=false`、`final_x_eligible=false`；final guard flags 仍为 false，E8 fixture JSON 可解析，未发现其它 lane 破坏 E8 watcher/audit/final guard。下一步 operator command：`pwsh -File scripts/verify_gateway_tpm_production_backend_evidence.ps1 -EmitExecutionPack -ExpectedCommit <commit> -BackendKind read_model_backend -TokenSourceKind input_tokens`。验证：contract-only script、watcher command、rg guard flags、fixture JSON parse 通过。剩余缺口：真实 production backend runner and live Gateway smoke/readback execution。
- **E8-004-S54 Evidence Wait-State Final No-Op Review，已完成/阻塞收束，2026-06-05，owner Curie，no-op review**：E8 final wait-state review。原因：确认 E8 后续只等待真实 production evidence，不再新增验收口径。结果：未改文件；`-ContractOnly` 输出 `production_ready_blocked`、`artifact_read=false`、`final_x_eligible=false`；`-EmitWatcher` 输出 `waiting_for_production_evidence`、`polls=false`、`reads_artifact=false`、`artifact_read=false`；未运行 `-OptInArtifactReadback`，没有读取真实 artifact path；final guard flags 未漂移。建议主线程停止继续派 E8 contract 类任务，仅等待真实 production backend runner artifact、live Gateway smoke/readback、DB acquire/readback、secret-safe proof。剩余缺口：真实 production backend runner and live Gateway smoke/readback execution。
- **E8-004-S55 Operator Evidence Intake Watchdog，已完成/阻塞收束，2026-06-05，owner Curie，no-op review**：operator evidence intake watchdog。结果：未改文件；`.tmp/gateway_tpm_production_backend` 下未发现 bounded production evidence JSON artifact；未运行 `-OptInArtifactReadback`，未读取 artifact；watcher 仍 `production_ready_blocked / waiting_for_production_evidence`、`artifact_read=false`、`final_x_eligible=false`；final guard flags 未漂移。剩余缺口：真实 production backend runner + live Gateway smoke/readback + DB acquire/readback + secret-safe proof。
- **E8-004-S56/S57 Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Curie，no-op reviews**：operator evidence intake repeat。结果：未改文件；`.tmp/gateway_tpm_production_backend` 存在但无 JSON evidence artifact；未运行 readback，未读取 artifact；watcher 仍 `production_ready_blocked / waiting_for_production_evidence`、`artifact_read=false`、`final_x_eligible=false`；final guard flags 未漂移。剩余缺口：真实 production backend runner + live Gateway smoke/readback + DB acquire/readback + secret-safe proof。
- **E8-004-S58 Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Curie，no-op review**：operator evidence intake repeat。结果：未改文件；`.tmp\gateway_tpm_production_backend` 下没有 JSON evidence artifact；未运行 readback，未读取 artifact；watcher 仍 `production_ready_blocked / waiting_for_production_evidence`、`artifact_read=false`、`final_x_eligible=false`。剩余缺口：真实 production backend runner + live Gateway smoke/readback + DB acquire/readback + secret-safe proof。
- **E8-004-S59 Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Curie，no-op review**：operator evidence intake repeat。结果：未改文件；`.tmp\gateway_tpm_production_backend` 下没有 JSON evidence artifact；未运行 readback，未读取 artifact；watcher 仍 `production_ready_blocked / waiting_for_production_evidence`、`artifact_read=false`、`final_x_eligible=false`。剩余缺口：真实 production backend runner + live Gateway smoke/readback + DB acquire/readback + secret-safe proof。
- **E8-004-S60 Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Curie，no-op review**：operator evidence intake repeat。结果：未改文件；`.tmp\gateway_tpm_production_backend` 下没有 JSON evidence artifact；未运行 readback，未读取 artifact；watcher 仍 blocked/final=false。剩余缺口：真实 production backend runner + live Gateway smoke/readback + DB acquire/readback + secret-safe proof。
- **E8-004-S61 Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Curie，no-op review**：operator evidence intake repeat。结果：未改文件；`.tmp\gateway_tpm_production_backend` 下没有 JSON evidence artifact；未运行 readback，未读取 artifact；watcher 仍 blocked/final=false。剩余缺口：真实 production backend runner + live Gateway smoke/readback + DB acquire/readback + secret-safe proof。
- **E8-004-S62 Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Curie，no-op review**：operator evidence intake repeat。结果：未改文件；`.tmp\gateway_tpm_production_backend` 下没有 JSON evidence artifact；未运行 readback，未读取 artifact；watcher 仍 `production_ready_blocked / waiting_for_production_evidence`，`artifact_read=false`、`final_x_eligible=false`。剩余缺口：真实 production backend runner + live Gateway smoke/readback + DB acquire/readback + secret-safe proof。
- **E8-004-S63 Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Curie，no-op review**：operator evidence intake repeat。结果：未改文件；`.tmp\gateway_tpm_production_backend` 下没有 JSON evidence artifact；未运行 readback，未读取 artifact；watcher 仍 `production_ready_blocked / waiting_for_production_evidence`，`artifact_read=false`、`final_x_eligible=false`。剩余缺口：真实 production backend runner + live Gateway smoke/readback + DB acquire/readback + secret-safe proof。
- **E8-004-S64 Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Curie，no-op review**：operator evidence intake repeat。结果：未改文件；`.tmp\gateway_tpm_production_backend` 目录存在，JSON artifact count=0；未运行 readback，未读取 artifact；watcher 仍 `production_ready_blocked / waiting_for_production_evidence`，`artifact_read=false`、`final_x_eligible=false`。剩余缺口：真实 production backend runner + live Gateway smoke/readback + DB acquire/readback + secret-safe proof。
- **E8-004-S65 Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Curie，no-op review**：operator evidence intake repeat。结果：未改文件；`.tmp\gateway_tpm_production_backend` 目录存在，JSON artifact count=0；未运行 readback，未读取 artifact；watcher 仍 `production_ready_blocked / waiting_for_production_evidence`，`artifact_read=false`、`final_x_eligible=false`。剩余缺口：真实 production backend runner + live Gateway smoke/readback + DB acquire/readback + secret-safe proof。
- **E8-004-S66 Evidence Intake Watchdog Repeat，已完成/阻塞收束，2026-06-05，owner Curie，no-op review**：operator evidence intake repeat。结果：未改文件；`.tmp\gateway_tpm_production_backend` 目录存在，JSON artifact count=0；未运行 readback，未读取 artifact；watcher 仍 `production_ready_blocked / waiting_for_production_evidence`，`artifact_read=false`、`final_x_eligible=false`。剩余缺口：真实 production backend runner + live Gateway smoke/readback + DB acquire/readback + secret-safe proof。
- **E8-004-S67 Production Backend Closure Attempt，已改派，2026-06-05，owner Curie**：主动运行/验证 production backend evidence operator path，尝试产出或读取真实 tokenizer/read-model backend + live Gateway smoke/readback + DB acquire/readback evidence；优先使用既有脚本/命令/fixtures，不新增 schema/contract，除非现有 gate 无法表达真实 closure attempt；不得伪造 production evidence，不得把 fixture/simulation/template 标 final。验收：回报实际命令、exit/classification、artifact directory exists、JSON artifact count、`artifact_read`、`final_x_eligible`、缺失 backend/Gateway/Postgres 条件与 next exact command。
- **E8-004-S68 Post-Env Production Backend Closure Reattempt，已完成/阻塞收束，2026-06-05，owner Curie**：用户补环境后重新尝试 E8 closure。结果：未改源码；`pwsh`、`psql`、Docker CLI 已可用；实际命令 `pwsh -File scripts/verify_gateway_tpm_production_backend_evidence.ps1 -EmitExecutionPack -ExpectedCommit 8ac37f4d1a637f80eef0638be6093e05bb2f868c -BackendKind read_model_backend -TokenSourceKind input_tokens` 返回 `production_ready_blocked/execution_pack_plan_only`；当前 Codex 进程未继承 `DATABASE_URL`、`POSTGRES_URL`、`GATEWAY_TPM_PRODUCTION_BACKEND_EVIDENCE`、`GATEWAY_TPM_TRUSTED_READ_MODEL_ENABLED`、`GATEWAY_TPM_TRUSTED_TOKENIZER_ENABLED`、`GATEWAY_TPM_PRODUCTION_ARTIFACT_PATH`、`GATEWAY_BASE_URL`、`GATEWAY_AUTH_TOKEN`；`.tmp\gateway_tpm_production_backend` JSON artifact count=0，未读取 artifact，`final_x_eligible=false`。剩余缺口：把 env 注入当前 worker 进程并产出真实 production backend/live Gateway smoke/DB acquire artifact。
- **E8-004-S69 Env Injection And Artifact Readback Reattempt，已完成/阻塞收束，2026-06-05，owner Curie**：复用 E8 上下文，专门关闭 S68 的 env visibility/artifact 缺口。结果：未改源码；目标 artifact `.tmp/gateway_tpm_production_backend/e8-s69-readback.json` 不存在，未运行 `-OptInArtifactReadback`；按 DoD 在命令内显式注入 `DATABASE_URL/POSTGRES_URL`、`GATEWAY_BASE_URL`、`GATEWAY_AUTH_TOKEN`、`GATEWAY_TPM_PRODUCTION_BACKEND_EVIDENCE=1`、`GATEWAY_TPM_TRUSTED_READ_MODEL_ENABLED=1`、`GATEWAY_TPM_PRODUCTION_ARTIFACT_PATH=.tmp/gateway_tpm_production_backend/e8-s69-readback.json` 后运行 execution pack，classification 仍为 `production_ready_blocked/execution_pack_plan_only`，`artifact_read=false`、`final_x_eligible=false`。剩余缺口：真实 production backend runner、Gateway live smoke、DB acquire/readback SQL evidence artifact。
- **E8-004-S70 Production Backend Artifact Producer Handoff，已完成/阻塞收束，2026-06-05，owner Curie**：复用 E8 上下文，专门把 S69 的缺口转成最短 operator artifact producer handoff。结果：未改源码、未 build、未写 fake artifact；只读取 existing runbook/script/fixtures；`pwsh -File scripts/verify_gateway_tpm_production_backend_evidence.ps1 -EmitLiveCommand -ExpectedCommit 8ac37f4d1a637f80eef0638be6093e05bb2f868c -BackendKind read_model_backend -TokenSourceKind input_tokens` 返回 `production_ready_blocked/live_command_handoff_only`；runbook 最短命令序列仍包含占位 `<production-backend-runner>`、`<gateway-live-smoke-command>` 与 `psql ... -f scripts/operator/e8_rate_limit_db_acquire_readback.sql`；repo 内未发现可执行 production read-model/tokenizer backend runner，且 exact missing file 为 `scripts/operator/e8_rate_limit_db_acquire_readback.sql`；当前不能立刻产出 `.tmp/gateway_tpm_production_backend/e8-s70-live-evidence.json`，不能 readback，`final_x_eligible=false`。
- **E8-004-S71 Minimal Production Evidence Producer And DB Readback Script，已完成/阻塞收束，2026-06-05，owner Curie**：补 S70 明确缺失的最小可执行 artifact producer 边界。结果：新增 `scripts/run_gateway_tpm_production_backend_evidence.ps1` 与 `scripts/operator/e8_rate_limit_db_acquire_readback.sql`，更新 `docs/E8-004_PRODUCTION_BACKEND_EVIDENCE_RUNBOOK.md` 与 `scripts/verify_gateway_tpm_production_backend_evidence.ps1` command shapes 指向真实文件名；未 build、未重型测试、未伪造 production evidence。验证：producer dry-run 无 env 时 safe refusal；显式 dev env 后 exact blocker 为 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND`、`GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；DB readback SQL 在 dev Postgres 返回 bounded/secret-safe projection；artifact readback path 可执行但 artifact 缺失，`production_ready_blocked/missing_backend`、`final_x_eligible=false`。剩余缺口：真实 external backend runner command、Gateway live smoke command 与 `.tmp/gateway_tpm_production_backend/e8-s71-live-evidence.json` artifact。
- **E8-004-S72 External Runner And Live Smoke Command Intake，已完成/阻塞收束，2026-06-05，owner Curie**：只处理真实外部命令注入与 artifact intake。结果：未 build、未重型测试、未改源码、未伪造 evidence；当前 operator 未提供 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；按 DoD 显式注入 dev DB/Gateway env、`GATEWAY_TPM_PRODUCTION_BACKEND_EVIDENCE=1`、`GATEWAY_TPM_TRUSTED_READ_MODEL_ENABLED=1`、`GATEWAY_TPM_PRODUCTION_ARTIFACT_PATH=.tmp/gateway_tpm_production_backend/e8-s72-live-evidence.json` 后，producer dry-run 返回 `missing_external_backend_env` 且 exact missing env 为上述两个命令；`scripts/operator/e8_rate_limit_db_acquire_readback.sql` 在 dev DB 成功返回 bounded/secret-safe projection，rate-limit rows/counts 为 0；artifact 不存在，opt-in readback 返回 `production_ready_blocked/missing_backend`、`artifact_read=false`、`final_x_eligible=false`。剩余缺口：真实 external backend runner command、Gateway live smoke command 与 live evidence artifact。
- **E8-004-S73 External Command Provision And 100% Closure Attempt，已完成/阻塞收束，2026-06-05，owner Curie**：Production closure attempt，不阻塞 Local P0。结果：未 build、未重型测试、未改源码、未伪造 evidence；当前进程未提供 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；按 DoD 显式注入 dev DB/Gateway env、`GATEWAY_TPM_PRODUCTION_BACKEND_EVIDENCE=1`、`GATEWAY_TPM_TRUSTED_READ_MODEL_ENABLED=1`、`GATEWAY_TPM_PRODUCTION_ARTIFACT_PATH=.tmp/gateway_tpm_production_backend/e8-s73-live-evidence.json` 后，producer dry-run 返回 `missing_external_backend_env` 且 exact missing env 为上述两个命令；DB readback SQL 在 dev DB 成功返回 bounded/secret-safe projection，rate-limit rows/counts 为 0；artifact 不存在，opt-in readback 返回 `production_ready_blocked/missing_backend`、`artifact_read=false`、`final_x_eligible=false`。剩余 Production closure 缺口：真实 external backend runner command、Gateway live smoke command 与 live evidence artifact。
- **E8-004-S74 Production Closure External Command Wait/Intake，已完成/阻塞收束，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0。结果：presence-only 检查未输出 raw command/credential；当前缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s74-live-evidence.json` 不存在；未运行真实 producer/live smoke/readback，`final_x_eligible=false`。最短 operator 命令：在同一 shell/session 设置这两个 env，再运行 S75 的 producer/live smoke/DB/readback 序列。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S75 Production Closure External Command Provided Readback，已完成/阻塞收束，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0。结果：presence-only 检查未输出 raw command/credential；当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s75-live-evidence.json` 不存在；未运行 producer/live smoke/DB readback/opt-in artifact readback，`final_x_eligible=false`。最短 operator export/run command 仍是同一 shell/session 设置这两个 env 后运行 S76。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S76 Production Closure External Command Provided Readback，已完成/阻塞收束，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta。结果：presence-only 检查未输出 raw command/credential；当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s76-live-evidence.json` 不存在；未运行 producer/live smoke/DB readback/opt-in artifact readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S77 Production Closure External Command Watcher，已完成/阻塞收束，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta。结果：presence-only 检查未输出 raw command/credential；当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s77-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S78 Production Closure External Command Watcher，已完成/阻塞收束，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta。结果：presence-only 检查未输出 raw command/credential；当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s78-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S79 Production Closure External Command Watcher，已完成/阻塞收束，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta。结果：presence-only 检查未输出 raw command/credential；当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s79-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S80 Production Closure External Command Watcher，已完成/阻塞收束，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta。结果：presence-only 检查未输出 raw command/credential；当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s80-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S81 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s81-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S82 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s82-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S83 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s83-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S84 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s84-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S85 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s85-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S86 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s86-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S87 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s87-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S88 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s88-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S89 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s89-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S90 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s90-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S91 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s91-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S92 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s92-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S93 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s93-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S94 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s94-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S95 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s95-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S96 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s96-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S97 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s97-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S98 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s98-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S99 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s99-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S100 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s100-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S101 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s101-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S102 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s102-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S103 Production Closure External Command Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前仍缺 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`；artifact `.tmp/gateway_tpm_production_backend/e8-s103-live-evidence.json` 不存在；按低频 watcher DoD 未重复 DB/readback，`final_x_eligible=false`。E8 进度条仍保持 99.85%，未改 100%，因为真实 production token evidence artifact 与 reservation/DB acquire readback 未闭合。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S104 Production Backend Evidence Intake And Readback，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；presence-only 检查未输出 raw command/credential，当前 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND=absent`、`GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND=absent`；指定 artifact `.tmp/gateway_tpm_production_backend/e8-s103-live-evidence.json` 不存在，因此未运行 verifier/readback、未重复 DB/readback，`final_x_eligible=false`。blockers：runner command missing、Gateway live smoke command missing、live evidence artifact missing。E8 进度条仍保持 99.85%，未改 100%，因为真实 production token evidence artifact 与 reservation/DB acquire readback 未闭合。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S105 Production Backend Evidence Minimal Delta Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；相对 S104 no new production command/artifact：`GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND=absent`、`GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND=absent`，`.tmp/gateway_tpm_production_backend/e8-s103-live-evidence.json` 与 `.tmp/gateway_tpm_production_backend/e8-s105-live-evidence.json` 均不存在；未运行 verifier/readback、未重复 DB/readback，`final_x_eligible=false`。blockers unchanged；operator action reference S104。E8 仍保持 99.85%，不得推进 100%。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S106 Production Backend Evidence Minimal Delta Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；相对 S105 no new production command/artifact：`GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND=absent`、`GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND=absent`，`.tmp/gateway_tpm_production_backend/e8-s103-live-evidence.json` 与 `.tmp/gateway_tpm_production_backend/e8-s106-live-evidence.json` 均不存在；未运行 verifier/readback、未重复 DB/readback，`final_x_eligible=false`。blockers unchanged；operator action reference S104/S105。E8 仍保持 99.85%，不得推进 100%。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S107 Production Backend Evidence Minimal Delta Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；相对 S106 no new production command/artifact：`GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND=absent`、`GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND=absent`，`.tmp/gateway_tpm_production_backend/e8-s103-live-evidence.json` 与 `.tmp/gateway_tpm_production_backend/e8-s107-live-evidence.json` 均不存在；未运行 verifier/readback、未重复 DB/readback，`final_x_eligible=false`。blockers unchanged；operator action reference S104/S105/S106。E8 仍保持 99.85%，不得推进 100%。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S108 Production Backend Evidence Minimal Delta Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；相对 S107 no new production command/artifact：`GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND=absent`、`GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND=absent`，`.tmp/gateway_tpm_production_backend/e8-s103-live-evidence.json` 与 `.tmp/gateway_tpm_production_backend/e8-s108-live-evidence.json` 均不存在；未运行 verifier/readback、未重复 DB/readback，`final_x_eligible=false`。blockers unchanged；operator action reference S104-S107。E8 仍保持 99.85%，不得推进 100%。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S109 Production Backend Evidence Minimal Delta Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；相对 S108 no new production command/artifact：`GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND=absent`、`GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND=absent`，`.tmp/gateway_tpm_production_backend/e8-s103-live-evidence.json` 与 `.tmp/gateway_tpm_production_backend/e8-s109-live-evidence.json` 均不存在；未运行 verifier/readback、未重复 DB/readback，`final_x_eligible=false`。blockers unchanged；operator action reference S104-S108。E8 仍保持 99.85%，不得推进 100%。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S110 Production Backend Evidence Minimal Delta Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；相对 S109 no new production command/artifact：`GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND=absent`、`GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND=absent`，`.tmp/gateway_tpm_production_backend/e8-s103-live-evidence.json` 与 `.tmp/gateway_tpm_production_backend/e8-s110-live-evidence.json` 均不存在；未运行 verifier/readback、未重复 DB/readback，`final_x_eligible=false`。blockers unchanged；operator action reference S104-S109。E8 仍保持 99.85%，不得推进 100%。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S111 Production Backend Evidence Minimal Delta Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；相对 S110 no new production command/artifact：`GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND=absent`、`GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND=absent`，`.tmp/gateway_tpm_production_backend/e8-s103-live-evidence.json` 与 `.tmp/gateway_tpm_production_backend/e8-s111-live-evidence.json` 均不存在；未运行 verifier/readback、未重复 DB/readback，`final_x_eligible=false`。blockers unchanged；operator action reference S104-S110。E8 仍保持 99.85%，不得推进 100%。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S112 Production Backend Evidence Minimal Delta Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；相对 S111 no new production command/artifact：`GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND=absent`、`GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND=absent`，`.tmp/gateway_tpm_production_backend/e8-s103-live-evidence.json` 与 `.tmp/gateway_tpm_production_backend/e8-s112-live-evidence.json` 均不存在；未运行 verifier/readback、未重复 DB/readback，`final_x_eligible=false`。blockers unchanged；operator action reference S104-S111。E8 仍保持 99.85%，不得推进 100%。未 build、未重型测试、未改源码、未伪造 evidence。
- **E8-004-S113 Production Backend Evidence Minimal Delta Watcher，完成，2026-06-05，owner Curie**：Production closure only，不阻塞 Local P0/Beta；相对 S112 no new production command/artifact：`GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND=absent`、`GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND=absent`，`.tmp/gateway_tpm_production_backend/e8-s103-live-evidence.json` 与 `.tmp/gateway_tpm_production_backend/e8-s113-live-evidence.json` 均不存在；未运行 verifier/readback、未重复 DB/readback，`final_x_eligible=false`。blockers unchanged；operator action reference S104-S112。E8 仍保持 99.85%，不得推进 100%。未 build、未重型测试、未改源码、未伪造 evidence。按 S113 规则不再自动派 watcher；建议主线程等待 evidence 状态变化，或另派非重复审计片。
- **E8-004-S114 Production Backend Evidence Feasibility/Runner Closure，完成，2026-06-05，owner Codex**：非重复 feasibility/readback closure，不再做 no-new-evidence watcher。当前 repo 状态：`scripts/run_gateway_tpm_production_backend_evidence.ps1`、`scripts/verify_gateway_tpm_production_backend_evidence.ps1`、`scripts/operator/e8_rate_limit_db_acquire_readback.sql` 与 `docs/E8-004_PRODUCTION_BACKEND_EVIDENCE_RUNBOOK.md` 已存在；`.tmp/gateway_tpm_production_backend` 当前无 JSON artifact；repo 内存在 bounded local/read-model harness fixture `tests/fixtures/gateway/trusted_read_model_backend_harness_ready.json`（`schema=gateway_tpm_trusted_numeric_source_file_read_model_backend_fixture_v1`、`source_type=read_model`、`token_kind=input_tokens`、`tokens=222`、`material_in_output=false`），但它不是 production runner artifact，不能标 final。已验证现有 bounded readback：`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_tpm_production_backend_evidence.ps1 -ContractOnly` 返回 `status=production_ready_blocked`、`blocker=missing_opt_in`、`artifact_read=false`、`closure_audit.final_x_eligible=false`；`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_tpm_production_backend_evidence.ps1 -OptInArtifactReadback -ArtifactPath tests/fixtures/gateway/production_backend_evidence_accepted_external_simulation.json -ExpectedCommit commit-current -BackendKind read_model_backend -TokenSourceKind input_tokens` 返回 `status=production_evidence_accepted_for_review`、`artifact_read=true`、`gateway_live_smoke_status=passed`、`db_acquire_readback_state=present`、`secret_safe_omission=true`、`simulation=true`、`real_operator_evidence=false`、`final_x_eligible=false`、blocking reasons `accepted_simulation_cannot_close_final_gap` 与 `simulation_can_close_final_gap_false`；同一 fixture 若按当前 runtime commit `8ac37f4d1a637f80eef0638be6093e05bb2f868c` readback，则正确拒绝为 `blocker=commit_mismatch`。producer dry-run 在显式 opt-in/artifact path 下仍收敛到真实缺口：`GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND`、`GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`、`DATABASE_URL`、`GATEWAY_BASE_URL`、`GATEWAY_AUTH_TOKEN`；artifact command sequence 已给出 backend runner、Gateway live smoke、DB acquire readback、artifact readback 四步。结论：最短真实路径不是补 watcher，而是在同一 operator session 提供真实 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` 与 `GATEWAY_TPM_GATEWAY_LIVE_SMOKE_COMMAND`，写入 `.tmp/gateway_tpm_production_backend/e8-s114-live-evidence.json`，再运行 DB readback SQL 与 verifier readback；现有 local/mock harness 只能作为 bounded readback/acceptance-gate proof，不能关闭 production final。剩余 blocker：无真实 production tokenizer/read-model backend runner command、无 Gateway live smoke command、无 `.tmp/gateway_tpm_production_backend/*.json` production artifact、无 production artifact -> reservation/DB acquire readback；E8 仍保持 99.85%，不得推进 100%。未改 E8 verifier/runbook、未改源码、未 build、未重型测试、未伪造 production artifact。
- **E8-004-S115 Local Trusted Backend Runner Prototype，完成，2026-06-05，owner Codex**：完成非 production-final local runner prototype，不再做 no-new-evidence watcher。审查结论：S114 前已有 `scripts/run_gateway_tpm_production_backend_evidence.ps1` 只支持外部 `GATEWAY_TPM_PRODUCTION_BACKEND_RUNNER_COMMAND` handoff，没有 repo-bounded local file/read-model runner；verifier 可读 artifact，但未显式把 `local_prototype=true` 纳入 final guard；runbook 也没有 local prototype 命令。实现：在 `scripts/run_gateway_tpm_production_backend_evidence.ps1` 新增显式 `-RunLocalPrototype` 与 `-LocalFixturePath`，默认仍不执行 local/external backend；local prototype 仅接受 `.tmp/gateway_tpm_production_backend/**` artifact path、仅读取 repo-bounded `tests/fixtures/gateway/trusted_read_model_backend_harness_ready.json`、不联网、不读 raw prompt/input/body/header、不运行 Gateway live smoke、不查 DB，写出的 artifact 明确 `local_prototype=true`、`real_operator_evidence=false`、`simulation_can_close_final_gap=false`、`gateway_live_smoke_status=local_prototype_not_run`。在 verifier 中补 `local_prototype`/`real_operator_evidence` readback 字段，并把 `local_prototype_cannot_close_final_gap` 固定为 closure audit blocker；runbook 增加 Local Prototype Runner 段。验证命令：`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_tpm_production_backend_evidence.ps1 -ContractOnly` 仍返回 `artifact_read=false`、`final_x_eligible=false`；`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/run_gateway_tpm_production_backend_evidence.ps1 -DryRun -RunLocalPrototype -ArtifactPath .tmp/gateway_tpm_production_backend/e8-s115-local-prototype.json -ExpectedCommit 8ac37f4d1a637f80eef0638be6093e05bb2f868c -BackendKind read_model_backend -TokenSourceKind input_tokens` 返回 `status=local_prototype_dry_run`、`artifact_path_bounded=true`、`local_fixture_path_bounded=true`、`real_operator_evidence=false`、`final_x_eligible=false`；实际写入命令去掉 `-DryRun` 后生成 `.tmp/gateway_tpm_production_backend/e8-s115-local-prototype.json`；readback 命令 `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_tpm_production_backend_evidence.ps1 -OptInArtifactReadback -ArtifactPath .tmp/gateway_tpm_production_backend/e8-s115-local-prototype.json -ExpectedCommit 8ac37f4d1a637f80eef0638be6093e05bb2f868c -BackendKind read_model_backend -TokenSourceKind input_tokens` 返回 `artifact_read=true`、`gateway_live_smoke_status=local_prototype_not_run`、`closure_audit.db_acquire_readback_state=missing`、`closure_audit.final_x_eligible=false`、`real_operator_evidence=false`、`local_prototype=true`，blocking reasons 包含 `missing_live_smoke`、`missing_db_acquire`、`missing_db_result`、`local_prototype_cannot_close_final_gap`、`simulation_can_close_final_gap_false`。距离 100% 仍差真实 production tokenizer/read-model backend runner、真实 Gateway live smoke、真实 DB acquire/readback evidence 与 production artifact review；E8 仍保持 99.85%，不得推进 100%。未改 E9/E13/E15，未 build、未重型测试、未伪造 production artifact。
- **E8-004-S116 Gateway Live Smoke/DB Acquire Readback Prototype，完成，2026-06-05，owner Codex**：在 S115 local prototype 基础上推进到 dev Gateway/Postgres/mock-provider live smoke 与 DB readback prototype，不再停在 runner 原型。环境检查：`docker ps` 显示 `docker-compose-gateway-1`、`docker-compose-postgres-1`、`docker-compose-mock-provider-1` 均 Up；`docker compose -f deploy/docker-compose/docker-compose.yml ps` 通过；Gateway `/healthz` 返回 200，mock-provider `/healthz` 返回 ok。preflight 命令 `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_rate_limit_reservation_smoke.ps1 -PreflightOnly` 通过，services/seed rows available。live 命令 `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_rate_limit_reservation_smoke.ps1 | Tee-Object -FilePath .tmp\gateway_tpm_production_backend\e8-s116-rate-limit-live-smoke.stdout.txt` 通过，`status=live_completed`、`live_requests_sent=true`、`measurements_available=true`、`closure_eligible=true`、request log rows `3`、provider attempt rows `3`、reservation acquire `3`、release `1`、not_applied `1`、fallback `1`、max affected rows per acquire `1`、gateway latency `1361ms`、concurrency contender job `1`。DB readback SQL：容器内直接 `-f scripts/operator/e8_rate_limit_db_acquire_readback.sql` 失败，具体缺口是该 repo script 未挂载进 postgres 容器；随后改用宿主机 SQL 经 stdin 喂给 `docker compose -f deploy/docker-compose/docker-compose.yml exec -T postgres psql ... --set artifact_path=.tmp/gateway_tpm_production_backend/e8-s116-live-smoke.json`，SQL 成功返回 bounded/secret-safe readback；因 live smoke finally 已恢复 rate-limit windows，post-restore rate-limit rows/current TPM counter 为 `0`，artifact 明确记录该 post-restore readback blocker，并以 live request/provider_attempt evidence 作为 S116 prototype acquire/readback row-count。产物：`.tmp/gateway_tpm_production_backend/e8-s116-live-smoke.json`，字段包含 `gateway_request_status.status=live_completed`、`request_log_rows=3`、`provider_attempt_rows=3`、`rate_limit_reservation_summary.acquire_count=3`、`release_count=1`、`not_applied_count=1`、`fallback_count=1`、`db_acquire_readback_count=3`、`db_result_readback_count=3`、`db_acquire_readback.sql=scripts/operator/e8_rate_limit_db_acquire_readback.sql`、`local_prototype=true`、`real_operator_evidence=false`、`simulation_can_close_final_gap=false`、`final_x_eligible=false`，且 marker scan 未发现 auth/provider key/raw prompt/input/request body material。E8 verifier readback：`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_tpm_production_backend_evidence.ps1 -OptInArtifactReadback -ArtifactPath .tmp/gateway_tpm_production_backend/e8-s116-live-smoke.json -ExpectedCommit 8ac37f4d1a637f80eef0638be6093e05bb2f868c -BackendKind read_model_backend -TokenSourceKind input_tokens` 返回 `status=production_evidence_accepted_for_review`、`artifact_read=true`、`gateway_live_smoke_status=passed`、`closure_audit.db_acquire_readback_state=present`、`db_acquire_readback_count_present=true`、`db_result_readback_count_present=true`、`closure_audit.final_x_eligible=false`、`local_prototype=true`、`real_operator_evidence=false`，blocking reasons `local_prototype_cannot_close_final_gap` 与 `simulation_can_close_final_gap_false`。距离 100%/production final 仍差：真实 non-local production tokenizer/read-model backend runner provenance、真实 production Gateway live smoke scope、production DB acquire/readback artifact review；S116 是本地 dev Gateway + mock-provider + repo-bounded local trusted runner prototype，不能把 E8 推到 100%。未改 E9/E13/E15，未 build、未重型测试、未伪造 production final。
- **E8-004-S117 Gateway live path reservation/readback hardening，完成，2026-06-05，owner Agent-E8/Gateway Runtime**：status `completed_local_dev_live_pass_non_production_final`。changed_files：`apps/gateway/src/main.rs`、`apps/gateway/src/tpm_estimate.rs`、`scripts/verify_gateway_rate_limit_reservation_smoke.ps1`、`scripts/operator/e8_rate_limit_db_acquire_readback.sql`、`tests/fixtures/gateway/rate_limit_reservation_live_smoke.json`、`tests/fixtures/gateway/rate_limit_tpm_estimate_mapper_contract.json`、`docs/todo/slices/E8-004.md`；未改 Control Plane/Admin UI/E11，未 revert 并行 E13 prompt-protection audit writer 变更。实现：TPM estimate summary 新增 `estimated` 与 `trusted_numeric_source_present`，缺真实 tokenizer/read-model 或使用 conservative fallback 时明确 `estimated=true`，不把 fallback 当 trusted numeric；smoke evidence 新增 `smoke_run_id`、request hashes、request ids 与 operator SQL 参数；operator SQL 改为按同一 smoke run/request scope readback `acquire_applied_count`、`release_applied_count`、`not_applied_count`、`fallback_count`、`provider_attempt_rows`；live smoke 并发 blocker 增加 `pg_advisory_xact_lock` ready signal，修复首次 live 发现的 race（`provider_attempts=1`）并证明 forced over-limit final 429 不进 provider_attempt；live smoke 还会 capture/restore bounded smoke keys 的 `status` 与 `cooldown_until`，避免 provider 429 cooldown 污染后续 preflight。commands_run：`git rev-parse --short HEAD` exit 0 -> `8e3318d`；`git status --short` exit 0；`git diff --check` baseline/final exit 0（仅既有 line-ending warnings）；`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_rate_limit_reservation_smoke.ps1 -PreflightOnly` first exit 1 due report field `request_body_hashes` secret-safe marker, fixed; rerun exit 0; after an earlier failed live left smoke key `00000000-0000-0000-0000-000000000112` in cooldown, one bounded dev DB cleanup `update provider_keys set status='enabled', cooldown_until=null ...` exit 0; final preflight exit 0; `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_rate_limit_reservation_smoke.ps1` first exit 1 with `concurrent over-limit final 429 should not create provider_attempts; got 1`, fixed by advisory ready signal; final live exit 0 with `request_log_rows=3`、`provider_attempt_rows=3`、`acquire=3`、`release=1`、`not_applied=1`、`fallback=1`、`gateway_latency_ms=1198`、`observed_contender_jobs=1`; operator SQL via stdin with same run `1780684610718` exit 0 and read back `request_log_rows=3`、`provider_attempt_rows=3`、`acquire_applied_count=3`、`release_applied_count=1`、`not_applied_count=1`、`fallback_count=1`; `cargo test -p ai-gateway rate_limit_tpm_estimate --all-targets` exit 0, 19 passed; `cargo test -p ai-gateway rate_limit_reservation --all-targets` exit 0, 15 passed. artifacts_written：no `.tmp` artifact written by this slice; live evidence emitted to stdout; operator SQL readback emitted to stdout. acceptance_results：DoD 1 pass for live acquire/release/fallback/not_applied readback metadata; DoD 2/6 pass via same smoke run/request scoped SQL readback; DoD 3 pass after advisory ready fix, final forced over-limit request produced 0 provider attempts; DoD 4 source/test pass for `estimated=true` and `trusted_numeric_source_present=false`, but running compose Gateway was not rebuilt so operator SQL readback over that live run reported `estimated_seen=false`; DoD 5 preserved by existing route selection/fallback_allowed/cooldown tests and unchanged selection path; DoD 7 commands run. blockers：production-final remains blocked by lack of non-local production tokenizer/read-model backend provenance and production-scope Gateway artifact; running compose Gateway must be rebuilt/restarted to make live DB readback show the new `estimated` field. next_task：rebuild/redeploy Gateway with this source, rerun live smoke plus same-run SQL readback, then produce non-local production tokenizer/read-model provenance artifact if E8 final `[x]` is required.
- **E8-004-S118 Bounded Gateway Rebuild/Rerun for Beta Closure，完成，2026-06-05，owner Agent-E8/Gateway Runtime**：status `beta_live_reservation_closure_passed_non_production_final`。changed_files：`scripts/verify_gateway_rate_limit_reservation_smoke.ps1`、`scripts/operator/e8_rate_limit_db_acquire_readback.sql`、`docs/todo/slices/E8-004.md`、`TODO/AI_GATEWAY_REBASED_TODO_2026-06-05.md`；fresh artifacts：`.tmp/gateway-rate-limit-reservation/e8-s118-beta-live-smoke.json`、`.tmp/gateway-rate-limit-reservation/e8-s118-beta-sql-readback.json`。实现：smoke script 新增 bounded `-ArtifactPath` 写入（仅 `.tmp`/`artifacts`，secret-safe JSON），artifact/SQL readback 均补 `forced_limit_provider_attempt_rows`；operator SQL 同一 request scope 新增 forced-limit request/provider_attempt readback。bounded rebuild/redeploy：`docker compose -f deploy/docker-compose/docker-compose.yml build gateway` exit 0；`docker compose -f deploy/docker-compose/docker-compose.yml up -d gateway` exit 1，blocker 是 Windows host port `127.0.0.1:6379` socket reservation；随后不改 compose 文件，用临时 host-port overrides `POSTGRES_HOST_PORT=55432`、`REDIS_HOST_PORT=56379` 执行 `docker compose -f deploy/docker-compose/docker-compose.yml up -d postgres redis gateway` exit 0，并 `docker compose ... restart gateway` exit 0，Gateway `/healthz` 返回 200。commands_run：`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_rate_limit_reservation_smoke.ps1 -PreflightOnly` first exit 1 due S118 script marker wrongly requiring old fixture field `artifact_write`, fixed without editing fixture; rerun exit 0；`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_rate_limit_reservation_smoke.ps1 -ArtifactPath .tmp/gateway-rate-limit-reservation/e8-s118-beta-live-smoke.json` exit 0，`status=live_completed`、request rows `3`、provider_attempt rows `3`、acquire `3`、release `1`、not_applied `1`、fallback `1`、forced-limit provider_attempt rows `0`、gateway latency `1007ms`；same-run SQL readback for `smoke_run_id=1780685446332` exit 0，request rows `3`、provider_attempt rows `3`、acquire_applied `3`、release_applied `1`、not_applied `1`、fallback `1`、forced_limit_request_rows `1`、forced_limit_provider_attempt_rows `0`、`tpm_estimate_readback.estimated_seen=true`、`trusted_numeric_source_seen=false`；artifact JSON parse and forbidden marker scan exit 0；`cargo test -p ai-gateway rate_limit_reservation --all-targets` exit 0, 15 passed；`cargo test -p ai-gateway rate_limit_tpm_estimate --all-targets` exit 0, 19 passed；scoped `git diff --check` exit 0 with line-ending warnings only。acceptance_results：TODO-12 Beta live reservation/readback pass；Gateway live path acquire/release/not_applied/fallback counts readable for the same smoke run；forced limit exceeded does not enter provider_attempts；missing tokenizer conservative TPM evidence is live-visible as `estimated_seen=true` and not trusted numeric；fallback candidate policy unchanged. remaining_RC_blockers：this is not production tokenizer/read-model final closure and does not mark E8 production final; production/non-local tokenizer or read-model provenance remains TODO-40/RC. next_task：main thread can mark TODO-12 Beta pass and move production tokenizer/read-model final evidence to TODO-40/RC; QA can rerun TODO-20 Beta gate once TODO-10/TODO-11/TODO-13 dependencies are in their required Beta states.
- **E8-004-S119 / TODO-30B Gateway Paid Hot Path Reserve-Settle-Refund Beta Implementation Slice，完成，2026-06-05，owner Agent-E8/Gateway Runtime**：status `paid_hot_path_bounded_beta_gateway_evidence_passed_overall_paid_still_blocked`。changed_files：`apps/gateway/src/main.rs`、`apps/gateway/src/db.rs`、`apps/gateway/src/streaming.rs`、`scripts/verify_gateway_paid_hot_path_smoke.ps1`、`scripts/operator/gateway_paid_hot_path_readback.sql`、`docs/todo/slices/E8-004.md`、`TODO/AI_GATEWAY_REBASED_TODO_2026-06-05.md`。实现：Gateway paid beta 默认关闭；仅 `GATEWAY_PAID_HOT_PATH_BETA` truthy 或 virtual key metadata `billing_mode=paid_controlled_beta` / `paid_hot_path_beta=true` 时启用；paid path 在 request log 后、rate-limit reservation/provider_attempt/provider_key/upstream 前读取 price/wallet 并在 DB transaction 中写 pending reserve，余额不足/缺 wallet/缺 price/reserve 失败均返回 billing 402 且不创建 provider_attempt；success path 写 confirmed settle 并把 pending reserve 置为 `reversed`（符合 billing-ledger settle planner），idempotency key `settle:{request_id}`；provider 429 failure path 通过 error finalizer release pending reserve，metadata 记录 `release:{request_id}`，full refund-after-settle 保留为 TODO-30 Production RC/policy hardening pending marker。live smoke/verifier：新增 `scripts/verify_gateway_paid_hot_path_smoke.ps1`，preflight exit 0；为旧 dev DB 安全执行既有 `db/migrations/0007_pricing_policy_selection.sql`，seed bounded local price/wallet/credit fixture，临时 opt-in dev virtual key metadata，三请求覆盖 success settle、provider 429 release、insufficient balance no-provider-call，并 restore virtual key/channel state；新增 `scripts/operator/gateway_paid_hot_path_readback.sql` 同 run/request id 读回 request/provider_attempt/ledger counts、ordering、idempotency、rollback/reconciliation/secret-safe。rebuild/redeploy：`docker compose -f deploy/docker-compose/docker-compose.yml build gateway` exit 0；`POSTGRES_HOST_PORT=55432 REDIS_HOST_PORT=56379 docker compose -f deploy/docker-compose/docker-compose.yml up -d postgres redis mock-provider gateway` exit 0；Gateway `/healthz` 200。artifacts_written：`.tmp/gateway-paid-hot-path/e8-paid-hot-path-live-smoke.json`，`schema=gateway_paid_hot_path_smoke_v1`，`smoke_run_id=e8-paid-1780687833881`，`status=passed`，secret-safe and raw request body/auth/provider secret/DB URL omitted。same-run SQL readback exit 0：request rows `3`、provider_attempt rows `2`、reserve rows `2`、reserve_reversed `2`、settle_confirmed `1`、failure_release `1`、insufficient_balance_provider_attempt_rows `0`、insufficient_balance_created_reserve_rows `0`、`reserve_before_provider_side_effect=true`、`successful_request_settled=true`、`failure_request_released=true`、`settle_idempotency=true`、`reserve_idempotency_seen=true`、`refund_idempotency.status=pending_marker` with `release_idempotency_seen=true`、`post_commit_readback=true`、`secret_safe.raw_or_secret_marker_present=false`。commands_run：`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_paid_hot_path_smoke.ps1 -PreflightOnly` exit 0；first live attempts exit 1 exposed old dev DB missing `0007` pricing selector columns and then route cooldown after provider 429, both fixed in verifier without faking pass；final live verifier with `-ArtifactPath .tmp/gateway-paid-hot-path/e8-paid-hot-path-live-smoke.json` exit 0；manual same-run SQL readback via stdin exit 0；`cargo test -p ai-gateway paid_hot_path --all-targets` exit 0, 3 passed；`cargo test -p ai-gateway paid_reserve --all-targets` exit 0, 1 passed；`cargo test -p ai-gateway pre_authorize --all-targets` exit 0, 9 passed；`cargo test -p ai-gateway rate_limit_reservation --all-targets` exit 0, 15 passed；`cargo test -p ai-gateway rate_limit_tpm_estimate --all-targets` exit 0, 19 passed；scoped `git diff --check` exit 0。acceptance_results：reserve-before-provider pass；insufficient balance prevents provider call pass (`provider_attempt_rows=0`)；success settle/readback pass；failure release/readback pass；settle idempotency pass；refund idempotency is explicit bounded pending marker via pending-reserve release, not claimed as full refund-after-settle; secret-safe pass；E8 rate-limit regression preserved. remaining_blockers：paid overall remains `paid_controlled_beta_requested` / `blocked_until_real_evidence`; still requires E11 Control Plane paid readback/reconciliation artifact, E9 real paid evidence bundle composer/readiness pass, QA paid aggregator/release gate, and production policy hardening for partial stream/client cancel/full refund-after-settle. next_task：E11/E9 should consume `.tmp/gateway-paid-hot-path/e8-paid-hot-path-live-smoke.json` for Control Plane paid readback and real paid evidence bundle composition; QA reruns paid acceptance after E11/E9 artifacts pass.
- **E8-004-S120 / TODO-30B-S2 Gateway Paid Hot Path Readback Consistency Fix，完成，2026-06-05，owner Agent-E8/Gateway Runtime**：status `paid_hot_path_readback_consistency_fixed_overall_paid_still_blocked`。review blocker reproduced：main-review artifact `.tmp/gateway-paid-hot-path/e8-paid-hot-path-main-review.json` passed internally, but independent SQL with only ordered `request_ids` returned `failure_release=0`、`successful_request_settled=false`、`failure_request_released=false`、`post_commit_readback=false`; rerun with explicit `success_request_id` / `failure_request_id` / `insufficient_request_id` returned the expected pass, so root cause was role parameter/readback handoff mismatch rather than Gateway runtime evidence missing. changed_files：`scripts/verify_gateway_paid_hot_path_smoke.ps1`、`scripts/operator/gateway_paid_hot_path_readback.sql`、`docs/todo/slices/E8-004.md`、`TODO/AI_GATEWAY_REBASED_TODO_2026-06-05.md`。实现：operator SQL now infers request roles from same-run ledger/request evidence when named role ids are omitted and emits `resolved_request_roles`; smoke artifact now includes explicit role ids, per-role request trace entries, `operator_readback.parameters`, and a secret-safe copyable operator command; live pass now uses one `Test-PaidHotPathReadbackPass` predicate requiring `successful_request_settled=true`、`failure_request_released=true`、`post_commit_readback=true`、`insufficient_balance_provider_attempt_rows=0` and no secret marker. regression selftest：`-SelfTest` covers complete pass, missing failure release rejected, missing settle rejected, insufficient-balance provider attempt rejected, raw/Auth marker rejected. artifacts_written：`.tmp/paid-beta/e8_gateway_paid_hot_path.json` and `.tmp/paid-beta/e8_gateway_paid_hot_path_readback.json`。fresh smoke/readback：`smoke_run_id=e8-paid-1780688349296`、request rows `3`、provider_attempt rows `2`、reserve `2`、reserve_reversed `2`、settle_confirmed `1`、failure_release `1`、insufficient_balance_provider_attempt_rows `0`、insufficient_balance_created_reserve_rows `0`、`successful_request_settled=true`、`failure_request_released=true`、`post_commit_readback=true`、`secret_safe.raw_or_secret_marker_present=false`; independent SQL using artifact's own parameters exit 0; independent SQL with only `request_ids` also exit 0 via role inference. commands_run：`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_paid_hot_path_smoke.ps1 -SelfTest` exit 0；`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_paid_hot_path_smoke.ps1 -PreflightOnly` exit 0；`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_paid_hot_path_smoke.ps1 -ArtifactPath .tmp/paid-beta/e8_gateway_paid_hot_path.json` exit 0；operator SQL with artifact parameters exit 0 and wrote `.tmp/paid-beta/e8_gateway_paid_hot_path_readback.json`；`cargo test -p ai-gateway paid_hot_path --all-targets` exit 0, 3 passed；`cargo test -p ai-gateway rate_limit_reservation --all-targets` exit 0, 15 passed. acceptance_results：S2 blocker fixed; artifact no longer passes unless same-run operator readback passes the required booleans; role id handoff is explicit and legacy omission is covered by SQL inference; evidence remains local bounded beta Gateway lane only. remaining_blockers：paid overall still not allowed; waits on E11 Control Plane paid readback/reconciliation, E9 real paid evidence bundle/readiness, QA paid aggregator/release gate, and Production RC policy hardening for partial stream/client cancel/full refund-after-settle. next_task：E11/E9/QA should consume `.tmp/paid-beta/e8_gateway_paid_hot_path.json` as the refreshed Gateway lane artifact; do not use the older `.tmp/gateway-paid-hot-path/e8-paid-hot-path-main-review.json` for paid bundle input.
- **E8-004-S120 addendum / E11 shape blocker，完成，2026-06-05**：E11/QA follow-up blocker `gateway_paid_hot_path_request_or_operation_ids_missing` reproduced because E11 verifier only consumes top-level `evidence`/`operations` items, not nested `request_trace`. Fix added top-level `evidence` with seven required keys and UUID request/operation ids mapped to real smoke request ids plus real ledger operation evidence from operator SQL (`success_reserve_ledger_entry_id`、`success_settle_ledger_entry_id`、`failure_release_ledger_entry_id`、`failure_release_idempotency_key`). Fresh artifact `.tmp/paid-beta/e8_gateway_paid_hot_path.json` now has evidence count `7`, request id count `3`, operation id count `3`. E11 command `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_control_plane_paid_ledger_readback.ps1 -GatewayArtifactPath .tmp/paid-beta/e8_gateway_paid_hot_path.json -OutputPath .tmp/paid-beta/e11_control_plane_paid_readback_reconciliation.json` returned nonzero runner exit with JSON `actual_exit_code=2`, `overall_status=blocked`, accepted all seven E8 evidence keys, `request_ids_present=true`, `operation_ids_present=true`, and blocker only `control_plane_paid_readback_sql_unavailable`; the previous request/operation id blocker is closed. This does not relax E11 SQL/readback gates and does not mark paid overall allowed.
- **E8-004-S121 / TODO-30B-S3 Gateway Paid Artifact Consumer Alias Hardening，完成，2026-06-05，owner Agent-E8/Gateway Runtime**：status `alias_hardened_paid_overall_still_blocked`。changed_files：`scripts/verify_gateway_paid_hot_path_smoke.ps1`、`docs/todo/slices/E8-004.md`、`TODO/AI_GATEWAY_REBASED_TODO_2026-06-05.md`。实现：E8 paid hot-path artifact now emits stable top-level `request_ids`, `operation_ids`, and `ledger_operation_ids` aliases while preserving `request_trace`、`operator_readback.parameters`、`evidence[]`; `request_ids` is derived from the same smoke request ids used in `request_trace.request_ids`; `operation_ids` is derived from `evidence[].operation_id` unique values; `ledger_operation_ids` is derived from real operator SQL ledger operation evidence. Selftest now rejects empty top-level operation aliases and keeps seven required evidence mapping coverage. Fresh artifact `.tmp/paid-beta/e8_gateway_paid_hot_path.json` has top-level request ids `3`, request_trace ids `3`, top-level operation ids `3`, evidence operation ids `3`, ledger operation ids `3`, evidence count `7`. Commands: `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_paid_hot_path_smoke.ps1 -SelfTest` exit 0; `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_paid_hot_path_smoke.ps1 -ArtifactPath .tmp/paid-beta/e8_gateway_paid_hot_path.json` exit 0; `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_paid_beta_artifact_contracts.ps1` returned runner exit 1 but JSON `actual_exit_code=2`, expected blocked overall, with E8 `consumer_status=pass`, request_id_count `3`, operation_id_count `7`, and `paid_controlled_beta_allowed=false`; captured wrapper exit confirmed `2`; `cargo test -p ai-gateway paid_hot_path --all-targets` exit 0, 3 passed; scoped `git diff --check` exit 0. remaining_blockers：paid overall still blocked by E11 `control_plane_paid_readback_sql_unavailable`, E9 readiness missing evidence/bundle, and missing real paid evidence bundle; S3 is alias hardening only, not paid allowed.
- **E8-004-S122 / TODO-30B-S4 Gateway Paid Production-RC Negative Guard Slice，完成，2026-06-05，owner Agent-E8/Gateway Runtime**：status `production_rc_negative_guard_stream_cancel_hardened_paid_overall_still_blocked`。chosen_negative_path：stream/client cancel after paid reserve. invariant：`StreamEndReason::ClientCancel` and provider stream errors must release a pending paid hot-path reserve exactly through the release path and must not produce a settle ledger entry; completed streams remain the only settle-eligible streaming finalization path. changed_files：`apps/gateway/src/streaming.rs`、`docs/todo/slices/E8-004.md`、`TODO/AI_GATEWAY_REBASED_TODO_2026-06-05.md`。实现：stream finalization now derives a paid release reason with `paid_hot_path_stream_release_reason`; `Completed` returns no release reason, `ClientCancel` uses stable reason `stream_client_cancel`, and other non-completed stream endings keep `stream_not_completed`. The finalizer releases rate-limit reservation and paid reserve only for non-completed endings, then settle projection remains gated by completed stream usage/rating. regression：new Gateway unit test `paid_hot_path_stream_negative_guard_releases_cancel_and_never_settles` proves cancel and upstream-error stream endings are not settle-eligible, while completed streams with complete usage and nonzero rating still are. commands_run：`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_paid_hot_path_smoke.ps1 -SelfTest` exit 0；`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_paid_hot_path_smoke.ps1 -PreflightOnly` exit 0；`cargo test -p ai-gateway paid_hot_path_stream_negative_guard --all-targets` exit 0, 1 passed；`cargo test -p ai-gateway paid_hot_path --all-targets` exit 0, 4 passed. acceptance_results：paid beta opt-in default unchanged; usage-only/free path remains outside reserve/settle/release; S4 adds one Production RC negative guard only and does not mark `paid_controlled_beta_allowed=true`. remaining_E8_RC_risks：full refund-after-settle policy/idempotency, partial stream accounting/reconciliation beyond cancel guard, real E11/E9/QA paid bundle acceptance, and production policy decisions remain open.
- **E8-004-S123 / TODO-30B-S5 Gateway Paid Refund-After-Settle Idempotency Slice，完成，2026-06-05，owner Agent-E8/Gateway Runtime**：status `production_rc_refund_after_settle_idempotency_contract_hardened_paid_overall_still_blocked`。chosen_scope：full refund after confirmed settle, bounded Gateway adapter/contract only; partial refund product policy remains out of scope. invariant：settled paid request refunds use stable source-settle scoped key `refund:{settle_ledger_entry_id}`; replaying the same full refund is idempotent and creates no second refund entry; an unsettled reserve is not a refund source and must stay on release/rollback path. changed_files：`apps/gateway/src/db.rs`、`docs/todo/slices/E8-004.md`、`TODO/AI_GATEWAY_REBASED_TODO_2026-06-05.md`。实现：Gateway DB adapter now has explicit `PaidSettledRefundEntry` / `PaidSettledRefundOutcome` and `insert_full_paid_refund_after_settle_ledger_entry`, guarded by confirmed settle debit lookup (`entry_type='settle'`, `status='confirmed'`, negative amount) and related refund/idempotency locks; it inserts confirmed positive `refund` rows with metadata schema `gateway_paid_hot_path_refund_after_settle_v1` and `refund_idempotency_key`, or returns `Idempotent`/`SourceNotSettled`/`NothingRefundable`. This helper is intentionally not wired into default hot path, so paid opt-in defaults and usage-only/free behavior are unchanged. regression：new Gateway tests `paid_refund_after_settle_uses_settle_entry_scoped_idempotency_key` and `paid_refund_after_settle_replay_is_idempotent_and_requires_settle_source` prove key shape, duplicate full refund idempotency via billing-ledger planner, and refusal of unsettled reserve as refund source. commands_run：`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_paid_hot_path_smoke.ps1 -SelfTest` exit 0；`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_paid_hot_path_smoke.ps1 -PreflightOnly` exit 0；`cargo test -p ai-gateway paid_refund_after_settle --all-targets` exit 0, 2 passed；`cargo test -p ai-gateway paid_hot_path --all-targets` exit 0, 4 passed. acceptance_results：duplicate refund idempotency contract pass; unsettled request release/rollback separation pass; paid beta opt-in default unchanged; S5 is Production RC hardening only and does not mark `paid_controlled_beta_allowed=true`. remaining_E8_RC_risks：live DB smoke/readback for after-settle refund helper, partial refund policy and partial operation ids, E11/E9/QA real paid bundle acceptance, and production reconciliation remain open.
- **E8-004-S124 / TODO-30B-S6 Bounded Paid Smoke Produces Real Refund Row，完成，2026-06-05，owner Agent-E8/Gateway Runtime**：status `bounded_smoke_refund_row_passed_paid_overall_still_false_until_bundle_review`。changed_files：`apps/gateway/src/main.rs`、`apps/gateway/src/db.rs`、`scripts/verify_gateway_paid_hot_path_smoke.ps1`、`scripts/operator/gateway_paid_hot_path_readback.sql`、`docs/todo/slices/E8-004.md`、`TODO/AI_GATEWAY_REBASED_TODO_2026-06-05.md`、`.tmp/paid-beta/e8_gateway_paid_hot_path.json`、`.tmp/paid-beta/e8_gateway_paid_hot_path_readback.independent.json`、`.tmp/paid-beta/e11_control_plane_paid_readback_reconciliation.json`。实现：新增 smoke-only Gateway endpoint `POST /__e8/paid-hot-path/refund-after-settle`，默认无专用 `X-E8-Paid-Hot-Path-Smoke-Refund: true` header 返回 404，且仍要求正常 Gateway auth 与 paid beta virtual key/env opt-in；该 endpoint 复用 S5 `insert_full_paid_refund_after_settle_ledger_entry`，不接入默认 production request path。Paid smoke now performs success settle, reads the settle ledger entry id, calls the smoke refund endpoint twice, expects first outcome `applied` and replay `idempotent`, then runs final operator readback. Operator SQL/artifact now expose `refund_rows`/`refund_count`、`refund_idempotency_key`、`related_settle_ledger_entry_id`、`refund_ledger_entry_id`、`duplicate_refund_idempotent=true` and `bounded_smoke_refund_step=true` / `production_default_path=false`. fresh_artifacts：`.tmp/paid-beta/e8_gateway_paid_hot_path.json` status `passed`; independent readback `.tmp/paid-beta/e8_gateway_paid_hot_path_readback.independent.json` shows request rows `3`, provider attempt rows `2`, refund rows `1`, refund status `passed`, duplicate refund idempotent `true`, post commit readback `true`; E11 verifier `.tmp/paid-beta/e11_control_plane_paid_readback_reconciliation.json` exit 0 / overall `passed`, counts reserve `1`, settle `1`, refund `1`, insufficient provider attempts `0`, blockers `[]`. commands_run：`cargo test -p ai-gateway paid_refund_after_settle --all-targets` exit 0, 2 passed；`cargo test -p ai-gateway paid_hot_path --all-targets` exit 0, 6 passed；`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_paid_hot_path_smoke.ps1 -SelfTest` exit 0；`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_paid_hot_path_smoke.ps1 -PreflightOnly` exit 0；`docker compose -f deploy/docker-compose/docker-compose.yml build gateway` exit 0；`POSTGRES_HOST_PORT=55432 REDIS_HOST_PORT=56379 docker compose -f deploy/docker-compose/docker-compose.yml up -d postgres redis mock-provider gateway` exit 0；`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_paid_hot_path_smoke.ps1 -ArtifactPath .tmp/paid-beta/e8_gateway_paid_hot_path.json` exit 0；independent `scripts/operator/gateway_paid_hot_path_readback.sql` with artifact params exit 0；`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_control_plane_paid_ledger_readback.ps1 -GatewayArtifactPath .tmp/paid-beta/e8_gateway_paid_hot_path.json -OutputPath .tmp/paid-beta/e11_control_plane_paid_readback_reconciliation.json` exit 0. acceptance_results：bounded smoke now produces a real confirmed refund ledger row; duplicate refund replay is idempotent; existing seven E8 evidence keys remain pass; E11 refund rows/mapping blocker closes in local readback. remaining_E8_RC_risks：paid overall still requires E9 real paid evidence bundle/readiness and QA/main review; smoke refund endpoint is bounded/dev evidence only, not production default behavior; partial refund product semantics and release policy remain RC work.
- **E8-CREDIT-01 / Gateway Credit Grant Balance Behavior Audit，完成，2026-06-05，owner Agent-E8/Gateway Runtime**：status `credit_grant_balance_contract_hardened_no_artifact_change`。changed_files：`apps/gateway/src/db.rs`、`docs/todo/slices/E8-004.md`、`TODO/AI_GATEWAY_REBASED_TODO_2026-06-05.md`。verdict：Gateway paid pre-authorize and reserve balance calculation includes active `credit_grants` for the selected wallet only, requires `cg.status='active'`, `valid_from <= now()`, and `valid_until is null or valid_until > now()`, subtracts `wallet.balance_floor`, and counts ledger rows in the pending/confirmed balance window. Reserve transaction lock order is wallet first, active credit grants second, relevant ledger window third, then insert. New SQL-shape regression strengthens pre-authorize assertions and adds reserve lock-order/credit-grant contract coverage. Boundary：Gateway consumes already-created active credit grants for reserve eligibility; user recharge, credit grant CRUD, issuance policy, and lifecycle management remain outside Gateway scope. Paid opt-in default and final paid artifacts were not changed.
- **E8-CREDIT-03 / TODO-32E Gateway impact watch，完成/no-op，2026-06-05，owner Agent-E8/Gateway Runtime**：status `no_runtime_change_needed`。reviewed：`docs/todo/slices/TODO-32-CREDIT-WALLET.md`、`apps/gateway/src/db.rs`、Gateway paid hot-path tests/artifact contract surfaces. verdict：Gateway already consumes existing active `credit_grants` rows in pre-authorize and paid reserve balance windows and only depends on wallet/credit/ledger rows being present, scoped, active, valid, and currency-compatible. Gateway does not need recharge/voucher/payment/order/invoice knowledge, credit grant CRUD endpoints, or user-facing remaining-balance summaries for TODO-32A/B; those are Control Plane/Admin/Billing productization surfaces. No paid reserve/settle/refund hot-path change is needed and paid artifacts remain unchanged. If TODO-32 later requires an additional Gateway-emitted balance summary field, that should be a separate bounded alias/contract patch.
- **E8-CREDIT-04 / Gateway watcher after TODO-32A/B，完成/no-op，2026-06-05，owner Agent-E8/Gateway Runtime**：status `no_gateway_mismatch_found`。scan：`TODO-32A|TODO-32B|E11-CREDIT-02R|E9-CREDIT-03|remaining-balance|credit_grants` across TODO/docs/Gateway/Billing/Control Plane found no new explicit Gateway-facing requirement after TODO-32A/B. E11/E9 surfaces still describe Control Plane/Admin/Billing productization and read-only/mutation contracts, while Gateway remains dependent only on valid active `credit_grants` rows being present for its existing balance window. No reserve/settle/refund hot-path change, no artifact change, and no balance-summary alias is currently required.
- **E8-CREDIT-05 / final Gateway no-regression watcher for credit artifact updates，完成/no-op，2026-06-05，owner Agent-E8/Gateway Runtime**：status `no_gateway_requirement_found`。scan：`billing_mutation_contract_tests|admin_readonly_runtime|remaining_balance|credit_grants|Gateway` across TODO/docs/scripts/.tmp/Gateway found E9-CREDIT-04 QA artifact `billing_mutation_contract_tests.v1` with `runtime_writer_changed=false` and `paid_gate_changed=false`, QA verifier fields for credit productization, and E11-CREDIT-02R Control Plane read-only wallet/credit runtime status. None introduces a Gateway hot-path requirement or changes the Gateway balance formula/status semantics. Gateway code and paid artifacts remain unchanged; no follow-up DoD is needed unless a future artifact explicitly requires Gateway-emitted balance summary fields.
- **E8-CREDIT-06 / TODO-32F Gateway impact watch，完成/no-op，2026-06-05，owner Agent-E8/Gateway Runtime**：status `opening_balance_import_no_gateway_change_needed`。scan：`TODO-32F|opening-balance|opening_balance_import|credit_grants|ledger_entries|balance window` found TODO-32F/E9-CREDIT-05 scoped to New API / One API opening-balance import through ledger/admin adjustment/opening-entry primitives. Gateway does not need to know import source, external reference, payment/order/invoice, or CRUD lifecycle details; once rows exist, the current pre-authorize/reserve balance window already reads active `credit_grants` and pending/confirmed `ledger_entries`. No `apps/gateway/src/db.rs` change, no paid reserve/settle/refund change, no Gateway artifact change, and no balance formula change is needed.
- **E8-CREDIT-25 / TODO-32H user-facing remaining balance Gateway impact plan，完成/no-op docs plan，2026-06-05，owner Agent-E8/Gateway Runtime**：status `gateway_noop_control_plane_user_api_plan`。reviewed：Gateway paid pre-authorize/reserve balance path, Gateway router, Control Plane Admin wallet read surfaces, TODO-32 remaining-balance contract, and accepted TODO-32F opening-balance runtime evidence. Decision：user-facing remaining balance belongs in Control Plane User/API or Admin/API, not Gateway, because Gateway is the model data plane and does not own a user/session/RBAC disclosure boundary for wallet balances. Minimal implementation slice should reuse Control Plane wallet/credit/ledger readback and expose decimal-string `available_to_spend`, active credit grant total, pending/confirmed ledger window, `wallet.balance_floor`, staleness/readback marker, bounded ledger/grant ids, and `secret_safe=true`. Gateway impact remains false：opening-balance import materializes ledger rows, future credit grant CRUD should materialize `credit_grants`/`ledger_entries`, and Gateway already consumes those through `credit_balance.amount + ledger_balance.amount - w.balance_floor`. No `apps/gateway/src/db.rs` change, paid reserve/settle/refund hot-path change, or Gateway artifact change is needed unless a future contract explicitly requires Gateway-emitted balance summary aliases.
- **E8-CREDIT-28 / Gateway credit-balance diff impact audit，完成/no-op audit，2026-06-06，owner Agent-E8/Gateway Runtime**：status `gateway_impact=false`、`paid_gate_changed=false`。Reviewed current dirty Gateway diff in `apps/gateway/src/db.rs`、`apps/gateway/src/main.rs`、`apps/gateway/src/streaming.rs`、`apps/gateway/src/tpm_estimate.rs` for TODO-32 credit/wallet and paid hot-path impact. Verdict：pre-authorize still executes before provider attempts, provider-key opening, and upstream side effects; insufficient balance still returns generic `billing_insufficient_balance` before provider attempts; active credit grant readback remains scoped to selected active wallet/currency/status/window and combines `credit_balance.amount + ledger_balance.amount - w.balance_floor`; paid reserve still inserts a pending reserve only after conservative balance check and uses stable idempotency; settle reverses pending reserve in the same transaction; refund-after-settle helper keeps settle-entry-scoped idempotency and refuses unsettled sources; streaming client cancel/provider error release path remains covered. No `opening_balance_imports` Gateway dependency and no TODO-32H Gateway endpoint requirement were found. TODO-32H remains Control Plane User/Admin API owned.
- **E8-LAUNCH-20 / Gateway distribution proof final-gate reread，完成/no-op，2026-06-07，owner Agent-E8/Fermat**：status `trusted_user_voucher_backed_beta_ready_with_productization_gaps`、`paid_gate_changed=false`。reviewed：`TODO/AGENT_COORDINATION_2026-06-05.md` E8 lane and `CTO-API-DIST-02`，Gateway API key auth、`/v1/models`、`/v1/chat/completions`、streaming contract、paid/balance/no-provider-call proof、voucher distribution readiness artifact and final handoff manifest consumption。No Gateway runtime or Control Plane/Admin UI code change was required. current_artifacts：`.tmp/launch/e8_gateway_paid_hot_path_launch_check.json` remains `status=passed` with insufficient-balance `provider_attempt_rows=0`; `.tmp/launch/gateway_voucher_distribution_readiness.json` remains `status=pass`; `.tmp/launch/voucher_api_distribution_readiness.json` refreshed as `overall_status=pass_with_productization_gaps`, `production_distribution_ready=true`, `gateway_current_launch_hot_path_verified=true`, `remaining_blockers=[]`; `.tmp/launch/final_launch_gate_summary.json` reports `ready_to_distribute_api=true`, `production_distribution_ready=true`, `production_distribution_full_ready=false`, `global_blockers=[]`, and manifest hash readback includes current/pass entries for both E8 Gateway artifacts. commands_run：`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_voucher_api_distribution_readiness.ps1 -SelfTest` exit 0；`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_gateway_paid_hot_path_smoke.ps1 -SelfTest` exit 0；`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_sdk_smoke.ps1 -ContractOnly` exit 0；`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/verify_voucher_api_distribution_readiness.ps1 -SecretScanPassed` exit 0；`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/test.ps1 -GatewayProtocolContractsOnly` exit 0；`pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/test.ps1 -FullDistributionGateOnly` exit 0。remaining_external_inputs：blocker id `per_user_handoff_values_missing` only blocks actual key handoff until Release/Ops fills `release_owner`、`support_contact`、`tenant_id`、`project_id`、`wallet_id`、`rate_budget_guardrails`、`voucher_quota`、`rollback_owner`; next command after values exist is `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/prepare_trusted_user_api_distribution_packet.ps1` without `-AllowMissingUserFields`, followed by manifest/quota verification and `scripts/test.ps1 -FullDistributionGateOnly`。
- **E8-LAUNCH-21 / Gateway protocol live evidence rerun，historical/superseded，2026-06-07，owner Agent-Gateway-LiveProtocol**：This earlier rerun correctly proved Docker/Compose were available but still observed protocol live blockers. It is superseded by E8-LAUNCH-22 below after Gateway/mock-provider rebuild and live SDK rerun. Keep this entry only as triage history; do not use it as current API distribution state.

- **E8-LAUNCH-22 / Gateway protocol live evidence closure，完成/pass，2026-06-07，owner CTO-Orchestrator**：status `gateway_protocol_live_pass`、`paid_gate_changed=false`。Main thread rebuilt the current Gateway/mock-provider compose stack, fixed Responses mock SSE termination to stop at `response.completed` instead of appending Chat-style `[DONE]`, and reran live checks. `scripts/verify_gateway_streaming_smoke.ps1` exits 0, including invalid JSON `parser_error`; `$env:SDK_SMOKE_INCLUDE_STREAMING="1"; $env:SDK_SMOKE_INCLUDE_PROTOCOL_COVERAGE="1"; scripts/verify_sdk_smoke.ps1 -SkipInstall` exits 0 against Gateway `:8080`, covering chat non-stream, chat stream, OpenAI Responses stream terminal, Anthropic Messages, Gemini GenerateContent, and `/v1/models` filtering. `.tmp/launch/gateway_protocol_live_evidence_summary.json` is refreshed with `blockers=[]`; prior blocker ids `gateway_chat_invalid_json_chunk_not_rejected` and `mock_provider_protocol_shapes_missing` are recorded as closed. This strengthens scoped API distribution evidence only; it is not full public/commercial readiness.
