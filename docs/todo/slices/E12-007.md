## E12-007 Sample Apply/Rollback Parity Boundary Verifier

Status: completed / verifier-only, 2026-06-07

Scope:
- Added `scripts/importers/verify-import-sample-apply-rollback-parity.ps1`.
- Covers the checked-in New API and One API OpenAI-compatible importer samples.
- Writes derived artifacts under `.tmp/importers/sample_apply_rollback_parity/`.

Acceptance evidence:
- Command: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/importers/verify-import-sample-apply-rollback-parity.ps1`
- Result: pass.
- Summary artifact: `.tmp/importers/sample_apply_rollback_parity/summary.json`
- Summary schema: `importer.sample-apply-rollback-parity-verification.v1`
- Command: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/importers/verify-import-provider-key-operator-handoff-packet.ps1`
- Result: pass.
- Summary artifact: `.tmp/importers/provider_key_operator_handoff/summary.json`
- Packet artifacts: `.tmp/importers/provider_key_operator_handoff/newapi.operator_handoff_packet.json`, `.tmp/importers/provider_key_operator_handoff/oneapi.operator_handoff_packet.json`

What it proves:
- New API and One API checked-in samples still parse through source dry-run, internal mapping, and apply-plan.
- Both sample apply plans have `preflight.status=pass`, no planned skips, supported SQL adapters, and rollback snapshot entries equal planned writes.
- Provider key handoff is sidecar-only: `raw_material_allowed=false`, `apply_directly_supported=false`, `raw_provider_keys_written=false`, `provider_key_write_targets=0`, and `provider_key_sql_operations=0`.
- The Control Plane provider-key boundary is explicit: handoff metadata points to `POST /admin/provider-keys`, but audit/readback is deferred until an operator supplies real secret material through that path.
- Operator handoff packets are generated from the apply plans and prove the importer can hand off provider-key create metadata without raw key material: `operator_handoff_metadata_entries=3` per sample and `raw_provider_keys_in_packets=false`.
- If present, `.tmp/importers/import_apply_live_runtime_verification.json` is consumed as focused live apply/rollback evidence for provider/channel, canonical model, model association, simple channel mapping, and conflict refusal/no-write behavior.

Deferred / blocker artifact:
- Full sample live apply/rollback with provider-key create audit/readback is recorded as deferred, not failed, because checked-in samples contain only redacted credential locators and the verifier must not write raw provider keys.
- This does not block API distribution: the importer remains secret-safe and the missing piece is an operator/runtime Control Plane readback proof.
