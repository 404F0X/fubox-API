## Epic E12：Migration Importer

- [x] **E12-001 New API 配置解析器**  
  优先级：P0  
  验收：样例 dump 可解析；当前最小切片已支持 New API 风格 provider/channel/model/key dry-run 解析。

- [x] **E12-002 One API 配置解析器**  
  优先级：P0  
  验收：样例 dump 可解析；当前最小切片已支持 One API 风格 channel/provider/model/key/model mapping dry-run 解析。

- [x] **E12-003 映射为内部模型**  
  优先级：P0  
  验收：模型映射转 canonical + association + channel mapping；当前已新增内部模型映射 dry-run report，可读取 New API/One API importer JSON report，输出 canonical model、model association、channel mapping、conflicts、manual review 和 next steps，不落库、不输出 secret material。

- [x] **E12-004 Dry-run report**  
  优先级：P0  
  验收：列出创建/更新/跳过/冲突/人工确认项；当前 dry-run/report 切片输出结构化 JSON report，包含 counts/providers/channels/provider_keys/models/associations、channel model mappings、groups/tags、warnings/unsupported_fields、conflicts/manual review/apply preflight；`scripts/importers/verify-import-source-dryrun-contract.ps1` 另覆盖 New API/One API 的 balance/profile/user token/group/pricing multiplier evidence，并将 opening balance、user profile、user token/API key、access group、pricing multiplier 标记为 `non_migratable_items`，明确需要 ledger/account/secret-management/price-book 路径承接；全链路仍为 dry-run，不落库、不调用服务、不执行真实 DB apply/rollback。

- [~] **E12-005 Apply + rollback snapshot**  
  优先级：P0  
  验收：导入失败可回滚，可重复执行。当前 `scripts/importers/import-apply-plan.ps1` 已在 read-only plan 基础上新增 PostgreSQL SQL-plan executor 与 rollback journal contract：默认仍为 dry-run，不连接 live DB、不写库；`-Apply` 必须配合 `-Force`，无冲突、source provider/channel 可绑定或可从 unbound source channel 派生 provider/channel 时进入 `prepared_sql_plan`；输出事务边界、operation id/idempotency manifest、rollback snapshot entry/before-image schema、refusal contract、JSON/SQL operation bundle、`SELECT ... FOR UPDATE` before-image 捕获 SQL、provider/channel `ON CONFLICT` upsert SQL、canonical model `ON CONFLICT` upsert SQL、bound explicit-channel model association upsert SQL、simple channel `model_mappings` JSONB merge SQL、`importer_apply_runs`/`importer_apply_operation_journal` DDL、apply run/operation journal insert SQL plan 和 rollback operation skeleton。新增 `scripts/importers/invoke-import-apply-live.ps1` 作为真实 live PostgreSQL runner，当前支持 `provider`、`channel`、`canonical_model`、已绑定 `channel_mapping_entry` 与已绑定 explicit-channel `model_association` adapter，会在事务内持久化 rollback journal、执行 apply、可 `-RollbackAfterApply` 回滚。Focused verifier `scripts/importers/verify-import-apply-live-runtime.ps1` 已通过，artifact `.tmp/importers/import_apply_live_runtime_verification.json` 为 `status=pass`、`live_database_connection=true`、`database_writes=true`、`rollback_verified=true`、`secret_safe=true`、`raw_sql_omitted=true`，并覆盖 `provider_channel_unbound`：从未绑定 source channel 创建 provider/channel/channel mapping，`provider_key_material_allowed=false`，apply 后读回存在、rollback 后读回移除；并覆盖 `conflict_blocked_no_write_refusal`：预期拒绝、preflight blocked、`database_writes=false`、目标 canonical model 未写入。新增 `scripts/importers/verify-import-provider-key-secret-path-artifacts.ps1` 覆盖 New API / One API sample 的 source dry-run、internal mapping、apply-plan 三段 artifact secret scan 与 sidecar-only handoff contract，summary `.tmp/importers/provider_key_secret_path/summary.json` 为 `status=pass`，证明 provider key 原始 locator 不落 artifact，且 `planned_provider_key_writes=0`、`sql_provider_key_operations=0`、operator path 为 `POST /admin/provider-keys`。仍未完成：Control Plane provider-key handoff audit/readback、复杂 channel mapping policy、真实 New API/One API sample 全链路 apply/rollback。

---

