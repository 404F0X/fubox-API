# TODO moved

同步日期：2026-06-12

[!] Admin wallet funding source compact readback 已补：`GET /admin/wallets`、`GET /admin/wallets/{wallet_id}`、`GET /billing/wallets/{wallet_id}/remaining-balance`、`GET /admin/users/{id}/detail` 和 user balance/billing history DTO 返回/声明 `funding_source_readback`，汇总 voucher、manual adjustment、payment order、subscription credit、negative proration credit 的 count、amount by currency、bounded refs、source status、ref presence 和 safe_next_action。Web client DTO、OpenAPI skeleton、frontend contract docs 和根 TODO 已同步；focused check only，不做真实 payment/refund、release evidence、UI 重构、gateway/payment provider/subscription executor 改动。响应禁止 raw voucher code/hash、raw ledger metadata、Authorization、API key secret、provider key、raw payload 和 raw source refs。
[!] Wallet / credit grant expiration compact readback 已补：`GET /admin/wallets`、`GET /admin/wallets/{wallet_id}`、`GET /billing/wallets/{wallet_id}/remaining-balance`、`GET /user/balance` 和 `GET /user/billing-history-readback` 返回 `credit_grant_expiration_readback`，汇总 active/expired/expiring soon grant count、按 currency 的 available amount、next expiration、bounded grant ids/status/amount、source refs presence 和 safe_next_action。Web client DTO、OpenAPI skeleton、frontend contract docs 和根 TODO 已同步；focused check only，不做真实 payment/refund、release evidence、subscription scheduler executor 或 UI 重构。响应禁止 raw voucher code/hash、raw ledger metadata、Authorization、API key secret、provider key、raw payload 和 raw source refs。

[!] Team/project membership compact readback 已补：`GET /admin/projects/{id}/members-summary` 返回 tenant/project scoped member ids、role/status、membership source、项目 key/profile access presence、30d request count/cost presence 和 safe next action；`GET /admin/users/{id}/detail` 附带 `membership_summary`，`GET /user/team-summary` 返回当前 user session 的 current project/team summary。响应不回显 raw email、API key secret/hash、Authorization、provider key、raw request/provider payload 或 raw metadata；前端 client、OpenAPI skeleton、frontend API contract 已同步。未做权限系统重构、真实目录同步或 release gate。
[!] Subscription package lifecycle user/admin readback 已补：subscription plan、scheduler candidate、scheduler event execute/readback 返回 `subscription_lifecycle_readback.v1`，覆盖 current plan、period status、quota/credit grant refs presence、scheduler event refs presence、payment handoff status 和 safe next action；不运行 scheduler daemon、不接真实支付、不回显 provider secret/payload、Authorization 或 raw invoice metadata。Web client、Admin Billing 详情、OpenAPI skeleton 和根 TODO 已同步；focused check only。

[!] Admin audit log detail/readback 已补：`GET /admin/audit-logs` 行响应新增 secret-safe `audit_log_detail_readback.v1`，覆盖 resource refs、action_result、actor/session presence、metadata redaction summary 和 safe next action；Admin UI detail drawer、web client DTO、OpenAPI skeleton 与 frontend contract docs 已同步。focused check only，未做发布验证；不回显 raw token/session/API key/provider key/credential header/raw payload。

[!] Request logs CSV/export audit readback 已补：`GET /admin/request-logs/export.csv` 的 best-effort audit metadata/after_snapshot 包含 secret-safe `export_audit_readback`，覆盖 allowed columns、filtered row count、audit id/ref presence、metadata-only redaction policy 和 safe next action；不记录 raw payload、provider response、Authorization、provider key、API key secret 或 CSV content。未做发布验证。

[!] Admin request trace timeline compact readback 已补：`GET /admin/request-logs/{id}` 新增 `trace_timeline_readback`，覆盖 auth/key/profile、routing decision、preauthorize/rate-limit、provider attempts/fallback、stream finalizer、ledger settlement、payload preview policy compact stages；每阶段仅返回 status/category/latency/ref presence 或 count/safe_next_action，并声明 forbidden raw fields，不回显 raw prompt/payload/provider response、Authorization、API key secret、provider key/provider_key_id/raw metadata。Web client DTO、OpenAPI skeleton、frontend rebuild contract docs 已同步；未做 UI 大改、production read-model evidence 或 release gate。
[!] Request payload preview policy/readback 已补：`GET /admin/request-logs/{id}/payload` 返回 secret-safe `payload_preview_policy_readback.v1`，覆盖 stored/not-stored、redaction、metadata-only、click-to-load、forbidden raw fields policy、safe next action 和 audit ref presence；不回显 raw prompt/body/provider response、Authorization、provider key/provider_key_id 或 raw payload。未做 UI 重构或发布验证。

[!] Health summary recovery action readback 已补：`GET /admin/providers/health-summary` 的 provider key 行新增 `recovery_action_readback`，包含 suggested_action、operator_confirmation_required、last_probe_status、cooldown_or_refusal_reason、safe_next_action、upstream_probe_executed=false 和 omitted_secret_policy。Web client 类型/恢复建议消费、OpenAPI skeleton、contract fixture 已同步；focused check only，不做真实 provider live smoke；响应禁止 provider key、Authorization、raw endpoint、raw payload、sealed secret、fingerprint 和 raw metadata。
[!] Provider/channel recovery apply-plan compact readback 已补：health summary provider key 行和 `POST /admin/provider-keys/{id}/recovery` 响应新增 `recovery_apply_plan_readback`，覆盖 suggested_action、preconditions、would_enable/would_rotate/would_probe/would_refuse、operator_confirmation_required、blocked_reasons、safe_next_action、local_execution_boundary；只表达本地可执行计划，不做真实 provider 网络或 live smoke，继续禁止 provider key secret、Authorization、raw endpoint、raw payload、sealed secret、fingerprint raw value、raw metadata。

[!] Importer mapping quality/readback 已补：NewAPI/OneAPI/Sub2API dry-run、Sub2API handoff bridge、internal mapping report 和 import apply-plan 现在输出 `mapping_quality_readback`，汇总 provider/channel/model/user/key/wallet/subscription mapping counts、conflicts、non-migratable reasons、safe_next_action 和 operator handoff refs presence。Admin UI artifact parser、OpenAPI skeleton、frontend contract docs 和 importer verifiers 已同步；focused check only，不做真实生产导入。readback 禁止 raw user key/provider key/token/DB URL/raw SQL/Authorization。

[!] Admin setup/readiness backend readback advanced: `GET /admin/setup/readback` now returns secret-safe `first_run_readiness` for admin, mock provider/channel/model, local test key, and Gateway chat/responses/embeddings readiness, with blocked reasons and safe next actions. Web client DTO, OpenAPI skeleton, and frontend contract docs are synced. No UI refactor or release validation was performed.

[!] Virtual key / project budget policy diagnostics readback advanced: admin/user virtual key DTOs now expose secret-safe `policy_diagnostics` for budget status, rate-limit limit/window presence, usage summary status, blocked/reject reason, safe next action, and ledger/preauth refs presence. No production validation was performed.

[!] Worker/jobs dashboard compact readback 已补：新增 `GET /admin/workers/jobs-dashboard`，只读汇总 subscription scheduler supervisor、pending scheduled events、import apply runner handoff、CRM retry handoff、provider health probe/recovery handoff、payment provider executor handoff 的 status/count/ref/next_action。Web client DTO/method、OpenAPI skeleton 与 frontend contract docs 已同步；不启动 daemon、不执行网络、不写业务表，不做 Helm/K8s/CronJob、真实 worker loop smoke 或 release evidence。响应禁止 Authorization、session token、provider key、DB URL、raw payload、raw SQL、raw webhook/body、raw metadata。

[!] Enterprise account compact admin dashboard/readback 已补：`GET /admin/enterprise/accounts` 每个 account 新增 secret-safe `admin_dashboard`，汇总 identity readiness、OIDC/SAML mock executor binding/identity-session issue-plan readback paths、sales activity next action/owner/due date、provisioning/invite handoff、CRM sync/retry worker handoff 和 billing/quota local readiness；`GET /admin/enterprise/sales-dashboard` 已同步到 OpenAPI skeleton。只做本地 control-plane 读写与 mock executor readback，不接真实 IdP/CRM/sales 系统，不做 live smoke/release gate；响应禁止 raw SAMLResponse、OIDC code/token、CRM payload、Authorization、provider secrets。外部 IdP/CRM/销售系统仍缺，保持 `[!]`。

[!] Enterprise account provisioning/invite delivery readback handoff 已补：`GET /admin/enterprise/accounts` 每个 account 返回 secret-safe `provisioning_or_invite_handoff`，汇总 account status、provisioning status、invite delivery status、retry/refusal reason、next action、audit ref presence 和 local record ref presence；不接真实邮件/CRM、不执行网络、不回显 raw email/contact、Authorization、secret、CRM/provider payload、raw audit metadata 或 raw idempotency material。OpenAPI、web client types 和 frontend contract docs 已同步。

当前项目 TODO 以仓库根目录 `TODO/` 为准，且已重置为功能/设计实现优先：

- [Code-first Design TODO](../TODO/CODE_FIRST_DESIGN_TODO_2026-06-12.md)

旧 Agent 调度、paid beta、production/release evidence、审查同步类 TODO 已归档，仅保留历史背景：

- [Legacy archived TODOs](../TODO/archive/2026-06-12_legacy_review_and_production_gate_todos/)

执行口径：代码和功能先行；本地可运行、UI 可点击、Gateway/API 可调用优先；生产 RC、manifest freshness、clean-clone release proof、真实支付/订阅/OIDC 等不得作为当前 P0/P1 blocker。

[!] Production read-model lag explainability detail 已补：`GET /admin/production/read-model/status` 返回 `read_model_lag_explainability`，前端可直接展示 Postgres/ClickHouse backend selection、lag/staleness source、tokenizer config presence、sample query plan presence 和 safe_next_action；OpenAPI、Admin UI client DTO 与前端合同文档已同步。未做真实 ClickHouse smoke；响应禁止 DB URL、credentials、Authorization、raw SQL/raw query 和 raw payload。

[!] Billing ledger adjustment/runtime writer 前端可消费 readback 已补：`POST /admin/ledger/adjustments/dry-run` 在 dry_run、execute_contract/refused、execute applied/idempotent 响应中返回 `ledger_adjustment_execution_readback`，展示 mode/outcome/status、wallet/credit_grant/budget/ledger/request refs presence、安全 idempotency fingerprint、blocked_reasons 和 safe_next_action；OpenAPI 与 Admin UI 类型/展示已同步。未做生产验证；响应禁止 raw SQL、wallet secret、Authorization、provider key、raw metadata 和 raw idempotency。

[!] Provider/Channel manual test explainability 已补：`POST /admin/channels/{id}/manual-test` dry-run 新增 `manual_test_explainability`，汇总 protocol、dry-run/mock/live/config-needed status、provider key lifecycle presence summary、endpoint capability path template、safe next action 和 `omitted_secret_policy`；Admin UI manual-test 结果面板、web client types、OpenAPI skeleton、contract fixture 与前端合同文档已同步。focused check only，未做真实 provider live smoke；响应禁止 provider key、Authorization、raw endpoint URL 和 raw payload。

[!] Provider key lifecycle/recovery readback 已补：`GET/PATCH /admin/provider-keys/{id}`、`POST /admin/provider-keys/{id}/recovery`、`POST /admin/provider-keys/{id}/rotate` 返回 `lifecycle_state`、`credential_generation`、`last_probe_summary`、`rotation_needed.reason`、`safe_next_action`、`omitted_secret_policy`。仍只做 bounded readback/recovery probe 合约，不做真实 provider live smoke；响应禁止 key secret、Authorization、raw endpoint、raw payload、sealed secret、fingerprint 和 raw metadata。

[!] Voucher/public API distribution readiness readback 已推进：`GET /admin/distribution/readiness` 现在返回可消费的 `voucher_batch_status`、`redeem_readiness`、`virtual_key_issuance_readiness`、`quota_pricing_guardrails` 和顶层 `safe_next_action`；只做 control-plane readback，不做发布证据；不回显 raw voucher code、voucher code hash、API key secret/hash、Authorization、provider key、raw request/provider payload。Web Distribution 页和 OpenAPI skeleton 已同步；真实公共分发 release evidence、真实 provider live smoke 和运营发放证明仍后置。

[!] User Portal developer quickstart readback seam 已补：`GET /user/developer-quickstart-readback` 返回 endpoint、profile-aware 可用模型、当前 key 状态、最近 request ids、mock chat/responses/embeddings readiness、billing balance summary 和 safe next actions；只做 user session + project scoped readback，不回显 raw API key secret/hash、Authorization、provider key/provider_key_id、prompt/messages、embedding input、raw payload 或 upstream provider payload。真实邮件、生产 provider/live smoke、release evidence 仍保持 `[!]`。
[x] API key/profile scoped model availability compact readback 已补：`GET /user/developer-quickstart-readback` 顶层返回 `model_availability_readback`，`GET /user/models` envelope meta 同步 `model_availability_readback`；按 user session project/profile/latest virtual key scope 返回 visible model sample、blocked count/reasons、protocol capability summary、rate/budget/pricing guardrail presence 和 safe next action。该 readback 只返回 model names/ids/status/count/reason，不回显 raw API key secret/hash、Authorization、provider key/provider_key_id、raw route policy、raw rate/budget policy 或 raw payload；OpenAPI、admin-ui client DTO、frontend contract 已同步。未做真实 provider live smoke、SDK smoke 或 release evidence。
[!] API distribution developer/operator packet readback 已推进：新增 `GET /user/developer-distribution-packet-readback`，返回 endpoint readiness、profile-filtered model availability、quota/rate/budget/pricing guardrail presence、voucher/key handoff refs 和单一 safe next action；operator packet verifier 只检查 endpoint reference 与 no-secret flags，不做发布验证。不回显 raw API key、voucher code/hash、provider key/provider_key_id、Authorization、token、raw request/response payload 或 upstream provider payload。真实发布验证、真实 provider live smoke 和外部发放证明仍后置。

[!] User security activity summary readback 已补：`GET /user/security-activity-summary` 返回 user-session/project scoped 登录 session count/sample、密码重置/邮箱验证请求 productization 状态、API key create/disable/delete audit counts/sample、balance/ledger count/sample 和 safe next actions；只返回安全 ID/status/time/count/amount 字段，不回显 password hash、session token/token hash、API key secret/hash、Authorization、raw request/response/body/payload、provider key/provider_key_id 或 raw ledger metadata。密码重置/邮箱验证仍未写持久 audit/event count，真实邮件和 release evidence 仍后置。

[!] Request detail usage explainability readback 已补：`GET /admin/request-logs/{id}` 返回 `usage_explainability` / `billing_usage_source`，按 endpoint/protocol 展示 prompt/completion/cache/reasoning/embedding token 来源、adapter usage vs gateway fallback、provider attempt token summary、rating status、ledger ref presence 和 safe mismatch reasons；只基于 request-log safe projections、provider_attempts summary 与 ledger refs，不回显 raw prompt/payload/provider response/Authorization/provider key。真实 production read-model evidence / release evidence 仍保持后置。

[!] Gateway preauthorize/rate-limit explainability readback 已补：`GET /admin/request-logs/{id}` 新增 `preauthorize_and_rate_limit_explainability`，汇总余额/预算/preauth status、RPM/TPM/concurrency reservation status、fallback/reject/billing refusal reason、safe next action、ledger reservation/settle ref presence；只读 request log safe projection、provider attempt summary、route summary 和 ledger refs，不回显 raw key、Authorization、payload、wallet/provider credential material、raw rate-limit window 或 ledger metadata。未做生产验证。

[!] Request provider attempts/fallback chain readback 已推进：`GET /admin/request-logs/{id}` 新增 `provider_attempts_explainability`，汇总 attempt count、selected/fallback sequence、provider/channel status、retryable/error category、latency/first-token presence 和 safe_next_action；只基于 provider_attempts 安全投影，不回显 endpoint raw URL、Authorization、provider key/provider_key_id、provider request id、raw response/payload 或 attempt metadata。Web client、OpenAPI skeleton 和 frontend contract docs 已同步；focused check only，未做发布验证。

[!] Enterprise OIDC/JWKS validator executor readback 已补：`POST /admin/enterprise/identity-connections/oidc/jwks-validator-executor` 只接受 fixture-safe JWT header/claims summary、kid/alg/aud/iss/exp/nonce presence/hash、JWKS key fingerprint presence 和 `signature_verified` boolean；返回 `validator_result`、`blocked_reasons`、binding/session handoff readiness 和 `omitted_fields`。不接真实 IdP/JWKS 网络、不创建外部 session、不回显 raw token/raw claims/JWKS/JWK body/client_secret/Authorization/private key/cert。逻辑完成但真实 IdP/JWKS 网络 validator 和 live evidence 仍为缺口。

[!] OpenAI Responses edge cases/parser 已推进本地 adapter 覆盖：`/v1/responses` upstream build 会剥离 raw compatibility `messages`，Responses output item fixture 覆盖 message、function/tool call、reasoning、image/file refs 的安全透传，stream terminal/error usage extraction 只接受明确 usage token 字段。真实 provider matrix、live SDK smoke、跨 provider Responses schema 差异和 release evidence 仍后置。

[!] OpenAI Embeddings Gateway 边界已推进：`POST /v1/embeddings` 复用 virtual key auth、profile/routing、provider key open、preauth、rate-limit、request log、usage rating 和 ledger settle；adapter 暴露 secret-safe input shape 摘要，稳定覆盖 string/string_array/token_array/token_array_batch 并拒绝 mixed/object/empty invalid input；request route metadata 记录 `gateway_openai_embeddings_compat_v1` 与 `openai_embeddings_response_shape_v1`，只回读 embedding count、dimension consistency、usage missing/abnormal 状态、x-request-id、response hash 和 ledger usage source，provider schema mismatch 归一为 parser error；mock provider 返回 OpenAI-compatible embeddings list/data/usage。不回显 Authorization、provider key、raw input、embedding values 或 raw payload。本地 provider/schema edge-case seam 已完成；真实 provider matrix、live SDK smoke、跨 provider embedding schema 差异和 release evidence 仍后置。

[!] Gemini native generateContent route/adapter 边界已推进：adapter 新增 secret-safe `gemini_generate_content_protocol_metadata_v1`，统一读取 non-stream/stream `usageMetadata`、finishReason 映射、error code/status、promptFeedback blockReason 和 safetyRatings count；Gateway native `:generateContent` 成功路径把 `gateway_gemini_generate_content_native_v1` 写入 provider attempt 与 request route metadata，记录 body hash/model rewrite、terminal/safety/error/usage readback；native `:streamGenerateContent` finalizer now carries Gemini protocol metadata。响应仍保持 passthrough，不输出 Authorization、provider key 或 raw payload。真实 provider matrix、live SDK smoke 和 release evidence 继续标 `[!]` 后置。

[!] Anthropic Messages native route/adapter 边界已推进：`POST /v1/messages` 继续复用 auth、profile/routing、provider key open、preauth、rate-limit、request log、usage rating 和 ledger settle；route candidates 支持 `anthropic`/`anthropic_messages`/`claude_compatible` channel，request log 写入 Anthropic protocol metadata；adapter/mock contract 覆盖 non-stream/stream usage、provider error mapper、`tool_use` content block parsing 和 `stop_reason=tool_use` 映射；mock provider 支持 `anthropic_tool_use`。不输出认证头、上游密钥、原始载荷或上游凭证明文。真实 provider matrix、live SDK smoke 和 release evidence 仍后置。
[!] Model/Profile visibility diagnostics readback 已推进：`POST /admin/model-associations/dry-run` 扩展为 admin readback，按 profile 返回 allowed/denied models、allowed channel tags、blocked provider ids、候选 provider/channel blocked reasons、provider key presence 计数、default price config presence、protocol endpoint capability readiness 和 safe next action；输出禁止 provider key material、Authorization、raw channel endpoint 和 raw upstream payload。仅同步 web client/OpenAPI/contract fixture；release evidence 仍后置。

[!] Virtual Key external scanner adapter config/readback + bounded ingress seam 已补：`GET /admin/virtual-keys/leak-candidates` 返回顶层 `external_scanner_adapter`，`source_policy.scanner_adapter` 也同步 provider/status/ref presence/sync/last marker/marker counts/readiness/blocked reason；`POST /admin/virtual-keys/external-scanner/handoff` 只接受 bounded finding summary（provider、finding_count、key_prefix/key_hash presence、repo_ref_hash、severity、detected_at、signature_validated、可选 virtual_key_id），命中 key 时写 `virtual_keys.metadata.leak_detection` external marker，不带 id 时只 planned。该切片只做本地 metadata handoff；不连接真实 scanner、不跑 repo scan、不输出 scanner refs、Authorization、raw findings、raw leak payload、virtual key secret/hash、scanner secret 或 webhook body。剩余缺口是真实 scanner provider、vault/ref resolution、webhook 验签、provider finding parser 和 live evidence。

CRM sync run retry attempt handoff 已补：`PATCH /admin/enterprise/accounts` 的 `external_crm_sync_run.retry_attempt_mode=plan|mark_attempted|mark_skipped` 支持本地 marker；`mark_attempted` 只更新当前 sync run metadata，不发真实网络；GET account readback 返回最近 marker。剩余缺口是真实 HTTP executor、真实 provider response/header parser、真实 retry executor。

[!] CRM HTTP executor boundary/readback 已补：`external_crm_sync_run.http_executor_boundary` 同步到 PATCH/GET readback，返回 method、logical endpoint ref、header presence、body shape、timeout、retry summary，并固定 `network_enabled=false`、`network_call_performed=false`；不回显 raw endpoint URL、Authorization、secret、raw body/header/response 或 raw CRM payload。真实 endpoint/secret vault resolution、HTTP send、provider parser、retry/rate-limit executor 仍后置。

[!] CRM provider response/header parser seam 已补：`external_crm_sync_run.provider_response_parser` 只接受 fixture-safe status、retry_after_seconds、rate-limit/reset/remaining presence、cursor presence/hash、external id counts、result counts、allowlisted error category 和 safe next action；PATCH/GET readback 返回 `provider_response_parser`，legacy `provider_response_summary` 仍可桥接到 reducer/retry marker。不发真实网络，不回显 raw endpoint、Authorization、secret、raw header/body/payload/cursor。剩余缺口是真实 endpoint/secret vault resolution、真实 HTTP send、live provider response/header capture/parser 和真实 retry/rate-limit executor。

[!] CRM sync retry worker handoff summary 已补：`GET /admin/enterprise/accounts` 与 `GET /admin/enterprise/sales-dashboard` 返回只读 `retry_worker_handoff_summary`，从 `enterprise_crm_sync_runs.metadata.retry_attempt_readback` 汇总 `scheduled_count`、`blocked_count`、`next_retry_after_seconds_min`、`provider_counts`、`operator_next_actions`；不执行 HTTP，不回显 Authorization、secret、raw endpoint、raw cursor、raw reset header 或 raw CRM payload。剩余缺口是真实 retry worker/executor、HTTP executor 和 live provider response/header capture。

[!] Enterprise sales activity mutation/readback 已补：`PATCH /admin/enterprise/accounts.sales_activity` 可追加本地 `call|email|demo|followup` 等活动，记录 allowlisted `outcome`、`due_at`、`owner`/`owner_marker` 和 `next_action`，`GET /admin/enterprise/accounts` 返回 activity readback 与 `next_action_reducer.recomputed=true`，sales dashboard 继续汇总 next action 状态；不连接真实 CRM，不接收/返回 raw contact email、Authorization、secret、CRM/provider payload。

[!] Enterprise SSO / SAML ACS XML signature parser executor readback 已补：`POST /admin/enterprise/identity-connections/saml/xml-signature-parser-executor` 仅接受 fixture-safe metadata，返回 `parser_input_summary`、`signature_result_readback`、`blocked_reasons`、`network_enabled=false`、`raw_xml_accepted=false`；功能逻辑完成，真实 XML parser/signature/assertion validator、IdP 网络和 session 创建仍后置。

[!] Enterprise SAML assertion validator/attribute mapper readback 已补到 `execute-validated-acs`：新增 fixture-safe `assertion_validator_input`，只接受 issuer/entity/audience/nameid hash 或 presence、group/role count、time window、`signature_verified` 和 `attribute_mapping_summary`；返回 `assertion_validator_result`、`role_group_mapping_result`、`binding_session_handoff_readiness`、`blocked_reasons`、`omitted_fields`。不接真实 IdP 网络，不接受/回显 raw SAMLResponse/XML/assertion、raw attributes/email/nameid、cert/private key 或 Authorization。剩余缺口 `[!]`：真实 XML parser、XML signature cryptographic validator、IdP-backed assertion validator 和 reviewed session creation path。

[!] Subscription scheduler 本地 execution/readback 已推进：billing-ledger 新增稳定 `admin_subscription_scheduler_execution_plan_readback.v1` 业务计划，control-plane candidate/worker/execute/run-due readback 返回 renewal、payment_failed/dunning、expire、proration 的 capture handoff、dunning 状态、proration delta、idempotency/refusal/no-write policy；不跑 daemon、不发 provider 网络、不回显 provider payload/secret/raw invoice metadata。剩余缺口仍是真实 scheduler daemon、真实 provider fetch/capture executor 与 production reconciliation evidence。

[!] Payment Stripe-like real provider response object parser/reconciliation seam 已补：billing-ledger `payment_provider_stripe_like_source_of_truth_summary.v1` 接受 fixture-safe event/payment_intent/charge/refund/dispute object summary，`payment_provider_stripe_like_response_object_reconciliation.v1` 输出 capture/refund/chargeback source-of-truth candidate、`matched|mismatch|blocked`、amount/currency/local refs/status/idempotency/source object presence、retry/rate-limit/parser-error safe next action；control-plane payment executor readback 同步。仍不发真实网络、不接真实商户、不回显 raw provider payload/header/body/ref/secret、Authorization 或 raw idempotency。剩余缺口是真实 Stripe-like HTTP send、真实 provider response capture、live merchant evidence 和 production reconciliation cutover。
[!] Billing local invoice/receipt/reconciliation readback 已补：local payment demo mark-paid/replay 响应新增 `invoice_receipt_reconciliation_readback`，前端可直接展示 invoice status、receipt status、payment refs presence、ledger refs presence、reconciliation status 和 safe next action；Web client、OpenAPI skeleton、frontend contract docs 已同步。不接真实商户、不标生产支付证据；响应禁止 provider payload、secret、Authorization、raw invoice metadata 和 raw idempotency。

[!] Payment provider normalized executor result/readback 已补：billing-ledger `PaymentProviderExecutorResult` 支持 callback/capture/refund/chargeback_ack 的 normalized result、idempotency/status mapping 和 ledger/reversal handoff；refund/chargeback 不伪造真实商户成功，不直接写 ledger，只输出后续 reversal apply 边界。逻辑完成但真实商户 provider API adapter、生产 callback/HTTP 和 live provider evidence 仍缺；响应禁止 provider secret、Authorization、raw webhook/body、raw provider payload、raw idempotency key 和 raw invoice metadata。

[!] Payment Stripe-like real client handoff/readback 已补：billing-ledger `StripeLikeFetchExecutorResult.client_handoff` 新增 `payment_provider_stripe_like_client_handoff.v1`，返回 runtime secret ref resolved、reqwest client configured、request builder configured、object fetch would be sent、blocked reasons、retry policy 和 `stripe_api_source_of_truth.fetch_adapter` handoff；仍固定 `network_call_performed=false`，不回显 raw object ref、Authorization、secret、raw headers/body 或 provider payload。剩余缺口是真实 secret vault resolution、真实 Stripe-like HTTP send、response capture/parser、retry 执行和 live provider evidence。

[!] Import production PostgreSQL runner dry-run guard/readback 已补：`scripts/importers/invoke-import-production-postgres-runner.ps1` 读取 reviewed apply-plan，返回 dry-run execution graph、rollback guard、idempotency fingerprint、plan readback 和 blocked reasons；默认不连生产 DB、不写库、不回显 DB URL/token/provider key/raw user key/raw SQL。剩余缺口是真实生产 PostgreSQL connection secret/config、operator authorization、approved execution window、真实 apply/rollback DB readback。
