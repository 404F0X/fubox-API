# TODO.md：AI Gateway / New API 替代产品开发任务清单

版本：0.1-dev-start  
日期：2026-06-01  
状态标记：`[ ] 未开始`、`[~] 部分完成/未最终验收`、`[x] 完成`、`[!] 阻塞`
优先级：P0 必须、P1 重要、P2 后续。

> 当前执行策略：`[~]` 表示该任务已有一个或多个切片落地，但还没满足最终验收；它不表示当前正在并行开发。并行开发只允许发生在下方已登记 lane 内；每个 lane 以一个可验收切片为单位推进，切片完成后先更新本文件，再由主线程合并、验证、提交。

当前活跃方向：
- **已恢复（2026-06-04）**：按用户最新要求重新吃满 5 个 Agent 位置；主线程继续作为协调者/Reviewer，只在切片验收后提交和推送。
- **本轮高性能横向目标仍保留**：E8/E9/E11/E13 的正确性 contract 已较多，但 `[x]` 仍需要把 live/preflight smoke 明确绑定到 latency、concurrency、tool/runtime availability、secret-safe evidence 和 SLO/性能摘要；本轮切片优先补这些“高性能可验收”证据，而不是继续堆宽泛 `[~]`。
- **本轮进行中切片**：E8-004-S28 Runtime、E9-004-S27 Runtime、E11-007-S43 Backend、E11-007-S44 Smoke、E13-005-S25 UI/Audit。
- **上一轮已完成**：E8-004-S20、E9-004-S18、E11-007-S29 Backend、E13-005-S18、E11-007-S28 Smoke。
- 多方向并行允许，但每个方向必须有明确 owner、切片目标、写入范围、验收检查和退出条件；没有切片记录的并行工作不能合并。

当前重点 TODO 进度条（估算口径：已验收切片 + 剩余最终验收缺口；不是最终 `[x]` 标记）：
- `[########--] 82%` **E8-004 rate-limit-aware key selection**：Gateway/runtime/DB reservation、TPM fallback contract 与 trusted numeric source runtime adapter boundary 已多轮落地；剩余真实 tokenizer/read-model implementation、runtime wiring、live Postgres/Gateway smoke。
- `[########--] 82%` **E9-004 reserve/settle/refund ledger**：ledger planner/executor、Control Plane execute/cutover guard 与 live probe executor rollback boundary 已落地；剩余真实 billing-ledger runtime writer 接管、live DB cutover/integration。
- `[########--] 76%` **E11-007 Billing/Price 页面**：UI execute/readiness、script handoff、browser runner readiness、OpenAPI evidence lifecycle/provenance、package cache/download opt-in evidence 与 browser artifact write/read freshness gate 已落地；剩余真实 backend/browser live smoke、真实 semantic/client-generation 工具链证据。
- `[#######---] 72%` **E13-005 Prompt Protection**：Gateway endpoint 覆盖、proof report lifecycle/provenance/freshness 与 Admin UI/audit freshness/replay refusal contract 已落地；剩余真实 Gateway/Postgres/mock-provider live proof 与完整 audit 闭环。
- `[####------] 36%` **横向高性能验收**：流式/backpressure、reservation、metrics 基础已经存在；E8 trusted numeric source adapter 已补 availability/preflight/estimate duration marker 边界，E9 live probe executor 已补 row-count/timing/rollback duration capture boundary，E11 browser runner readiness 已补 per-action duration/artifact round-trip 与 artifact write/read freshness 证据，OpenAPI semantic cache gate 已补 tool/package probe duration、cache size 与 download provenance；缺口是把 P0 live/preflight smoke 统一产出 latency/TTFT/concurrency/row-count/tool-runtime duration 等性能摘要，并明确哪些指标才能关闭最终 DoD。

多人协作规则：
- 主线程 Codex 是协调者，负责选择活跃方向、拆切片、合并结果、跑验证、提交和推送。
- 子 Agent 可以负责不同方向，但每个方向必须是协调者显式分配的 lane；不能自行扩大到未分配 Epic/目标。
- 每个切片必须写清：目标 TODO、切片目标、为什么需要这个切片、写入范围、验收检查、完成结果、剩余缺口。
- 切片合并后必须更新本文件，再提交；否则 TODO 不能作为进度参考。
- 切片存在的原因：把一个大 TODO 拆成可提交、可测试、可回滚、可审查的小验收单元；切片完成不等于 TODO 完成，只有满足该 TODO 的最终验收才改为 `[x]`。

当前目标切片记录：
- **E8-004-S1，已完成，2026-06-04**：建立 DB repository execution boundary。原因：Gateway 不能直接靠 request-local 计划实现分布式限流，必须先有 bounded DB acquire/release 写边界。范围：`crates/db/**`。验收：Noop/Refused 不查询 DB，SqlReady 使用 bounded SQL，tenant/provider_key/channel bind，secret-safe summary，`<=1` row semantics。
- **E8-004-S2，已完成，2026-06-04**：强化 DB execution contract。原因：在接 Gateway 前先锁住 no-query、limited/unlimited、<=1 row 和 secret-safe 语义，避免 runtime 接线后才发现 DB 合约不稳。范围：`crates/db/**`、`tests/fixtures/db/**`。验收：fixture 覆盖 no-query Noop、invalid required Refused、limited/unlimited dimensions、acquire/release bind count、`fetch_optional`、schema scope 和 secret-safe output。
- **E8-004-S3，已完成，2026-06-04**：Gateway runtime 接入 DB reservation execution。原因：把已存在的 request-local reservation 计划提升为真实 provider-key counter mutation 的运行时路径，同时保留 fallback 和 no-side-effect ordering。范围：`apps/gateway/**`、`tests/fixtures/gateway/**`、`Cargo.lock`。验收：provider key 选中后、provider_attempt/key open/upstream 前 acquire；NotApplied/Refused 走 fallback/429；已 Applied 但未正常消耗时 release；全 unlimited/no-counter route 不打 DB；不重复 release；request/provider logs 只记录安全摘要；targeted gateway/db/routing tests、workspace check/test 通过。
- **E8-004-S4，已完成，2026-06-04，owner Mencius，live 执行阻塞**：live Postgres/concurrency smoke。原因：S1-S3 已覆盖 contract 和 runtime ordering，但 `[x]` 需要证明真实 DB counter mutation、并发 NotApplied、fallback、release 在实际 schema 上工作。范围：smoke scripts/fixtures 或现有 integration harness。验收：真实 Postgres 上 acquire 会更新 bounded counters；并发超额请求产生 NotApplied/fallback/429；失败/fallback 会 release；secret-safe logs/snapshots；若本机缺 Docker/Postgres，则标记外部阻塞而不是继续增加 contract-only 切片。结果：新增 `scripts/verify_gateway_rate_limit_reservation_smoke.ps1` 与 live smoke fixture，覆盖 provider-key acquire counter mutation、fallback release、concurrent NotApplied -> fallback -> 429 和 secret-safe evidence；dry-run 与 targeted Gateway tests 通过。剩余缺口：本机 Docker daemon 未运行，尚未执行真实 Postgres/Gateway live smoke；E8 仍不能标 `[x]`。
- **E8-004-S5，已完成，2026-06-04，owner Mencius，live 执行阻塞**：rate-limit reservation live smoke 测试入口接入。原因：S4 已新增可运行 harness，但开发/CI 需要清晰 opt-in 入口和 Docker/Postgres preflight，避免 live smoke 被误当成普通 contract test 或因环境缺失造成假失败。范围：`scripts/**`、必要的测试/验收文档；不改 Gateway 生产逻辑。验收：现有测试入口能发现/调用 dry-run 或 opt-in live smoke；Docker/Postgres 不可用时输出明确阻塞；默认非 live 流程不被破坏；相关 script dry-run/check 通过。结果：rate-limit reservation live smoke 已接入 `scripts/test.ps1` 与 release smoke gate，默认 dry-run，显式 flag/env 才进入 preflight/live。剩余缺口：本机 Docker daemon 不可用，live Postgres/concurrency 验证待可用环境补跑。
- **E8-004-S6，已完成，2026-06-04，owner Mencius**：precise TPM estimate contract。原因：E8 runtime DB reservation 与 live smoke 入口已落地，但 `[x]` 仍需要 rate-limit key selection 对 token-per-minute reservation 有稳定、可解释、不过度乐观的估算 contract。范围：`crates/routing/**`、routing fixtures/tests；不改 Gateway/Control Plane/UI。验收：routing pure planner 对 prompt/completion/token estimate 的 TPM reservation 选择有 contract fixture；缺 usage/tokenizer 数据时使用明确保守 fallback；相关 routing tests 通过。结果：routing pure planner 新增 TPM reservation estimate contract，优先使用 total/prompt/completion/max completion 数值估算，缺 usage/tokenizer 时走 conservative fallback，并用 fixture/tests 锁定 secret-safe 输出。剩余缺口：Gateway runtime 尚未接入 tokenizer/usage 输入。
- **E8-004-S7，已完成，2026-06-04，owner Mencius**：TPM reservation estimate edge-case hardening。原因：S6 已定义 routing TPM estimate contract，后续 Gateway runtime 接线前需要锁定极端大数、最小 1-token clamp、negative sanitization 与 `i64` capacity clamp，避免 runtime 输入导致过度乐观或溢出。范围：`crates/routing/**`、routing fixtures/tests；不改 Gateway/Control Plane/UI/scripts。验收：新增/强化 edge-case tests 或 fixture，覆盖 overflow/minimum/negative fallback；routing tests 通过。结果：routing TPM estimate 增加 zero/negative/extreme edge-case contract，锁定最小 1-token clamp、负数 sanitize 后保守 fallback，以及 `i64::MAX` capacity clamp，fixture 同步记录安全输出。剩余缺口：Gateway runtime 尚未把实际 tokenizer/usage/fallback 配置接入该估算输入。
- **E8-004-S8，已完成，2026-06-04，owner Mencius**：TPM estimate 到 reservation capacity 的 runtime bridge contract。原因：S6/S7 已锁定估算本身，但 Gateway runtime 接线前还需要证明 `RateLimitTpmReservationEstimate` 转成 `RateLimitRequiredCapacity` 与 reservation plan 时不会丢失 clamp/保守 fallback 语义，也不会把 payload/window/provider secret material 写进输出。范围：`crates/routing/**`、routing fixtures/tests；不改 Gateway/Control Plane/UI/scripts。验收：新增或强化 fixture/test，覆盖 estimate source 到 `tokens_per_minute` required capacity 的转换、`required_tokens_i64` clamp、reservation dimension 使用的 TPM required 值、secret-safe serialized output；routing tests 通过。结果：新增 estimate -> required capacity -> reservation plan bridge contract，覆盖 total/prompt+completion/max-completion/fallback/partial source、`i64::MAX` clamp、TPM dimension required 值与 secret-safe serialized output，fixture 增加 `capacity_bridge_behaviors`。剩余缺口：Gateway runtime 仍需把 tokenizer/usage/fallback 配置接入 `RateLimitTpmEstimateInput`。
- **E8-004-S9，已完成，2026-06-04，owner Mencius**：Gateway TPM runtime wiring 设计/验收文档。原因：routing 纯 contract 已完成，但 Gateway 代码当前由 Prompt Protection native spy 切片占用；进入接线前需要先固定 endpoint/request token 信号到 `RateLimitTpmEstimateInput` 的映射、fallback 策略和验收测试清单，避免后续实现时和 prompt-protection side-effect ordering 混淆。范围：`docs/**`；不改 Gateway/Control Plane/UI/routing/scripts。验收：文档列出 OpenAI chat/responses、Anthropic、Gemini native 的可用 token/max-token 字段、缺失时 fallback、负数/大数处理、reservation timing、secret-safe logging/test plan；明确下一代码切片的文件边界和退出条件。结果：新增 `docs/E8-004_GATEWAY_TPM_RUNTIME_WIRING.md`，固定 OpenAI chat/responses、Anthropic、Gemini native 的 request-local token/max-output 映射、conservative fallback、负数/大数/0-token 处理、prompt-protection reject 与 reservation acquire ordering、secret-safe runtime metadata 白名单和下一代码切片退出条件。剩余缺口：Gateway runtime 代码尚未接入共享 TPM estimate mapper/reservation summary。
- **E8-004-S10，已完成，2026-06-04，owner Mencius**：Gateway TPM estimate mapper code contract。原因：S9 已固定设计，下一步先实现共享 mapper 和 DB-free endpoint mapping tests，再接 reservation acquire side effects，降低与 prompt-protection/provider-attempt ordering 的耦合风险。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：共享 helper 将 OpenAI chat/responses、Anthropic messages、Gemini native 的 request max-output/token signals 映射到 `RateLimitTpmEstimateInput` 或等价 safe summary；覆盖 missing/negative/large/fallback 行为；不执行 reservation acquire/provider side effects；targeted Gateway tests 与 diff check 通过。结果：新增 DB-free `tpm_estimate` mapper，覆盖 OpenAI chat `max_completion_tokens/max_tokens`、OpenAI Responses `max_output_tokens`、Anthropic `max_tokens`、Gemini native `generationConfig.maxOutputTokens` 到 routing TPM estimate 的映射；fixture 和单测锁定 missing tokenizer、partial fallback、negative sanitize、大整数 clamp、0-token minimum clamp，以及 safe summary 不泄露 raw prompt/body/header/provider secret。剩余缺口：mapper 尚未接入 runtime reservation acquire；真实 tokenizer/prompt-token estimate 仍未实现，当前只提供缺 tokenizer 时的保守 fallback contract。
- **E8-004-S11，已完成，2026-06-04，owner Mencius**：Gateway TPM estimate mapper runtime reservation bridge。原因：S10 已有 DB-free endpoint mapper，但 Gateway reservation acquire/release 仍使用固定 fallback token capacity；下一步先把 mapper 输出桥接到 request-local reservation required capacity 和 safe metadata，再逐步覆盖更多 endpoint/tokenizer signals。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：OpenAI chat/responses 至少使用 request max-output + conservative fallback 计算 TPM required capacity，并传入 acquire/release plan 与 DB execution required capacity；reservation metadata 展示 secret-safe TPM estimate summary；prompt-protection reject 仍在 reservation/provider side effect 前；不回显 raw prompt/body/header/provider secret；targeted Gateway tests、fmt check 和 diff check 通过。结果：OpenAI chat/responses 在 prompt-protection 后、provider attempt/provider key/upstream 前生成 TPM estimate，non-stream 和 streaming attempt loop 均将 `required_tokens_i64` 传入 acquire/release plan 与 DB required capacity；reservation metadata 增加 secret-safe `tpm_estimate` source/numeric summary，fixture 固定 partial fallback、missing max fallback 和 forbidden marker。剩余缺口：Anthropic/Gemini/embeddings 尚未接 runtime TPM mapper；当前 bridge 会从 bounded request body 再解析 JSON，后续可减少重复解析。
- **E8-004-S12，已完成，2026-06-04，owner Mencius**：Gateway TPM runtime bridge for native providers。原因：S11 已覆盖 OpenAI chat/responses，但 Gateway native provider paths 仍沿用旧固定 fallback capacity；最终 TPM reservation 需要 Anthropic Messages 和 Gemini native 的 max-output signals 也进入同一 dynamic capacity/metadata 边界。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：Anthropic `/v1/messages` 和 Gemini native generate/stream 至少使用 request max-output + conservative fallback 计算 TPM required capacity，并传入 acquire/release plan 与 DB execution required capacity；prompt-protection reject ordering 保持在 reservation/provider side effect 前；metadata secret-safe；targeted Gateway tests、fmt check 和 diff check 通过。结果：Anthropic Messages non-stream/stream 使用 `max_tokens + 1024` conservative fallback，Gemini native generate/stream 使用 `generationConfig.maxOutputTokens + 1024` conservative fallback，并将估算结果传入 acquire/release plan、DB required capacity 和 secret-safe reservation metadata；prompt-protection reject 仍早于 reservation/provider side effect。剩余缺口：embeddings 仍走旧 fixed fallback；真实 tokenizer/prompt token 信号和 live DB/provider e2e 仍未接入。
- **E8-004-S13，已完成，2026-06-04，owner Mencius**：Gateway TPM runtime bridge for embeddings fallback boundary。原因：S11/S12 已覆盖 OpenAI chat/responses、Anthropic Messages 和 Gemini native，但 embeddings 路径仍沿用旧固定 TPM fallback；E8 最终需要所有 Gateway rate-limitable endpoints 至少进入同一 safe TPM estimate metadata/capacity 边界。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：embeddings 请求在 prompt-protection 通过后、reservation/provider side effect 前生成 secret-safe TPM estimate；缺 tokenizer/usage 时使用明确 conservative fallback，输入数组/字符串不回显；estimate 传入 reservation acquire/release plan 与 DB required capacity；targeted Gateway tests、fmt check 和 diff check 通过。结果：新增 `OpenAiEmbeddings` TPM estimate endpoint；embeddings 在 prompt-protection 通过后、reservation/provider side effect 前生成 conservative fallback estimate，并将该 estimate 传入 reservation acquire/release plan 与 DB required capacity；metadata 只保留 endpoint/source/numeric summary，不输出 raw input/body/header/provider secret/current window。剩余缺口：embeddings 仍未接入 tokenizer 或可信 input-token 信号；本轮仍是 Gateway contract/unit 覆盖，未做 live DB/provider e2e。
- **E8-004-S14，已完成，2026-06-04，owner Mencius**：Gateway TPM trusted input-token signal boundary。原因：S11-S13 已让主要 Gateway endpoints 都进入 TPM estimate/capacity 边界，但 estimate 仍主要依赖 max-output 和 conservative fallback；后续需要先固定可信 input-token/tokenizer 信号如何进入 mapper，避免直接从 raw prompt/input 推断并泄露内容。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：新增或强化 DB-free mapper/summary contract，覆盖 trusted prompt/input token signal 存在、缺失、负数、溢出和 fallback；secret-safe summary 只输出 numeric/source，不输出 raw prompt/input；不改变 runtime provider side-effect ordering；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `GatewayTpmEstimateSignals::trusted_prompt_tokens` 与 `trusted_input_tokens`，固定 trusted prompt+max-output、input-only total tokens、缺失/负数/overflow/invalid fallback 行为；summary 仍只输出 numeric/source，不接收或输出 raw prompt/input/body/header/provider secret。剩余缺口：runtime 仍未接真实 tokenizer/input-token provider；后续接线必须只传可信数字结果，不能从 raw prompt/input 做 ad hoc 推断。
- **E8-004-S15，已完成，2026-06-04，owner Mencius**：Gateway TPM runtime trusted-signal source guard。原因：S14 已开放 trusted numeric signal API，但当前 runtime 仍应只传 missing-tokenizer fallback，直到有可信 tokenizer/read-model 数字来源；需要用 source-order/contract 测试防止 runtime 从 raw prompt/input 长度或内容做临时估算。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：新增或强化 runtime/source-order contract，证明 OpenAI chat/responses/embeddings、Anthropic、Gemini native 只传可信 numeric signal 或 explicit missing-tokenizer fallback；禁止 raw prompt/input/body string length 估算；metadata secret-safe；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 runtime/source-order guard，覆盖 OpenAI chat/responses/embeddings、Anthropic Messages 和 Gemini native，证明当前 runtime TPM estimate 只使用 `missing_tokenizer` 与 bounded conservative fallback，禁止 `trusted_*` helper、`new(...)` 和 raw prompt/input 长度/内容估算模式。剩余缺口：trusted numeric signal API 仍未接真实 tokenizer/read-model 来源；后续接线需更新该 guard。
- **E8-004-S16，已完成，2026-06-04，owner Mencius**：Gateway TPM trusted signal runtime wiring checklist。原因：S14/S15 已固定 trusted numeric signal API 和 runtime guard；真正接 tokenizer/read-model 前，需要列清每个 endpoint 可接受的可信 token 来源、禁止来源和退出条件，避免实现时重新打开 raw prompt/input 推断。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：新增或强化 Gateway-local checklist/fixture/test，按 endpoint 列出 trusted signal allowed sources、current missing-tokenizer status、future wiring exit condition 和 forbidden raw-material sources；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `trusted_signal_runtime_wiring_checklist` fixture/test，覆盖 OpenAI chat/responses/embeddings、Anthropic Messages、Gemini native 的 current missing-tokenizer status、allowed trusted numeric sources、future wiring exit condition、forbidden raw-material sources 和 no provider side-effect 边界。剩余缺口：仍未接真实 tokenizer/read-model numeric source。
- **E8-004-S17，已完成，2026-06-04，owner Mencius**：Gateway TPM trusted signal smoke handoff contract。原因：S16 已有 endpoint checklist；后续真正接 trusted tokenizer 前，需要给 smoke/acceptance 一个稳定 handoff contract，说明哪些 endpoint 仍 fallback、哪些证据能证明未来 trusted source 已接入。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：新增或强化 Gateway fixture/test，输出 per-endpoint handoff status、required evidence fields、forbidden evidence fields 和 live smoke closure conditions；不改变 runtime behavior；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `gateway_tpm_trusted_signal_smoke_handoff_v1` fixture/test，按 OpenAI chat/responses/embeddings、Anthropic Messages、Gemini native 输出当前 `fallback_missing_tokenizer` 状态、未来 tokenizer/read-model allowed sources、required evidence fields、forbidden raw-material fields 和 live smoke closure conditions；不改变 runtime behavior。剩余缺口：仍未接真实 trusted tokenizer/read-model numeric source；后续 live smoke 还需用该 handoff contract 证明 trusted signal 已在 reservation acquire 前接入且无 raw material 输出。
- **E8-004-S18，已完成，2026-06-04，owner Mencius**：Gateway TPM trusted signal smoke evidence projection contract。原因：S17 已定义 smoke/acceptance 需要采集的证据字段，但后续 smoke 仍需要 Gateway-local projection contract，证明现有 secret-safe TPM/reservation metadata 能稳定映射到这些证据字段，而不是依赖可见日志文案或 raw payload。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：新增或强化 Gateway fixture/test，按 endpoint 生成或验证 smoke evidence projection，覆盖 `tpm_estimate.*`、`required_capacity.tokens_per_minute`、`acquire.dimensions.tpm.required`、`db_required_capacity.tokens_per_minute` 和 `trusted_signal.*` 当前 fallback 状态；证明 capacity 字段一致、forbidden raw material 不出现；不改变 runtime behavior；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `gateway_tpm_trusted_signal_smoke_evidence_projection_v1` fixture contract 和 Gateway-local regression，覆盖 OpenAI chat/responses/embeddings、Anthropic Messages、Gemini native；证明 `tpm_estimate.required_tokens_i64`、reservation required capacity、acquire TPM required 和 DB required capacity 一致，projection evidence 只含 endpoint/source/numeric/status 字段且不泄露 raw prompt/input/body/header/provider/current-window material。剩余缺口：仍未接真实 trusted tokenizer/read-model numeric source；后续 live smoke 需要采集同形 evidence。
- **E8-004-S19，已完成，2026-06-04，owner Mencius**：Gateway TPM trusted numeric source availability contract。原因：S18 已证明 smoke evidence projection 稳定，但 Gateway runtime 仍处于 `missing_tokenizer` fallback；本切片锁定 trusted numeric source 的可用性边界，明确当前没有 tokenizer/read-model 时为何必须 fallback，以及未来 source 接入前不能使用 raw prompt/input 长度估算。范围：`apps/gateway/**`、必要 Gateway fixtures；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：新增或强化 Gateway fixture/test，覆盖 trusted numeric source available/unavailable/invalid 状态、source type whitelist（tokenizer/read-model）、numeric-only material policy、unavailable 时继续 conservative fallback、forbidden raw prompt/input/body/header/provider material；不改变 provider side-effect ordering；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `gateway_tpm_trusted_numeric_source_availability_v1` contract，区分 available/unavailable/invalid trusted numeric source，只允许 tokenizer/read-model numeric token material，并证明 request body/raw prompt/raw input/header/provider/body length 等 raw material 不能进入 TPM estimate；当前 runtime 仍保持 missing-tokenizer conservative fallback。剩余缺口：helper 尚未接入 runtime adapter；下一片需要证明 runtime 不会旁路 helper 或意外启用 raw source。
- **E8-004-S20，已完成，2026-06-04，owner Mencius**：Gateway TPM trusted numeric source runtime no-op integration guard。原因：S19 已提供 trusted numeric source helper，但 runtime 当前仍应只走 `missing_tokenizer` fallback；本切片把“尚未接入 tokenizer/read-model adapter 时不启用 trusted source”的运行时边界写清，避免后续接线前出现隐式 raw source 或 bypass helper。范围：`apps/gateway/**`、必要 Gateway fixtures/tests；不改 Control Plane/Admin UI/routing/scripts/billing-ledger。验收：新增或强化 runtime/source-order tests，证明当前 runtime 没有 tokenizer/read-model adapter 时仍只输出 missing-tokenizer fallback，trusted helper 不被 bypass，raw prompt/body/input/header/provider material 不进入 estimate summary；保留 provider side-effect ordering；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `gateway_tpm_trusted_numeric_source_runtime_noop_guard_v1` fixture/test，覆盖 OpenAI chat/responses/embeddings、Anthropic、Gemini native 当前仍使用 `GatewayTpmEstimateSignals::missing_tokenizer` 和 conservative fallback，且不调用 trusted source helper 或 raw length/tokenize bypass；safe summary 测试证明 raw prompt/input/body/header/provider/current-window material 不泄露。剩余缺口：tokenizer/read-model trusted numeric source 仍未接入 runtime；后续接入必须走 S19 helper/contract。
- **E8-004-S21 Performance，已完成，2026-06-04，owner Mencius**：Gateway rate-limit reservation performance smoke preflight contract。原因：E8 当前 contract 很多，但高性能目标需要把 live/preflight smoke 的并发、latency/TTFT、NotApplied/fallback、acquire/release 证据写成可验收输出，避免只证明“会跑”而不证明“高性能路径可观测”。范围：`scripts/verify_gateway_rate_limit_reservation_smoke.ps1`、必要 Gateway smoke fixtures/tests；不改 Control Plane/Admin UI/billing-ledger。验收：默认不跑 live；dry-run/preflight 输出 bounded performance evidence schema，包含 concurrency/latency/TTFT 或明确 unavailable marker、NotApplied/fallback rate、acquire/release count、secret-safe command summary；Docker/Postgres/Gateway 缺失时输出 blocker 而非假通过；相关 script self-test/default check 通过。结果：新增 `gateway_rate_limit_reservation_performance_evidence_v1` contract，dry-run 和 preflight 输出 bounded performance evidence，覆盖 concurrency、latency/TTFT、NotApplied/fallback rate、acquire/release expected counts、secret-safe command summary 和 blocker status；当前 Docker/Postgres/Gateway 不可用时输出 `status=blocked` 或 unavailable marker，不伪造 live latency/observed counts。剩余缺口：真实 live Postgres/Gateway smoke 仍未执行，实际 latency/TTFT/observed counts 仍需 live 环境采集，E8 不能标 `[x]`。
- **E8-004-S22 Runtime，已完成，2026-06-04，owner Mencius**：Gateway TPM trusted numeric source adapter boundary。原因：S19-S20 已证明 runtime 不能旁路 trusted source helper，S21 已补 smoke performance evidence；下一片需要为真实 tokenizer/read-model numeric source adapter 固定安全接入边界，避免后续直接用 raw prompt/body 长度估算。范围：`apps/gateway/**`、必要 Gateway fixtures/tests；不改 Control Plane/Admin UI/billing-ledger/scripts。验收：新增或强化 adapter boundary tests，覆盖 trusted source adapter unavailable/available/invalid、source type whitelist、numeric-only material、raw prompt/body/header/provider/window material omitted、fallback 保守且不 bypass helper、provider side-effect ordering 不变；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `GatewayTrustedNumericSourceAdapterOutput` 和 `gateway_trusted_numeric_source_availability_from_adapter(...)`，固定未来 tokenizer/read-model adapter 的 numeric-only 输出边界；fixture/test 覆盖 adapter unavailable、available tokenizer prompt、available read-model input、negative tokens invalid、raw source rejected，并证明 runtime 当前仍未接入 adapter，prompt-protection before estimate、estimate before reservation 顺序不变。剩余缺口：真实 tokenizer/read-model implementation 未接入，live smoke 仍需后续采集真实 trusted numeric source evidence。
- **E8-004-S23 Runtime，已完成，2026-06-04，owner Mencius**：Gateway tokenizer/read-model adapter readiness guard。原因：S22 已固定 adapter boundary，下一片需要在 runtime 接线前明确哪些 tokenizer/read-model provider 可用、缺失时如何 fallback、以及 adapter readiness evidence 如何进入 smoke report。范围：`apps/gateway/**`、必要 Gateway fixtures/tests；不改 Control Plane/Admin UI/billing-ledger/scripts。验收：新增或强化 readiness guard/tests，覆盖 tokenizer/read-model unavailable marker、adapter config disabled-by-default、numeric source feature availability marker、fallback conservative、safe summary、no raw material、runtime ordering 不变；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `gateway_tpm_trusted_numeric_source_readiness_v1` readiness guard，固定 tokenizer/read-model `disabled/unavailable/available` 状态、`feature_available`、`fallback_required`、`material_in_output=false` 和 smoke evidence projection；readiness disabled/unavailable 时强制走 `missing_tokenizer` conservative fallback，即使 adapter availability 有数字也不进入 runtime；runtime source-order guard 证明当前请求路径没有接入 readiness/adapter helper、没有从 raw prompt/body/input/header/provider/window material 估算 token，prompt-protection 仍早于 reservation/provider side effect。剩余缺口：真实 tokenizer/read-model runtime implementation、配置接入和 live trusted numeric source evidence 仍需后续验收。
- **E8-004-S24 Runtime，已完成，2026-06-04，owner Mencius**：Gateway trusted tokenizer/read-model config preflight gate。原因：S23 已固定 readiness guard，但真正 runtime 接线前还缺配置 preflight，明确哪些 env/config 可以启用 tokenizer/read-model trusted numeric source、默认禁用如何输出 blocker、以及启用但 provider unavailable 时如何继续 fallback。范围：`apps/gateway/**`、必要 Gateway fixtures/tests；不改 Control Plane/Admin UI/billing-ledger/scripts。验收：新增或强化 config/preflight contract，覆盖 default disabled、enabled-but-unavailable、available-ready 三类状态；输出 secret-safe readiness/performance evidence 字段，不回显 raw prompt/input/header/provider secret；不改变 provider side-effect ordering；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `gateway_tpm_trusted_numeric_source_config_preflight_v1` contract，覆盖 default disabled、tokenizer/read-model enabled-but-provider-unavailable、tokenizer/read-model ready 三态；输出 `feature_enabled`、`feature_available`、`fallback_required`、blocker、availability marker 和 duration marker 等 secret-safe evidence；runtime source-order guard 证明当前请求路径未接入真实 tokenizer/read-model config/preflight helper，prompt-protection 仍早于 estimate/reservation/provider side effect。剩余缺口：真实 tokenizer/read-model implementation、配置读取接入和 live smoke evidence 仍未实现。
- **E8-004-S25 Runtime，已完成，2026-06-04，owner Mencius**：Gateway trusted numeric source opt-in runtime evidence gate。原因：S24 已固定 config preflight；下一片需要定义显式 opt-in runtime evidence gate，说明未来启用 tokenizer/read-model 后如何在不泄露 raw prompt/input 的前提下证明 trusted numeric source 在 reservation 前生效，并输出高性能 evidence 名称。范围：`apps/gateway/**`、必要 Gateway fixtures/tests；不改 Control Plane/Admin UI/billing-ledger/scripts/E11/E13。验收：新增或强化 opt-in evidence contract，覆盖 disabled/blocker/ready 三态到 reservation evidence 的映射、duration/availability/source/token count marker 名称、forbidden raw material、prompt-protection before estimate before reservation ordering；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `gateway_tpm_trusted_numeric_source_opt_in_evidence_v1` evidence gate，覆盖 disabled、blocked、ready、ready-but-capacity-misaligned 四态；safe summary 输出 availability/preflight/estimate duration marker、source/token-count marker、TPM estimate/acquire/DB required capacity 对齐状态、reservation evidence readiness 和 live gap closure readiness；runtime ordering guard 证明当前没有把 tokenizer/read-model evidence 直接接入生产路径，也不从 raw prompt/input/body length 推断 token。剩余缺口：真实 tokenizer/read-model implementation、reservation evidence runtime projection 和 live DB/provider smoke 仍需后续验收。
- **E8-004-S26 Runtime，已完成，2026-06-04，owner Mencius**：Gateway trusted numeric source reservation evidence projection handoff。原因：S25 已定义 opt-in evidence gate，但最终 E8 `[x]` 还需要证明未来 trusted tokenizer/read-model 结果进入 reservation/smoke evidence 的字段、顺序和性能摘要；下一片先固定 projection handoff，避免 runtime implementation 后 evidence 丢失或泄露 raw prompt/input。范围：`apps/gateway/**`、必要 Gateway fixtures/tests；不改 Control Plane/Admin UI/billing-ledger/scripts/E11/E13。验收：新增或强化 reservation evidence projection contract，覆盖 trusted_source_evidence 字段进入 rate-limit reservation metadata/smoke evidence 的 blocked/ready/unavailable 三态、availability/preflight/estimate duration markers、source/token-count marker、capacity alignment、secret-safe omission、prompt-protection allow before estimate before reservation acquire ordering；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 trusted source reservation/smoke evidence projection handoff，固定 `rate_limit_reservation.trusted_source_evidence` 与 `smoke.rate_limit_reservation.trusted_source_evidence` 字段，覆盖 unavailable/blocked/ready projection、availability/preflight/estimate duration markers、source/token-count marker、capacity alignment、performance markers present 和 secret-safe material omission；runtime ordering guard 继续证明 prompt-protection allow 在 estimate 前、estimate 在 reservation acquire/provider side effect 前。剩余缺口：真实 tokenizer/read-model implementation、runtime evidence 写入和 live DB/provider smoke 仍需后续验收。
- **E8-004-S27 Runtime，已完成，2026-06-04，owner Mencius**：Gateway trusted numeric source runtime adapter boundary。原因：S26 已固定 projection handoff，下一步需要定义真实 tokenizer/read-model runtime adapter 的安全边界和 opt-in wiring guard，开始从 contract-only 走向 implementation，但仍禁止 raw prompt/input ad hoc 估算。范围：`apps/gateway/**`、必要 Gateway fixtures/tests；不改 Control Plane/Admin UI/billing-ledger/scripts/E11/E13。验收：新增或强化 runtime adapter boundary，覆盖 adapter trait/input/output、disabled/blocker/ready 三态、duration/availability/source/token-count evidence、raw material forbidden markers、no provider side effect、prompt-protection allow before adapter before reservation acquire ordering；targeted Gateway tests、fmt 和 diff check 通过。结果：新增 `GatewayTrustedNumericSourceRuntimeAdapter` trait、`GatewayTrustedNumericSourceRuntimeAdapterInput`/`Output`、`gateway_trusted_numeric_source_runtime_adapter_boundary` 和 safe summary，固定 disabled/blocked 跳过 adapter、ready 调用 adapter、adapter unavailable/material/provider-side-effect 时 blocker fallback；summary 只输出 endpoint/preflight/availability/source/token-count/adapter_invoked/fallback_required/conservative fallback 与 marker 名称，不接受 raw prompt/input/header/body/window/provider material；runtime ordering guard 继续证明当前生产路径没有接入 trusted adapter，也不从 `.len()`/`.chars()`/`.bytes()`/tokenize/raw material 推断 tokens。验证：`cargo test -p ai-gateway tpm_estimate -- --nocapture`、`cargo test -p ai-gateway rate_limit_reservation -- --nocapture`、`cargo test -p ai-gateway rate_limit -- --nocapture`、`cargo test -p ai-gateway prompt_protection -- --nocapture`、`cargo fmt -p ai-gateway --check` 和 scoped diff check 通过。剩余缺口：真实 tokenizer/read-model provider implementation、runtime wiring、live DB/provider smoke 仍需后续验收。
- **E8-004-S28 Runtime，进行中，2026-06-04，owner Mencius**：Gateway trusted numeric source runtime config wiring guard。原因：S27 已有 adapter trait/boundary，但生产路径仍没有配置化入口决定何时允许调用 adapter；下一片需要固定 runtime config/env wiring guard，确保未来开启 tokenizer/read-model 时必须经过显式 opt-in、preflight ready、secret-safe evidence 和 prompt-protection-before-adapter ordering。范围：`apps/gateway/**`、必要 Gateway fixtures/tests；不改 Control Plane/Admin UI/billing-ledger/scripts/E11/E13。验收：新增或强化 runtime config wiring guard，覆盖 env/config disabled default、tokenizer/read-model opt-in flag/env、provider unavailable blocker、adapter not wired blocker、ready-but-no-raw-material input、availability/preflight/estimate duration marker projection、no provider side effect、prompt-protection allow before config/preflight before adapter before reservation acquire ordering；targeted Gateway tests、fmt 和 diff check 通过。退出条件：只关闭 config wiring guard；真实 tokenizer/read-model provider implementation、actual runtime token counting 和 live DB/provider smoke 仍需后续验收。
- **E9-004-S4，已完成，2026-06-04，owner Confucius，live 执行阻塞**：billing ledger SQLx/live schema 验证。原因：ledger reserve/settle/refund 已有计划转换与 contract，但最终完成需要证明 SQLx query、schema、事务边界和幂等键在真实迁移结构上可执行。范围：`crates/billing-ledger/**`、ledger 相关 fixtures、必要的 integration harness；不改 Gateway/Control Plane 路由。验收：reserve/settle/refund 的 SQLx bind/returning/schema contract 固定；真实 Postgres 可用时跑 live schema smoke；不可用时记录明确外部阻塞和已覆盖的静态/contract 检查。结果：SQLx reserve/settle/refund executable schema contract 已固定 bind order、`RETURNING` columns、row-count-backed `fetch_all` execution、secret-safe operation-key debug，以及 `ledger_entry_id` status-update targeting。剩余缺口：未设置 `BILLING_LEDGER_LIVE_DATABASE_URL`，尚未在 migrated schema 上跑 live Postgres smoke。
- **E9-004-S5，已完成，2026-06-04，owner Confucius，live 执行阻塞**：billing-ledger SQLx live smoke opt-in/preflight 入口。原因：S4 已固定 SQLx schema contract，但 live DB URL 缺失时开发者需要明确入口、环境变量说明和 preflight failure，而不是把 contract-only 测试误读为 live 已完成。范围：`crates/billing-ledger/**`、`scripts/**`、billing fixtures 或验收文档；不改 Gateway/Control Plane/UI。验收：有明确命令可 dry-run/preflight SQLx live smoke；未设置 `BILLING_LEDGER_LIVE_DATABASE_URL` 时报告外部阻塞；默认测试不依赖 live DB；相关 billing-ledger tests/check 通过。结果：新增 opt-in billing-ledger SQLx live smoke preflight 入口；默认测试保持 DB-free，缺 `BILLING_LEDGER_LIVE_DATABASE_URL` 明确报告外部阻塞，live run 使用 `EXPLAIN` + transaction rollback 验证 reserve/settle/refund SQL shape。剩余缺口：尚未在 migrated live DB 上执行 preflight。
- **E9-004-S6，已完成，2026-06-04，owner Confucius**：billing-ledger SQLx executor runtime wiring readiness。原因：S4/S5 已固定 SQLx statement/schema/live preflight，但后续接 Control Plane/Gateway 前还需要 crate 内清晰证明 executor API 的事务结果、row-count error、secret-safe output 能作为 runtime writer 边界复用。范围：`crates/billing-ledger/**`、billing fixtures；不改 scripts/Gateway/Control Plane/UI。验收：新增或强化 executor-facing contract/test，覆盖 transaction result summary、row-count mismatch/refusal、operation key secret-safe debug/output；默认 tests 和 `postgres-sqlx` feature tests 通过。结果：新增 billing-ledger executor result summary contract，覆盖 transaction flags/counts、row-count refusal mapping、SQLx validation refusal，以及 secret-safe operation-key output/debug，且不依赖 live DB。剩余缺口：尚未把该 runtime writer 边界接入外部 app。
- **E9-004-S7，已完成，2026-06-04，owner Confucius**：Control Plane execute writer 与 billing-ledger executor summary 集成准备。原因：E11-S4 已有 Control Plane 本地最小 SQL writer，E9-S6 已有 billing-ledger executor summary；下一步需要锁定两者之间可迁移的 response/audit summary 边界，避免后续 runtime writer 接入时改变 UI/API 安全输出。范围：`apps/control-plane/**`、control-plane billing fixtures；不改 scripts/Gateway/Admin UI/routing。验收：Control Plane execute response 或 fixture 明确包含/预留 billing-ledger executor summary 兼容字段，保持 secret-safe；targeted control-plane billing/ledger tests 通过。结果：Control Plane ledger adjustment execute contract、applied response、idempotent replay response 与 success audit metadata 增加 `billing_ledger_postgres_executor_summary.v1` 兼容的 secret-safe summary 边界，固定 omitted operation key/error detail/raw metadata/credential 语义。剩余缺口：尚未把 Control Plane 本地 writer 迁移到 billing-ledger runtime writer；后续迁移需保持当前 `ledger_executor_summary` 字段语义不变。
- **E9-004-S8，已完成，2026-06-04，owner Confucius**：Control Plane execute refusal/rollback executor summary 边界。原因：S7 固定了 applied/idempotent success side summary，但 runtime writer 迁移还需要 blocked/refusal/rollback 侧的安全输出语义，避免 UI/API 在错误状态下泄露 operation key、raw metadata 或 executor error detail。范围：`apps/control-plane/**`、control-plane billing fixtures；不改 Gateway/Admin UI/routing/scripts。验收：execute preflight/refusal 或 rollback-adjacent contract 明确包含 compatible refusal summary 或 refusal summary contract；覆盖 committed/rolled_back、refused_statement_count、row_count_mismatch/error_detail_output 语义；保持 secret-safe；targeted control-plane ledger/billing tests 通过。结果：execute contract refusal response 增加 `refused_preflight` ledger executor summary，execute/transaction contract 预留 rollback summary contract，覆盖 committed/rolled_back、refused statement count、row-count mismatch 与 `error_detail_output=omitted`，fixture 同步并增加 secret-safe regression。剩余缺口：仍未迁移到 billing-ledger runtime writer；真实 DB write/audit insert 失败路径后续需映射到同一 summary 形状。
- **E9-004-S9，已完成，2026-06-04，owner Confucius**：Control Plane 到 billing-ledger runtime writer 的纯映射 contract。原因：S7/S8 已固定 response/refusal summary 形状，下一步需要先用 DB-free contract 锁定 Control Plane validated ledger adjustment plan 到 billing-ledger runtime writer operation/summary 的字段映射，再做真实 writer 替换。范围：`apps/control-plane/**`、必要的 `crates/billing-ledger/**` contract/tests 与 fixtures；不改 Gateway/Admin UI/routing/scripts。验收：新增或强化纯函数/test，覆盖 adjust/refund applied/idempotent/refusal 映射到 billing-ledger executor operation/outcome/statement summary；保持 operation key/raw metadata/credential omitted；不要求 live DB；targeted control-plane 与 billing-ledger tests 通过。结果：新增 `control_plane_billing_ledger_runtime_writer_mapping.v1` DB-free mapping contract，锁定 refund -> `ConsistentLedgerWriteRequest::RefundPartial`，adjust -> `admin_adjustment_extension_required`，并证明 applied/idempotent/refused_preflight/refused_rollback summary 与现有 Control Plane response shape 一致且不回显 operation key/raw metadata/credential/raw error detail。剩余缺口：未替换生产 SQL writer；billing-ledger 仍需 admin adjust operation 或替代 writer path。
- **E9-004-S10，已完成，2026-06-04，owner Confucius**：billing-ledger admin adjustment operation contract。原因：S9 已证明 refund 可映射到 `RefundPartial`，但 adjust 仍是 `admin_adjustment_extension_required`；迁移 Control Plane writer 前，billing-ledger 需要 DB-free admin adjustment operation/summary contract 或明确拒绝边界。范围：`crates/billing-ledger/**`、billing-ledger fixtures/tests；不改 Gateway/Admin UI/routing/scripts/Control Plane。验收：新增或强化 billing-ledger pure contract，覆盖 admin adjust confirmed credit/debit plan、idempotency/operation key secret-safe output、executor summary applied/idempotent/refusal shape；默认 tests 通过，不要求 live DB。结果：billing-ledger 新增 `AdminAdjustmentLedgerRequest`、`LedgerOperationKind::AdminAdjustment`、`LedgerEntryType::Adjust` 与 credit/debit metadata，`plan_ledger_admin_adjustment` 支持 confirmed credit/debit、零金额拒绝、idempotent replay 与 conflict refusal；Postgres execution/writer summary contract 支持 admin adjustment executor summary，输出继续隐藏 operation key/idempotency material/raw metadata/credential/raw executor detail。剩余缺口：尚未把 admin adjustment 接入 `ConsistentLedgerWriteRequest` / runtime command executor 的真实 write path；还没有 live SQLx admin adjustment write smoke；Control Plane writer 尚未迁移到 billing-ledger runtime writer。
- **E9-004-S11，已完成，2026-06-04，owner Confucius**：billing-ledger admin adjustment runtime writer bridge。原因：S10 已定义 admin adjustment operation contract，但 consistent writer/runtime command 仍缺将 admin adjustment request 转成可执行 writer command 的边界，Control Plane 迁移前需要先在 ledger crate 内固定该 bridge。范围：`crates/billing-ledger/**`；不改 Gateway/Control Plane/Admin UI/routing/scripts/docs。验收：`ConsistentLedgerWriteRequest` 或等价 runtime writer request 支持 admin adjustment credit/debit；覆盖 apply/idempotent/refusal 到 executor summary 的映射；SQL/Postgres command shape 保持 bounded、row-count enforced、operation key bind-only；public output 不含 operation key/raw metadata/credential/raw executor detail；默认 tests、`postgres-sqlx` feature contract tests、fmt/diff check 通过。结果：`ConsistentLedgerWriteRequest::AdminAdjustment` 支持 confirmed credit/debit，admin debit 进入余额/预算检查，admin credit 进入 credit/refund-like balance window；command executor、Postgres plan 与 SQLx adapter contract 覆盖 admin adjustment applied/idempotent/refusal summary，operation key 仍为 bind-only/private，public serialization/debug 不输出 raw operation key/raw metadata/credential/executor detail/idempotency material。剩余缺口：尚未替换 Control Plane writer；真实 Postgres I/O/live DB smoke 仍需外部环境。
- **E9-004-S12，已完成，2026-06-04，owner Confucius**：Control Plane billing-ledger writer mapping unblocks admin adjustment。原因：S9 的 Control Plane -> billing-ledger mapping 仍把 `adjust` 标为 `admin_adjustment_extension_required`，S10/S11 已在 billing-ledger 内补齐 admin adjustment operation/runtime writer bridge；下一步需要更新 Control Plane mapping contract，证明 adjust/refund 都能映射到 billing-ledger writer request。范围：`apps/control-plane/**`、必要 `crates/billing-ledger/**` tests/fixtures；不改 Gateway/Admin UI/routing/scripts/docs。验收：Control Plane validated adjustment/refund plan 映射到 `ConsistentLedgerWriteRequest::AdminAdjustment`/`RefundPartial` 或等价 writer request；覆盖 applied/idempotent/refused_preflight/refused_rollback summary compatibility；不要求 live DB；operation key/raw metadata/credential/raw error detail omitted；targeted control-plane 与 billing-ledger tests、fmt/diff check 通过。结果：Control Plane mapping contract 现在将 `adjust` 映射到 `ConsistentLedgerWriteRequest::AdminAdjustment`，`refund` 继续映射到 `RefundPartial`，并固定 writer request 字段、operation id 来源、scope/request/amount/currency 和 bind-only operation key；applied/idempotent/refused_preflight/refused_rollback summary compatibility 继续保持 secret-safe。剩余缺口：尚未把 Control Plane 真实 execute writer 迁移到 billing-ledger runtime writer；未做 live DB/runtime adapter 切换。
- **E9-004-S13，已完成，2026-06-04，owner Confucius**：Control Plane billing-ledger writer adapter boundary。原因：S12 已证明 adjustment/refund 都能映射到 billing-ledger writer request，但生产 execute path 仍使用 Control Plane 本地 SQL writer；迁移前需要先固定 adapter 输入、事务结果、错误摘要和 secret-safe 输出边界，避免一次性替换真实 writer。范围：`apps/control-plane/**`、必要 control-plane fixtures/tests；不改 Gateway/Admin UI/routing/scripts/docs。验收：新增或强化 DB-free adapter boundary contract，覆盖 execute validated plan -> billing-ledger writer request -> response/audit executor summary 的字段映射；applied/idempotent/refused_preflight/refused_rollback 均保持现有 API/UI summary shape；operation key/raw metadata/credential/raw executor error detail omitted；不要求 live DB；targeted control-plane tests、fmt 和 diff check 通过。结果：新增 `control_plane_billing_ledger_writer_adapter_boundary.v1`，固定 Control Plane execute validated plan -> billing-ledger writer request contract -> response/audit executor summary 的 DB-free adapter boundary，覆盖 AdminAdjustment/RefundPartial 与 applied/idempotent/refused_preflight/refused_rollback；success audit summary 只在 applied 时写入，secret-safe 输出保持现有 API/UI shape。剩余缺口：生产 execute path 仍未迁移到 billing-ledger runtime writer；未做 live DB 或真实 adapter 调用。
- **E9-004-S14，已完成，2026-06-04，owner Confucius**：Control Plane billing-ledger writer adapter refusal boundary。原因：S13 固定了 adapter 输入/输出和 success audit summary，但真实 writer 迁移前还需要锁定 adapter failure/refusal 分类，避免 billing-ledger runtime writer 返回 row-count mismatch、SQLx validation refusal 或 adapter unavailable 时破坏现有 execute_contract/refusal UI 语义。范围：`apps/control-plane/**`、必要 control-plane fixtures/tests；不改 Gateway/Admin UI/routing/scripts/docs。验收：DB-free contract 覆盖 adapter unavailable、row_count_mismatch、sqlx_validation_refusal 或等价 failure 分类到 refused_preflight/refused_rollback summary；operation key/raw metadata/credential/raw executor error detail omitted；不要求 live DB；targeted control-plane tests、fmt 和 diff check 通过。结果：adapter boundary 增加 `adapter_unavailable`/`sqlx_validation_refusal` -> `refused_preflight` 和 `row_count_mismatch` -> `refused_rollback` 分类，输出 UI-safe status/message 与 commit/rollback/row-count 语义，同时保持 `ledger_executor_summary` shape、operation key/error detail omitted 和 refused/idempotent 无 success audit summary。剩余缺口：尚未接入真实 billing-ledger runtime writer；未做 live DB/SQLx runtime refusal e2e。
- **E9-004-S15，已完成，2026-06-04，owner Confucius**：Control Plane billing-ledger writer adapter shadow invocation contract。原因：S13/S14 已固定 adapter success/refusal summary contract，但生产 writer 仍未替换；下一步需要 shadow invocation contract，证明未来调用 billing-ledger writer 时如何和当前本地 SQL writer 对齐且不产生双写。范围：`apps/control-plane/**`、必要 control-plane fixtures/tests；不改 Gateway/Admin UI/routing/scripts/docs。验收：DB-free contract 覆盖 shadow invocation enabled/disabled、no-double-write、response parity summary 和 audit parity summary；不要求 live DB、不替换生产 writer；secret-safe 输出保持；targeted control-plane tests、fmt 和 diff check 通过。结果：新增 `control_plane_billing_ledger_writer_shadow_invocation.v1`，覆盖 shadow disabled/enabled-contract-only、现有本地 SQL writer 仍为 source of truth、billing-ledger shadow 不允许 ledger/audit write 或 transaction commit、response/audit parity summary 与 operation key bind-only/secret-safe 输出；execute_contract、applied、idempotent 和 success audit metadata 均固定该 handoff。剩余缺口：尚未调用真实 billing-ledger runtime writer；未做 live DB 或 shadow runtime invocation e2e。
- **E9-004-S16，已完成，2026-06-04，owner Confucius**：Control Plane billing-ledger writer cutover preflight contract。原因：S15 已固定 shadow invocation 与 no-double-write 语义；真正替换 production writer 前，需要先锁定 cutover preflight、source-of-truth switch、rollback-to-local writer 和 refusal mapping，避免在没有 live DB 证据时误把 shadow contract 当成生产切换。范围：`apps/control-plane/**`、必要 control-plane fixtures/tests；不改 Gateway/Admin UI/routing/scripts/docs。验收：DB-free contract 覆盖 cutover disabled/shadow/ready 状态、feature flag/env 或等价配置摘要、source-of-truth 字段、no-double-write、rollback-to-local writer、secret-safe refusal output；不要求 live DB、不实际替换 production writer；targeted control-plane tests、fmt 和 diff check 通过。结果：新增 `control_plane_billing_ledger_writer_cutover_preflight.v1`，覆盖 `disabled`/`shadow`/`ready` 三种 DB-free 状态、`AI_CONTROL_PLANE_BILLING_LEDGER_WRITER_CUTOVER_MODE` 摘要、source-of-truth、no-double-write、rollback-to-local writer 和 adapter/sqlx/row-count refusal mapping；当前 contract 不允许 billing-ledger writer commit，也不替换生产 writer。剩余缺口：尚未解析真实 cutover mode 配置；未调用 billing-ledger runtime writer；未做 live DB/SQLx cutover preflight e2e。
- **E9-004-S17，已完成，2026-06-04，owner Confucius**：Control Plane billing-ledger writer cutover mode parser guard。原因：S16 已固定 cutover preflight contract，但配置模式仍只是摘要；本切片锁定 cutover mode 的解析/默认/非法值边界，确保未显式配置时保持 disabled，非法值不会进入 ready，也不会回显 raw env/secret。范围：`apps/control-plane/**`、必要 control-plane fixtures/tests；不改 Gateway/Admin UI/routing/scripts/docs。验收：新增或强化 parser/guard contract，覆盖 missing/default disabled、`disabled`、`shadow`、`ready`、case/whitespace normalization 或明确拒绝、invalid secret-safe refusal；invalid 不允许 production writer replacement 或 billing-ledger commit；operation key/raw metadata/credential/raw env value omitted；targeted control-plane tests、fmt 和 diff check 通过。结果：新增 billing-ledger writer cutover mode parser guard，固定 missing/blank 默认 disabled、`disabled`/`shadow`/`ready` 合法值、ASCII trim/lowercase normalization、invalid -> `disabled_guard_refusal`，并把 parser guard 嵌入 cutover preflight contract；非法值不会替换 production writer、不会允许 billing-ledger commit，raw env/secret/operation key/raw metadata 均保持 omitted。剩余缺口：parser 仍主要通过 contract/config helper 验证；下一片需要锁定实际 env/config integration 的 disabled-by-default 行为。
- **E9-004-S18，已完成，2026-06-04，owner Confucius**：Control Plane billing-ledger writer cutover env integration disabled-by-default guard。原因：S17 已固定 cutover mode parser guard；本切片把 parser 绑定到 env/config injection 摘要路径，证明未配置或非法配置不会让 billing-ledger runtime writer 接管生产写入。范围：`apps/control-plane/**`、必要 control-plane fixtures/tests；不改 Gateway/Admin UI/routing/scripts/docs。验收：新增或强化 env/config injection 或纯 adapter tests，覆盖 missing/invalid 默认 disabled、合法 `ready` 只有显式配置才可进入 ready 摘要、raw env/secret 不输出；runtime 仍不替换 production writer、不允许同请求双写；targeted control-plane tests、fmt 和 diff check 通过。结果：新增 `billing_ledger_writer_cutover_env_config_injection` summary contract，execute contract/applied/idempotent 响应通过 injected config path 且未注入时默认 disabled；missing/blank/invalid 保持 `control_plane_local_sql_writer`，合法 `ready` 只有显式配置才进入 ready 摘要；raw env/secret/operation key/raw metadata/credential 仍 omitted/bind-only。剩余缺口：仍是 DB-free/injected env-config contract；尚未接真实 runtime env provider，也未替换 production local SQL writer。
- **E9-004-S19 Performance，已完成，2026-06-04，owner Confucius**：billing-ledger writer runtime handoff performance guard。原因：E9 要从 local SQL writer/cutover contract 走向真实 runtime writer，下一步需要证明 handoff 摘要能承载 transaction latency、row-count、commit/rollback timing 等高性能/可观测字段，同时默认不接管生产写入。范围：`apps/control-plane/**`、必要 control-plane fixtures/tests；不改 Gateway/Admin UI/scripts/docs。验收：新增或强化 runtime writer handoff/cutover tests，覆盖 disabled-by-default、ready-but-not-cutover、latency/row-count/commit/rollback timing summary、double-write prevention、secret-safe output；targeted control-plane tests、fmt 和 diff check 通过。结果：新增 `control_plane_billing_ledger_writer_handoff_performance.v1` summary contract，cutover preflight 输出 `handoff_performance_summary`，覆盖 disabled、ready-but-not-cutover、row-count mismatch rollback refusal，包含 transaction/begin/lock/statement/commit/rollback latency 字段、row-count/refused count/mismatch、bounded statement scope、no-double-write 和 secret-safe output；当前 contract 明确 `db_io_performed=false`、`production_writer_replaced=false`、`billing_ledger_writer_commit_allowed_in_this_contract=false`。剩余缺口：真实 billing-ledger DB writer 接管和 live cutover 仍未实现，必须单独验收。
- **E9-004-S20 Runtime，已完成，2026-06-04，owner Confucius**：Control Plane billing-ledger runtime env provider preflight。原因：S19 已固定 handoff performance summary，但仍没有把真实 runtime env/config provider、feature gate 和 live cutover preflight 的边界写清；下一片需要证明配置来源可以被安全读取和汇总，同时仍不替换 production writer。范围：`apps/control-plane/**`、必要 control-plane fixtures/tests；不改 Gateway/Admin UI/scripts/docs。验收：新增或强化 runtime env/config provider/preflight tests，覆盖 missing/invalid/disabled/shadow/ready、live DB URL presence marker、runtime writer feature availability marker、handoff performance summary passthrough、no double write、secret-safe output；targeted control-plane tests、fmt 和 diff check 通过。结果：新增 `control_plane_billing_ledger_writer_runtime_env_provider_preflight.v1` contract，覆盖 cutover mode provider、`BILLING_LEDGER_LIVE_DATABASE_URL` presence marker、`billing_ledger_runtime_writer` feature availability marker、handoff performance passthrough 和 live cutover prerequisites；missing/invalid/disabled/shadow/ready 均保持 `production_writer_replaced=false`、`billing_ledger_writer_commit_allowed_in_this_contract=false`、no double write，DB URL 和 env 原值只输出 omitted/presence marker。剩余缺口：真实 billing-ledger writer 接管、live DB cutover 和 production source-of-truth switch 仍需单独验收。
- **E9-004-S21 Runtime，已完成，2026-06-04，owner Confucius**：billing-ledger runtime writer readiness smoke wrapper。原因：S20 已固定 env provider preflight，但后续 live cutover 还需要一个默认 DB-free、显式 opt-in 的 readiness smoke wrapper 来分类 live DB/runtime writer unavailable、ready、blocked，并复用 performance/env summaries。范围：`scripts/**` 中 billing-ledger/runtime writer readiness 相关脚本、必要 `apps/control-plane/**` contract helper/tests；不改 Gateway/Admin UI/routing。验收：新增或强化 script/preflight contract，默认不接 live DB、不切 production writer；显式 flag/env 才检查 live DB URL/runtime writer availability；输出 readiness、duration、row-count/transaction summary unavailable marker 或 blocker；secret-safe；targeted control-plane/script checks、fmt/diff check 通过。结果：新增 `control_plane_billing_ledger_writer_readiness_smoke_wrapper.v1` contract 和 `scripts/verify_control_plane_billing_ledger_runtime_writer_readiness.ps1`；默认 DB-free 输出 `readiness=unavailable`，显式 `-Live`/env 才检查 live DB URL presence 和 runtime writer feature marker，ready/blocked/unavailable 均输出 duration、row-count/transaction/handoff performance unavailable marker、no-double-write 和 secret-safe summary，不回显 DB URL/env/raw metadata/credential。剩余缺口：真实 billing-ledger writer 接管、live DB cutover、production source-of-truth switch 仍需后续单独验收。
- **E9-004-S22 Runtime，已完成，2026-06-04，owner Confucius**：billing-ledger live cutover blocker/evidence matrix。原因：S21 已有 readiness wrapper，但仍需要把 live cutover 前必须满足的 evidence matrix 固定，包括 migrated DB、runtime writer feature、source-of-truth switch、rollback path、performance summaries 和 blocker 分类。范围：`scripts/**` billing-ledger runtime readiness 相关脚本、必要 `apps/control-plane/**` contract/tests；不改 Gateway/Admin UI/routing。验收：新增或强化 live cutover blocker/evidence matrix contract，覆盖 ready/pass/blocker/fail 分类、required env/tool/schema markers、performance summary required fields、rollback/local-writer fallback marker、secret-safe output；默认不切 production writer；targeted tests/script checks、fmt/diff check 通过。结果：新增 `control_plane_billing_ledger_live_cutover_evidence_matrix.v1`，固定 migrated DB presence、runtime writer feature、source-of-truth switch guard、rollback/local writer fallback、performance summaries 和 schema markers 的 required evidence；readiness wrapper 输出 `evidence_matrix`，默认 `classification=blocker`，显式 live/ready/feature opt-in 时可输出 `classification=pass`，但仍保持 `production_source_of_truth_switch_allowed=false`、`billing_ledger_writer_commit_allowed=false`、`dual_commit_allowed=false`；DB URL/env/operation key/credential/raw executor detail 均不回显。剩余缺口：真实 billing-ledger writer 接管、live DB cutover、production source-of-truth switch 和真实 row-count/transaction performance evidence 仍需后续验收。
- **E9-004-S23 Runtime，已完成，2026-06-04，owner Confucius**：billing-ledger runtime writer dry-run execution evidence gate。原因：S22 已固定 cutover evidence matrix，但 `pass` 仍是 readiness-level marker；下一片需要证明在不切 production writer 的前提下，可以形成一次 billing-ledger runtime writer dry-run execution evidence，包含 bounded command count、row-count expectation、transaction step timing 名称和 rollback/fallback guard。范围：`scripts/**` billing-ledger readiness 相关脚本、必要 `apps/control-plane/**` contract/tests 或 `crates/billing-ledger/**` dry-run contract；不改 Gateway/Admin UI/routing/OpenAPI。验收：新增或强化 dry-run execution evidence contract，默认不写 DB、不切 writer、不 dual commit；输出 bounded command/row-count/transaction timing evidence 名称、failure/blocker classification、secret-safe summary；targeted tests/script checks、fmt/diff check 通过。结果：新增 `control_plane_billing_ledger_runtime_writer_dry_run_execution_evidence.v1`，readiness wrapper 输出 `dry_run_execution_evidence`，包含 bounded command count、command kinds、row-count expectations、transaction timing names、rollback/local writer fallback guard 和 blocker/fail classification；默认和 ready opt-in 都保持 `db_write_performed=false`、`production_writer_replaced=false`、`billing_ledger_writer_commit_allowed=false`、`dual_commit_allowed=false`，DB URL/env/operation key/raw metadata/credential/raw executor detail 均不回显。剩余缺口：真实 billing-ledger runtime writer commit、production source-of-truth cutover 和 live DB transaction/row-count/timing 采样仍需后续验收。
- **E9-004-S24 Runtime，已完成，2026-06-04，owner Confucius**：billing-ledger live execution blocker-to-command handoff。原因：S23 已固定 dry-run execution evidence，但还缺把 blocker matrix 转成可执行的 live command handoff，明确在具备 DB/runtime writer 后如何跑一次不切 production writer 的 live execution probe，并如何采集 row-count/timing evidence。范围：`scripts/**` billing-ledger readiness/runtime writer 相关脚本、必要 `apps/control-plane/**` contract/tests；不改 Gateway/Admin UI/routing/OpenAPI/E11/E13。验收：新增或强化 live execution handoff contract，输出 safe live probe command、required env/flag、expected blocker/pass/fail classification、row-count/timing evidence names、rollback/no-commit guard；默认不写 DB、不切 writer、不 dual commit；targeted tests/script checks、fmt/diff check 通过。结果：新增 `control_plane_billing_ledger_live_execution_handoff.v1` contract 和 `-LiveExecutionProbe` script handoff，输出 safe live probe command、required env markers、blocker/pass/fail classification、row-count evidence names、transaction timing names、rollback/no-commit guard 与 secret-safe output；默认 probe_requested=false 且 blocker，ready opt-in 输出 pass handoff 但 `probe_commits_billing_ledger=false`、`production_writer_replaced=false`、`dual_commit_allowed=false`，缺 live DB URL 时按预期 blocker exit 2。剩余缺口：真实 billing-ledger DB writer commit、row-count measurement 和 production cutover 仍需后续 live 验收。
- **E9-004-S25 Runtime，已完成，2026-06-04，owner Confucius**：billing-ledger live probe evidence artifact contract。原因：S24 已给出 live execution handoff，但真实 DB writer 验收前还需要固定 probe 执行后的 evidence artifact 结构，明确 row-count/timing/rollback measurement 如何判定 pass/fail/blocker，避免未来 live probe 只输出日志而无法关闭验收缺口。范围：`scripts/**` billing-ledger readiness/runtime writer 相关脚本、必要 `apps/control-plane/**` contract/tests；不改 Gateway/Admin UI/routing/OpenAPI/E11/E13。验收：新增或强化 live probe evidence artifact contract，覆盖 blocker/pass/fail 三态、row-count actual/expected fields、transaction timing duration fields、rollback/no-commit proof、production writer unchanged、secret-safe command summary；默认不写 DB、不切 writer、不 dual commit；targeted tests/script checks、fmt/diff check 通过。结果：新增 `control_plane_billing_ledger_live_probe_evidence_artifact.v1`，固定 blocker/pass/fail 三态、row-count expected/actual/rows_match/mismatch classification、transaction timing duration fields、rollback/no-commit proof、production writer unchanged 和 secret-safe command summary；默认和 ready handoff 均不写 DB、不切 writer、不 dual commit，ready handoff 下 artifact 仍保持 measurement blocker，`-SimulateProbeRowCountMismatch` 只用于 contract fail 形状验证且不触 DB。剩余缺口：真实 billing-ledger runtime writer commit、live row-count/timing/rollback measurement 和 production cutover 仍需后续验收。
- **E9-004-S26 Runtime，已完成，2026-06-04，owner Confucius**：billing-ledger live probe executor boundary。原因：S25 已固定 post-probe evidence artifact，下一步需要定义真实 DB probe executor 的事务边界、rollback/no-commit 语义和 row-count measurement 输入，确保后续接入 live DB 时不会切 production writer 或双写。范围：`scripts/**` billing-ledger readiness/runtime writer 相关脚本、必要 `apps/control-plane/**` contract/tests；不改 Gateway/Admin UI/routing/OpenAPI/E11/E13。验收：新增或强化 live probe executor boundary contract，覆盖 begin/lock/insert/mark/rollback step ordering、row-count actual/expected capture、timing duration capture、rollback required、commit forbidden、production writer unchanged、secret-safe output；默认不写 DB、不切 writer、不 dual commit；targeted tests/script checks、fmt/diff check 通过。结果：新增 `control_plane_billing_ledger_live_probe_executor_boundary.v1`，固定 rollback-only probe executor step ordering、row-count capture fields、begin/lock/insert/mark/row-count/rollback timing duration fields、row-count expectation inputs、rollback required、commit forbidden、production writer unchanged、dual commit forbidden、bounded statement scope 和 secret-safe output；readiness script 输出 `live_probe_executor_boundary`，默认 `db_write_performed=false`，`-Live -LiveExecutionProbe` 也只进入 handoff/probe boundary，不替换 production writer、不提交 billing ledger、不回显 DB URL/env/operation key/raw metadata。验证：`cargo test -p ai-control-plane ledger_adjustment_billing_runtime -- --nocapture`、`cargo test -p ai-control-plane ledger_adjustment -- --nocapture`、默认 readiness script、`-Live -LiveExecutionProbe` secret-safe run、`cargo fmt -p ai-control-plane --check` 和 scoped diff check 通过。剩余缺口：真实 billing-ledger runtime writer commit、production source-of-truth cutover、live DB begin/lock/insert/rollback executor measurement 仍需后续验收。
- **E9-004-S27 Runtime，进行中，2026-06-04，owner Confucius**：billing-ledger live probe executor measurement artifact contract。原因：S26 已固定 rollback-only executor boundary，但 row-count/timing 仍是 pending inputs；下一片需要定义真实 probe executor 运行后如何把 actual row-count、duration、rollback/no-commit proof 写入 evidence artifact，并拒绝缺失或 stale measurement。范围：`scripts/**` billing-ledger readiness/runtime writer 相关脚本、必要 `apps/control-plane/**` contract/tests；不改 Gateway/Admin UI/routing/OpenAPI/E11/E13。验收：新增或强化 live probe measurement artifact contract，覆盖 row-count actual/expected/rows_match、begin/lock/insert/mark/row-count/rollback duration、rollback observed、commit observed false、production writer unchanged、stale/missing measurement refusal、secret-safe DB URL/env/operation key omission；默认不写 DB、不切 writer、不 dual commit；targeted tests/script checks、fmt/diff check 通过。退出条件：只关闭 measurement artifact contract；真实 DB writer commit 与 production cutover 仍需后续 live 验收。
- **E11-007-S4，已完成，2026-06-04，owner Goodall，live 执行阻塞**：ledger adjustment execute 后端真实写入边界。原因：Control Plane 目前已有 execute preflight contract，但 Billing/Price 页面最终验收需要后端存在可事务化写 ledger + success audit 的真实边界或明确阻塞点。范围：`apps/control-plane/**`、`examples/openapi_admin_skeleton.yaml`、control-plane fixtures；不改 Admin UI。验收：execute 前置校验、dedupe key、billing permission、transaction plan、success audit 同事务边界被 contract 固定；若缺少底层 ledger writer，则写明不可伪造执行并保留阻塞。结果：`mode=execute` 已新增最小真实事务 writer：preflight 后 revalidate/lock bounded rows，插入 confirmed ledger entry 与 success audit，共用同一 DB transaction；server-generated dedupe material 不回显，idempotent replay 返回既有 bounded ledger entry 且不伪造新 ledger/audit write。剩余缺口：未跑 live Postgres integration/smoke；真实 FK、并发 refund race、admin session audit actor 仍需 DB 环境验证。
- **E11-007-S6，已完成，2026-06-04，owner Goodall，live 执行阻塞**：Control Plane ledger adjustment execute live smoke。原因：S4 已接入最小事务 writer，但最终验收需要证明真实 migrated schema 上 FK、audit actor、idempotent replay、refund remaining recompute 与并发 race 行为可运行。范围：`scripts/**`、`tests/fixtures/control-plane/**`、必要的 control-plane smoke harness；不改 Admin UI/Gateway。验收：live DB 可用时能执行 apply + idempotent replay + refund over-remaining refusal + audit same-transaction evidence；DB 不可用时明确外部阻塞；默认测试不依赖 live DB；targeted control-plane checks 通过。结果：新增 Control Plane ledger adjustment execute live Postgres smoke 入口，覆盖 apply、idempotent replay、over-remaining refusal、concurrent refund race 与 success-audit evidence；contract-only checks 通过。剩余缺口：当前本机 Docker/compose Postgres/control-plane 不可用，真实 live run 外部阻塞。
- **E11-007-S8，已完成，2026-06-04，owner Goodall，live 执行阻塞**：Control Plane ledger adjustment execute smoke 测试入口接入。原因：S6 已新增可运行 harness，但开发/CI 需要明确默认 contract-only 与 opt-in live gate，避免真实 DB 不可用时造成默认失败。范围：`scripts/**`、必要验收文档；不改 Gateway/Admin UI/billing-ledger。验收：test/release 入口能默认跑 contract-only；显式 flag/env 才跑 live；Docker/Postgres/control-plane 不可用时明确阻塞；相关 script checks 通过。结果：ledger adjustment execute smoke 已接入 test/release gates：默认 contract-only，显式 test/release flag/env 才进入 live Postgres concurrency evidence。剩余缺口：当前本机 Docker compose Postgres/control-plane 不可用，live run 外部阻塞。
- **E11-007-S9，已完成，2026-06-04，owner Goodall，live 执行阻塞**：ledger execute live smoke runbook/acceptance blocker 文档。原因：S6/S8 已有 live harness 和 opt-in gate，但最终验收还需要明确操作手册和阻塞条件，方便有 Docker/Postgres 环境的人复跑并关闭 live 缺口。范围：`docs/**`、`TEST_AND_ACCEPTANCE.md`；不改 Gateway/Control Plane/Admin UI/billing-ledger/routing。验收：文档包含 contract-only 默认命令、live opt-in 命令、所需 compose services、预期 blocker exit code、通过后可关闭的 TODO 缺口。结果：`TEST_AND_ACCEPTANCE.md` 新增 ledger adjustment execute live smoke runbook，明确 contract-only 默认命令、test/release live opt-in flag/env、Docker compose Postgres/control-plane 条件、direct live blocker exit code `2` 与 `[BLOCKED]` 输出，以及 live 通过后可关闭 E11-S4/S6/S8 backend live 缺口。剩余缺口：当前本机仍无可用 Docker/compose Postgres/control-plane live 环境，真实 live run 需要外部环境补跑。
- **E11-007-S11，已完成，2026-06-04，owner Goodall**：ledger adjustment execute OpenAPI/acceptance contract 同步。原因：E11-S7/E9-S7 已让 UI 和 Control Plane 使用真实 execute response 与 `ledger_executor_summary` 边界，但 OpenAPI skeleton/acceptance text 需要同步最新 applied/idempotent/executor-summary/secret-safe 字段，避免客户端按旧 contract 生成。范围：`examples/openapi_admin_skeleton.yaml`、必要的 `TEST_AND_ACCEPTANCE.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger 代码。验收：OpenAPI 描述 `mode=execute` applied/idempotent response、`ledger_executor_summary_contract`/`ledger_executor_summary`、dedupe material omitted、raw metadata/credential 不回显；必要文档指向对应 contract；OpenAPI/yaml diff check 通过。结果：OpenAPI skeleton 同步 applied/idempotent execute response、execute_contract refusal response、executor summary/refusal/rollback schemas 与 secret-safe omission 语义；acceptance runbook 增加 executor summary live smoke 检查。剩余缺口：未运行完整 OpenAPI validator/client generation；live Postgres smoke 仍需外部环境。
- **E11-007-S13，已完成，2026-06-04，owner Goodall**：ledger execute OpenAPI contract validation gate。原因：S11 已同步 OpenAPI skeleton，但目前只有 YAML parse 和人工 diff；需要一个轻量可运行检查锁定 ledger adjustment execute/executor summary refs，避免后续 schema drift。范围：`scripts/**`、`examples/openapi_admin_skeleton.yaml`、必要验收文档；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger 代码。验收：新增或强化脚本/检查能 parse OpenAPI YAML，并断言 `/admin/ledger/adjustments/dry-run`、execute applied/idempotent schema、execute_contract refusal schema、executor summary/refusal/rollback refs 和 secret-safe omission markers 存在；默认不依赖外部服务；diff check 通过。结果：新增 `verify_control_plane_ledger_adjustment_openapi_contract.ps1`，默认无外部服务依赖，锁定 ledger execute OpenAPI refs 和 secret-safe omission markers，并接入 `scripts/test.ps1 -ControlPlaneLedgerAdjustmentExecuteSmokeOnly` 与 release smoke gate。剩余缺口：该 gate 不替代完整 OpenAPI semantic validator/client generation；live Postgres smoke 仍需外部环境。
- **E11-007-S14，已完成，2026-06-04，owner Goodall**：ledger execute OpenAPI semantic validator/client generation runbook。原因：S13 已提供轻量默认 gate，但完整 OpenAPI semantic validator 和 client generation 仍是独立验收项，需要明确工具、命令、外部依赖、失败解释和关闭条件。范围：`docs/**`、必要 `TEST_AND_ACCEPTANCE.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/scripts。验收：文档列出推荐 validator/client-generation 命令、依赖安装方式、离线/缺工具 blocker 语义、需要检查的 generated client contract、通过后可关闭的 E11 OpenAPI 缺口，以及不能关闭的 live Postgres/UI/audit 缺口；diff check 通过。结果：新增 `docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`，记录 Redocly/OpenAPI Generator/Python validator、`openapi-typescript`/`typescript-fetch` client generation、离线/缺工具 blocker、generated client 字段检查和 secret-safe omission 检查；`TEST_AND_ACCEPTANCE.md` 增加 11.7 入口。剩余缺口：本切片只提供 runbook，未实际运行完整 semantic validator/client generation；live Postgres、success audit live evidence、Admin UI E2E 和 billing-ledger runtime writer 仍是独立缺口。
- **E11-007-S16，已完成，2026-06-04，owner Goodall**：ledger execute OpenAPI semantic validation script/preflight。原因：S14 已写 runbook，但开发/CI 仍需要一个 opt-in wrapper，把默认轻量 gate、外部工具 blocker、semantic validation failure 和 client-generation mismatch 区分清楚。范围：`scripts/**`、`docs/**`、必要 `TEST_AND_ACCEPTANCE.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger 代码。验收：新增或强化脚本，默认不下载包、不生成客户端且能跑轻量 OpenAPI gate；显式 flag/env 才运行 Redocly/OpenAPI Generator/openapi-typescript；缺工具/离线 package cache exit `2`，schema/client mismatch exit `1`，全部通过 exit `0`；临时 generated artifacts 可清理且不提交；diff check 通过。结果：新增 `scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`，默认只运行 lightweight OpenAPI gate；`-Semantic`/`-Redocly`/`-OpenApiGeneratorValidate` 才运行 semantic validators，`-ClientGeneration`/`-OpenApiTypescript`/`-TypescriptFetch` 才生成临时客户端；缺外部工具或离线 npm cache 返回 exit `2`，contract mismatch 返回 exit `1`，通过返回 exit `0`；runbook 与 TEST 同步命令、artifact 和 exit 语义。剩余缺口：未执行真实 Redocly/OpenAPI Generator/openapi-typescript 全通过流程；live Postgres、success audit live evidence、Admin UI E2E 和 billing-ledger runtime writer 仍是独立缺口。
- **E11-007-S19，已完成，2026-06-04，owner Goodall**：ledger execute OpenAPI semantic wrapper failure-path self-test。原因：S16 已有 opt-in wrapper 和 exit `0/1/2` 语义，但 blocker/mismatch 分支仍主要靠人工触发；后续需要脚本级自测锁住默认轻量 gate、外部 blocker 和 schema/client mismatch 的退出码，避免 CI/开发入口漂移。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger，也不改 Prompt Protection 脚本。验收：新增或强化 self-test/simulated failure switches，在不下载 npm 包、不生成真实客户端、不连接 live DB 的情况下证明 default exit `0`、simulated external blocker exit `2`、simulated schema/client mismatch exit `1`；runbook 记录命令和边界；默认 wrapper 行为不变；diff check 通过。结果：新增 `-SelfTest` 与 env opt-in，子进程验证 default lightweight path exit `0`、`-SimulateExternalBlocker` exit `2`、`-SimulateSchemaMismatch`/`-SimulateClientMismatch` exit `1`；自测不下载 npm、不生成真实客户端、不连接 DB/服务，并检查输出不包含 Authorization/Cookie/bearer token/credential/raw operation-key 等材料。剩余缺口：未替代真实 Redocly/OpenAPI Generator/openapi-typescript 运行；live Postgres、真实 semantic/client generation、UI E2E 与 billing-ledger runtime writer 仍未关闭。
- **E11-007-S20，已完成，2026-06-04，owner Goodall**：ledger execute OpenAPI semantic wrapper path/output hardening。原因：S16/S19 已有 wrapper 和 failure-path self-test，但临时目录、npm cache、generated artifact 输出和 failure tail 的边界还需要更强 contract，避免外部工具路径或错误输出泄露 secret 或写出 repo 外。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection。验收：wrapper 对 `-TempRoot`/`-NpmCache` repo 内约束、artifact cleanup、child output tail redaction、external tool command display 有 self-test/contract 覆盖；默认 lightweight 行为不变；`-SelfTest`、默认 wrapper、`-Clean` 和 diff check 通过。结果：`-TempRoot` 限制在 repo `.tmp` 下，`-NpmCache` 限制在 repo `.tool-cache` 或 `.tmp` 下；`-Clean` 只清理受限 TempRoot；外部工具命令、成功 tail 与失败输出统一 redaction；`-SelfTest` 覆盖敏感 output tail/command failure redaction、路径逃逸拒绝和 artifact cleanup。剩余缺口：仍未运行真实 Redocly/OpenAPI Generator/openapi-typescript；live Postgres、真实 semantic-client generation、UI E2E 和 billing-ledger runtime writer 缺口未关闭。
- **E11-007-S22，已完成，2026-06-04，owner Goodall**：ledger execute OpenAPI generated-client inspection contract。原因：S16/S19/S20 已把 wrapper 默认/失败/路径输出边界锁住，但真实 client generation 通过后还需要有稳定 inspection contract，明确 generated client 必须包含哪些 execute response/executor summary 字段且哪些 secret 字段必须缺失。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection/TEST。验收：新增或强化 generated-client inspection helper/contract，能在模拟 generated files 或 wrapper self-test 中证明 required fields present、secret fields absent、missing required field exit `1`；不下载真实包；默认 lightweight 行为不变；`-SelfTest`、默认 wrapper、`-Clean` 和 diff check 通过。结果：wrapper 新增 generated-client inspection contract，真实 `openapi-typescript`/`typescript-fetch` 生成成功后检查 ledger execute result、execute contract envelope、executor summary/refusal/rollback summary 模型和字段；self-test 模拟 required fields present/secret fields absent exit `0` 与 missing required field exit `1`。剩余缺口：真实 Redocly/OpenAPI Generator/openapi-typescript 仍需有工具缓存或联网环境 opt-in 执行。
- **E11-007-S24，已完成，2026-06-04，owner Goodall**：ledger execute OpenAPI semantic validator opt-in evidence contract。原因：S22 已锁定 generated client inspection，但完整 semantic validator 通过后还需要有 bounded evidence 记录 validator/tool/version、checked schema 和 blocker/failure 分类，避免只看 console 输出。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection/TEST。验收：新增或强化 opt-in evidence summary/report/self-test，覆盖 semantic validator pass/fail/blocker 的 bounded fields、secret-safe output、exit `0/1/2`；不下载真实包；默认 lightweight 行为不变；`-SelfTest`、默认 wrapper、`-Clean`、diff check 通过。结果：wrapper 新增 `ledger_openapi_semantic_evidence.v1` bounded evidence report，记录 validator/client tool、version、package、checked schema、classification、redacted command、bounded output tail 和 failure/blocker reason；self-test 覆盖 semantic evidence pass/failure/blocker 三条模拟路径并锁定 exit `0/1/2`；默认 lightweight 不下载 npm、不生成 client、不写 evidence report。剩余缺口：真实 Redocly/OpenAPI Generator/openapi-typescript opt-in 仍未运行；evidence report lifecycle/path/cleanup 仍需继续固定。
- **E11-007-S27 Backend，已完成，2026-06-04，owner Goodall**：ledger execute OpenAPI semantic evidence report lifecycle contract。原因：S24 已新增 opt-in evidence report，但 report artifact 的生成、覆盖、清理和 stale evidence 边界还需要锁住，避免默认 gate 或 `-Clean` 后遗留旧报告被误读为新 validator 证据。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection/TEST。验收：新增或强化 self-test/contract，证明默认 lightweight 不写 report，opt-in/simulated report 只写在 validated temp root，`-Clean` 只删除 wrapper-owned temp artifacts 且移除 stale evidence，repo 外/source/`.git`/非 temp path 不会被写入或清理；secret-safe output 保持；默认 wrapper、`-SelfTest`、`-Clean`、diff check 通过。结果：wrapper 启动时清理 stale evidence，默认 lightweight 不写新 report；evidence report path 派生自 validated `TempRoot`；source、`.git`、repo 外、非 `.tmp` path 在写入/清理前拒绝；`-Clean` 只删除 wrapper-owned evidence/generated-client/self-test artifacts 并保留非 owned temp marker；runbook 记录 evidence lifecycle 与 cleanup allowlist。剩余缺口：report 尚未绑定当前 commit、命令 provenance 和 fixture freshness；真实 Redocly/OpenAPI Generator/openapi-typescript semantic run 仍需 opt-in 工具链证据。
- **E11-007-S29 Backend，已完成，2026-06-04，owner Goodall**：ledger execute OpenAPI semantic evidence provenance contract。原因：S27 已锁住 evidence artifact lifecycle，但 report 仍需要绑定当前 repo commit、命令摘要、工具/模拟模式和 fixture freshness，避免旧 report 或 simulated report 被误当成当前真实 validator evidence。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection/TEST。验收：新增或强化 report schema/self-test，覆盖 repo commit 或明确 unavailable marker、generated_at、command summary redaction、real/simulated provenance、OpenAPI fixture freshness guidance、stale/simulated report 不可关闭真实 semantic gap；默认 lightweight 不写 report；`-SelfTest`、default wrapper、`-Clean`、diff check 通过。结果：evidence report 新增 `repo_commit`/status、`generated_at_utc`、`provenance_mode`、bounded `command_summary` 和 OpenAPI fixture SHA-256/size/last-write fingerprint；每条 evidence record 标记 real/simulated；self-test 验证 simulated reports 的 provenance、fixture freshness、redaction 和 output bounds；默认 lightweight 仍不写 report。剩余缺口：真实 Redocly/OpenAPI Generator/openapi-typescript opt-in semantic/client generation 仍未运行；live Postgres、真实 generated-client acceptance、Admin UI E2E 仍是独立缺口。
- **E11-007-S30 Backend，已完成，2026-06-04，owner Goodall**：OpenAPI semantic real-tool opt-in preflight/performance evidence contract。原因：S29 已防止 simulated/stale evidence 被误关口，但高性能/真实工具链验收还需要明确 Redocly/OpenAPI Generator/openapi-typescript 的可用性、运行耗时和 blocker 输出。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger。验收：新增或强化 opt-in preflight/self-test，检查真实工具是否可用、版本/路径是否 safe、命令 duration 是否 bounded、package/cache 状态是否明确；默认不下载、不运行重工具；缺工具输出 blocker 而非 simulated pass；`-SelfTest`、默认 wrapper、`-Clean`、diff check 通过。结果：evidence record 新增 `tool_path`、`preflight_status`、`package_cache_status`、`package_download_allowed` 和 bounded `duration_ms`；`Invoke-NpmTool` 在 opt-in 前记录 node/npm/java 可用性，缺工具时写 blocker evidence 而不是 simulated pass；新增 `-SimulateToolPreflightBlocker` self-test 固定缺工具/cache/duration/redaction contract；默认 lightweight 仍不下载、不运行重工具、不写 evidence report。剩余缺口：真实 Redocly/OpenAPI Generator/openapi-typescript semantic/client-generation 仍未运行；本片只关闭 real-tool preflight/perf evidence contract。
- **E11-007-S33 Backend，已完成，2026-06-04，owner Goodall**：generated-client inspection readiness gate。原因：S30 已固定真实工具 preflight/perf evidence，但 generated-client acceptance 仍缺一个把 generated output freshness、target directory safety、required execute fields 和 blocker 分类串起来的 readiness gate。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger。验收：新增或强化 generated-client inspection readiness self-test/preflight，覆盖 generated output missing/stale/current marker、target dir safe path、required ledger execute/executor summary fields、duration/package/tool evidence passthrough、secret-safe output；默认不生成 client、不下载 package；`-SelfTest`、默认 wrapper、`-Clean`、diff check 通过。结果：generated-client inspection 前新增 readiness gate，要求 generated target 位于 validated `TempRoot`、output 存在、wrapper-owned `.ledger-openapi-generated-client-readiness.json` marker 当前且绑定 OpenAPI fixture SHA-256；marker 记录 target/tool/provenance/package cache/download/duration，current 后才检查 ledger execute/executor summary required fields；self-test 覆盖 missing output、stale marker、unsafe target 均 exit `1`。剩余缺口：真实 generated-client production acceptance 仍需后续显式 opt-in 跑 `-OpenApiTypescript`、`-TypescriptFetch` 或 `-ClientGeneration`。
- **E11-007-S35 Backend，已完成，2026-06-04，owner Goodall**：OpenAPI real opt-in command matrix blocker audit。原因：S30/S33 已固定工具 preflight 和 generated-client readiness，但最终真实 semantic/client-generation 前需要一份本地环境命令矩阵，清楚区分哪些 opt-in 命令在当前机器 blocked、哪些能运行，以及每个 blocker 产出的 evidence 字段。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger。验收：新增或强化 command matrix dry-run/self-test，列出 Redocly/OpenAPI Generator/openapi-typescript/typescript-fetch 的 opt-in flags、required tools、package/cache policy、expected exit classification、evidence fields、safe command examples；默认不下载、不运行 heavy tools；`-SelfTest`、默认 wrapper、`-Clean`、diff check 通过。结果：wrapper 新增 `-CommandMatrix` 与 env opt-in，输出 Redocly、OpenAPI Generator validate、openapi-typescript、typescript-fetch 四个真实 opt-in 入口的 required tools、package/cache policy、exit classification、evidence fields 和 safe wrapper command；command matrix dry-run 不运行 npm/java、不下载包、不生成 client、不写 evidence report，并进入 self-test；runbook 增加 blocker audit/command matrix 章节。剩余缺口：真实 Redocly/OpenAPI Generator/openapi-typescript/typescript-fetch opt-in pass 仍需后续可用工具/cache 环境运行。
- **E11-007-S37 Backend，已完成，2026-06-04，owner Goodall**：OpenAPI real-tool cache availability probe。原因：S35 已列出真实 opt-in command matrix；下一片需要在不下载 package、不生成 client 的前提下探测本机/CI 是否已有 Redocly、OpenAPI Generator、openapi-typescript 所需 cache/tooling，并把 blocker 输出绑定到 evidence fields。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection。验收：新增或强化 cache/tool availability probe，覆盖 node/npm/java path/version、repo cache present/missing、download disabled marker、per-command blocker classification、duration marker和 secret-safe output；默认不下载、不运行 heavy validator/generator；`-SelfTest`、默认 wrapper、`-CommandMatrix`、`-Clean`、diff check 通过。结果：新增 `-CacheProbe` / `CONTROL_PLANE_LEDGER_OPENAPI_CACHE_PROBE=1` 与 simulated cache probe self-test，输出 per-command tool path/version、offline repo cache present/missing、package download disabled marker、preflight status、classification、bounded duration 和 redacted output tail；默认仍只跑 lightweight gate，不下载 package、不运行 Redocly/OpenAPI Generator/openapi-typescript；本机实际 probe 显示 node/npm/java 可用，但 `.tool-cache\npm` 缺少所需 package cache，因此按预期返回 blocker exit 2 并写 evidence。剩余缺口：真实 Redocly/OpenAPI Generator/openapi-typescript semantic/client-generation pass 仍未运行；本片只关闭 cache/tool availability probe。
- **E11-007-S39 Backend，已完成，2026-06-04，owner Goodall**：OpenAPI real-tool opt-in execution evidence artifact。原因：S37 已能判断工具/cache 是否具备，但最终缺口仍是一次真实 Redocly/OpenAPI Generator/openapi-typescript opt-in execution 的 pass/blocker/fail evidence；下一片需要固定真实执行时的 evidence artifact 形状和 closure rules，避免 simulated/cache-only 结果被误当成真实 semantic/client-generation pass。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection。验收：新增或强化 real-tool opt-in execution evidence contract，覆盖 tool/cache preflight passed、real command duration、exit classification、generated-client readiness marker、artifact provenance/freshness、secret-safe command/output tail；默认不下载、不运行 heavy validator/generator；缺 cache/tool 返回 blocker 而非 simulated pass；`-SelfTest`、默认 wrapper、`-CommandMatrix`、`-CacheProbe` blocker path、`-Clean`、diff check 通过。结果：evidence record 新增 `execution_mode`、`real_command_executed`、`readiness_marker_status`、`closure_eligible`，并固定 closure rule：只有 `provenance_mode=real`、`execution_mode=real_tool_execution`、`real_command_executed=true`、`classification=pass` 且 generated-client readiness marker current 才能关闭真实 semantic/client-generation 缺口；新增 simulated real execution pass/fail/blocker self-test，默认仍不下载、不运行 heavy tools。当前实际 `-CacheProbe` 显示 node/npm/java 可用，但离线 npm cache 缺 Redocly/OpenAPI Generator/openapi-typescript，因此四个真实 opt-in command 仍按预期 blocker exit 2。剩余缺口：真实 Redocly/OpenAPI Generator/openapi-typescript/typescript-fetch pass 仍需预置 cache 或显式 download opt-in 后运行。
- **E11-007-S41 Backend，已完成，2026-06-04，owner Goodall**：OpenAPI package cache provisioning/download opt-in evidence gate。原因：S39 已固定真实执行 evidence closure rule，但当前实际 blocker 是 `.tool-cache\npm` 缺少 Redocly/OpenAPI Generator/openapi-typescript；下一片需要固定 package cache provisioning 或显式 download opt-in 的证据语义，避免下载行为不透明或 cache 预置后无法证明 provenance。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection。验收：新增或强化 cache provisioning/download opt-in gate，覆盖 package list、cache directory safety、download opt-in flag/env、network/download disabled default、package version/provenance marker、duration/cache size summary、secret-safe npm output tail；默认不下载、不运行 heavy validator/generator；`-SelfTest`、默认 wrapper、`-CommandMatrix`、`-CacheProbe` blocker path、`-Clean`、diff check 通过。结果：evidence record 和 command matrix 新增 `package_list`、`package_version`、`package_provenance`、`package_cache_path`、`package_cache_bytes`、`package_probe_duration_ms`，并锁定 Redocly/OpenAPI Generator/openapi-typescript package list；默认 `-CacheProbe` 在离线 cache 缺包时输出 `offline_repo_cache_missing` blocker，不下载、不运行 validator/generator；`-CacheProbe -AllowPackageDownload` 只记录 `package_download_allowed=true`、`package_provenance=download_opt_in`、bounded cache path/size/probe duration 和 safe npm/tool output tail，不实际下载或关闭真实 semantic gap。验证：默认 wrapper、`-SelfTest`、`-CommandMatrix`、`-CacheProbe` blocker path、`-CacheProbe -AllowPackageDownload`、`-Clean` 和 diff check 通过。剩余缺口：真实 Redocly/OpenAPI Generator/openapi-typescript semantic/client-generation pass 仍需预置 `.tool-cache\npm` 或显式受控下载后再跑真实工具链。
- **E11-007-S43 Backend，进行中，2026-06-04，owner Goodall**：OpenAPI package cache materialization executor boundary。原因：S41 已固定 package cache/download opt-in evidence，但还没有明确真实 npm package materialization 何时允许执行、如何限定 cache 目录、如何记录 package size/version/provenance/duration，以及失败时如何输出 blocker 而不污染 repo 或关闭 semantic gap；下一片先固定受控 executor boundary。范围：`scripts/verify_control_plane_ledger_adjustment_openapi_semantic.ps1`、`docs/E11-007_LEDGER_EXECUTE_OPENAPI_VALIDATION_RUNBOOK.md`；不改 Gateway/Control Plane/Admin UI/routing/billing-ledger/Prompt Protection。验收：新增或强化 materialization executor boundary，覆盖 disabled-by-default、`-AllowPackageDownload` + explicit materialize flag/env 双 opt-in、repo-local cache path safety、package allowlist、npm install/download command summary redaction、per-package duration/cache bytes/version/provenance evidence、failure/blocker classification、no validator/generator execution by default、stale/incomplete cache refusal；`-SelfTest`、默认 wrapper、`-CommandMatrix`、`-CacheProbe`、materialize dry-run/self-test、`-Clean` 和 diff check 通过。退出条件：只关闭 package cache materialization executor boundary；真实 semantic/client-generation pass 仍需后续显式 real-tool command 运行。
- **E11-007-S5，已完成，2026-06-04，owner Godel**：Admin UI execute-contract v2 呈现与错误流。原因：后端真实执行落地前，UI 需要把 execute contract v2 的预检、dedupe、事务/audit 摘要和阻塞原因展示清楚，避免用户把 plan-only 当成已入账。范围：`web/admin-ui/**`；不改后端。验收：UI 明确区分 dry-run/execute-preflight/blocked/success；错误不泄露 secret；相关 UI 测试和 bundle check 覆盖。结果：Billing/Price 页面已区分 dry-run、execute-preflight、blocked 和 future execute 状态，并展示 v2 transaction/dedupe/audit/safe-output 摘要，未暴露 raw credential 或 idempotency material。剩余缺口：后端真实 execute submit 尚未接通。
- **E13-005-S3，已完成，2026-06-04，owner Pauli**：Prompt Protection Gateway endpoint 覆盖。原因：embeddings 已完成，但 Prompt Protection 最终验收需要覆盖 remaining native/provider paths，并保持 billing/log/audit 的 sanitized prompt_protection signals。范围：`apps/gateway/**`、Gateway fixtures；不改 Admin UI/Control Plane。验收：至少一个 remaining endpoint/provider path 的 protection ordering、block response、log metadata sanitizer 和 regression fixture 固定；未覆盖路径列入剩余缺口。结果：Anthropic `/v1/messages` stream/non-stream shared preflight 已在 routing/request_started/provider-key/upstream side effect 前执行，reject metadata 保持 secret-safe，并更新 runtime ordering fixture。剩余缺口：Gemini native passthrough、audit UI 展示仍未覆盖。
- **E13-005-S4，已完成，2026-06-04，owner Pauli**：Prompt Protection Gemini native Gateway 覆盖。原因：S3 已覆盖 Anthropic，但 final E13 Gateway path 仍缺 Gemini native passthrough，必须证明 provider-native endpoint 同样在 request/provider side effect 前拒绝。范围：`apps/gateway/**`、`tests/fixtures/gateway/prompt_protection_runtime_contract.json`；不改 Admin UI/Control Plane，不碰 rate-limit smoke fixture。验收：Gemini native passthrough 至少一个 stream/non-stream 相关路径有 preflight、block response、secret-safe metadata 和 runtime ordering fixture；targeted Gateway tests 通过。结果：Gemini native `generateContent`/`streamGenerateContent` 现在在 routing/request_started/provider-key/upstream side effect 前执行 prompt protection，reject logging 只保留 hash 与 sanitized `contents` scope metadata。剩余缺口：仍缺完整 HTTP/repository mock 级别断言来证明 provider_attempt 未创建。
- **E13-005-S6，已完成，2026-06-04，owner Pauli**：Prompt Protection reject no-side-effect 回归覆盖。原因：S3/S4 已用 source ordering 和 unit tests 覆盖 endpoint preflight，但最终 Gateway 验收还需要 HTTP/repository 层证明 reject 不会创建 provider_attempt、不会打开 provider key、不会调用 upstream。范围：`apps/gateway/**`、Gateway prompt-protection fixtures；不改 Admin UI/Control Plane。验收：至少一个 chat/responses/native endpoint 的 prompt-protection reject 通过 repository/mock 断言 request error log secret-safe 且 provider_attempt count 为 0；targeted Gateway tests 通过。结果：新增 chat completions prompt-protection reject 的 handler-level HTTP spy regression，证明 reject response 产生 hash-only rejected request log，provider_attempt/provider_key_open/upstream count 均为 0，metadata 不含 raw prompt/pattern/header/provider secret。剩余缺口：该 slice 尚非真实 Postgres repository provider_attempt 表计数；responses/Anthropic/Gemini 可按同模式继续补。
- **E13-005-S7，已完成，2026-06-04，owner Pauli**：Prompt Protection no-side-effect regression 扩展到 non-chat paths。原因：S6 已覆盖 chat completions reject，但 Prompt Protection final Gateway surface 仍需要 responses、Anthropic 或 Gemini native 至少一个 non-chat path 的 no-side-effect regression，避免只有 chat path 证明 request/provider side effect ordering。范围：`apps/gateway/**`、Gateway prompt-protection fixtures；不改 Admin UI/Control Plane/billing/routing。验收：至少一个 non-chat endpoint 的 reject 有 no provider_attempt/provider key/upstream 断言；metadata secret-safe；targeted Gateway tests 通过。结果：`/v1/responses` 新增 handler-level HTTP spy regression，证明 prompt-protection reject 后只产生 hash-only rejected request log，provider_attempt/provider_key_open/upstream count 均为 0，runtime fixture 同步记录 no-side-effect contract。剩余缺口：Anthropic/Gemini native 仍缺 handler-level spy regression；该证据仍不是真实 Postgres provider_attempt 表计数。
- **E13-005-S8，已完成，2026-06-04，owner Pauli**：Prompt Protection no-side-effect regression 扩展到 native provider path。原因：S6/S7 已覆盖 chat completions 与 `/v1/responses`，但 Gemini/Anthropic native passthrough 仍只有 preflight ordering/unit 证据；最终 Gateway surface 需要至少一个 native path 的 handler-level no-side-effect 证明。范围：`apps/gateway/**`、Gateway prompt-protection fixtures；不改 Admin UI/Control Plane/billing/routing/scripts。验收：Gemini native 或 Anthropic native reject 通过 handler-level spy 断言 provider_attempt/provider key/upstream count 为 0；request log hash-only 且 metadata secret-safe；runtime fixture 增加对应 native no-side-effect contract；targeted Gateway tests 通过。结果：Anthropic `/v1/messages` 新增 handler-level prompt-protection reject spy，证明 request log hash-only、finished rejected、provider_attempt/provider_key_open/upstream count 均为 0，runtime fixture 新增 native no-side-effect entry。剩余缺口：Gemini native 仍缺同级 handler-level spy regression；证据仍不是真实 Postgres `provider_attempts` 表集成计数。
- **E13-005-S9，已完成，2026-06-04，owner Pauli**：Prompt Protection no-side-effect regression 扩展到 Gemini native path。原因：S8 已补 Anthropic native handler-level spy，E13 Gateway surface 仍剩 Gemini native 只有 preflight ordering/unit 证据。范围：`apps/gateway/**`、Gateway prompt-protection fixtures；不改 Admin UI/Control Plane/billing/routing/scripts/docs。验收：Gemini native reject 通过 handler-level spy 断言 provider_attempt/provider key/upstream count 为 0；request log hash-only 且 metadata secret-safe；runtime fixture 增加 Gemini native no-side-effect contract；targeted Gateway tests 通过。结果：Gemini native `generateContent` reject 新增 handler-level spy regression，证明 request log hash-only、finished rejected、provider_attempt/provider_key_open/upstream count 均为 0，runtime fixture 新增 Gemini no-side-effect entry。剩余缺口：handler-level spy 已覆盖 chat/responses/Anthropic/Gemini；真实 Postgres `provider_attempts` 表计数和更完整 audit/UI 可视化仍是后续方向。
- **E13-005-S10，已完成，2026-06-04，owner Pauli**：Prompt Protection provider_attempts Postgres proof runbook。原因：S6-S9 已用 handler-level spies 覆盖主要 Gateway surfaces，但最终 no-side-effect 证据仍缺真实 Postgres `provider_attempts` 表计数或 live/integration runbook。范围：`docs/**`、必要 `TEST_AND_ACCEPTANCE.md`；不改 Gateway/Control Plane/Admin UI/routing/scripts。验收：文档列出真实 DB 验证前置条件、命令/环境、要覆盖的 endpoints、expected provider_attempts count=0、request log hash-only、blocked exit semantics；明确该 runbook 通过后可关闭的 E13 剩余缺口和不能关闭的 UI/audit 可视化缺口。结果：新增 `docs/E13-005_PROMPT_PROTECTION_POSTGRES_PROOF_RUNBOOK.md` 并在 `TEST_AND_ACCEPTANCE.md` 增加验收入口，覆盖 chat completions、Responses、Anthropic Messages、Gemini native 的 live Postgres proof 命令形态、SQL evidence、secret-safety checks、provider_attempts count=0、provider key/upstream not-called proxy 和 exit 0/1/2 语义。剩余缺口：本切片只提供 runbook，真实 Docker/Postgres/Gateway live run 仍需可用环境补跑；Admin UI/audit 可视化缺口不能由该 DB proof 关闭。
- **E13-005-S11，已完成，2026-06-04，owner Pauli**：Prompt Protection provider_attempts Postgres proof script/preflight。原因：S10 已写 live runbook，但开发/CI 需要 opt-in script/preflight，把外部 blocker、证据失败和 contract-only 自检区分开。范围：`scripts/**`、`docs/**`、必要 `TEST_AND_ACCEPTANCE.md`；不改 Gateway/Control Plane/Admin UI/routing 代码。验收：新增或强化脚本，默认 contract/preflight 不依赖 live DB；显式 live flag/env 才检查 Gateway/Postgres；外部 blocker exit `2`、证据失败 exit `1`、全部通过 exit `0`；覆盖四个 endpoints 的 expected evidence 描述；diff check 通过。结果：新增 `scripts/verify_prompt_protection_postgres_proof.ps1`，默认只做 contract/preflight 自检，不连接 Gateway/Postgres/mock-provider；`-Live` 或 `PROMPT_PROTECTION_POSTGRES_PROOF_LIVE=1` 才检查 Gateway/Postgres 并发送四个 endpoint 请求；live evidence 验证 `provider_attempts_count=0`、hash-only request log、rejected status、route/provider-key side-effect 字段为空和 raw prompt/pattern/header/secret omission；文档和 TEST 同步 exit `0/1/2` 语义。剩余缺口：未执行 `-Live`，真实 Postgres evidence 仍需可用 Gateway/Postgres/mock-provider 环境和 live virtual key 补跑；Admin UI/audit 可视化缺口仍独立存在。
- **E13-005-S12，已完成，2026-06-04，owner Pauli**：Prompt Protection Postgres proof test/release opt-in gate wiring。原因：S11 已提供独立脚本，但开发/CI 需要统一入口运行默认 contract/preflight，并保留 live proof 显式 opt-in，避免手动脚本与 test/release gates 漂移。范围：`scripts/**`、`docs/**`、必要 `TEST_AND_ACCEPTANCE.md`；不改 Gateway/Control Plane/Admin UI/routing 代码。验收：`scripts/test.ps1` 或 release smoke gate 有明确 Prompt Protection Postgres proof contract-only/default 入口；live flag/env 才进入 `-Live`；外部 blocker exit `2` 不被误报为默认失败；release/test 文档说明可关闭与不可关闭缺口；相关脚本默认检查和 diff check 通过。结果：`scripts/test.ps1` 新增 `-PromptProtectionPostgresProofOnly`/`-PromptProtectionPostgresProofLive` 与 env-only 入口，默认/full test gate 调用 proof `-ContractOnly`；release smoke 默认加入 `verify_prompt_protection_postgres_proof.ps1 -ContractOnly`，`-RunRuntimeSmoke` 才调用 `-Live`；文档补齐 test/release 命令和 live opt-in 语义。剩余缺口：未运行 live proof，真实 Gateway/Postgres/mock-provider/virtual key 环境仍需外部补跑；需要继续固定 blocker/failure path 的 contract evidence，避免 exit `1/2` 语义漂移。
- **E13-005-S13，已完成，2026-06-04，owner Pauli**：Prompt Protection proof blocker/failure path contract tests。原因：S11/S12 已有 proof script 和统一入口，但 blocker exit `2`、evidence mismatch exit `1` 和 default contract pass exit `0` 仍主要靠人工语义；后续需要脚本级自测或 fixture 锁住这些 failure paths。范围：`scripts/**`、`docs/**`、必要 `TEST_AND_ACCEPTANCE.md`；不改 Gateway/Control Plane/Admin UI/routing 代码。验收：新增或强化 contract/self-test，能在不连接 live DB 的情况下证明 missing Gateway/Postgres/psql 等 live preflight blocker 走 exit `2`，contract/default 走 exit `0`，模拟 evidence mismatch 或 documented failure path 走 exit `1`；release/test wrapper 不把默认 contract-only 误报为 live blocker；相关脚本 checks 和 diff check 通过。结果：`verify_prompt_protection_postgres_proof.ps1` 新增 `-SelfTestExitSemantics`，子进程断言 default/`-ContractOnly` exit `0`、`-SimulateLivePreflightBlocker` exit `2`、`-SimulateEvidenceMismatch` exit `1`；默认 contract 也锁定 `scripts/test.ps1`/`release_check.ps1` 只在显式 opt-in 时进入 live；runbook 与 TEST 同步自测命令和边界。剩余缺口：未运行 `-Live`，真实 Gateway/Postgres/mock-provider/virtual key 的 `provider_attempts_count=0` 证据仍需可用环境补跑；Admin UI/audit 可视化缺口仍独立。
- **E13-005-S14，已完成，2026-06-04，owner Pauli**：Prompt Protection live proof preflight evidence envelope。原因：S13 已锁定 exit semantics，但真正执行 `-Live` 前还需要固定 preflight/evidence 输出包络，确保缺环境时报告外部 blocker，环境可用时能记录 provider_attempts=0、request log hash-only、provider key/upstream not-called 等证据且不泄露 secret。范围：`scripts/verify_prompt_protection_postgres_proof.ps1`、`docs/E13-005_PROMPT_PROTECTION_POSTGRES_PROOF_RUNBOOK.md`、必要 `TEST_AND_ACCEPTANCE.md`；不改 Gateway/Control Plane/Admin UI/routing 代码。验收：live/preflight 输出列出必需 env、endpoint catalog、SQL evidence fields 和 secret-safe omission fields；缺 Gateway/Postgres/psql/mock-provider/virtual key 时 exit `2` 且给出 bounded blocker；不改变默认 contract-only gate；self-test/default/release smoke 仍通过；diff check 通过。结果：`-Live`/`-Live -PreflightOnly` 会先输出 `prompt_protection_postgres_proof_evidence_envelope.v1`，列出 required env 名、endpoint catalog、SQL evidence fields、hash-only request log fields、provider key/upstream not-called fields 和 secret-safe omission fields；live blocker 输出收敛为 bounded precondition，缺环境 exit `2` 且不泄露 URL/token/DSN/header/cookie/raw prompt/pattern/provider secret。剩余缺口：仍未执行真实 `-Live` evidence run；Admin UI/audit 可视化缺口仍独立。
- **E13-005-S15，已完成，2026-06-04，owner Pauli**：Prompt Protection proof evidence report contract。原因：S14 已有 live/preflight envelope，但真实 `-Live` 跑完后仍需要一个 secret-safe evidence report contract，便于记录每个 endpoint 的 provider_attempts=0、hash-only request log 和 not-called fields，而不是只依赖 console 文本。范围：`scripts/verify_prompt_protection_postgres_proof.ps1`、`docs/E13-005_PROMPT_PROTECTION_POSTGRES_PROOF_RUNBOOK.md`、必要 `TEST_AND_ACCEPTANCE.md`；不改 Gateway/Control Plane/Admin UI/routing 代码。验收：新增或强化 bounded evidence report schema/fixture/self-test，覆盖 per-endpoint expected fields、exit `0/1/2` status、secret-safe omission markers；报告不包含 raw prompt/body/header/token/DSN/provider secret；默认 contract-only gate 不写 live report；self-test/default/release smoke 和 diff check 通过。结果：新增 `-EvidenceReportPath`/`PROMPT_PROTECTION_POSTGRES_PROOF_REPORT_PATH` live-only report，schema 为 `prompt_protection_postgres_proof_evidence_report.v1`，覆盖 per-endpoint request hash、expected/observed response、hash-only request log、provider_attempts/not-called fields、safe reason/scope、secret-safe omission markers，以及 pass/fail/blocker exit `0/1/2`；`-SelfTestEvidenceReportContract` 无 live 依赖验证 pass/fail/blocker report。剩余缺口：仍未执行真实 `-Live` pass；需要可用 Gateway/Postgres/mock-provider/virtual key 环境补跑并保存 report artifact。
- **E13-005-S16，已完成，2026-06-04，owner Pauli**：Prompt Protection proof report path safety contract。原因：S15 已提供 live evidence report，但 report path 仍需要像其他 generated artifact 一样有 repo 内路径/cleanup/overwrite 边界，避免 live proof 把报告写到 repo 外或覆盖源码/其他 worker 文件。范围：`scripts/verify_prompt_protection_postgres_proof.ps1`、`docs/E13-005_PROMPT_PROTECTION_POSTGRES_PROOF_RUNBOOK.md`、必要 `TEST_AND_ACCEPTANCE.md`；不改 Gateway/Control Plane/Admin UI/routing。验收：report path 限制在 `.tmp` 或明确安全 artifact 目录；拒绝 repo 外、源码文件、`.git` 路径；self-test 覆盖 safe path/pass、unsafe path exit `1/2` 或 bounded refusal；默认 contract-only 不写 report；release/test smoke 和 diff check 通过。结果：`-EvidenceReportPath` 现在只允许写入 repo 内 `.tmp/**` 或 `artifacts/prompt-protection-postgres-proof/**` 的 `.json` 文件，拒绝 repo 外、`.git/**`、source/script/docs 和非 JSON 路径；新增 `-SelfTestEvidenceReportPathSafety` 覆盖 safe path 与 unsafe refusal，refusal 输出不回显用户路径或 secret-like segment；默认 contract-only 仍不写 report，test/release smoke 通过。剩余缺口：仍未执行真实 `-Live` pass；cleanup/overwrite lifecycle 仍需单独固定，避免后续 report artifact 覆盖或清理到非 proof 文件。
- **E13-005-S17，已完成，2026-06-04，owner Pauli**：Prompt Protection proof report cleanup/overwrite safety contract。原因：S16 已限定 report 写入路径，但生成 artifact 的 overwrite 和 cleanup 行为仍需要被 contract 锁住，避免 live proof 清理或覆盖源码、`.git`、其他 worker 文件，或把旧 report 误当成新 evidence。范围：`scripts/verify_prompt_protection_postgres_proof.ps1`、`docs/E13-005_PROMPT_PROTECTION_POSTGRES_PROOF_RUNBOOK.md`、必要 `TEST_AND_ACCEPTANCE.md`；不改 Gateway/Control Plane/Admin UI/routing。验收：新增或强化 report artifact cleanup/overwrite self-test 或 dry-run contract；只允许覆盖当前请求的 safe report JSON 或清理 proof-owned generated JSON；拒绝删除/覆盖源码、`.git`、repo 外和非 proof artifact；默认 contract-only 不清理/不写 report；release/test smoke 和 diff check 通过。结果：新增 `-CleanupEvidenceReportPath`、`-CleanupEvidenceReportDryRun` 和 `-SelfTestEvidenceReportLifecycle`；report overwrite 只允许 proof-owned schema JSON，cleanup 只删除 proof-owned generated JSON artifact，拒绝 repo 外、`.git`、source/script/docs、非 JSON、非 proof JSON 和其他 worker artifact；默认 contract-only 不写不清理，test/release smoke 通过。剩余缺口：真实 `-Live` evidence 仍未运行；report provenance/freshness 仍需固定，避免旧 artifact 被误用。
- **E13-005-S18，已完成，2026-06-04，owner Pauli**：Prompt Protection proof report provenance/freshness contract。原因：S17 已固定 report 路径与 lifecycle，但 live report 仍需要绑定 repo commit、生成时间、运行模式和命令摘要，避免旧报告或 simulated report 被误当成当前 live evidence。范围：`scripts/verify_prompt_protection_postgres_proof.ps1`、`docs/E13-005_PROMPT_PROTECTION_POSTGRES_PROOF_RUNBOOK.md`、必要 `TEST_AND_ACCEPTANCE.md`；不改 Gateway/Control Plane/Admin UI/routing。验收：report schema/self-test 覆盖 repo commit 或明确 unavailable marker、generated_at、live/preflight/contract mode、redacted command summary、simulated/live provenance 标记和 freshness guidance；default contract-only 不写 live report；secret-safe output；release/test smoke 和 diff check 通过。结果：proof evidence report 新增 root/provenance/freshness `generated_at_utc`、repo head commit 或 unavailable marker、dirty/untracked counts（路径 omitted）、`provenance.mode`、`provenance.kind`、proof run id hash 和 redacted command summary；closure eligibility 必须满足 live/current commit/current run/status passed/exit 0/四 endpoint 全 passed，preflight/contract/simulated/stale/wrong-commit/dirty-unreviewed 均不能关闭 live gap。剩余缺口：仍需真实 `-Live` Gateway/Postgres/mock-provider 环境跑出当前 commit 的 provider_attempts=0 report；Admin UI/audit 可视化缺口仍独立存在。
- **E13-005-S19 Performance，已完成，2026-06-04，owner Pauli**：Prompt Protection live proof performance envelope。原因：Prompt Protection 已证明 reject path/provider_attempts=0 的 contract/provenance，但高性能验收还要能从 proof report 看出 reject/preflight path 的 per-case duration、bounded latency 和 live blocker 状态。范围：`scripts/verify_prompt_protection_postgres_proof.ps1`、`docs/E13-005_PROMPT_PROTECTION_POSTGRES_PROOF_RUNBOOK.md`、必要 `TEST_AND_ACCEPTANCE.md`；不改 Gateway/Control Plane/Admin UI/routing。验收：report/self-test 覆盖 per-endpoint/per-case duration 或明确 unavailable marker、provider_attempts=0、preflight/live/contract provenance、secret-safe command summary、latency envelope closure rules；默认 contract-only 不写 live report；release/test smoke 和 diff check 通过。结果：proof report endpoint 增加 `performance` 字段，包含 `total_case_duration_ms`、`request_preflight_duration_ms`、`db_evidence_duration_ms`、`duration_available` 和 bounded `latency_envelope`；root `performance_envelope` 固定 latency bounds、`live_blocker_status`、`external_blocker_count`、provider_attempts=0 closure rule 和 `latency_envelope_closure_eligible`；contract/preflight/not-run/blocker 使用 `duration_available=false`，不伪造 live latency。剩余缺口：真实 Gateway/Postgres/mock-provider `-Live` proof 仍未执行；必须跑出当前 commit 的 provider_attempts=0 和 latency report 后才能关闭 live gap。
- **E13-005-S20 UI/Audit，已完成，2026-06-04，owner Pauli**：Prompt Protection audit performance envelope UI contract。原因：S19 已让 proof report 产生 performance envelope，但 Admin UI/audit 可视化仍不能展示 prompt-protection proof/performance 摘要；下一片先固定 UI 白名单/fixture contract，避免后续展示 raw prompt 或 secret。范围：`web/admin-ui/**`、必要 prompt protection audit fixtures/tests；不改 Gateway/Control Plane/routing/scripts。验收：新增或强化 UI/API tests，覆盖 prompt_protection audit/request detail 对 `performance_envelope`、duration unavailable marker、provider_attempts=0、live blocker status 的白名单展示；不渲染 raw prompt/body/header/token/credential/DSN/provider secret；UI tests/build/bundle check 通过。结果：Prompt Protection summary 白名单新增 Provider attempts、Duration、Latency envelope、Live blocker 展示，request detail 和 audit detail fixture 覆盖 `provider_attempts_count=0`、`duration_available=false`/unavailable reason、latency closure status 和 live blocker status；测试加入 raw body/header/token/DSN/provider secret decoy 并断言不渲染。剩余缺口：真实 live proof 和真实 audit E2E 仍需后续验收。
- **E13-005-S21 UI/Audit，已完成，2026-06-04，owner Pauli**：Prompt Protection live proof artifact readiness UI contract。原因：S20 已展示 performance envelope 字段，但 UI 仍缺对 live proof artifact freshness/closure eligibility 的安全摘要；下一片固定当前 commit/live artifact readiness 的 UI contract，避免旧 report 被误读为当前 live evidence。范围：`web/admin-ui/**`、必要 prompt protection audit fixtures/tests；不改 Gateway/Control Plane/routing/scripts。验收：新增或强化 UI/API tests，覆盖 generated_at/current commit/live mode/closure eligible/stale or simulated marker 的白名单展示；不渲染 raw report path、command secrets、DSN/token/header/provider secret；UI tests/build/bundle check 通过。结果：Prompt Protection summary 新增 `Artifact generated`、`Current commit`、`Proof mode`、`Proof closure`、`Stale/simulated marker` 白名单展示；request/audit log fixtures 加入 raw report path、command header、DSN、provider secret 和 proof run hash decoy，并断言这些敏感值不渲染；UI 只展示 ISO 秒级时间、短 commit、`contract / simulated`、closure eligibility 和不能关闭 live gap marker。剩余缺口：真实 live proof report、Gateway/Postgres/mock-provider live execution 和真实 audit E2E 仍需后续验收。
- **E13-005-S22 UI/Audit，已完成，2026-06-04，owner Pauli**：Prompt Protection live proof audit E2E readiness contract。原因：S21 已把 proof artifact readiness 显示到 Request/Audit UI，但真实验收还需要固定 audit E2E 如何判定 live/current proof 进入审计记录，以及旧 report/simulated report 如何明确不能关闭缺口。范围：`web/admin-ui/**`、必要 prompt protection audit fixtures/tests；不改 Gateway/Control Plane/routing/scripts/E11 Billing smoke。验收：新增或强化 UI/API tests，覆盖 audit log detail 中 live/current proof eligible、stale/simulated refused、provider_attempts=0、latency/performance envelope、secret-safe raw artifact omission；不渲染 raw report path、command、DSN/token/header/provider secret；UI tests/build/bundle check 通过。结果：Audit log detail 的 Prompt Protection panel 能显示 live/current proof eligible 状态、`current live proof`、provider_attempts=0、`not_blocked` live blocker、latency envelope 摘要和 repo commit marker；新增 regression 证明 raw report path、command、DSN、Authorization/header/token、provider secret、proof hash 和 raw performance body 不进入 DOM。剩余缺口：真实 Gateway/Postgres/mock-provider live proof 仍未运行；本片只关闭 Admin UI/audit readiness contract。
- **E13-005-S23 UI/Audit，已完成，2026-06-04，owner Pauli**：Prompt Protection live proof audit command/evidence handoff。原因：S22 已固定 UI 如何展示 current live proof，但真实验收还缺一个从 live proof script/report 到 Admin UI audit readiness 的 handoff contract，说明具备 Gateway/Postgres/mock-provider 后如何把 provider_attempts=0、latency envelope 和 current proof provenance 输入 UI/audit，并在缺环境时产出 blocker evidence。范围：`web/admin-ui/**`、必要 prompt protection audit fixtures/tests；不改 Gateway/Control Plane/routing/scripts/E11 Billing smoke。验收：新增或强化 UI/API contract，覆盖 live proof command/evidence handoff fields、blocked/pass/fail audit readiness classification、provider_attempts=0 closure rule、latency/performance envelope fields、stale/simulated refusal、secret-safe raw artifact omission；UI tests/build/bundle check 通过。结果：Prompt Protection summary 增加 Audit readiness、Proof command、Proof evidence、Closure rule 白名单字段，覆盖 blocked/pass/fail handoff、provider_attempts=0、latency bounded、duration available、current provenance closure rule；tests 证明 raw report path、raw command、Authorization、DSN/token/header/provider secret、proof raw id 不进入 DOM，并修正重复 `blocked` 文本导致的非唯一断言。验证：Pauli 在 S40 开始前跑过 `npm test -- --run src/App.test.tsx src/api/client.test.ts` 77 passed、build、bundle、diff check；当前 worktree full test 被并行 E11-S40 未完成 JSON artifact 同步打断，已反馈给 Godel，E13 提交不包含该 Billing hunk。剩余缺口：真实 Gateway/Postgres/mock-provider live proof 仍未运行；本片只关闭 Admin UI/audit handoff contract。
- **E13-005-S24 UI/Audit，已完成，2026-06-04，owner Pauli**：Prompt Protection live proof freshness/replay refusal UI contract。原因：S23 已固定 audit handoff 字段，但真实验收还需要证明 UI/audit 不会把旧 live proof、重复 proof run、stale commit 或 simulated replay 当作 current proof；下一片固定 freshness/replay refusal 语义。范围：`web/admin-ui/**`、必要 prompt protection audit fixtures/tests；不改 Gateway/Control Plane/routing/scripts/E11 Billing smoke。验收：新增或强化 UI/API tests，覆盖 current live proof、stale generated_at、stale repo commit、duplicate proof_run_id_hash、simulated replay 四类 classification；显示 blocker/fail/pass readiness 但不渲染 raw report path、raw command、DSN/token/header/provider secret；UI tests/build/bundle check 通过。结果：Prompt Protection summary 新增 `Freshness/replay` 展示，优先读取 audit handoff classification，再回退到 freshness classification/live eligibility/simulated refusal；Admin UI tests 覆盖 `current_live_proof`、`stale_generated_at_refused`、`stale_repo_commit_refused`、`duplicate_proof_run_refused`、`simulated_replay_refused` 和既有 failed refusal，断言 raw report path、raw command、DSN、Authorization/header/token、provider secret 与 proof hash prefix 不渲染。剩余缺口：真实 Gateway/Postgres/mock-provider live proof 仍需后续运行，完整 audit 闭环未最终 `[x]`。
- **E13-005-S25 UI/Audit，进行中，2026-06-04，owner Pauli**：Prompt Protection live proof audit closure checklist UI contract。原因：S24 已固定 freshness/replay refusal 显示，但 operator 仍缺一个可审查 checklist 来判断真实 live proof 是否具备 closing evidence；下一片把 live proof closure 所需字段、缺口和 blocker/pass/fail 状态固定成 UI/audit 白名单展示。范围：`web/admin-ui/**`、必要 prompt protection audit fixtures/tests；不改 Gateway/Control Plane/routing/scripts/E11 Billing smoke。验收：新增或强化 UI/API tests，覆盖 Gateway live proof、Postgres audit row、mock-provider upstream refusal、provider_attempts=0、latency envelope/current provenance/duration available、freshness/replay classification 等 checklist 项；显示 pass/blocker/fail 和缺口摘要，但不渲染 raw report path、raw command、DSN/token/header/provider secret/proof raw id；UI targeted tests/build/bundle check 和 diff check 通过。退出条件：只关闭 Admin UI/audit closure checklist contract；真实 Gateway/Postgres/mock-provider live proof 仍需后续运行。
- **E13-005-S5，已完成，2026-06-04，owner Godel**：Prompt Protection Admin UI/audit 展示缺口。原因：Gateway 已产生 sanitized prompt_protection signals，但最终验收还需要 Admin UI 可读地展示 request/audit log 中的 protection summary，并避免显示 raw prompt/secret。范围：`web/admin-ui/**`；不改后端/Gateway。验收：UI 对 prompt_protection action/reason/hit summary 有可识别呈现；原始 prompt、Authorization、Cookie、secret-like pattern 不渲染；相关 UI tests 和 bundle check 覆盖。结果：Admin UI Request/Audit detail 已渲染 secret-safe prompt_protection summary，并从通用 JSON 摘要中剥离原始 prompt_protection 节点；旧日志无相关 metadata 时保持兼容。剩余缺口：未来后端若新增 safe prompt-protection 字段，需要显式加入 UI 白名单。
- **E11-007-S7，已完成，2026-06-04，owner Godel**：Billing/Price 页面真实 execute 提交流程。原因：S4 后端已支持 `mode=execute` 最小事务 writer，S5 UI 已能展示 future execute states，但页面仍缺真实提交入口和成功/idempotent response 呈现。范围：`web/admin-ui/**`；不改后端/Gateway。验收：UI 在 dry-run fresh 且用户显式操作后调用 `mode=execute`；区分 applied/idempotent/blocked/failed；不回显 dedupe material/raw metadata/credential；相关 UI tests、build、bundle check 通过。结果：Billing/Price 页面新增 fresh dry-run gated 的 `mode=execute` 显式提交，展示 applied/idempotent/blocked/failed 安全摘要，API client 固定同源 execute POST，UI 不渲染 dedupe material、raw metadata、Authorization/Cookie/token/credential。剩余缺口：未跑真实 backend smoke；后端新增 executor summary 字段尚未在 UI 中专门展示。
- **E11-007-S10，已完成，2026-06-04，owner Godel**：Billing/Price 页面展示 ledger executor summary 安全摘要。原因：E9-S7 已准备 `billing_ledger_postgres_executor_summary.v1` 兼容边界，E11-S7 UI 已能执行 `mode=execute`，但页面尚未专门展示 executor summary，用户无法区分 writer outcome、statement count、row-count mismatch 等执行层信号。范围：`web/admin-ui/**`；不改后端/Gateway/routing/scripts。验收：dry-run/execute contract 和 applied/idempotent execute response 中的 `ledger_executor_summary_contract`/`ledger_executor_summary` 有可读安全摘要；展示 schema、executor、outcome、commit/rollback、statement/row count、final statement kind、row-count mismatch；不展示 operation key、error detail、dedupe material、raw metadata、credential；相关 UI tests、build、bundle check 通过。结果：Billing/Price 页面新增 executor summary contract 与 applied/idempotent executor summary 白名单展示，固定 schema/executor/outcome/commit/rollback/statement/row-count 安全字段，过滤 operation key、error detail、dedupe material、raw metadata、Authorization/Cookie/token/credential。剩余缺口：未跑真实后端 smoke；后端后续新增安全字段需显式加入 UI 白名单。
- **E11-007-S12，已完成，2026-06-04，owner Godel**：Billing/Price 页面展示 execute refusal/rollback executor summary。原因：E9-S8 已在 Control Plane execute contract refusal response 中加入 `refused_preflight` summary，并在 transaction contract 中预留 rollback summary contract；E11-S10 只覆盖 applied/idempotent 与 summary contract，错误侧还未专门展示。范围：`web/admin-ui/**`；不改后端/Gateway/routing/scripts/docs。验收：execute_contract refusal response 的 `ledger_executor_summary` 和 rollback executor summary contract 有白名单展示；展示 refused_preflight/refused_rollback、committed/rolled_back、refused statement count、row-count mismatch、error_detail_output omitted；不展示 operation key、raw metadata、raw executor error detail、credential；相关 UI tests、build、bundle check 通过。结果：Billing/Price 页面现在对 execute_contract refusal response 的 `ledger_executor_summary`、`ledger_executor_refusal_summary_contract`、transaction rollback executor summary contract 进行白名单展示，覆盖 refused_preflight/refused_rollback、committed/rolled_back、refused statement count、row-count mismatch、failure output omitted 和 raw executor error omission；API 类型和 UI tests 固定 secret-safe 输出。剩余缺口：未跑真实后端/UI E2E smoke；executor/rollback summary 后续新增字段仍需显式加入 UI 白名单。
- **E11-007-S15，已完成，2026-06-04，owner Godel**：Billing/Price execute freshness/refresh regression。原因：S7/S10/S12 已覆盖 submit 与 executor summary 展示，但真实用户流程还需要锁定 stale dry-run、execute 后刷新和 blocked/failed 状态之间的边界，避免用户把旧 plan 或旧 summary 当作当前入账结果。范围：`web/admin-ui/**`；不改后端/Gateway/routing/scripts/docs。验收：修改调整/退款输入后 execute 操作必须重新回到 stale/disabled 或明确需要重新 dry-run；applied/idempotent execute 成功后刷新相关 ledger view 或明确显示 fresh result 不混入旧 dry-run；blocked/failed/refusal 不覆盖为成功态；所有状态仍保持 executor summary secret-safe；相关 UI tests、build、bundle check 和 diff check 通过。结果：Billing/Price 修改 adjustment/refund 输入后会清掉旧 execute/contract result 并禁用 execute；execute/execute-contract 开始时清除旧 success summary；applied/idempotent 成功后刷新 ledger entries 并显示 `execute_result_fresh=true`、`ledger_entries_refresh_after_execute=success`；后续 failed/refusal 不保留旧 applied summary/executor summary，secret-safe DOM 断言继续覆盖敏感字段。剩余缺口：未跑真实后端 smoke；ledger refresh 失败会显示 refresh failure 并保留 fresh execute result，但失败分支尚缺专门 UI 测试。
- **E11-007-S17，已完成，2026-06-04，owner Godel**：Billing/Price execute refresh failure regression。原因：S15 已实现 execute 后刷新 ledger entries，但 refresh failure 分支目前只靠实现逻辑，没有 UI regression 证明失败时既不丢失 fresh execute result，也不会把 ledger list 失败误读为执行失败。范围：`web/admin-ui/**`；不改后端/Gateway/routing/scripts/docs。验收：mock ledger entries refresh failure 后，applied/idempotent execute result 仍显示 fresh 且 executor summary 保持 secret-safe；UI 明确展示 refresh failure，不把 execute outcome 改成 failed/refusal；旧/stale dry-run guard 仍生效；相关 UI tests、build、bundle check 和 diff check 通过。结果：新增 applied/idempotent 两条 refresh-failure UI regression，证明 execute 成功结果保持 `execute_result_fresh=true`，显示 `ledger_entries_refresh_after_execute=error`，executor summary 仍按白名单展示且不泄露 Authorization/token/credential/operation key/raw metadata/raw executor error；表单修改后旧 execute summary 被清除并回到 stale/disabled guard。剩余缺口：未跑真实后端/UI E2E smoke；refresh failure 仍是 mock 级证据。
- **E11-007-S18，已完成，2026-06-04，owner Godel**：Billing/Price execute backend-smoke readiness regression。原因：S7/S10/S12/S15/S17 已覆盖 UI mock flows，但 E11 最终仍缺真实后端 smoke；在执行 live smoke 前，UI 需要先固定可重复的 readiness regression，证明页面选择器、fresh-result 标记、refresh 状态和 secret-safe 断言足够支撑后续 backend smoke。范围：`web/admin-ui/**`；不改后端/Gateway/routing/scripts/docs。验收：新增或强化 UI test/helper，覆盖 Billing/Price execute smoke 所需的稳定选择器与状态标记（dry-run fresh、execute enabled、execute result fresh、ledger refresh success/error）；断言 applied/idempotent/blocked/failed 不混淆，敏感字段仍不渲染；`npm test -- --run src/App.test.tsx src/api/client.test.ts`、build、bundle check 和 diff check 通过。结果：Billing execute readiness 区域新增稳定 `data-testid`，覆盖 dry-run fresh、execute enabled、execute result fresh、execute outcome、ledger refresh success/error、execute/contract network-call markers；新增 readiness helper 和 blocked execute regression，固定 applied/idempotent/blocked/failed/stale guard 的 smoke selector/state contract，secret-safe DOM 断言继续覆盖敏感字段。剩余缺口：仍未执行真实 backend smoke；下一步需验证真实后端响应中的新增/未知安全字段不会破坏 UI 状态。
- **E11-007-S21，已完成，2026-06-04，owner Godel**：Billing/Price execute backend response tolerance regression。原因：S18 已固定 backend-smoke 选择器和状态标记，但真实后端后续接 billing-ledger runtime writer 时可能增加安全字段或轻微调整 summary payload；UI 需要证明只按白名单读取并忽略未知字段，不把新增安全字段误渲染为 raw JSON 或混淆 outcome。范围：`web/admin-ui/**`；不改后端/Gateway/routing/scripts/docs。验收：新增或强化 UI/API tests，覆盖 applied/idempotent/blocked/failed response 携带未知安全字段、额外 executor summary 字段或 null/missing optional fields 时的渲染；白名单字段可读，unknown/raw metadata/credential/operation key/error detail 不渲染；readiness testids 仍稳定；`npm test -- --run src/App.test.tsx src/api/client.test.ts`、build、bundle check 和 diff check 通过。结果：UI/API tests 覆盖 applied/idempotent tolerant backend response、null/missing optional fields、unknown/extra executor fields，以及 blocked/failed error envelope with data；readiness testids 保持稳定，unknown/raw metadata/credential/operation_key/dedupe/raw executor detail/Authorization/Cookie/token 不渲染。剩余缺口：仍未跑真实 backend smoke；本轮固定 UI/API response tolerance。
- **E11-007-S23，已完成，2026-06-04，owner Godel**：Billing/Price execute live-smoke selector export contract。原因：S18/S21 已固定页面内 selectors 和 response tolerance；后续真实 backend smoke 需要一个可复用的 selector/status contract，避免 smoke 脚本复制易漂移的字符串。范围：`web/admin-ui/**`；不改后端/Gateway/routing/scripts/docs。验收：新增或强化 UI-side selector/status contract export 或 test helper，列出 live smoke 应使用的 `data-testid`、expected status markers 和 forbidden sensitive markers；不改变可见 UI；UI/API tests、build、bundle check、diff check 通过。结果：新增 `ledgerAdjustmentExecuteLiveSmokeContract`，集中导出 live smoke selectors、status markers、refresh markers 和 forbidden sensitive markers；Billing readiness `data-testid` 与 App test helper 改为引用同一 contract，并新增完整性测试。剩余缺口：仍未执行真实 backend smoke；本轮只锁定 UI-side selector/status contract。
- **E11-007-S25，已完成，2026-06-04，owner Godel**：Billing/Price execute live-smoke script handoff contract。原因：S23 已导出 UI-side selector/status contract，但还缺把这些 selector 如何供 smoke 脚本使用的 handoff 说明/测试，避免后续脚本误用可见文案或不稳定 DOM。范围：`web/admin-ui/**`；不改后端/Gateway/routing/scripts/docs。验收：新增或强化 UI-side handoff test/fixture，证明 smoke 应使用 exported selectors、expected status markers、forbidden sensitive markers 和 readiness states；不改变可见 UI；UI/API tests、build、bundle check、diff check 通过。结果：新增 `ledgerAdjustmentExecuteLiveSmokeHandoff`，导出 smoke 脚本应使用的 selectors、status markers、forbidden sensitive markers、script usage 规则和 readiness states；App tests 将页面 readiness 与 handoff states 对齐，覆盖 dry-run required、execute preflight、contract blocked、applied/idempotent refresh success/error、blocked、failed 和 stale plan；不改变可见 UI。剩余缺口：真实 backend live-smoke 脚本仍未接入这些导出的 selectors/readiness states。
- **E11-007-S26，已完成，2026-06-04，owner Godel**：Billing/Price execute live-smoke handoff serialization contract。原因：S25 已导出 UI-side handoff object，但 smoke 脚本后续可能通过 JSON/compiled artifact 消费 contract；需要先锁定 handoff 的可序列化形状，避免 `undefined` marker、函数或不可枚举字段在序列化后丢失并导致脚本误判。范围：`web/admin-ui/**`；不改后端/Gateway/routing/scripts/docs。验收：新增或强化 UI-side serializable handoff export/test，证明 selectors、status markers、readiness states、absent optional markers 和 forbidden sensitive markers 能稳定 JSON 序列化/反序列化；缺省字段使用明确 `null` 或等价稳定标记；不改变可见 UI；UI/API tests、build、bundle check、diff check 通过。结果：`ledgerAdjustmentExecuteLiveSmokeHandoff` 的缺省 optional marker 改为显式 `null`，新增 `ledgerAdjustmentExecuteLiveSmokeSerializableHandoff`、serialization metadata 和 readiness marker key list；测试证明 JSON serialize/parse 后 selectors、status markers、readiness states、forbidden markers 和 absent marker 均稳定保留；不改变可见 UI。剩余缺口：真实 live-smoke 脚本仍未消费该 serializable handoff。
- **E11-007-S28，已完成，2026-06-04，owner Godel**：Billing/Price execute live-smoke selector consumption contract。原因：S26 已提供 UI-side serializable handoff，但真实 smoke 脚本仍未接入；本切片固定脚本侧如何消费 selectors/readiness states，避免脚本复制可见文案或硬编码漂移 DOM。范围：`scripts/**` 中 ledger adjustment execute smoke 相关脚本、必要 `web/admin-ui/**` contract 辅助；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection。验收：新增或强化 smoke script dry-run/contract，证明脚本使用 `ledgerAdjustmentExecuteLiveSmokeSerializableHandoff` 或等价稳定 artifact 的 selectors、readiness states 和 forbidden markers；默认不跑 live backend；不依赖可见 UI 文案；相关 UI tests/build/bundle 与 script diff/default check 通过。结果：新增 `web/admin-ui/src/billingExecuteSmokeContract.serializable.json`，脚本 `-ContractOnly` 读取该 artifact 并校验 selectors、readiness states、forbidden sensitive markers、JSON null optional marker 和 round-trip 稳定性；UI test 断言 TS export 与 JSON artifact 完全一致；默认 smoke 不跑 live backend、不依赖可见 UI 文案。剩余缺口：真实 browser/backend live smoke 还没有实际驱动 Admin UI 页面。
- **E11-007-S31 Smoke，已完成，2026-06-04，owner Godel**：Billing execute browser live-smoke harness preflight。原因：S28 已让脚本消费 UI handoff artifact，但还没有真正证明 browser/backend live smoke 的启动条件、选择器驱动和性能摘要可用。范围：`scripts/**` 中 ledger adjustment execute smoke 相关脚本、必要 `web/admin-ui/**` contract 辅助；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection。验收：新增或强化 browser live-smoke preflight/dry-run，检查 Playwright/browser tooling、Admin UI URL、Control Plane backend URL、handoff artifact freshness；默认不跑 live backend；输出 readiness、submit latency、ledger refresh duration 或明确 unavailable marker；不依赖可见文案、不泄露 secret；相关 script default/contract check、UI tests/build/bundle 通过。结果：UI handoff artifact 新增 `browserPreflight` contract，script contract-only 路径校验 Admin UI/Control Plane safe URL、handoff freshness、node/Playwright tooling，并输出 `browser_smoke_readiness`、`submit_latency_ms`、`ledger_refresh_duration_ms`；默认不访问 live backend、不依赖可见文案，缺 Playwright 时明确输出 `unavailable:playwright`。剩余缺口：真实 browser/backend E2E smoke 仍未执行；本片只关闭 harness preflight/perf contract。
- **E11-007-S32 Smoke，已完成，2026-06-04，owner Godel**：Billing execute browser smoke service readiness gate。原因：S31 已固定 browser harness preflight，但真实 E2E 前还需要把 Admin UI/Control Plane service readiness、health probing、auth/session material omission 和 failure classification 固定，避免 live smoke 因环境缺失被误读为 UI 失败。范围：`scripts/**` 中 ledger adjustment execute smoke 相关脚本、必要 `web/admin-ui/**` contract 辅助；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection。验收：新增或强化 live service readiness dry-run/preflight，覆盖 Admin UI reachable/unreachable、Control Plane health reachable/unreachable、session/token present marker without echo、browser tooling available/unavailable、bounded timeout/duration output、secret-safe blocker classification；默认不跑 live mutation；相关 script check、UI tests/build/bundle 通过。结果：browser smoke handoff 增加 service readiness gate，脚本 contract-only 路径只读 probe Admin UI `/` 与 Control Plane `/healthz`，输出 `admin_ui_reachable`、`control_plane_health_reachable`、`service_probe_timeout_ms`、`service_readiness_duration_ms`、`session_material_present`、`session_material_echoed=false` 和 `service_blocker`；当前本机缺 Playwright/服务时归类为 `browser_tooling_unavailable+admin_ui_unreachable+control_plane_health_unreachable`，不做 live mutation、不回显 token。剩余缺口：真实 apply/idempotent/refund browser E2E 仍需后续 live 环境验收。
- **E11-007-S34 Smoke，已完成，2026-06-04，owner Godel**：Billing execute browser action plan dry-run contract。原因：S32 已固定服务 readiness gate；下一步在不执行 live mutation 的前提下固定 browser E2E 会如何驱动 dry-run、execute、idempotent replay、refund refusal 和 ledger refresh，避免真正 live 前 action sequence 依赖可见文案或不稳定 DOM。范围：`scripts/**` 中 ledger adjustment execute smoke 相关脚本、必要 `web/admin-ui/**` contract 辅助；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection。验收：新增或强化 browser action plan dry-run/contract，列出 stable selectors、expected states、mutation opt-in gate、action duration markers、failure classification 和 forbidden sensitive markers；默认不点击/不提交 live mutation；script check、UI tests/build/bundle 通过。结果：UI handoff artifact 新增 `browserActionPlan`，固定 dry-run、execute apply、idempotent replay、refund refusal、ledger refresh 的 stable selector、expected readiness state、live mutation opt-in gate、failure classification 与 duration markers；Billing dry-run form/input/button 增加 smoke-only `data-testid`；脚本 contract-only 输出 `browser_action_plan_live_mutation_enabled`、`browser_action_plan_failure_classification`、`dry_run_plan_duration_ms`、`execute_apply_duration_ms`、`idempotent_replay_duration_ms`、`refund_refusal_duration_ms` 和 `ledger_refresh_duration_ms`，默认不点击/不提交 live mutation，显式 env opt-in 也仅关闭 contract gate 不执行 live。剩余缺口：真实 browser E2E 仍需 live Admin UI、Control Plane、Playwright/session 与 mutation 环境验收。
- **E11-007-S36 Smoke，已完成，2026-06-04，owner Godel**：Billing execute browser E2E live opt-in/runbook gate。原因：S34 已固定 action plan dry-run，但真正 live 前还缺一个明确的 opt-in run command gate 和 blocker matrix，把 Playwright、Admin UI、Control Plane、session material、mutation opt-in 与性能 duration evidence 统一起来，避免默认路径误触发真实账务 mutation。范围：`scripts/**` 中 ledger adjustment execute smoke 相关脚本、必要 `web/admin-ui/**` contract 辅助；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection/OpenAPI semantic。验收：新增或强化 live opt-in gate/runbook contract，输出可复制的 live run command、required env/flag matrix、mutation disabled 默认、session material present/echo omitted、service/browser/tooling blocker classification、action duration evidence 名称；默认仍 contract-only/dry-run；script check、UI tests/build/bundle 通过。结果：新增 `browserLiveRunbook` handoff contract，脚本 `-ContractOnly` 输出可复制 live run command、required env/flag matrix、`browser_live_mutation_enabled`、`browser_live_session_material_present`、`browser_live_session_material_echoed=false`、service/browser/session/mutation blocker classification，以及 `service_readiness_duration_ms`、`submit_latency_ms`、`dry_run_plan_duration_ms`、`execute_apply_duration_ms`、`idempotent_replay_duration_ms`、`refund_refusal_duration_ms`、`ledger_refresh_duration_ms` evidence 名称；默认 mutation disabled，显式 env+flag 只关闭 gate，不执行真实账务 mutation，也不回显 session material。剩余缺口：真实 apply/idempotent/refund browser E2E 仍需具备 live Admin UI、Control Plane、Playwright/session 与 mutation 环境后验收。
- **E11-007-S38 Smoke，已完成，2026-06-04，owner Godel**：Billing execute browser live E2E blocker/pass evidence artifact。原因：S36 已给出 live opt-in command gate，但下一步需要把真实 browser E2E 的 blocker/pass/fail evidence artifact 形状固定下来，确保即使缺 Playwright/Admin UI/Control Plane/session，也能产出可审查、secret-safe、带性能 duration 名称的证据；具备 live 环境时同一 artifact 能记录 apply/idempotent/refund/refresh 的 pass evidence。范围：`scripts/**` 中 ledger adjustment execute smoke 相关脚本、必要 `web/admin-ui/**` contract 辅助；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection/OpenAPI semantic。验收：新增或强化 live E2E evidence artifact contract，覆盖 blocked/pass/fail 三态、per-action duration fields、service readiness duration、mutation opt-in/session/tooling/service blocker matrix、secret-safe command summary、artifact freshness/provenance；默认不执行 live mutation、不写真实账务；script check、UI tests/build/bundle 通过。结果：新增 `billing_execute_browser_live_e2e_evidence.v1` handoff contract 和 dry-run artifact 输出，固定 `blocked`/`failed`/`passed` 三态、service readiness/submit/dry-run/apply/idempotent/refund/refresh duration 字段、tooling/service/session/mutation matrix、per-action expected state、provenance 和 secret-safe summary；默认缺 Playwright/Admin UI/Control Plane/session/mutation opt-in 时输出 blocked evidence，不执行真实账务 mutation，显式 env+flag 也只关闭 gate 并保持 session/token 不回显。验证：script contract、opt-in secret-safe contract、`scripts\test.ps1 -ControlPlaneLedgerAdjustmentExecuteSmokeOnly`、build、bundle 和 scoped diff check 通过；全量 Admin UI test 当前被并行 E13-S23 未验收变更中的重复 `blocked` 文本断言打断，已反馈给 Pauli 修正，S38 提交不包含该 E13 hunk。剩余缺口：真实 apply/idempotent/refund/ledger refresh browser E2E pass 仍需具备 live Admin UI、Control Plane、Playwright/session 与 mutation 环境后验收。
- **E11-007-S40 Smoke，已完成，2026-06-04，owner Godel**：Billing execute browser live runner readiness gate。原因：S38 已固定 blocker/pass/fail evidence artifact，下一步需要固定真实 browser runner 何时允许驱动 Admin UI actions，如何在默认 no-mutation 下只做 readiness/blocker evidence，以及具备 Playwright/服务/session/mutation opt-in 时如何把 per-action duration 写回同一 artifact。范围：`scripts/**` 中 ledger adjustment execute smoke 相关脚本、必要 `web/admin-ui/**` contract 辅助；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection/OpenAPI semantic。验收：新增或强化 browser runner readiness gate，覆盖 Playwright install/browser availability、Admin UI/Control Plane URL safety、session/mutation opt-in guard、action selector readiness、default no-click/no-mutation behavior、per-action duration capture names、artifact write/read freshness、secret-safe output；script check、UI tests/build/bundle 通过。结果：新增 `browserRunnerReadiness` handoff contract，固定 `runner_readiness_only` 默认模式、Playwright/Admin UI/Control Plane/session/mutation opt-in/action selector readiness guard、默认不点击 Admin UI actions、不写 live mutation；runner readiness 输出 `actions_allowed`、`browser_available`、`session_material_present`、`mutation_opt_in_enabled`、`selector_readiness`、`no_mutation_default` 和 `artifact_roundtrip_fresh`，并通过 `browser_runner_evidence_json` round-trip 同一 `billing_execute_browser_live_e2e_evidence.v1` artifact；duration capture 名称与 `service_readiness_duration_ms`、`submit_latency_ms`、`dry_run_plan_duration_ms`、`execute_apply_duration_ms`、`idempotent_replay_duration_ms`、`refund_refusal_duration_ms`、`ledger_refresh_duration_ms` 对齐，输出保持 secret-safe。验证：`verify_control_plane_ledger_adjustment_execute_smoke.ps1 -ContractOnly -BrowserProbeTimeoutMilliseconds 120`、带 session/mutation opt-in 的 contract run、`scripts/test.ps1 -ControlPlaneLedgerAdjustmentExecuteSmokeOnly`、`npm test -- --run src/App.test.tsx src/api/client.test.ts`、`npm run build`、`npm run check:bundle` 和 diff check 通过。剩余缺口：真实 apply/idempotent/refund browser E2E pass 仍需 Playwright、Admin UI、Control Plane、session 和 mutation opt-in live 环境验收。
- **E11-007-S42 Smoke，已完成，2026-06-04，owner Godel**：Billing execute browser runner artifact write/read opt-in path。原因：S40 已固定 runner readiness gate 和 `browser_runner_evidence_json` round-trip，但真实 browser live smoke 还需要一个明确的 opt-in artifact write/read 路径，证明具备 Playwright/服务/session/mutation opt-in 后如何把 dry-run/apply/idempotent/refund/ledger refresh 的 duration 与 outcome 写入同一 evidence artifact，并在默认模式下只产出 blocker/readiness 证据。范围：`scripts/**` 中 ledger adjustment execute smoke 相关脚本、必要 `web/admin-ui/**` Billing smoke contract；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection/OpenAPI semantic。验收：新增或强化 browser runner artifact write/read opt-in contract，覆盖 artifact path safety、write disabled by default、explicit write flag/env、JSON schema round-trip、per-action outcome/duration capture、stale artifact refusal、secret-safe session/request/material omission、missing Playwright/Admin UI/Control Plane/session/mutation blocker 输出；script check、UI targeted tests/build/bundle 和 diff check 通过。结果：browser evidence artifact contract 新增 `freshness` 顶层字段；runner readiness 新增 explicit opt-in artifact write/read gate，默认不写 artifact，`-BrowserEvidenceArtifactWriteOptIn` 或 `CONTROL_PLANE_LEDGER_ADJUSTMENT_EXECUTE_BROWSER_ARTIFACT_WRITE=1` 才写入 repo 内 bounded path；readback 会校验 provenance、freshness、matrix、duration field names、per-action `outcome`/`duration_ms`、secret-safe；old commit、old generated_at、handoff stale 和缺 freshness 均被拒绝；repo 外路径按预期非零退出并输出 path refusal。剩余缺口：真实 browser E2E mutation pass 仍需具备 Playwright/Admin UI/Control Plane/session/mutation opt-in 的 live 环境运行。
- **E11-007-S44 Smoke，进行中，2026-06-04，owner Godel**：Billing execute browser action runner dry-run DOM plan boundary。原因：S42 已有 artifact write/read freshness gate，但真实 browser live smoke 还缺一个明确的 Playwright/DOM action runner dry-run 边界，说明 selector、step ordering、mutation guard、duration capture 与 artifact emission 如何从 contract 走向可执行 browser runner。范围：`scripts/**` 中 ledger adjustment execute smoke/browser runner 相关脚本、必要 `web/admin-ui/**` Billing smoke contract；不改 Gateway/Control Plane/routing/billing-ledger/Prompt Protection/OpenAPI semantic。验收：新增或强化 dry-run DOM action runner boundary，覆盖 default no-click/no-mutation、Playwright unavailable blocker、selector availability summary、step ordering dry-run、per-step planned timeout/duration field mapping、artifact emission handoff、secret-safe URL/session/request omission；script contract checks、Admin UI targeted tests/build/bundle 和 diff check 通过。退出条件：只关闭 browser action runner dry-run boundary；真实 browser mutation pass 仍需后续 live Playwright/Admin UI/Control Plane/session/mutation opt-in 环境运行。

> 使用方式：开发组可以直接把本文件拆到 Jira/GitHub Issues。每个任务的完成必须满足 `TEST_AND_ACCEPTANCE.md` 中 Definition of Done。

---

## Epic E0：仓库、工程化和开发环境

- [x] **E0-001 初始化 monorepo/repo 结构**  
  优先级：P0  
  产出：`Cargo.toml`、`apps/`、`crates/`、`web/`、`db/migrations/`、`deploy/`、`tests/`、`docs/`。  
  验收：本地 `make dev` 或 `./scripts/dev.ps1` 可启动 gateway、control API、worker、mock provider。

- [x] **E0-002 建立基础 CI**  
  优先级：P0  
  内容：lint、unit test、frontend typecheck、container build、secret scan。  
  验收：PR 自动运行，失败阻断 merge。当前 CI/scripts/test smoke 串联已 fail-fast，`scripts/test.ps1` 已通过全套 cargo tests、smoke dry-run、npm ci/test，并已将 frontend `build` + `check:bundle` 纳入本地 wrapper 与 CI；wrapper 已修复 `$LASTEXITCODE` 为 null 时错误退出的问题，并使用 hashtable splat 传递 `DryRun`，不再被当位置参数；并补齐 control-plane CRUD 与 retry/fallback dry-run 覆盖。

- [x] **E0-003 建立本地 Docker Compose**  
  优先级：P0  
  内容：PostgreSQL、Redis、gateway、control-plane、worker、admin-ui、mock-provider。  
  验收：一条命令启动，健康检查通过。Docker Compose 已验证启动，gateway/control-plane/mock-provider 健康检查通过，admin-ui 返回 200；已配置 provider key master key 供 dev sealed key 解密；compose dev 暴露端口已收敛为仅绑定 `127.0.0.1`。

- [x] **E0-004 定义全局配置 schema**  
  优先级：P0  
  内容：env、DB config、Redis、timeouts、limits、log policy。  
  验收：非法配置启动失败并给出明确错误。当前 `AI_GATEWAY_ENV=production` 时必须显式设置 `AI_GATEWAY_CONFIG`，避免生产误读相对路径示例配置；routing timeout/attempt/body limit 均已有配置校验。

- [x] **E0-005 建立 provider mock framework**  
  优先级：P0  
  内容：模拟 200、429、5xx、timeout、EOF、invalid SSE、large chunk。  
  验收：集成测试可复现路由和 streaming 故障。

---

## Epic E1：身份、租户、RBAC、审计

- [x] **E1-001 设计并实现 tenant/team/project/user 表**  
  优先级：P0  
  验收：支持单租户默认初始化，所有业务表具备 tenant_id。当前迁移已通过 PostgreSQL 16 fresh init 和跨租户 FK 负向断言。

- [~] **E1-002 实现本地管理员登录**  
  优先级：P0  
  验收：密码 hash、安全 session、登录失败限速。当前 `ai-gateway-auth` 已实现 PBKDF2-HMAC-SHA256 密码 hash、session token 生成/lookup/hash，`db/migrations/0005_*` 已新增 `user_sessions`；Control Plane 已接入 `/admin/auth/login|me|logout`，Admin UI 已调用真实登录并默认依赖 HttpOnly Cookie，会话 header 仅保留为显式 fallback；`verify_control_plane_crud_smoke.ps1` 已覆盖登录取 token；登录失败限速已接入 Control Plane：按 tenant + 规范化用户名维度生成 fingerprint，不保存原始用户名，连续失败达到阈值返回 `429 login_rate_limited`，成功登录创建 session 后清理计数，错误响应不泄露凭据/session/token 或用户是否存在；默认仍使用单进程内存 store，并新增显式 `AI_GATEWAY_ADMIN_LOGIN_RATE_LIMIT_STORE=redis|distributed_redis` 的 Redis 分布式最小 runtime backend，使用 `config.redis.addr/db`、fingerprint-only key、Redis `EVAL` + `SET EX` 原子失败计数和 `DEL` 成功清理；Redis 不可用时 fail-closed 返回同样 secret-safe `429 login_rate_limited`，不回显用户名/凭据/session/token。Redis auth/TLS/连接池、分布式 live smoke 和更完整运维配置尚未完成。

- [~] **E1-003 实现 RBAC 后端中间件**  
  优先级：P0  
  验收：SuperAdmin/TenantAdmin/Ops/Billing/Developer/Viewer 权限测试通过。当前 `ai-gateway-auth` 已按 DB role（owner/admin/ops/billing/developer/viewer）实现权限矩阵纯函数并补单测；Control Plane admin router 已通过 `require_admin_rbac` 接入 session 认证，`rbac.rs` 已补 Control Plane 专用角色判定与 secret-safe capability summary，并已通过 `/admin/auth/me` 返回 `capability_summary.capabilities`/`denied_capabilities`，完整覆盖 provider/key/log/billing/price/manual-test/request-log/health/reconciliation/alert webhook/price version create/prompt eval shadow dry-run 矩阵；key manage 在 Control Plane 收紧为 owner/admin/ops，BillingAdjust 仅 owner/billing，Viewer 不拿 credential-sensitive read；OpenAPI extension 与 `tests/fixtures/control-plane/rbac_matrix_contract.json` 已同步。Admin UI 已按 capability summary 裁剪导航和 Providers 关键操作入口；Redis/分布式策略缓存尚未完成。

- [~] **E1-004 实现 Audit Log**  
  优先级：P0  
  验收：key、provider、price、route、billing 修改均写 audit。当前 Control Plane admin provider/channel/provider_key/api_key_profile/virtual_key/model/model_association 写操作已写入 `audit_logs`，并记录 before/after snapshot 与 session 元数据；audit metadata/snapshot 已接入敏感信息 sanitizer，避免 secret、Authorization、Cookie、payload/body、raw headers、raw UA/client IP、endpoint/base_url/url-like metadata key 等原文落审计，channel audit snapshot 不记录 endpoint/base_url 原文，api_key_profile audit snapshot 仅记录 model_aliases/trace_header_rules 安全 key 与 request_overrides/ip_allowlist 等 count 摘要，canonical model audit snapshot 仅记录 capabilities 安全 key 且对 display/capability 值做 secret-like sanitizer，model_association audit snapshot 仅记录 conditions 安全 key 且对 model_pattern/upstream_model_name 做 secret-like sanitizer。provider create/update/delete、channel create/update/delete、api_key_profile create/update/delete、canonical model create/update/delete、model_association create/update/delete、provider_key create/update/delete、price_version create 与 virtual_key create/disable/expire 已进入事务化闭环：业务写入与 success audit 使用同一 DB transaction，缺失业务行不会构造 success audit，audit insert 失败会回滚对应写入；canonical model 默认 price book selector set/null-clear 已并入 create/patch 的事务化 success audit 闭环，selector 写入与 model 写入、success audit 共用 DB transaction，invalid price book 或 missing model 不构造 success audit；相关 audit metadata 已记录 `user_agent_sha256`、`client_ip_sha256`、IP 来源/类型/范围等安全摘要。Control Plane 已新增只读 `GET /admin/audit-logs` 查询契约/API，支持 tenant、actor、action/resource、时间范围与 limit 过滤，归 `AuditRead`，响应再次 secret-safe 清洗。billing adjustment/refund 已有 BillingAdjust dry-run contract 固定未来 ledger write + success audit 同事务边界；更多真实 billing 写操作覆盖和 audit UI 验收尚未完成。

- [~] **E1-005 OIDC 基础登录草案**  
  优先级：P1  
  验收：可通过一个 OIDC provider 登录，role mapping P1 后续完善。当前 Control Plane 已新增 `GET /admin/auth/oidc/authorize-url` 草案切片：从 `AI_GATEWAY_OIDC_*` env 读取 provider、authorization endpoint、client_id、redirect_uri、scopes，生成 OIDC authorization code URL，并强制 authorization endpoint 使用 HTTPS、redirect URI 使用 HTTPS 或本地 loopback HTTP、state/nonce 随机生成、scope 去重且补齐 `openid`；响应明确 `server_state_persisted=true`、`callback_implemented=false`。`GET /admin/auth/oidc/callback` 已校验并消费 server-side state/nonce，区分 missing/expired/invalid/replay/provider mismatch；valid state 后仍不做真实网络请求、不交换 code、不验签 ID token、不创建 session，但会返回 secret-safe exchange/JWKS/session plan-only refusal contract，锁定 token endpoint HTTPS、PKCE/server-side verifier、server-side client auth、JWKS kid/alg/aud/iss/exp/nonce 校验、`user_identities` lookup 与 role mapping 边界；继续拒绝调用方提交 id_token/access_token/email/roles/groups/client_secret/code_verifier 等 claims/token 直接登录，错误体不回显 provider config、code、state、nonce、token、secret 或 claims。OpenAPI skeleton 与 Control Plane auth fixture 已同步。真实 OIDC callback code exchange、ID token/JWKS 验签、`user_identities` 绑定查找后创建 session、role/group claim mapping 尚未完成。

---

## Epic E2：Virtual Key 与 API Key Profile

- [x] **E2-001 实现 Virtual Key 创建/禁用/过期**  
  优先级：P0  
  验收：secret 只显示一次，DB 只存 hash。当前 Control Plane 已实现 `/admin/virtual-keys` create/list/get 和 `/disable`/`/expire`，服务端生成 `vk_...` secret 并只在创建响应返回，DB 持久化 prefix/hash；Admin UI 已接入创建、查询、禁用、过期和一次性 secret 展示，相关单测覆盖 secret/hash 脱敏。

- [x] **E2-002 实现 API Key 鉴权中间件**  
  优先级：P0  
  验收：OpenAI-compatible Authorization Bearer 可用，无效 key 返回兼容错误。当前 Gateway 已强制 Bearer、按 prefix+hash 查 `virtual_keys`，strict compose smoke 已覆盖缺失/有效 key。

- [x] **E2-003 实现 API Key Profile 数据模型**  
  优先级：P0  
  验收：profile 可配置模型别名、可见模型、渠道 tag 限制。当前已落库 `api_key_profiles` 与 `virtual_key_profile_bindings`。

- [~] **E2-004 实现 Profile 切换 Header**  
  优先级：P0  
  内容：`x-ai-profile`。  
  验收：同一 key 使用不同 profile，`/v1/models` 和路由结果不同。当前 Gateway 已读取并校验 `x-ai-profile`，按 key 绑定 profile 解析后用于 `/v1/models` 和 chat route；profile switching + model visibility smoke dry-run 已补，覆盖 profile header、默认/缺失 profile、非法 profile、public/internal 可见性契约，并已接入 `scripts/test.ps1`。

- [~] **E2-005 实现 IP allowlist 和过期检查**  
  优先级：P0  
  验收：被拒绝请求不进入上游调用。当前 Gateway 已做 key 过期/禁用拒绝；IP allowlist 第一切片已接入：使用 ConnectInfo TCP peer IP，不信任未授权的 `X-Forwarded-For`/`X-Real-IP`；读取 `virtual_keys.ip_allowlist`，空列表允许；支持单 IP/CIDR IPv4/IPv6；非法条目忽略且不放行；拒绝时返回 OpenAI-compatible 403 `api_key_ip_forbidden`，不创建 `request_logs`/`provider_attempts`，不进入上游。`server.trusted_proxy_allowlist` 已接入：默认空列表，仅当 TCP peer 命中受信代理单 IP/CIDR 时才使用 `X-Forwarded-For` 首个 IP 或 `X-Real-IP`，畸形 forwarded IP 在 auth 阶段拒绝；profile 独立 allowlist 已接入：`api_key_profiles.ip_allowlist` 专用 schema/API/Gateway 读取已落地，key allowlist 先放行、profile allowlist 再收紧，空 profile allowlist 不额外限制，畸形/非法条目不放行；旧版 `request_overrides` 中的 `type=profile_ip_allowlist` policy 仍保持兼容。Admin UI profile create/patch 专用 `ip_allowlist` JSON array 表单已接入，前端阻止 malformed JSON、非数组和非字符串条目提交，列表摘要只展示条目数且不渲染 raw payload/Authorization/secret；live smoke 尚未完成。

---

## Epic E3：Provider、Channel、Key Pool

- [x] **E3-001 实现 Provider/Channel CRUD**  
  优先级：P0  
  验收：支持 endpoint、protocol_mode、tags、priority、weight、timeout。当前 Control Plane 已支持 provider/channel create/list/get/patch/delete，`verify_control_plane_crud_smoke.ps1 -IncludeFullCrud -StrictFullCrud` 已通过；provider/channel endpoint 已新增统一安全校验，默认要求 HTTPS 并拒绝 userinfo、query/fragment、localhost、private/link-local/multicast/unspecified IP 和 metadata IP，Gateway 出站前会在打开 provider key 前复核静态 URL 并对 DNS 解析 IP 做 forbidden-range 检查，OpenAI-compatible 与 native/streaming 路径共用该 guard；本地 Compose/Helm mock-provider 场景需显式 `AI_GATEWAY_ALLOW_UNSAFE_PROVIDER_ENDPOINTS=true`。

- [~] **E3-002 实现 Provider Key 加密存储**  
  优先级：P0  
  验收：数据库无明文 key，日志 mask。当前 `ai-gateway-auth` 已实现 provider key AES-256-GCM seal/open、HMAC fingerprint 和 redaction；Control Plane 创建接口已用 env master key 加密提交的 secret，拒绝调用方传入 `encrypted_secret`/fingerprint 和后续 secret patch，响应不回显密文或 fingerprint，Admin UI 已接入创建/状态管理；Gateway 已按路由选择 provider_key_id、读取 sealed provider key、用 env master key 解密并向上游注入 Authorization，`request_logs`/`provider_attempts` 已记录 `provider_key_id`；dev seed/Compose/env 已配置 dev sealed key，provider key runtime smoke 脚本与 fixture 已新增并接入 `scripts/test.ps1` 和 CI dry-run，dry-run 已校验 gateway/control-plane master key 一致、dev seed sealed payload 可由 Compose master key 打开，fixture fingerprint 不直接打印，seed payload parser 不再依赖固定字段顺序；retry/fallback strict live fixture/dev seed 已覆盖 429/5xx/timeout/EOF models/routes/provider keys，并已在子 Agent 环境通过。主线程本机无 Docker，仅复跑 dry-run；同 channel retry 多 key 策略、运行态错误后的 key 状态迁移/轮换和密钥查看审计尚未完成。

- [~] **E3-003 实现 Channel 手动测试**  
  优先级：P0  
  验收：可选择模型测试，不计入用户账务。当前 Control Plane 已新增 `POST /admin/channels/{id}/manual-test` dry-run 契约，可选择 requested/upstream model 并返回 channel/provider/request plan，明确 `upstream_call=false`、`billable=false`、`ledger_write=false`，OpenAPI 与 contract fixture 已补且单测验证不暴露 provider key secret/fingerprint；Admin UI Provider/Channel 管理页已接入 channel manual test dry-run 表单，可选择 requested/upstream model，经 same-origin API wrapper 调用并展示 channel/provider/request plan 与 non-billable flags，结果展示不展开 provider key secret/fingerprint/raw payload。真实非计费上游 probe 尚未完成。

- [~] **E3-004 实现 Key 状态机**  
  优先级：P0  
  状态：enabled/degraded/cooldown/recovery_probe/auth_failed/quota_exhausted/manual_disabled。  
  验收：429/auth/quota 错误正确改变状态。当前 DB 约束和 routing 过滤支持状态枚举；Gateway runtime 已接入 provider key 状态 best-effort 写回，覆盖非流式 chat/responses/embeddings、Gemini native passthrough 和 streaming pre-response adapter error，将 auth/rate-limit/quota-like/server retry-after/timeout 映射到 `auth_failed`、`cooldown`、`quota_exhausted`、`degraded`，并通过 tenant/channel/provider/key 限定更新、跳过 `manual_disabled`/`deleted`，metadata 仅记录结构化状态摘要且不含 provider secret、Authorization 或 payload body；Control Plane provider key read/patch contract 已锁定，PATCH 仅允许人工进入 `enabled`/`manual_disabled`/`degraded`/`recovery_probe`，禁止直接进入 runtime-managed `auth_failed`/`quota_exhausted`/`cooldown` 和 `deleted`，响应/audit 不返回 secret/fingerprint/current_window_state。真实恢复闭环、轮换策略、channel-level 状态和 live 验收矩阵尚未完成。

- [~] **E3-005 实现恢复探测 Worker**  
  优先级：P0  
  验收：cooldown 到期后探测成功自动恢复，失败继续冷却。当前 `ai-worker recovery-probe` 默认 dry-run，已能列出到期 `cooldown` 与 `recovery_probe` provider key 候选，输出不发起上游调用的 JSON probe plan；执行模式必须显式 `--execute`/`--live` 才启用，已抽出 probe/status-writer trait 与 mocked tests 覆盖成功恢复 `enabled`、失败继续 `cooldown` 或保留 `recovery_probe`、状态更新错误脱敏，并新增 `tests/fixtures/worker/recovery_probe_contract.json` 自测锁定 cooldown/recovery_probe 到期候选、success -> enabled、failure -> cooldown 保持、recovery_probe failure preserving status 和状态更新错误脱敏契约。plan/report 不读取/解密/输出 provider secret、fingerprint、raw metadata/current_window_state，不做账务写入；endpoint/alias/error 等展示字段会脱敏。真实上游 probe 上下文、非计费探测请求和 channel-level 恢复状态机尚未完成。

- [~] **E3-006 实现 provider/channel/key health dashboard API**  
  优先级：P0  
  验收：展示健康分、最近错误、冷却时间、成功率。当前 Control Plane 已新增 `GET /admin/providers/health-summary` 后端切片，汇总 provider/channel/provider_key/model 状态、health score/state、冷却时间、request log bounded sample 统计、最近错误和按 `window_minutes`/`sample_limit` 约束的窗口成功率；OpenAPI 与 contract fixture 已补，响应不返回 endpoint、provider metadata、provider key metadata/current_window_state、encrypted secret、fingerprint 或 raw key，并对展示字符串做 secret-like sanitizer；已新增 `POST /admin/provider-keys/{id}/recovery` 手动恢复最小 API，归 `KeyManage`，仅允许 `cooldown`/`degraded`/`recovery_probe` 安全来源进入 `recovery_probe` 或 `enabled`，写事务化 audit，不读取/返回 provider secret、fingerprint、current_window_state 或 raw metadata，且不执行真实上游 probe、request log 或账务写入；Admin UI Overview 已接入该 summary、窗口成功率摘要和 provider key recovery action。实时 probe/健康分刷新、真实非计费上游恢复 probe 和更完整时间窗口趋势图尚未完成。

---

## Epic E4：Model Catalog 与 Association

- [~] **E4-001 实现 Canonical Model 表和 CRUD**  
  优先级：P0  
  验收：能力标签、上下文、可见性、默认价格可配置。当前 schema 和 Control Plane 已支持 canonical model create/list/get/patch/delete，strict full CRUD smoke 已通过；默认 price book selector 已在 schema/Gateway rate path 中接入（profile > project > tenant > canonical model），并有 pricing fixture/单测覆盖生效版本选择；Control Plane backend dry-run 第一切片已支持 project/profile/requested_model/canonical_model 输入并返回候选、选择和 snapshot，dry-run OpenAPI/fixture 非 UI 契约已接入；Control Plane canonical model 默认 price book selector 已补 read/create/patch 字段、tenant/canonical model/price book 关系校验、secret-safe 写审计 metadata 和 OpenAPI/fixture 契约；patch 显式 `default_price_book_id: null` 已支持 clear，create/patch selector set/null-clear 已与 canonical model 写入和 success audit 共用 DB transaction，response 继续 reload persisted `default_price_book_id`。默认价格配置 UI、dry-run UI 和 live smoke 尚未完成。

- [~] **E4-002 实现 Model Association**  
  优先级：P0  
  内容：explicit channel、channel tag、regex、priority。  
  验收：同一 canonical model 可对应多个渠道。当前 schema、Control Plane CRUD 和 Gateway DB routing 已支持 explicit/channel_tag/model_pattern/global 候选语义，strict routing smoke 已覆盖 explicit channel；多候选/fallback/dry-run 验收尚未完成。

- [~] **E4-003 实现 Channel Model Mapping**  
  优先级：P0  
  内容：upstream model name、prefix trim、大小写策略。  
  验收：OpenRouter 风格 provider/model 前缀可处理。当前 Gateway routing 已按 `model_associations.upstream_model_name` 或 `channels.model_mappings` 改写 upstream model，并由 strict routing smoke 验证日志落库；routing crate 已新增 channel mapping policy 纯函数切片，覆盖 explicit mapping 优先、prefix trim、preserve/lower/upper 大小写策略和空/非法 policy 稳定行为；Gateway runtime 已读取 `ma.upstream_model_name` 与 `channels.model_mappings`，复用 routing policy 解析/映射，兼容旧版 `{ "model": "upstream" }` 显式映射和 `explicit_mappings`/`trim_prefixes`/`case_policy`，映射后的 upstream model 会进入上游 request model、request log 和 provider attempt。live smoke 与更多 provider 风格 mapping 验收尚未完成。

- [~] **E4-004 实现 `/v1/models` 可见性过滤**  
  优先级：P0  
  验收：不同 key/profile 返回不同模型列表。当前 Gateway 已按默认 profile allow/deny 过滤并通过 strict smoke；profile switching + model visibility smoke dry-run 已补，覆盖 `x-ai-profile` 切换、默认/缺失 profile 行为、非法 profile 拒绝和 public/internal 可见性契约。

- [~] **E4-005 实现 Model Association Dry-run**  
  优先级：P0  
  验收：输入 key+model，输出候选渠道和过滤原因。当前 Control Plane backend 已新增 `POST /admin/model-associations/dry-run` 第一切片，支持 project/profile/requested_model/canonical_model dry-run，返回 route candidates、selection 和 snapshot；不发起上游请求、不暴露 provider key secret，也不评估真实 provider key 可用性或 rate-limit。`examples/openapi_admin_skeleton.yaml` 已补充 dry-run schema，`tests/fixtures/control-plane/model_association_dry_run_contract.json` 已补 selected/no-candidate fixture，control-plane 测试已检查 fixture 不含 secret material；dry-run response/OpenAPI/fixture 已暴露 `fallback_allowed`，targeted tests 已通过；Admin UI 已在 Routing 与 Models 页面复用 dry-run 表单/结果展示，显示 candidates、selected、`fallback_allowed`、过滤原因和 route snapshot 摘要，并通过 secret-safe sanitizer 避免渲染 Authorization、secret、payload/raw snapshot；live smoke 和更完整 key 输入验收尚未完成。

---

## Epic E5：Data Plane Gateway 与协议入口

- [x] **E5-001 实现 Gateway HTTP server**  
  优先级：P0  
  验收：`/healthz`、`/readyz`、`/metrics` 可用，Compose smoke 已覆盖；Gateway 请求体大小限制已前置到 Axum `DefaultBodyLimit`，不再在 `Bytes` extractor 后才检查；Gateway CORS 已从 permissive 改为 `AI_GATEWAY_CORS_ALLOWED_ORIGINS` allowlist；Gateway/Control Plane `readyz` 已收窄为可用性摘要，不暴露 database driver、Redis 地址、upstream URL 或数据库错误原文。

- [x] **E5-002 实现 OpenAI Chat Completions 非流式**  
  优先级：P0  
  验收：OpenAI SDK 基础请求成功。当前 Gateway 已支持强鉴权、非流式 mock-provider 转发、请求校验、provider 429 JSON 映射，并在上游请求中注入解密后的 provider key Authorization；`verify_sdk_smoke.ps1` 已通过，provider key live strict smoke 尚未完成。

- [~] **E5-003 实现 OpenAI Chat Completions 流式**  
  优先级：P0  
  验收：stream chunk 和 `[DONE]` 正确。当前 Gateway `stream=true` 已路由到 `streaming::chat_completions_streaming`，通过 OpenAI-compatible adapter 透传上游 SSE 并返回 `text/event-stream`，SDK smoke 已支持 `-IncludeStreaming`，`verify_compose_smoke.ps1` 已改为探测 SSE stream；client cancel/backpressure 第一切片已用 pull-based forwarding、bounded chunk/SSE buffer、downstream body drop=`client_cancel` 和 no-late-fallback contract 覆盖，stream usage/rating 已有 completed confirmed 写入；`routing.stream_idle_timeout_seconds` 已接入 stream forwarding，idle timeout 会记录为 `stream_end_reason=timeout` 且不做 late fallback；完整 streaming acceptance matrix、live cancel/backpressure smoke 和 usage 估算/estimated 仍未完成。

- [~] **E5-004 实现 OpenAI Responses 基础**  
  优先级：P0  
  验收：文本请求和 stream terminal event 正确。当前 Gateway 已新增 `POST /v1/responses` 非流式基础入口，复用认证、DB 路由、provider key 注入、fallback、request log、metrics、usage rating 和 ledger settle；OpenAI-compatible adapter 已支持 Responses upstream build/parse、usage 映射、非流式 fixture 与 `responses_stream_with_provider_key` 最小 stream 发送入口。`stream=true` 已路由到 `streaming::responses_streaming`，复用 provider key 注入、preauth、request/provider logs、pre-response fallback、Responses terminal usage/rating/settle 和 no-late-fallback stream finalizer；`tests/fixtures/gateway/responses_stream_runtime_contract.json` 已锁定无 501、secret-safe 与 terminal behavior。完整 schema/tool semantics、live smoke 和更深 runtime DB integration 验收尚未完成。

- [~] **E5-005 实现 Embeddings**  
  优先级：P0  
  验收：embedding 请求可路由和记账。当前 Gateway 已新增 `POST /v1/embeddings` 最小切片，复用认证、DB route selection、provider key 注入、request log、metrics、fallback、usage rating 和 ledger settle；已补 embeddings runtime contract fixture/self-test，固定 auth、request log、preauth 与 provider attempt/provider key/upstream 调用顺序，preauth 拒绝不触达上游；OpenAI-compatible adapter 已支持 embeddings upstream build/parse、usage 映射和 fixture，embedding usage 以 input tokens + 0 output tokens 参与定价。live smoke、专用 embedding route/capability 约束和更完整输入 schema 尚未完成。

- [~] **E5-006 实现 Anthropic Messages 基础**  
  优先级：P0  
  验收：非流式和流式可用。当前 `ai-gateway-adapters` 已新增 Anthropic Messages adapter-only 切片；Gateway 已新增 `POST /v1/messages` 非流式最小 runtime contract，复用认证、DB route selection、provider key `x-api-key` 注入、request/provider logs、fallback、usage rating 和 ledger settle，并补 fixture/self-test 固定 auth、request log、preauth、provider attempt/provider key/upstream、usage/rating/settle 顺序；Anthropic Messages streaming runtime 最小切片已接入，`stream=true` 进入 `streaming::anthropic_messages_streaming`，复用 preauth、provider attempt、provider key `x-api-key` 注入、pre-response fallback 和 no-late-fallback stream finalizer，`message_stop` terminal 结束为 completed，`error` terminal 映射 `upstream_error`，invalid JSON/parser 错误映射 `parser_error`，`tests/fixtures/gateway/anthropic_messages_stream_runtime_contract.json` 已锁定无 501 与 secret-safe contract。专用 Anthropic route/capability 约束、live smoke 和更完整 Messages tool/content-block semantics 尚未完成。

- [~] **E5-007 实现 Gemini GenerateContent 基础**  
  优先级：P0  
  验收：文本和基础 stream 可用。当前 `ai-gateway-adapters` 已新增 Gemini GenerateContent adapter-only 切片，支持非流式 `POST /v1beta/models/{model}:generateContent` build、body 字段保留、usageMetadata 映射、provider status/invalid JSON/retry-after error mapping 和 stream event JSON fixture；adapter 对 path model segment 做安全校验。Gateway 已接入 Gemini `streamGenerateContent` native streaming 最小 runtime，支持 path `:streamGenerateContent`、body `stream=true` 或 `streamGenerateContent=true` 进入 `streaming::gemini_generate_content_streaming`，复用认证、DB route selection、request log、preauth、provider attempt、provider key `x-goog-api-key` 注入、pre-response fallback 和 no-late-fallback finalizer；`finishReason` terminal 映射 completed，`error` terminal 映射 `upstream_error`，invalid JSON/parser 错误映射 `parser_error`，`usageMetadata` 完整时可进入 stream rating/settle。专用 Gemini route/capability 约束、live smoke 和完整 streaming/tool semantics 尚未完成。

- [~] **E5-008 实现 Native Passthrough 模式**  
  优先级：P0  
  验收：不重建 body，只做鉴权、路由、key 注入、模型名映射。当前 Gateway 已新增 Gemini `generateContent` native passthrough 最小切片：`/v1beta/models/{model}:generateContent` 复用鉴权、DB route selection、provider key 注入、fallback、request/provider logs、metrics、usage rating 和 ledger settle；body 无需改写时保留原始 bytes，需要模型映射时仅改写顶层 `model`，并记录 request/upstream body hash；上游错误会脱敏 provider key，非 JSON 错误只落 hash；Gemini `usageMetadata` 已进入计费，缺少 `candidatesTokenCount` 时可由 `totalTokenCount - promptTokenCount` 兜底。`streamGenerateContent`/`stream=true` 已从旧 501 改为 native streaming runtime，并继续保持不重建 body、仅在模型映射时改写顶层 `model`；通用 native protocol_mode 选择、非 Gemini provider 和 live smoke 尚未完成。

---

## Epic E6：Adapter 与协议转换

- [~] **E6-001 定义 Adapter interface**  
  优先级：P0  
  验收：extract routing fields、build upstream、parse response、parse stream、map errors、extract usage。当前 `crates/adapters` 已定义 adapter contract、routing fields、upstream request、usage、error mapping 与协议级 stream parse 合约；OpenAI/Anthropic/Gemini/MCP fixtures 已纳入 conformance harness。Gateway runtime 接线、更深 trait replay 和 live smoke 仍待完成。

- [~] **E6-002 实现 OpenAI-compatible adapter**  
  优先级：P0  
  验收：chat/responses/embeddings/models fixtures 通过。当前 OpenAI-compatible chat 非流式 adapter 已支持请求校验、upstream build、response parse、usage 提取和错误映射；`stream=true` 请求构建、stream event/`[DONE]` parse 和 `chat_completions_stream` passthrough 已接入；Responses/Embeddings 非流式 upstream/parse/usage fixture 已补；Responses stream adapter parse contract 已覆盖 `response.completed` terminal、`response.failed`/`error` error terminal、invalid JSON parser mapping、terminal usage extraction 和 missing usage 不合成；Models list adapter-level 第一切片已覆盖 `GET /v1/models` upstream build/parse、invalid JSON error mapping、`expected_usage=null` 无 usage 合约、`stream=false` 上游合约与 `stream=true` unsupported。Gateway runtime/live smoke 与更深 trait replay 仍待完成。

- [~] **E6-003 实现 Anthropic adapter**  
  优先级：P0  
  验收：messages、content block、message_stop fixtures 通过。当前 `AnthropicAdapter`/`AnthropicMessagesRequest` 已实现 messages 非流式与 `stream=true` adapter contract、routing fields、upstream build、response parse、usage 映射、provider status/invalid JSON/retry-after error mapping，以及 `end_turn`/`stop_sequence`/`max_tokens`/`tool_use` 的稳定 finish reason 映射；`stream=true` 已生成 `POST /v1/messages` 上游请求并保留 body `stream=true`，`AdapterCapabilities.stream_policy` 标记为 parseable streaming；`AnthropicMessagesStreamEvent` 与 `AnthropicStreamTerminalKind` 可解析 Anthropic SSE data 并分类 `message_stop`/`error` terminal，fixtures 覆盖 request/response content block、stop_reason、usage、secret-safe、valid stream request、stream `message_stop` 与 `error` contract。完整 content block/tool semantics、streaming runtime 和 gateway runtime 接线待完成。

- [~] **E6-004 实现 Gemini adapter**  
  优先级：P0  
  验收：generateContent、stream、finish reason fixtures 通过。当前 `GeminiAdapter`/`GeminiGenerateContentRequest` 已支持 generateContent 非流式 adapter contract、routing fields、upstream path/body build、response parse、usageMetadata 映射、provider status/invalid JSON/retry-after error mapping；第二切片已固定 `STOP`/`MAX_TOKENS`/`SAFETY`/`RECITATION`/`OTHER` finishReason 映射，2xx 非流式响应缺失或非法 candidate terminal 会映射为 parser-owned `provider_invalid_response`，`usageMetadata` 支持缺 `candidatesTokenCount` 时由 `totalTokenCount - promptTokenCount` 兜底，secret-safe fixtures 覆盖 valid、fallback 与 terminal error contract；`streamGenerateContent`/`stream=true` adapter build 已生成 `:streamGenerateContent?alt=sse` 上游请求并标记 stream，`GeminiGenerateContentStreamEvent` 可解析 SSE data、识别 `finishReason` terminal 和 error event。完整 schema/tool semantics 和 Gateway runtime 接线尚未完成。

- [~] **E6-005 实现 provider error mapper**  
  优先级：P0  
  验收：429/5xx/auth/quota/network/parser 归一化。当前 routing core 已实现基础分类，并新增 adapter provider error bridge，将 adapter status/transport/retry-after/owner/stage/code 输入归一化为 health impact、same-channel retry 与 fallback decision，fixture 覆盖 429、5xx、auth、quota、network、parser、client request 且输出不含 raw adapter/provider payload 或 credential material；Gateway runtime 接入仍待完成。

- [~] **E6-006 建立 Adapter Conformance Test Harness**  
  优先级：P0  
  验收：每个 adapter 必须跑 fixture，CI 阻断失败。当前 `ai-gateway-adapters` test-only conformance harness 已枚举 OpenAI/Anthropic/Gemini/MCP JSON 与 SSE fixtures，强制 fixture name 全局唯一、valid/error/stream 分类齐备、必要 contract 字段完整、示例不含 secret-like 字符串，并带 synthetic fixture contract self-test；`scripts/adapter_conformance.ps1` 已支持本地/CI 直接执行 focused adapters conformance test，输出逐 adapter 覆盖摘要，提供 `-DryRun` 计划模式与 `-Strict` 全 crate adapters 测试扩展。CI workflow 与 `scripts/test.ps1` 已强制接入 adapter conformance strict，并由 `scripts/test_adapter_conformance_ci_contract.ps1` 自检覆盖；真实网络调用和更深 adapter trait 行为回放尚未完成。

---

## Epic E7：Streaming Engine

- [x] **E7-001 实现统一 SSE parser**  
  优先级：P0  
  验收：大于 64KB event 正常处理，Rust 单测覆盖大 event 与超限拒绝。

- [~] **E7-002 实现 partial_sent tracking**  
  优先级：P0  
  验收：已输出后不 fallback。当前 Gateway `ForwardStreamState.partial_sent` 在首个非 terminal data event 后置位并记录 request/provider TTFT，stream 错误后写 partial 并关闭而不是 late fallback；live fallback/partial acceptance smoke 和 client cancel 场景尚未完成。

- [~] **E7-003 实现 terminal event validation**  
  优先级：P0  
  验收：OpenAI/Responses/Anthropic/Gemini terminal fixtures 通过。当前 `ai-gateway-stream` 已实现四类协议 terminal event 纯函数与单测，Gateway OpenAI Chat streaming runtime 已用 terminal event 判定区分 completed/upstream_eof；已补 Responses/Anthropic/Gemini cross-protocol SSE terminal fixtures，覆盖 completed、failed/error、invalid JSON、missing terminal、split chunk decode 和 EOF end reason 映射，adapter stream/harness focused tests 已通过。Gateway Responses streaming runtime 已接入 Responses terminal observation，completed terminal 进入 completed finalizer，failed/error terminal 映射 `upstream_error`，invalid JSON 映射 `parser_error`；Anthropic Messages streaming runtime 已接入 Anthropic terminal observation，`message_stop` 映射 completed，`error` 映射 `upstream_error`，adapter parser error 映射 `parser_error`；Gemini GenerateContent streaming runtime 已接入 Gemini terminal observation，`finishReason` 映射 completed，`error` 映射 `upstream_error`，adapter parser error 映射 `parser_error`。跨协议 live streaming acceptance matrix 仍待完成。

- [~] **E7-004 实现 stream_end_reason**  
  优先级：P0  
  验收：completed/client_cancel/upstream_eof/parser_error/timeout 可区分。当前 `StreamEndReason`/`StreamEndSignal` 核心判定和 DB `request_logs.stream_end_reason` 新约束已落地，Gateway streaming runtime 已写入 `request_logs.stream_end_reason`、`partial_sent`、`ttft_ms` 和 provider attempt 终态；client_cancel/backpressure focused contract 已覆盖，timeout live 验收、完整 streaming acceptance matrix 和跨协议 runtime 接入仍待完成。

- [~] **E7-005 实现 backpressure 和 client cancel 处理**  
  优先级：P0  
  验收：下游断开取消上游；慢客户端内存稳定。当前 Gateway OpenAI Chat streaming 已通过 `Body::from_stream` 按下游 poll 拉取 upstream，并新增 per-chunk/backing SSE buffer 上限；forward failure contract 明确 response start 后不 late fallback，upstream read error 记 `upstream_error`，decode/oversized chunk 记 `parser_error`；body drop/downstream write failure/client disconnect 会以 `client_cancel` finalization 写 request/provider attempt 终态。focused streaming tests 已覆盖 partial 后 upstream failure、preflight no partial、oversized chunk、bounded buffer failure、body drop=`client_cancel`；live cancel/backpressure smoke 仍缺。

- [~] **E7-006 实现 stream usage reconcile**  
  优先级：P0  
  验收：usage 缺失有估算和 estimated 标记。当前 OpenAI chat/Responses/Anthropic Messages/Gemini GenerateContent streaming completed finalization 已在 terminal 前观察到完整 usage 时写入 input/output tokens、rating、final_cost、currency、price_version_id，并在 completed+完整 usage+非零 cost 时写 confirmed settle ledger entry；缺 usage、不完整 usage、client_cancel、非 completed 或 zero cost 均不会扣费。usage 估算/estimated 标记和 live reconcile smoke 尚未完成。

---

## Epic E8：Routing、Fallback、Health

- [~] **E8-001 实现候选渠道生成**  
  优先级：P0  
  验收：按 model association 和 profile 权限过滤。当前 `ai-gateway-routing` 已新增 `generate_route_candidates` 纯函数切片，支持 explicit channel、channel tag、regex model_pattern、global association、association/channel priority 合成、upstream model override/channel mapping、profile model visibility/channel tag/provider deny 过滤，并复用 selection status/health/rate/zero-weight filter reason；routing fixture 已反序列化覆盖 profile model allow/deny、channel tag allow/deny、provider deny、association `fallback_allowed` 与 filter reasons，并校验候选输出不含凭据/请求体材料。Gateway DB 候选生成接入、真实 provider key/rate 窗口可用性和 request detail UI 展示仍待完成。

- [~] **E8-002 实现 priority + weight 路由**  
  优先级：P0  
  验收：权重分布测试通过。当前 routing core 已实现 priority + deterministic weight 选择；routing crate 纯函数单测已补空候选、禁用/0 权重不吸票、seed 边界分布与重复稳定；待接 DB/Gateway 后补策略验收。

- [~] **E8-003 实现 health-aware filtering**  
  优先级：P0  
  验收：cooldown/manual_disabled/auth_failed 不被选择。当前 routing core 已覆盖 cooldown/recovery_probe/auth_failed/quota_exhausted/manual_disabled/unhealthy 过滤；待接运行链路。

- [~] **E8-004 实现 rate-limit-aware key selection**  
  进度：`[########--] 80%`。已完成 DB/Gateway reservation runtime、TPM mapper/fallback、trusted numeric source guard；剩余真实 tokenizer/read-model trusted source 与 live Postgres/Gateway smoke，未达最终 `[x]`。
  Update 2026-06-04: DB-layer provider-key rate-limit reservation persistence now has a contract-only bounded SQL skeleton and pure planner for acquire/release, scoped by tenant/provider_key/channel, with secret-safe summaries and Noop when no counters require mutation. Remaining work: real repository execution/migrations/integration with Gateway distributed counters, precise TPM estimation, and live smoke.
  Update 2026-06-04: DB repository execution boundary is now in place for provider-key rate-limit reservation acquire/release, using bounded SQL, tenant/provider_key/channel binds, no-query Noop/Refused paths, secret-safe row summaries, and <=1 affected-row semantics. Remaining work: Gateway runtime wiring to the repository method, live Postgres integration tests, distributed counter behavior, precise TPM estimation, and live smoke.
  Update 2026-06-04: Gateway runtime now calls DB provider-key reservation execution after preauth/request-local planning and before provider_attempt/provider-key/upstream side effects across non-streaming and streaming routes, skips/fallbacks on DB NotApplied/Refused, releases Applied reservations on fallback/error/non-completed stream finalization, avoids DB calls for all-unlimited/no-counter routes, prevents double release, and records only bounded secret-safe execution metadata. Remaining work: live Postgres/concurrency smoke, precise TPM estimation, and final distributed counter acceptance.
  优先级：P0  
  验收：RPM/TPM/concurrency 超限不被选择。当前 DB schema 与 routing core 具备基础字段/过滤位；routing crate 已新增 rate-limit availability 纯函数，统一评估 RPM/TPM/concurrency 窗口、limit/used/required、有限额缺失 counter、0/缺失 limit、非法限额和负数占用，超限或有限额缺失 counter 候选返回 `RateLimitExceeded` 并保留阻断维度摘要，可将 availability 纯函数结果落到 route candidate/filter/snapshot；单测覆盖未超限可选、RPM/TPM/concurrency 超限、并发占用、0/缺失 limit、有限额缺失 counter 保守阻断、负数 used 归零、非法 limit/required 阻断和 snapshot/filter reason 稳定。Gateway 运行态第一切片已在 provider key lateral selection read-model 中 bounded 读取 `rpm_limit`/`tpm_limit`/`concurrency_limit` 与 `current_window_state`，只解析 used 摘要并传入 routing candidate 的 `rate_limit_available`；RPM/TPM/concurrency 任一超限或有限额缺 counter 时候选会被 `RateLimitExceeded` 过滤，不进入 selected/attempt/upstream；`route_decision_snapshot.gateway_rate_limit` 只记录 schema、候选/阻断计数、阻断维度、required capacity 和 window material omitted，不输出 Authorization、provider key、endpoint、payload/body、raw window state。本轮 routing crate 新增 `rate_limit_reservation_contract_v1` 纯函数计划层，固定 acquire/release 对 RPM/TPM/concurrency 三维 counter 的 bounded 增减语义：acquire 仅在所有配置限额窗口 present 且容量足够时计划 increment，超限、缺窗口、非法 limit/required 均保守 reject 且不计划 counter update；release 仅在 prior reservation acquired 时计划 saturating decrement，重复 release no-op，输出只含数字 counter 摘要和维度状态。Gateway runtime 已将 reservation plan 接入非流式与 streaming provider attempt 路径，在 preauth 后、provider attempt/provider key/upstream side effect 前形成 request-local acquire 计划，provider attempt metadata 记录 acquire/finalize 摘要，stream finalizer 对 completed/client_cancel/stream_error 记录 release/finalize 语义；本轮继续补齐 runtime reservation reject fallback：非流式和 streaming chat/responses/embeddings/Anthropic/Gemini native 在 request-local acquire rejected 时会跳过当前 route、在 provider attempt/key/upstream side effect 前尝试下一候选，所有候选 reservation rejected 时返回 secret-safe `429 rate_limit_exceeded`，并在 request route snapshot 写入 bounded `gateway_rate_limit_reservation_rejection_v1` 摘要，过滤 provider key/raw endpoint/payload/window material。真实 Redis/DB counter 写入、精确 TPM 估算和 live smoke 尚未完成。

- [~] **E8-005 实现 retry/fallback engine**  
  优先级：P0  
  验收：500/429/timeout/EOF 按矩阵处理。当前非流式 chat 已对 provider 429/5xx/timeout/EOF/transport 和首字节前错误 fallback 到下一 route candidate，attempt 上限已由 `routing.default_max_attempts` 控制；OpenAI-compatible 与 native passthrough 上游 timeout 已由 `routing.default_timeout_seconds` 控制，HTTP client 已关闭 redirect；非流式 UpstreamRead/body-read 失败已禁止 fallback，避免上游已返回 headers/部分响应后的重复调用/计费；streaming 已支持 pre-response fallback，首个响应输出后不 late fallback；最终成功 route 会回写 `request_logs`，`provider_attempts` 已记录 `fallback_reason` 和 metadata。Gateway `ResolvedChatRoute` 已读取 `model_associations.fallback_allowed`；attempt list 只在追加失败后的 fallback 目标时过滤 `fallback_allowed=false`，初始 selected route 即使 `fallback_allowed=false` 仍可作为首选；streaming 复用同一 `attempt_routes`，因此同步继承限制；测试已覆盖 `chat_attempt_routes_excludes_fallback_disallowed_candidates_but_keeps_selected` 与配置化 attempt cap。`scripts/verify_gateway_retry_fallback_smoke.ps1` 已支持 `-StrictGatewayFallback` 和 `-PreflightOnly`，fixture/dev seed 已新增 429/5xx/timeout/EOF strict live models/routes/provider keys；strict fallback models 已改为 `internal`，默认 profile 不再暴露 strict models，strict preflight 已校验 `public`/`internal` 可见性；retry/fallback smoke 已校验 `request_logs.upstream_model` 与最终 provider attempt 一致，`connection_closed` direct mock-provider 检查已拒绝 DNS/refused 类失败混入。Darwin/Mendel review 项已在主线程修复；Darwin 已在子 Agent 环境跑通 strict live smoke，主线程本机无 Docker，仅复跑 dry-run。同 channel retry、backoff、健康分/cooldown 写回和完整验收矩阵尚未完成。

- [~] **E8-006 实现 route decision snapshot**  
  优先级：P0  
  验收：Request detail 可显示候选、分数、过滤原因。当前 routing core 已有 `RouteDecisionSnapshot`、候选 score/filter/selected 和 trace affinity 决策；已新增 `select_route_from_evaluated` 纯函数入口，routing fixture 已锁定 selected candidate、snapshot summary、score/filter/selected 标记和凭据/请求体安全输出；Gateway 运行态 `route_decision_snapshot` 已在保留安全 root 快照字段的基础上写入 `summary`，稳定暴露 version、requested/canonical model、selected channel/provider model、candidate/filter count、filter reasons、selected score 和 trace affinity status，供 request detail 直接展示，并由 gateway fixture/单测校验不携带原始 trace_id、Authorization、secret、request/response body 或 raw payload；Control Plane dry-run 第一切片可返回 route candidates、selection 和 snapshot；request detail API/UI 更完整展示、真实 provider key 可用性/rate-limit 评估待完成。

- [~] **E8-007 实现 trace affinity**  
  优先级：P0  
  验收：同 trace 优先之前成功渠道。当前 routing core 已支持 previous successful channel affinity，纯函数单测覆盖同 trace 稳定命中、前序渠道不可选 fallback、空候选 not-candidate 和不同 trace pin 到不同渠道；DB 侧已新增 previous-success lookup contract/SQL skeleton 和最小 repository 读取方法：按 tenant + trace_id + created_at TTL 使用 `idx_request_logs_trace_time`，可选 project/canonical model scope，仅读取 `status='succeeded'` 且 channel/provider 非空的最近记录，limit 有上限，输出只保留 channel/provider/model 摘要，不输出原始 trace_id、request_id、provider_key_id、payload/body/hash/metadata/secret；Gateway 运行态已接入 bounded `x-ai-trace-id`：有 trace 时在 selection 前 best-effort 查询 previous successful channel/provider/model，命中则传入 `RouteSelectionContext` 影响后续同 trace 选择，miss/error/skipped 不阻塞请求；`request_logs.trace_id` 仅记录通过安全校验的 trace，`route_decision_snapshot` root/nested trace fields 会移除原始 trace_id，`gateway_trace_affinity` 仅记录 schema、trace 长度/status、lookup 状态/TTL/limit 和 channel/provider/model 摘要，不含 Authorization、provider key、payload/body、raw endpoint 或原始 trace_id；Gateway fixture/单测覆盖命中 previous channel、previous channel 不可用 fallback、无 trace/lookup failure 不影响请求和 secret-safe snapshot。live DB/runtime smoke 待补。

---

## Epic E9：Billing Ledger

- [x] **E9-001 实现 Wallet/Credit/Budget 数据模型**  
  优先级：P0  
  验收：项目/Key 预算可配置。当前迁移已包含 `wallets`、`credit_grants`、`budgets` 和约束。

- [~] **E9-002 实现 Price Book / Price Version**  
  优先级：P0  
  验收：修改价格产生新版本，历史不变。当前已落库 `price_books`、`price_versions`；Gateway 已按 profile/project/tenant/canonical model selector 解析 active/effective price version，并对非流式 usage 写入 `final_cost`、`currency`、`price_version_id`；Control Plane 已新增最小 `POST /admin/price-versions` create 切片，归 `BillingAdjust`，校验 price book currency、canonical model、rate schema 和可选 `retired_at`，拒绝 payload/secret/raw key 字段，并与 success audit 同事务写入。Admin UI 已接入 price version 创建表单和列表刷新；supersede/retire 工作流、账本调整/退款体验和 stream usage rating 待实现。

- [~] **E9-003 实现 pre_authorize**  
  优先级：P0  
  验收：余额不足不调用上游。当前 Gateway 已在非流式 chat/responses/embeddings、streaming chat 和 Gemini native passthrough 的 provider attempt/provider key/upstream 调用前执行 pre_authorize；余额或预算明确不足时返回 OpenAI-compatible `402 billing_insufficient_balance`，回写 request log 失败状态，不创建 provider_attempt、不打开 provider key、不调用上游，错误响应不包含 wallet/budget/id/金额/secret 细节；已新增 dry-run runtime contract fixture/self-test，固定 request_logs rejected-only、provider_attempts 不创建和 provider key/upstream 不触达顺序。pre_authorize 使用 active price version 估算 fixed request cost 和 billable usage rate，读取 project/tenant wallet、active credit grants、pending/confirmed ledger debit/credit 与 tenant/project/virtual_key budgets；缺价格、缺 wallet/budget 或读取异常时 best-effort 放行，避免破坏现有成功路径。本轮 billing-ledger 纯函数新增强一致 writer contract，固定未来 Postgres writer 在锁内重算 wallet + active credit grants + pending/confirmed ledger 与 tenant/project/virtual_key budget 的窗口，明确余额不足和多预算不足拒绝语义；Gateway/DB runtime reserve 强一致扣减仍未接入。

- [~] **E9-004 实现 reserve/settle/refund ledger**  
  进度：`[########--] 80%`。已完成 ledger planner/executor、Control Plane execute contract、writer cutover parser/env guards；剩余真实 billing-ledger runtime writer、production cutover 与 live DB integration，未达最终 `[x]`。
  Update 2026-06-04: billing-ledger now exposes a `billing_ledger_postgres_sqlx_adapter_contract.v1` boundary derived from the Postgres execution plan, with sqlx dependency/I/O explicitly still disabled, transaction lifecycle, bind-marker-only operation key handling, row-count enforcement handoff, and secret-safe DB error code/category mapping. Remaining work: feature-gated real sqlx transaction executor and integration tests against Postgres.
  Update 2026-06-04: billing-ledger now has a feature-gated `postgres-sqlx` executor boundary with optional workspace sqlx dependency, async begin/execute/commit/rollback flow, executable statement bind values, private operation-key bind handling, and sqlx error mapping to stable secret-safe code/category. Remaining work: convert bounded command plans into concrete SQL/binds automatically and add live Postgres integration tests.
  Update 2026-06-04: billing-ledger `postgres-sqlx` now automatically converts bounded Postgres execution plans plus the writer's private operation key into ordered concrete SQLx executable statements/binds, with SQL/debug output redacted to bind markers, bind type fixture coverage, context-mismatch refusal, and existing row-count/rollback lifecycle preserved. Remaining work: live Postgres integration tests against real schema/constraints and runtime wiring.
  优先级：P0  
  验收：幂等 key 生效，重复 settle 不重复扣。当前 DB 已验证重复 settle 被拒绝；非流式 chat 成功链路已在 usage/rating 完整且 `final_cost` 非 0 时 best-effort 写入 confirmed settle ledger entry，幂等 key 为 `settle:{request_id}`；billing-ledger 已新增 reserve/settle/refund contract plan 纯函数，固定 `reserve:{request_id}`、`settle:{request_id}`、`refund:{related_ledger_entry_id}`、`refund_partial:{related_ledger_entry_id}:{refund_operation_id}` 幂等 key，reserve 生成 pending debit，settle 生成 confirmed debit 并计划反转同 request pending reserve，重复 reserve/settle/full refund/partial refund 幂等返回，非幂等重复 settle 与同 refund key 不同金额被拒绝；第二切片已新增 `tests/fixtures/billing/reserve_settle_refund_ledger_contract.json` fixture 回放，覆盖 reserve pending -> settle confirmed -> partial/full refund、重复请求幂等、非法重复 settle/refund 拒绝和 metadata secret-safe。本轮新增 `billing_ledger_consistent_writer_plan.v1` 纯函数/fixture，输出未来 writer 的 `wallets -> credit_grants -> budgets -> ledger_entries` FOR UPDATE 锁顺序、tenant/request/source/idempotency bounded ledger reads、余额窗口、预算维度检查、reserve pending debit、settle incremental debit + reserve reversal、refund confirmed credit 和并发拒绝状态机；`tests/fixtures/billing/consistent_writer_contract.json` 覆盖余额足够/不足、幂等 replay、非幂等重复 settle、refund over amount 和 multi-budget limit，序列化输出不含 payload/Authorization/secret/raw wallet credential/DB URL；本轮继续新增 `billing_ledger_command_executor.v1` mockable writer trait/command plan 与 in-memory executor，能把 reserve/settle/refund plan 转为 bounded assert/insert/update commands，并固定 applied/idempotent/rejected 执行结果语义；`tests/fixtures/billing/consistent_writer_executor_contract.json` 覆盖幂等 replay、非幂等重复、余额不足、multi-budget 不足、refund over amount 和 reserve/settle/refund apply，执行序列化同样不输出 raw idempotency key/payload/Authorization/provider key/DB URL。runtime reserve/refund Postgres writer 和真实 DB integration 验收仍待完成。

  本轮新增 `billing_ledger_postgres_execution_plan.v1` contract-only planner，将 bounded command plan 转为未来 sqlx writer 可消费的 ordered lock SQL、bounded assertion/insert/update statement shapes 和 transaction steps，固定 wallet/credit_grants/budgets/ledger_entries 锁顺序、FOR UPDATE/WHERE bounds、idempotent replay 无写命令，以及 status update 使用 `ledger_entry_id` 而不是 `related_ledger_entry_id`；仍未实现真实 Postgres I/O。

  本轮继续新增 `billing_ledger_postgres_executor.v1` dependency-light 执行边界：通过 mockable `ConsistentLedgerPostgresTransactionExecutor` trait 消费 Postgres execution plan，固定 begin/ordered statements/commit 与 refusal rollback 语义，覆盖 idempotent replay 无写命令、duplicate locked-row refusal、ledger insert failure rollback、Ok(Refused) rollback 和公开 statement result 脱敏归一化；本轮继续补齐 row-count enforcement/DB error mapping contract：statement plan 标注 `ExactlyOne`/`ZeroOrMore`，executor 在 commit 前强制 wallet lock、assert、ledger insert/update 等有界语句恰好 1 行，0 行/多行以稳定 `row_count_enforcement` code rollback，DB constraint-like refusal 输出仅保留 sanitized code/category/detail omitted；真实 sqlx/Postgres I/O 仍待接入。

- [~] **E9-005 实现 usage rating engine**  
  优先级：P0  
  验收：input/output/cache/reasoning token 可计价。当前非流式 chat rating 已写入 request `final_cost` 并供 settle ledger metadata 使用；stream request-log rating 已接入 completed+usage 完整路径；billing-ledger 纯函数已支持 cache/reasoning token rate 计价；runtime usage extraction contract/API 可从 OpenAI/Responses、Anthropic、Gemini usage JSON 中提取并拆分 cache/reasoning token，按分类 token 计价时避免 input/cache 与 output/reasoning 双算，fixture 覆盖 secret-safe 错误与 payload/header/provider marker 不回显；Gateway 非流式 chat/responses/embeddings/Anthropic Messages rating helper 已传入 provider JSON payload，并在 runtime usage totals 与 adapter usage totals 对齐时使用 cache/reasoning 拆分后的 `ExtendedTokenUsage` 计价，invalid/mismatch 时回退旧 input/output usage 且不回显 provider payload/secret。Streaming finalizer、Gemini native passthrough、request log cache/reasoning 字段持久化和真实 DB integration 仍未接入。

- [~] **E9-006 实现 daily reconciliation job**  
  优先级：P0  
  验收：ledger 和 request usage 差异报告。当前已新增只读 daily reconciliation report 基础：billing-ledger 纯函数按 request `final_cost` 推导期望 settle debit，对比 `ledger_entries` 中 settle/refund 的 pending/confirmed 汇总，输出缺失、意外、金额不匹配、币种不匹配摘要和差异明细；Control Plane 已挂载 `GET /admin/billing/reconciliation` 并用 `BillingRead` RBAC，fixture/OpenAPI 已补，report 展示字段会脱敏且不含 payload/secret。Worker 已新增 `ai-worker billing-reconciliation --dry-run --input ...` plan-only 切片，生成 daily scheduler/window/scope/report contract，并补 daily scheduler/window 第二切片：可按 `scheduler.now_utc` 推导上一个完整 UTC day，输出 closed-open UTC window、last-run/watermark contract，默认不写 DB、不发 webhook；DB scheduler/read plan contract 已扩展为 mockable repository 契约，固定 `read_scheduler_state`/`read_reconciliation_batch`、last-run/watermark state read query、cursor closed-open bounds、Postgres `request_logs`/`ledger_entries` 只读查询 skeleton、project filter 参数、bounded batch/has-more/resume cursor 和 payload/header/provider/wallet/DB URL omission，仍不连接 DB、不写 DB、不发送告警，`--execute`/`--send` 当前需 `--force` 且明确拒绝 future live DB reader/writer 与 alert sender；plan serialization 会去除 request/header/provider/wallet/scheduler/DB URL credential material。真实 live DB scheduler 读取、Postgres integration test、真实 alert send 和自动调度尚未完成。

---

## Epic E10：Observability、Logs、Metrics

- [~] **E10-001 实现 request log 和 provider attempts**  
  优先级：P0  
  验收：每次上游尝试独立记录。当前 Gateway 调用链已写入 `request_logs` started/final 和 `provider_attempts` started/final，并落库 canonical/upstream/provider/channel/provider_key_id/route policy；`verify_gateway_routing_smoke.ps1 -StrictGatewayRouting` 覆盖了 mock-provider 非流式成功链路，streaming runtime 已写入 partial/end reason/TTFT 终态；retry/fallback 第一切片已在 fallback 到下一 route candidate 时记录每次 provider attempt、`fallback_reason` 和 metadata，并在最终成功时回写 request log route。同 channel retry、backoff、健康分/cooldown 写回和完整失败链路验收尚未完成。

- [~] **E10-002 实现 Thread/Trace/Request 模型**  
  优先级：P0  
  验收：同 trace 请求可聚合。当前 request/trace/thread 字段和索引已落库；Control Plane 已新增 `GET /admin/traces/{trace_id}` 只读 trace summary API，按 tenant+trace 查询 bounded request summaries，默认 limit=50/最大 500，走 `LogReadMetadata` RBAC，响应只返回请求元数据、hash、token/cost 汇总和错误摘要，不返回 payload body/object ref 或 route snapshot，并对字符串做 secret redaction；request detail 与 trace summary 已新增 bounded ledger summary：按 tenant/request_id 读取 ledger rows，只返回 entry type/status/amount/currency/request/time 等摘要，省略 idempotency_key、usage_snapshot、policy_snapshot、metadata、payload/body/raw metadata；OpenAPI 与 contract fixture 已补。Admin UI 已接入 Trace ID 查询与 summary 展示；payload 懒加载和更完整 trace drilldown 仍待完成。

- [~] **E10-003 实现 payload policy**  
  优先级：P0  
  验收：metadata_only/hash/redacted/full 策略生效。当前 schema 有 payload policy 字段，Gateway 已写请求/响应 body hash；`ai-gateway-observability` 已新增 payload policy 纯函数切片，支持 bytes/JSON 输入和 `metadata_only`、`hash/hash_only`、`redacted`、`full`、`sampled` 决策，未知/空策略默认退回 hash，不泄露 raw payload，redacted/sample preview 复用现有 payload redaction，只有显式 `full` 才返回 raw payload。Gateway runtime 第一切片已按 active profile payload policy/default policy 选择策略，并在 `request_logs` 写入 `payload_policy_id`、`redaction_status`、hash/redacted preview 安全 metadata；默认仍不存 raw payload，显式 `full` 在未接对象存储前降级为 hash marker 并记录 fallback reason，避免 secret-like 内容进入日志/fixture。Admin UI 已补 payload preview lazy-load contract：request detail 只在用户显式点击时调用 same-origin `GET /admin/request-logs/{id}/payload`，403/404 显示无权限/未实现空状态，展示层仅使用 hash/redacted metadata 白名单并继续移除 Authorization/secret/token/cookie/provider key/raw headers/raw key。payload 对象存储、真实懒加载后端读取 API 和 sampled runtime 仍待完成。

- [~] **E10-004 实现 Prometheus metrics**  
  优先级：P0  
  验收：requests/errors/latency/ttft/cost/fallback 可 scrape。当前 `/metrics` 保留 service gauge，并已接入 Gateway runtime requests/errors/latency histogram/fallback/cost 指标；label 低基数且会拒绝 secret-like/unbounded label 值，非流式、stream finalizer 和 fallback 路径已有单测覆盖；已新增 `ai_gateway_request_ttft_ms` Prometheus histogram，streaming finalizer 仅在存在 request TTFT 时记录 `_bucket/_sum/_count`，labels 限定为 `endpoint/method/status/status_class/outcome/error_owner/error_code` 并复用 bounded label sanitizer，非流式路径不记录 TTFT。provider/channel/key 维度、更多 endpoint 和外部 metrics backend 尚未完成。

- [~] **E10-005 实现 alert webhook**  
  优先级：P0  
  验收：错误率、key cooldown、ledger lag 告警。当前 Control Plane 已新增 `POST /admin/alerts/webhook/dry-run` 最小 validate-only 切片，使用 `ProviderManage` RBAC，校验 HTTPS URL、拒绝 userinfo/localhost/private/link-local/multicast/unspecified literal IP 和可疑内部域名，响应只返回 URL path/query、headers、secret 与 payload 的脱敏摘要且不发送出站请求；header secret-like 名称也会拒绝且错误不回显。Worker 已新增 `ai-worker alert-webhook` plan-only dry-run 切片，可从 input JSON 或 `AI_GATEWAY_ALERT_WEBHOOK_*` env 读取 webhook 目的地并生成错误率、provider key cooldown、ledger lag 三类稳定 alert contract，同时输出真实发送前 preflight contract：URL SSRF guard 顺序、DNS 解析后 SSRF 复查 contract、managed/custom header redaction、metadata-only payload body redaction、timeout/retry bounds、would-send transaction shape 和 sender/error sanitization；默认不发送网络请求，`--send/--execute` 在当前切片仍需 `--force` 且明确拒绝，模块内 sender trait 会拒绝 network-capable sender，DNS resolver 只接收 host 且任一解析 IP 为 localhost/private/link-local/multicast/unspecified 都拒绝，force 也不能绕过 DNS 复查或触发 live network；输出会脱敏 URL path/query、secret/header 值、provider key-like 输入和 payload/body 原文，模块内 mock sender 已覆盖成功、失败重试、SSRF validation-before-send、DNS resolved-IP recheck、secret-safe transaction serialization 和 force/network-disabled gate。OpenAPI skeleton、Control Plane contract fixture 与 Worker contract fixture 已补。配置持久化、真实 HTTP webhook sender、真实 timeout/retry 执行、真实数据源驱动的告警规则触发尚未完成。

- [~] **E10-006 实现 OpenTelemetry exporter**  
  优先级：P1  
  验收：trace/metrics/logs 可导出。当前 `ai-gateway-observability` 已新增 OTEL 配置解析与 no-op 初始化切片，并由 `tests/fixtures/observability/otel_exporter_contract.json` contract 锁定 disabled/stdout/otlp endpoint 形态、标准/项目 env 变量、service/resource attributes sanitizer、OTLP endpoint 校验、endpoint/userinfo/query secret 脱敏和 `sdk_pipeline_enabled=false` 边界；配置错误会脱敏 embedded userinfo、secret-like exporter 值和 endpoint secret，初始化仍不接 runtime、不安装 SDK provider、不发网络请求。真实 OpenTelemetry SDK pipeline、Gateway/Control Plane 入口接入、trace/metrics/logs 实际导出和 collector smoke 尚未完成。

---

## Epic E11：Admin UI

- [~] **E11-001 初始化 Admin UI**  
  优先级：P0  
  验收：登录、导航、权限菜单。当前 Admin UI 已接入真实 `/admin/auth/login`/logout，默认通过 HttpOnly Cookie 保持会话，并已在登录后读取 `/admin/auth/me` 的 secret-safe `capability_summary.capabilities`/`denied_capabilities` 裁剪导航与 Providers 关键操作入口，覆盖 Viewer/Billing/Ops 菜单差异；本轮新增 mount session restore：App 启动先通过 cookie 调用 `/admin/auth/me` 恢复 existing session，恢复期间不闪现导航，失败时清理 fallback token 并返回登录页，错误文案 secret-safe；logout 会清空 fallback token、health summary、probe result 与 recovery pending/error state。跨 tab session sync 和更完整配置页面仍待完成。

- [~] **E11-002 Provider/Channel 管理页面**  
  优先级：P0  
  验收：CRUD、测试、启停、错误提示。当前 Provider/Channel 管理页第一切片已完成并进入后续打磨：UI 支持 list/create/disable/delete providers/channels 与核心字段表单，并已为 channels 接入 manual test dry-run 表单/结果摘要（requested/upstream model、same-origin wrapper、channel/provider/request plan、non-billable flags，secret-safe 不显示 provider key secret/fingerprint/raw payload）；本切片已补 provider metadata 与 channel model_mappings/tags/request_overrides/probe_policy/timeout_policy 高级 JSON create/patch 表单，前端会拒绝 malformed JSON 与 Authorization/secret/token/password/payload/body/raw headers/raw key 等 unsafe 字段，列表仅展示 secret-safe 字段计数/key 摘要；API client/test/App tests 已覆盖；Admin UI 已对 ProvidersPage、ProviderKeysPage、ModelsPage、RoutingPage、RequestLogsPage、BillingPage 和 VirtualKeysPage 做 lazy-load，bundle check 以 initial JS 为预算口径并通过，当前 Initial JS 222.8 KiB/250.0 KiB、Total JS 343.0 KiB。更完整 full CRUD polish 尚未完成。

- [~] **E11-003 Model Catalog/Association 页面**  
  优先级：P0  
  验收：模型、关联、mapping、dry-run。当前 lazy-loaded ModelsPage MVP 已支持 canonical model 与 model association list/create/disable/delete，并在模型页接入 model association dry-run 联动，可使用现有 catalog 作为 canonical model key/id 选择来源展示 candidates、selected、fallback 和过滤原因；API wrapper 和 App tests 已覆盖 secret-safe dry-run 展示；advanced association 条件、mapping polish 和 live smoke 尚未完成。

- [~] **E11-004 Virtual Key/Profile 页面**  
  优先级：P0  
  验收：创建 key、profile、预算、模型权限。当前 Admin UI 已有 Virtual Keys/Profile 页，支持 profile list/create/patch/delete、virtual key list/create/get/disable/expire；本切片已新增 profile visible/denied model、model alias 与专用 IP allowlist JSON create/patch 表单、列表摘要展示，virtual key rate-limit/budget policy 安全 JSON 占位与摘要/详情展示，并统一剔除 secret/Authorization/raw payload/body 等 unsafe JSON 字段；创建 key 后仅在创建结果区域一次性展示服务端返回 secret，列表/详情/JSON 摘要不再渲染一次性 credential 原文。更完整预算工作流、专用 budget schema 表单和 profile 其他策略字段编辑仍待完成。

- [~] **E11-005 Request/Trace 页面**  
  优先级：P0  
  验收：查询、详情、route trace、ledger、payload 懒加载。当前 Admin UI 已将 Request/Trace workspace 接入导航并改为 lazy-loaded `RequestLogsPage`，复用 same-origin request log list/detail API wrapper；页面已新增 Trace ID 查询，调用 `GET /admin/traces/{trace_id}` 展示 trace summary metrics、last error、时间范围、currency 和 bounded request rows；request detail 保留 provider attempts，并新增 secret-safe Route Trace 摘要，仅展示 Gateway `route_decision_snapshot.summary` 稳定字段（selected channel/provider model、candidate/filter 数、filter reasons、score、trace affinity），不渲染 raw snapshot/payload/secret；request detail 与 trace summary 已展示 ledger summary rows（entry/status/amount/request/time），不渲染 idempotency key、usage/policy snapshot、metadata、payload、secret 或 Authorization；本切片新增 Payload Preview 面板，默认只展示已有 hash/policy/redaction metadata，只有点击 Load preview 才调用 same-origin `GET /admin/request-logs/{id}/payload`，`payload_stored=false` 禁用操作，403/404 分别显示无权限/未实现状态；preview 展示仅白名单 metadata/redacted preview/hash 字段，并通过 sanitizer 移除 Authorization、secret、token、cookie、provider key、raw headers/raw key 和 body/raw payload 字段；前端测试覆盖 route/trace/ledger/payload 字段脱敏和 lazy-load 按钮行为。真实 payload 对象存储后端、完整 payload drilldown 和更完整 trace drilldown 仍待完成。

- [~] **E11-006 Health Dashboard**  
  优先级：P0  
  验收：provider/channel/key/model 状态、手动恢复。当前 Overview 保留服务探针，并已接入 `GET /admin/providers/health-summary` 渲染 provider/channel/key/model 健康矩阵、recent error、score、route count 和窗口成功率摘要；本切片新增 operator controls，可选择 `window_minutes`/`sample_limit` 后手动刷新 same-origin health summary，并支持按 scope/search 扫描 provider/channel/key/model rows；本轮继续新增 bounded auto-refresh（Off/30s/60s）与 parent/local summary state sync，手动/自动 summary refresh 成功后清理旧错误和旧数据，Service Refresh 成功后同步回父级 summary；provider key recovery action 继续调用 same-origin `POST /admin/provider-keys/{id}/recovery`，仅在具备 `provider_key.recovery` capability 时展示，pending/success/error 状态保持，recovery/summary error 使用 secret-safe 文案；展示层继续 secret-safe，不渲染 provider secret、fingerprint、current_window_state 或 raw metadata。真实非计费上游恢复 probe 和更完整成功率时间窗口图仍待完成。

- [~] **E11-007 Billing/Price 页面**  
  进度：`[########--] 76%`。已完成 Billing/Price UI execute/readiness、smoke handoff consumption、OpenAPI evidence lifecycle/provenance、package cache/download opt-in evidence、browser runner readiness 与 artifact write/read freshness gate；剩余真实 backend/browser live smoke、真实 semantic/client-generation 工具链证据与 E2E 验收，未达最终 `[x]`。
  Update 2026-06-04: Control Plane ledger adjustment/refund dry-run now validates refund remaining amount against same-tenant/source/currency confirmed refund/adjust credits, returns a bounded `refund_remaining_summary`, and Admin UI exposes a plan-only adjustment/refund dry-run panel with remaining summary, planned entry, related entry, and future audit/write contract while omitting execute actions, idempotency keys, and raw metadata. Remaining work: real execute workflow and broader billing write/audit coverage.
  Update 2026-06-04: Control Plane dry-run endpoint now accepts `mode=execute_contract`, reuses full validation, and returns a stable `501 future_writer_required` contract with ledger/request-log/audit/upstream writes disabled and future transaction/audit guarantees documented. Admin UI now tracks fresh vs stale dry-run state and exposes a disabled execute-readiness affordance without issuing execute network calls. Remaining work: real transactional execute endpoint and UI submit flow.
  优先级：P0  
  验收：价格版本、账务流水、对账报告。当前后端已新增只读 `GET /admin/price-versions`、`GET /admin/ledger/entries` 和 `GET /admin/billing/reconciliation`，均归 `BillingRead`、带 limit/filter 校验和 secret/payload-safe JSON 输出；Request/Trace detail 侧也已复用 ledger 只读摘要能力串联 request_id 关联账本行，但仍不暴露 Billing 页完整 usage/policy/metadata；并新增最小 `POST /admin/price-versions` create 写 API，归 `BillingAdjust`、可持久化 `effective_at` 与可选 `retired_at`，写入事务化 audit；Admin UI reconciliation report API/client/UI 已接入 lazy-loaded Billing/Prices workspace，支持 Price Versions、Ledger Overview、Reconciliation 三个 tab 的 list/filter/detail/report 展示，并保持 bundle budget；Admin UI price write 第一切片已接入，支持创建 price version 表单（price_book_id/canonical_model_id/version/effective_at/retired_at/status/pricing_rules JSON）、same-origin `POST /admin/price-versions`、成功后刷新列表，并对 pricing_rules 输入/展示拒绝或移除 payload/secret/Authorization/raw key 等 unsafe JSON 字段。本轮后端新增 `POST /admin/ledger/adjustments/dry-run`，归 `BillingAdjust`，plan-only 且 `ledger_write`/`upstream_call`/`request_log_write`/`audit_log_write` 均为 false；支持 adjust/refund amount/currency/related ledger entry 边界校验，拒绝 raw idempotency/payload/Authorization/provider key/secret 字段，响应只返回 bounded ids/amount/status/未来 write+audit transaction contract。真实账本调整/退款执行工作流、余额/累计退款校验和更完整财务审计体验仍待完成。

---

## Epic E12：Migration Importer

- [x] **E12-001 New API 配置解析器**  
  优先级：P0  
  验收：样例 dump 可解析；当前最小切片已支持 New API 风格 provider/channel/model/key dry-run 解析。

- [x] **E12-002 One API 配置解析器**  
  优先级：P0  
  验收：样例 dump 可解析；当前最小切片已支持 One API 风格 channel/provider/model/key/model mapping dry-run 解析。

- [x] **E12-003 映射为内部模型**  
  优先级：P0  
  验收：模型映射转 canonical + association + channel mapping；当前已新增内部模型映射 dry-run report，可读取 New API/One API importer JSON report，输出 canonical model、model association、channel mapping、conflicts、manual review 和 next steps，不落库、不输出 secret material。

- [x] **E12-004 Dry-run report**  
  优先级：P0  
  验收：列出创建/更新/跳过/冲突/人工确认项；当前最小切片输出结构化 JSON report，包含 counts/providers/channels/models/warnings/unsupported_fields，且不落库、不调用服务。

- [~] **E12-005 Apply + rollback snapshot**  
  优先级：P0  
  验收：导入失败可回滚，可重复执行。当前 `scripts/importers/import-apply-plan.ps1` 已在 read-only plan 基础上新增 PostgreSQL SQL-plan executor 与 rollback journal contract：默认仍为 dry-run，不连接 live DB、不写库；`-Apply` 必须配合 `-Force`，无冲突、source provider/channel 绑定通过且 adapter 支持时只进入 `prepared_sql_plan`，并明确 live PostgreSQL runner 未实现、不会真实写库；输出事务边界、operation id/idempotency manifest、rollback snapshot entry/before-image schema、refusal contract、JSON/SQL operation bundle、`SELECT ... FOR UPDATE` before-image 捕获 SQL、canonical model `ON CONFLICT` upsert SQL、simple channel `model_mappings` JSONB merge SQL、`importer_apply_runs`/`importer_apply_operation_journal` DDL、apply run/operation journal insert SQL plan 和 rollback operation skeleton；rollback execution dry-run plan 已锁定 reverse apply order、operation/run `rolled_back` status SQL skeleton、replay/idempotency contract 与 stale target hash refusal。`tests/fixtures/importers/apply_plan_canonical_only.sample.json` 与 `postgresql_sql_executor_contract.expected.json` 已覆盖无冲突 canonical adapter、rollback journal DDL/insert、rollback execution refusal/replay contract；`tests/fixtures/importers/apply_plan_channel_mapping_bound.sample.json` 已覆盖 source channel binding 给定、无 alias conflict 的 `channel_mapping_entry` SQL plan，包含 before-image `FOR UPDATE`、`channels.model_mappings` patch SQL、rollback journal row 和 channel mapping rollback skeleton；`model_association` 以及复杂 channel mapping policy 仍会因 alias conflict、source channel 到内部 channel/provider 绑定缺失或 adapter 未覆盖被 preflight 阻断；真实 live PostgreSQL runner、事务内 journal 持久化执行和 rollback compensating mutation runner 尚未完成。

---

## Epic E13：安全和合规

- [~] **E13-001 Secret masking middleware**  
  优先级：P0  
  验收：日志不出现 Authorization/API key/Cookie。当前 `ai-gateway-observability` 统一 sanitizer 已被 Gateway 错误路径接入，database error log、adapter error body 和 OpenAI-compatible error body 会脱敏 Authorization/API key/Cookie/token/password/secret，同时保留 `model_key`/`cache_key`/`public_key_id` 等非敏感标识；本轮审查修复已补 Gateway body limit 前置、CORS allowlist、provider endpoint static + DNS SSRF guard（provider key 打开前执行）、HTTP redirect 禁用、`readyz` 脱敏和生产配置路径显式化；完整 HTTP middleware 层与更多入口接入仍待完成。

- [~] **E13-002 Provider key encryption**  
  优先级：P0  
  验收：密钥加密，密钥查看审计。当前 `ai-gateway-auth` AES-256-GCM provider key 加密/fingerprint 已实现，Control Plane 创建接口使用 env master key 加密并拒绝明文回显，Gateway 已完成 provider key runtime 第一切片（选择 provider_key_id、解密 sealed key、上游 Authorization 注入、日志关联），provider key runtime smoke 脚本/fixture 已新增并接入 `scripts/test.ps1` 和 CI dry-run，dry-run 已校验 gateway/control-plane master key 一致、dev seed sealed payload 可由 Compose master key 打开，fixture fingerprint 不直接打印，seed payload parser 不再依赖固定字段顺序，provider key 管理写操作已进入 admin audit 第一切片；retry/fallback strict live fixture/dev seed 已覆盖 429/5xx/timeout/EOF models/routes/provider keys，并已在子 Agent 环境通过。主线程本机无 Docker，仅复跑 dry-run；密钥查看审计、轮换流程和运行态状态迁移尚未完成。

- [x] **E13-003 Payload redaction 基础规则**  
  优先级：P0  
  验收：邮箱、token、常见 key 自动脱敏。当前 `ai-gateway-observability` 已提供 payload/json redaction 纯函数切片，覆盖 `serde_json::Value` 递归脱敏、字符串 payload 基础扫描、email、Bearer/API token-like value 和 password/secret/token/authorization/cookie/api_key 等常见敏感 key，并通过单测覆盖 `model_key`、`cache_key`、`public_key_id` 不过度脱敏；Gateway payload policy runtime 已复用该 redaction 生成 request/response redacted preview 安全 metadata，并由 gateway runtime fixture/contract 锁定 email、token、common key redaction 与不存 raw payload。更完整审计查询/UI 懒加载仍待完成。

- [~] **E13-004 供应链扫描**  
  优先级：P0  
  验收：CI 含 secret/dependency/container scan。当前 CI 已运行安全输出 `scan_secrets.ps1`；`.github/workflows/ci.yml` 已加入 `scan_supply_chain.ps1 -SkipNetwork`、`generate_supply_chain_artifacts.ps1`、SHA256 checksum 和 `actions/upload-artifact` 上传；`scan_supply_chain.ps1 -SkipNetwork` 可校验 Cargo.lock source/checksum、npm lock integrity、Dockerfile/Compose image pinning 与 artifact generator/CI 上传 contract（OutputDirectory、upload path、artifact name、`if-no-files-found:error`），缺 Docker/trivy/grype 时 warning/skip；artifact generator 已输出 SBOM/provenance/manifest/checksum，`manifest.json` 已记录离线 dry-run contract、覆盖输入、缺工具策略和剩余缺口，本地 `scripts/test_supply_chain_scan.ps1` 与 `scripts/test_supply_chain_artifacts.ps1` 自检通过。联网漏洞扫描、真实镜像扫描和 digest pinning 尚未完成。

- [~] **E13-005 Prompt Protection**  
  进度：`[#######---] 72%`。已完成 Gateway 多 endpoint preflight、proof report path/lifecycle/provenance/freshness 与 Admin UI/audit freshness/replay refusal contract；剩余真实 Gateway/Postgres/mock-provider live proof 与完整 Admin UI/audit 闭环，未达最终 `[x]`。
  Update 2026-06-04: Gateway startup now wires `security.prompt_protection` YAML/AppConfig into the runtime config with precedence `AI_GATEWAY_PROMPT_PROTECTION_CONFIG_JSON` > YAML/AppConfig > legacy `AI_GATEWAY_PROMPT_PROTECTION` > default enforce, and validation errors remain secret-safe. Remaining work: audit query/UI coverage, broader endpoint coverage where required, and any future dynamic reload path.
  Update 2026-06-04: `/v1/responses` now reuses the already-parsed prompt protection runtime config for non-stream and stream requests, running after parse/hash and before canonical routing, request_started, provider_attempt, provider-key open, and upstream calls. Remaining work: embeddings or any other required endpoint coverage, audit query/UI coverage, and any future dynamic reload path.
  优先级：P1  
  验收：regex mask/reject、scope、命中日志。当前 `ai-gateway-observability` 已新增 prompt protection 纯函数切片，支持 text/json/payload 输入，输出 action、scoped hits、safe_text/safe_json；覆盖 secret-like token、Bearer、password/API key/sensitive fields mask，以及明显 prompt-injection phrase reject，并保留 `model_key`、`cache_key`、`public_key_id` 等公开标识；hit summary 已做 bounded cap，避免恶意 payload 放大日志/去重成本。Gateway `/v1/chat/completions` 非流式与 stream=true 已接入 runtime preflight，默认 `AI_GATEWAY_PROMPT_PROTECTION=enforce`，支持 `audit`/`disabled`；enforce 命中后在 canonical routing、request_started、pre_authorize、provider_attempt、provider key 解密和上游调用前返回 OpenAI-compatible `prompt_protection_rejected`，request log 使用 hash-only payload metadata，并只写 action/mode/reason/hit_count/scopes/hit_kinds 安全摘要；audit 命中在 provider_attempt 前仅记录 request hash/action/reason/hit_count 后继续，不落 raw payload/secret。`tests/fixtures/gateway/prompt_protection_runtime_contract.json` 已锁定该 runtime 合约。本轮新增可配置规则纯函数/contract：`prompt_protection_rules_v1` 支持 bounded literal/contains/regex/regex_like mask/reject、text/json path scope、规则数量/name/pattern/scope 长度限制、regex config-parse 编译与 program size limit、拒绝 empty-match regex、每字段扫描/替换上限、secret-like rule name/pattern value 拒绝、错误消息与 summary 脱敏；同时补充 observability 纯函数 runtime config boundary，可解析/合并 default rules + custom `rules`/`custom_rules`，支持 `mode=enforce|audit|disabled`，default rules 默认保留并先执行，audit/disabled 的 effective action 为 allow，summary 仅输出 mode/action/count/scopes/rule names/limits 等安全摘要，不输出 raw payload/pattern。本轮 Gateway 已新增 `AI_GATEWAY_PROMPT_PROTECTION_CONFIG_JSON` 启动期 bounded JSON 配置入口，解析 `prompt_protection_rules_v1` 后缓存 precompiled custom rules，chat preflight 只应用已解析配置、不做 per-request regex/config compilation；未提供 JSON 时继续沿用旧 `AI_GATEWAY_PROMPT_PROTECTION` mode 行为。Config crate 已新增 `security.prompt_protection` schema/validation，支持 mode/default_rules/custom_rules/limits，validate 阶段复用 observability parser 并实际执行自定义 max_rules/name/pattern/scope 上限，safe summary 与错误不回显 raw pattern/payload/secret；本轮继续补齐 `examples/config.example.yaml` 的 safe enforce demo custom rule，并对 prompt_protection 顶层/limits/custom rule/pattern unknown fields 做 schema 拒绝，secret-like unknown key/value 不回显。Gateway YAML/config runtime wiring 仍未接入，env JSON 入口保持兼容。`tests/fixtures/observability/prompt_protection_configurable_rules_contract.json` 与 `tests/fixtures/gateway/prompt_protection_runtime_contract.json` 已覆盖 regex mask/reject、scope、invalid regex、过长 pattern、too many rules、secret-safe serialization、default rules retained、audit safe summary、startup parse boundary 和 public id 不误伤。审计查询/UI 仍待完成。

---

## Epic E14：部署、运维和发布

- [~] **E14-001 Dockerfile 和 Compose**  
  优先级：P0  
  验收：本地和 staging 可启动。当前本地 Compose、Docker release build、smoke 已通过，compose dev 暴露端口已收敛为仅绑定 `127.0.0.1`；Admin UI 镜像已切为生产 build + nginx same-origin `/api/*` 代理，示例 Compose 已挂载 dev seed；staging 未验证。

- [~] **E14-002 Helm Chart**  
  优先级：P0  
  验收：K8s staging 部署成功。当前 Admin UI Helm 默认值已移除浏览器 `VITE_*` cluster DNS，改用容器内 `*_UPSTREAM` 反代；Chart 已补 values schema、资源/探针/Secret/ConfigMap/Ingress/service 引用校验、backend ConfigMap 挂载约束、默认 ServiceAccount token 关闭、RuntimeDefault seccomp、drop ALL 和 `allowPrivilegeEscalation=false`；`python deploy/helm/validate_chart.py --skip-helm --self-test` 静态校验和 contract self-test 通过，缺 Helm 时 lint/template 只 warning。K8s staging 尚未验收。

- [~] **E14-003 Metrics dashboard 模板**  
  优先级：P0  
  验收：Grafana/Prometheus 可导入。当前 `examples/observability` dashboard/rules 已对齐真实 E10-004 runtime metrics：requests/errors/latency histogram/TTFT histogram/fallback/cost 及实际 labels；TTFT 面板已接入 `ai_gateway_request_ttft_ms_bucket/_sum/_count`，TTFT p95 alert 仅引用已落地指标；ledger/event lag、key cooldown、provider/channel/key/model 维度均保持 clearly marked pending placeholder，未重新引入未落地查询或 active alert；`validate_templates.py` 可在无 `promtool` 环境下校验 JSON/YAML、metric/label 引用和 pending panel 契约。

- [~] **E14-004 Backup/restore 脚本**  
  优先级：P0  
  验收：staging 恢复演练成功。当前已新增最小 PostgreSQL `scripts/db/backup.ps1` 与 `scripts/db/restore.ps1`，支持 `DATABASE_URL`/显式参数、dry-run/preflight、URL 密码打码、backup overwrite `-Force` 和 restore 默认 dry-run/执行需 `-Force`；`scripts/db/test_backup_restore_scripts.ps1` 已覆盖 `DATABASE_URL` 解析与 URL 编码密码透传、密码打码、缺 `pg_dump`/`pg_restore` warning、restore 默认 dry-run、restore 执行需 `-Force`、backup 路径覆盖需 `-Force`、检查模式不创建 dump 且输出不泄露 secret；既有 release backup gate 仍会跑 `scripts/db/verify_backup_restore_contract.ps1`，缺 DB 参数或 `pg_dump` 时仅 warning，不执行数据库命令。staging 恢复演练尚未完成。

- [~] **E14-005 Release checklist 和 runbook**  
  优先级：P0  
  验收：按 `project/RELEASE_CHECKLIST.md` 发布。当前已有 JSON release gate、release checklist 和 deployment ops runbook；gate 覆盖 format/test/frontend/build/security/backup/helm/smoke，默认只做安全 check/dry-run，支持逗号列表 `-Checks backup,helm,smoke`；JSON summary 已补 `statusPolicy` 区分 pass/warn/fail 与本机缺工具 warning，security gate 已包含 SBOM/provenance/manifest/checksum artifact generation；release checklist/runbook 已补 GitHub 首次 push 前本地收口流程，要求跑 `-Checks security`、`-Checks backup,helm,smoke` 和 `scripts/scan_secrets.ps1`，并明确 backup/helm/smoke dry-run 缺 DB 参数、`pg_dump`、Helm、Docker、`node` 或 `npm` 时使用 `local warning:` 文案、必须在具备工具的环境补跑，显式 runtime smoke 缺必需工具仍 fail。尚未按清单完成 staging 发布验收。

---

## Epic E15：P1/P2 增强

- [~] **E15-001 Request Override 可视化和 dry-run**  
  优先级：P1。当前 Admin UI 已在现有 Routing/Models model-association dry-run 结果中展示 request override 摘要，覆盖 profile request_overrides、payload policy 与 profile IP allowlist 派生信息；Profiles 列表同步展示 request controls 摘要。展示路径复用 secret-safe JSON sanitization，不新增后端 dry-run 参数，不渲染 Authorization、secret、raw payload。真实配置 dry-run/后端 override 模拟执行仍待完成。

- [~] **E15-002 Exact Cache 和 cache billing**  
  优先级：P1
  当前 `ai-gateway-billing-ledger` 已新增 exact cache billing 纯函数合约切片：支持由 matched input tokens 推导 request cache `hit`/`miss`/`partial_hit`，partial hit 会把 cached input tokens 与 billable input tokens 分离，cached input 可按 read policy 免费、折扣 token rate 或固定成本计价，uncached input/output/fixed request 仍按正常请求计价；plan 同时输出 read/write cache operation idempotency keys 与 settle key，ledger metadata 只序列化 cache key hash、cache entry id、策略和 token 摘要，不携带 raw key/payload/secret/idempotency key；`tests/fixtures/billing/exact_cache_billing_contract.json` 已覆盖 hit/miss/partial-hit、免费/折扣/固定读写、幂等 replay、非法 token split 和非幂等重复 settle。Gateway runtime exact cache lookup/store、cache hit response path、对象存储/Redis 后端、pre_authorize/reserve 集成和 live smoke 尚未完成。

- [~] **E15-003 OIDC/SAML 完整集成**  
  优先级：P1

- [~] **E15-004 Coding Agent Profiles**  
  优先级：P1

- [~] **E15-005 ClickHouse Log Store**  
  优先级：P1
  当前 Worker 已新增 `ai-worker clickhouse-log-store --dry-run --input ...` plan-only 切片，复用 `ai-gateway-observability` ClickHouse config/contract 校验并输出 secret-safe ingestion plan，覆盖 bounded queue、backpressure、dedup keys、request_logs/provider_attempts/event_log table mapping、payload policy 和 credential presence；本切片已补 durable queue/WAL contract，输出 WAL directory/segment/record shape、bounded disk budget、enqueue/dequeue/ack/retry idempotency、retention/load safety 和 dedup journal linkage。默认不读写 DB、不写队列、不创建目录/文件、不连接 ClickHouse、不发网络请求，`--execute`/`--send` 需 `--force` 且当前切片仍明确拒绝。真实 ClickHouse writer、DB changefeed/export cursor、durable WAL writer、dedup journal runtime persistence、load/retention smoke 尚未完成。

- [~] **E15-006 MCP Gateway**  
  优先级：P2

- [~] **E15-007 Prompt Registry / Eval Dataset / Shadow Traffic**  
  优先级：P2
  当前 Worker 已新增 `ai-worker prompt-eval-shadow --dry-run` plan-only 切片，输出 prompt registry、eval dataset、payload policy 与 shadow traffic 的 hash/count/label/flag 合约，不读写 DB/对象存储、不调用 Gateway、不发网络请求；Control Plane 已新增 `POST /admin/prompt-eval-shadow/dry-run` validate-only API，归 `ProviderManage`，OpenAPI 与 fixture 覆盖 secret-safe envelope、RBAC 和无副作用 flags。真实 registry/dataset 持久化、UI 配置、Gateway shadow 执行和评测结果写入仍待完成。

---

## P0 里程碑建议

| 里程碑 | 目标 | 完成条件 |
|---|---|---|
| M0 | 工程骨架 | local compose + CI + mock provider |
| M1 | 基础转发 | OpenAI chat non-stream/stream 通过 |
| M2 | 模型和渠道 | Profile + Model Association + Channel Mapping |
| M3 | 路由和流式稳定 | fallback、health、stream engine 通过核心测试 |
| M4 | 账务和观测 | ledger、trace、metrics、request detail |
| M5 | 管理后台 | P0 UI 完成，配置 dry-run |
| M6 | 迁移和验收 | New API/One API importer + P0 验收测试 |
| M7 | Staging RC | load/chaos/security/release checklist 通过 |
